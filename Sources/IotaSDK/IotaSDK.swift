// This file was autogenerated by some hot garbage in the `uniffi` crate.
// Trust me, you don't want to mess with it!

// swiftlint:disable all
import Foundation

// Depending on the consumer's build setup, the low-level FFI code
// might be in a separate module, or it might be compiled inline into
// this module. This is a bit of light hackery to work with both.
#if canImport(CIotaSDK)
import CIotaSDK
#endif

fileprivate extension RustBuffer {
    // Allocate a new buffer, copying the contents of a `UInt8` array.
    init(bytes: [UInt8]) {
        let rbuf = bytes.withUnsafeBufferPointer { ptr in
            RustBuffer.from(ptr)
        }
        self.init(capacity: rbuf.capacity, len: rbuf.len, data: rbuf.data)
    }

    static func empty() -> RustBuffer {
        RustBuffer(capacity: 0, len:0, data: nil)
    }

    static func from(_ ptr: UnsafeBufferPointer<UInt8>) -> RustBuffer {
        try! rustCall { ffi_iota_sdk_ffi_rustbuffer_from_bytes(ForeignBytes(bufferPointer: ptr), $0) }
    }

    // Frees the buffer in place.
    // The buffer must not be used after this is called.
    func deallocate() {
        try! rustCall { ffi_iota_sdk_ffi_rustbuffer_free(self, $0) }
    }
}

fileprivate extension ForeignBytes {
    init(bufferPointer: UnsafeBufferPointer<UInt8>) {
        self.init(len: Int32(bufferPointer.count), data: bufferPointer.baseAddress)
    }
}

// For every type used in the interface, we provide helper methods for conveniently
// lifting and lowering that type from C-compatible data, and for reading and writing
// values of that type in a buffer.

// Helper classes/extensions that don't change.
// Someday, this will be in a library of its own.

fileprivate extension Data {
    init(rustBuffer: RustBuffer) {
        self.init(
            bytesNoCopy: rustBuffer.data!,
            count: Int(rustBuffer.len),
            deallocator: .none
        )
    }
}

// Define reader functionality.  Normally this would be defined in a class or
// struct, but we use standalone functions instead in order to make external
// types work.
//
// With external types, one swift source file needs to be able to call the read
// method on another source file's FfiConverter, but then what visibility
// should Reader have?
// - If Reader is fileprivate, then this means the read() must also
//   be fileprivate, which doesn't work with external types.
// - If Reader is internal/public, we'll get compile errors since both source
//   files will try define the same type.
//
// Instead, the read() method and these helper functions input a tuple of data

fileprivate func createReader(data: Data) -> (data: Data, offset: Data.Index) {
    (data: data, offset: 0)
}

// Reads an integer at the current offset, in big-endian order, and advances
// the offset on success. Throws if reading the integer would move the
// offset past the end of the buffer.
fileprivate func readInt<T: FixedWidthInteger>(_ reader: inout (data: Data, offset: Data.Index)) throws -> T {
    let range = reader.offset..<reader.offset + MemoryLayout<T>.size
    guard reader.data.count >= range.upperBound else {
        throw UniffiInternalError.bufferOverflow
    }
    if T.self == UInt8.self {
        let value = reader.data[reader.offset]
        reader.offset += 1
        return value as! T
    }
    var value: T = 0
    let _ = withUnsafeMutableBytes(of: &value, { reader.data.copyBytes(to: $0, from: range)})
    reader.offset = range.upperBound
    return value.bigEndian
}

// Reads an arbitrary number of bytes, to be used to read
// raw bytes, this is useful when lifting strings
fileprivate func readBytes(_ reader: inout (data: Data, offset: Data.Index), count: Int) throws -> Array<UInt8> {
    let range = reader.offset..<(reader.offset+count)
    guard reader.data.count >= range.upperBound else {
        throw UniffiInternalError.bufferOverflow
    }
    var value = [UInt8](repeating: 0, count: count)
    value.withUnsafeMutableBufferPointer({ buffer in
        reader.data.copyBytes(to: buffer, from: range)
    })
    reader.offset = range.upperBound
    return value
}

// Reads a float at the current offset.
fileprivate func readFloat(_ reader: inout (data: Data, offset: Data.Index)) throws -> Float {
    return Float(bitPattern: try readInt(&reader))
}

// Reads a float at the current offset.
fileprivate func readDouble(_ reader: inout (data: Data, offset: Data.Index)) throws -> Double {
    return Double(bitPattern: try readInt(&reader))
}

// Indicates if the offset has reached the end of the buffer.
fileprivate func hasRemaining(_ reader: (data: Data, offset: Data.Index)) -> Bool {
    return reader.offset < reader.data.count
}

// Define writer functionality.  Normally this would be defined in a class or
// struct, but we use standalone functions instead in order to make external
// types work.  See the above discussion on Readers for details.

fileprivate func createWriter() -> [UInt8] {
    return []
}

fileprivate func writeBytes<S>(_ writer: inout [UInt8], _ byteArr: S) where S: Sequence, S.Element == UInt8 {
    writer.append(contentsOf: byteArr)
}

// Writes an integer in big-endian order.
//
// Warning: make sure what you are trying to write
// is in the correct type!
fileprivate func writeInt<T: FixedWidthInteger>(_ writer: inout [UInt8], _ value: T) {
    var value = value.bigEndian
    withUnsafeBytes(of: &value) { writer.append(contentsOf: $0) }
}

fileprivate func writeFloat(_ writer: inout [UInt8], _ value: Float) {
    writeInt(&writer, value.bitPattern)
}

fileprivate func writeDouble(_ writer: inout [UInt8], _ value: Double) {
    writeInt(&writer, value.bitPattern)
}

// Protocol for types that transfer other types across the FFI. This is
// analogous to the Rust trait of the same name.
fileprivate protocol FfiConverter {
    associatedtype FfiType
    associatedtype SwiftType

    static func lift(_ value: FfiType) throws -> SwiftType
    static func lower(_ value: SwiftType) -> FfiType
    static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType
    static func write(_ value: SwiftType, into buf: inout [UInt8])
}

// Types conforming to `Primitive` pass themselves directly over the FFI.
fileprivate protocol FfiConverterPrimitive: FfiConverter where FfiType == SwiftType { }

extension FfiConverterPrimitive {
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func lift(_ value: FfiType) throws -> SwiftType {
        return value
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func lower(_ value: SwiftType) -> FfiType {
        return value
    }
}

// Types conforming to `FfiConverterRustBuffer` lift and lower into a `RustBuffer`.
// Used for complex types where it's hard to write a custom lift/lower.
fileprivate protocol FfiConverterRustBuffer: FfiConverter where FfiType == RustBuffer {}

extension FfiConverterRustBuffer {
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func lift(_ buf: RustBuffer) throws -> SwiftType {
        var reader = createReader(data: Data(rustBuffer: buf))
        let value = try read(from: &reader)
        if hasRemaining(reader) {
            throw UniffiInternalError.incompleteData
        }
        buf.deallocate()
        return value
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func lower(_ value: SwiftType) -> RustBuffer {
          var writer = createWriter()
          write(value, into: &writer)
          return RustBuffer(bytes: writer)
    }
}
// An error type for FFI errors. These errors occur at the UniFFI level, not
// the library level.
fileprivate enum UniffiInternalError: LocalizedError {
    case bufferOverflow
    case incompleteData
    case unexpectedOptionalTag
    case unexpectedEnumCase
    case unexpectedNullPointer
    case unexpectedRustCallStatusCode
    case unexpectedRustCallError
    case unexpectedStaleHandle
    case rustPanic(_ message: String)

    public var errorDescription: String? {
        switch self {
        case .bufferOverflow: return "Reading the requested value would read past the end of the buffer"
        case .incompleteData: return "The buffer still has data after lifting its containing value"
        case .unexpectedOptionalTag: return "Unexpected optional tag; should be 0 or 1"
        case .unexpectedEnumCase: return "Raw enum value doesn't match any cases"
        case .unexpectedNullPointer: return "Raw pointer value was null"
        case .unexpectedRustCallStatusCode: return "Unexpected RustCallStatus code"
        case .unexpectedRustCallError: return "CALL_ERROR but no errorClass specified"
        case .unexpectedStaleHandle: return "The object in the handle map has been dropped already"
        case let .rustPanic(message): return message
        }
    }
}

fileprivate extension NSLock {
    func withLock<T>(f: () throws -> T) rethrows -> T {
        self.lock()
        defer { self.unlock() }
        return try f()
    }
}

fileprivate let CALL_SUCCESS: Int8 = 0
fileprivate let CALL_ERROR: Int8 = 1
fileprivate let CALL_UNEXPECTED_ERROR: Int8 = 2
fileprivate let CALL_CANCELLED: Int8 = 3

fileprivate extension RustCallStatus {
    init() {
        self.init(
            code: CALL_SUCCESS,
            errorBuf: RustBuffer.init(
                capacity: 0,
                len: 0,
                data: nil
            )
        )
    }
}

private func rustCall<T>(_ callback: (UnsafeMutablePointer<RustCallStatus>) -> T) throws -> T {
    let neverThrow: ((RustBuffer) throws -> Never)? = nil
    return try makeRustCall(callback, errorHandler: neverThrow)
}

private func rustCallWithError<T, E: Swift.Error>(
    _ errorHandler: @escaping (RustBuffer) throws -> E,
    _ callback: (UnsafeMutablePointer<RustCallStatus>) -> T) throws -> T {
    try makeRustCall(callback, errorHandler: errorHandler)
}

private func makeRustCall<T, E: Swift.Error>(
    _ callback: (UnsafeMutablePointer<RustCallStatus>) -> T,
    errorHandler: ((RustBuffer) throws -> E)?
) throws -> T {
    uniffiEnsureIotaSdkFfiInitialized()
    var callStatus = RustCallStatus.init()
    let returnedVal = callback(&callStatus)
    try uniffiCheckCallStatus(callStatus: callStatus, errorHandler: errorHandler)
    return returnedVal
}

private func uniffiCheckCallStatus<E: Swift.Error>(
    callStatus: RustCallStatus,
    errorHandler: ((RustBuffer) throws -> E)?
) throws {
    switch callStatus.code {
        case CALL_SUCCESS:
            return

        case CALL_ERROR:
            if let errorHandler = errorHandler {
                throw try errorHandler(callStatus.errorBuf)
            } else {
                callStatus.errorBuf.deallocate()
                throw UniffiInternalError.unexpectedRustCallError
            }

        case CALL_UNEXPECTED_ERROR:
            // When the rust code sees a panic, it tries to construct a RustBuffer
            // with the message.  But if that code panics, then it just sends back
            // an empty buffer.
            if callStatus.errorBuf.len > 0 {
                throw UniffiInternalError.rustPanic(try FfiConverterString.lift(callStatus.errorBuf))
            } else {
                callStatus.errorBuf.deallocate()
                throw UniffiInternalError.rustPanic("Rust panic")
            }

        case CALL_CANCELLED:
            fatalError("Cancellation not supported yet")

        default:
            throw UniffiInternalError.unexpectedRustCallStatusCode
    }
}

private func uniffiTraitInterfaceCall<T>(
    callStatus: UnsafeMutablePointer<RustCallStatus>,
    makeCall: () throws -> T,
    writeReturn: (T) -> ()
) {
    do {
        try writeReturn(makeCall())
    } catch let error {
        callStatus.pointee.code = CALL_UNEXPECTED_ERROR
        callStatus.pointee.errorBuf = FfiConverterString.lower(String(describing: error))
    }
}

private func uniffiTraitInterfaceCallWithError<T, E>(
    callStatus: UnsafeMutablePointer<RustCallStatus>,
    makeCall: () throws -> T,
    writeReturn: (T) -> (),
    lowerError: (E) -> RustBuffer
) {
    do {
        try writeReturn(makeCall())
    } catch let error as E {
        callStatus.pointee.code = CALL_ERROR
        callStatus.pointee.errorBuf = lowerError(error)
    } catch {
        callStatus.pointee.code = CALL_UNEXPECTED_ERROR
        callStatus.pointee.errorBuf = FfiConverterString.lower(String(describing: error))
    }
}
fileprivate final class UniffiHandleMap<T>: @unchecked Sendable {
    // All mutation happens with this lock held, which is why we implement @unchecked Sendable.
    private let lock = NSLock()
    private var map: [UInt64: T] = [:]
    private var currentHandle: UInt64 = 1

    func insert(obj: T) -> UInt64 {
        lock.withLock {
            let handle = currentHandle
            currentHandle += 1
            map[handle] = obj
            return handle
        }
    }

     func get(handle: UInt64) throws -> T {
        try lock.withLock {
            guard let obj = map[handle] else {
                throw UniffiInternalError.unexpectedStaleHandle
            }
            return obj
        }
    }

    @discardableResult
    func remove(handle: UInt64) throws -> T {
        try lock.withLock {
            guard let obj = map.removeValue(forKey: handle) else {
                throw UniffiInternalError.unexpectedStaleHandle
            }
            return obj
        }
    }

    var count: Int {
        get {
            map.count
        }
    }
}


// Public interface members begin here.
// Magic number for the Rust proxy to call using the same mechanism as every other method,
// to free the callback once it's dropped by Rust.
private let IDX_CALLBACK_FREE: Int32 = 0
// Callback return codes
private let UNIFFI_CALLBACK_SUCCESS: Int32 = 0
private let UNIFFI_CALLBACK_ERROR: Int32 = 1
private let UNIFFI_CALLBACK_UNEXPECTED_ERROR: Int32 = 2

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterUInt8: FfiConverterPrimitive {
    typealias FfiType = UInt8
    typealias SwiftType = UInt8

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> UInt8 {
        return try lift(readInt(&buf))
    }

    public static func write(_ value: UInt8, into buf: inout [UInt8]) {
        writeInt(&buf, lower(value))
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterInt8: FfiConverterPrimitive {
    typealias FfiType = Int8
    typealias SwiftType = Int8

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Int8 {
        return try lift(readInt(&buf))
    }

    public static func write(_ value: Int8, into buf: inout [UInt8]) {
        writeInt(&buf, lower(value))
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterUInt16: FfiConverterPrimitive {
    typealias FfiType = UInt16
    typealias SwiftType = UInt16

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> UInt16 {
        return try lift(readInt(&buf))
    }

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        writeInt(&buf, lower(value))
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterInt16: FfiConverterPrimitive {
    typealias FfiType = Int16
    typealias SwiftType = Int16

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Int16 {
        return try lift(readInt(&buf))
    }

    public static func write(_ value: Int16, into buf: inout [UInt8]) {
        writeInt(&buf, lower(value))
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterUInt32: FfiConverterPrimitive {
    typealias FfiType = UInt32
    typealias SwiftType = UInt32

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> UInt32 {
        return try lift(readInt(&buf))
    }

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        writeInt(&buf, lower(value))
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterInt32: FfiConverterPrimitive {
    typealias FfiType = Int32
    typealias SwiftType = Int32

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Int32 {
        return try lift(readInt(&buf))
    }

    public static func write(_ value: Int32, into buf: inout [UInt8]) {
        writeInt(&buf, lower(value))
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterUInt64: FfiConverterPrimitive {
    typealias FfiType = UInt64
    typealias SwiftType = UInt64

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> UInt64 {
        return try lift(readInt(&buf))
    }

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        writeInt(&buf, lower(value))
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterInt64: FfiConverterPrimitive {
    typealias FfiType = Int64
    typealias SwiftType = Int64

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Int64 {
        return try lift(readInt(&buf))
    }

    public static func write(_ value: Int64, into buf: inout [UInt8]) {
        writeInt(&buf, lower(value))
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterBool : FfiConverter {
    typealias FfiType = Int8
    typealias SwiftType = Bool

    public static func lift(_ value: Int8) throws -> Bool {
        return value != 0
    }

    public static func lower(_ value: Bool) -> Int8 {
        return value ? 1 : 0
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Bool {
        return try lift(readInt(&buf))
    }

    public static func write(_ value: Bool, into buf: inout [UInt8]) {
        writeInt(&buf, lower(value))
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterString: FfiConverter {
    typealias SwiftType = String
    typealias FfiType = RustBuffer

    public static func lift(_ value: RustBuffer) throws -> String {
        defer {
            value.deallocate()
        }
        if value.data == nil {
            return String()
        }
        let bytes = UnsafeBufferPointer<UInt8>(start: value.data!, count: Int(value.len))
        return String(bytes: bytes, encoding: String.Encoding.utf8)!
    }

    public static func lower(_ value: String) -> RustBuffer {
        return value.utf8CString.withUnsafeBufferPointer { ptr in
            // The swift string gives us int8_t, we want uint8_t.
            ptr.withMemoryRebound(to: UInt8.self) { ptr in
                // The swift string gives us a trailing null byte, we don't want it.
                let buf = UnsafeBufferPointer(rebasing: ptr.prefix(upTo: ptr.count - 1))
                return RustBuffer.from(buf)
            }
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> String {
        let len: Int32 = try readInt(&buf)
        return String(bytes: try readBytes(&buf, count: Int(len)), encoding: String.Encoding.utf8)!
    }

    public static func write(_ value: String, into buf: inout [UInt8]) {
        let len = Int32(value.utf8.count)
        writeInt(&buf, len)
        writeBytes(&buf, value.utf8)
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterData: FfiConverterRustBuffer {
    typealias SwiftType = Data

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Data {
        let len: Int32 = try readInt(&buf)
        return Data(try readBytes(&buf, count: Int(len)))
    }

    public static func write(_ value: Data, into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        writeBytes(&buf, value)
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterDuration: FfiConverterRustBuffer {
    typealias SwiftType = TimeInterval

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> TimeInterval {
        let seconds: UInt64 = try readInt(&buf)
        let nanoseconds: UInt32 = try readInt(&buf)
        return Double(seconds) + (Double(nanoseconds) / 1.0e9)
    }

    public static func write(_ value: TimeInterval, into buf: inout [UInt8]) {
        if value.rounded(.down) > Double(Int64.max) {
            fatalError("Duration overflow, exceeds max bounds supported by Uniffi")
        }

        if value < 0 {
            fatalError("Invalid duration, must be non-negative")
        }

        let seconds = UInt64(value)
        let nanoseconds = UInt32((value - Double(seconds)) * 1.0e9)
        writeInt(&buf, seconds)
        writeInt(&buf, nanoseconds)
    }
}




/**
 * Unique identifier for an Account on the IOTA blockchain.
 *
 * An `Address` is a 32-byte pseudonymous identifier used to uniquely identify
 * an account and asset-ownership on the IOTA blockchain. Often, human-readable
 * addresses are encoded in hexadecimal with a `0x` prefix. For example, this
 * is a valid IOTA address:
 * `0x02a212de6a9dfa3a69e22387acfbafbb1a9e591bd9d636e7895dcfc8de05f331`.
 *
 * # Deriving an Address
 *
 * Addresses are cryptographically derived from a number of user account
 * authenticators, the simplest of which is an
 * `Ed25519PublicKey`.
 *
 * Deriving an address consists of the Blake2b256 hash of the sequence of bytes
 * of its corresponding authenticator, prefixed with a domain-separator (except
 * ed25519, for compatibility reasons). For each other authenticator, this
 * domain-separator is the single byte-value of its
 * `SignatureScheme` flag. E.g. `hash(signature schema flag || authenticator
 * bytes)`.
 *
 * Each authenticator has a method for deriving its `Address` as well as
 * documentation for the specifics of how the derivation is done. See
 * `Ed25519PublicKey::derive_address` for an example.
 *
 * ## Relationship to ObjectIds
 *
 * `ObjectId`s and `Address`es share the same 32-byte addressable space but
 * are derived leveraging different domain-separator values to ensure that,
 * cryptographically, there won't be any overlap, e.g. there can't be a
 * valid `Object` who's `ObjectId` is equal to that of the `Address` of a user
 * account.
 *
 * # BCS
 *
 * An `Address`'s BCS serialized form is defined by the following:
 *
 * ```text
 * address = 32OCTET
 * ```
 */
public protocol AddressProtocol: AnyObject, Sendable {
    
    func toBytes()  -> Data
    
    /**
     * Returns the string representation of this address using the
     * canonical display, with or without a `0x` prefix.
     */
    func toCanonicalString(withPrefix: Bool)  -> String
    
    func toHex()  -> String
    
    /**
     * Returns the shortest possible string representation of the address (i.e.
     * with leading zeroes trimmed).
     */
    func toShortString(withPrefix: Bool)  -> String
    
}
/**
 * Unique identifier for an Account on the IOTA blockchain.
 *
 * An `Address` is a 32-byte pseudonymous identifier used to uniquely identify
 * an account and asset-ownership on the IOTA blockchain. Often, human-readable
 * addresses are encoded in hexadecimal with a `0x` prefix. For example, this
 * is a valid IOTA address:
 * `0x02a212de6a9dfa3a69e22387acfbafbb1a9e591bd9d636e7895dcfc8de05f331`.
 *
 * # Deriving an Address
 *
 * Addresses are cryptographically derived from a number of user account
 * authenticators, the simplest of which is an
 * `Ed25519PublicKey`.
 *
 * Deriving an address consists of the Blake2b256 hash of the sequence of bytes
 * of its corresponding authenticator, prefixed with a domain-separator (except
 * ed25519, for compatibility reasons). For each other authenticator, this
 * domain-separator is the single byte-value of its
 * `SignatureScheme` flag. E.g. `hash(signature schema flag || authenticator
 * bytes)`.
 *
 * Each authenticator has a method for deriving its `Address` as well as
 * documentation for the specifics of how the derivation is done. See
 * `Ed25519PublicKey::derive_address` for an example.
 *
 * ## Relationship to ObjectIds
 *
 * `ObjectId`s and `Address`es share the same 32-byte addressable space but
 * are derived leveraging different domain-separator values to ensure that,
 * cryptographically, there won't be any overlap, e.g. there can't be a
 * valid `Object` who's `ObjectId` is equal to that of the `Address` of a user
 * account.
 *
 * # BCS
 *
 * An `Address`'s BCS serialized form is defined by the following:
 *
 * ```text
 * address = 32OCTET
 * ```
 */
open class Address: AddressProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_address(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_address(pointer, $0) }
    }

    
public static func framework() -> Address  {
    return try!  FfiConverterTypeAddress_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_address_framework($0
    )
})
}
    
public static func fromBytes(bytes: Data)throws  -> Address  {
    return try  FfiConverterTypeAddress_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_address_from_bytes(
        FfiConverterData.lower(bytes),$0
    )
})
}
    
    /**
     * Parses an Address from a hex string, with or without a `0x` prefix.
     * The string can be of variable length; if it's shorter than 64 hex
     * characters, it will be left-padded with `0`s.
     */
public static func fromHex(hex: String)throws  -> Address  {
    return try  FfiConverterTypeAddress_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_address_from_hex(
        FfiConverterString.lower(hex),$0
    )
})
}
    
public static func generate() -> Address  {
    return try!  FfiConverterTypeAddress_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_address_generate($0
    )
})
}
    
public static func std() -> Address  {
    return try!  FfiConverterTypeAddress_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_address_std($0
    )
})
}
    
public static func system() -> Address  {
    return try!  FfiConverterTypeAddress_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_address_system($0
    )
})
}
    
public static func zero() -> Address  {
    return try!  FfiConverterTypeAddress_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_address_zero($0
    )
})
}
    

    
open func toBytes() -> Data  {
    return try!  FfiConverterData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_address_to_bytes(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Returns the string representation of this address using the
     * canonical display, with or without a `0x` prefix.
     */
open func toCanonicalString(withPrefix: Bool) -> String  {
    return try!  FfiConverterString.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_address_to_canonical_string(self.uniffiClonePointer(),
        FfiConverterBool.lower(withPrefix),$0
    )
})
}
    
open func toHex() -> String  {
    return try!  FfiConverterString.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_address_to_hex(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Returns the shortest possible string representation of the address (i.e.
     * with leading zeroes trimmed).
     */
open func toShortString(withPrefix: Bool) -> String  {
    return try!  FfiConverterString.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_address_to_short_string(self.uniffiClonePointer(),
        FfiConverterBool.lower(withPrefix),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_address_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    open var description: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_address_uniffi_trait_display(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: Address, other: Address) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_address_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeAddress_lower(other),$0
    )
}
        )
    }
    open func hash(into hasher: inout Hasher) {
        let val = try!  FfiConverterUInt64.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_address_uniffi_trait_hash(self.uniffiClonePointer(),$0
    )
}
        )
        hasher.combine(val)
    }

}
extension Address: CustomDebugStringConvertible {}
extension Address: CustomStringConvertible {}
extension Address: Equatable {}
extension Address: Hashable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeAddress: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = Address

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> Address {
        return Address(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: Address) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Address {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: Address, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeAddress_lift(_ pointer: UnsafeMutableRawPointer) throws -> Address {
    return try FfiConverterTypeAddress.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeAddress_lower(_ value: Address) -> UnsafeMutableRawPointer {
    return FfiConverterTypeAddress.lower(value)
}






/**
 * An argument to a programmable transaction command
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * argument    =  argument-gas
 * =/ argument-input
 * =/ argument-result
 * =/ argument-nested-result
 *
 * argument-gas            = %x00
 * argument-input          = %x01 u16
 * argument-result         = %x02 u16
 * argument-nested-result  = %x03 u16 u16
 * ```
 */
public protocol ArgumentProtocol: AnyObject, Sendable {
    
    /**
     * Get the nested result for this result at the given index. Returns None
     * if this is not a Result.
     */
    func getNestedResult(ix: UInt16)  -> Argument?
    
}
/**
 * An argument to a programmable transaction command
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * argument    =  argument-gas
 * =/ argument-input
 * =/ argument-result
 * =/ argument-nested-result
 *
 * argument-gas            = %x00
 * argument-input          = %x01 u16
 * argument-result         = %x02 u16
 * argument-nested-result  = %x03 u16 u16
 * ```
 */
open class Argument: ArgumentProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_argument(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_argument(pointer, $0) }
    }

    
    /**
     * The gas coin. The gas coin can only be used by-ref, except for with
     * `TransferObjects`, which can use it by-value.
     */
public static func newGas() -> Argument  {
    return try!  FfiConverterTypeArgument_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_argument_new_gas($0
    )
})
}
    
    /**
     * One of the input objects or primitive values (from
     * `ProgrammableTransaction` inputs)
     */
public static func newInput(input: UInt16) -> Argument  {
    return try!  FfiConverterTypeArgument_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_argument_new_input(
        FfiConverterUInt16.lower(input),$0
    )
})
}
    
    /**
     * Like a `Result` but it accesses a nested result. Currently, the only
     * usage of this is to access a value from a Move call with multiple
     * return values.
     */
public static func newNestedResult(commandIndex: UInt16, subresultIndex: UInt16) -> Argument  {
    return try!  FfiConverterTypeArgument_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_argument_new_nested_result(
        FfiConverterUInt16.lower(commandIndex),
        FfiConverterUInt16.lower(subresultIndex),$0
    )
})
}
    
    /**
     * The result of another command (from `ProgrammableTransaction` commands)
     */
public static func newResult(result: UInt16) -> Argument  {
    return try!  FfiConverterTypeArgument_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_argument_new_result(
        FfiConverterUInt16.lower(result),$0
    )
})
}
    

    
    /**
     * Get the nested result for this result at the given index. Returns None
     * if this is not a Result.
     */
open func getNestedResult(ix: UInt16) -> Argument?  {
    return try!  FfiConverterOptionTypeArgument.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_argument_get_nested_result(self.uniffiClonePointer(),
        FfiConverterUInt16.lower(ix),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_argument_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: Argument, other: Argument) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_argument_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeArgument_lower(other),$0
    )
}
        )
    }

}
extension Argument: CustomDebugStringConvertible {}
extension Argument: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeArgument: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = Argument

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> Argument {
        return Argument(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: Argument) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Argument {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: Argument, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeArgument_lift(_ pointer: UnsafeMutableRawPointer) throws -> Argument {
    return try FfiConverterTypeArgument.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeArgument_lower(_ value: Argument) -> UnsafeMutableRawPointer {
    return FfiConverterTypeArgument.lower(value)
}






public protocol Bls12381PrivateKeyProtocol: AnyObject, Sendable {
    
    func publicKey()  -> Bls12381PublicKey
    
    func scheme()  -> SignatureScheme
    
    func signCheckpointSummary(summary: CheckpointSummary)  -> ValidatorSignature
    
    func trySign(message: Data) throws  -> Bls12381Signature
    
    func verifyingKey()  -> Bls12381VerifyingKey
    
}
open class Bls12381PrivateKey: Bls12381PrivateKeyProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_bls12381privatekey(self.pointer, $0) }
    }
public convenience init(bytes: Data)throws  {
    let pointer =
        try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_bls12381privatekey_new(
        FfiConverterData.lower(bytes),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_bls12381privatekey(pointer, $0) }
    }

    
public static func generate() -> Bls12381PrivateKey  {
    return try!  FfiConverterTypeBls12381PrivateKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_bls12381privatekey_generate($0
    )
})
}
    

    
open func publicKey() -> Bls12381PublicKey  {
    return try!  FfiConverterTypeBls12381PublicKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_bls12381privatekey_public_key(self.uniffiClonePointer(),$0
    )
})
}
    
open func scheme() -> SignatureScheme  {
    return try!  FfiConverterTypeSignatureScheme_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_bls12381privatekey_scheme(self.uniffiClonePointer(),$0
    )
})
}
    
open func signCheckpointSummary(summary: CheckpointSummary) -> ValidatorSignature  {
    return try!  FfiConverterTypeValidatorSignature_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_bls12381privatekey_sign_checkpoint_summary(self.uniffiClonePointer(),
        FfiConverterTypeCheckpointSummary_lower(summary),$0
    )
})
}
    
open func trySign(message: Data)throws  -> Bls12381Signature  {
    return try  FfiConverterTypeBls12381Signature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_bls12381privatekey_try_sign(self.uniffiClonePointer(),
        FfiConverterData.lower(message),$0
    )
})
}
    
open func verifyingKey() -> Bls12381VerifyingKey  {
    return try!  FfiConverterTypeBls12381VerifyingKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_bls12381privatekey_verifying_key(self.uniffiClonePointer(),$0
    )
})
}
    

}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeBls12381PrivateKey: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = Bls12381PrivateKey

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> Bls12381PrivateKey {
        return Bls12381PrivateKey(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: Bls12381PrivateKey) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Bls12381PrivateKey {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: Bls12381PrivateKey, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeBls12381PrivateKey_lift(_ pointer: UnsafeMutableRawPointer) throws -> Bls12381PrivateKey {
    return try FfiConverterTypeBls12381PrivateKey.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeBls12381PrivateKey_lower(_ value: Bls12381PrivateKey) -> UnsafeMutableRawPointer {
    return FfiConverterTypeBls12381PrivateKey.lower(value)
}






/**
 * A bls12381 min-sig public key.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * bls-public-key = %x60 96OCTET
 * ```
 *
 * Due to historical reasons, even though a min-sig `Bls12381PublicKey` has a
 * fixed-length of 96, IOTA's binary representation of a min-sig
 * `Bls12381PublicKey` is prefixed with its length meaning its serialized
 * binary form (in bcs) is 97 bytes long vs a more compact 96 bytes.
 */
public protocol Bls12381PublicKeyProtocol: AnyObject, Sendable {
    
    func toBytes()  -> Data
    
}
/**
 * A bls12381 min-sig public key.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * bls-public-key = %x60 96OCTET
 * ```
 *
 * Due to historical reasons, even though a min-sig `Bls12381PublicKey` has a
 * fixed-length of 96, IOTA's binary representation of a min-sig
 * `Bls12381PublicKey` is prefixed with its length meaning its serialized
 * binary form (in bcs) is 97 bytes long vs a more compact 96 bytes.
 */
open class Bls12381PublicKey: Bls12381PublicKeyProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_bls12381publickey(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_bls12381publickey(pointer, $0) }
    }

    
public static func fromBytes(bytes: Data)throws  -> Bls12381PublicKey  {
    return try  FfiConverterTypeBls12381PublicKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_bls12381publickey_from_bytes(
        FfiConverterData.lower(bytes),$0
    )
})
}
    
public static func fromStr(s: String)throws  -> Bls12381PublicKey  {
    return try  FfiConverterTypeBls12381PublicKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_bls12381publickey_from_str(
        FfiConverterString.lower(s),$0
    )
})
}
    
public static func generate() -> Bls12381PublicKey  {
    return try!  FfiConverterTypeBls12381PublicKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_bls12381publickey_generate($0
    )
})
}
    

    
open func toBytes() -> Data  {
    return try!  FfiConverterData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_bls12381publickey_to_bytes(self.uniffiClonePointer(),$0
    )
})
}
    
    public static func == (self: Bls12381PublicKey, other: Bls12381PublicKey) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_bls12381publickey_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeBls12381PublicKey_lower(other),$0
    )
}
        )
    }
    open func hash(into hasher: inout Hasher) {
        let val = try!  FfiConverterUInt64.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_bls12381publickey_uniffi_trait_hash(self.uniffiClonePointer(),$0
    )
}
        )
        hasher.combine(val)
    }

}
extension Bls12381PublicKey: Equatable {}
extension Bls12381PublicKey: Hashable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeBls12381PublicKey: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = Bls12381PublicKey

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> Bls12381PublicKey {
        return Bls12381PublicKey(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: Bls12381PublicKey) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Bls12381PublicKey {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: Bls12381PublicKey, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeBls12381PublicKey_lift(_ pointer: UnsafeMutableRawPointer) throws -> Bls12381PublicKey {
    return try FfiConverterTypeBls12381PublicKey.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeBls12381PublicKey_lower(_ value: Bls12381PublicKey) -> UnsafeMutableRawPointer {
    return FfiConverterTypeBls12381PublicKey.lower(value)
}






/**
 * A bls12381 min-sig public key.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * bls-public-key = %x60 96OCTET
 * ```
 *
 * Due to historical reasons, even though a min-sig `Bls12381PublicKey` has a
 * fixed-length of 96, IOTA's binary representation of a min-sig
 * `Bls12381PublicKey` is prefixed with its length meaning its serialized
 * binary form (in bcs) is 97 bytes long vs a more compact 96 bytes.
 */
public protocol Bls12381SignatureProtocol: AnyObject, Sendable {
    
    func toBytes()  -> Data
    
}
/**
 * A bls12381 min-sig public key.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * bls-public-key = %x60 96OCTET
 * ```
 *
 * Due to historical reasons, even though a min-sig `Bls12381PublicKey` has a
 * fixed-length of 96, IOTA's binary representation of a min-sig
 * `Bls12381PublicKey` is prefixed with its length meaning its serialized
 * binary form (in bcs) is 97 bytes long vs a more compact 96 bytes.
 */
open class Bls12381Signature: Bls12381SignatureProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_bls12381signature(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_bls12381signature(pointer, $0) }
    }

    
public static func fromBytes(bytes: Data)throws  -> Bls12381Signature  {
    return try  FfiConverterTypeBls12381Signature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_bls12381signature_from_bytes(
        FfiConverterData.lower(bytes),$0
    )
})
}
    
public static func fromStr(s: String)throws  -> Bls12381Signature  {
    return try  FfiConverterTypeBls12381Signature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_bls12381signature_from_str(
        FfiConverterString.lower(s),$0
    )
})
}
    
public static func generate() -> Bls12381Signature  {
    return try!  FfiConverterTypeBls12381Signature_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_bls12381signature_generate($0
    )
})
}
    

    
open func toBytes() -> Data  {
    return try!  FfiConverterData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_bls12381signature_to_bytes(self.uniffiClonePointer(),$0
    )
})
}
    
    public static func == (self: Bls12381Signature, other: Bls12381Signature) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_bls12381signature_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeBls12381Signature_lower(other),$0
    )
}
        )
    }
    open func hash(into hasher: inout Hasher) {
        let val = try!  FfiConverterUInt64.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_bls12381signature_uniffi_trait_hash(self.uniffiClonePointer(),$0
    )
}
        )
        hasher.combine(val)
    }

}
extension Bls12381Signature: Equatable {}
extension Bls12381Signature: Hashable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeBls12381Signature: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = Bls12381Signature

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> Bls12381Signature {
        return Bls12381Signature(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: Bls12381Signature) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Bls12381Signature {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: Bls12381Signature, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeBls12381Signature_lift(_ pointer: UnsafeMutableRawPointer) throws -> Bls12381Signature {
    return try FfiConverterTypeBls12381Signature.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeBls12381Signature_lower(_ value: Bls12381Signature) -> UnsafeMutableRawPointer {
    return FfiConverterTypeBls12381Signature.lower(value)
}






public protocol Bls12381VerifyingKeyProtocol: AnyObject, Sendable {
    
    func publicKey()  -> Bls12381PublicKey
    
    func verify(message: Data, signature: Bls12381Signature) throws 
    
}
open class Bls12381VerifyingKey: Bls12381VerifyingKeyProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_bls12381verifyingkey(self.pointer, $0) }
    }
public convenience init(publicKey: Bls12381PublicKey)throws  {
    let pointer =
        try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_bls12381verifyingkey_new(
        FfiConverterTypeBls12381PublicKey_lower(publicKey),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_bls12381verifyingkey(pointer, $0) }
    }

    

    
open func publicKey() -> Bls12381PublicKey  {
    return try!  FfiConverterTypeBls12381PublicKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_bls12381verifyingkey_public_key(self.uniffiClonePointer(),$0
    )
})
}
    
open func verify(message: Data, signature: Bls12381Signature)throws   {try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_bls12381verifyingkey_verify(self.uniffiClonePointer(),
        FfiConverterData.lower(message),
        FfiConverterTypeBls12381Signature_lower(signature),$0
    )
}
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_bls12381verifyingkey_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }

}
extension Bls12381VerifyingKey: CustomDebugStringConvertible {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeBls12381VerifyingKey: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = Bls12381VerifyingKey

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> Bls12381VerifyingKey {
        return Bls12381VerifyingKey(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: Bls12381VerifyingKey) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Bls12381VerifyingKey {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: Bls12381VerifyingKey, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeBls12381VerifyingKey_lift(_ pointer: UnsafeMutableRawPointer) throws -> Bls12381VerifyingKey {
    return try FfiConverterTypeBls12381VerifyingKey.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeBls12381VerifyingKey_lower(_ value: Bls12381VerifyingKey) -> UnsafeMutableRawPointer {
    return FfiConverterTypeBls12381VerifyingKey.lower(value)
}






/**
 * A point on the BN254 elliptic curve.
 *
 * This is a 32-byte, or 256-bit, value that is generally represented as
 * radix10 when a human-readable display format is needed, and is represented
 * as a 32-byte big-endian value while in memory.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * bn254-field-element = *DIGIT ; which is then interpreted as a radix10 encoded 32-byte value
 * ```
 */
public protocol Bn254FieldElementProtocol: AnyObject, Sendable {
    
    func padded()  -> Data
    
    func unpadded()  -> Data
    
}
/**
 * A point on the BN254 elliptic curve.
 *
 * This is a 32-byte, or 256-bit, value that is generally represented as
 * radix10 when a human-readable display format is needed, and is represented
 * as a 32-byte big-endian value while in memory.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * bn254-field-element = *DIGIT ; which is then interpreted as a radix10 encoded 32-byte value
 * ```
 */
open class Bn254FieldElement: Bn254FieldElementProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_bn254fieldelement(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_bn254fieldelement(pointer, $0) }
    }

    
public static func fromBytes(bytes: Data)throws  -> Bn254FieldElement  {
    return try  FfiConverterTypeBn254FieldElement_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_bn254fieldelement_from_bytes(
        FfiConverterData.lower(bytes),$0
    )
})
}
    
public static func fromStr(s: String)throws  -> Bn254FieldElement  {
    return try  FfiConverterTypeBn254FieldElement_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_bn254fieldelement_from_str(
        FfiConverterString.lower(s),$0
    )
})
}
    
public static func fromStrRadix10(s: String)throws  -> Bn254FieldElement  {
    return try  FfiConverterTypeBn254FieldElement_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_bn254fieldelement_from_str_radix_10(
        FfiConverterString.lower(s),$0
    )
})
}
    

    
open func padded() -> Data  {
    return try!  FfiConverterData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_bn254fieldelement_padded(self.uniffiClonePointer(),$0
    )
})
}
    
open func unpadded() -> Data  {
    return try!  FfiConverterData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_bn254fieldelement_unpadded(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_bn254fieldelement_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: Bn254FieldElement, other: Bn254FieldElement) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_bn254fieldelement_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeBn254FieldElement_lower(other),$0
    )
}
        )
    }

}
extension Bn254FieldElement: CustomDebugStringConvertible {}
extension Bn254FieldElement: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeBn254FieldElement: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = Bn254FieldElement

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> Bn254FieldElement {
        return Bn254FieldElement(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: Bn254FieldElement) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Bn254FieldElement {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: Bn254FieldElement, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeBn254FieldElement_lift(_ pointer: UnsafeMutableRawPointer) throws -> Bn254FieldElement {
    return try FfiConverterTypeBn254FieldElement.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeBn254FieldElement_lower(_ value: Bn254FieldElement) -> UnsafeMutableRawPointer {
    return FfiConverterTypeBn254FieldElement.lower(value)
}






/**
 * A transaction that was cancelled
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * cancelled-transaction = digest (vector version-assignment)
 * ```
 */
public protocol CancelledTransactionProtocol: AnyObject, Sendable {
    
    func digest()  -> Digest
    
    func versionAssignments()  -> [VersionAssignment]
    
}
/**
 * A transaction that was cancelled
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * cancelled-transaction = digest (vector version-assignment)
 * ```
 */
open class CancelledTransaction: CancelledTransactionProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_cancelledtransaction(self.pointer, $0) }
    }
public convenience init(digest: Digest, versionAssignments: [VersionAssignment]) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_cancelledtransaction_new(
        FfiConverterTypeDigest_lower(digest),
        FfiConverterSequenceTypeVersionAssignment.lower(versionAssignments),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_cancelledtransaction(pointer, $0) }
    }

    

    
open func digest() -> Digest  {
    return try!  FfiConverterTypeDigest_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_cancelledtransaction_digest(self.uniffiClonePointer(),$0
    )
})
}
    
open func versionAssignments() -> [VersionAssignment]  {
    return try!  FfiConverterSequenceTypeVersionAssignment.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_cancelledtransaction_version_assignments(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_cancelledtransaction_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: CancelledTransaction, other: CancelledTransaction) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_cancelledtransaction_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeCancelledTransaction_lower(other),$0
    )
}
        )
    }

}
extension CancelledTransaction: CustomDebugStringConvertible {}
extension CancelledTransaction: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeCancelledTransaction: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = CancelledTransaction

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> CancelledTransaction {
        return CancelledTransaction(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: CancelledTransaction) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> CancelledTransaction {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: CancelledTransaction, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeCancelledTransaction_lift(_ pointer: UnsafeMutableRawPointer) throws -> CancelledTransaction {
    return try FfiConverterTypeCancelledTransaction.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeCancelledTransaction_lower(_ value: CancelledTransaction) -> UnsafeMutableRawPointer {
    return FfiConverterTypeCancelledTransaction.lower(value)
}






/**
 * System transaction used to change the epoch
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * change-epoch = u64  ; next epoch
 * u64  ; protocol version
 * u64  ; storage charge
 * u64  ; computation charge
 * u64  ; storage rebate
 * u64  ; non-refundable storage fee
 * u64  ; epoch start timestamp
 * (vector system-package)
 * ```
 */
public protocol ChangeEpochProtocol: AnyObject, Sendable {
    
    /**
     * The total amount of gas charged for computation during the epoch.
     */
    func computationCharge()  -> UInt64
    
    /**
     * The next (to become) epoch ID.
     */
    func epoch()  -> UInt64
    
    /**
     * Unix timestamp when epoch started
     */
    func epochStartTimestampMs()  -> UInt64
    
    /**
     * The non-refundable storage fee.
     */
    func nonRefundableStorageFee()  -> UInt64
    
    /**
     * The protocol version in effect in the new epoch.
     */
    func protocolVersion()  -> UInt64
    
    /**
     * The total amount of gas charged for storage during the epoch.
     */
    func storageCharge()  -> UInt64
    
    /**
     * The amount of storage rebate refunded to the txn senders.
     */
    func storageRebate()  -> UInt64
    
    /**
     * System packages (specifically framework and move stdlib) that are
     * written before the new epoch starts.
     */
    func systemPackages()  -> [SystemPackage]
    
}
/**
 * System transaction used to change the epoch
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * change-epoch = u64  ; next epoch
 * u64  ; protocol version
 * u64  ; storage charge
 * u64  ; computation charge
 * u64  ; storage rebate
 * u64  ; non-refundable storage fee
 * u64  ; epoch start timestamp
 * (vector system-package)
 * ```
 */
open class ChangeEpoch: ChangeEpochProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_changeepoch(self.pointer, $0) }
    }
public convenience init(epoch: UInt64, protocolVersion: UInt64, storageCharge: UInt64, computationCharge: UInt64, storageRebate: UInt64, nonRefundableStorageFee: UInt64, epochStartTimestampMs: UInt64, systemPackages: [SystemPackage]) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_changeepoch_new(
        FfiConverterUInt64.lower(epoch),
        FfiConverterUInt64.lower(protocolVersion),
        FfiConverterUInt64.lower(storageCharge),
        FfiConverterUInt64.lower(computationCharge),
        FfiConverterUInt64.lower(storageRebate),
        FfiConverterUInt64.lower(nonRefundableStorageFee),
        FfiConverterUInt64.lower(epochStartTimestampMs),
        FfiConverterSequenceTypeSystemPackage.lower(systemPackages),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_changeepoch(pointer, $0) }
    }

    

    
    /**
     * The total amount of gas charged for computation during the epoch.
     */
open func computationCharge() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepoch_computation_charge(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The next (to become) epoch ID.
     */
open func epoch() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepoch_epoch(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Unix timestamp when epoch started
     */
open func epochStartTimestampMs() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepoch_epoch_start_timestamp_ms(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The non-refundable storage fee.
     */
open func nonRefundableStorageFee() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepoch_non_refundable_storage_fee(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The protocol version in effect in the new epoch.
     */
open func protocolVersion() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepoch_protocol_version(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The total amount of gas charged for storage during the epoch.
     */
open func storageCharge() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepoch_storage_charge(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The amount of storage rebate refunded to the txn senders.
     */
open func storageRebate() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepoch_storage_rebate(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * System packages (specifically framework and move stdlib) that are
     * written before the new epoch starts.
     */
open func systemPackages() -> [SystemPackage]  {
    return try!  FfiConverterSequenceTypeSystemPackage.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepoch_system_packages(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepoch_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: ChangeEpoch, other: ChangeEpoch) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepoch_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeChangeEpoch_lower(other),$0
    )
}
        )
    }

}
extension ChangeEpoch: CustomDebugStringConvertible {}
extension ChangeEpoch: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeChangeEpoch: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = ChangeEpoch

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> ChangeEpoch {
        return ChangeEpoch(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: ChangeEpoch) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ChangeEpoch {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: ChangeEpoch, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeChangeEpoch_lift(_ pointer: UnsafeMutableRawPointer) throws -> ChangeEpoch {
    return try FfiConverterTypeChangeEpoch.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeChangeEpoch_lower(_ value: ChangeEpoch) -> UnsafeMutableRawPointer {
    return FfiConverterTypeChangeEpoch.lower(value)
}






/**
 * System transaction used to change the epoch
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * change-epoch = u64  ; next epoch
 * u64  ; protocol version
 * u64  ; storage charge
 * u64  ; computation charge
 * u64  ; computation charge burned
 * u64  ; storage rebate
 * u64  ; non-refundable storage fee
 * u64  ; epoch start timestamp
 * (vector system-package)
 * ```
 */
public protocol ChangeEpochV2Protocol: AnyObject, Sendable {
    
    /**
     * The total amount of gas charged for computation during the epoch.
     */
    func computationCharge()  -> UInt64
    
    /**
     * The total amount of gas burned for computation during the epoch.
     */
    func computationChargeBurned()  -> UInt64
    
    /**
     * The next (to become) epoch ID.
     */
    func epoch()  -> UInt64
    
    /**
     * Unix timestamp when epoch started
     */
    func epochStartTimestampMs()  -> UInt64
    
    /**
     * The non-refundable storage fee.
     */
    func nonRefundableStorageFee()  -> UInt64
    
    /**
     * The protocol version in effect in the new epoch.
     */
    func protocolVersion()  -> UInt64
    
    /**
     * The total amount of gas charged for storage during the epoch.
     */
    func storageCharge()  -> UInt64
    
    /**
     * The amount of storage rebate refunded to the txn senders.
     */
    func storageRebate()  -> UInt64
    
    /**
     * System packages (specifically framework and move stdlib) that are
     * written before the new epoch starts.
     */
    func systemPackages()  -> [SystemPackage]
    
}
/**
 * System transaction used to change the epoch
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * change-epoch = u64  ; next epoch
 * u64  ; protocol version
 * u64  ; storage charge
 * u64  ; computation charge
 * u64  ; computation charge burned
 * u64  ; storage rebate
 * u64  ; non-refundable storage fee
 * u64  ; epoch start timestamp
 * (vector system-package)
 * ```
 */
open class ChangeEpochV2: ChangeEpochV2Protocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_changeepochv2(self.pointer, $0) }
    }
public convenience init(epoch: UInt64, protocolVersion: UInt64, storageCharge: UInt64, computationCharge: UInt64, computationChargeBurned: UInt64, storageRebate: UInt64, nonRefundableStorageFee: UInt64, epochStartTimestampMs: UInt64, systemPackages: [SystemPackage]) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_changeepochv2_new(
        FfiConverterUInt64.lower(epoch),
        FfiConverterUInt64.lower(protocolVersion),
        FfiConverterUInt64.lower(storageCharge),
        FfiConverterUInt64.lower(computationCharge),
        FfiConverterUInt64.lower(computationChargeBurned),
        FfiConverterUInt64.lower(storageRebate),
        FfiConverterUInt64.lower(nonRefundableStorageFee),
        FfiConverterUInt64.lower(epochStartTimestampMs),
        FfiConverterSequenceTypeSystemPackage.lower(systemPackages),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_changeepochv2(pointer, $0) }
    }

    

    
    /**
     * The total amount of gas charged for computation during the epoch.
     */
open func computationCharge() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepochv2_computation_charge(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The total amount of gas burned for computation during the epoch.
     */
open func computationChargeBurned() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepochv2_computation_charge_burned(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The next (to become) epoch ID.
     */
open func epoch() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepochv2_epoch(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Unix timestamp when epoch started
     */
open func epochStartTimestampMs() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepochv2_epoch_start_timestamp_ms(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The non-refundable storage fee.
     */
open func nonRefundableStorageFee() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepochv2_non_refundable_storage_fee(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The protocol version in effect in the new epoch.
     */
open func protocolVersion() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepochv2_protocol_version(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The total amount of gas charged for storage during the epoch.
     */
open func storageCharge() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepochv2_storage_charge(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The amount of storage rebate refunded to the txn senders.
     */
open func storageRebate() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepochv2_storage_rebate(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * System packages (specifically framework and move stdlib) that are
     * written before the new epoch starts.
     */
open func systemPackages() -> [SystemPackage]  {
    return try!  FfiConverterSequenceTypeSystemPackage.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepochv2_system_packages(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepochv2_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: ChangeEpochV2, other: ChangeEpochV2) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepochv2_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeChangeEpochV2_lower(other),$0
    )
}
        )
    }

}
extension ChangeEpochV2: CustomDebugStringConvertible {}
extension ChangeEpochV2: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeChangeEpochV2: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = ChangeEpochV2

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> ChangeEpochV2 {
        return ChangeEpochV2(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: ChangeEpochV2) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ChangeEpochV2 {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: ChangeEpochV2, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeChangeEpochV2_lift(_ pointer: UnsafeMutableRawPointer) throws -> ChangeEpochV2 {
    return try FfiConverterTypeChangeEpochV2.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeChangeEpochV2_lower(_ value: ChangeEpochV2) -> UnsafeMutableRawPointer {
    return FfiConverterTypeChangeEpochV2.lower(value)
}






public protocol ChangeEpochV3Protocol: AnyObject, Sendable {
    
    /**
     * The total amount of gas charged for computation during the epoch.
     */
    func computationCharge()  -> UInt64
    
    /**
     * The total amount of gas burned for computation during the epoch.
     */
    func computationChargeBurned()  -> UInt64
    
    /**
     * Vector of active validator indices eligible to take part in committee
     * selection because they support the new, target protocol version.
     */
    func eligibleActiveValidators()  -> [UInt64]
    
    /**
     * The next (to become) epoch ID.
     */
    func epoch()  -> UInt64
    
    /**
     * Unix timestamp when epoch started
     */
    func epochStartTimestampMs()  -> UInt64
    
    /**
     * The non-refundable storage fee.
     */
    func nonRefundableStorageFee()  -> UInt64
    
    /**
     * The protocol version in effect in the new epoch.
     */
    func protocolVersion()  -> UInt64
    
    /**
     * The total amount of gas charged for storage during the epoch.
     */
    func storageCharge()  -> UInt64
    
    /**
     * The amount of storage rebate refunded to the txn senders.
     */
    func storageRebate()  -> UInt64
    
    /**
     * System packages (specifically framework and move stdlib) that are
     * written before the new epoch starts.
     */
    func systemPackages()  -> [SystemPackage]
    
}
open class ChangeEpochV3: ChangeEpochV3Protocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_changeepochv3(self.pointer, $0) }
    }
public convenience init(epoch: UInt64, protocolVersion: UInt64, storageCharge: UInt64, computationCharge: UInt64, computationChargeBurned: UInt64, storageRebate: UInt64, nonRefundableStorageFee: UInt64, epochStartTimestampMs: UInt64, systemPackages: [SystemPackage], eligibleActiveValidators: [UInt64]) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_changeepochv3_new(
        FfiConverterUInt64.lower(epoch),
        FfiConverterUInt64.lower(protocolVersion),
        FfiConverterUInt64.lower(storageCharge),
        FfiConverterUInt64.lower(computationCharge),
        FfiConverterUInt64.lower(computationChargeBurned),
        FfiConverterUInt64.lower(storageRebate),
        FfiConverterUInt64.lower(nonRefundableStorageFee),
        FfiConverterUInt64.lower(epochStartTimestampMs),
        FfiConverterSequenceTypeSystemPackage.lower(systemPackages),
        FfiConverterSequenceUInt64.lower(eligibleActiveValidators),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_changeepochv3(pointer, $0) }
    }

    

    
    /**
     * The total amount of gas charged for computation during the epoch.
     */
open func computationCharge() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepochv3_computation_charge(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The total amount of gas burned for computation during the epoch.
     */
open func computationChargeBurned() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepochv3_computation_charge_burned(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Vector of active validator indices eligible to take part in committee
     * selection because they support the new, target protocol version.
     */
open func eligibleActiveValidators() -> [UInt64]  {
    return try!  FfiConverterSequenceUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepochv3_eligible_active_validators(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The next (to become) epoch ID.
     */
open func epoch() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepochv3_epoch(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Unix timestamp when epoch started
     */
open func epochStartTimestampMs() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepochv3_epoch_start_timestamp_ms(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The non-refundable storage fee.
     */
open func nonRefundableStorageFee() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepochv3_non_refundable_storage_fee(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The protocol version in effect in the new epoch.
     */
open func protocolVersion() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepochv3_protocol_version(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The total amount of gas charged for storage during the epoch.
     */
open func storageCharge() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepochv3_storage_charge(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The amount of storage rebate refunded to the txn senders.
     */
open func storageRebate() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepochv3_storage_rebate(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * System packages (specifically framework and move stdlib) that are
     * written before the new epoch starts.
     */
open func systemPackages() -> [SystemPackage]  {
    return try!  FfiConverterSequenceTypeSystemPackage.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepochv3_system_packages(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepochv3_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: ChangeEpochV3, other: ChangeEpochV3) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepochv3_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeChangeEpochV3_lower(other),$0
    )
}
        )
    }

}
extension ChangeEpochV3: CustomDebugStringConvertible {}
extension ChangeEpochV3: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeChangeEpochV3: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = ChangeEpochV3

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> ChangeEpochV3 {
        return ChangeEpochV3(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: ChangeEpochV3) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ChangeEpochV3 {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: ChangeEpochV3, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeChangeEpochV3_lift(_ pointer: UnsafeMutableRawPointer) throws -> ChangeEpochV3 {
    return try FfiConverterTypeChangeEpochV3.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeChangeEpochV3_lower(_ value: ChangeEpochV3) -> UnsafeMutableRawPointer {
    return FfiConverterTypeChangeEpochV3.lower(value)
}






public protocol ChangeEpochV4Protocol: AnyObject, Sendable {
    
    /**
     * Whether to adjust validator rewards based on score.
     */
    func adjustRewardsByScore()  -> Bool
    
    /**
     * The total amount of gas charged for computation during the epoch.
     */
    func computationCharge()  -> UInt64
    
    /**
     * The total amount of gas burned for computation during the epoch.
     */
    func computationChargeBurned()  -> UInt64
    
    /**
     * Vector of active validator indices eligible to take part in committee
     * selection because they support the new, target protocol version.
     */
    func eligibleActiveValidators()  -> [UInt64]
    
    /**
     * The next (to become) epoch ID.
     */
    func epoch()  -> UInt64
    
    /**
     * Unix timestamp when epoch started
     */
    func epochStartTimestampMs()  -> UInt64
    
    /**
     * The non-refundable storage fee.
     */
    func nonRefundableStorageFee()  -> UInt64
    
    /**
     * The protocol version in effect in the new epoch.
     */
    func protocolVersion()  -> UInt64
    
    /**
     * Vector of scores relative to the past epoch performance of each
     * validator, ordered by the past epoch's validator index.
     */
    func scores()  -> [UInt64]
    
    /**
     * The total amount of gas charged for storage during the epoch.
     */
    func storageCharge()  -> UInt64
    
    /**
     * The amount of storage rebate refunded to the txn senders.
     */
    func storageRebate()  -> UInt64
    
    /**
     * System packages (specifically framework and move stdlib) that are
     * written before the new epoch starts.
     */
    func systemPackages()  -> [SystemPackage]
    
}
open class ChangeEpochV4: ChangeEpochV4Protocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_changeepochv4(self.pointer, $0) }
    }
public convenience init(epoch: UInt64, protocolVersion: UInt64, storageCharge: UInt64, computationCharge: UInt64, computationChargeBurned: UInt64, storageRebate: UInt64, nonRefundableStorageFee: UInt64, epochStartTimestampMs: UInt64, systemPackages: [SystemPackage], eligibleActiveValidators: [UInt64], scores: [UInt64], adjustRewardsByScore: Bool) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_changeepochv4_new(
        FfiConverterUInt64.lower(epoch),
        FfiConverterUInt64.lower(protocolVersion),
        FfiConverterUInt64.lower(storageCharge),
        FfiConverterUInt64.lower(computationCharge),
        FfiConverterUInt64.lower(computationChargeBurned),
        FfiConverterUInt64.lower(storageRebate),
        FfiConverterUInt64.lower(nonRefundableStorageFee),
        FfiConverterUInt64.lower(epochStartTimestampMs),
        FfiConverterSequenceTypeSystemPackage.lower(systemPackages),
        FfiConverterSequenceUInt64.lower(eligibleActiveValidators),
        FfiConverterSequenceUInt64.lower(scores),
        FfiConverterBool.lower(adjustRewardsByScore),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_changeepochv4(pointer, $0) }
    }

    

    
    /**
     * Whether to adjust validator rewards based on score.
     */
open func adjustRewardsByScore() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepochv4_adjust_rewards_by_score(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The total amount of gas charged for computation during the epoch.
     */
open func computationCharge() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepochv4_computation_charge(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The total amount of gas burned for computation during the epoch.
     */
open func computationChargeBurned() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepochv4_computation_charge_burned(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Vector of active validator indices eligible to take part in committee
     * selection because they support the new, target protocol version.
     */
open func eligibleActiveValidators() -> [UInt64]  {
    return try!  FfiConverterSequenceUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepochv4_eligible_active_validators(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The next (to become) epoch ID.
     */
open func epoch() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepochv4_epoch(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Unix timestamp when epoch started
     */
open func epochStartTimestampMs() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepochv4_epoch_start_timestamp_ms(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The non-refundable storage fee.
     */
open func nonRefundableStorageFee() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepochv4_non_refundable_storage_fee(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The protocol version in effect in the new epoch.
     */
open func protocolVersion() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepochv4_protocol_version(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Vector of scores relative to the past epoch performance of each
     * validator, ordered by the past epoch's validator index.
     */
open func scores() -> [UInt64]  {
    return try!  FfiConverterSequenceUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepochv4_scores(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The total amount of gas charged for storage during the epoch.
     */
open func storageCharge() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepochv4_storage_charge(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The amount of storage rebate refunded to the txn senders.
     */
open func storageRebate() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepochv4_storage_rebate(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * System packages (specifically framework and move stdlib) that are
     * written before the new epoch starts.
     */
open func systemPackages() -> [SystemPackage]  {
    return try!  FfiConverterSequenceTypeSystemPackage.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_changeepochv4_system_packages(self.uniffiClonePointer(),$0
    )
})
}
    

}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeChangeEpochV4: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = ChangeEpochV4

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> ChangeEpochV4 {
        return ChangeEpochV4(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: ChangeEpochV4) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ChangeEpochV4 {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: ChangeEpochV4, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeChangeEpochV4_lift(_ pointer: UnsafeMutableRawPointer) throws -> ChangeEpochV4 {
    return try FfiConverterTypeChangeEpochV4.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeChangeEpochV4_lower(_ value: ChangeEpochV4) -> UnsafeMutableRawPointer {
    return FfiConverterTypeChangeEpochV4.lower(value)
}






/**
 * A commitment made by a checkpoint.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * ; CheckpointCommitment is an enum and each variant is prefixed with its index
 * checkpoint-commitment = ecmh-live-object-set
 * ecmh-live-object-set = %x00 digest
 * ```
 */
public protocol CheckpointCommitmentProtocol: AnyObject, Sendable {
    
    func asEcmhLiveObjectSetDigest()  -> Digest
    
    func isEcmhLiveObjectSet()  -> Bool
    
}
/**
 * A commitment made by a checkpoint.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * ; CheckpointCommitment is an enum and each variant is prefixed with its index
 * checkpoint-commitment = ecmh-live-object-set
 * ecmh-live-object-set = %x00 digest
 * ```
 */
open class CheckpointCommitment: CheckpointCommitmentProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_checkpointcommitment(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_checkpointcommitment(pointer, $0) }
    }

    

    
open func asEcmhLiveObjectSetDigest() -> Digest  {
    return try!  FfiConverterTypeDigest_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_checkpointcommitment_as_ecmh_live_object_set_digest(self.uniffiClonePointer(),$0
    )
})
}
    
open func isEcmhLiveObjectSet() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_checkpointcommitment_is_ecmh_live_object_set(self.uniffiClonePointer(),$0
    )
})
}
    

}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeCheckpointCommitment: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = CheckpointCommitment

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> CheckpointCommitment {
        return CheckpointCommitment(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: CheckpointCommitment) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> CheckpointCommitment {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: CheckpointCommitment, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeCheckpointCommitment_lift(_ pointer: UnsafeMutableRawPointer) throws -> CheckpointCommitment {
    return try FfiConverterTypeCheckpointCommitment.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeCheckpointCommitment_lower(_ value: CheckpointCommitment) -> UnsafeMutableRawPointer {
    return FfiConverterTypeCheckpointCommitment.lower(value)
}






/**
 * The committed to contents of a checkpoint.
 *
 * `CheckpointContents` contains a list of digests of Transactions, their
 * effects, and the user signatures that authorized their execution included in
 * a checkpoint.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * checkpoint-contents = %x00 checkpoint-contents-v1 ; variant 0
 *
 * checkpoint-contents-v1 = (vector (digest digest)) ; vector of transaction and effect digests
 * (vector (vector bcs-user-signature)) ; set of user signatures for each
 * ; transaction. MUST be the same
 * ; length as the vector of digests
 * ```
 */
public protocol CheckpointContentsProtocol: AnyObject, Sendable {
    
    func digest()  -> Digest
    
    func transactionInfo()  -> [CheckpointTransactionInfo]
    
}
/**
 * The committed to contents of a checkpoint.
 *
 * `CheckpointContents` contains a list of digests of Transactions, their
 * effects, and the user signatures that authorized their execution included in
 * a checkpoint.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * checkpoint-contents = %x00 checkpoint-contents-v1 ; variant 0
 *
 * checkpoint-contents-v1 = (vector (digest digest)) ; vector of transaction and effect digests
 * (vector (vector bcs-user-signature)) ; set of user signatures for each
 * ; transaction. MUST be the same
 * ; length as the vector of digests
 * ```
 */
open class CheckpointContents: CheckpointContentsProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_checkpointcontents(self.pointer, $0) }
    }
public convenience init(transactionInfo: [CheckpointTransactionInfo]) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_checkpointcontents_new(
        FfiConverterSequenceTypeCheckpointTransactionInfo.lower(transactionInfo),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_checkpointcontents(pointer, $0) }
    }

    

    
open func digest() -> Digest  {
    return try!  FfiConverterTypeDigest_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_checkpointcontents_digest(self.uniffiClonePointer(),$0
    )
})
}
    
open func transactionInfo() -> [CheckpointTransactionInfo]  {
    return try!  FfiConverterSequenceTypeCheckpointTransactionInfo.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_checkpointcontents_transaction_info(self.uniffiClonePointer(),$0
    )
})
}
    

}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeCheckpointContents: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = CheckpointContents

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> CheckpointContents {
        return CheckpointContents(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: CheckpointContents) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> CheckpointContents {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: CheckpointContents, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeCheckpointContents_lift(_ pointer: UnsafeMutableRawPointer) throws -> CheckpointContents {
    return try FfiConverterTypeCheckpointContents.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeCheckpointContents_lower(_ value: CheckpointContents) -> UnsafeMutableRawPointer {
    return FfiConverterTypeCheckpointContents.lower(value)
}






/**
 * A header for a Checkpoint on the IOTA blockchain.
 *
 * On the IOTA network, checkpoints define the history of the blockchain. They
 * are quite similar to the concept of blocks used by other blockchains like
 * Bitcoin or Ethereum. The IOTA blockchain, however, forms checkpoints after
 * transaction execution has already happened to provide a certified history of
 * the chain, instead of being formed before execution.
 *
 * Checkpoints commit to a variety of state including but not limited to:
 * - The hash of the previous checkpoint.
 * - The set of transaction digests, their corresponding effects digests, as
 * well as the set of user signatures which authorized its execution.
 * - The object's produced by a transaction.
 * - The set of live objects that make up the current state of the chain.
 * - On epoch transitions, the next validator committee.
 *
 * `CheckpointSummary`s themselves don't directly include all of the above
 * information but they are the top-level type by which all the above are
 * committed to transitively via cryptographic hashes included in the summary.
 * `CheckpointSummary`s are signed and certified by a quorum of the validator
 * committee in a given epoch in order to allow verification of the chain's
 * state.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * checkpoint-summary = u64                            ; epoch
 * u64                            ; sequence_number
 * u64                            ; network_total_transactions
 * digest                         ; content_digest
 * (option digest)                ; previous_digest
 * gas-cost-summary               ; epoch_rolling_gas_cost_summary
 * u64                            ; timestamp_ms
 * (vector checkpoint-commitment) ; checkpoint_commitments
 * (option end-of-epoch-data)     ; end_of_epoch_data
 * bytes                          ; version_specific_data
 * ```
 */
public protocol CheckpointSummaryProtocol: AnyObject, Sendable {
    
    /**
     * Commitments to checkpoint-specific state.
     */
    func checkpointCommitments()  -> [CheckpointCommitment]
    
    /**
     * The hash of the `CheckpointContents` for this checkpoint.
     */
    func contentDigest()  -> Digest
    
    func digest()  -> Digest
    
    /**
     * Extra data only present in the final checkpoint of an epoch.
     */
    func endOfEpochData()  -> EndOfEpochData?
    
    /**
     * Epoch that this checkpoint belongs to.
     */
    func epoch()  -> UInt64
    
    /**
     * The running total gas costs of all transactions included in the current
     * epoch so far until this checkpoint.
     */
    func epochRollingGasCostSummary()  -> GasCostSummary
    
    /**
     * Total number of transactions committed since genesis, including those in
     * this checkpoint.
     */
    func networkTotalTransactions()  -> UInt64
    
    /**
     * The hash of the previous `CheckpointSummary`.
     *
     * This will be only be `None` for the first, or genesis checkpoint.
     */
    func previousDigest()  -> Digest?
    
    /**
     * The height of this checkpoint.
     */
    func sequenceNumber()  -> UInt64
    
    func signingMessage()  -> Data
    
    func signingMessageHex()  -> String
    
    /**
     * Timestamp of the checkpoint - number of milliseconds from the Unix epoch
     * Checkpoint timestamps are monotonic, but not strongly monotonic -
     * subsequent checkpoints can have same timestamp if they originate
     * from the same underlining consensus commit
     */
    func timestampMs()  -> UInt64
    
    /**
     * CheckpointSummary is not an evolvable structure - it must be readable by
     * any version of the code. Therefore, in order to allow extensions to
     * be added to CheckpointSummary, we allow opaque data to be added to
     * checkpoints which can be deserialized based on the current
     * protocol version.
     */
    func versionSpecificData()  -> Data
    
}
/**
 * A header for a Checkpoint on the IOTA blockchain.
 *
 * On the IOTA network, checkpoints define the history of the blockchain. They
 * are quite similar to the concept of blocks used by other blockchains like
 * Bitcoin or Ethereum. The IOTA blockchain, however, forms checkpoints after
 * transaction execution has already happened to provide a certified history of
 * the chain, instead of being formed before execution.
 *
 * Checkpoints commit to a variety of state including but not limited to:
 * - The hash of the previous checkpoint.
 * - The set of transaction digests, their corresponding effects digests, as
 * well as the set of user signatures which authorized its execution.
 * - The object's produced by a transaction.
 * - The set of live objects that make up the current state of the chain.
 * - On epoch transitions, the next validator committee.
 *
 * `CheckpointSummary`s themselves don't directly include all of the above
 * information but they are the top-level type by which all the above are
 * committed to transitively via cryptographic hashes included in the summary.
 * `CheckpointSummary`s are signed and certified by a quorum of the validator
 * committee in a given epoch in order to allow verification of the chain's
 * state.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * checkpoint-summary = u64                            ; epoch
 * u64                            ; sequence_number
 * u64                            ; network_total_transactions
 * digest                         ; content_digest
 * (option digest)                ; previous_digest
 * gas-cost-summary               ; epoch_rolling_gas_cost_summary
 * u64                            ; timestamp_ms
 * (vector checkpoint-commitment) ; checkpoint_commitments
 * (option end-of-epoch-data)     ; end_of_epoch_data
 * bytes                          ; version_specific_data
 * ```
 */
open class CheckpointSummary: CheckpointSummaryProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_checkpointsummary(self.pointer, $0) }
    }
public convenience init(epoch: UInt64, sequenceNumber: UInt64, networkTotalTransactions: UInt64, contentDigest: Digest, previousDigest: Digest?, epochRollingGasCostSummary: GasCostSummary, timestampMs: UInt64, checkpointCommitments: [CheckpointCommitment], endOfEpochData: EndOfEpochData?, versionSpecificData: Data) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_checkpointsummary_new(
        FfiConverterUInt64.lower(epoch),
        FfiConverterUInt64.lower(sequenceNumber),
        FfiConverterUInt64.lower(networkTotalTransactions),
        FfiConverterTypeDigest_lower(contentDigest),
        FfiConverterOptionTypeDigest.lower(previousDigest),
        FfiConverterTypeGasCostSummary_lower(epochRollingGasCostSummary),
        FfiConverterUInt64.lower(timestampMs),
        FfiConverterSequenceTypeCheckpointCommitment.lower(checkpointCommitments),
        FfiConverterOptionTypeEndOfEpochData.lower(endOfEpochData),
        FfiConverterData.lower(versionSpecificData),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_checkpointsummary(pointer, $0) }
    }

    

    
    /**
     * Commitments to checkpoint-specific state.
     */
open func checkpointCommitments() -> [CheckpointCommitment]  {
    return try!  FfiConverterSequenceTypeCheckpointCommitment.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_checkpointsummary_checkpoint_commitments(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The hash of the `CheckpointContents` for this checkpoint.
     */
open func contentDigest() -> Digest  {
    return try!  FfiConverterTypeDigest_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_checkpointsummary_content_digest(self.uniffiClonePointer(),$0
    )
})
}
    
open func digest() -> Digest  {
    return try!  FfiConverterTypeDigest_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_checkpointsummary_digest(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Extra data only present in the final checkpoint of an epoch.
     */
open func endOfEpochData() -> EndOfEpochData?  {
    return try!  FfiConverterOptionTypeEndOfEpochData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_checkpointsummary_end_of_epoch_data(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Epoch that this checkpoint belongs to.
     */
open func epoch() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_checkpointsummary_epoch(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The running total gas costs of all transactions included in the current
     * epoch so far until this checkpoint.
     */
open func epochRollingGasCostSummary() -> GasCostSummary  {
    return try!  FfiConverterTypeGasCostSummary_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_checkpointsummary_epoch_rolling_gas_cost_summary(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Total number of transactions committed since genesis, including those in
     * this checkpoint.
     */
open func networkTotalTransactions() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_checkpointsummary_network_total_transactions(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The hash of the previous `CheckpointSummary`.
     *
     * This will be only be `None` for the first, or genesis checkpoint.
     */
open func previousDigest() -> Digest?  {
    return try!  FfiConverterOptionTypeDigest.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_checkpointsummary_previous_digest(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The height of this checkpoint.
     */
open func sequenceNumber() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_checkpointsummary_sequence_number(self.uniffiClonePointer(),$0
    )
})
}
    
open func signingMessage() -> Data  {
    return try!  FfiConverterData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_checkpointsummary_signing_message(self.uniffiClonePointer(),$0
    )
})
}
    
open func signingMessageHex() -> String  {
    return try!  FfiConverterString.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_checkpointsummary_signing_message_hex(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Timestamp of the checkpoint - number of milliseconds from the Unix epoch
     * Checkpoint timestamps are monotonic, but not strongly monotonic -
     * subsequent checkpoints can have same timestamp if they originate
     * from the same underlining consensus commit
     */
open func timestampMs() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_checkpointsummary_timestamp_ms(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * CheckpointSummary is not an evolvable structure - it must be readable by
     * any version of the code. Therefore, in order to allow extensions to
     * be added to CheckpointSummary, we allow opaque data to be added to
     * checkpoints which can be deserialized based on the current
     * protocol version.
     */
open func versionSpecificData() -> Data  {
    return try!  FfiConverterData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_checkpointsummary_version_specific_data(self.uniffiClonePointer(),$0
    )
})
}
    

}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeCheckpointSummary: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = CheckpointSummary

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> CheckpointSummary {
        return CheckpointSummary(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: CheckpointSummary) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> CheckpointSummary {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: CheckpointSummary, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeCheckpointSummary_lift(_ pointer: UnsafeMutableRawPointer) throws -> CheckpointSummary {
    return try FfiConverterTypeCheckpointSummary.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeCheckpointSummary_lower(_ value: CheckpointSummary) -> UnsafeMutableRawPointer {
    return FfiConverterTypeCheckpointSummary.lower(value)
}






/**
 * Transaction information committed to in a checkpoint
 */
public protocol CheckpointTransactionInfoProtocol: AnyObject, Sendable {
    
    func effects()  -> Digest
    
    func signatures()  -> [UserSignature]
    
    func transaction()  -> Digest
    
}
/**
 * Transaction information committed to in a checkpoint
 */
open class CheckpointTransactionInfo: CheckpointTransactionInfoProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_checkpointtransactioninfo(self.pointer, $0) }
    }
public convenience init(transaction: Digest, effects: Digest, signatures: [UserSignature]) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_checkpointtransactioninfo_new(
        FfiConverterTypeDigest_lower(transaction),
        FfiConverterTypeDigest_lower(effects),
        FfiConverterSequenceTypeUserSignature.lower(signatures),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_checkpointtransactioninfo(pointer, $0) }
    }

    

    
open func effects() -> Digest  {
    return try!  FfiConverterTypeDigest_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_checkpointtransactioninfo_effects(self.uniffiClonePointer(),$0
    )
})
}
    
open func signatures() -> [UserSignature]  {
    return try!  FfiConverterSequenceTypeUserSignature.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_checkpointtransactioninfo_signatures(self.uniffiClonePointer(),$0
    )
})
}
    
open func transaction() -> Digest  {
    return try!  FfiConverterTypeDigest_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_checkpointtransactioninfo_transaction(self.uniffiClonePointer(),$0
    )
})
}
    

}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeCheckpointTransactionInfo: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = CheckpointTransactionInfo

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> CheckpointTransactionInfo {
        return CheckpointTransactionInfo(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: CheckpointTransactionInfo) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> CheckpointTransactionInfo {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: CheckpointTransactionInfo, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeCheckpointTransactionInfo_lift(_ pointer: UnsafeMutableRawPointer) throws -> CheckpointTransactionInfo {
    return try FfiConverterTypeCheckpointTransactionInfo.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeCheckpointTransactionInfo_lower(_ value: CheckpointTransactionInfo) -> UnsafeMutableRawPointer {
    return FfiConverterTypeCheckpointTransactionInfo.lower(value)
}






/**
 * A G1 point
 *
 * This represents the canonical decimal representation of the projective
 * coordinates in Fq.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * circom-g1 = %x03 3(bn254-field-element)
 * ```
 */
public protocol CircomG1Protocol: AnyObject, Sendable {
    
}
/**
 * A G1 point
 *
 * This represents the canonical decimal representation of the projective
 * coordinates in Fq.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * circom-g1 = %x03 3(bn254-field-element)
 * ```
 */
open class CircomG1: CircomG1Protocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_circomg1(self.pointer, $0) }
    }
public convenience init(el0: Bn254FieldElement, el1: Bn254FieldElement, el2: Bn254FieldElement) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_circomg1_new(
        FfiConverterTypeBn254FieldElement_lower(el0),
        FfiConverterTypeBn254FieldElement_lower(el1),
        FfiConverterTypeBn254FieldElement_lower(el2),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_circomg1(pointer, $0) }
    }

    

    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_circomg1_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: CircomG1, other: CircomG1) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_circomg1_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeCircomG1_lower(other),$0
    )
}
        )
    }

}
extension CircomG1: CustomDebugStringConvertible {}
extension CircomG1: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeCircomG1: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = CircomG1

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> CircomG1 {
        return CircomG1(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: CircomG1) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> CircomG1 {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: CircomG1, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeCircomG1_lift(_ pointer: UnsafeMutableRawPointer) throws -> CircomG1 {
    return try FfiConverterTypeCircomG1.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeCircomG1_lower(_ value: CircomG1) -> UnsafeMutableRawPointer {
    return FfiConverterTypeCircomG1.lower(value)
}






/**
 * A G2 point
 *
 * This represents the canonical decimal representation of the coefficients of
 * the projective coordinates in Fq2.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * circom-g2 = %x03 3(%x02 2(bn254-field-element))
 * ```
 */
public protocol CircomG2Protocol: AnyObject, Sendable {
    
}
/**
 * A G2 point
 *
 * This represents the canonical decimal representation of the coefficients of
 * the projective coordinates in Fq2.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * circom-g2 = %x03 3(%x02 2(bn254-field-element))
 * ```
 */
open class CircomG2: CircomG2Protocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_circomg2(self.pointer, $0) }
    }
public convenience init(el00: Bn254FieldElement, el01: Bn254FieldElement, el10: Bn254FieldElement, el11: Bn254FieldElement, el20: Bn254FieldElement, el21: Bn254FieldElement) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_circomg2_new(
        FfiConverterTypeBn254FieldElement_lower(el00),
        FfiConverterTypeBn254FieldElement_lower(el01),
        FfiConverterTypeBn254FieldElement_lower(el10),
        FfiConverterTypeBn254FieldElement_lower(el11),
        FfiConverterTypeBn254FieldElement_lower(el20),
        FfiConverterTypeBn254FieldElement_lower(el21),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_circomg2(pointer, $0) }
    }

    

    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_circomg2_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: CircomG2, other: CircomG2) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_circomg2_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeCircomG2_lower(other),$0
    )
}
        )
    }

}
extension CircomG2: CustomDebugStringConvertible {}
extension CircomG2: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeCircomG2: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = CircomG2

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> CircomG2 {
        return CircomG2(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: CircomG2) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> CircomG2 {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: CircomG2, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeCircomG2_lift(_ pointer: UnsafeMutableRawPointer) throws -> CircomG2 {
    return try FfiConverterTypeCircomG2.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeCircomG2_lower(_ value: CircomG2) -> UnsafeMutableRawPointer {
    return FfiConverterTypeCircomG2.lower(value)
}






/**
 * A builder for creating transactions which uses a GraphQL client to
 * automatically resolve inputs. Use `finish` to finalize the transaction data.
 */
public protocol ClientTransactionBuilderProtocol: AnyObject, Sendable {
    
    /**
     * Dry run the transaction.
     */
    func dryRun(skipChecks: Bool) async throws  -> DryRunResult
    
    /**
     * Execute the transaction and optionally wait for finalization.
     */
    func execute(signer: TransactionSigner, waitFor: WaitForTx?) async throws  -> TransactionEffects
    
    /**
     * Execute the transaction and optionally wait for finalization.
     */
    func executeWithSponsor(signer: TransactionSigner, sponsorSigner: TransactionSigner, waitFor: WaitForTx?) async throws  -> TransactionEffects
    
    /**
     * Set the expiration of the transaction to be a specific epoch.
     */
    func expiration(epoch: UInt64)  -> ClientTransactionBuilder
    
    /**
     * Convert this builder into a transaction.
     */
    func finish() async throws  -> Transaction
    
    /**
     * Add gas coins that will be consumed. Optional.
     */
    func gas(objectIds: [ObjectId])  -> ClientTransactionBuilder
    
    /**
     * Set the gas budget for the transaction.
     */
    func gasBudget(budget: UInt64)  -> ClientTransactionBuilder
    
    /**
     * Set the gas price for the transaction.
     */
    func gasPrice(price: UInt64)  -> ClientTransactionBuilder
    
    /**
     * Set the gas station sponsor.
     */
    func gasStationSponsor(url: String, duration: TimeInterval?, headers: [String: [String]]?)  -> ClientTransactionBuilder
    
    /**
     * Make a move vector from a list of elements. The elements must all be of
     * the type indicated by `type_tag`.
     */
    func makeMoveVec(elements: [MoveArg], typeTag: TypeTag, name: String)  -> ClientTransactionBuilder
    
    /**
     * Merge multiple coins into one.
     *
     * This method combines the balances of multiple coins of the same coin
     * type into a single coin. The `primary_coin` will receive the balances
     * from all `consumed_coins`. After merging, the `consumed_coins` will
     * be consumed and no longer exist.
     */
    func mergeCoins(primaryCoin: PtbArgument, consumedCoins: [PtbArgument])  -> ClientTransactionBuilder
    
    /**
     * Call a Move function with the given arguments.
     */
    func moveCall(package: Address, module: Identifier, function: Identifier, arguments: [PtbArgument], typeArgs: [TypeTag], names: [String])  -> ClientTransactionBuilder
    
    /**
     * Publish a list of modules with the given dependencies. The result
     * assigned to `upgrade_cap_name` is the `0x2::package::UpgradeCap`
     * Move type. Note that the upgrade capability needs to be handled
     * after this call:
     * - transfer it to the transaction sender or another address
     * - burn it
     * - wrap it for access control
     * - discard the it to make a package immutable
     *
     * The arguments required for this command are:
     * - `modules`: is the modules' bytecode to be published
     * - `dependencies`: is the list of IDs of the transitive dependencies of
     * the package
     */
    func publish(packageData: MovePackageData, upgradeCapName: String)  -> ClientTransactionBuilder
    
    /**
     * Transfer some coins to a recipient address. If multiple coins are
     * provided then they will be merged.
     *
     * The `amount` parameter specifies the quantity in NANOS, where 1 IOTA
     * equals 1_000_000_000 NANOS.
     * If `amount` is provided, that amount is split from the provided coins
     * and sent.
     * If `amount` is `None`, the entire coins are transferred.
     *
     * All provided coins must have the same coin type. Mixing coins of
     * different types will result in an error.
     *
     * If you intend to transfer all provided coins to another address in a
     * single transaction, consider using
     * `TransactionBuilder::transfer_objects()` instead.
     */
    func sendCoins(coins: [PtbArgument], recipient: Address, amount: PtbArgument?)  -> ClientTransactionBuilder
    
    /**
     * Send IOTA to a recipient address.
     *
     * The `amount` parameter specifies the quantity in NANOS, where 1 IOTA
     * equals 1_000_000_000 NANOS. That amount is split from the gas coin and
     * sent.
     */
    func sendIota(recipient: Address, amount: PtbArgument)  -> ClientTransactionBuilder
    
    /**
     * Set the sender address.
     */
    func setSender(sender: Address) 
    
    /**
     * Split a coin into many.
     */
    func splitCoins(coin: PtbArgument, amounts: [PtbArgument], names: [String])  -> ClientTransactionBuilder
    
    /**
     * Set the sponsor of the transaction.
     */
    func sponsor(sponsor: Address)  -> ClientTransactionBuilder
    
    /**
     * Add stake to a validator's staking pool.
     *
     * This is a high-level function which will split the provided stake amount
     * from the gas coin and then stake using the resulting coin.
     */
    func stake(stake: PtbArgument, validatorAddress: Address)  -> ClientTransactionBuilder
    
    /**
     * Transfer a list of objects to the given address, without producing any
     * result.
     */
    func transferObjects(recipient: Address, objects: [PtbArgument])  -> ClientTransactionBuilder
    
    /**
     * Withdraw stake from a validator's staking pool.
     */
    func unstake(stakedIota: PtbArgument)  -> ClientTransactionBuilder
    
    /**
     * Upgrade a Move package.
     *
     * - `modules`: is the modules' bytecode for the modules to be published
     * - `dependencies`: is the list of IDs of the transitive dependencies of
     * the package to be upgraded
     * - `package`: is the ID of the current package being upgraded
     * - `ticket`: is the upgrade ticket
     *
     * To get the ticket, you have to call the
     * `0x2::package::authorize_upgrade` function, and pass the package
     * ID, the upgrade policy, and package digest.
     */
    func upgrade(packageId: ObjectId, packageData: MovePackageData, upgradeTicket: PtbArgument, name: String?)  -> ClientTransactionBuilder
    
}
/**
 * A builder for creating transactions which uses a GraphQL client to
 * automatically resolve inputs. Use `finish` to finalize the transaction data.
 */
open class ClientTransactionBuilder: ClientTransactionBuilderProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_clienttransactionbuilder(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_clienttransactionbuilder(pointer, $0) }
    }

    

    
    /**
     * Dry run the transaction.
     */
open func dryRun(skipChecks: Bool = false)async throws  -> DryRunResult  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_clienttransactionbuilder_dry_run(
                    self.uniffiClonePointer(),
                    FfiConverterBool.lower(skipChecks)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterTypeDryRunResult_lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Execute the transaction and optionally wait for finalization.
     */
open func execute(signer: TransactionSigner, waitFor: WaitForTx? = nil)async throws  -> TransactionEffects  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_clienttransactionbuilder_execute(
                    self.uniffiClonePointer(),
                    FfiConverterTypeTransactionSigner_lower(signer),FfiConverterOptionTypeWaitForTx.lower(waitFor)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_pointer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_pointer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_pointer,
            liftFunc: FfiConverterTypeTransactionEffects_lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Execute the transaction and optionally wait for finalization.
     */
open func executeWithSponsor(signer: TransactionSigner, sponsorSigner: TransactionSigner, waitFor: WaitForTx? = nil)async throws  -> TransactionEffects  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_clienttransactionbuilder_execute_with_sponsor(
                    self.uniffiClonePointer(),
                    FfiConverterTypeTransactionSigner_lower(signer),FfiConverterTypeTransactionSigner_lower(sponsorSigner),FfiConverterOptionTypeWaitForTx.lower(waitFor)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_pointer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_pointer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_pointer,
            liftFunc: FfiConverterTypeTransactionEffects_lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Set the expiration of the transaction to be a specific epoch.
     */
open func expiration(epoch: UInt64) -> ClientTransactionBuilder  {
    return try!  FfiConverterTypeClientTransactionBuilder_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_clienttransactionbuilder_expiration(self.uniffiClonePointer(),
        FfiConverterUInt64.lower(epoch),$0
    )
})
}
    
    /**
     * Convert this builder into a transaction.
     */
open func finish()async throws  -> Transaction  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_clienttransactionbuilder_finish(
                    self.uniffiClonePointer()
                    
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_pointer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_pointer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_pointer,
            liftFunc: FfiConverterTypeTransaction_lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Add gas coins that will be consumed. Optional.
     */
open func gas(objectIds: [ObjectId]) -> ClientTransactionBuilder  {
    return try!  FfiConverterTypeClientTransactionBuilder_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_clienttransactionbuilder_gas(self.uniffiClonePointer(),
        FfiConverterSequenceTypeObjectId.lower(objectIds),$0
    )
})
}
    
    /**
     * Set the gas budget for the transaction.
     */
open func gasBudget(budget: UInt64) -> ClientTransactionBuilder  {
    return try!  FfiConverterTypeClientTransactionBuilder_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_clienttransactionbuilder_gas_budget(self.uniffiClonePointer(),
        FfiConverterUInt64.lower(budget),$0
    )
})
}
    
    /**
     * Set the gas price for the transaction.
     */
open func gasPrice(price: UInt64) -> ClientTransactionBuilder  {
    return try!  FfiConverterTypeClientTransactionBuilder_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_clienttransactionbuilder_gas_price(self.uniffiClonePointer(),
        FfiConverterUInt64.lower(price),$0
    )
})
}
    
    /**
     * Set the gas station sponsor.
     */
open func gasStationSponsor(url: String, duration: TimeInterval? = nil, headers: [String: [String]]? = nil) -> ClientTransactionBuilder  {
    return try!  FfiConverterTypeClientTransactionBuilder_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_clienttransactionbuilder_gas_station_sponsor(self.uniffiClonePointer(),
        FfiConverterString.lower(url),
        FfiConverterOptionDuration.lower(duration),
        FfiConverterOptionDictionaryStringSequenceString.lower(headers),$0
    )
})
}
    
    /**
     * Make a move vector from a list of elements. The elements must all be of
     * the type indicated by `type_tag`.
     */
open func makeMoveVec(elements: [MoveArg], typeTag: TypeTag, name: String) -> ClientTransactionBuilder  {
    return try!  FfiConverterTypeClientTransactionBuilder_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_clienttransactionbuilder_make_move_vec(self.uniffiClonePointer(),
        FfiConverterSequenceTypeMoveArg.lower(elements),
        FfiConverterTypeTypeTag_lower(typeTag),
        FfiConverterString.lower(name),$0
    )
})
}
    
    /**
     * Merge multiple coins into one.
     *
     * This method combines the balances of multiple coins of the same coin
     * type into a single coin. The `primary_coin` will receive the balances
     * from all `consumed_coins`. After merging, the `consumed_coins` will
     * be consumed and no longer exist.
     */
open func mergeCoins(primaryCoin: PtbArgument, consumedCoins: [PtbArgument]) -> ClientTransactionBuilder  {
    return try!  FfiConverterTypeClientTransactionBuilder_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_clienttransactionbuilder_merge_coins(self.uniffiClonePointer(),
        FfiConverterTypePTBArgument_lower(primaryCoin),
        FfiConverterSequenceTypePTBArgument.lower(consumedCoins),$0
    )
})
}
    
    /**
     * Call a Move function with the given arguments.
     */
open func moveCall(package: Address, module: Identifier, function: Identifier, arguments: [PtbArgument] = [], typeArgs: [TypeTag] = [], names: [String] = []) -> ClientTransactionBuilder  {
    return try!  FfiConverterTypeClientTransactionBuilder_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_clienttransactionbuilder_move_call(self.uniffiClonePointer(),
        FfiConverterTypeAddress_lower(package),
        FfiConverterTypeIdentifier_lower(module),
        FfiConverterTypeIdentifier_lower(function),
        FfiConverterSequenceTypePTBArgument.lower(arguments),
        FfiConverterSequenceTypeTypeTag.lower(typeArgs),
        FfiConverterSequenceString.lower(names),$0
    )
})
}
    
    /**
     * Publish a list of modules with the given dependencies. The result
     * assigned to `upgrade_cap_name` is the `0x2::package::UpgradeCap`
     * Move type. Note that the upgrade capability needs to be handled
     * after this call:
     * - transfer it to the transaction sender or another address
     * - burn it
     * - wrap it for access control
     * - discard the it to make a package immutable
     *
     * The arguments required for this command are:
     * - `modules`: is the modules' bytecode to be published
     * - `dependencies`: is the list of IDs of the transitive dependencies of
     * the package
     */
open func publish(packageData: MovePackageData, upgradeCapName: String) -> ClientTransactionBuilder  {
    return try!  FfiConverterTypeClientTransactionBuilder_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_clienttransactionbuilder_publish(self.uniffiClonePointer(),
        FfiConverterTypeMovePackageData_lower(packageData),
        FfiConverterString.lower(upgradeCapName),$0
    )
})
}
    
    /**
     * Transfer some coins to a recipient address. If multiple coins are
     * provided then they will be merged.
     *
     * The `amount` parameter specifies the quantity in NANOS, where 1 IOTA
     * equals 1_000_000_000 NANOS.
     * If `amount` is provided, that amount is split from the provided coins
     * and sent.
     * If `amount` is `None`, the entire coins are transferred.
     *
     * All provided coins must have the same coin type. Mixing coins of
     * different types will result in an error.
     *
     * If you intend to transfer all provided coins to another address in a
     * single transaction, consider using
     * `TransactionBuilder::transfer_objects()` instead.
     */
open func sendCoins(coins: [PtbArgument], recipient: Address, amount: PtbArgument? = nil) -> ClientTransactionBuilder  {
    return try!  FfiConverterTypeClientTransactionBuilder_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_clienttransactionbuilder_send_coins(self.uniffiClonePointer(),
        FfiConverterSequenceTypePTBArgument.lower(coins),
        FfiConverterTypeAddress_lower(recipient),
        FfiConverterOptionTypePTBArgument.lower(amount),$0
    )
})
}
    
    /**
     * Send IOTA to a recipient address.
     *
     * The `amount` parameter specifies the quantity in NANOS, where 1 IOTA
     * equals 1_000_000_000 NANOS. That amount is split from the gas coin and
     * sent.
     */
open func sendIota(recipient: Address, amount: PtbArgument) -> ClientTransactionBuilder  {
    return try!  FfiConverterTypeClientTransactionBuilder_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_clienttransactionbuilder_send_iota(self.uniffiClonePointer(),
        FfiConverterTypeAddress_lower(recipient),
        FfiConverterTypePTBArgument_lower(amount),$0
    )
})
}
    
    /**
     * Set the sender address.
     */
open func setSender(sender: Address)  {try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_clienttransactionbuilder_set_sender(self.uniffiClonePointer(),
        FfiConverterTypeAddress_lower(sender),$0
    )
}
}
    
    /**
     * Split a coin into many.
     */
open func splitCoins(coin: PtbArgument, amounts: [PtbArgument], names: [String] = []) -> ClientTransactionBuilder  {
    return try!  FfiConverterTypeClientTransactionBuilder_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_clienttransactionbuilder_split_coins(self.uniffiClonePointer(),
        FfiConverterTypePTBArgument_lower(coin),
        FfiConverterSequenceTypePTBArgument.lower(amounts),
        FfiConverterSequenceString.lower(names),$0
    )
})
}
    
    /**
     * Set the sponsor of the transaction.
     */
open func sponsor(sponsor: Address) -> ClientTransactionBuilder  {
    return try!  FfiConverterTypeClientTransactionBuilder_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_clienttransactionbuilder_sponsor(self.uniffiClonePointer(),
        FfiConverterTypeAddress_lower(sponsor),$0
    )
})
}
    
    /**
     * Add stake to a validator's staking pool.
     *
     * This is a high-level function which will split the provided stake amount
     * from the gas coin and then stake using the resulting coin.
     */
open func stake(stake: PtbArgument, validatorAddress: Address) -> ClientTransactionBuilder  {
    return try!  FfiConverterTypeClientTransactionBuilder_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_clienttransactionbuilder_stake(self.uniffiClonePointer(),
        FfiConverterTypePTBArgument_lower(stake),
        FfiConverterTypeAddress_lower(validatorAddress),$0
    )
})
}
    
    /**
     * Transfer a list of objects to the given address, without producing any
     * result.
     */
open func transferObjects(recipient: Address, objects: [PtbArgument]) -> ClientTransactionBuilder  {
    return try!  FfiConverterTypeClientTransactionBuilder_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_clienttransactionbuilder_transfer_objects(self.uniffiClonePointer(),
        FfiConverterTypeAddress_lower(recipient),
        FfiConverterSequenceTypePTBArgument.lower(objects),$0
    )
})
}
    
    /**
     * Withdraw stake from a validator's staking pool.
     */
open func unstake(stakedIota: PtbArgument) -> ClientTransactionBuilder  {
    return try!  FfiConverterTypeClientTransactionBuilder_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_clienttransactionbuilder_unstake(self.uniffiClonePointer(),
        FfiConverterTypePTBArgument_lower(stakedIota),$0
    )
})
}
    
    /**
     * Upgrade a Move package.
     *
     * - `modules`: is the modules' bytecode for the modules to be published
     * - `dependencies`: is the list of IDs of the transitive dependencies of
     * the package to be upgraded
     * - `package`: is the ID of the current package being upgraded
     * - `ticket`: is the upgrade ticket
     *
     * To get the ticket, you have to call the
     * `0x2::package::authorize_upgrade` function, and pass the package
     * ID, the upgrade policy, and package digest.
     */
open func upgrade(packageId: ObjectId, packageData: MovePackageData, upgradeTicket: PtbArgument, name: String? = nil) -> ClientTransactionBuilder  {
    return try!  FfiConverterTypeClientTransactionBuilder_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_clienttransactionbuilder_upgrade(self.uniffiClonePointer(),
        FfiConverterTypeObjectId_lower(packageId),
        FfiConverterTypeMovePackageData_lower(packageData),
        FfiConverterTypePTBArgument_lower(upgradeTicket),
        FfiConverterOptionString.lower(name),$0
    )
})
}
    

}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeClientTransactionBuilder: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = ClientTransactionBuilder

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> ClientTransactionBuilder {
        return ClientTransactionBuilder(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: ClientTransactionBuilder) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ClientTransactionBuilder {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: ClientTransactionBuilder, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeClientTransactionBuilder_lift(_ pointer: UnsafeMutableRawPointer) throws -> ClientTransactionBuilder {
    return try FfiConverterTypeClientTransactionBuilder.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeClientTransactionBuilder_lower(_ value: ClientTransactionBuilder) -> UnsafeMutableRawPointer {
    return FfiConverterTypeClientTransactionBuilder.lower(value)
}






public protocol CoinProtocol: AnyObject, Sendable {
    
    func balance()  -> UInt64
    
    func coinType()  -> TypeTag
    
    func id()  -> ObjectId
    
}
open class Coin: CoinProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_coin(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_coin(pointer, $0) }
    }

    
public static func tryFromObject(object: Object)throws  -> Coin  {
    return try  FfiConverterTypeCoin_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_coin_try_from_object(
        FfiConverterTypeObject_lower(object),$0
    )
})
}
    

    
open func balance() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_coin_balance(self.uniffiClonePointer(),$0
    )
})
}
    
open func coinType() -> TypeTag  {
    return try!  FfiConverterTypeTypeTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_coin_coin_type(self.uniffiClonePointer(),$0
    )
})
}
    
open func id() -> ObjectId  {
    return try!  FfiConverterTypeObjectId_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_coin_id(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_coin_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }

}
extension Coin: CustomDebugStringConvertible {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeCoin: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = Coin

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> Coin {
        return Coin(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: Coin) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Coin {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: Coin, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeCoin_lift(_ pointer: UnsafeMutableRawPointer) throws -> Coin {
    return try FfiConverterTypeCoin.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeCoin_lower(_ value: Coin) -> UnsafeMutableRawPointer {
    return FfiConverterTypeCoin.lower(value)
}






/**
 * A single command in a programmable transaction.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * command =  command-move-call
 * =/ command-transfer-objects
 * =/ command-split-coins
 * =/ command-merge-coins
 * =/ command-publish
 * =/ command-make-move-vector
 * =/ command-upgrade
 *
 * command-move-call           = %x00 move-call
 * command-transfer-objects    = %x01 transfer-objects
 * command-split-coins         = %x02 split-coins
 * command-merge-coins         = %x03 merge-coins
 * command-publish             = %x04 publish
 * command-make-move-vector    = %x05 make-move-vector
 * command-upgrade             = %x06 upgrade
 * ```
 */
public protocol CommandProtocol: AnyObject, Sendable {
    
}
/**
 * A single command in a programmable transaction.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * command =  command-move-call
 * =/ command-transfer-objects
 * =/ command-split-coins
 * =/ command-merge-coins
 * =/ command-publish
 * =/ command-make-move-vector
 * =/ command-upgrade
 *
 * command-move-call           = %x00 move-call
 * command-transfer-objects    = %x01 transfer-objects
 * command-split-coins         = %x02 split-coins
 * command-merge-coins         = %x03 merge-coins
 * command-publish             = %x04 publish
 * command-make-move-vector    = %x05 make-move-vector
 * command-upgrade             = %x06 upgrade
 * ```
 */
open class Command: CommandProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_command(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_command(pointer, $0) }
    }

    
    /**
     * Given n-values of the same type, it constructs a vector. For non objects
     * or an empty vector, the type tag must be specified.
     */
public static func newMakeMoveVector(makeMoveVector: MakeMoveVector) -> Command  {
    return try!  FfiConverterTypeCommand_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_command_new_make_move_vector(
        FfiConverterTypeMakeMoveVector_lower(makeMoveVector),$0
    )
})
}
    
    /**
     * It merges n-coins into the first coin
     */
public static func newMergeCoins(mergeCoins: MergeCoins) -> Command  {
    return try!  FfiConverterTypeCommand_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_command_new_merge_coins(
        FfiConverterTypeMergeCoins_lower(mergeCoins),$0
    )
})
}
    
    /**
     * A call to either an entry or a public Move function
     */
public static func newMoveCall(moveCall: MoveCall) -> Command  {
    return try!  FfiConverterTypeCommand_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_command_new_move_call(
        FfiConverterTypeMoveCall_lower(moveCall),$0
    )
})
}
    
    /**
     * Publishes a Move package. It takes the package bytes and a list of the
     * package's transitive dependencies to link against on-chain.
     */
public static func newPublish(publish: Publish) -> Command  {
    return try!  FfiConverterTypeCommand_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_command_new_publish(
        FfiConverterTypePublish_lower(publish),$0
    )
})
}
    
    /**
     * It splits off some amounts into a new coins with those amounts
     */
public static func newSplitCoins(splitCoins: SplitCoins) -> Command  {
    return try!  FfiConverterTypeCommand_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_command_new_split_coins(
        FfiConverterTypeSplitCoins_lower(splitCoins),$0
    )
})
}
    
    /**
     * It sends n-objects to the specified address. These objects must have
     * store (public transfer) and either the previous owner must be an
     * address or the object must be newly created.
     */
public static func newTransferObjects(transferObjects: TransferObjects) -> Command  {
    return try!  FfiConverterTypeCommand_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_command_new_transfer_objects(
        FfiConverterTypeTransferObjects_lower(transferObjects),$0
    )
})
}
    
    /**
     * Upgrades a Move package
     * Takes (in order):
     * 1. A vector of serialized modules for the package.
     * 2. A vector of object ids for the transitive dependencies of the new
     * package.
     * 3. The object ID of the package being upgraded.
     * 4. An argument holding the `UpgradeTicket` that must have been produced
     * from an earlier command in the same programmable transaction.
     */
public static func newUpgrade(upgrade: Upgrade) -> Command  {
    return try!  FfiConverterTypeCommand_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_command_new_upgrade(
        FfiConverterTypeUpgrade_lower(upgrade),$0
    )
})
}
    

    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_command_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: Command, other: Command) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_command_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeCommand_lower(other),$0
    )
}
        )
    }

}
extension Command: CustomDebugStringConvertible {}
extension Command: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeCommand: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = Command

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> Command {
        return Command(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: Command) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Command {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: Command, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeCommand_lift(_ pointer: UnsafeMutableRawPointer) throws -> Command {
    return try FfiConverterTypeCommand.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeCommand_lower(_ value: Command) -> UnsafeMutableRawPointer {
    return FfiConverterTypeCommand.lower(value)
}






/**
 * V1 of the consensus commit prologue system transaction
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * consensus-commit-prologue-v1 = u64 u64 (option u64) u64 digest
 * consensus-determined-version-assignments
 * ```
 */
public protocol ConsensusCommitPrologueV1Protocol: AnyObject, Sendable {
    
    /**
     * Unix timestamp from consensus
     */
    func commitTimestampMs()  -> UInt64
    
    /**
     * Digest of consensus output
     */
    func consensusCommitDigest()  -> Digest
    
    /**
     * Stores consensus handler determined shared object version assignments.
     */
    func consensusDeterminedVersionAssignments()  -> ConsensusDeterminedVersionAssignments
    
    /**
     * Epoch of the commit prologue transaction
     */
    func epoch()  -> UInt64
    
    /**
     * Consensus round of the commit
     */
    func round()  -> UInt64
    
    /**
     * The sub DAG index of the consensus commit. This field will be populated
     * if there are multiple consensus commits per round.
     */
    func subDagIndex()  -> UInt64?
    
}
/**
 * V1 of the consensus commit prologue system transaction
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * consensus-commit-prologue-v1 = u64 u64 (option u64) u64 digest
 * consensus-determined-version-assignments
 * ```
 */
open class ConsensusCommitPrologueV1: ConsensusCommitPrologueV1Protocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_consensuscommitprologuev1(self.pointer, $0) }
    }
public convenience init(epoch: UInt64, round: UInt64, subDagIndex: UInt64?, commitTimestampMs: UInt64, consensusCommitDigest: Digest, consensusDeterminedVersionAssignments: ConsensusDeterminedVersionAssignments) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_consensuscommitprologuev1_new(
        FfiConverterUInt64.lower(epoch),
        FfiConverterUInt64.lower(round),
        FfiConverterOptionUInt64.lower(subDagIndex),
        FfiConverterUInt64.lower(commitTimestampMs),
        FfiConverterTypeDigest_lower(consensusCommitDigest),
        FfiConverterTypeConsensusDeterminedVersionAssignments_lower(consensusDeterminedVersionAssignments),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_consensuscommitprologuev1(pointer, $0) }
    }

    

    
    /**
     * Unix timestamp from consensus
     */
open func commitTimestampMs() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_consensuscommitprologuev1_commit_timestamp_ms(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Digest of consensus output
     */
open func consensusCommitDigest() -> Digest  {
    return try!  FfiConverterTypeDigest_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_consensuscommitprologuev1_consensus_commit_digest(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Stores consensus handler determined shared object version assignments.
     */
open func consensusDeterminedVersionAssignments() -> ConsensusDeterminedVersionAssignments  {
    return try!  FfiConverterTypeConsensusDeterminedVersionAssignments_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_consensuscommitprologuev1_consensus_determined_version_assignments(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Epoch of the commit prologue transaction
     */
open func epoch() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_consensuscommitprologuev1_epoch(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Consensus round of the commit
     */
open func round() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_consensuscommitprologuev1_round(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The sub DAG index of the consensus commit. This field will be populated
     * if there are multiple consensus commits per round.
     */
open func subDagIndex() -> UInt64?  {
    return try!  FfiConverterOptionUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_consensuscommitprologuev1_sub_dag_index(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_consensuscommitprologuev1_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: ConsensusCommitPrologueV1, other: ConsensusCommitPrologueV1) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_consensuscommitprologuev1_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeConsensusCommitPrologueV1_lower(other),$0
    )
}
        )
    }

}
extension ConsensusCommitPrologueV1: CustomDebugStringConvertible {}
extension ConsensusCommitPrologueV1: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeConsensusCommitPrologueV1: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = ConsensusCommitPrologueV1

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> ConsensusCommitPrologueV1 {
        return ConsensusCommitPrologueV1(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: ConsensusCommitPrologueV1) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ConsensusCommitPrologueV1 {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: ConsensusCommitPrologueV1, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeConsensusCommitPrologueV1_lift(_ pointer: UnsafeMutableRawPointer) throws -> ConsensusCommitPrologueV1 {
    return try FfiConverterTypeConsensusCommitPrologueV1.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeConsensusCommitPrologueV1_lower(_ value: ConsensusCommitPrologueV1) -> UnsafeMutableRawPointer {
    return FfiConverterTypeConsensusCommitPrologueV1.lower(value)
}






public protocol ConsensusDeterminedVersionAssignmentsProtocol: AnyObject, Sendable {
    
    func asCancelledTransactions()  -> [CancelledTransaction]
    
    func isCancelledTransactions()  -> Bool
    
}
open class ConsensusDeterminedVersionAssignments: ConsensusDeterminedVersionAssignmentsProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_consensusdeterminedversionassignments(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_consensusdeterminedversionassignments(pointer, $0) }
    }

    
public static func newCancelledTransactions(cancelledTransactions: [CancelledTransaction]) -> ConsensusDeterminedVersionAssignments  {
    return try!  FfiConverterTypeConsensusDeterminedVersionAssignments_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_consensusdeterminedversionassignments_new_cancelled_transactions(
        FfiConverterSequenceTypeCancelledTransaction.lower(cancelledTransactions),$0
    )
})
}
    

    
open func asCancelledTransactions() -> [CancelledTransaction]  {
    return try!  FfiConverterSequenceTypeCancelledTransaction.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_consensusdeterminedversionassignments_as_cancelled_transactions(self.uniffiClonePointer(),$0
    )
})
}
    
open func isCancelledTransactions() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_consensusdeterminedversionassignments_is_cancelled_transactions(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_consensusdeterminedversionassignments_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: ConsensusDeterminedVersionAssignments, other: ConsensusDeterminedVersionAssignments) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_consensusdeterminedversionassignments_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeConsensusDeterminedVersionAssignments_lower(other),$0
    )
}
        )
    }

}
extension ConsensusDeterminedVersionAssignments: CustomDebugStringConvertible {}
extension ConsensusDeterminedVersionAssignments: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeConsensusDeterminedVersionAssignments: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = ConsensusDeterminedVersionAssignments

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> ConsensusDeterminedVersionAssignments {
        return ConsensusDeterminedVersionAssignments(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: ConsensusDeterminedVersionAssignments) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ConsensusDeterminedVersionAssignments {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: ConsensusDeterminedVersionAssignments, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeConsensusDeterminedVersionAssignments_lift(_ pointer: UnsafeMutableRawPointer) throws -> ConsensusDeterminedVersionAssignments {
    return try FfiConverterTypeConsensusDeterminedVersionAssignments.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeConsensusDeterminedVersionAssignments_lower(_ value: ConsensusDeterminedVersionAssignments) -> UnsafeMutableRawPointer {
    return FfiConverterTypeConsensusDeterminedVersionAssignments.lower(value)
}






/**
 * A 32-byte Blake2b256 hash output.
 *
 * # BCS
 *
 * A `Digest`'s BCS serialized form is defined by the following:
 *
 * ```text
 * digest = %x20 32OCTET
 * ```
 *
 * Due to historical reasons, even though a `Digest` has a fixed-length of 32,
 * IOTA's binary representation of a `Digest` is prefixed with its length
 * meaning its serialized binary form (in bcs) is 33 bytes long vs a more
 * compact 32 bytes.
 */
public protocol DigestProtocol: AnyObject, Sendable {
    
    /**
     * Returns the next digest in byte-increasing order.
     */
    func nextLexicographical()  -> Digest
    
    func toBase58()  -> String
    
    func toBytes()  -> Data
    
}
/**
 * A 32-byte Blake2b256 hash output.
 *
 * # BCS
 *
 * A `Digest`'s BCS serialized form is defined by the following:
 *
 * ```text
 * digest = %x20 32OCTET
 * ```
 *
 * Due to historical reasons, even though a `Digest` has a fixed-length of 32,
 * IOTA's binary representation of a `Digest` is prefixed with its length
 * meaning its serialized binary form (in bcs) is 33 bytes long vs a more
 * compact 32 bytes.
 */
open class Digest: DigestProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_digest(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_digest(pointer, $0) }
    }

    
public static func fromBase58(base58: String)throws  -> Digest  {
    return try  FfiConverterTypeDigest_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_digest_from_base58(
        FfiConverterString.lower(base58),$0
    )
})
}
    
public static func fromBytes(bytes: Data)throws  -> Digest  {
    return try  FfiConverterTypeDigest_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_digest_from_bytes(
        FfiConverterData.lower(bytes),$0
    )
})
}
    
public static func generate() -> Digest  {
    return try!  FfiConverterTypeDigest_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_digest_generate($0
    )
})
}
    

    
    /**
     * Returns the next digest in byte-increasing order.
     */
open func nextLexicographical() -> Digest  {
    return try!  FfiConverterTypeDigest_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_digest_next_lexicographical(self.uniffiClonePointer(),$0
    )
})
}
    
open func toBase58() -> String  {
    return try!  FfiConverterString.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_digest_to_base58(self.uniffiClonePointer(),$0
    )
})
}
    
open func toBytes() -> Data  {
    return try!  FfiConverterData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_digest_to_bytes(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_digest_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    open var description: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_digest_uniffi_trait_display(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: Digest, other: Digest) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_digest_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeDigest_lower(other),$0
    )
}
        )
    }
    open func hash(into hasher: inout Hasher) {
        let val = try!  FfiConverterUInt64.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_digest_uniffi_trait_hash(self.uniffiClonePointer(),$0
    )
}
        )
        hasher.combine(val)
    }

}
extension Digest: CustomDebugStringConvertible {}
extension Digest: CustomStringConvertible {}
extension Digest: Equatable {}
extension Digest: Hashable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeDigest: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = Digest

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> Digest {
        return Digest(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: Digest) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Digest {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: Digest, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeDigest_lift(_ pointer: UnsafeMutableRawPointer) throws -> Digest {
    return try FfiConverterTypeDigest.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeDigest_lower(_ value: Digest) -> UnsafeMutableRawPointer {
    return FfiConverterTypeDigest.lower(value)
}






public protocol Ed25519PrivateKeyProtocol: AnyObject, Sendable {
    
    func publicKey()  -> Ed25519PublicKey
    
    func scheme()  -> SignatureScheme
    
    /**
     * Sign a personal message and return a UserSignature.
     */
    func signPersonalMessage(message: PersonalMessage) throws  -> UserSignature
    
    /**
     * Sign a transaction and return a UserSignature.
     */
    func signTransaction(transaction: Transaction) throws  -> UserSignature
    
    /**
     * Encode this private key as `flag || privkey` in Bech32 starting with
     * "iotaprivkey" to a string.
     */
    func toBech32() throws  -> String
    
    /**
     * Serialize this private key to bytes.
     */
    func toBytes()  -> Data
    
    /**
     * Serialize this private key as DER-encoded PKCS#8
     */
    func toDer() throws  -> Data
    
    /**
     * Serialize this private key as PEM-encoded PKCS#8
     */
    func toPem() throws  -> String
    
    func trySign(message: Data) throws  -> Ed25519Signature
    
    func trySignSimple(message: Data) throws  -> SimpleSignature
    
    func trySignUser(message: Data) throws  -> UserSignature
    
    func verifyingKey()  -> Ed25519VerifyingKey
    
}
open class Ed25519PrivateKey: Ed25519PrivateKeyProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_ed25519privatekey(self.pointer, $0) }
    }
public convenience init(bytes: Data)throws  {
    let pointer =
        try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_ed25519privatekey_new(
        FfiConverterData.lower(bytes),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_ed25519privatekey(pointer, $0) }
    }

    
    /**
     * Decode a private key from `flag || privkey` in Bech32 starting with
     * "iotaprivkey".
     */
public static func fromBech32(value: String)throws  -> Ed25519PrivateKey  {
    return try  FfiConverterTypeEd25519PrivateKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_ed25519privatekey_from_bech32(
        FfiConverterString.lower(value),$0
    )
})
}
    
    /**
     * Deserialize PKCS#8 private key from ASN.1 DER-encoded data (binary
     * format).
     */
public static func fromDer(bytes: Data)throws  -> Ed25519PrivateKey  {
    return try  FfiConverterTypeEd25519PrivateKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_ed25519privatekey_from_der(
        FfiConverterData.lower(bytes),$0
    )
})
}
    
    /**
     * Construct the private key from a mnemonic phrase
     */
public static func fromMnemonic(phrase: String, accountIndex: UInt64 = UInt64(0), password: String = "")throws  -> Ed25519PrivateKey  {
    return try  FfiConverterTypeEd25519PrivateKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_ed25519privatekey_from_mnemonic(
        FfiConverterString.lower(phrase),
        FfiConverterUInt64.lower(accountIndex),
        FfiConverterString.lower(password),$0
    )
})
}
    
    /**
     * Create an instance from a mnemonic phrase and a derivation path like
     * `"m/44'/4218'/0'/0'/0'"`
     */
public static func fromMnemonicWithPath(phrase: String, path: String, password: String = "")throws  -> Ed25519PrivateKey  {
    return try  FfiConverterTypeEd25519PrivateKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_ed25519privatekey_from_mnemonic_with_path(
        FfiConverterString.lower(phrase),
        FfiConverterString.lower(path),
        FfiConverterString.lower(password),$0
    )
})
}
    
    /**
     * Deserialize PKCS#8-encoded private key from PEM.
     */
public static func fromPem(s: String)throws  -> Ed25519PrivateKey  {
    return try  FfiConverterTypeEd25519PrivateKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_ed25519privatekey_from_pem(
        FfiConverterString.lower(s),$0
    )
})
}
    
public static func generate() -> Ed25519PrivateKey  {
    return try!  FfiConverterTypeEd25519PrivateKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_ed25519privatekey_generate($0
    )
})
}
    

    
open func publicKey() -> Ed25519PublicKey  {
    return try!  FfiConverterTypeEd25519PublicKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_ed25519privatekey_public_key(self.uniffiClonePointer(),$0
    )
})
}
    
open func scheme() -> SignatureScheme  {
    return try!  FfiConverterTypeSignatureScheme_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_ed25519privatekey_scheme(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Sign a personal message and return a UserSignature.
     */
open func signPersonalMessage(message: PersonalMessage)throws  -> UserSignature  {
    return try  FfiConverterTypeUserSignature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_ed25519privatekey_sign_personal_message(self.uniffiClonePointer(),
        FfiConverterTypePersonalMessage_lower(message),$0
    )
})
}
    
    /**
     * Sign a transaction and return a UserSignature.
     */
open func signTransaction(transaction: Transaction)throws  -> UserSignature  {
    return try  FfiConverterTypeUserSignature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_ed25519privatekey_sign_transaction(self.uniffiClonePointer(),
        FfiConverterTypeTransaction_lower(transaction),$0
    )
})
}
    
    /**
     * Encode this private key as `flag || privkey` in Bech32 starting with
     * "iotaprivkey" to a string.
     */
open func toBech32()throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_ed25519privatekey_to_bech32(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Serialize this private key to bytes.
     */
open func toBytes() -> Data  {
    return try!  FfiConverterData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_ed25519privatekey_to_bytes(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Serialize this private key as DER-encoded PKCS#8
     */
open func toDer()throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_ed25519privatekey_to_der(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Serialize this private key as PEM-encoded PKCS#8
     */
open func toPem()throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_ed25519privatekey_to_pem(self.uniffiClonePointer(),$0
    )
})
}
    
open func trySign(message: Data)throws  -> Ed25519Signature  {
    return try  FfiConverterTypeEd25519Signature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_ed25519privatekey_try_sign(self.uniffiClonePointer(),
        FfiConverterData.lower(message),$0
    )
})
}
    
open func trySignSimple(message: Data)throws  -> SimpleSignature  {
    return try  FfiConverterTypeSimpleSignature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_ed25519privatekey_try_sign_simple(self.uniffiClonePointer(),
        FfiConverterData.lower(message),$0
    )
})
}
    
open func trySignUser(message: Data)throws  -> UserSignature  {
    return try  FfiConverterTypeUserSignature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_ed25519privatekey_try_sign_user(self.uniffiClonePointer(),
        FfiConverterData.lower(message),$0
    )
})
}
    
open func verifyingKey() -> Ed25519VerifyingKey  {
    return try!  FfiConverterTypeEd25519VerifyingKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_ed25519privatekey_verifying_key(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_ed25519privatekey_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: Ed25519PrivateKey, other: Ed25519PrivateKey) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_ed25519privatekey_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeEd25519PrivateKey_lower(other),$0
    )
}
        )
    }

}
extension Ed25519PrivateKey: CustomDebugStringConvertible {}
extension Ed25519PrivateKey: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeEd25519PrivateKey: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = Ed25519PrivateKey

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> Ed25519PrivateKey {
        return Ed25519PrivateKey(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: Ed25519PrivateKey) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Ed25519PrivateKey {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: Ed25519PrivateKey, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeEd25519PrivateKey_lift(_ pointer: UnsafeMutableRawPointer) throws -> Ed25519PrivateKey {
    return try FfiConverterTypeEd25519PrivateKey.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeEd25519PrivateKey_lower(_ value: Ed25519PrivateKey) -> UnsafeMutableRawPointer {
    return FfiConverterTypeEd25519PrivateKey.lower(value)
}






/**
 * An ed25519 public key.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * ed25519-public-key = 32OCTET
 * ```
 */
public protocol Ed25519PublicKeyProtocol: AnyObject, Sendable {
    
    /**
     * Derive an `Address` from this Public Key
     *
     * An `Address` can be derived from an `Ed25519PublicKey` by hashing the
     * bytes of the public key with no prefix flag.
     *
     * `hash(32-byte ed25519 public key)`
     */
    func deriveAddress()  -> Address
    
    /**
     * Returns the signature scheme for this public key.
     */
    func scheme()  -> SignatureScheme
    
    func toBytes()  -> Data
    
    /**
     * Returns the bytes with signature scheme flag prepended.
     */
    func toFlaggedBytes()  -> Data
    
}
/**
 * An ed25519 public key.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * ed25519-public-key = 32OCTET
 * ```
 */
open class Ed25519PublicKey: Ed25519PublicKeyProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_ed25519publickey(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_ed25519publickey(pointer, $0) }
    }

    
public static func fromBytes(bytes: Data)throws  -> Ed25519PublicKey  {
    return try  FfiConverterTypeEd25519PublicKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_ed25519publickey_from_bytes(
        FfiConverterData.lower(bytes),$0
    )
})
}
    
public static func fromStr(s: String)throws  -> Ed25519PublicKey  {
    return try  FfiConverterTypeEd25519PublicKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_ed25519publickey_from_str(
        FfiConverterString.lower(s),$0
    )
})
}
    
public static func generate() -> Ed25519PublicKey  {
    return try!  FfiConverterTypeEd25519PublicKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_ed25519publickey_generate($0
    )
})
}
    

    
    /**
     * Derive an `Address` from this Public Key
     *
     * An `Address` can be derived from an `Ed25519PublicKey` by hashing the
     * bytes of the public key with no prefix flag.
     *
     * `hash(32-byte ed25519 public key)`
     */
open func deriveAddress() -> Address  {
    return try!  FfiConverterTypeAddress_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_ed25519publickey_derive_address(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Returns the signature scheme for this public key.
     */
open func scheme() -> SignatureScheme  {
    return try!  FfiConverterTypeSignatureScheme_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_ed25519publickey_scheme(self.uniffiClonePointer(),$0
    )
})
}
    
open func toBytes() -> Data  {
    return try!  FfiConverterData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_ed25519publickey_to_bytes(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Returns the bytes with signature scheme flag prepended.
     */
open func toFlaggedBytes() -> Data  {
    return try!  FfiConverterData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_ed25519publickey_to_flagged_bytes(self.uniffiClonePointer(),$0
    )
})
}
    
    public static func == (self: Ed25519PublicKey, other: Ed25519PublicKey) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_ed25519publickey_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeEd25519PublicKey_lower(other),$0
    )
}
        )
    }
    open func hash(into hasher: inout Hasher) {
        let val = try!  FfiConverterUInt64.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_ed25519publickey_uniffi_trait_hash(self.uniffiClonePointer(),$0
    )
}
        )
        hasher.combine(val)
    }

}
extension Ed25519PublicKey: Equatable {}
extension Ed25519PublicKey: Hashable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeEd25519PublicKey: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = Ed25519PublicKey

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> Ed25519PublicKey {
        return Ed25519PublicKey(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: Ed25519PublicKey) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Ed25519PublicKey {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: Ed25519PublicKey, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeEd25519PublicKey_lift(_ pointer: UnsafeMutableRawPointer) throws -> Ed25519PublicKey {
    return try FfiConverterTypeEd25519PublicKey.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeEd25519PublicKey_lower(_ value: Ed25519PublicKey) -> UnsafeMutableRawPointer {
    return FfiConverterTypeEd25519PublicKey.lower(value)
}






/**
 * An ed25519 signature.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * ed25519-signature = 64OCTET
 * ```
 */
public protocol Ed25519SignatureProtocol: AnyObject, Sendable {
    
    func toBytes()  -> Data
    
}
/**
 * An ed25519 signature.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * ed25519-signature = 64OCTET
 * ```
 */
open class Ed25519Signature: Ed25519SignatureProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_ed25519signature(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_ed25519signature(pointer, $0) }
    }

    
public static func fromBytes(bytes: Data)throws  -> Ed25519Signature  {
    return try  FfiConverterTypeEd25519Signature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_ed25519signature_from_bytes(
        FfiConverterData.lower(bytes),$0
    )
})
}
    
public static func fromStr(s: String)throws  -> Ed25519Signature  {
    return try  FfiConverterTypeEd25519Signature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_ed25519signature_from_str(
        FfiConverterString.lower(s),$0
    )
})
}
    
public static func generate() -> Ed25519Signature  {
    return try!  FfiConverterTypeEd25519Signature_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_ed25519signature_generate($0
    )
})
}
    

    
open func toBytes() -> Data  {
    return try!  FfiConverterData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_ed25519signature_to_bytes(self.uniffiClonePointer(),$0
    )
})
}
    
    public static func == (self: Ed25519Signature, other: Ed25519Signature) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_ed25519signature_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeEd25519Signature_lower(other),$0
    )
}
        )
    }
    open func hash(into hasher: inout Hasher) {
        let val = try!  FfiConverterUInt64.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_ed25519signature_uniffi_trait_hash(self.uniffiClonePointer(),$0
    )
}
        )
        hasher.combine(val)
    }

}
extension Ed25519Signature: Equatable {}
extension Ed25519Signature: Hashable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeEd25519Signature: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = Ed25519Signature

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> Ed25519Signature {
        return Ed25519Signature(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: Ed25519Signature) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Ed25519Signature {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: Ed25519Signature, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeEd25519Signature_lift(_ pointer: UnsafeMutableRawPointer) throws -> Ed25519Signature {
    return try FfiConverterTypeEd25519Signature.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeEd25519Signature_lower(_ value: Ed25519Signature) -> UnsafeMutableRawPointer {
    return FfiConverterTypeEd25519Signature.lower(value)
}






public protocol Ed25519VerifierProtocol: AnyObject, Sendable {
    
    func verifySimple(message: Data, signature: SimpleSignature) throws 
    
    func verifyUser(message: Data, signature: UserSignature) throws 
    
}
open class Ed25519Verifier: Ed25519VerifierProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_ed25519verifier(self.pointer, $0) }
    }
public convenience init() {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_ed25519verifier_new($0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_ed25519verifier(pointer, $0) }
    }

    

    
open func verifySimple(message: Data, signature: SimpleSignature)throws   {try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_ed25519verifier_verify_simple(self.uniffiClonePointer(),
        FfiConverterData.lower(message),
        FfiConverterTypeSimpleSignature_lower(signature),$0
    )
}
}
    
open func verifyUser(message: Data, signature: UserSignature)throws   {try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_ed25519verifier_verify_user(self.uniffiClonePointer(),
        FfiConverterData.lower(message),
        FfiConverterTypeUserSignature_lower(signature),$0
    )
}
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_ed25519verifier_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }

}
extension Ed25519Verifier: CustomDebugStringConvertible {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeEd25519Verifier: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = Ed25519Verifier

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> Ed25519Verifier {
        return Ed25519Verifier(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: Ed25519Verifier) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Ed25519Verifier {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: Ed25519Verifier, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeEd25519Verifier_lift(_ pointer: UnsafeMutableRawPointer) throws -> Ed25519Verifier {
    return try FfiConverterTypeEd25519Verifier.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeEd25519Verifier_lower(_ value: Ed25519Verifier) -> UnsafeMutableRawPointer {
    return FfiConverterTypeEd25519Verifier.lower(value)
}






public protocol Ed25519VerifyingKeyProtocol: AnyObject, Sendable {
    
    func publicKey()  -> Ed25519PublicKey
    
    /**
     * Serialize this public key as DER-encoded data
     */
    func toDer() throws  -> Data
    
    /**
     * Serialize this public key into PEM format
     */
    func toPem() throws  -> String
    
    func verify(message: Data, signature: Ed25519Signature) throws 
    
    func verifySimple(message: Data, signature: SimpleSignature) throws 
    
    func verifyUser(message: Data, signature: UserSignature) throws 
    
}
open class Ed25519VerifyingKey: Ed25519VerifyingKeyProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_ed25519verifyingkey(self.pointer, $0) }
    }
public convenience init(publicKey: Ed25519PublicKey)throws  {
    let pointer =
        try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_ed25519verifyingkey_new(
        FfiConverterTypeEd25519PublicKey_lower(publicKey),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_ed25519verifyingkey(pointer, $0) }
    }

    
    /**
     * Deserialize public key from ASN.1 DER-encoded data (binary format).
     */
public static func fromDer(bytes: Data)throws  -> Ed25519VerifyingKey  {
    return try  FfiConverterTypeEd25519VerifyingKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_ed25519verifyingkey_from_der(
        FfiConverterData.lower(bytes),$0
    )
})
}
    
    /**
     * Deserialize public key from PEM.
     */
public static func fromPem(s: String)throws  -> Ed25519VerifyingKey  {
    return try  FfiConverterTypeEd25519VerifyingKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_ed25519verifyingkey_from_pem(
        FfiConverterString.lower(s),$0
    )
})
}
    

    
open func publicKey() -> Ed25519PublicKey  {
    return try!  FfiConverterTypeEd25519PublicKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_ed25519verifyingkey_public_key(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Serialize this public key as DER-encoded data
     */
open func toDer()throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_ed25519verifyingkey_to_der(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Serialize this public key into PEM format
     */
open func toPem()throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_ed25519verifyingkey_to_pem(self.uniffiClonePointer(),$0
    )
})
}
    
open func verify(message: Data, signature: Ed25519Signature)throws   {try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_ed25519verifyingkey_verify(self.uniffiClonePointer(),
        FfiConverterData.lower(message),
        FfiConverterTypeEd25519Signature_lower(signature),$0
    )
}
}
    
open func verifySimple(message: Data, signature: SimpleSignature)throws   {try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_ed25519verifyingkey_verify_simple(self.uniffiClonePointer(),
        FfiConverterData.lower(message),
        FfiConverterTypeSimpleSignature_lower(signature),$0
    )
}
}
    
open func verifyUser(message: Data, signature: UserSignature)throws   {try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_ed25519verifyingkey_verify_user(self.uniffiClonePointer(),
        FfiConverterData.lower(message),
        FfiConverterTypeUserSignature_lower(signature),$0
    )
}
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_ed25519verifyingkey_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: Ed25519VerifyingKey, other: Ed25519VerifyingKey) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_ed25519verifyingkey_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeEd25519VerifyingKey_lower(other),$0
    )
}
        )
    }

}
extension Ed25519VerifyingKey: CustomDebugStringConvertible {}
extension Ed25519VerifyingKey: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeEd25519VerifyingKey: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = Ed25519VerifyingKey

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> Ed25519VerifyingKey {
        return Ed25519VerifyingKey(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: Ed25519VerifyingKey) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Ed25519VerifyingKey {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: Ed25519VerifyingKey, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeEd25519VerifyingKey_lift(_ pointer: UnsafeMutableRawPointer) throws -> Ed25519VerifyingKey {
    return try FfiConverterTypeEd25519VerifyingKey.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeEd25519VerifyingKey_lower(_ value: Ed25519VerifyingKey) -> UnsafeMutableRawPointer {
    return FfiConverterTypeEd25519VerifyingKey.lower(value)
}






/**
 * Operation run at the end of an epoch
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * end-of-epoch-transaction-kind   =  eoe-change-epoch
 * =/ eoe-authenticator-state-create
 * =/ eoe-authenticator-state-expire
 * =/ eoe-randomness-state-create
 * =/ eoe-deny-list-state-create
 * =/ eoe-bridge-state-create
 * =/ eoe-bridge-committee-init
 * =/ eoe-store-execution-time-observations
 *
 * eoe-change-epoch                = %x00 change-epoch
 * eoe-authenticator-state-create  = %x01
 * eoe-authenticator-state-expire  = %x02 authenticator-state-expire
 * eoe-randomness-state-create     = %x03
 * eoe-deny-list-state-create      = %x04
 * eoe-bridge-state-create         = %x05 digest
 * eoe-bridge-committee-init       = %x06 u64
 * eoe-store-execution-time-observations = %x07 stored-execution-time-observations
 * ```
 */
public protocol EndOfEpochTransactionKindProtocol: AnyObject, Sendable {
    
}
/**
 * Operation run at the end of an epoch
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * end-of-epoch-transaction-kind   =  eoe-change-epoch
 * =/ eoe-authenticator-state-create
 * =/ eoe-authenticator-state-expire
 * =/ eoe-randomness-state-create
 * =/ eoe-deny-list-state-create
 * =/ eoe-bridge-state-create
 * =/ eoe-bridge-committee-init
 * =/ eoe-store-execution-time-observations
 *
 * eoe-change-epoch                = %x00 change-epoch
 * eoe-authenticator-state-create  = %x01
 * eoe-authenticator-state-expire  = %x02 authenticator-state-expire
 * eoe-randomness-state-create     = %x03
 * eoe-deny-list-state-create      = %x04
 * eoe-bridge-state-create         = %x05 digest
 * eoe-bridge-committee-init       = %x06 u64
 * eoe-store-execution-time-observations = %x07 stored-execution-time-observations
 * ```
 */
open class EndOfEpochTransactionKind: EndOfEpochTransactionKindProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_endofepochtransactionkind(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_endofepochtransactionkind(pointer, $0) }
    }

    
public static func newAuthenticatorStateCreate() -> EndOfEpochTransactionKind  {
    return try!  FfiConverterTypeEndOfEpochTransactionKind_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_endofepochtransactionkind_new_authenticator_state_create($0
    )
})
}
    
public static func newAuthenticatorStateExpire(tx: AuthenticatorStateExpire) -> EndOfEpochTransactionKind  {
    return try!  FfiConverterTypeEndOfEpochTransactionKind_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_endofepochtransactionkind_new_authenticator_state_expire(
        FfiConverterTypeAuthenticatorStateExpire_lower(tx),$0
    )
})
}
    
public static func newChangeEpoch(tx: ChangeEpoch) -> EndOfEpochTransactionKind  {
    return try!  FfiConverterTypeEndOfEpochTransactionKind_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_endofepochtransactionkind_new_change_epoch(
        FfiConverterTypeChangeEpoch_lower(tx),$0
    )
})
}
    
public static func newChangeEpochV2(tx: ChangeEpochV2) -> EndOfEpochTransactionKind  {
    return try!  FfiConverterTypeEndOfEpochTransactionKind_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_endofepochtransactionkind_new_change_epoch_v2(
        FfiConverterTypeChangeEpochV2_lower(tx),$0
    )
})
}
    
public static func newChangeEpochV3(tx: ChangeEpochV3) -> EndOfEpochTransactionKind  {
    return try!  FfiConverterTypeEndOfEpochTransactionKind_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_endofepochtransactionkind_new_change_epoch_v3(
        FfiConverterTypeChangeEpochV3_lower(tx),$0
    )
})
}
    
public static func newChangeEpochV4(tx: ChangeEpochV4) -> EndOfEpochTransactionKind  {
    return try!  FfiConverterTypeEndOfEpochTransactionKind_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_endofepochtransactionkind_new_change_epoch_v4(
        FfiConverterTypeChangeEpochV4_lower(tx),$0
    )
})
}
    

    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_endofepochtransactionkind_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: EndOfEpochTransactionKind, other: EndOfEpochTransactionKind) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_endofepochtransactionkind_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeEndOfEpochTransactionKind_lower(other),$0
    )
}
        )
    }

}
extension EndOfEpochTransactionKind: CustomDebugStringConvertible {}
extension EndOfEpochTransactionKind: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeEndOfEpochTransactionKind: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = EndOfEpochTransactionKind

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> EndOfEpochTransactionKind {
        return EndOfEpochTransactionKind(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: EndOfEpochTransactionKind) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> EndOfEpochTransactionKind {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: EndOfEpochTransactionKind, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeEndOfEpochTransactionKind_lift(_ pointer: UnsafeMutableRawPointer) throws -> EndOfEpochTransactionKind {
    return try FfiConverterTypeEndOfEpochTransactionKind.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeEndOfEpochTransactionKind_lower(_ value: EndOfEpochTransactionKind) -> UnsafeMutableRawPointer {
    return FfiConverterTypeEndOfEpochTransactionKind.lower(value)
}






public protocol ExecutionTimeObservationProtocol: AnyObject, Sendable {
    
    func key()  -> ExecutionTimeObservationKey
    
    func observations()  -> [ValidatorExecutionTimeObservation]
    
}
open class ExecutionTimeObservation: ExecutionTimeObservationProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_executiontimeobservation(self.pointer, $0) }
    }
public convenience init(key: ExecutionTimeObservationKey, observations: [ValidatorExecutionTimeObservation]) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_executiontimeobservation_new(
        FfiConverterTypeExecutionTimeObservationKey_lower(key),
        FfiConverterSequenceTypeValidatorExecutionTimeObservation.lower(observations),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_executiontimeobservation(pointer, $0) }
    }

    

    
open func key() -> ExecutionTimeObservationKey  {
    return try!  FfiConverterTypeExecutionTimeObservationKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_executiontimeobservation_key(self.uniffiClonePointer(),$0
    )
})
}
    
open func observations() -> [ValidatorExecutionTimeObservation]  {
    return try!  FfiConverterSequenceTypeValidatorExecutionTimeObservation.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_executiontimeobservation_observations(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_executiontimeobservation_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: ExecutionTimeObservation, other: ExecutionTimeObservation) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_executiontimeobservation_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeExecutionTimeObservation_lower(other),$0
    )
}
        )
    }
    open func hash(into hasher: inout Hasher) {
        let val = try!  FfiConverterUInt64.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_executiontimeobservation_uniffi_trait_hash(self.uniffiClonePointer(),$0
    )
}
        )
        hasher.combine(val)
    }

}
extension ExecutionTimeObservation: CustomDebugStringConvertible {}
extension ExecutionTimeObservation: Equatable {}
extension ExecutionTimeObservation: Hashable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeExecutionTimeObservation: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = ExecutionTimeObservation

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> ExecutionTimeObservation {
        return ExecutionTimeObservation(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: ExecutionTimeObservation) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ExecutionTimeObservation {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: ExecutionTimeObservation, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeExecutionTimeObservation_lift(_ pointer: UnsafeMutableRawPointer) throws -> ExecutionTimeObservation {
    return try FfiConverterTypeExecutionTimeObservation.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeExecutionTimeObservation_lower(_ value: ExecutionTimeObservation) -> UnsafeMutableRawPointer {
    return FfiConverterTypeExecutionTimeObservation.lower(value)
}






/**
 * Key for an execution time observation
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * execution-time-observation-key  =  %x00 move-entry-point
 * =/ %x01 ; transfer-objects
 * =/ %x02 ; split-coins
 * =/ %x03 ; merge-coins
 * =/ %x04 ; publish
 * =/ %x05 ; make-move-vec
 * =/ %x06 ; upgrade
 *
 * move-entry-point = object-id string string (vec type-tag)
 * ```
 */
public protocol ExecutionTimeObservationKeyProtocol: AnyObject, Sendable {
    
}
/**
 * Key for an execution time observation
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * execution-time-observation-key  =  %x00 move-entry-point
 * =/ %x01 ; transfer-objects
 * =/ %x02 ; split-coins
 * =/ %x03 ; merge-coins
 * =/ %x04 ; publish
 * =/ %x05 ; make-move-vec
 * =/ %x06 ; upgrade
 *
 * move-entry-point = object-id string string (vec type-tag)
 * ```
 */
open class ExecutionTimeObservationKey: ExecutionTimeObservationKeyProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_executiontimeobservationkey(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_executiontimeobservationkey(pointer, $0) }
    }

    
public static func newMakeMoveVec() -> ExecutionTimeObservationKey  {
    return try!  FfiConverterTypeExecutionTimeObservationKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_executiontimeobservationkey_new_make_move_vec($0
    )
})
}
    
public static func newMergeCoins() -> ExecutionTimeObservationKey  {
    return try!  FfiConverterTypeExecutionTimeObservationKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_executiontimeobservationkey_new_merge_coins($0
    )
})
}
    
public static func newMoveEntryPoint(package: ObjectId, module: String, function: String, typeArguments: [TypeTag]) -> ExecutionTimeObservationKey  {
    return try!  FfiConverterTypeExecutionTimeObservationKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_executiontimeobservationkey_new_move_entry_point(
        FfiConverterTypeObjectId_lower(package),
        FfiConverterString.lower(module),
        FfiConverterString.lower(function),
        FfiConverterSequenceTypeTypeTag.lower(typeArguments),$0
    )
})
}
    
public static func newPublish() -> ExecutionTimeObservationKey  {
    return try!  FfiConverterTypeExecutionTimeObservationKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_executiontimeobservationkey_new_publish($0
    )
})
}
    
public static func newSplitCoins() -> ExecutionTimeObservationKey  {
    return try!  FfiConverterTypeExecutionTimeObservationKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_executiontimeobservationkey_new_split_coins($0
    )
})
}
    
public static func newTransferObjects() -> ExecutionTimeObservationKey  {
    return try!  FfiConverterTypeExecutionTimeObservationKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_executiontimeobservationkey_new_transfer_objects($0
    )
})
}
    
public static func newUpgrade() -> ExecutionTimeObservationKey  {
    return try!  FfiConverterTypeExecutionTimeObservationKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_executiontimeobservationkey_new_upgrade($0
    )
})
}
    

    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_executiontimeobservationkey_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: ExecutionTimeObservationKey, other: ExecutionTimeObservationKey) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_executiontimeobservationkey_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeExecutionTimeObservationKey_lower(other),$0
    )
}
        )
    }
    open func hash(into hasher: inout Hasher) {
        let val = try!  FfiConverterUInt64.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_executiontimeobservationkey_uniffi_trait_hash(self.uniffiClonePointer(),$0
    )
}
        )
        hasher.combine(val)
    }

}
extension ExecutionTimeObservationKey: CustomDebugStringConvertible {}
extension ExecutionTimeObservationKey: Equatable {}
extension ExecutionTimeObservationKey: Hashable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeExecutionTimeObservationKey: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = ExecutionTimeObservationKey

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> ExecutionTimeObservationKey {
        return ExecutionTimeObservationKey(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: ExecutionTimeObservationKey) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ExecutionTimeObservationKey {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: ExecutionTimeObservationKey, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeExecutionTimeObservationKey_lift(_ pointer: UnsafeMutableRawPointer) throws -> ExecutionTimeObservationKey {
    return try FfiConverterTypeExecutionTimeObservationKey.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeExecutionTimeObservationKey_lower(_ value: ExecutionTimeObservationKey) -> UnsafeMutableRawPointer {
    return FfiConverterTypeExecutionTimeObservationKey.lower(value)
}






/**
 * Set of Execution Time Observations from the committee.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * stored-execution-time-observations =  %x00 v1-stored-execution-time-observations
 *
 * v1-stored-execution-time-observations = (vec
 * execution-time-observation-key
 * (vec execution-time-observation)
 * )
 * ```
 */
public protocol ExecutionTimeObservationsProtocol: AnyObject, Sendable {
    
}
/**
 * Set of Execution Time Observations from the committee.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * stored-execution-time-observations =  %x00 v1-stored-execution-time-observations
 *
 * v1-stored-execution-time-observations = (vec
 * execution-time-observation-key
 * (vec execution-time-observation)
 * )
 * ```
 */
open class ExecutionTimeObservations: ExecutionTimeObservationsProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_executiontimeobservations(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_executiontimeobservations(pointer, $0) }
    }

    
public static func newV1(executionTimeObservations: [ExecutionTimeObservation]) -> ExecutionTimeObservations  {
    return try!  FfiConverterTypeExecutionTimeObservations_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_executiontimeobservations_new_v1(
        FfiConverterSequenceTypeExecutionTimeObservation.lower(executionTimeObservations),$0
    )
})
}
    

    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_executiontimeobservations_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: ExecutionTimeObservations, other: ExecutionTimeObservations) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_executiontimeobservations_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeExecutionTimeObservations_lower(other),$0
    )
}
        )
    }
    open func hash(into hasher: inout Hasher) {
        let val = try!  FfiConverterUInt64.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_executiontimeobservations_uniffi_trait_hash(self.uniffiClonePointer(),$0
    )
}
        )
        hasher.combine(val)
    }

}
extension ExecutionTimeObservations: CustomDebugStringConvertible {}
extension ExecutionTimeObservations: Equatable {}
extension ExecutionTimeObservations: Hashable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeExecutionTimeObservations: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = ExecutionTimeObservations

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> ExecutionTimeObservations {
        return ExecutionTimeObservations(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: ExecutionTimeObservations) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ExecutionTimeObservations {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: ExecutionTimeObservations, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeExecutionTimeObservations_lift(_ pointer: UnsafeMutableRawPointer) throws -> ExecutionTimeObservations {
    return try FfiConverterTypeExecutionTimeObservations.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeExecutionTimeObservations_lower(_ value: ExecutionTimeObservations) -> UnsafeMutableRawPointer {
    return FfiConverterTypeExecutionTimeObservations.lower(value)
}






public protocol FaucetClientProtocol: AnyObject, Sendable {
    
    /**
     * Request gas from the faucet. Note that this will return the UUID of the
     * request and not wait until the token is received. Use
     * `request_and_wait` to wait for the token.
     */
    func request(address: Address) async throws  -> String?
    
    /**
     * Request gas from the faucet and wait until the request is completed and
     * token is transferred. Returns `FaucetReceipt` if the request is
     * successful, which contains the list of tokens transferred, and the
     * transaction digest.
     *
     * Note that the faucet is heavily rate-limited, so calling repeatedly the
     * faucet would likely result in a 429 code or 502 code.
     *
     * If you intend to use the transferred tokens with the graphql client,
     * consider using `request_and_wait_for_finalized` instead to ensure
     * the tokens are available in the indexer.
     */
    func requestAndWait(address: Address) async throws  -> FaucetReceipt?
    
    /**
     * Request gas from the faucet and wait until the request is completed and
     * token is transferred and finalized on the ledger. Returns
     * `FaucetReceipt` if the request is successful, which contains the
     * list of tokens transferred, and the transaction digest.
     *
     * This is a convenience method that combines `request_and_wait` and
     * waiting for the funding transactions to be finalized using the provided
     * GraphQL `Client`.
     */
    func requestAndWaitForFinalized(address: Address, client: GraphQlClient) async throws  -> FaucetReceipt?
    
    /**
     * Check the faucet request status.
     *
     * Possible statuses are defined in: `BatchSendStatusType`
     */
    func requestStatus(id: String) async throws  -> BatchSendStatus?
    
}
open class FaucetClient: FaucetClientProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_faucetclient(self.pointer, $0) }
    }
    /**
     * Construct a new `FaucetClient` with the given faucet service URL. This
     * `FaucetClient` expects that the service provides two endpoints:
     * /v1/gas and /v1/status. As such, do not provide the request
     * endpoint, just the top level service endpoint.
     *
     * - /v1/gas is used to request gas
     * - /v1/status/task-uuid is used to check the status of the request
     */
public convenience init(faucetUrl: String) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_faucetclient_new(
        FfiConverterString.lower(faucetUrl),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_faucetclient(pointer, $0) }
    }

    
    /**
     * Create a new Faucet client connected to the `devnet` faucet.
     */
public static func newDevnet() -> FaucetClient  {
    return try!  FfiConverterTypeFaucetClient_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_faucetclient_new_devnet($0
    )
})
}
    
    /**
     * Create a new Faucet client connected to a `localnet` faucet.
     */
public static func newLocalnet() -> FaucetClient  {
    return try!  FfiConverterTypeFaucetClient_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_faucetclient_new_localnet($0
    )
})
}
    
    /**
     * Create a new Faucet client connected to the `testnet` faucet.
     */
public static func newTestnet() -> FaucetClient  {
    return try!  FfiConverterTypeFaucetClient_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_faucetclient_new_testnet($0
    )
})
}
    

    
    /**
     * Request gas from the faucet. Note that this will return the UUID of the
     * request and not wait until the token is received. Use
     * `request_and_wait` to wait for the token.
     */
open func request(address: Address)async throws  -> String?  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_faucetclient_request(
                    self.uniffiClonePointer(),
                    FfiConverterTypeAddress_lower(address)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterOptionString.lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Request gas from the faucet and wait until the request is completed and
     * token is transferred. Returns `FaucetReceipt` if the request is
     * successful, which contains the list of tokens transferred, and the
     * transaction digest.
     *
     * Note that the faucet is heavily rate-limited, so calling repeatedly the
     * faucet would likely result in a 429 code or 502 code.
     *
     * If you intend to use the transferred tokens with the graphql client,
     * consider using `request_and_wait_for_finalized` instead to ensure
     * the tokens are available in the indexer.
     */
open func requestAndWait(address: Address)async throws  -> FaucetReceipt?  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_faucetclient_request_and_wait(
                    self.uniffiClonePointer(),
                    FfiConverterTypeAddress_lower(address)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterOptionTypeFaucetReceipt.lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Request gas from the faucet and wait until the request is completed and
     * token is transferred and finalized on the ledger. Returns
     * `FaucetReceipt` if the request is successful, which contains the
     * list of tokens transferred, and the transaction digest.
     *
     * This is a convenience method that combines `request_and_wait` and
     * waiting for the funding transactions to be finalized using the provided
     * GraphQL `Client`.
     */
open func requestAndWaitForFinalized(address: Address, client: GraphQlClient)async throws  -> FaucetReceipt?  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_faucetclient_request_and_wait_for_finalized(
                    self.uniffiClonePointer(),
                    FfiConverterTypeAddress_lower(address),FfiConverterTypeGraphQLClient_lower(client)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterOptionTypeFaucetReceipt.lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Check the faucet request status.
     *
     * Possible statuses are defined in: `BatchSendStatusType`
     */
open func requestStatus(id: String)async throws  -> BatchSendStatus?  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_faucetclient_request_status(
                    self.uniffiClonePointer(),
                    FfiConverterString.lower(id)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterOptionTypeBatchSendStatus.lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    

}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeFaucetClient: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = FaucetClient

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> FaucetClient {
        return FaucetClient(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: FaucetClient) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> FaucetClient {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: FaucetClient, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeFaucetClient_lift(_ pointer: UnsafeMutableRawPointer) throws -> FaucetClient {
    return try FfiConverterTypeFaucetClient.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeFaucetClient_lower(_ value: FaucetClient) -> UnsafeMutableRawPointer {
    return FfiConverterTypeFaucetClient.lower(value)
}






/**
 * An object part of the initial chain state
 *
 * `GenesisObject`'s are included as a part of genesis, the initial
 * checkpoint/transaction, that initializes the state of the blockchain.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * genesis-object = object-data owner
 * ```
 */
public protocol GenesisObjectProtocol: AnyObject, Sendable {
    
    func data()  -> ObjectData
    
    func objectId()  -> ObjectId
    
    func objectType()  -> ObjectType
    
    func owner()  -> Owner
    
    func version()  -> UInt64
    
}
/**
 * An object part of the initial chain state
 *
 * `GenesisObject`'s are included as a part of genesis, the initial
 * checkpoint/transaction, that initializes the state of the blockchain.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * genesis-object = object-data owner
 * ```
 */
open class GenesisObject: GenesisObjectProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_genesisobject(self.pointer, $0) }
    }
public convenience init(data: ObjectData, owner: Owner) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_genesisobject_new(
        FfiConverterTypeObjectData_lower(data),
        FfiConverterTypeOwner_lower(owner),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_genesisobject(pointer, $0) }
    }

    

    
open func data() -> ObjectData  {
    return try!  FfiConverterTypeObjectData_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_genesisobject_data(self.uniffiClonePointer(),$0
    )
})
}
    
open func objectId() -> ObjectId  {
    return try!  FfiConverterTypeObjectId_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_genesisobject_object_id(self.uniffiClonePointer(),$0
    )
})
}
    
open func objectType() -> ObjectType  {
    return try!  FfiConverterTypeObjectType_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_genesisobject_object_type(self.uniffiClonePointer(),$0
    )
})
}
    
open func owner() -> Owner  {
    return try!  FfiConverterTypeOwner_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_genesisobject_owner(self.uniffiClonePointer(),$0
    )
})
}
    
open func version() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_genesisobject_version(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_genesisobject_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: GenesisObject, other: GenesisObject) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_genesisobject_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeGenesisObject_lower(other),$0
    )
}
        )
    }

}
extension GenesisObject: CustomDebugStringConvertible {}
extension GenesisObject: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeGenesisObject: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = GenesisObject

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> GenesisObject {
        return GenesisObject(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: GenesisObject) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> GenesisObject {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: GenesisObject, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeGenesisObject_lift(_ pointer: UnsafeMutableRawPointer) throws -> GenesisObject {
    return try FfiConverterTypeGenesisObject.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeGenesisObject_lower(_ value: GenesisObject) -> UnsafeMutableRawPointer {
    return FfiConverterTypeGenesisObject.lower(value)
}






/**
 * The genesis transaction
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * genesis-transaction = (vector genesis-object)
 * ```
 */
public protocol GenesisTransactionProtocol: AnyObject, Sendable {
    
    func events()  -> [Event]
    
    func objects()  -> [GenesisObject]
    
}
/**
 * The genesis transaction
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * genesis-transaction = (vector genesis-object)
 * ```
 */
open class GenesisTransaction: GenesisTransactionProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_genesistransaction(self.pointer, $0) }
    }
public convenience init(objects: [GenesisObject], events: [Event]) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_genesistransaction_new(
        FfiConverterSequenceTypeGenesisObject.lower(objects),
        FfiConverterSequenceTypeEvent.lower(events),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_genesistransaction(pointer, $0) }
    }

    

    
open func events() -> [Event]  {
    return try!  FfiConverterSequenceTypeEvent.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_genesistransaction_events(self.uniffiClonePointer(),$0
    )
})
}
    
open func objects() -> [GenesisObject]  {
    return try!  FfiConverterSequenceTypeGenesisObject.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_genesistransaction_objects(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_genesistransaction_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: GenesisTransaction, other: GenesisTransaction) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_genesistransaction_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeGenesisTransaction_lower(other),$0
    )
}
        )
    }

}
extension GenesisTransaction: CustomDebugStringConvertible {}
extension GenesisTransaction: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeGenesisTransaction: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = GenesisTransaction

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> GenesisTransaction {
        return GenesisTransaction(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: GenesisTransaction) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> GenesisTransaction {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: GenesisTransaction, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeGenesisTransaction_lift(_ pointer: UnsafeMutableRawPointer) throws -> GenesisTransaction {
    return try FfiConverterTypeGenesisTransaction.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeGenesisTransaction_lower(_ value: GenesisTransaction) -> UnsafeMutableRawPointer {
    return FfiConverterTypeGenesisTransaction.lower(value)
}






/**
 * The GraphQL client for interacting with the IOTA blockchain.
 */
public protocol GraphQlClientProtocol: AnyObject, Sendable {
    
    /**
     * Get the list of active validators for the provided epoch, including
     * related metadata. If no epoch is provided, it will return the active
     * validators for the current epoch.
     */
    func activeValidators(epoch: UInt64?, paginationFilter: PaginationFilter?) async throws  -> ValidatorPage
    
    /**
     * Get the balance of all the coins owned by address for the provided coin
     * type. Coin type will default to `0x2::coin::Coin<0x2::iota::IOTA>`
     * if not provided.
     */
    func balance(address: Address, coinType: String?) async throws  -> UInt64?
    
    /**
     * Get the chain identifier.
     */
    func chainId() async throws  -> String
    
    /**
     * Get the `CheckpointSummary` for a given checkpoint digest or
     * checkpoint id. If none is provided, it will use the last known
     * checkpoint id.
     */
    func checkpoint(digest: Digest?, seqNum: UInt64?) async throws  -> CheckpointSummary?
    
    /**
     * Get a page of `CheckpointSummary` for the provided parameters.
     */
    func checkpoints(paginationFilter: PaginationFilter?) async throws  -> CheckpointSummaryPage
    
    /**
     * Get the coin metadata for the coin type.
     */
    func coinMetadata(coinType: String) async throws  -> CoinMetadata?
    
    /**
     * Get the list of coins for the specified address.
     *
     * If `coin_type` is not provided, all coins will be returned. For IOTA
     * coins, pass in the coin type: `0x2::iota::IOTA`.
     */
    func coins(owner: Address, paginationFilter: PaginationFilter?, coinType: StructTag?) async throws  -> CoinPage
    
    /**
     * Dry run a `Transaction` and return the transaction effects and dry run
     * error (if any).
     *
     * `skipChecks` optional flag disables the usual verification checks that
     * prevent access to objects that are owned by addresses other than the
     * sender, and calling non-public, non-entry functions, and some other
     * checks. Defaults to false.
     */
    func dryRunTx(tx: Transaction, skipChecks: Bool) async throws  -> DryRunResult
    
    /**
     * Dry run a `TransactionKind` and return the transaction effects and dry
     * run error (if any).
     *
     * `skipChecks` optional flag disables the usual verification checks that
     * prevent access to objects that are owned by addresses other than the
     * sender, and calling non-public, non-entry functions, and some other
     * checks. Defaults to false.
     *
     * `tx_meta` is the transaction metadata.
     */
    func dryRunTxKind(txKind: TransactionKind, txMeta: TransactionMetadata, skipChecks: Bool) async throws  -> DryRunResult
    
    /**
     * Access a dynamic field on an object using its name. Names are arbitrary
     * Move values whose type have copy, drop, and store, and are specified
     * using their type, and their BCS contents, Base64 encoded.
     *
     * The `name` argument is a json serialized type.
     *
     * This returns `DynamicFieldOutput` which contains the name, the value
     * as json, and object.
     */
    func dynamicField(address: Address, typeTag: TypeTag, name: Value) async throws  -> DynamicFieldOutput?
    
    /**
     * Get a page of dynamic fields for the provided address. Note that this
     * will also fetch dynamic fields on wrapped objects.
     *
     * This returns a page of `DynamicFieldOutput`s.
     */
    func dynamicFields(address: Address, paginationFilter: PaginationFilter?) async throws  -> DynamicFieldOutputPage
    
    /**
     * Access a dynamic object field on an object using its name. Names are
     * arbitrary Move values whose type have copy, drop, and store, and are
     * specified using their type, and their BCS contents, Base64 encoded.
     *
     * The `name` argument is a json serialized type.
     *
     * This returns `DynamicFieldOutput` which contains the name, the value
     * as json, and object.
     */
    func dynamicObjectField(address: Address, typeTag: TypeTag, name: Value) async throws  -> DynamicFieldOutput?
    
    /**
     * Return the epoch information for the provided epoch. If no epoch is
     * provided, it will return the last known epoch.
     */
    func epoch(epoch: UInt64?) async throws  -> Epoch?
    
    /**
     * Return the number of checkpoints in this epoch. This will return
     * `Ok(None)` if the epoch requested is not available in the GraphQL
     * service (e.g., due to pruning).
     */
    func epochTotalCheckpoints(epoch: UInt64?) async throws  -> UInt64?
    
    /**
     * Return the number of transaction blocks in this epoch. This will return
     * `Ok(None)` if the epoch requested is not available in the GraphQL
     * service (e.g., due to pruning).
     */
    func epochTotalTransactionBlocks(epoch: UInt64?) async throws  -> UInt64?
    
    /**
     * Return a page of tuple (event, transaction digest) based on the
     * (optional) event filter.
     */
    func events(filter: EventFilter?, paginationFilter: PaginationFilter?) async throws  -> EventPage
    
    /**
     * Execute a transaction.
     */
    func executeTx(signatures: [UserSignature], tx: Transaction, waitFor: WaitForTx?) async throws  -> TransactionEffects
    
    /**
     * Get the list of gas coins for the specified address.
     */
    func gasCoins(owner: Address, paginationFilter: PaginationFilter?) async throws  -> CoinPage
    
    /**
     * Get the default name pointing to this address, if one exists.
     */
    func iotaNamesDefaultName(address: Address, format: NameFormat?) async throws  -> Name?
    
    /**
     * Return the resolved address for the given name.
     */
    func iotaNamesLookup(name: String) async throws  -> Address?
    
    /**
     * Find all registration NFTs for the given address.
     */
    func iotaNamesRegistrations(address: Address, paginationFilter: PaginationFilter) async throws  -> NameRegistrationPage
    
    /**
     * Returns whether the transaction for the given digest has been included
     * in a checkpoint (finalized).
     */
    func isTxFinalized(digest: Digest) async throws  -> Bool
    
    /**
     * Returns whether the transaction for the given digest has been indexed
     * on the node. This means that it can be queried by its digest and its
     * effects will be usable for subsequent transactions. To check for
     * full finalization, use `is_tx_finalized`.
     */
    func isTxIndexedOnNode(digest: Digest) async throws  -> Bool
    
    /**
     * Return the sequence number of the latest checkpoint that has been
     * executed.
     */
    func latestCheckpointSequenceNumber() async throws  -> UInt64?
    
    /**
     * Lazily fetch the max page size
     */
    func maxPageSize() async throws  -> Int32
    
    /**
     * Return the contents' JSON of an object that is a Move object.
     *
     * If the object does not exist (e.g., due to pruning), this will return
     * `Ok(None)`. Similarly, if this is not an object but an address, it
     * will return `Ok(None)`.
     */
    func moveObjectContents(objectId: ObjectId, version: UInt64?) async throws  -> Value?
    
    /**
     * Return the BCS of an object that is a Move object.
     *
     * If the object does not exist (e.g., due to pruning), this will return
     * `Ok(None)`. Similarly, if this is not an object but an address, it
     * will return `Ok(None)`.
     */
    func moveObjectContentsBcs(objectId: ObjectId, version: UInt64?) async throws  -> Data?
    
    /**
     * Execute a Move View Function.
     *
     * A View Function is a function in a Move module with a return type that
     * does not alter the state of the ledger. When using this interface,
     * no transactions are submitted to the network for inclusion into the
     * ledger.
     *
     * This method allows calling nearly any Move function with a return type
     * and any arguments. The function's result values are provided and
     * decoded using the appropriate Move type, then formatted in JSON.
     *
     * The use of this interface does not require signature checks (even for
     * functions that take Owned Objects as input) or gas coins, as it does
     * not alter ledger state. Spam attacks are dealt with at the RPC level
     * rather than execution level.
     *
     * # Arguments
     * * `function_name` - The Move function fully qualified name as
     * `<package_id>::<module_name>::<function_name>`, e.g.,
     * `0x2::hash::blake2b256`
     * * `type_arguments` - The type arguments of the Move function
     * * `arguments` - The typed arguments to be passed into the Move function
     *
     * # Returns
     * A `MoveViewResult` containing either execution results (return values)
     * or an error.
     */
    func moveViewCall(functionName: String, typeArguments: [TypeTag]?, arguments: [MoveViewArg]?) async throws  -> MoveViewResult
    
    /**
     * Execute a Move View Function with raw JSON arguments.
     *
     * This is an alternative to [`GraphQLClient::move_view_call`] that accepts
     * raw JSON values instead of typed arguments.
     *
     * A View Function is a function in a Move module with a return type that
     * does not alter the state of the ledger. When using this interface,
     * no transactions are submitted to the network for inclusion into the
     * ledger.
     *
     * # Arguments
     * * `function_name` - The Move function fully qualified name as
     * `<package_id>::<module_name>::<function_name>`, e.g.,
     * `0x2::hash::blake2b256`
     * * `type_arguments` - The type arguments of the Move function
     * * `arguments` - The arguments to be passed into the Move function, in
     * JSON format
     *
     * # Returns
     * A `MoveViewResult` containing either execution results (return values)
     * or an error.
     */
    func moveViewCallJson(functionName: String, typeArguments: [String]?, arguments: [Value]?) async throws  -> MoveViewResult
    
    /**
     * Return the normalized Move function data for the provided package,
     * module, and function.
     */
    func normalizedMoveFunction(package: Address, module: String, function: String, version: UInt64?) async throws  -> MoveFunction?
    
    /**
     * Return the normalized Move module data for the provided module.
     */
    func normalizedMoveModule(package: Address, module: String, version: UInt64?, paginationFilterEnums: PaginationFilter?, paginationFilterFriends: PaginationFilter?, paginationFilterFunctions: PaginationFilter?, paginationFilterStructs: PaginationFilter?) async throws  -> MoveModule?
    
    /**
     * Return an object based on the provided `Address`.
     *
     * If the object does not exist (e.g., due to pruning), this will return
     * `Ok(None)`. Similarly, if this is not an object but an address, it
     * will return `Ok(None)`.
     */
    func object(objectId: ObjectId, version: UInt64?) async throws  -> Object?
    
    /**
     * Return the object's bcs content `Vec<u8>` based on the provided
     * `Address`.
     */
    func objectBcs(objectId: ObjectId) async throws  -> Data?
    
    /**
     * Return a page of objects based on the provided parameters.
     *
     * Use this function together with the `ObjectFilter::owner` to get the
     * objects owned by an address.
     */
    func objects(filter: ObjectFilter?, paginationFilter: PaginationFilter?) async throws  -> ObjectPage
    
    /**
     * The package corresponding to the given address (at the optionally given
     * version). When no version is given, the package is loaded directly
     * from the address given. Otherwise, the address is translated before
     * loading to point to the package whose original ID matches
     * the package at address, but whose version is version. For non-system
     * packages, this might result in a different address than address
     * because different versions of a package, introduced by upgrades,
     * exist at distinct addresses.
     *
     * Note that this interpretation of version is different from a historical
     * object read (the interpretation of version for the object query).
     */
    func package(address: Address, version: UInt64?) async throws  -> MovePackage?
    
    /**
     * Fetch the latest version of the package at address.
     * This corresponds to the package with the highest version that shares its
     * original ID with the package at address.
     */
    func packageLatest(address: Address) async throws  -> MovePackage?
    
    /**
     * Fetch all versions of package at address (packages that share this
     * package's original ID), optionally bounding the versions exclusively
     * from below with afterVersion, or from above with beforeVersion.
     */
    func packageVersions(address: Address, afterVersion: UInt64?, beforeVersion: UInt64?, paginationFilter: PaginationFilter?) async throws  -> MovePackagePage
    
    /**
     * The Move packages that exist in the network, optionally filtered to be
     * strictly before beforeCheckpoint and/or strictly after
     * afterCheckpoint.
     *
     * This query returns all versions of a given user package that appear
     * between the specified checkpoints, but only records the latest
     * versions of system packages.
     */
    func packages(afterCheckpoint: UInt64?, beforeCheckpoint: UInt64?, paginationFilter: PaginationFilter?) async throws  -> MovePackagePage
    
    /**
     * Get the protocol configuration.
     */
    func protocolConfig(version: UInt64?) async throws  -> ProtocolConfigs
    
    /**
     * Get the reference gas price for the provided epoch or the last known one
     * if no epoch is provided.
     *
     * This will return `Ok(None)` if the epoch requested is not available in
     * the GraphQL service (e.g., due to pruning).
     */
    func referenceGasPrice(epoch: UInt64?) async throws  -> UInt64?
    
    /**
     * Run a query.
     */
    func runQuery(query: Query) async throws  -> Value
    
    /**
     * Get the GraphQL service configuration, including complexity limits, read
     * and mutation limits, supported versions, and others.
     */
    func serviceConfig() async throws  -> ServiceConfig
    
    /**
     * Set the server address for the GraphQL client. It should be a
     * valid URL with a host and optionally a port number.
     */
    func setRpcServer(server: String) async throws 
    
    /**
     * Get total supply for the coin type.
     */
    func totalSupply(coinType: String) async throws  -> UInt64?
    
    /**
     * The total number of transaction blocks in the network by the end of the
     * last known checkpoint.
     */
    func totalTransactionBlocks() async throws  -> UInt64?
    
    /**
     * The total number of transaction blocks in the network by the end of the
     * provided checkpoint digest.
     */
    func totalTransactionBlocksByDigest(digest: Digest) async throws  -> UInt64?
    
    /**
     * The total number of transaction blocks in the network by the end of the
     * provided checkpoint sequence number.
     */
    func totalTransactionBlocksBySeqNum(seqNum: UInt64) async throws  -> UInt64?
    
    /**
     * Get a transaction by its digest.
     */
    func transaction(digest: Digest) async throws  -> SignedTransaction?
    
    /**
     * Get a transaction's data and effects by its digest.
     */
    func transactionDataEffects(digest: Digest) async throws  -> TransactionDataEffects?
    
    /**
     * Get a transaction's effects by its digest.
     */
    func transactionEffects(digest: Digest) async throws  -> TransactionEffects?
    
    /**
     * Get a page of transactions based on the provided filters.
     */
    func transactions(filter: TransactionsFilter?, paginationFilter: PaginationFilter?) async throws  -> SignedTransactionPage
    
    /**
     * Get a page of transactions' data and effects based on the provided
     * filters.
     */
    func transactionsDataEffects(filter: TransactionsFilter?, paginationFilter: PaginationFilter?) async throws  -> TransactionDataEffectsPage
    
    /**
     * Get a page of transactions' effects based on the provided filters.
     */
    func transactionsEffects(filter: TransactionsFilter?, paginationFilter: PaginationFilter?) async throws  -> TransactionEffectsPage
    
    /**
     * Wait for the indexing (on the node, not the indexer) or finalization of
     * a transaction by its digest. An optional timeout can be provided,
     * which, if exceeded, will return an error (default 60s).
     */
    func waitForTx(digest: Digest, waitFor: WaitForTx, timeout: TimeInterval?) async throws 
    
}
/**
 * The GraphQL client for interacting with the IOTA blockchain.
 */
open class GraphQlClient: GraphQlClientProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_graphqlclient(self.pointer, $0) }
    }
    /**
     * Create a new GraphQL client with the provided server address.
     */
public convenience init(server: String)throws  {
    let pointer =
        try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_graphqlclient_new(
        FfiConverterString.lower(server),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_graphqlclient(pointer, $0) }
    }

    
    /**
     * Create a new GraphQL client connected to the `devnet` GraphQL server:
     * {DEVNET_HOST}.
     */
public static func newDevnet() -> GraphQlClient  {
    return try!  FfiConverterTypeGraphQLClient_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_graphqlclient_new_devnet($0
    )
})
}
    
    /**
     * Create a new GraphQL client connected to the `localhost` GraphQL server:
     * {DEFAULT_LOCAL_HOST}.
     */
public static func newLocalnet() -> GraphQlClient  {
    return try!  FfiConverterTypeGraphQLClient_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_graphqlclient_new_localnet($0
    )
})
}
    
    /**
     * Create a new GraphQL client connected to the `mainnet` GraphQL server:
     * {MAINNET_HOST}.
     */
public static func newMainnet() -> GraphQlClient  {
    return try!  FfiConverterTypeGraphQLClient_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_graphqlclient_new_mainnet($0
    )
})
}
    
    /**
     * Create a new GraphQL client connected to the `testnet` GraphQL server:
     * {TESTNET_HOST}.
     */
public static func newTestnet() -> GraphQlClient  {
    return try!  FfiConverterTypeGraphQLClient_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_graphqlclient_new_testnet($0
    )
})
}
    

    
    /**
     * Get the list of active validators for the provided epoch, including
     * related metadata. If no epoch is provided, it will return the active
     * validators for the current epoch.
     */
open func activeValidators(epoch: UInt64? = nil, paginationFilter: PaginationFilter? = nil)async throws  -> ValidatorPage  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_active_validators(
                    self.uniffiClonePointer(),
                    FfiConverterOptionUInt64.lower(epoch),FfiConverterOptionTypePaginationFilter.lower(paginationFilter)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterTypeValidatorPage_lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Get the balance of all the coins owned by address for the provided coin
     * type. Coin type will default to `0x2::coin::Coin<0x2::iota::IOTA>`
     * if not provided.
     */
open func balance(address: Address, coinType: String? = nil)async throws  -> UInt64?  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_balance(
                    self.uniffiClonePointer(),
                    FfiConverterTypeAddress_lower(address),FfiConverterOptionString.lower(coinType)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterOptionUInt64.lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Get the chain identifier.
     */
open func chainId()async throws  -> String  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_chain_id(
                    self.uniffiClonePointer()
                    
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterString.lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Get the `CheckpointSummary` for a given checkpoint digest or
     * checkpoint id. If none is provided, it will use the last known
     * checkpoint id.
     */
open func checkpoint(digest: Digest? = nil, seqNum: UInt64? = nil)async throws  -> CheckpointSummary?  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_checkpoint(
                    self.uniffiClonePointer(),
                    FfiConverterOptionTypeDigest.lower(digest),FfiConverterOptionUInt64.lower(seqNum)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterOptionTypeCheckpointSummary.lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Get a page of `CheckpointSummary` for the provided parameters.
     */
open func checkpoints(paginationFilter: PaginationFilter? = nil)async throws  -> CheckpointSummaryPage  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_checkpoints(
                    self.uniffiClonePointer(),
                    FfiConverterOptionTypePaginationFilter.lower(paginationFilter)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterTypeCheckpointSummaryPage_lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Get the coin metadata for the coin type.
     */
open func coinMetadata(coinType: String)async throws  -> CoinMetadata?  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_coin_metadata(
                    self.uniffiClonePointer(),
                    FfiConverterString.lower(coinType)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterOptionTypeCoinMetadata.lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Get the list of coins for the specified address.
     *
     * If `coin_type` is not provided, all coins will be returned. For IOTA
     * coins, pass in the coin type: `0x2::iota::IOTA`.
     */
open func coins(owner: Address, paginationFilter: PaginationFilter? = nil, coinType: StructTag? = nil)async throws  -> CoinPage  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_coins(
                    self.uniffiClonePointer(),
                    FfiConverterTypeAddress_lower(owner),FfiConverterOptionTypePaginationFilter.lower(paginationFilter),FfiConverterOptionTypeStructTag.lower(coinType)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterTypeCoinPage_lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Dry run a `Transaction` and return the transaction effects and dry run
     * error (if any).
     *
     * `skipChecks` optional flag disables the usual verification checks that
     * prevent access to objects that are owned by addresses other than the
     * sender, and calling non-public, non-entry functions, and some other
     * checks. Defaults to false.
     */
open func dryRunTx(tx: Transaction, skipChecks: Bool = false)async throws  -> DryRunResult  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_dry_run_tx(
                    self.uniffiClonePointer(),
                    FfiConverterTypeTransaction_lower(tx),FfiConverterBool.lower(skipChecks)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterTypeDryRunResult_lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Dry run a `TransactionKind` and return the transaction effects and dry
     * run error (if any).
     *
     * `skipChecks` optional flag disables the usual verification checks that
     * prevent access to objects that are owned by addresses other than the
     * sender, and calling non-public, non-entry functions, and some other
     * checks. Defaults to false.
     *
     * `tx_meta` is the transaction metadata.
     */
open func dryRunTxKind(txKind: TransactionKind, txMeta: TransactionMetadata, skipChecks: Bool = false)async throws  -> DryRunResult  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_dry_run_tx_kind(
                    self.uniffiClonePointer(),
                    FfiConverterTypeTransactionKind_lower(txKind),FfiConverterTypeTransactionMetadata_lower(txMeta),FfiConverterBool.lower(skipChecks)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterTypeDryRunResult_lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Access a dynamic field on an object using its name. Names are arbitrary
     * Move values whose type have copy, drop, and store, and are specified
     * using their type, and their BCS contents, Base64 encoded.
     *
     * The `name` argument is a json serialized type.
     *
     * This returns `DynamicFieldOutput` which contains the name, the value
     * as json, and object.
     */
open func dynamicField(address: Address, typeTag: TypeTag, name: Value)async throws  -> DynamicFieldOutput?  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_dynamic_field(
                    self.uniffiClonePointer(),
                    FfiConverterTypeAddress_lower(address),FfiConverterTypeTypeTag_lower(typeTag),FfiConverterTypeValue_lower(name)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterOptionTypeDynamicFieldOutput.lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Get a page of dynamic fields for the provided address. Note that this
     * will also fetch dynamic fields on wrapped objects.
     *
     * This returns a page of `DynamicFieldOutput`s.
     */
open func dynamicFields(address: Address, paginationFilter: PaginationFilter? = nil)async throws  -> DynamicFieldOutputPage  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_dynamic_fields(
                    self.uniffiClonePointer(),
                    FfiConverterTypeAddress_lower(address),FfiConverterOptionTypePaginationFilter.lower(paginationFilter)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterTypeDynamicFieldOutputPage_lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Access a dynamic object field on an object using its name. Names are
     * arbitrary Move values whose type have copy, drop, and store, and are
     * specified using their type, and their BCS contents, Base64 encoded.
     *
     * The `name` argument is a json serialized type.
     *
     * This returns `DynamicFieldOutput` which contains the name, the value
     * as json, and object.
     */
open func dynamicObjectField(address: Address, typeTag: TypeTag, name: Value)async throws  -> DynamicFieldOutput?  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_dynamic_object_field(
                    self.uniffiClonePointer(),
                    FfiConverterTypeAddress_lower(address),FfiConverterTypeTypeTag_lower(typeTag),FfiConverterTypeValue_lower(name)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterOptionTypeDynamicFieldOutput.lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Return the epoch information for the provided epoch. If no epoch is
     * provided, it will return the last known epoch.
     */
open func epoch(epoch: UInt64? = nil)async throws  -> Epoch?  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_epoch(
                    self.uniffiClonePointer(),
                    FfiConverterOptionUInt64.lower(epoch)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterOptionTypeEpoch.lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Return the number of checkpoints in this epoch. This will return
     * `Ok(None)` if the epoch requested is not available in the GraphQL
     * service (e.g., due to pruning).
     */
open func epochTotalCheckpoints(epoch: UInt64? = nil)async throws  -> UInt64?  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_epoch_total_checkpoints(
                    self.uniffiClonePointer(),
                    FfiConverterOptionUInt64.lower(epoch)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterOptionUInt64.lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Return the number of transaction blocks in this epoch. This will return
     * `Ok(None)` if the epoch requested is not available in the GraphQL
     * service (e.g., due to pruning).
     */
open func epochTotalTransactionBlocks(epoch: UInt64? = nil)async throws  -> UInt64?  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_epoch_total_transaction_blocks(
                    self.uniffiClonePointer(),
                    FfiConverterOptionUInt64.lower(epoch)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterOptionUInt64.lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Return a page of tuple (event, transaction digest) based on the
     * (optional) event filter.
     */
open func events(filter: EventFilter? = nil, paginationFilter: PaginationFilter? = nil)async throws  -> EventPage  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_events(
                    self.uniffiClonePointer(),
                    FfiConverterOptionTypeEventFilter.lower(filter),FfiConverterOptionTypePaginationFilter.lower(paginationFilter)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterTypeEventPage_lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Execute a transaction.
     */
open func executeTx(signatures: [UserSignature], tx: Transaction, waitFor: WaitForTx? = nil)async throws  -> TransactionEffects  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_execute_tx(
                    self.uniffiClonePointer(),
                    FfiConverterSequenceTypeUserSignature.lower(signatures),FfiConverterTypeTransaction_lower(tx),FfiConverterOptionTypeWaitForTx.lower(waitFor)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_pointer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_pointer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_pointer,
            liftFunc: FfiConverterTypeTransactionEffects_lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Get the list of gas coins for the specified address.
     */
open func gasCoins(owner: Address, paginationFilter: PaginationFilter? = nil)async throws  -> CoinPage  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_gas_coins(
                    self.uniffiClonePointer(),
                    FfiConverterTypeAddress_lower(owner),FfiConverterOptionTypePaginationFilter.lower(paginationFilter)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterTypeCoinPage_lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Get the default name pointing to this address, if one exists.
     */
open func iotaNamesDefaultName(address: Address, format: NameFormat?)async throws  -> Name?  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_iota_names_default_name(
                    self.uniffiClonePointer(),
                    FfiConverterTypeAddress_lower(address),FfiConverterOptionTypeNameFormat.lower(format)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterOptionTypeName.lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Return the resolved address for the given name.
     */
open func iotaNamesLookup(name: String)async throws  -> Address?  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_iota_names_lookup(
                    self.uniffiClonePointer(),
                    FfiConverterString.lower(name)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterOptionTypeAddress.lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Find all registration NFTs for the given address.
     */
open func iotaNamesRegistrations(address: Address, paginationFilter: PaginationFilter)async throws  -> NameRegistrationPage  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_iota_names_registrations(
                    self.uniffiClonePointer(),
                    FfiConverterTypeAddress_lower(address),FfiConverterTypePaginationFilter_lower(paginationFilter)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterTypeNameRegistrationPage_lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Returns whether the transaction for the given digest has been included
     * in a checkpoint (finalized).
     */
open func isTxFinalized(digest: Digest)async throws  -> Bool  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_is_tx_finalized(
                    self.uniffiClonePointer(),
                    FfiConverterTypeDigest_lower(digest)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_i8,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_i8,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_i8,
            liftFunc: FfiConverterBool.lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Returns whether the transaction for the given digest has been indexed
     * on the node. This means that it can be queried by its digest and its
     * effects will be usable for subsequent transactions. To check for
     * full finalization, use `is_tx_finalized`.
     */
open func isTxIndexedOnNode(digest: Digest)async throws  -> Bool  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_is_tx_indexed_on_node(
                    self.uniffiClonePointer(),
                    FfiConverterTypeDigest_lower(digest)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_i8,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_i8,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_i8,
            liftFunc: FfiConverterBool.lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Return the sequence number of the latest checkpoint that has been
     * executed.
     */
open func latestCheckpointSequenceNumber()async throws  -> UInt64?  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_latest_checkpoint_sequence_number(
                    self.uniffiClonePointer()
                    
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterOptionUInt64.lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Lazily fetch the max page size
     */
open func maxPageSize()async throws  -> Int32  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_max_page_size(
                    self.uniffiClonePointer()
                    
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_i32,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_i32,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_i32,
            liftFunc: FfiConverterInt32.lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Return the contents' JSON of an object that is a Move object.
     *
     * If the object does not exist (e.g., due to pruning), this will return
     * `Ok(None)`. Similarly, if this is not an object but an address, it
     * will return `Ok(None)`.
     */
open func moveObjectContents(objectId: ObjectId, version: UInt64? = nil)async throws  -> Value?  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_move_object_contents(
                    self.uniffiClonePointer(),
                    FfiConverterTypeObjectId_lower(objectId),FfiConverterOptionUInt64.lower(version)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterOptionTypeValue.lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Return the BCS of an object that is a Move object.
     *
     * If the object does not exist (e.g., due to pruning), this will return
     * `Ok(None)`. Similarly, if this is not an object but an address, it
     * will return `Ok(None)`.
     */
open func moveObjectContentsBcs(objectId: ObjectId, version: UInt64? = nil)async throws  -> Data?  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_move_object_contents_bcs(
                    self.uniffiClonePointer(),
                    FfiConverterTypeObjectId_lower(objectId),FfiConverterOptionUInt64.lower(version)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterOptionData.lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Execute a Move View Function.
     *
     * A View Function is a function in a Move module with a return type that
     * does not alter the state of the ledger. When using this interface,
     * no transactions are submitted to the network for inclusion into the
     * ledger.
     *
     * This method allows calling nearly any Move function with a return type
     * and any arguments. The function's result values are provided and
     * decoded using the appropriate Move type, then formatted in JSON.
     *
     * The use of this interface does not require signature checks (even for
     * functions that take Owned Objects as input) or gas coins, as it does
     * not alter ledger state. Spam attacks are dealt with at the RPC level
     * rather than execution level.
     *
     * # Arguments
     * * `function_name` - The Move function fully qualified name as
     * `<package_id>::<module_name>::<function_name>`, e.g.,
     * `0x2::hash::blake2b256`
     * * `type_arguments` - The type arguments of the Move function
     * * `arguments` - The typed arguments to be passed into the Move function
     *
     * # Returns
     * A `MoveViewResult` containing either execution results (return values)
     * or an error.
     */
open func moveViewCall(functionName: String, typeArguments: [TypeTag]? = nil, arguments: [MoveViewArg]? = nil)async throws  -> MoveViewResult  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_move_view_call(
                    self.uniffiClonePointer(),
                    FfiConverterString.lower(functionName),FfiConverterOptionSequenceTypeTypeTag.lower(typeArguments),FfiConverterOptionSequenceTypeMoveViewArg.lower(arguments)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterTypeMoveViewResult_lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Execute a Move View Function with raw JSON arguments.
     *
     * This is an alternative to [`GraphQLClient::move_view_call`] that accepts
     * raw JSON values instead of typed arguments.
     *
     * A View Function is a function in a Move module with a return type that
     * does not alter the state of the ledger. When using this interface,
     * no transactions are submitted to the network for inclusion into the
     * ledger.
     *
     * # Arguments
     * * `function_name` - The Move function fully qualified name as
     * `<package_id>::<module_name>::<function_name>`, e.g.,
     * `0x2::hash::blake2b256`
     * * `type_arguments` - The type arguments of the Move function
     * * `arguments` - The arguments to be passed into the Move function, in
     * JSON format
     *
     * # Returns
     * A `MoveViewResult` containing either execution results (return values)
     * or an error.
     */
open func moveViewCallJson(functionName: String, typeArguments: [String]? = nil, arguments: [Value]? = nil)async throws  -> MoveViewResult  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_move_view_call_json(
                    self.uniffiClonePointer(),
                    FfiConverterString.lower(functionName),FfiConverterOptionSequenceString.lower(typeArguments),FfiConverterOptionSequenceTypeValue.lower(arguments)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterTypeMoveViewResult_lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Return the normalized Move function data for the provided package,
     * module, and function.
     */
open func normalizedMoveFunction(package: Address, module: String, function: String, version: UInt64? = nil)async throws  -> MoveFunction?  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_normalized_move_function(
                    self.uniffiClonePointer(),
                    FfiConverterTypeAddress_lower(package),FfiConverterString.lower(module),FfiConverterString.lower(function),FfiConverterOptionUInt64.lower(version)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterOptionTypeMoveFunction.lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Return the normalized Move module data for the provided module.
     */
open func normalizedMoveModule(package: Address, module: String, version: UInt64? = nil, paginationFilterEnums: PaginationFilter? = nil, paginationFilterFriends: PaginationFilter? = nil, paginationFilterFunctions: PaginationFilter? = nil, paginationFilterStructs: PaginationFilter? = nil)async throws  -> MoveModule?  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_normalized_move_module(
                    self.uniffiClonePointer(),
                    FfiConverterTypeAddress_lower(package),FfiConverterString.lower(module),FfiConverterOptionUInt64.lower(version),FfiConverterOptionTypePaginationFilter.lower(paginationFilterEnums),FfiConverterOptionTypePaginationFilter.lower(paginationFilterFriends),FfiConverterOptionTypePaginationFilter.lower(paginationFilterFunctions),FfiConverterOptionTypePaginationFilter.lower(paginationFilterStructs)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterOptionTypeMoveModule.lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Return an object based on the provided `Address`.
     *
     * If the object does not exist (e.g., due to pruning), this will return
     * `Ok(None)`. Similarly, if this is not an object but an address, it
     * will return `Ok(None)`.
     */
open func object(objectId: ObjectId, version: UInt64? = nil)async throws  -> Object?  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_object(
                    self.uniffiClonePointer(),
                    FfiConverterTypeObjectId_lower(objectId),FfiConverterOptionUInt64.lower(version)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterOptionTypeObject.lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Return the object's bcs content `Vec<u8>` based on the provided
     * `Address`.
     */
open func objectBcs(objectId: ObjectId)async throws  -> Data?  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_object_bcs(
                    self.uniffiClonePointer(),
                    FfiConverterTypeObjectId_lower(objectId)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterOptionData.lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Return a page of objects based on the provided parameters.
     *
     * Use this function together with the `ObjectFilter::owner` to get the
     * objects owned by an address.
     */
open func objects(filter: ObjectFilter? = nil, paginationFilter: PaginationFilter? = nil)async throws  -> ObjectPage  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_objects(
                    self.uniffiClonePointer(),
                    FfiConverterOptionTypeObjectFilter.lower(filter),FfiConverterOptionTypePaginationFilter.lower(paginationFilter)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterTypeObjectPage_lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * The package corresponding to the given address (at the optionally given
     * version). When no version is given, the package is loaded directly
     * from the address given. Otherwise, the address is translated before
     * loading to point to the package whose original ID matches
     * the package at address, but whose version is version. For non-system
     * packages, this might result in a different address than address
     * because different versions of a package, introduced by upgrades,
     * exist at distinct addresses.
     *
     * Note that this interpretation of version is different from a historical
     * object read (the interpretation of version for the object query).
     */
open func package(address: Address, version: UInt64? = nil)async throws  -> MovePackage?  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_package(
                    self.uniffiClonePointer(),
                    FfiConverterTypeAddress_lower(address),FfiConverterOptionUInt64.lower(version)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterOptionTypeMovePackage.lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Fetch the latest version of the package at address.
     * This corresponds to the package with the highest version that shares its
     * original ID with the package at address.
     */
open func packageLatest(address: Address)async throws  -> MovePackage?  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_package_latest(
                    self.uniffiClonePointer(),
                    FfiConverterTypeAddress_lower(address)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterOptionTypeMovePackage.lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Fetch all versions of package at address (packages that share this
     * package's original ID), optionally bounding the versions exclusively
     * from below with afterVersion, or from above with beforeVersion.
     */
open func packageVersions(address: Address, afterVersion: UInt64? = nil, beforeVersion: UInt64? = nil, paginationFilter: PaginationFilter? = nil)async throws  -> MovePackagePage  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_package_versions(
                    self.uniffiClonePointer(),
                    FfiConverterTypeAddress_lower(address),FfiConverterOptionUInt64.lower(afterVersion),FfiConverterOptionUInt64.lower(beforeVersion),FfiConverterOptionTypePaginationFilter.lower(paginationFilter)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterTypeMovePackagePage_lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * The Move packages that exist in the network, optionally filtered to be
     * strictly before beforeCheckpoint and/or strictly after
     * afterCheckpoint.
     *
     * This query returns all versions of a given user package that appear
     * between the specified checkpoints, but only records the latest
     * versions of system packages.
     */
open func packages(afterCheckpoint: UInt64? = nil, beforeCheckpoint: UInt64? = nil, paginationFilter: PaginationFilter? = nil)async throws  -> MovePackagePage  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_packages(
                    self.uniffiClonePointer(),
                    FfiConverterOptionUInt64.lower(afterCheckpoint),FfiConverterOptionUInt64.lower(beforeCheckpoint),FfiConverterOptionTypePaginationFilter.lower(paginationFilter)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterTypeMovePackagePage_lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Get the protocol configuration.
     */
open func protocolConfig(version: UInt64? = nil)async throws  -> ProtocolConfigs  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_protocol_config(
                    self.uniffiClonePointer(),
                    FfiConverterOptionUInt64.lower(version)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterTypeProtocolConfigs_lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Get the reference gas price for the provided epoch or the last known one
     * if no epoch is provided.
     *
     * This will return `Ok(None)` if the epoch requested is not available in
     * the GraphQL service (e.g., due to pruning).
     */
open func referenceGasPrice(epoch: UInt64? = nil)async throws  -> UInt64?  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_reference_gas_price(
                    self.uniffiClonePointer(),
                    FfiConverterOptionUInt64.lower(epoch)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterOptionUInt64.lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Run a query.
     */
open func runQuery(query: Query)async throws  -> Value  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_run_query(
                    self.uniffiClonePointer(),
                    FfiConverterTypeQuery_lower(query)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterTypeValue_lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Get the GraphQL service configuration, including complexity limits, read
     * and mutation limits, supported versions, and others.
     */
open func serviceConfig()async throws  -> ServiceConfig  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_service_config(
                    self.uniffiClonePointer()
                    
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterTypeServiceConfig_lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Set the server address for the GraphQL client. It should be a
     * valid URL with a host and optionally a port number.
     */
open func setRpcServer(server: String)async throws   {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_set_rpc_server(
                    self.uniffiClonePointer(),
                    FfiConverterString.lower(server)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_void,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_void,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_void,
            liftFunc: { $0 },
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Get total supply for the coin type.
     */
open func totalSupply(coinType: String)async throws  -> UInt64?  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_total_supply(
                    self.uniffiClonePointer(),
                    FfiConverterString.lower(coinType)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterOptionUInt64.lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * The total number of transaction blocks in the network by the end of the
     * last known checkpoint.
     */
open func totalTransactionBlocks()async throws  -> UInt64?  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_total_transaction_blocks(
                    self.uniffiClonePointer()
                    
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterOptionUInt64.lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * The total number of transaction blocks in the network by the end of the
     * provided checkpoint digest.
     */
open func totalTransactionBlocksByDigest(digest: Digest)async throws  -> UInt64?  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_total_transaction_blocks_by_digest(
                    self.uniffiClonePointer(),
                    FfiConverterTypeDigest_lower(digest)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterOptionUInt64.lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * The total number of transaction blocks in the network by the end of the
     * provided checkpoint sequence number.
     */
open func totalTransactionBlocksBySeqNum(seqNum: UInt64)async throws  -> UInt64?  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_total_transaction_blocks_by_seq_num(
                    self.uniffiClonePointer(),
                    FfiConverterUInt64.lower(seqNum)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterOptionUInt64.lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Get a transaction by its digest.
     */
open func transaction(digest: Digest)async throws  -> SignedTransaction?  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_transaction(
                    self.uniffiClonePointer(),
                    FfiConverterTypeDigest_lower(digest)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterOptionTypeSignedTransaction.lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Get a transaction's data and effects by its digest.
     */
open func transactionDataEffects(digest: Digest)async throws  -> TransactionDataEffects?  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_transaction_data_effects(
                    self.uniffiClonePointer(),
                    FfiConverterTypeDigest_lower(digest)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterOptionTypeTransactionDataEffects.lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Get a transaction's effects by its digest.
     */
open func transactionEffects(digest: Digest)async throws  -> TransactionEffects?  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_transaction_effects(
                    self.uniffiClonePointer(),
                    FfiConverterTypeDigest_lower(digest)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterOptionTypeTransactionEffects.lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Get a page of transactions based on the provided filters.
     */
open func transactions(filter: TransactionsFilter? = nil, paginationFilter: PaginationFilter? = nil)async throws  -> SignedTransactionPage  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_transactions(
                    self.uniffiClonePointer(),
                    FfiConverterOptionTypeTransactionsFilter.lower(filter),FfiConverterOptionTypePaginationFilter.lower(paginationFilter)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterTypeSignedTransactionPage_lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Get a page of transactions' data and effects based on the provided
     * filters.
     */
open func transactionsDataEffects(filter: TransactionsFilter? = nil, paginationFilter: PaginationFilter? = nil)async throws  -> TransactionDataEffectsPage  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_transactions_data_effects(
                    self.uniffiClonePointer(),
                    FfiConverterOptionTypeTransactionsFilter.lower(filter),FfiConverterOptionTypePaginationFilter.lower(paginationFilter)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterTypeTransactionDataEffectsPage_lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Get a page of transactions' effects based on the provided filters.
     */
open func transactionsEffects(filter: TransactionsFilter? = nil, paginationFilter: PaginationFilter? = nil)async throws  -> TransactionEffectsPage  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_transactions_effects(
                    self.uniffiClonePointer(),
                    FfiConverterOptionTypeTransactionsFilter.lower(filter),FfiConverterOptionTypePaginationFilter.lower(paginationFilter)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterTypeTransactionEffectsPage_lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Wait for the indexing (on the node, not the indexer) or finalization of
     * a transaction by its digest. An optional timeout can be provided,
     * which, if exceeded, will return an error (default 60s).
     */
open func waitForTx(digest: Digest, waitFor: WaitForTx, timeout: TimeInterval? = nil)async throws   {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_graphqlclient_wait_for_tx(
                    self.uniffiClonePointer(),
                    FfiConverterTypeDigest_lower(digest),FfiConverterTypeWaitForTx_lower(waitFor),FfiConverterOptionDuration.lower(timeout)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_void,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_void,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_void,
            liftFunc: { $0 },
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    

}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeGraphQLClient: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = GraphQlClient

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> GraphQlClient {
        return GraphQlClient(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: GraphQlClient) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> GraphQlClient {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: GraphQlClient, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeGraphQLClient_lift(_ pointer: UnsafeMutableRawPointer) throws -> GraphQlClient {
    return try FfiConverterTypeGraphQLClient.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeGraphQLClient_lower(_ value: GraphQlClient) -> UnsafeMutableRawPointer {
    return FfiConverterTypeGraphQLClient.lower(value)
}






/**
 * A move identifier
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * identifier = %x01-80    ; length of the identifier
 * (ALPHA *127(ALPHA / DIGIT / UNDERSCORE)) /
 * (UNDERSCORE 1*127(ALPHA / DIGIT / UNDERSCORE))
 *
 * UNDERSCORE = %x95
 * ```
 */
public protocol IdentifierProtocol: AnyObject, Sendable {
    
    func asStr()  -> String
    
}
/**
 * A move identifier
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * identifier = %x01-80    ; length of the identifier
 * (ALPHA *127(ALPHA / DIGIT / UNDERSCORE)) /
 * (UNDERSCORE 1*127(ALPHA / DIGIT / UNDERSCORE))
 *
 * UNDERSCORE = %x95
 * ```
 */
open class Identifier: IdentifierProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_identifier(self.pointer, $0) }
    }
public convenience init(identifier: String)throws  {
    let pointer =
        try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_identifier_new(
        FfiConverterString.lower(identifier),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_identifier(pointer, $0) }
    }

    

    
open func asStr() -> String  {
    return try!  FfiConverterString.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_identifier_as_str(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_identifier_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    open var description: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_identifier_uniffi_trait_display(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: Identifier, other: Identifier) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_identifier_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeIdentifier_lower(other),$0
    )
}
        )
    }
    open func hash(into hasher: inout Hasher) {
        let val = try!  FfiConverterUInt64.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_identifier_uniffi_trait_hash(self.uniffiClonePointer(),$0
    )
}
        )
        hasher.combine(val)
    }

}
extension Identifier: CustomDebugStringConvertible {}
extension Identifier: CustomStringConvertible {}
extension Identifier: Equatable {}
extension Identifier: Hashable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeIdentifier: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = Identifier

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> Identifier {
        return Identifier(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: Identifier) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Identifier {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: Identifier, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeIdentifier_lift(_ pointer: UnsafeMutableRawPointer) throws -> Identifier {
    return try FfiConverterTypeIdentifier.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeIdentifier_lower(_ value: Identifier) -> UnsafeMutableRawPointer {
    return FfiConverterTypeIdentifier.lower(value)
}






/**
 * An input to a user transaction
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * input = input-pure / input-immutable-or-owned / input-shared / input-receiving
 *
 * input-pure                  = %x00 bytes
 * input-immutable-or-owned    = %x01 object-ref
 * input-shared                = %x02 object-id u64 bool
 * input-receiving             = %x04 object-ref
 * ```
 */
public protocol InputProtocol: AnyObject, Sendable {
    
}
/**
 * An input to a user transaction
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * input = input-pure / input-immutable-or-owned / input-shared / input-receiving
 *
 * input-pure                  = %x00 bytes
 * input-immutable-or-owned    = %x01 object-ref
 * input-shared                = %x02 object-id u64 bool
 * input-receiving             = %x04 object-ref
 * ```
 */
open class Input: InputProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_input(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_input(pointer, $0) }
    }

    
    /**
     * A move object that is either immutable or address owned
     */
public static func newImmutableOrOwned(objectRef: ObjectReference) -> Input  {
    return try!  FfiConverterTypeInput_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_input_new_immutable_or_owned(
        FfiConverterTypeObjectReference_lower(objectRef),$0
    )
})
}
    
    /**
     * For normal operations this is required to be a move primitive type and
     * not contain structs or objects.
     */
public static func newPure(value: Data) -> Input  {
    return try!  FfiConverterTypeInput_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_input_new_pure(
        FfiConverterData.lower(value),$0
    )
})
}
    
public static func newReceiving(objectRef: ObjectReference) -> Input  {
    return try!  FfiConverterTypeInput_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_input_new_receiving(
        FfiConverterTypeObjectReference_lower(objectRef),$0
    )
})
}
    
    /**
     * A move object whose owner is "Shared"
     */
public static func newShared(objectId: ObjectId, initialSharedVersion: UInt64, mutable: Bool) -> Input  {
    return try!  FfiConverterTypeInput_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_input_new_shared(
        FfiConverterTypeObjectId_lower(objectId),
        FfiConverterUInt64.lower(initialSharedVersion),
        FfiConverterBool.lower(mutable),$0
    )
})
}
    

    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_input_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: Input, other: Input) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_input_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeInput_lower(other),$0
    )
}
        )
    }

}
extension Input: CustomDebugStringConvertible {}
extension Input: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeInput: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = Input

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> Input {
        return Input(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: Input) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Input {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: Input, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeInput_lift(_ pointer: UnsafeMutableRawPointer) throws -> Input {
    return try FfiConverterTypeInput.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeInput_lower(_ value: Input) -> UnsafeMutableRawPointer {
    return FfiConverterTypeInput.lower(value)
}






/**
 * A Signing Intent
 *
 * An intent is a compact struct that serves as the domain separator for a
 * message that a signature commits to. It consists of three parts:
 * 1. IntentScope (what the type of the message is)
 * 2. IntentVersion
 * 3. IntentAppId (what application the signature refers to).
 *
 * The serialization of an Intent is a 3-byte array where each field is
 * represented by a byte and it is prepended onto a message before it is signed
 * in IOTA.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * intent = intent-scope intent-version intent-app-id
 * ```
 */
public protocol IntentProtocol: AnyObject, Sendable {
    
    /**
     * Get the app id of the signing intent.
     */
    func appId()  -> IntentAppId
    
    /**
     * Get the scope of the signing intent.
     */
    func scope()  -> IntentScope
    
    /**
     * Convert the signing intent to bytes.
     */
    func toBytes()  -> Data
    
    /**
     * Get the version of the signing intent.
     */
    func version()  -> IntentVersion
    
}
/**
 * A Signing Intent
 *
 * An intent is a compact struct that serves as the domain separator for a
 * message that a signature commits to. It consists of three parts:
 * 1. IntentScope (what the type of the message is)
 * 2. IntentVersion
 * 3. IntentAppId (what application the signature refers to).
 *
 * The serialization of an Intent is a 3-byte array where each field is
 * represented by a byte and it is prepended onto a message before it is signed
 * in IOTA.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * intent = intent-scope intent-version intent-app-id
 * ```
 */
open class Intent: IntentProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_intent(self.pointer, $0) }
    }
    /**
     * Create a new signing intent.
     */
public convenience init(scope: IntentScope, version: IntentVersion, appId: IntentAppId) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_intent_new(
        FfiConverterTypeIntentScope_lower(scope),
        FfiConverterTypeIntentVersion_lower(version),
        FfiConverterTypeIntentAppId_lower(appId),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_intent(pointer, $0) }
    }

    
    /**
     * Create a signing intent from bytes.
     */
public static func fromBytes(bytes: Data)throws  -> Intent  {
    return try  FfiConverterTypeIntent_lift(try rustCallWithError(FfiConverterTypeIntentError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_intent_from_bytes(
        FfiConverterData.lower(bytes),$0
    )
})
}
    
    /**
     * Create a signing intent from a hex string.
     */
public static func fromHex(hex: String)throws  -> Intent  {
    return try  FfiConverterTypeIntent_lift(try rustCallWithError(FfiConverterTypeIntentError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_intent_from_hex(
        FfiConverterString.lower(hex),$0
    )
})
}
    
    /**
     * Create a new Consensus app signing intent.
     */
public static func newConsensusApp(scope: IntentScope) -> Intent  {
    return try!  FfiConverterTypeIntent_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_intent_new_consensus_app(
        FfiConverterTypeIntentScope_lower(scope),$0
    )
})
}
    
    /**
     * Create a new IOTA app signing intent.
     */
public static func newIotaApp(scope: IntentScope) -> Intent  {
    return try!  FfiConverterTypeIntent_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_intent_new_iota_app(
        FfiConverterTypeIntentScope_lower(scope),$0
    )
})
}
    
    /**
     * Create a new IOTA transaction signing intent.
     */
public static func newIotaTransaction() -> Intent  {
    return try!  FfiConverterTypeIntent_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_intent_new_iota_transaction($0
    )
})
}
    
    /**
     * Create a new IOTA personal message signing intent.
     */
public static func newPersonalMessage() -> Intent  {
    return try!  FfiConverterTypeIntent_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_intent_new_personal_message($0
    )
})
}
    

    
    /**
     * Get the app id of the signing intent.
     */
open func appId() -> IntentAppId  {
    return try!  FfiConverterTypeIntentAppId_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_intent_app_id(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Get the scope of the signing intent.
     */
open func scope() -> IntentScope  {
    return try!  FfiConverterTypeIntentScope_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_intent_scope(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Convert the signing intent to bytes.
     */
open func toBytes() -> Data  {
    return try!  FfiConverterData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_intent_to_bytes(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Get the version of the signing intent.
     */
open func version() -> IntentVersion  {
    return try!  FfiConverterTypeIntentVersion_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_intent_version(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_intent_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: Intent, other: Intent) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_intent_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeIntent_lower(other),$0
    )
}
        )
    }
    open func hash(into hasher: inout Hasher) {
        let val = try!  FfiConverterUInt64.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_intent_uniffi_trait_hash(self.uniffiClonePointer(),$0
    )
}
        )
        hasher.combine(val)
    }

}
extension Intent: CustomDebugStringConvertible {}
extension Intent: Equatable {}
extension Intent: Hashable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeIntent: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = Intent

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> Intent {
        return Intent(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: Intent) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Intent {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: Intent, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeIntent_lift(_ pointer: UnsafeMutableRawPointer) throws -> Intent {
    return try FfiConverterTypeIntent.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeIntent_lower(_ value: Intent) -> UnsafeMutableRawPointer {
    return FfiConverterTypeIntent.lower(value)
}






/**
 * Command to build a move vector out of a set of individual elements
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * make-move-vector = (option type-tag) (vector argument)
 * ```
 */
public protocol MakeMoveVectorProtocol: AnyObject, Sendable {
    
    /**
     * The set individual elements to build the vector with
     */
    func elements()  -> [Argument]
    
    /**
     * Type of the individual elements
     *
     * This is required to be set when the type can't be inferred, for example
     * when the set of provided arguments are all pure input values.
     */
    func typeTag()  -> TypeTag?
    
}
/**
 * Command to build a move vector out of a set of individual elements
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * make-move-vector = (option type-tag) (vector argument)
 * ```
 */
open class MakeMoveVector: MakeMoveVectorProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_makemovevector(self.pointer, $0) }
    }
public convenience init(typeTag: TypeTag?, elements: [Argument]) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_makemovevector_new(
        FfiConverterOptionTypeTypeTag.lower(typeTag),
        FfiConverterSequenceTypeArgument.lower(elements),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_makemovevector(pointer, $0) }
    }

    

    
    /**
     * The set individual elements to build the vector with
     */
open func elements() -> [Argument]  {
    return try!  FfiConverterSequenceTypeArgument.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_makemovevector_elements(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Type of the individual elements
     *
     * This is required to be set when the type can't be inferred, for example
     * when the set of provided arguments are all pure input values.
     */
open func typeTag() -> TypeTag?  {
    return try!  FfiConverterOptionTypeTypeTag.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_makemovevector_type_tag(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_makemovevector_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: MakeMoveVector, other: MakeMoveVector) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_makemovevector_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeMakeMoveVector_lower(other),$0
    )
}
        )
    }

}
extension MakeMoveVector: CustomDebugStringConvertible {}
extension MakeMoveVector: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMakeMoveVector: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = MakeMoveVector

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> MakeMoveVector {
        return MakeMoveVector(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: MakeMoveVector) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MakeMoveVector {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: MakeMoveVector, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMakeMoveVector_lift(_ pointer: UnsafeMutableRawPointer) throws -> MakeMoveVector {
    return try FfiConverterTypeMakeMoveVector.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMakeMoveVector_lower(_ value: MakeMoveVector) -> UnsafeMutableRawPointer {
    return FfiConverterTypeMakeMoveVector.lower(value)
}






/**
 * Command to merge multiple coins of the same type into a single coin
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * merge-coins = argument (vector argument)
 * ```
 */
public protocol MergeCoinsProtocol: AnyObject, Sendable {
    
    /**
     * Coin to merge coins into
     */
    func coin()  -> Argument
    
    /**
     * Set of coins to merge into `coin`
     *
     * All listed coins must be of the same type and be the same type as `coin`
     */
    func coinsToMerge()  -> [Argument]
    
}
/**
 * Command to merge multiple coins of the same type into a single coin
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * merge-coins = argument (vector argument)
 * ```
 */
open class MergeCoins: MergeCoinsProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_mergecoins(self.pointer, $0) }
    }
public convenience init(coin: Argument, coinsToMerge: [Argument]) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_mergecoins_new(
        FfiConverterTypeArgument_lower(coin),
        FfiConverterSequenceTypeArgument.lower(coinsToMerge),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_mergecoins(pointer, $0) }
    }

    

    
    /**
     * Coin to merge coins into
     */
open func coin() -> Argument  {
    return try!  FfiConverterTypeArgument_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_mergecoins_coin(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Set of coins to merge into `coin`
     *
     * All listed coins must be of the same type and be the same type as `coin`
     */
open func coinsToMerge() -> [Argument]  {
    return try!  FfiConverterSequenceTypeArgument.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_mergecoins_coins_to_merge(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_mergecoins_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: MergeCoins, other: MergeCoins) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_mergecoins_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeMergeCoins_lower(other),$0
    )
}
        )
    }

}
extension MergeCoins: CustomDebugStringConvertible {}
extension MergeCoins: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMergeCoins: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = MergeCoins

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> MergeCoins {
        return MergeCoins(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: MergeCoins) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MergeCoins {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: MergeCoins, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMergeCoins_lift(_ pointer: UnsafeMutableRawPointer) throws -> MergeCoins {
    return try FfiConverterTypeMergeCoins.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMergeCoins_lower(_ value: MergeCoins) -> UnsafeMutableRawPointer {
    return FfiConverterTypeMergeCoins.lower(value)
}






public protocol MoveArgProtocol: AnyObject, Sendable {
    
}
open class MoveArg: MoveArgProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_movearg(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_movearg(pointer, $0) }
    }

    
public static func address(address: Address) -> MoveArg  {
    return try!  FfiConverterTypeMoveArg_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_movearg_address(
        FfiConverterTypeAddress_lower(address),$0
    )
})
}
    
public static func addressFromHex(hex: String)throws  -> MoveArg  {
    return try  FfiConverterTypeMoveArg_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_movearg_address_from_hex(
        FfiConverterString.lower(hex),$0
    )
})
}
    
public static func addressVec(addresses: [Address]) -> MoveArg  {
    return try!  FfiConverterTypeMoveArg_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_movearg_address_vec(
        FfiConverterSequenceTypeAddress.lower(addresses),$0
    )
})
}
    
public static func addressVecFromHex(addresses: [String])throws  -> MoveArg  {
    return try  FfiConverterTypeMoveArg_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_movearg_address_vec_from_hex(
        FfiConverterSequenceString.lower(addresses),$0
    )
})
}
    
public static func bool(value: Bool) -> MoveArg  {
    return try!  FfiConverterTypeMoveArg_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_movearg_bool(
        FfiConverterBool.lower(value),$0
    )
})
}
    
public static func boolVec(values: [Bool]) -> MoveArg  {
    return try!  FfiConverterTypeMoveArg_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_movearg_bool_vec(
        FfiConverterSequenceBool.lower(values),$0
    )
})
}
    
public static func digest(digest: Digest) -> MoveArg  {
    return try!  FfiConverterTypeMoveArg_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_movearg_digest(
        FfiConverterTypeDigest_lower(digest),$0
    )
})
}
    
public static func digestFromBase58(base58: String)throws  -> MoveArg  {
    return try  FfiConverterTypeMoveArg_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_movearg_digest_from_base58(
        FfiConverterString.lower(base58),$0
    )
})
}
    
public static func digestVec(digests: [Digest]) -> MoveArg  {
    return try!  FfiConverterTypeMoveArg_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_movearg_digest_vec(
        FfiConverterSequenceTypeDigest.lower(digests),$0
    )
})
}
    
public static func digestVecFromBase58(digests: [String])throws  -> MoveArg  {
    return try  FfiConverterTypeMoveArg_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_movearg_digest_vec_from_base58(
        FfiConverterSequenceString.lower(digests),$0
    )
})
}
    
public static func option(value: MoveArg?) -> MoveArg  {
    return try!  FfiConverterTypeMoveArg_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_movearg_option(
        FfiConverterOptionTypeMoveArg.lower(value),$0
    )
})
}
    
public static func string(string: String) -> MoveArg  {
    return try!  FfiConverterTypeMoveArg_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_movearg_string(
        FfiConverterString.lower(string),$0
    )
})
}
    
public static func stringVec(addresses: [String]) -> MoveArg  {
    return try!  FfiConverterTypeMoveArg_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_movearg_string_vec(
        FfiConverterSequenceString.lower(addresses),$0
    )
})
}
    
public static func u128(value: String)throws  -> MoveArg  {
    return try  FfiConverterTypeMoveArg_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_movearg_u128(
        FfiConverterString.lower(value),$0
    )
})
}
    
public static func u128Vec(values: [String])throws  -> MoveArg  {
    return try  FfiConverterTypeMoveArg_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_movearg_u128_vec(
        FfiConverterSequenceString.lower(values),$0
    )
})
}
    
public static func u16(value: UInt16) -> MoveArg  {
    return try!  FfiConverterTypeMoveArg_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_movearg_u16(
        FfiConverterUInt16.lower(value),$0
    )
})
}
    
public static func u16Vec(values: [UInt16]) -> MoveArg  {
    return try!  FfiConverterTypeMoveArg_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_movearg_u16_vec(
        FfiConverterSequenceUInt16.lower(values),$0
    )
})
}
    
public static func u256(value: String)throws  -> MoveArg  {
    return try  FfiConverterTypeMoveArg_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_movearg_u256(
        FfiConverterString.lower(value),$0
    )
})
}
    
public static func u256Vec(values: [String])throws  -> MoveArg  {
    return try  FfiConverterTypeMoveArg_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_movearg_u256_vec(
        FfiConverterSequenceString.lower(values),$0
    )
})
}
    
public static func u32(value: UInt32) -> MoveArg  {
    return try!  FfiConverterTypeMoveArg_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_movearg_u32(
        FfiConverterUInt32.lower(value),$0
    )
})
}
    
public static func u32Vec(values: [UInt32]) -> MoveArg  {
    return try!  FfiConverterTypeMoveArg_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_movearg_u32_vec(
        FfiConverterSequenceUInt32.lower(values),$0
    )
})
}
    
public static func u64(value: UInt64) -> MoveArg  {
    return try!  FfiConverterTypeMoveArg_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_movearg_u64(
        FfiConverterUInt64.lower(value),$0
    )
})
}
    
public static func u64Vec(values: [UInt64]) -> MoveArg  {
    return try!  FfiConverterTypeMoveArg_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_movearg_u64_vec(
        FfiConverterSequenceUInt64.lower(values),$0
    )
})
}
    
public static func u8(value: UInt8) -> MoveArg  {
    return try!  FfiConverterTypeMoveArg_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_movearg_u8(
        FfiConverterUInt8.lower(value),$0
    )
})
}
    
public static func u8Vec(values: Data) -> MoveArg  {
    return try!  FfiConverterTypeMoveArg_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_movearg_u8_vec(
        FfiConverterData.lower(values),$0
    )
})
}
    

    

}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMoveArg: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = MoveArg

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> MoveArg {
        return MoveArg(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: MoveArg) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MoveArg {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: MoveArg, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveArg_lift(_ pointer: UnsafeMutableRawPointer) throws -> MoveArg {
    return try FfiConverterTypeMoveArg.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveArg_lower(_ value: MoveArg) -> UnsafeMutableRawPointer {
    return FfiConverterTypeMoveArg.lower(value)
}






/**
 * MoveAuthenticator is a signature variant that enables a method of
 * authentication through Move code. This type represents the data received
 * by the Move authenticate function during the Account Abstraction
 * authentication flow.
 */
public protocol MoveAuthenticatorProtocol: AnyObject, Sendable {
    
    func address()  -> Address
    
    func callArgs()  -> [Input]
    
    func objectToAuthenticate()  -> Input
    
    func typeArgs()  -> [TypeTag]
    
}
/**
 * MoveAuthenticator is a signature variant that enables a method of
 * authentication through Move code. This type represents the data received
 * by the Move authenticate function during the Account Abstraction
 * authentication flow.
 */
open class MoveAuthenticator: MoveAuthenticatorProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_moveauthenticator(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_moveauthenticator(pointer, $0) }
    }

    
    /**
     * Create a new move authenticator from an immutable object.
     */
public static func newImmutable(callArgs: [Input], typeArgs: [TypeTag], objectToAuthenticate: ObjectReference) -> MoveAuthenticator  {
    return try!  FfiConverterTypeMoveAuthenticator_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_moveauthenticator_new_immutable(
        FfiConverterSequenceTypeInput.lower(callArgs),
        FfiConverterSequenceTypeTypeTag.lower(typeArgs),
        FfiConverterTypeObjectReference_lower(objectToAuthenticate),$0
    )
})
}
    
    /**
     * Create a new move authenticator from a shared object.
     */
public static func newShared(callArgs: [Input], typeArgs: [TypeTag], objectToAuthenticate: ObjectId, initialSharedVersion: UInt64) -> MoveAuthenticator  {
    return try!  FfiConverterTypeMoveAuthenticator_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_moveauthenticator_new_shared(
        FfiConverterSequenceTypeInput.lower(callArgs),
        FfiConverterSequenceTypeTypeTag.lower(typeArgs),
        FfiConverterTypeObjectId_lower(objectToAuthenticate),
        FfiConverterUInt64.lower(initialSharedVersion),$0
    )
})
}
    

    
open func address() -> Address  {
    return try!  FfiConverterTypeAddress_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_moveauthenticator_address(self.uniffiClonePointer(),$0
    )
})
}
    
open func callArgs() -> [Input]  {
    return try!  FfiConverterSequenceTypeInput.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_moveauthenticator_call_args(self.uniffiClonePointer(),$0
    )
})
}
    
open func objectToAuthenticate() -> Input  {
    return try!  FfiConverterTypeInput_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_moveauthenticator_object_to_authenticate(self.uniffiClonePointer(),$0
    )
})
}
    
open func typeArgs() -> [TypeTag]  {
    return try!  FfiConverterSequenceTypeTypeTag.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_moveauthenticator_type_args(self.uniffiClonePointer(),$0
    )
})
}
    

}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMoveAuthenticator: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = MoveAuthenticator

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> MoveAuthenticator {
        return MoveAuthenticator(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: MoveAuthenticator) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MoveAuthenticator {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: MoveAuthenticator, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveAuthenticator_lift(_ pointer: UnsafeMutableRawPointer) throws -> MoveAuthenticator {
    return try FfiConverterTypeMoveAuthenticator.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveAuthenticator_lower(_ value: MoveAuthenticator) -> UnsafeMutableRawPointer {
    return FfiConverterTypeMoveAuthenticator.lower(value)
}






public protocol MoveAuthenticatorBuilderProtocol: AnyObject, Sendable {
    
    /**
     * Resolve this move authenticator builder into a `MoveAuthenticator` which
     * can be used to execute a transaction.
     */
    func finish(client: GraphQlClient) async throws  -> MoveAuthenticator
    
}
open class MoveAuthenticatorBuilder: MoveAuthenticatorBuilderProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_moveauthenticatorbuilder(self.pointer, $0) }
    }
    /**
     * Create a new move authenticator call with the account ID, function
     * inputs, and generic types.
     */
public convenience init(accountId: ObjectId, callArgs: [PtbArgument], typeArgs: [TypeTag]) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_moveauthenticatorbuilder_new(
        FfiConverterTypeObjectId_lower(accountId),
        FfiConverterSequenceTypePTBArgument.lower(callArgs),
        FfiConverterSequenceTypeTypeTag.lower(typeArgs),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_moveauthenticatorbuilder(pointer, $0) }
    }

    

    
    /**
     * Resolve this move authenticator builder into a `MoveAuthenticator` which
     * can be used to execute a transaction.
     */
open func finish(client: GraphQlClient)async throws  -> MoveAuthenticator  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_moveauthenticatorbuilder_finish(
                    self.uniffiClonePointer(),
                    FfiConverterTypeGraphQLClient_lower(client)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_pointer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_pointer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_pointer,
            liftFunc: FfiConverterTypeMoveAuthenticator_lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    

}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMoveAuthenticatorBuilder: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = MoveAuthenticatorBuilder

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> MoveAuthenticatorBuilder {
        return MoveAuthenticatorBuilder(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: MoveAuthenticatorBuilder) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MoveAuthenticatorBuilder {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: MoveAuthenticatorBuilder, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveAuthenticatorBuilder_lift(_ pointer: UnsafeMutableRawPointer) throws -> MoveAuthenticatorBuilder {
    return try FfiConverterTypeMoveAuthenticatorBuilder.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveAuthenticatorBuilder_lower(_ value: MoveAuthenticatorBuilder) -> UnsafeMutableRawPointer {
    return FfiConverterTypeMoveAuthenticatorBuilder.lower(value)
}






/**
 * Command to call a move function
 *
 * Functions that can be called by a `MoveCall` command are those that have a
 * function signature that is either `entry` or `public` (which don't have a
 * reference return type).
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * move-call = object-id           ; package id
 * identifier          ; module name
 * identifier          ; function name
 * (vector type-tag)   ; type arguments, if any
 * (vector argument)   ; input arguments
 * ```
 */
public protocol MoveCallProtocol: AnyObject, Sendable {
    
    /**
     * The arguments to the function.
     */
    func arguments()  -> [Argument]
    
    /**
     * The function to be called.
     */
    func function()  -> Identifier
    
    /**
     * The specific module in the package containing the function.
     */
    func module()  -> Identifier
    
    /**
     * The package containing the module and function.
     */
    func package()  -> ObjectId
    
    /**
     * The type arguments to the function.
     */
    func typeArguments()  -> [TypeTag]
    
}
/**
 * Command to call a move function
 *
 * Functions that can be called by a `MoveCall` command are those that have a
 * function signature that is either `entry` or `public` (which don't have a
 * reference return type).
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * move-call = object-id           ; package id
 * identifier          ; module name
 * identifier          ; function name
 * (vector type-tag)   ; type arguments, if any
 * (vector argument)   ; input arguments
 * ```
 */
open class MoveCall: MoveCallProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_movecall(self.pointer, $0) }
    }
public convenience init(package: ObjectId, module: Identifier, function: Identifier, typeArguments: [TypeTag], arguments: [Argument]) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_movecall_new(
        FfiConverterTypeObjectId_lower(package),
        FfiConverterTypeIdentifier_lower(module),
        FfiConverterTypeIdentifier_lower(function),
        FfiConverterSequenceTypeTypeTag.lower(typeArguments),
        FfiConverterSequenceTypeArgument.lower(arguments),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_movecall(pointer, $0) }
    }

    

    
    /**
     * The arguments to the function.
     */
open func arguments() -> [Argument]  {
    return try!  FfiConverterSequenceTypeArgument.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_movecall_arguments(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The function to be called.
     */
open func function() -> Identifier  {
    return try!  FfiConverterTypeIdentifier_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_movecall_function(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The specific module in the package containing the function.
     */
open func module() -> Identifier  {
    return try!  FfiConverterTypeIdentifier_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_movecall_module(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The package containing the module and function.
     */
open func package() -> ObjectId  {
    return try!  FfiConverterTypeObjectId_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_movecall_package(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The type arguments to the function.
     */
open func typeArguments() -> [TypeTag]  {
    return try!  FfiConverterSequenceTypeTypeTag.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_movecall_type_arguments(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_movecall_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: MoveCall, other: MoveCall) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_movecall_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeMoveCall_lower(other),$0
    )
}
        )
    }

}
extension MoveCall: CustomDebugStringConvertible {}
extension MoveCall: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMoveCall: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = MoveCall

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> MoveCall {
        return MoveCall(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: MoveCall) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MoveCall {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: MoveCall, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveCall_lift(_ pointer: UnsafeMutableRawPointer) throws -> MoveCall {
    return try FfiConverterTypeMoveCall.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveCall_lower(_ value: MoveCall) -> UnsafeMutableRawPointer {
    return FfiConverterTypeMoveCall.lower(value)
}






public protocol MoveFunctionProtocol: AnyObject, Sendable {
    
    func isEntry()  -> Bool
    
    func name()  -> String
    
    func parameters()  -> [OpenMoveType]?
    
    func returnType()  -> [OpenMoveType]?
    
    func typeParameters()  -> [MoveFunctionTypeParameter]?
    
    func visibility()  -> MoveVisibility?
    
}
open class MoveFunction: MoveFunctionProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_movefunction(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_movefunction(pointer, $0) }
    }

    

    
open func isEntry() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_movefunction_is_entry(self.uniffiClonePointer(),$0
    )
})
}
    
open func name() -> String  {
    return try!  FfiConverterString.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_movefunction_name(self.uniffiClonePointer(),$0
    )
})
}
    
open func parameters() -> [OpenMoveType]?  {
    return try!  FfiConverterOptionSequenceTypeOpenMoveType.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_movefunction_parameters(self.uniffiClonePointer(),$0
    )
})
}
    
open func returnType() -> [OpenMoveType]?  {
    return try!  FfiConverterOptionSequenceTypeOpenMoveType.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_movefunction_return_type(self.uniffiClonePointer(),$0
    )
})
}
    
open func typeParameters() -> [MoveFunctionTypeParameter]?  {
    return try!  FfiConverterOptionSequenceTypeMoveFunctionTypeParameter.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_movefunction_type_parameters(self.uniffiClonePointer(),$0
    )
})
}
    
open func visibility() -> MoveVisibility?  {
    return try!  FfiConverterOptionTypeMoveVisibility.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_movefunction_visibility(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_movefunction_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    open var description: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_movefunction_uniffi_trait_display(self.uniffiClonePointer(),$0
    )
}
        )
    }

}
extension MoveFunction: CustomDebugStringConvertible {}
extension MoveFunction: CustomStringConvertible {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMoveFunction: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = MoveFunction

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> MoveFunction {
        return MoveFunction(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: MoveFunction) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MoveFunction {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: MoveFunction, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveFunction_lift(_ pointer: UnsafeMutableRawPointer) throws -> MoveFunction {
    return try FfiConverterTypeMoveFunction.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveFunction_lower(_ value: MoveFunction) -> UnsafeMutableRawPointer {
    return FfiConverterTypeMoveFunction.lower(value)
}






/**
 * A move package
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * object-move-package = object-id u64 move-modules type-origin-table linkage-table
 *
 * move-modules = map (identifier bytes)
 * type-origin-table = vector type-origin
 * linkage-table = map (object-id upgrade-info)
 * ```
 */
public protocol MovePackageProtocol: AnyObject, Sendable {
    
    func id()  -> ObjectId
    
    func linkageTable()  -> [ObjectId: UpgradeInfo]
    
    func modules()  -> [Identifier: Data]
    
    func typeOriginTable()  -> [TypeOrigin]
    
    func version()  -> UInt64
    
}
/**
 * A move package
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * object-move-package = object-id u64 move-modules type-origin-table linkage-table
 *
 * move-modules = map (identifier bytes)
 * type-origin-table = vector type-origin
 * linkage-table = map (object-id upgrade-info)
 * ```
 */
open class MovePackage: MovePackageProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_movepackage(self.pointer, $0) }
    }
public convenience init(id: ObjectId, version: UInt64, modules: [Identifier: Data], typeOriginTable: [TypeOrigin], linkageTable: [ObjectId: UpgradeInfo])throws  {
    let pointer =
        try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_movepackage_new(
        FfiConverterTypeObjectId_lower(id),
        FfiConverterUInt64.lower(version),
        FfiConverterDictionaryTypeIdentifierData.lower(modules),
        FfiConverterSequenceTypeTypeOrigin.lower(typeOriginTable),
        FfiConverterDictionaryTypeObjectIdTypeUpgradeInfo.lower(linkageTable),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_movepackage(pointer, $0) }
    }

    

    
open func id() -> ObjectId  {
    return try!  FfiConverterTypeObjectId_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_movepackage_id(self.uniffiClonePointer(),$0
    )
})
}
    
open func linkageTable() -> [ObjectId: UpgradeInfo]  {
    return try!  FfiConverterDictionaryTypeObjectIdTypeUpgradeInfo.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_movepackage_linkage_table(self.uniffiClonePointer(),$0
    )
})
}
    
open func modules() -> [Identifier: Data]  {
    return try!  FfiConverterDictionaryTypeIdentifierData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_movepackage_modules(self.uniffiClonePointer(),$0
    )
})
}
    
open func typeOriginTable() -> [TypeOrigin]  {
    return try!  FfiConverterSequenceTypeTypeOrigin.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_movepackage_type_origin_table(self.uniffiClonePointer(),$0
    )
})
}
    
open func version() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_movepackage_version(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_movepackage_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: MovePackage, other: MovePackage) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_movepackage_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeMovePackage_lower(other),$0
    )
}
        )
    }
    open func hash(into hasher: inout Hasher) {
        let val = try!  FfiConverterUInt64.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_movepackage_uniffi_trait_hash(self.uniffiClonePointer(),$0
    )
}
        )
        hasher.combine(val)
    }

}
extension MovePackage: CustomDebugStringConvertible {}
extension MovePackage: Equatable {}
extension MovePackage: Hashable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMovePackage: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = MovePackage

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> MovePackage {
        return MovePackage(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: MovePackage) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MovePackage {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: MovePackage, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMovePackage_lift(_ pointer: UnsafeMutableRawPointer) throws -> MovePackage {
    return try FfiConverterTypeMovePackage.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMovePackage_lower(_ value: MovePackage) -> UnsafeMutableRawPointer {
    return FfiConverterTypeMovePackage.lower(value)
}






/**
 * Type corresponding to the output of `iota move build
 * --dump-bytecode-as-base64`
 */
public protocol MovePackageDataProtocol: AnyObject, Sendable {
    
    /**
     * Returns the package dependencies.
     */
    func dependencies()  -> [ObjectId]
    
    /**
     * Returns the package digest.
     */
    func digest()  -> Digest
    
    /**
     * Returns the package modules.
     */
    func modules()  -> [Data]
    
    func toBase64()  -> String
    
    func toJson()  -> String
    
}
/**
 * Type corresponding to the output of `iota move build
 * --dump-bytecode-as-base64`
 */
open class MovePackageData: MovePackageDataProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_movepackagedata(self.pointer, $0) }
    }
public convenience init(modules: [Data], dependencies: [ObjectId]) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_movepackagedata_new(
        FfiConverterSequenceData.lower(modules),
        FfiConverterSequenceTypeObjectId.lower(dependencies),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_movepackagedata(pointer, $0) }
    }

    
public static func fromBase64(base64: String)throws  -> MovePackageData  {
    return try  FfiConverterTypeMovePackageData_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_movepackagedata_from_base64(
        FfiConverterString.lower(base64),$0
    )
})
}
    
public static func fromJson(json: String)throws  -> MovePackageData  {
    return try  FfiConverterTypeMovePackageData_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_movepackagedata_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
    

    
    /**
     * Returns the package dependencies.
     */
open func dependencies() -> [ObjectId]  {
    return try!  FfiConverterSequenceTypeObjectId.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_movepackagedata_dependencies(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Returns the package digest.
     */
open func digest() -> Digest  {
    return try!  FfiConverterTypeDigest_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_movepackagedata_digest(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Returns the package modules.
     */
open func modules() -> [Data]  {
    return try!  FfiConverterSequenceData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_movepackagedata_modules(self.uniffiClonePointer(),$0
    )
})
}
    
open func toBase64() -> String  {
    return try!  FfiConverterString.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_movepackagedata_to_base64(self.uniffiClonePointer(),$0
    )
})
}
    
open func toJson() -> String  {
    return try!  FfiConverterString.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_movepackagedata_to_json(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_movepackagedata_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }

}
extension MovePackageData: CustomDebugStringConvertible {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMovePackageData: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = MovePackageData

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> MovePackageData {
        return MovePackageData(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: MovePackageData) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MovePackageData {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: MovePackageData, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMovePackageData_lift(_ pointer: UnsafeMutableRawPointer) throws -> MovePackageData {
    return try FfiConverterTypeMovePackageData.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMovePackageData_lower(_ value: MovePackageData) -> UnsafeMutableRawPointer {
    return FfiConverterTypeMovePackageData.lower(value)
}






/**
 * An argument for a Move View Function call.
 *
 * This enum represents the different types of values that can be passed
 * as arguments to a Move View Function.
 */
public protocol MoveViewArgProtocol: AnyObject, Sendable {
    
}
/**
 * An argument for a Move View Function call.
 *
 * This enum represents the different types of values that can be passed
 * as arguments to a Move View Function.
 */
open class MoveViewArg: MoveViewArgProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_moveviewarg(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_moveviewarg(pointer, $0) }
    }

    
public static func address(value: Address) -> MoveViewArg  {
    return try!  FfiConverterTypeMoveViewArg_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_moveviewarg_address(
        FfiConverterTypeAddress_lower(value),$0
    )
})
}
    
public static func bool(value: Bool) -> MoveViewArg  {
    return try!  FfiConverterTypeMoveViewArg_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_moveviewarg_bool(
        FfiConverterBool.lower(value),$0
    )
})
}
    
public static func json(value: String) -> MoveViewArg  {
    return try!  FfiConverterTypeMoveViewArg_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_moveviewarg_json(
        FfiConverterString.lower(value),$0
    )
})
}
    
public static func null() -> MoveViewArg  {
    return try!  FfiConverterTypeMoveViewArg_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_moveviewarg_null($0
    )
})
}
    
public static func objectId(value: ObjectId) -> MoveViewArg  {
    return try!  FfiConverterTypeMoveViewArg_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_moveviewarg_object_id(
        FfiConverterTypeObjectId_lower(value),$0
    )
})
}
    
public static func option(value: MoveViewArg?) -> MoveViewArg  {
    return try!  FfiConverterTypeMoveViewArg_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_moveviewarg_option(
        FfiConverterOptionTypeMoveViewArg.lower(value),$0
    )
})
}
    
public static func string(value: String) -> MoveViewArg  {
    return try!  FfiConverterTypeMoveViewArg_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_moveviewarg_string(
        FfiConverterString.lower(value),$0
    )
})
}
    
public static func stringVec(values: [String]) -> MoveViewArg  {
    return try!  FfiConverterTypeMoveViewArg_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_moveviewarg_string_vec(
        FfiConverterSequenceString.lower(values),$0
    )
})
}
    
public static func u128(value: String) -> MoveViewArg  {
    return try!  FfiConverterTypeMoveViewArg_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_moveviewarg_u128(
        FfiConverterString.lower(value),$0
    )
})
}
    
public static func u16(value: UInt16) -> MoveViewArg  {
    return try!  FfiConverterTypeMoveViewArg_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_moveviewarg_u16(
        FfiConverterUInt16.lower(value),$0
    )
})
}
    
public static func u32(value: UInt32) -> MoveViewArg  {
    return try!  FfiConverterTypeMoveViewArg_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_moveviewarg_u32(
        FfiConverterUInt32.lower(value),$0
    )
})
}
    
public static func u64(value: UInt64) -> MoveViewArg  {
    return try!  FfiConverterTypeMoveViewArg_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_moveviewarg_u64(
        FfiConverterUInt64.lower(value),$0
    )
})
}
    
public static func u8(value: UInt8) -> MoveViewArg  {
    return try!  FfiConverterTypeMoveViewArg_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_moveviewarg_u8(
        FfiConverterUInt8.lower(value),$0
    )
})
}
    
public static func u8Vec(values: Data) -> MoveViewArg  {
    return try!  FfiConverterTypeMoveViewArg_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_moveviewarg_u8_vec(
        FfiConverterData.lower(values),$0
    )
})
}
    

    

}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMoveViewArg: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = MoveViewArg

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> MoveViewArg {
        return MoveViewArg(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: MoveViewArg) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MoveViewArg {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: MoveViewArg, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveViewArg_lift(_ pointer: UnsafeMutableRawPointer) throws -> MoveViewArg {
    return try FfiConverterTypeMoveViewArg.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveViewArg_lower(_ value: MoveViewArg) -> UnsafeMutableRawPointer {
    return FfiConverterTypeMoveViewArg.lower(value)
}






/**
 * Aggregated signature from members of a multisig committee.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * multisig-aggregated-signature = (vector multisig-member-signature)
 * u16     ; bitmap
 * multisig-committee
 * ```
 *
 * There is also a legacy encoding for this type defined as:
 *
 * ```text
 * legacy-multisig-aggregated-signature = (vector multisig-member-signature)
 * roaring-bitmap   ; bitmap
 * legacy-multisig-committee
 * roaring-bitmap = bytes  ; where the contents of the bytes are valid
 * ; according to the serialized spec for
 * ; roaring bitmaps
 * ```
 *
 * See <https://github.com/RoaringBitmap/RoaringFormatSpec> for the specification for the
 * serialized format of RoaringBitmaps.
 */
public protocol MultisigAggregatedSignatureProtocol: AnyObject, Sendable {
    
    /**
     * The bitmap that indicates which committee members provided their
     * signature.
     */
    func bitmap()  -> UInt16
    
    func committee()  -> MultisigCommittee
    
    /**
     * The list of signatures from committee members
     */
    func signatures()  -> [MultisigMemberSignature]
    
}
/**
 * Aggregated signature from members of a multisig committee.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * multisig-aggregated-signature = (vector multisig-member-signature)
 * u16     ; bitmap
 * multisig-committee
 * ```
 *
 * There is also a legacy encoding for this type defined as:
 *
 * ```text
 * legacy-multisig-aggregated-signature = (vector multisig-member-signature)
 * roaring-bitmap   ; bitmap
 * legacy-multisig-committee
 * roaring-bitmap = bytes  ; where the contents of the bytes are valid
 * ; according to the serialized spec for
 * ; roaring bitmaps
 * ```
 *
 * See <https://github.com/RoaringBitmap/RoaringFormatSpec> for the specification for the
 * serialized format of RoaringBitmaps.
 */
open class MultisigAggregatedSignature: MultisigAggregatedSignatureProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_multisigaggregatedsignature(self.pointer, $0) }
    }
    /**
     * Construct a new aggregated multisig signature.
     *
     * Since the list of signatures doesn't contain sufficient information to
     * identify which committee member provided the signature, it is up to
     * the caller to ensure that the provided signature list is in the same
     * order as it's corresponding member in the provided committee
     * and that it's position in the provided bitmap is set.
     */
public convenience init(committee: MultisigCommittee, signatures: [MultisigMemberSignature], bitmap: UInt16) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_multisigaggregatedsignature_new(
        FfiConverterTypeMultisigCommittee_lower(committee),
        FfiConverterSequenceTypeMultisigMemberSignature.lower(signatures),
        FfiConverterUInt16.lower(bitmap),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_multisigaggregatedsignature(pointer, $0) }
    }

    

    
    /**
     * The bitmap that indicates which committee members provided their
     * signature.
     */
open func bitmap() -> UInt16  {
    return try!  FfiConverterUInt16.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigaggregatedsignature_bitmap(self.uniffiClonePointer(),$0
    )
})
}
    
open func committee() -> MultisigCommittee  {
    return try!  FfiConverterTypeMultisigCommittee_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigaggregatedsignature_committee(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The list of signatures from committee members
     */
open func signatures() -> [MultisigMemberSignature]  {
    return try!  FfiConverterSequenceTypeMultisigMemberSignature.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigaggregatedsignature_signatures(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigaggregatedsignature_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: MultisigAggregatedSignature, other: MultisigAggregatedSignature) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigaggregatedsignature_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeMultisigAggregatedSignature_lower(other),$0
    )
}
        )
    }

}
extension MultisigAggregatedSignature: CustomDebugStringConvertible {}
extension MultisigAggregatedSignature: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMultisigAggregatedSignature: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = MultisigAggregatedSignature

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> MultisigAggregatedSignature {
        return MultisigAggregatedSignature(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: MultisigAggregatedSignature) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MultisigAggregatedSignature {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: MultisigAggregatedSignature, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMultisigAggregatedSignature_lift(_ pointer: UnsafeMutableRawPointer) throws -> MultisigAggregatedSignature {
    return try FfiConverterTypeMultisigAggregatedSignature.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMultisigAggregatedSignature_lower(_ value: MultisigAggregatedSignature) -> UnsafeMutableRawPointer {
    return FfiConverterTypeMultisigAggregatedSignature.lower(value)
}






public protocol MultisigAggregatorProtocol: AnyObject, Sendable {
    
    func finish() throws  -> MultisigAggregatedSignature
    
    func verifier()  -> MultisigVerifier
    
    func withSignature(signature: UserSignature) throws  -> MultisigAggregator
    
    func withVerifier(verifier: MultisigVerifier)  -> MultisigAggregator
    
}
open class MultisigAggregator: MultisigAggregatorProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_multisigaggregator(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_multisigaggregator(pointer, $0) }
    }

    
public static func newWithMessage(committee: MultisigCommittee, message: Data) -> MultisigAggregator  {
    return try!  FfiConverterTypeMultisigAggregator_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_multisigaggregator_new_with_message(
        FfiConverterTypeMultisigCommittee_lower(committee),
        FfiConverterData.lower(message),$0
    )
})
}
    
public static func newWithTransaction(committee: MultisigCommittee, transaction: Transaction) -> MultisigAggregator  {
    return try!  FfiConverterTypeMultisigAggregator_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_multisigaggregator_new_with_transaction(
        FfiConverterTypeMultisigCommittee_lower(committee),
        FfiConverterTypeTransaction_lower(transaction),$0
    )
})
}
    

    
open func finish()throws  -> MultisigAggregatedSignature  {
    return try  FfiConverterTypeMultisigAggregatedSignature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_multisigaggregator_finish(self.uniffiClonePointer(),$0
    )
})
}
    
open func verifier() -> MultisigVerifier  {
    return try!  FfiConverterTypeMultisigVerifier_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigaggregator_verifier(self.uniffiClonePointer(),$0
    )
})
}
    
open func withSignature(signature: UserSignature)throws  -> MultisigAggregator  {
    return try  FfiConverterTypeMultisigAggregator_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_multisigaggregator_with_signature(self.uniffiClonePointer(),
        FfiConverterTypeUserSignature_lower(signature),$0
    )
})
}
    
open func withVerifier(verifier: MultisigVerifier) -> MultisigAggregator  {
    return try!  FfiConverterTypeMultisigAggregator_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigaggregator_with_verifier(self.uniffiClonePointer(),
        FfiConverterTypeMultisigVerifier_lower(verifier),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigaggregator_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }

}
extension MultisigAggregator: CustomDebugStringConvertible {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMultisigAggregator: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = MultisigAggregator

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> MultisigAggregator {
        return MultisigAggregator(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: MultisigAggregator) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MultisigAggregator {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: MultisigAggregator, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMultisigAggregator_lift(_ pointer: UnsafeMutableRawPointer) throws -> MultisigAggregator {
    return try FfiConverterTypeMultisigAggregator.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMultisigAggregator_lower(_ value: MultisigAggregator) -> UnsafeMutableRawPointer {
    return FfiConverterTypeMultisigAggregator.lower(value)
}






/**
 * A multisig committee
 *
 * A `MultisigCommittee` is a set of members who collectively control a single
 * `Address` on the IOTA blockchain. The number of required signatures to
 * authorize the execution of a transaction is determined by
 * `(signature_0_weight + signature_1_weight ..) >= threshold`.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * multisig-committee = (vector multisig-member)
 * u16    ; threshold
 * ```
 *
 * There is also a legacy encoding for this type defined as:
 *
 * ```text
 * legacy-multisig-committee = (vector legacy-multisig-member)
 * u16     ; threshold
 * ```
 */
public protocol MultisigCommitteeProtocol: AnyObject, Sendable {
    
    /**
     * Derive an `Address` from this MultisigCommittee.
     *
     * A MultiSig address
     * is defined as the 32-byte Blake2b hash of serializing the
     * `SignatureScheme` flag (0x03), the threshold (in little endian), and
     * the concatenation of all n flag, public keys and its weight.
     *
     * `hash(0x03 || threshold || flag_1 || pk_1 || weight_1
     * || ... || flag_n || pk_n || weight_n)`.
     *
     * When flag_i is ZkLogin, the pk_i for the `ZkLoginPublicIdentifier`
     * refers to the same input used when deriving the address using the
     * `ZkLoginPublicIdentifier::derive_address_padded` method (using the
     * full 32-byte `address_seed` value).
     */
    func deriveAddress()  -> Address
    
    /**
     * Checks if the Committee is valid.
     *
     * A valid committee is one that:
     * - Has a nonzero threshold
     * - Has at least one member
     * - Has at most ten members
     * - No member has weight 0
     * - the sum of the weights of all members must be larger than the
     * threshold
     * - contains no duplicate members
     */
    func isValid()  -> Bool
    
    /**
     * The members of the committee
     */
    func members()  -> [MultisigMember]
    
    /**
     * Return the flag for this signature scheme
     */
    func scheme()  -> SignatureScheme
    
    /**
     * The total signature weight required to authorize a transaction for the
     * address corresponding to this `MultisigCommittee`.
     */
    func threshold()  -> UInt16
    
}
/**
 * A multisig committee
 *
 * A `MultisigCommittee` is a set of members who collectively control a single
 * `Address` on the IOTA blockchain. The number of required signatures to
 * authorize the execution of a transaction is determined by
 * `(signature_0_weight + signature_1_weight ..) >= threshold`.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * multisig-committee = (vector multisig-member)
 * u16    ; threshold
 * ```
 *
 * There is also a legacy encoding for this type defined as:
 *
 * ```text
 * legacy-multisig-committee = (vector legacy-multisig-member)
 * u16     ; threshold
 * ```
 */
open class MultisigCommittee: MultisigCommitteeProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_multisigcommittee(self.pointer, $0) }
    }
    /**
     * Construct a new committee from a list of `MultisigMember`s and a
     * `threshold`.
     *
     * Note that the order of the members is significant towards deriving the
     * `Address` governed by this committee.
     */
public convenience init(members: [MultisigMember], threshold: UInt16) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_multisigcommittee_new(
        FfiConverterSequenceTypeMultisigMember.lower(members),
        FfiConverterUInt16.lower(threshold),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_multisigcommittee(pointer, $0) }
    }

    

    
    /**
     * Derive an `Address` from this MultisigCommittee.
     *
     * A MultiSig address
     * is defined as the 32-byte Blake2b hash of serializing the
     * `SignatureScheme` flag (0x03), the threshold (in little endian), and
     * the concatenation of all n flag, public keys and its weight.
     *
     * `hash(0x03 || threshold || flag_1 || pk_1 || weight_1
     * || ... || flag_n || pk_n || weight_n)`.
     *
     * When flag_i is ZkLogin, the pk_i for the `ZkLoginPublicIdentifier`
     * refers to the same input used when deriving the address using the
     * `ZkLoginPublicIdentifier::derive_address_padded` method (using the
     * full 32-byte `address_seed` value).
     */
open func deriveAddress() -> Address  {
    return try!  FfiConverterTypeAddress_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigcommittee_derive_address(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Checks if the Committee is valid.
     *
     * A valid committee is one that:
     * - Has a nonzero threshold
     * - Has at least one member
     * - Has at most ten members
     * - No member has weight 0
     * - the sum of the weights of all members must be larger than the
     * threshold
     * - contains no duplicate members
     */
open func isValid() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigcommittee_is_valid(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The members of the committee
     */
open func members() -> [MultisigMember]  {
    return try!  FfiConverterSequenceTypeMultisigMember.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigcommittee_members(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Return the flag for this signature scheme
     */
open func scheme() -> SignatureScheme  {
    return try!  FfiConverterTypeSignatureScheme_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigcommittee_scheme(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The total signature weight required to authorize a transaction for the
     * address corresponding to this `MultisigCommittee`.
     */
open func threshold() -> UInt16  {
    return try!  FfiConverterUInt16.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigcommittee_threshold(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigcommittee_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: MultisigCommittee, other: MultisigCommittee) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigcommittee_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeMultisigCommittee_lower(other),$0
    )
}
        )
    }

}
extension MultisigCommittee: CustomDebugStringConvertible {}
extension MultisigCommittee: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMultisigCommittee: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = MultisigCommittee

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> MultisigCommittee {
        return MultisigCommittee(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: MultisigCommittee) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MultisigCommittee {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: MultisigCommittee, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMultisigCommittee_lift(_ pointer: UnsafeMutableRawPointer) throws -> MultisigCommittee {
    return try FfiConverterTypeMultisigCommittee.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMultisigCommittee_lower(_ value: MultisigCommittee) -> UnsafeMutableRawPointer {
    return FfiConverterTypeMultisigCommittee.lower(value)
}






/**
 * A member in a multisig committee
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * multisig-member = multisig-member-public-key
 * u8    ; weight
 * ```
 *
 * There is also a legacy encoding for this type defined as:
 *
 * ```text
 * legacy-multisig-member = legacy-multisig-member-public-key
 * u8     ; weight
 * ```
 */
public protocol MultisigMemberProtocol: AnyObject, Sendable {
    
    /**
     * This member's public key.
     */
    func publicKey()  -> MultisigMemberPublicKey
    
    /**
     * Weight of this member's signature.
     */
    func weight()  -> UInt8
    
}
/**
 * A member in a multisig committee
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * multisig-member = multisig-member-public-key
 * u8    ; weight
 * ```
 *
 * There is also a legacy encoding for this type defined as:
 *
 * ```text
 * legacy-multisig-member = legacy-multisig-member-public-key
 * u8     ; weight
 * ```
 */
open class MultisigMember: MultisigMemberProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_multisigmember(self.pointer, $0) }
    }
    /**
     * Construct a new member from a `MultisigMemberPublicKey` and a `weight`.
     */
public convenience init(publicKey: MultisigMemberPublicKey, weight: UInt8) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_multisigmember_new(
        FfiConverterTypeMultisigMemberPublicKey_lower(publicKey),
        FfiConverterUInt8.lower(weight),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_multisigmember(pointer, $0) }
    }

    

    
    /**
     * This member's public key.
     */
open func publicKey() -> MultisigMemberPublicKey  {
    return try!  FfiConverterTypeMultisigMemberPublicKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigmember_public_key(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Weight of this member's signature.
     */
open func weight() -> UInt8  {
    return try!  FfiConverterUInt8.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigmember_weight(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigmember_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: MultisigMember, other: MultisigMember) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigmember_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeMultisigMember_lower(other),$0
    )
}
        )
    }

}
extension MultisigMember: CustomDebugStringConvertible {}
extension MultisigMember: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMultisigMember: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = MultisigMember

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> MultisigMember {
        return MultisigMember(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: MultisigMember) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MultisigMember {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: MultisigMember, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMultisigMember_lift(_ pointer: UnsafeMutableRawPointer) throws -> MultisigMember {
    return try FfiConverterTypeMultisigMember.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMultisigMember_lower(_ value: MultisigMember) -> UnsafeMutableRawPointer {
    return FfiConverterTypeMultisigMember.lower(value)
}






/**
 * Enum of valid public keys for multisig committee members
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * multisig-member-public-key = ed25519-multisig-member-public-key /
 * secp256k1-multisig-member-public-key /
 * secp256r1-multisig-member-public-key /
 * zklogin-multisig-member-public-key
 *
 * ed25519-multisig-member-public-key   = %x00 ed25519-public-key
 * secp256k1-multisig-member-public-key = %x01 secp256k1-public-key
 * secp256r1-multisig-member-public-key = %x02 secp256r1-public-key
 * zklogin-multisig-member-public-key   = %x03 zklogin-public-identifier
 * ```
 *
 * There is also a legacy encoding for this type defined as:
 *
 * ```text
 * legacy-multisig-member-public-key = string ; which is valid base64 encoded
 * ; and the decoded bytes are defined
 * ; by legacy-public-key
 * legacy-public-key = (ed25519-flag ed25519-public-key) /
 * (secp256k1-flag secp256k1-public-key) /
 * (secp256r1-flag secp256r1-public-key)
 * ```
 */
public protocol MultisigMemberPublicKeyProtocol: AnyObject, Sendable {
    
    func asEd25519()  -> Ed25519PublicKey
    
    func asEd25519Opt()  -> Ed25519PublicKey?
    
    func asSecp256k1()  -> Secp256k1PublicKey
    
    func asSecp256k1Opt()  -> Secp256k1PublicKey?
    
    func asSecp256r1()  -> Secp256r1PublicKey
    
    func asSecp256r1Opt()  -> Secp256r1PublicKey?
    
    func asZklogin()  -> ZkLoginPublicIdentifier
    
    func asZkloginOpt()  -> ZkLoginPublicIdentifier?
    
    func isEd25519()  -> Bool
    
    func isSecp256k1()  -> Bool
    
    func isSecp256r1()  -> Bool
    
    func isZklogin()  -> Bool
    
    func scheme()  -> SignatureScheme
    
}
/**
 * Enum of valid public keys for multisig committee members
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * multisig-member-public-key = ed25519-multisig-member-public-key /
 * secp256k1-multisig-member-public-key /
 * secp256r1-multisig-member-public-key /
 * zklogin-multisig-member-public-key
 *
 * ed25519-multisig-member-public-key   = %x00 ed25519-public-key
 * secp256k1-multisig-member-public-key = %x01 secp256k1-public-key
 * secp256r1-multisig-member-public-key = %x02 secp256r1-public-key
 * zklogin-multisig-member-public-key   = %x03 zklogin-public-identifier
 * ```
 *
 * There is also a legacy encoding for this type defined as:
 *
 * ```text
 * legacy-multisig-member-public-key = string ; which is valid base64 encoded
 * ; and the decoded bytes are defined
 * ; by legacy-public-key
 * legacy-public-key = (ed25519-flag ed25519-public-key) /
 * (secp256k1-flag secp256k1-public-key) /
 * (secp256r1-flag secp256r1-public-key)
 * ```
 */
open class MultisigMemberPublicKey: MultisigMemberPublicKeyProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_multisigmemberpublickey(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_multisigmemberpublickey(pointer, $0) }
    }

    

    
open func asEd25519() -> Ed25519PublicKey  {
    return try!  FfiConverterTypeEd25519PublicKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigmemberpublickey_as_ed25519(self.uniffiClonePointer(),$0
    )
})
}
    
open func asEd25519Opt() -> Ed25519PublicKey?  {
    return try!  FfiConverterOptionTypeEd25519PublicKey.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigmemberpublickey_as_ed25519_opt(self.uniffiClonePointer(),$0
    )
})
}
    
open func asSecp256k1() -> Secp256k1PublicKey  {
    return try!  FfiConverterTypeSecp256k1PublicKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigmemberpublickey_as_secp256k1(self.uniffiClonePointer(),$0
    )
})
}
    
open func asSecp256k1Opt() -> Secp256k1PublicKey?  {
    return try!  FfiConverterOptionTypeSecp256k1PublicKey.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigmemberpublickey_as_secp256k1_opt(self.uniffiClonePointer(),$0
    )
})
}
    
open func asSecp256r1() -> Secp256r1PublicKey  {
    return try!  FfiConverterTypeSecp256r1PublicKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigmemberpublickey_as_secp256r1(self.uniffiClonePointer(),$0
    )
})
}
    
open func asSecp256r1Opt() -> Secp256r1PublicKey?  {
    return try!  FfiConverterOptionTypeSecp256r1PublicKey.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigmemberpublickey_as_secp256r1_opt(self.uniffiClonePointer(),$0
    )
})
}
    
open func asZklogin() -> ZkLoginPublicIdentifier  {
    return try!  FfiConverterTypeZkLoginPublicIdentifier_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigmemberpublickey_as_zklogin(self.uniffiClonePointer(),$0
    )
})
}
    
open func asZkloginOpt() -> ZkLoginPublicIdentifier?  {
    return try!  FfiConverterOptionTypeZkLoginPublicIdentifier.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigmemberpublickey_as_zklogin_opt(self.uniffiClonePointer(),$0
    )
})
}
    
open func isEd25519() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigmemberpublickey_is_ed25519(self.uniffiClonePointer(),$0
    )
})
}
    
open func isSecp256k1() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigmemberpublickey_is_secp256k1(self.uniffiClonePointer(),$0
    )
})
}
    
open func isSecp256r1() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigmemberpublickey_is_secp256r1(self.uniffiClonePointer(),$0
    )
})
}
    
open func isZklogin() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigmemberpublickey_is_zklogin(self.uniffiClonePointer(),$0
    )
})
}
    
open func scheme() -> SignatureScheme  {
    return try!  FfiConverterTypeSignatureScheme_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigmemberpublickey_scheme(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigmemberpublickey_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: MultisigMemberPublicKey, other: MultisigMemberPublicKey) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigmemberpublickey_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeMultisigMemberPublicKey_lower(other),$0
    )
}
        )
    }

}
extension MultisigMemberPublicKey: CustomDebugStringConvertible {}
extension MultisigMemberPublicKey: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMultisigMemberPublicKey: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = MultisigMemberPublicKey

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> MultisigMemberPublicKey {
        return MultisigMemberPublicKey(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: MultisigMemberPublicKey) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MultisigMemberPublicKey {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: MultisigMemberPublicKey, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMultisigMemberPublicKey_lift(_ pointer: UnsafeMutableRawPointer) throws -> MultisigMemberPublicKey {
    return try FfiConverterTypeMultisigMemberPublicKey.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMultisigMemberPublicKey_lower(_ value: MultisigMemberPublicKey) -> UnsafeMutableRawPointer {
    return FfiConverterTypeMultisigMemberPublicKey.lower(value)
}






/**
 * A signature from a member of a multisig committee.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * multisig-member-signature = ed25519-multisig-member-signature /
 * secp256k1-multisig-member-signature /
 * secp256r1-multisig-member-signature /
 * zklogin-multisig-member-signature
 *
 * ed25519-multisig-member-signature   = %x00 ed25519-signature
 * secp256k1-multisig-member-signature = %x01 secp256k1-signature
 * secp256r1-multisig-member-signature = %x02 secp256r1-signature
 * zklogin-multisig-member-signature   = %x03 zklogin-authenticator
 * ```
 */
public protocol MultisigMemberSignatureProtocol: AnyObject, Sendable {
    
    func asEd25519()  -> Ed25519Signature
    
    func asEd25519Opt()  -> Ed25519Signature?
    
    func asSecp256k1()  -> Secp256k1Signature
    
    func asSecp256k1Opt()  -> Secp256k1Signature?
    
    func asSecp256r1()  -> Secp256r1Signature
    
    func asSecp256r1Opt()  -> Secp256r1Signature?
    
    func asZklogin()  -> ZkLoginAuthenticator
    
    func asZkloginOpt()  -> ZkLoginAuthenticator?
    
    func isEd25519()  -> Bool
    
    func isSecp256k1()  -> Bool
    
    func isSecp256r1()  -> Bool
    
    func isZklogin()  -> Bool
    
}
/**
 * A signature from a member of a multisig committee.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * multisig-member-signature = ed25519-multisig-member-signature /
 * secp256k1-multisig-member-signature /
 * secp256r1-multisig-member-signature /
 * zklogin-multisig-member-signature
 *
 * ed25519-multisig-member-signature   = %x00 ed25519-signature
 * secp256k1-multisig-member-signature = %x01 secp256k1-signature
 * secp256r1-multisig-member-signature = %x02 secp256r1-signature
 * zklogin-multisig-member-signature   = %x03 zklogin-authenticator
 * ```
 */
open class MultisigMemberSignature: MultisigMemberSignatureProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_multisigmembersignature(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_multisigmembersignature(pointer, $0) }
    }

    

    
open func asEd25519() -> Ed25519Signature  {
    return try!  FfiConverterTypeEd25519Signature_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigmembersignature_as_ed25519(self.uniffiClonePointer(),$0
    )
})
}
    
open func asEd25519Opt() -> Ed25519Signature?  {
    return try!  FfiConverterOptionTypeEd25519Signature.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigmembersignature_as_ed25519_opt(self.uniffiClonePointer(),$0
    )
})
}
    
open func asSecp256k1() -> Secp256k1Signature  {
    return try!  FfiConverterTypeSecp256k1Signature_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigmembersignature_as_secp256k1(self.uniffiClonePointer(),$0
    )
})
}
    
open func asSecp256k1Opt() -> Secp256k1Signature?  {
    return try!  FfiConverterOptionTypeSecp256k1Signature.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigmembersignature_as_secp256k1_opt(self.uniffiClonePointer(),$0
    )
})
}
    
open func asSecp256r1() -> Secp256r1Signature  {
    return try!  FfiConverterTypeSecp256r1Signature_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigmembersignature_as_secp256r1(self.uniffiClonePointer(),$0
    )
})
}
    
open func asSecp256r1Opt() -> Secp256r1Signature?  {
    return try!  FfiConverterOptionTypeSecp256r1Signature.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigmembersignature_as_secp256r1_opt(self.uniffiClonePointer(),$0
    )
})
}
    
open func asZklogin() -> ZkLoginAuthenticator  {
    return try!  FfiConverterTypeZkLoginAuthenticator_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigmembersignature_as_zklogin(self.uniffiClonePointer(),$0
    )
})
}
    
open func asZkloginOpt() -> ZkLoginAuthenticator?  {
    return try!  FfiConverterOptionTypeZkLoginAuthenticator.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigmembersignature_as_zklogin_opt(self.uniffiClonePointer(),$0
    )
})
}
    
open func isEd25519() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigmembersignature_is_ed25519(self.uniffiClonePointer(),$0
    )
})
}
    
open func isSecp256k1() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigmembersignature_is_secp256k1(self.uniffiClonePointer(),$0
    )
})
}
    
open func isSecp256r1() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigmembersignature_is_secp256r1(self.uniffiClonePointer(),$0
    )
})
}
    
open func isZklogin() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigmembersignature_is_zklogin(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigmembersignature_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: MultisigMemberSignature, other: MultisigMemberSignature) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigmembersignature_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeMultisigMemberSignature_lower(other),$0
    )
}
        )
    }

}
extension MultisigMemberSignature: CustomDebugStringConvertible {}
extension MultisigMemberSignature: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMultisigMemberSignature: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = MultisigMemberSignature

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> MultisigMemberSignature {
        return MultisigMemberSignature(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: MultisigMemberSignature) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MultisigMemberSignature {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: MultisigMemberSignature, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMultisigMemberSignature_lift(_ pointer: UnsafeMutableRawPointer) throws -> MultisigMemberSignature {
    return try FfiConverterTypeMultisigMemberSignature.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMultisigMemberSignature_lower(_ value: MultisigMemberSignature) -> UnsafeMutableRawPointer {
    return FfiConverterTypeMultisigMemberSignature.lower(value)
}






public protocol MultisigVerifierProtocol: AnyObject, Sendable {
    
    func verify(message: Data, signature: MultisigAggregatedSignature) throws 
    
    func withZkloginVerifier(zkloginVerifier: ZkloginVerifier)  -> MultisigVerifier
    
    func zkloginVerifier()  -> ZkloginVerifier?
    
}
open class MultisigVerifier: MultisigVerifierProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_multisigverifier(self.pointer, $0) }
    }
public convenience init() {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_multisigverifier_new($0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_multisigverifier(pointer, $0) }
    }

    

    
open func verify(message: Data, signature: MultisigAggregatedSignature)throws   {try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_multisigverifier_verify(self.uniffiClonePointer(),
        FfiConverterData.lower(message),
        FfiConverterTypeMultisigAggregatedSignature_lower(signature),$0
    )
}
}
    
open func withZkloginVerifier(zkloginVerifier: ZkloginVerifier) -> MultisigVerifier  {
    return try!  FfiConverterTypeMultisigVerifier_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigverifier_with_zklogin_verifier(self.uniffiClonePointer(),
        FfiConverterTypeZkloginVerifier_lower(zkloginVerifier),$0
    )
})
}
    
open func zkloginVerifier() -> ZkloginVerifier?  {
    return try!  FfiConverterOptionTypeZkloginVerifier.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigverifier_zklogin_verifier(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_multisigverifier_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }

}
extension MultisigVerifier: CustomDebugStringConvertible {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMultisigVerifier: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = MultisigVerifier

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> MultisigVerifier {
        return MultisigVerifier(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: MultisigVerifier) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MultisigVerifier {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: MultisigVerifier, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMultisigVerifier_lift(_ pointer: UnsafeMutableRawPointer) throws -> MultisigVerifier {
    return try FfiConverterTypeMultisigVerifier.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMultisigVerifier_lower(_ value: MultisigVerifier) -> UnsafeMutableRawPointer {
    return FfiConverterTypeMultisigVerifier.lower(value)
}






public protocol NameProtocol: AnyObject, Sendable {
    
    /**
     * Formats a name into a string based on the available output formats.
     * The default separator is `.`
     */
    func format(format: NameFormat)  -> String
    
    /**
     * Returns whether this name is a second-level name (Ex. `test.iota`)
     */
    func isSln()  -> Bool
    
    /**
     * Returns whether this name is a subname (Ex. `sub.test.iota`)
     */
    func isSubname()  -> Bool
    
    /**
     * Get the label at the given index
     */
    func label(index: UInt32)  -> String?
    
    /**
     * Get all of the labels. NOTE: These are in reverse order starting with
     * the top-level name and proceeding to subnames.
     */
    func labels()  -> [String]
    
    /**
     * Returns the number of labels including TLN.
     */
    func numLabels()  -> UInt32
    
    /**
     * parents; second-level names return `None`.
     */
    func parent()  -> Name?
    
}
open class Name: NameProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_name(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_name(pointer, $0) }
    }

    
public static func fromStr(s: String)throws  -> Name  {
    return try  FfiConverterTypeName_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_name_from_str(
        FfiConverterString.lower(s),$0
    )
})
}
    

    
    /**
     * Formats a name into a string based on the available output formats.
     * The default separator is `.`
     */
open func format(format: NameFormat) -> String  {
    return try!  FfiConverterString.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_name_format(self.uniffiClonePointer(),
        FfiConverterTypeNameFormat_lower(format),$0
    )
})
}
    
    /**
     * Returns whether this name is a second-level name (Ex. `test.iota`)
     */
open func isSln() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_name_is_sln(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Returns whether this name is a subname (Ex. `sub.test.iota`)
     */
open func isSubname() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_name_is_subname(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Get the label at the given index
     */
open func label(index: UInt32) -> String?  {
    return try!  FfiConverterOptionString.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_name_label(self.uniffiClonePointer(),
        FfiConverterUInt32.lower(index),$0
    )
})
}
    
    /**
     * Get all of the labels. NOTE: These are in reverse order starting with
     * the top-level name and proceeding to subnames.
     */
open func labels() -> [String]  {
    return try!  FfiConverterSequenceString.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_name_labels(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Returns the number of labels including TLN.
     */
open func numLabels() -> UInt32  {
    return try!  FfiConverterUInt32.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_name_num_labels(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * parents; second-level names return `None`.
     */
open func parent() -> Name?  {
    return try!  FfiConverterOptionTypeName.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_name_parent(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_name_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    open var description: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_name_uniffi_trait_display(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: Name, other: Name) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_name_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeName_lower(other),$0
    )
}
        )
    }
    open func hash(into hasher: inout Hasher) {
        let val = try!  FfiConverterUInt64.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_name_uniffi_trait_hash(self.uniffiClonePointer(),$0
    )
}
        )
        hasher.combine(val)
    }

}
extension Name: CustomDebugStringConvertible {}
extension Name: CustomStringConvertible {}
extension Name: Equatable {}
extension Name: Hashable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeName: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = Name

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> Name {
        return Name(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: Name) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Name {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: Name, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeName_lift(_ pointer: UnsafeMutableRawPointer) throws -> Name {
    return try FfiConverterTypeName.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeName_lower(_ value: Name) -> UnsafeMutableRawPointer {
    return FfiConverterTypeName.lower(value)
}






/**
 * An object to manage a second-level name (SLN).
 */
public protocol NameRegistrationProtocol: AnyObject, Sendable {
    
    func expirationTimestampMs()  -> UInt64
    
    func id()  -> ObjectId
    
    func name()  -> Name
    
    func nameStr()  -> String
    
}
/**
 * An object to manage a second-level name (SLN).
 */
open class NameRegistration: NameRegistrationProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_nameregistration(self.pointer, $0) }
    }
public convenience init(id: ObjectId, name: Name, nameStr: String, expirationTimestampMs: UInt64) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_nameregistration_new(
        FfiConverterTypeObjectId_lower(id),
        FfiConverterTypeName_lower(name),
        FfiConverterString.lower(nameStr),
        FfiConverterUInt64.lower(expirationTimestampMs),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_nameregistration(pointer, $0) }
    }

    

    
open func expirationTimestampMs() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_nameregistration_expiration_timestamp_ms(self.uniffiClonePointer(),$0
    )
})
}
    
open func id() -> ObjectId  {
    return try!  FfiConverterTypeObjectId_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_nameregistration_id(self.uniffiClonePointer(),$0
    )
})
}
    
open func name() -> Name  {
    return try!  FfiConverterTypeName_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_nameregistration_name(self.uniffiClonePointer(),$0
    )
})
}
    
open func nameStr() -> String  {
    return try!  FfiConverterString.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_nameregistration_name_str(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_nameregistration_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: NameRegistration, other: NameRegistration) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_nameregistration_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeNameRegistration_lower(other),$0
    )
}
        )
    }

}
extension NameRegistration: CustomDebugStringConvertible {}
extension NameRegistration: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeNameRegistration: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = NameRegistration

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> NameRegistration {
        return NameRegistration(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: NameRegistration) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> NameRegistration {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: NameRegistration, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeNameRegistration_lift(_ pointer: UnsafeMutableRawPointer) throws -> NameRegistration {
    return try FfiConverterTypeNameRegistration.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeNameRegistration_lower(_ value: NameRegistration) -> UnsafeMutableRawPointer {
    return FfiConverterTypeNameRegistration.lower(value)
}






/**
 * An object on the IOTA blockchain
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * object = object-data owner digest u64
 * ```
 */
public protocol ObjectProtocol: AnyObject, Sendable {
    
    /**
     * Interpret this object as a move package
     */
    func asPackage()  -> MovePackage
    
    /**
     * Try to interpret this object as a move package
     */
    func asPackageOpt()  -> MovePackage?
    
    /**
     * Interpret this object as a move struct
     */
    func asStruct()  -> MoveStruct
    
    /**
     * Try to interpret this object as a move struct
     */
    func asStructOpt()  -> MoveStruct?
    
    /**
     * Return this object's data
     */
    func data()  -> ObjectData
    
    /**
     * Calculate the digest of this `Object`
     *
     * This is done by hashing the BCS bytes of this `Object` prefixed
     */
    func digest()  -> Digest
    
    /**
     * Return this object's id
     */
    func objectId()  -> ObjectId
    
    /**
     * Return this object's reference
     */
    func objectRef()  -> ObjectReference
    
    /**
     * Return this object's type
     */
    func objectType()  -> ObjectType
    
    /**
     * Return this object's owner
     */
    func owner()  -> Owner
    
    /**
     * Return the digest of the transaction that last modified this object
     */
    func previousTransaction()  -> Digest
    
    /**
     * Return the storage rebate locked in this object
     *
     * Storage rebates are credited to the gas coin used in a transaction that
     * deletes this object.
     */
    func storageRebate()  -> UInt64
    
    /**
     * Return this object's version
     */
    func version()  -> UInt64
    
}
/**
 * An object on the IOTA blockchain
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * object = object-data owner digest u64
 * ```
 */
open class Object: ObjectProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_object(self.pointer, $0) }
    }
public convenience init(data: ObjectData, owner: Owner, previousTransaction: Digest, storageRebate: UInt64) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_object_new(
        FfiConverterTypeObjectData_lower(data),
        FfiConverterTypeOwner_lower(owner),
        FfiConverterTypeDigest_lower(previousTransaction),
        FfiConverterUInt64.lower(storageRebate),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_object(pointer, $0) }
    }

    

    
    /**
     * Interpret this object as a move package
     */
open func asPackage() -> MovePackage  {
    return try!  FfiConverterTypeMovePackage_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_object_as_package(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Try to interpret this object as a move package
     */
open func asPackageOpt() -> MovePackage?  {
    return try!  FfiConverterOptionTypeMovePackage.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_object_as_package_opt(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Interpret this object as a move struct
     */
open func asStruct() -> MoveStruct  {
    return try!  FfiConverterTypeMoveStruct_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_object_as_struct(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Try to interpret this object as a move struct
     */
open func asStructOpt() -> MoveStruct?  {
    return try!  FfiConverterOptionTypeMoveStruct.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_object_as_struct_opt(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Return this object's data
     */
open func data() -> ObjectData  {
    return try!  FfiConverterTypeObjectData_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_object_data(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Calculate the digest of this `Object`
     *
     * This is done by hashing the BCS bytes of this `Object` prefixed
     */
open func digest() -> Digest  {
    return try!  FfiConverterTypeDigest_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_object_digest(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Return this object's id
     */
open func objectId() -> ObjectId  {
    return try!  FfiConverterTypeObjectId_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_object_object_id(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Return this object's reference
     */
open func objectRef() -> ObjectReference  {
    return try!  FfiConverterTypeObjectReference_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_object_object_ref(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Return this object's type
     */
open func objectType() -> ObjectType  {
    return try!  FfiConverterTypeObjectType_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_object_object_type(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Return this object's owner
     */
open func owner() -> Owner  {
    return try!  FfiConverterTypeOwner_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_object_owner(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Return the digest of the transaction that last modified this object
     */
open func previousTransaction() -> Digest  {
    return try!  FfiConverterTypeDigest_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_object_previous_transaction(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Return the storage rebate locked in this object
     *
     * Storage rebates are credited to the gas coin used in a transaction that
     * deletes this object.
     */
open func storageRebate() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_object_storage_rebate(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Return this object's version
     */
open func version() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_object_version(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_object_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: Object, other: Object) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_object_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeObject_lower(other),$0
    )
}
        )
    }

}
extension Object: CustomDebugStringConvertible {}
extension Object: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeObject: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = Object

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> Object {
        return Object(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: Object) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Object {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: Object, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeObject_lift(_ pointer: UnsafeMutableRawPointer) throws -> Object {
    return try FfiConverterTypeObject.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeObject_lower(_ value: Object) -> UnsafeMutableRawPointer {
    return FfiConverterTypeObject.lower(value)
}






/**
 * Object data, either a package or struct
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * object-data = object-data-struct / object-data-package
 *
 * object-data-struct  = %x00 object-move-struct
 * object-data-package = %x01 object-move-package
 * ```
 */
public protocol ObjectDataProtocol: AnyObject, Sendable {
    
    /**
     * Try to interpret this object as a `MovePackage`
     */
    func asPackageOpt()  -> MovePackage?
    
    /**
     * Try to interpret this object as a `MoveStruct`
     */
    func asStructOpt()  -> MoveStruct?
    
    /**
     * Return whether this object is a `MovePackage`
     */
    func isPackage()  -> Bool
    
    /**
     * Return whether this object is a `MoveStruct`
     */
    func isStruct()  -> Bool
    
}
/**
 * Object data, either a package or struct
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * object-data = object-data-struct / object-data-package
 *
 * object-data-struct  = %x00 object-move-struct
 * object-data-package = %x01 object-move-package
 * ```
 */
open class ObjectData: ObjectDataProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_objectdata(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_objectdata(pointer, $0) }
    }

    
    /**
     * Create an `ObjectData` from  `MovePackage`
     */
public static func newMovePackage(movePackage: MovePackage) -> ObjectData  {
    return try!  FfiConverterTypeObjectData_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_objectdata_new_move_package(
        FfiConverterTypeMovePackage_lower(movePackage),$0
    )
})
}
    
    /**
     * Create an `ObjectData` from a `MoveStruct`
     */
public static func newMoveStruct(moveStruct: MoveStruct) -> ObjectData  {
    return try!  FfiConverterTypeObjectData_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_objectdata_new_move_struct(
        FfiConverterTypeMoveStruct_lower(moveStruct),$0
    )
})
}
    

    
    /**
     * Try to interpret this object as a `MovePackage`
     */
open func asPackageOpt() -> MovePackage?  {
    return try!  FfiConverterOptionTypeMovePackage.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_objectdata_as_package_opt(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Try to interpret this object as a `MoveStruct`
     */
open func asStructOpt() -> MoveStruct?  {
    return try!  FfiConverterOptionTypeMoveStruct.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_objectdata_as_struct_opt(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Return whether this object is a `MovePackage`
     */
open func isPackage() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_objectdata_is_package(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Return whether this object is a `MoveStruct`
     */
open func isStruct() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_objectdata_is_struct(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_objectdata_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: ObjectData, other: ObjectData) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_objectdata_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeObjectData_lower(other),$0
    )
}
        )
    }
    open func hash(into hasher: inout Hasher) {
        let val = try!  FfiConverterUInt64.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_objectdata_uniffi_trait_hash(self.uniffiClonePointer(),$0
    )
}
        )
        hasher.combine(val)
    }

}
extension ObjectData: CustomDebugStringConvertible {}
extension ObjectData: Equatable {}
extension ObjectData: Hashable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeObjectData: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = ObjectData

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> ObjectData {
        return ObjectData(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: ObjectData) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ObjectData {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: ObjectData, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeObjectData_lift(_ pointer: UnsafeMutableRawPointer) throws -> ObjectData {
    return try FfiConverterTypeObjectData.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeObjectData_lower(_ value: ObjectData) -> UnsafeMutableRawPointer {
    return FfiConverterTypeObjectData.lower(value)
}






/**
 * An `ObjectId` is a 32-byte identifier used to uniquely identify an object on
 * the IOTA blockchain.
 *
 * ## Relationship to Address
 *
 * `Address`es and `ObjectId`s share the same 32-byte addressable space but
 * are derived leveraging different domain-separator values to ensure,
 * cryptographically, that there won't be any overlap, e.g. there can't be a
 * valid `Object` whose `ObjectId` is equal to that of the `Address` of a user
 * account.
 *
 * # BCS
 *
 * An `ObjectId`'s BCS serialized form is defined by the following:
 *
 * ```text
 * object-id = 32*OCTET
 * ```
 */
public protocol ObjectIdProtocol: AnyObject, Sendable {
    
    /**
     * Derive an ObjectId for a Dynamic Child Object.
     *
     * hash(parent || len(key) || key || key_type_tag)
     */
    func deriveDynamicChildId(keyTypeTag: TypeTag, keyBytes: Data)  -> ObjectId
    
    func toAddress()  -> Address
    
    func toBytes()  -> Data
    
    /**
     * Returns the string representation of this object ID using the
     * canonical display, with or without a `0x` prefix.
     */
    func toCanonicalString(withPrefix: Bool)  -> String
    
    func toHex()  -> String
    
    /**
     * Returns the shortest possible string representation of the object ID
     * (i.e. with leading zeroes trimmed).
     */
    func toShortString(withPrefix: Bool)  -> String
    
}
/**
 * An `ObjectId` is a 32-byte identifier used to uniquely identify an object on
 * the IOTA blockchain.
 *
 * ## Relationship to Address
 *
 * `Address`es and `ObjectId`s share the same 32-byte addressable space but
 * are derived leveraging different domain-separator values to ensure,
 * cryptographically, that there won't be any overlap, e.g. there can't be a
 * valid `Object` whose `ObjectId` is equal to that of the `Address` of a user
 * account.
 *
 * # BCS
 *
 * An `ObjectId`'s BCS serialized form is defined by the following:
 *
 * ```text
 * object-id = 32*OCTET
 * ```
 */
open class ObjectId: ObjectIdProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_objectid(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_objectid(pointer, $0) }
    }

    
public static func clock() -> ObjectId  {
    return try!  FfiConverterTypeObjectId_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_objectid_clock($0
    )
})
}
    
    /**
     * Create an ObjectId from a transaction digest and the number of objects
     * that have been created during a transactions.
     */
public static func deriveId(digest: Digest, count: UInt64) -> ObjectId  {
    return try!  FfiConverterTypeObjectId_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_objectid_derive_id(
        FfiConverterTypeDigest_lower(digest),
        FfiConverterUInt64.lower(count),$0
    )
})
}
    
public static func fromBytes(bytes: Data)throws  -> ObjectId  {
    return try  FfiConverterTypeObjectId_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_objectid_from_bytes(
        FfiConverterData.lower(bytes),$0
    )
})
}
    
public static func fromHex(hex: String)throws  -> ObjectId  {
    return try  FfiConverterTypeObjectId_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_objectid_from_hex(
        FfiConverterString.lower(hex),$0
    )
})
}
    
public static func system() -> ObjectId  {
    return try!  FfiConverterTypeObjectId_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_objectid_system($0
    )
})
}
    
public static func zero() -> ObjectId  {
    return try!  FfiConverterTypeObjectId_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_objectid_zero($0
    )
})
}
    

    
    /**
     * Derive an ObjectId for a Dynamic Child Object.
     *
     * hash(parent || len(key) || key || key_type_tag)
     */
open func deriveDynamicChildId(keyTypeTag: TypeTag, keyBytes: Data) -> ObjectId  {
    return try!  FfiConverterTypeObjectId_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_objectid_derive_dynamic_child_id(self.uniffiClonePointer(),
        FfiConverterTypeTypeTag_lower(keyTypeTag),
        FfiConverterData.lower(keyBytes),$0
    )
})
}
    
open func toAddress() -> Address  {
    return try!  FfiConverterTypeAddress_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_objectid_to_address(self.uniffiClonePointer(),$0
    )
})
}
    
open func toBytes() -> Data  {
    return try!  FfiConverterData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_objectid_to_bytes(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Returns the string representation of this object ID using the
     * canonical display, with or without a `0x` prefix.
     */
open func toCanonicalString(withPrefix: Bool) -> String  {
    return try!  FfiConverterString.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_objectid_to_canonical_string(self.uniffiClonePointer(),
        FfiConverterBool.lower(withPrefix),$0
    )
})
}
    
open func toHex() -> String  {
    return try!  FfiConverterString.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_objectid_to_hex(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Returns the shortest possible string representation of the object ID
     * (i.e. with leading zeroes trimmed).
     */
open func toShortString(withPrefix: Bool) -> String  {
    return try!  FfiConverterString.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_objectid_to_short_string(self.uniffiClonePointer(),
        FfiConverterBool.lower(withPrefix),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_objectid_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    open var description: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_objectid_uniffi_trait_display(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: ObjectId, other: ObjectId) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_objectid_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeObjectId_lower(other),$0
    )
}
        )
    }
    open func hash(into hasher: inout Hasher) {
        let val = try!  FfiConverterUInt64.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_objectid_uniffi_trait_hash(self.uniffiClonePointer(),$0
    )
}
        )
        hasher.combine(val)
    }

}
extension ObjectId: CustomDebugStringConvertible {}
extension ObjectId: CustomStringConvertible {}
extension ObjectId: Equatable {}
extension ObjectId: Hashable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeObjectId: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = ObjectId

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> ObjectId {
        return ObjectId(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: ObjectId) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ObjectId {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: ObjectId, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeObjectId_lift(_ pointer: UnsafeMutableRawPointer) throws -> ObjectId {
    return try FfiConverterTypeObjectId.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeObjectId_lower(_ value: ObjectId) -> UnsafeMutableRawPointer {
    return FfiConverterTypeObjectId.lower(value)
}






/**
 * Type of an IOTA object
 */
public protocol ObjectTypeProtocol: AnyObject, Sendable {
    
    func asStruct()  -> StructTag
    
    func asStructOpt()  -> StructTag?
    
    func isPackage()  -> Bool
    
    func isStruct()  -> Bool
    
}
/**
 * Type of an IOTA object
 */
open class ObjectType: ObjectTypeProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_objecttype(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_objecttype(pointer, $0) }
    }

    
public static func newPackage() -> ObjectType  {
    return try!  FfiConverterTypeObjectType_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_objecttype_new_package($0
    )
})
}
    
public static func newStruct(structTag: StructTag) -> ObjectType  {
    return try!  FfiConverterTypeObjectType_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_objecttype_new_struct(
        FfiConverterTypeStructTag_lower(structTag),$0
    )
})
}
    

    
open func asStruct() -> StructTag  {
    return try!  FfiConverterTypeStructTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_objecttype_as_struct(self.uniffiClonePointer(),$0
    )
})
}
    
open func asStructOpt() -> StructTag?  {
    return try!  FfiConverterOptionTypeStructTag.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_objecttype_as_struct_opt(self.uniffiClonePointer(),$0
    )
})
}
    
open func isPackage() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_objecttype_is_package(self.uniffiClonePointer(),$0
    )
})
}
    
open func isStruct() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_objecttype_is_struct(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_objecttype_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    open var description: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_objecttype_uniffi_trait_display(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: ObjectType, other: ObjectType) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_objecttype_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeObjectType_lower(other),$0
    )
}
        )
    }

}
extension ObjectType: CustomDebugStringConvertible {}
extension ObjectType: CustomStringConvertible {}
extension ObjectType: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeObjectType: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = ObjectType

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> ObjectType {
        return ObjectType(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: ObjectType) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ObjectType {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: ObjectType, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeObjectType_lift(_ pointer: UnsafeMutableRawPointer) throws -> ObjectType {
    return try FfiConverterTypeObjectType.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeObjectType_lower(_ value: ObjectType) -> UnsafeMutableRawPointer {
    return FfiConverterTypeObjectType.lower(value)
}






/**
 * Enum of different types of ownership for an object.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * owner = owner-address / owner-object / owner-shared / owner-immutable
 *
 * owner-address   = %x00 address
 * owner-object    = %x01 object-id
 * owner-shared    = %x02 u64
 * owner-immutable = %x03
 * ```
 */
public protocol OwnerProtocol: AnyObject, Sendable {
    
    func asAddress()  -> Address
    
    func asAddressOpt()  -> Address?
    
    func asObject()  -> ObjectId
    
    func asObjectOpt()  -> ObjectId?
    
    func asShared()  -> UInt64
    
    func asSharedOpt()  -> UInt64?
    
    func isAddress()  -> Bool
    
    func isImmutable()  -> Bool
    
    func isObject()  -> Bool
    
    func isShared()  -> Bool
    
}
/**
 * Enum of different types of ownership for an object.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * owner = owner-address / owner-object / owner-shared / owner-immutable
 *
 * owner-address   = %x00 address
 * owner-object    = %x01 object-id
 * owner-shared    = %x02 u64
 * owner-immutable = %x03
 * ```
 */
open class Owner: OwnerProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_owner(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_owner(pointer, $0) }
    }

    
public static func newAddress(address: Address) -> Owner  {
    return try!  FfiConverterTypeOwner_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_owner_new_address(
        FfiConverterTypeAddress_lower(address),$0
    )
})
}
    
public static func newImmutable() -> Owner  {
    return try!  FfiConverterTypeOwner_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_owner_new_immutable($0
    )
})
}
    
public static func newObject(id: ObjectId) -> Owner  {
    return try!  FfiConverterTypeOwner_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_owner_new_object(
        FfiConverterTypeObjectId_lower(id),$0
    )
})
}
    
public static func newShared(version: UInt64) -> Owner  {
    return try!  FfiConverterTypeOwner_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_owner_new_shared(
        FfiConverterUInt64.lower(version),$0
    )
})
}
    

    
open func asAddress() -> Address  {
    return try!  FfiConverterTypeAddress_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_owner_as_address(self.uniffiClonePointer(),$0
    )
})
}
    
open func asAddressOpt() -> Address?  {
    return try!  FfiConverterOptionTypeAddress.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_owner_as_address_opt(self.uniffiClonePointer(),$0
    )
})
}
    
open func asObject() -> ObjectId  {
    return try!  FfiConverterTypeObjectId_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_owner_as_object(self.uniffiClonePointer(),$0
    )
})
}
    
open func asObjectOpt() -> ObjectId?  {
    return try!  FfiConverterOptionTypeObjectId.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_owner_as_object_opt(self.uniffiClonePointer(),$0
    )
})
}
    
open func asShared() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_owner_as_shared(self.uniffiClonePointer(),$0
    )
})
}
    
open func asSharedOpt() -> UInt64?  {
    return try!  FfiConverterOptionUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_owner_as_shared_opt(self.uniffiClonePointer(),$0
    )
})
}
    
open func isAddress() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_owner_is_address(self.uniffiClonePointer(),$0
    )
})
}
    
open func isImmutable() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_owner_is_immutable(self.uniffiClonePointer(),$0
    )
})
}
    
open func isObject() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_owner_is_object(self.uniffiClonePointer(),$0
    )
})
}
    
open func isShared() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_owner_is_shared(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_owner_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    open var description: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_owner_uniffi_trait_display(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: Owner, other: Owner) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_owner_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeOwner_lower(other),$0
    )
}
        )
    }
    open func hash(into hasher: inout Hasher) {
        let val = try!  FfiConverterUInt64.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_owner_uniffi_trait_hash(self.uniffiClonePointer(),$0
    )
}
        )
        hasher.combine(val)
    }

}
extension Owner: CustomDebugStringConvertible {}
extension Owner: CustomStringConvertible {}
extension Owner: Equatable {}
extension Owner: Hashable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeOwner: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = Owner

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> Owner {
        return Owner(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: Owner) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Owner {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: Owner, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeOwner_lift(_ pointer: UnsafeMutableRawPointer) throws -> Owner {
    return try FfiConverterTypeOwner.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeOwner_lower(_ value: Owner) -> UnsafeMutableRawPointer {
    return FfiConverterTypeOwner.lower(value)
}






public protocol PtbArgumentProtocol: AnyObject, Sendable {
    
}
open class PtbArgument: PtbArgumentProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_ptbargument(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_ptbargument(pointer, $0) }
    }

    
public static func address(address: Address) -> PtbArgument  {
    return try!  FfiConverterTypePTBArgument_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_ptbargument_address(
        FfiConverterTypeAddress_lower(address),$0
    )
})
}
    
public static func addressFromHex(hex: String)throws  -> PtbArgument  {
    return try  FfiConverterTypePTBArgument_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_ptbargument_address_from_hex(
        FfiConverterString.lower(hex),$0
    )
})
}
    
public static func addressVec(addresses: [Address]) -> PtbArgument  {
    return try!  FfiConverterTypePTBArgument_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_ptbargument_address_vec(
        FfiConverterSequenceTypeAddress.lower(addresses),$0
    )
})
}
    
public static func addressVecFromHex(addresses: [String])throws  -> PtbArgument  {
    return try  FfiConverterTypePTBArgument_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_ptbargument_address_vec_from_hex(
        FfiConverterSequenceString.lower(addresses),$0
    )
})
}
    
public static func assigned(name: String) -> PtbArgument  {
    return try!  FfiConverterTypePTBArgument_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_ptbargument_assigned(
        FfiConverterString.lower(name),$0
    )
})
}
    
public static func bool(value: Bool) -> PtbArgument  {
    return try!  FfiConverterTypePTBArgument_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_ptbargument_bool(
        FfiConverterBool.lower(value),$0
    )
})
}
    
public static func boolVec(values: [Bool]) -> PtbArgument  {
    return try!  FfiConverterTypePTBArgument_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_ptbargument_bool_vec(
        FfiConverterSequenceBool.lower(values),$0
    )
})
}
    
public static func digest(digest: Digest) -> PtbArgument  {
    return try!  FfiConverterTypePTBArgument_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_ptbargument_digest(
        FfiConverterTypeDigest_lower(digest),$0
    )
})
}
    
public static func digestFromBase58(base58: String)throws  -> PtbArgument  {
    return try  FfiConverterTypePTBArgument_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_ptbargument_digest_from_base58(
        FfiConverterString.lower(base58),$0
    )
})
}
    
public static func digestVec(digests: [Digest]) -> PtbArgument  {
    return try!  FfiConverterTypePTBArgument_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_ptbargument_digest_vec(
        FfiConverterSequenceTypeDigest.lower(digests),$0
    )
})
}
    
public static func digestVecFromBase58(digests: [String])throws  -> PtbArgument  {
    return try  FfiConverterTypePTBArgument_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_ptbargument_digest_vec_from_base58(
        FfiConverterSequenceString.lower(digests),$0
    )
})
}
    
public static func gas() -> PtbArgument  {
    return try!  FfiConverterTypePTBArgument_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_ptbargument_gas($0
    )
})
}
    
public static func moveArg(arg: MoveArg) -> PtbArgument  {
    return try!  FfiConverterTypePTBArgument_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_ptbargument_move_arg(
        FfiConverterTypeMoveArg_lower(arg),$0
    )
})
}
    
public static func objectId(id: ObjectId) -> PtbArgument  {
    return try!  FfiConverterTypePTBArgument_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_ptbargument_object_id(
        FfiConverterTypeObjectId_lower(id),$0
    )
})
}
    
public static func objectIdFromHex(hex: String)throws  -> PtbArgument  {
    return try  FfiConverterTypePTBArgument_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_ptbargument_object_id_from_hex(
        FfiConverterString.lower(hex),$0
    )
})
}
    
public static func objectRef(id: ObjectReference) -> PtbArgument  {
    return try!  FfiConverterTypePTBArgument_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_ptbargument_object_ref(
        FfiConverterTypeObjectReference_lower(id),$0
    )
})
}
    
public static func option(value: MoveArg?) -> PtbArgument  {
    return try!  FfiConverterTypePTBArgument_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_ptbargument_option(
        FfiConverterOptionTypeMoveArg.lower(value),$0
    )
})
}
    
public static func receiving(id: ObjectId) -> PtbArgument  {
    return try!  FfiConverterTypePTBArgument_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_ptbargument_receiving(
        FfiConverterTypeObjectId_lower(id),$0
    )
})
}
    
public static func receivingFromHex(hex: String)throws  -> PtbArgument  {
    return try  FfiConverterTypePTBArgument_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_ptbargument_receiving_from_hex(
        FfiConverterString.lower(hex),$0
    )
})
}
    
public static func shared(id: ObjectId) -> PtbArgument  {
    return try!  FfiConverterTypePTBArgument_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_ptbargument_shared(
        FfiConverterTypeObjectId_lower(id),$0
    )
})
}
    
public static func sharedFromHex(hex: String)throws  -> PtbArgument  {
    return try  FfiConverterTypePTBArgument_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_ptbargument_shared_from_hex(
        FfiConverterString.lower(hex),$0
    )
})
}
    
public static func sharedMut(id: ObjectId) -> PtbArgument  {
    return try!  FfiConverterTypePTBArgument_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_ptbargument_shared_mut(
        FfiConverterTypeObjectId_lower(id),$0
    )
})
}
    
public static func sharedMutFromHex(hex: String)throws  -> PtbArgument  {
    return try  FfiConverterTypePTBArgument_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_ptbargument_shared_mut_from_hex(
        FfiConverterString.lower(hex),$0
    )
})
}
    
public static func string(string: String) -> PtbArgument  {
    return try!  FfiConverterTypePTBArgument_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_ptbargument_string(
        FfiConverterString.lower(string),$0
    )
})
}
    
public static func u128(value: String)throws  -> PtbArgument  {
    return try  FfiConverterTypePTBArgument_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_ptbargument_u128(
        FfiConverterString.lower(value),$0
    )
})
}
    
public static func u128Vec(values: [String])throws  -> PtbArgument  {
    return try  FfiConverterTypePTBArgument_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_ptbargument_u128_vec(
        FfiConverterSequenceString.lower(values),$0
    )
})
}
    
public static func u16(value: UInt16) -> PtbArgument  {
    return try!  FfiConverterTypePTBArgument_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_ptbargument_u16(
        FfiConverterUInt16.lower(value),$0
    )
})
}
    
public static func u16Vec(values: [UInt16]) -> PtbArgument  {
    return try!  FfiConverterTypePTBArgument_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_ptbargument_u16_vec(
        FfiConverterSequenceUInt16.lower(values),$0
    )
})
}
    
public static func u256(value: String)throws  -> PtbArgument  {
    return try  FfiConverterTypePTBArgument_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_ptbargument_u256(
        FfiConverterString.lower(value),$0
    )
})
}
    
public static func u256Vec(values: [String])throws  -> PtbArgument  {
    return try  FfiConverterTypePTBArgument_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_ptbargument_u256_vec(
        FfiConverterSequenceString.lower(values),$0
    )
})
}
    
public static func u32(value: UInt32) -> PtbArgument  {
    return try!  FfiConverterTypePTBArgument_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_ptbargument_u32(
        FfiConverterUInt32.lower(value),$0
    )
})
}
    
public static func u32Vec(values: [UInt32]) -> PtbArgument  {
    return try!  FfiConverterTypePTBArgument_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_ptbargument_u32_vec(
        FfiConverterSequenceUInt32.lower(values),$0
    )
})
}
    
public static func u64(value: UInt64) -> PtbArgument  {
    return try!  FfiConverterTypePTBArgument_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_ptbargument_u64(
        FfiConverterUInt64.lower(value),$0
    )
})
}
    
public static func u64Vec(values: [UInt64]) -> PtbArgument  {
    return try!  FfiConverterTypePTBArgument_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_ptbargument_u64_vec(
        FfiConverterSequenceUInt64.lower(values),$0
    )
})
}
    
public static func u8(value: UInt8) -> PtbArgument  {
    return try!  FfiConverterTypePTBArgument_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_ptbargument_u8(
        FfiConverterUInt8.lower(value),$0
    )
})
}
    
public static func u8Vec(values: Data) -> PtbArgument  {
    return try!  FfiConverterTypePTBArgument_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_ptbargument_u8_vec(
        FfiConverterData.lower(values),$0
    )
})
}
    

    

}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypePTBArgument: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = PtbArgument

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> PtbArgument {
        return PtbArgument(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: PtbArgument) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> PtbArgument {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: PtbArgument, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePTBArgument_lift(_ pointer: UnsafeMutableRawPointer) throws -> PtbArgument {
    return try FfiConverterTypePTBArgument.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePTBArgument_lower(_ value: PtbArgument) -> UnsafeMutableRawPointer {
    return FfiConverterTypePTBArgument.lower(value)
}






/**
 * A passkey authenticator.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * passkey-bcs = bytes               ; where the contents of the bytes are
 * ; defined by <passkey>
 * passkey     = passkey-flag
 * bytes               ; passkey authenticator data
 * client-data-json    ; valid json
 * simple-signature    ; required to be a secp256r1 signature
 *
 * client-data-json = string ; valid json
 * ```
 *
 * See <https://www.w3.org/TR/webauthn-2/#dictdef-collectedclientdata> for
 * the required json-schema for the `client-data-json` rule. In addition, IOTA
 * currently requires that the `CollectedClientData.type` field is required to
 * be `webauthn.get`.
 *
 * Note: Due to historical reasons, signatures are serialized slightly
 * different from the majority of the types in IOTA. In particular if a
 * signature is ever embedded in another structure it generally is serialized
 * as `bytes` meaning it has a length prefix that defines the length of
 * the completely serialized signature.
 */
public protocol PasskeyAuthenticatorProtocol: AnyObject, Sendable {
    
    /**
     * Opaque authenticator data for this passkey signature.
     *
     * See <https://www.w3.org/TR/webauthn-2/#sctn-authenticator-data>
     * for more information on this field.
     */
    func authenticatorData()  -> Data
    
    /**
     * The parsed challenge message for this passkey signature.
     *
     * This is parsed by decoding the base64url data from the
     * `client_data_json.challenge` field.
     */
    func challenge()  -> Data
    
    /**
     * Structured, unparsed, JSON for this passkey signature.
     *
     * See <https://www.w3.org/TR/webauthn-2/#dictdef-collectedclientdata>
     * for more information on this field.
     */
    func clientDataJson()  -> String
    
    /**
     * The passkey public key
     */
    func publicKey()  -> PasskeyPublicKey
    
    /**
     * The passkey signature.
     */
    func signature()  -> SimpleSignature
    
}
/**
 * A passkey authenticator.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * passkey-bcs = bytes               ; where the contents of the bytes are
 * ; defined by <passkey>
 * passkey     = passkey-flag
 * bytes               ; passkey authenticator data
 * client-data-json    ; valid json
 * simple-signature    ; required to be a secp256r1 signature
 *
 * client-data-json = string ; valid json
 * ```
 *
 * See <https://www.w3.org/TR/webauthn-2/#dictdef-collectedclientdata> for
 * the required json-schema for the `client-data-json` rule. In addition, IOTA
 * currently requires that the `CollectedClientData.type` field is required to
 * be `webauthn.get`.
 *
 * Note: Due to historical reasons, signatures are serialized slightly
 * different from the majority of the types in IOTA. In particular if a
 * signature is ever embedded in another structure it generally is serialized
 * as `bytes` meaning it has a length prefix that defines the length of
 * the completely serialized signature.
 */
open class PasskeyAuthenticator: PasskeyAuthenticatorProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_passkeyauthenticator(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_passkeyauthenticator(pointer, $0) }
    }

    

    
    /**
     * Opaque authenticator data for this passkey signature.
     *
     * See <https://www.w3.org/TR/webauthn-2/#sctn-authenticator-data>
     * for more information on this field.
     */
open func authenticatorData() -> Data  {
    return try!  FfiConverterData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_passkeyauthenticator_authenticator_data(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The parsed challenge message for this passkey signature.
     *
     * This is parsed by decoding the base64url data from the
     * `client_data_json.challenge` field.
     */
open func challenge() -> Data  {
    return try!  FfiConverterData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_passkeyauthenticator_challenge(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Structured, unparsed, JSON for this passkey signature.
     *
     * See <https://www.w3.org/TR/webauthn-2/#dictdef-collectedclientdata>
     * for more information on this field.
     */
open func clientDataJson() -> String  {
    return try!  FfiConverterString.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_passkeyauthenticator_client_data_json(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The passkey public key
     */
open func publicKey() -> PasskeyPublicKey  {
    return try!  FfiConverterTypePasskeyPublicKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_passkeyauthenticator_public_key(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The passkey signature.
     */
open func signature() -> SimpleSignature  {
    return try!  FfiConverterTypeSimpleSignature_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_passkeyauthenticator_signature(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_passkeyauthenticator_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: PasskeyAuthenticator, other: PasskeyAuthenticator) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_passkeyauthenticator_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypePasskeyAuthenticator_lower(other),$0
    )
}
        )
    }

}
extension PasskeyAuthenticator: CustomDebugStringConvertible {}
extension PasskeyAuthenticator: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypePasskeyAuthenticator: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = PasskeyAuthenticator

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> PasskeyAuthenticator {
        return PasskeyAuthenticator(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: PasskeyAuthenticator) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> PasskeyAuthenticator {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: PasskeyAuthenticator, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePasskeyAuthenticator_lift(_ pointer: UnsafeMutableRawPointer) throws -> PasskeyAuthenticator {
    return try FfiConverterTypePasskeyAuthenticator.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePasskeyAuthenticator_lower(_ value: PasskeyAuthenticator) -> UnsafeMutableRawPointer {
    return FfiConverterTypePasskeyAuthenticator.lower(value)
}






/**
 * Public key of a `PasskeyAuthenticator`.
 *
 * This is used to derive the onchain `Address` for a `PasskeyAuthenticator`.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * passkey-public-key = passkey-flag secp256r1-public-key
 * ```
 */
public protocol PasskeyPublicKeyProtocol: AnyObject, Sendable {
    
    /**
     * Derive an `Address` from this Passkey Public Key
     *
     * An `Address` can be derived from a `PasskeyPublicKey` by hashing the
     * bytes of the `Secp256r1PublicKey` that corresponds to this passkey
     * prefixed with the Passkey `SignatureScheme` flag (`0x06`).
     *
     * `hash( 0x06 || 33-byte secp256r1 public key)`
     */
    func deriveAddress()  -> Address
    
    func inner()  -> Secp256r1PublicKey
    
}
/**
 * Public key of a `PasskeyAuthenticator`.
 *
 * This is used to derive the onchain `Address` for a `PasskeyAuthenticator`.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * passkey-public-key = passkey-flag secp256r1-public-key
 * ```
 */
open class PasskeyPublicKey: PasskeyPublicKeyProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_passkeypublickey(self.pointer, $0) }
    }
public convenience init(publicKey: Secp256r1PublicKey) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_passkeypublickey_new(
        FfiConverterTypeSecp256r1PublicKey_lower(publicKey),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_passkeypublickey(pointer, $0) }
    }

    

    
    /**
     * Derive an `Address` from this Passkey Public Key
     *
     * An `Address` can be derived from a `PasskeyPublicKey` by hashing the
     * bytes of the `Secp256r1PublicKey` that corresponds to this passkey
     * prefixed with the Passkey `SignatureScheme` flag (`0x06`).
     *
     * `hash( 0x06 || 33-byte secp256r1 public key)`
     */
open func deriveAddress() -> Address  {
    return try!  FfiConverterTypeAddress_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_passkeypublickey_derive_address(self.uniffiClonePointer(),$0
    )
})
}
    
open func inner() -> Secp256r1PublicKey  {
    return try!  FfiConverterTypeSecp256r1PublicKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_passkeypublickey_inner(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_passkeypublickey_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: PasskeyPublicKey, other: PasskeyPublicKey) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_passkeypublickey_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypePasskeyPublicKey_lower(other),$0
    )
}
        )
    }

}
extension PasskeyPublicKey: CustomDebugStringConvertible {}
extension PasskeyPublicKey: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypePasskeyPublicKey: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = PasskeyPublicKey

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> PasskeyPublicKey {
        return PasskeyPublicKey(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: PasskeyPublicKey) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> PasskeyPublicKey {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: PasskeyPublicKey, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePasskeyPublicKey_lift(_ pointer: UnsafeMutableRawPointer) throws -> PasskeyPublicKey {
    return try FfiConverterTypePasskeyPublicKey.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePasskeyPublicKey_lower(_ value: PasskeyPublicKey) -> UnsafeMutableRawPointer {
    return FfiConverterTypePasskeyPublicKey.lower(value)
}






public protocol PasskeyVerifierProtocol: AnyObject, Sendable {
    
    func verify(message: Data, authenticator: PasskeyAuthenticator) throws 
    
}
open class PasskeyVerifier: PasskeyVerifierProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_passkeyverifier(self.pointer, $0) }
    }
public convenience init() {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_passkeyverifier_new($0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_passkeyverifier(pointer, $0) }
    }

    

    
open func verify(message: Data, authenticator: PasskeyAuthenticator)throws   {try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_passkeyverifier_verify(self.uniffiClonePointer(),
        FfiConverterData.lower(message),
        FfiConverterTypePasskeyAuthenticator_lower(authenticator),$0
    )
}
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_passkeyverifier_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }

}
extension PasskeyVerifier: CustomDebugStringConvertible {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypePasskeyVerifier: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = PasskeyVerifier

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> PasskeyVerifier {
        return PasskeyVerifier(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: PasskeyVerifier) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> PasskeyVerifier {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: PasskeyVerifier, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePasskeyVerifier_lift(_ pointer: UnsafeMutableRawPointer) throws -> PasskeyVerifier {
    return try FfiConverterTypePasskeyVerifier.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePasskeyVerifier_lower(_ value: PasskeyVerifier) -> UnsafeMutableRawPointer {
    return FfiConverterTypePasskeyVerifier.lower(value)
}






/**
 * A personal message that wraps around a byte array.
 */
public protocol PersonalMessageProtocol: AnyObject, Sendable {
    
    /**
     * Get the message as bytes.
     */
    func messageBytes()  -> Data
    
    /**
     * Get the signing digest as bytes.
     */
    func signingDigest()  -> Data
    
    /**
     * Get the signing digest as hex string.
     */
    func signingDigestHex()  -> String
    
}
/**
 * A personal message that wraps around a byte array.
 */
open class PersonalMessage: PersonalMessageProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_personalmessage(self.pointer, $0) }
    }
    /**
     * Create a new personal message from bytes.
     */
public convenience init(messageBytes: Data) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_personalmessage_new(
        FfiConverterData.lower(messageBytes),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_personalmessage(pointer, $0) }
    }

    

    
    /**
     * Get the message as bytes.
     */
open func messageBytes() -> Data  {
    return try!  FfiConverterData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_personalmessage_message_bytes(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Get the signing digest as bytes.
     */
open func signingDigest() -> Data  {
    return try!  FfiConverterData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_personalmessage_signing_digest(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Get the signing digest as hex string.
     */
open func signingDigestHex() -> String  {
    return try!  FfiConverterString.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_personalmessage_signing_digest_hex(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_personalmessage_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: PersonalMessage, other: PersonalMessage) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_personalmessage_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypePersonalMessage_lower(other),$0
    )
}
        )
    }

}
extension PersonalMessage: CustomDebugStringConvertible {}
extension PersonalMessage: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypePersonalMessage: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = PersonalMessage

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> PersonalMessage {
        return PersonalMessage(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: PersonalMessage) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> PersonalMessage {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: PersonalMessage, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePersonalMessage_lift(_ pointer: UnsafeMutableRawPointer) throws -> PersonalMessage {
    return try FfiConverterTypePersonalMessage.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePersonalMessage_lower(_ value: PersonalMessage) -> UnsafeMutableRawPointer {
    return FfiConverterTypePersonalMessage.lower(value)
}






/**
 * A user transaction
 *
 * Contains a series of native commands and move calls where the results of one
 * command can be used in future commands.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * ptb = (vector input) (vector command)
 * ```
 */
public protocol ProgrammableTransactionProtocol: AnyObject, Sendable {
    
    /**
     * The commands to be executed sequentially. A failure in any command will
     * result in the failure of the entire transaction.
     */
    func commands()  -> [Command]
    
    /**
     * Input objects or primitive values
     */
    func inputs()  -> [Input]
    
}
/**
 * A user transaction
 *
 * Contains a series of native commands and move calls where the results of one
 * command can be used in future commands.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * ptb = (vector input) (vector command)
 * ```
 */
open class ProgrammableTransaction: ProgrammableTransactionProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_programmabletransaction(self.pointer, $0) }
    }
public convenience init(inputs: [Input], commands: [Command]) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_programmabletransaction_new(
        FfiConverterSequenceTypeInput.lower(inputs),
        FfiConverterSequenceTypeCommand.lower(commands),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_programmabletransaction(pointer, $0) }
    }

    

    
    /**
     * The commands to be executed sequentially. A failure in any command will
     * result in the failure of the entire transaction.
     */
open func commands() -> [Command]  {
    return try!  FfiConverterSequenceTypeCommand.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_programmabletransaction_commands(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Input objects or primitive values
     */
open func inputs() -> [Input]  {
    return try!  FfiConverterSequenceTypeInput.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_programmabletransaction_inputs(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_programmabletransaction_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: ProgrammableTransaction, other: ProgrammableTransaction) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_programmabletransaction_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeProgrammableTransaction_lower(other),$0
    )
}
        )
    }

}
extension ProgrammableTransaction: CustomDebugStringConvertible {}
extension ProgrammableTransaction: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeProgrammableTransaction: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = ProgrammableTransaction

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> ProgrammableTransaction {
        return ProgrammableTransaction(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: ProgrammableTransaction) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ProgrammableTransaction {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: ProgrammableTransaction, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeProgrammableTransaction_lift(_ pointer: UnsafeMutableRawPointer) throws -> ProgrammableTransaction {
    return try FfiConverterTypeProgrammableTransaction.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeProgrammableTransaction_lower(_ value: ProgrammableTransaction) -> UnsafeMutableRawPointer {
    return FfiConverterTypeProgrammableTransaction.lower(value)
}






/**
 * Command to publish a new move package
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * publish = (vector bytes)        ; the serialized move modules
 * (vector object-id)    ; the set of package dependencies
 * ```
 */
public protocol PublishProtocol: AnyObject, Sendable {
    
    /**
     * Set of packages that the to-be published package depends on
     */
    func dependencies()  -> [ObjectId]
    
    /**
     * The serialized move modules
     */
    func modules()  -> [Data]
    
}
/**
 * Command to publish a new move package
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * publish = (vector bytes)        ; the serialized move modules
 * (vector object-id)    ; the set of package dependencies
 * ```
 */
open class Publish: PublishProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_publish(self.pointer, $0) }
    }
public convenience init(modules: [Data], dependencies: [ObjectId]) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_publish_new(
        FfiConverterSequenceData.lower(modules),
        FfiConverterSequenceTypeObjectId.lower(dependencies),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_publish(pointer, $0) }
    }

    

    
    /**
     * Set of packages that the to-be published package depends on
     */
open func dependencies() -> [ObjectId]  {
    return try!  FfiConverterSequenceTypeObjectId.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_publish_dependencies(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The serialized move modules
     */
open func modules() -> [Data]  {
    return try!  FfiConverterSequenceData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_publish_modules(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_publish_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: Publish, other: Publish) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_publish_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypePublish_lower(other),$0
    )
}
        )
    }

}
extension Publish: CustomDebugStringConvertible {}
extension Publish: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypePublish: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = Publish

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> Publish {
        return Publish(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: Publish) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Publish {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: Publish, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePublish_lift(_ pointer: UnsafeMutableRawPointer) throws -> Publish {
    return try FfiConverterTypePublish.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePublish_lower(_ value: Publish) -> UnsafeMutableRawPointer {
    return FfiConverterTypePublish.lower(value)
}






public protocol Secp256k1PrivateKeyProtocol: AnyObject, Sendable {
    
    func publicKey()  -> Secp256k1PublicKey
    
    func scheme()  -> SignatureScheme
    
    /**
     * Sign a personal message and return a UserSignature.
     */
    func signPersonalMessage(message: PersonalMessage) throws  -> UserSignature
    
    /**
     * Sign a transaction and return a UserSignature.
     */
    func signTransaction(transaction: Transaction) throws  -> UserSignature
    
    /**
     * Encode this private key as `flag || privkey` in Bech32 starting with
     * "iotaprivkey" to a string.
     */
    func toBech32() throws  -> String
    
    /**
     * Serialize this private key to bytes.
     */
    func toBytes()  -> Data
    
    /**
     * Serialize this private key as DER-encoded PKCS#8
     */
    func toDer() throws  -> Data
    
    /**
     * Serialize this private key as PEM-encoded PKCS#8
     */
    func toPem() throws  -> String
    
    func trySign(message: Data) throws  -> Secp256k1Signature
    
    func trySignSimple(message: Data) throws  -> SimpleSignature
    
    func trySignUser(message: Data) throws  -> UserSignature
    
    func verifyingKey()  -> Secp256k1VerifyingKey
    
}
open class Secp256k1PrivateKey: Secp256k1PrivateKeyProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_secp256k1privatekey(self.pointer, $0) }
    }
public convenience init(bytes: Data)throws  {
    let pointer =
        try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_secp256k1privatekey_new(
        FfiConverterData.lower(bytes),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_secp256k1privatekey(pointer, $0) }
    }

    
    /**
     * Decode a private key from `flag || privkey` in Bech32 starting with
     * "iotaprivkey".
     */
public static func fromBech32(value: String)throws  -> Secp256k1PrivateKey  {
    return try  FfiConverterTypeSecp256k1PrivateKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_secp256k1privatekey_from_bech32(
        FfiConverterString.lower(value),$0
    )
})
}
    
    /**
     * Deserialize PKCS#8 private key from ASN.1 DER-encoded data (binary
     * format).
     */
public static func fromDer(bytes: Data)throws  -> Secp256k1PrivateKey  {
    return try  FfiConverterTypeSecp256k1PrivateKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_secp256k1privatekey_from_der(
        FfiConverterData.lower(bytes),$0
    )
})
}
    
    /**
     * Construct the private key from a mnemonic phrase
     */
public static func fromMnemonic(phrase: String, accountIndex: UInt64 = UInt64(0), password: String = "")throws  -> Secp256k1PrivateKey  {
    return try  FfiConverterTypeSecp256k1PrivateKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_secp256k1privatekey_from_mnemonic(
        FfiConverterString.lower(phrase),
        FfiConverterUInt64.lower(accountIndex),
        FfiConverterString.lower(password),$0
    )
})
}
    
    /**
     * Create an instance from a mnemonic phrase and a derivation path like
     * `"m/54'/4218'/0'/0/0"`
     */
public static func fromMnemonicWithPath(phrase: String, path: String, password: String = "")throws  -> Secp256k1PrivateKey  {
    return try  FfiConverterTypeSecp256k1PrivateKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_secp256k1privatekey_from_mnemonic_with_path(
        FfiConverterString.lower(phrase),
        FfiConverterString.lower(path),
        FfiConverterString.lower(password),$0
    )
})
}
    
    /**
     * Deserialize PKCS#8-encoded private key from PEM.
     */
public static func fromPem(s: String)throws  -> Secp256k1PrivateKey  {
    return try  FfiConverterTypeSecp256k1PrivateKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_secp256k1privatekey_from_pem(
        FfiConverterString.lower(s),$0
    )
})
}
    
public static func generate() -> Secp256k1PrivateKey  {
    return try!  FfiConverterTypeSecp256k1PrivateKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_secp256k1privatekey_generate($0
    )
})
}
    

    
open func publicKey() -> Secp256k1PublicKey  {
    return try!  FfiConverterTypeSecp256k1PublicKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256k1privatekey_public_key(self.uniffiClonePointer(),$0
    )
})
}
    
open func scheme() -> SignatureScheme  {
    return try!  FfiConverterTypeSignatureScheme_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256k1privatekey_scheme(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Sign a personal message and return a UserSignature.
     */
open func signPersonalMessage(message: PersonalMessage)throws  -> UserSignature  {
    return try  FfiConverterTypeUserSignature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_secp256k1privatekey_sign_personal_message(self.uniffiClonePointer(),
        FfiConverterTypePersonalMessage_lower(message),$0
    )
})
}
    
    /**
     * Sign a transaction and return a UserSignature.
     */
open func signTransaction(transaction: Transaction)throws  -> UserSignature  {
    return try  FfiConverterTypeUserSignature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_secp256k1privatekey_sign_transaction(self.uniffiClonePointer(),
        FfiConverterTypeTransaction_lower(transaction),$0
    )
})
}
    
    /**
     * Encode this private key as `flag || privkey` in Bech32 starting with
     * "iotaprivkey" to a string.
     */
open func toBech32()throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_secp256k1privatekey_to_bech32(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Serialize this private key to bytes.
     */
open func toBytes() -> Data  {
    return try!  FfiConverterData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256k1privatekey_to_bytes(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Serialize this private key as DER-encoded PKCS#8
     */
open func toDer()throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_secp256k1privatekey_to_der(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Serialize this private key as PEM-encoded PKCS#8
     */
open func toPem()throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_secp256k1privatekey_to_pem(self.uniffiClonePointer(),$0
    )
})
}
    
open func trySign(message: Data)throws  -> Secp256k1Signature  {
    return try  FfiConverterTypeSecp256k1Signature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_secp256k1privatekey_try_sign(self.uniffiClonePointer(),
        FfiConverterData.lower(message),$0
    )
})
}
    
open func trySignSimple(message: Data)throws  -> SimpleSignature  {
    return try  FfiConverterTypeSimpleSignature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_secp256k1privatekey_try_sign_simple(self.uniffiClonePointer(),
        FfiConverterData.lower(message),$0
    )
})
}
    
open func trySignUser(message: Data)throws  -> UserSignature  {
    return try  FfiConverterTypeUserSignature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_secp256k1privatekey_try_sign_user(self.uniffiClonePointer(),
        FfiConverterData.lower(message),$0
    )
})
}
    
open func verifyingKey() -> Secp256k1VerifyingKey  {
    return try!  FfiConverterTypeSecp256k1VerifyingKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256k1privatekey_verifying_key(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256k1privatekey_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: Secp256k1PrivateKey, other: Secp256k1PrivateKey) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256k1privatekey_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeSecp256k1PrivateKey_lower(other),$0
    )
}
        )
    }

}
extension Secp256k1PrivateKey: CustomDebugStringConvertible {}
extension Secp256k1PrivateKey: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSecp256k1PrivateKey: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = Secp256k1PrivateKey

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> Secp256k1PrivateKey {
        return Secp256k1PrivateKey(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: Secp256k1PrivateKey) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Secp256k1PrivateKey {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: Secp256k1PrivateKey, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSecp256k1PrivateKey_lift(_ pointer: UnsafeMutableRawPointer) throws -> Secp256k1PrivateKey {
    return try FfiConverterTypeSecp256k1PrivateKey.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSecp256k1PrivateKey_lower(_ value: Secp256k1PrivateKey) -> UnsafeMutableRawPointer {
    return FfiConverterTypeSecp256k1PrivateKey.lower(value)
}






/**
 * A secp256k1 signature.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * secp256k1-signature = 64OCTET
 * ```
 */
public protocol Secp256k1PublicKeyProtocol: AnyObject, Sendable {
    
    /**
     * Derive an `Address` from this Public Key
     *
     * An `Address` can be derived from a `Secp256k1PublicKey` by hashing the
     * bytes of the public key prefixed with the Secp256k1
     * `SignatureScheme` flag (`0x01`).
     *
     * `hash( 0x01 || 33-byte secp256k1 public key)`
     */
    func deriveAddress()  -> Address
    
    /**
     * Returns the signature scheme for this public key.
     */
    func scheme()  -> SignatureScheme
    
    func toBytes()  -> Data
    
    /**
     * Returns the bytes with signature scheme flag prepended.
     */
    func toFlaggedBytes()  -> Data
    
}
/**
 * A secp256k1 signature.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * secp256k1-signature = 64OCTET
 * ```
 */
open class Secp256k1PublicKey: Secp256k1PublicKeyProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_secp256k1publickey(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_secp256k1publickey(pointer, $0) }
    }

    
public static func fromBytes(bytes: Data)throws  -> Secp256k1PublicKey  {
    return try  FfiConverterTypeSecp256k1PublicKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_secp256k1publickey_from_bytes(
        FfiConverterData.lower(bytes),$0
    )
})
}
    
public static func fromStr(s: String)throws  -> Secp256k1PublicKey  {
    return try  FfiConverterTypeSecp256k1PublicKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_secp256k1publickey_from_str(
        FfiConverterString.lower(s),$0
    )
})
}
    
public static func generate() -> Secp256k1PublicKey  {
    return try!  FfiConverterTypeSecp256k1PublicKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_secp256k1publickey_generate($0
    )
})
}
    

    
    /**
     * Derive an `Address` from this Public Key
     *
     * An `Address` can be derived from a `Secp256k1PublicKey` by hashing the
     * bytes of the public key prefixed with the Secp256k1
     * `SignatureScheme` flag (`0x01`).
     *
     * `hash( 0x01 || 33-byte secp256k1 public key)`
     */
open func deriveAddress() -> Address  {
    return try!  FfiConverterTypeAddress_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256k1publickey_derive_address(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Returns the signature scheme for this public key.
     */
open func scheme() -> SignatureScheme  {
    return try!  FfiConverterTypeSignatureScheme_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256k1publickey_scheme(self.uniffiClonePointer(),$0
    )
})
}
    
open func toBytes() -> Data  {
    return try!  FfiConverterData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256k1publickey_to_bytes(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Returns the bytes with signature scheme flag prepended.
     */
open func toFlaggedBytes() -> Data  {
    return try!  FfiConverterData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256k1publickey_to_flagged_bytes(self.uniffiClonePointer(),$0
    )
})
}
    
    public static func == (self: Secp256k1PublicKey, other: Secp256k1PublicKey) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256k1publickey_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeSecp256k1PublicKey_lower(other),$0
    )
}
        )
    }
    open func hash(into hasher: inout Hasher) {
        let val = try!  FfiConverterUInt64.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256k1publickey_uniffi_trait_hash(self.uniffiClonePointer(),$0
    )
}
        )
        hasher.combine(val)
    }

}
extension Secp256k1PublicKey: Equatable {}
extension Secp256k1PublicKey: Hashable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSecp256k1PublicKey: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = Secp256k1PublicKey

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> Secp256k1PublicKey {
        return Secp256k1PublicKey(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: Secp256k1PublicKey) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Secp256k1PublicKey {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: Secp256k1PublicKey, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSecp256k1PublicKey_lift(_ pointer: UnsafeMutableRawPointer) throws -> Secp256k1PublicKey {
    return try FfiConverterTypeSecp256k1PublicKey.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSecp256k1PublicKey_lower(_ value: Secp256k1PublicKey) -> UnsafeMutableRawPointer {
    return FfiConverterTypeSecp256k1PublicKey.lower(value)
}






/**
 * A secp256k1 public key.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * secp256k1-public-key = 33OCTET
 * ```
 */
public protocol Secp256k1SignatureProtocol: AnyObject, Sendable {
    
    func toBytes()  -> Data
    
}
/**
 * A secp256k1 public key.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * secp256k1-public-key = 33OCTET
 * ```
 */
open class Secp256k1Signature: Secp256k1SignatureProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_secp256k1signature(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_secp256k1signature(pointer, $0) }
    }

    
public static func fromBytes(bytes: Data)throws  -> Secp256k1Signature  {
    return try  FfiConverterTypeSecp256k1Signature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_secp256k1signature_from_bytes(
        FfiConverterData.lower(bytes),$0
    )
})
}
    
public static func fromStr(s: String)throws  -> Secp256k1Signature  {
    return try  FfiConverterTypeSecp256k1Signature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_secp256k1signature_from_str(
        FfiConverterString.lower(s),$0
    )
})
}
    
public static func generate() -> Secp256k1Signature  {
    return try!  FfiConverterTypeSecp256k1Signature_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_secp256k1signature_generate($0
    )
})
}
    

    
open func toBytes() -> Data  {
    return try!  FfiConverterData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256k1signature_to_bytes(self.uniffiClonePointer(),$0
    )
})
}
    
    public static func == (self: Secp256k1Signature, other: Secp256k1Signature) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256k1signature_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeSecp256k1Signature_lower(other),$0
    )
}
        )
    }
    open func hash(into hasher: inout Hasher) {
        let val = try!  FfiConverterUInt64.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256k1signature_uniffi_trait_hash(self.uniffiClonePointer(),$0
    )
}
        )
        hasher.combine(val)
    }

}
extension Secp256k1Signature: Equatable {}
extension Secp256k1Signature: Hashable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSecp256k1Signature: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = Secp256k1Signature

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> Secp256k1Signature {
        return Secp256k1Signature(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: Secp256k1Signature) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Secp256k1Signature {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: Secp256k1Signature, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSecp256k1Signature_lift(_ pointer: UnsafeMutableRawPointer) throws -> Secp256k1Signature {
    return try FfiConverterTypeSecp256k1Signature.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSecp256k1Signature_lower(_ value: Secp256k1Signature) -> UnsafeMutableRawPointer {
    return FfiConverterTypeSecp256k1Signature.lower(value)
}






public protocol Secp256k1VerifierProtocol: AnyObject, Sendable {
    
    func verifySimple(message: Data, signature: SimpleSignature) throws 
    
    func verifyUser(message: Data, signature: UserSignature) throws 
    
}
open class Secp256k1Verifier: Secp256k1VerifierProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_secp256k1verifier(self.pointer, $0) }
    }
public convenience init() {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_secp256k1verifier_new($0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_secp256k1verifier(pointer, $0) }
    }

    

    
open func verifySimple(message: Data, signature: SimpleSignature)throws   {try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_secp256k1verifier_verify_simple(self.uniffiClonePointer(),
        FfiConverterData.lower(message),
        FfiConverterTypeSimpleSignature_lower(signature),$0
    )
}
}
    
open func verifyUser(message: Data, signature: UserSignature)throws   {try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_secp256k1verifier_verify_user(self.uniffiClonePointer(),
        FfiConverterData.lower(message),
        FfiConverterTypeUserSignature_lower(signature),$0
    )
}
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256k1verifier_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }

}
extension Secp256k1Verifier: CustomDebugStringConvertible {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSecp256k1Verifier: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = Secp256k1Verifier

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> Secp256k1Verifier {
        return Secp256k1Verifier(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: Secp256k1Verifier) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Secp256k1Verifier {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: Secp256k1Verifier, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSecp256k1Verifier_lift(_ pointer: UnsafeMutableRawPointer) throws -> Secp256k1Verifier {
    return try FfiConverterTypeSecp256k1Verifier.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSecp256k1Verifier_lower(_ value: Secp256k1Verifier) -> UnsafeMutableRawPointer {
    return FfiConverterTypeSecp256k1Verifier.lower(value)
}






public protocol Secp256k1VerifyingKeyProtocol: AnyObject, Sendable {
    
    func publicKey()  -> Secp256k1PublicKey
    
    /**
     * Serialize this public key as DER-encoded data
     */
    func toDer() throws  -> Data
    
    /**
     * Serialize this public key into PEM
     */
    func toPem() throws  -> String
    
    func verify(message: Data, signature: Secp256k1Signature) throws 
    
    func verifySimple(message: Data, signature: SimpleSignature) throws 
    
    func verifyUser(message: Data, signature: UserSignature) throws 
    
}
open class Secp256k1VerifyingKey: Secp256k1VerifyingKeyProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_secp256k1verifyingkey(self.pointer, $0) }
    }
public convenience init(publicKey: Secp256k1PublicKey)throws  {
    let pointer =
        try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_secp256k1verifyingkey_new(
        FfiConverterTypeSecp256k1PublicKey_lower(publicKey),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_secp256k1verifyingkey(pointer, $0) }
    }

    
    /**
     * Deserialize public key from ASN.1 DER-encoded data (binary format).
     */
public static func fromDer(bytes: Data)throws  -> Secp256k1VerifyingKey  {
    return try  FfiConverterTypeSecp256k1VerifyingKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_secp256k1verifyingkey_from_der(
        FfiConverterData.lower(bytes),$0
    )
})
}
    
    /**
     * Deserialize public key from PEM.
     */
public static func fromPem(s: String)throws  -> Secp256k1VerifyingKey  {
    return try  FfiConverterTypeSecp256k1VerifyingKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_secp256k1verifyingkey_from_pem(
        FfiConverterString.lower(s),$0
    )
})
}
    

    
open func publicKey() -> Secp256k1PublicKey  {
    return try!  FfiConverterTypeSecp256k1PublicKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256k1verifyingkey_public_key(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Serialize this public key as DER-encoded data
     */
open func toDer()throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_secp256k1verifyingkey_to_der(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Serialize this public key into PEM
     */
open func toPem()throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_secp256k1verifyingkey_to_pem(self.uniffiClonePointer(),$0
    )
})
}
    
open func verify(message: Data, signature: Secp256k1Signature)throws   {try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_secp256k1verifyingkey_verify(self.uniffiClonePointer(),
        FfiConverterData.lower(message),
        FfiConverterTypeSecp256k1Signature_lower(signature),$0
    )
}
}
    
open func verifySimple(message: Data, signature: SimpleSignature)throws   {try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_secp256k1verifyingkey_verify_simple(self.uniffiClonePointer(),
        FfiConverterData.lower(message),
        FfiConverterTypeSimpleSignature_lower(signature),$0
    )
}
}
    
open func verifyUser(message: Data, signature: UserSignature)throws   {try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_secp256k1verifyingkey_verify_user(self.uniffiClonePointer(),
        FfiConverterData.lower(message),
        FfiConverterTypeUserSignature_lower(signature),$0
    )
}
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256k1verifyingkey_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: Secp256k1VerifyingKey, other: Secp256k1VerifyingKey) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256k1verifyingkey_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeSecp256k1VerifyingKey_lower(other),$0
    )
}
        )
    }

}
extension Secp256k1VerifyingKey: CustomDebugStringConvertible {}
extension Secp256k1VerifyingKey: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSecp256k1VerifyingKey: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = Secp256k1VerifyingKey

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> Secp256k1VerifyingKey {
        return Secp256k1VerifyingKey(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: Secp256k1VerifyingKey) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Secp256k1VerifyingKey {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: Secp256k1VerifyingKey, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSecp256k1VerifyingKey_lift(_ pointer: UnsafeMutableRawPointer) throws -> Secp256k1VerifyingKey {
    return try FfiConverterTypeSecp256k1VerifyingKey.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSecp256k1VerifyingKey_lower(_ value: Secp256k1VerifyingKey) -> UnsafeMutableRawPointer {
    return FfiConverterTypeSecp256k1VerifyingKey.lower(value)
}






public protocol Secp256r1PrivateKeyProtocol: AnyObject, Sendable {
    
    /**
     * Get the public key corresponding to this private key.
     */
    func publicKey()  -> Secp256r1PublicKey
    
    func scheme()  -> SignatureScheme
    
    /**
     * Sign a personal message and return a UserSignature.
     */
    func signPersonalMessage(message: PersonalMessage) throws  -> UserSignature
    
    /**
     * Sign a transaction and return a UserSignature.
     */
    func signTransaction(transaction: Transaction) throws  -> UserSignature
    
    /**
     * Encode this private key as `flag || privkey` in Bech32 starting with
     * "iotaprivkey" to a string.
     */
    func toBech32() throws  -> String
    
    /**
     * Serialize this private key to bytes.
     */
    func toBytes()  -> Data
    
    /**
     * Serialize this private key as DER-encoded PKCS#8
     */
    func toDer() throws  -> Data
    
    /**
     * Serialize this private key as PEM-encoded PKCS#8
     */
    func toPem() throws  -> String
    
    /**
     * Sign a message and return a Secp256r1Signature.
     */
    func trySign(message: Data) throws  -> Secp256r1Signature
    
    /**
     * Sign a message and return a SimpleSignature.
     */
    func trySignSimple(message: Data) throws  -> SimpleSignature
    
    /**
     * Sign a message and return a UserSignature.
     */
    func trySignUser(message: Data) throws  -> UserSignature
    
    func verifyingKey()  -> Secp256r1VerifyingKey
    
}
open class Secp256r1PrivateKey: Secp256r1PrivateKeyProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_secp256r1privatekey(self.pointer, $0) }
    }
public convenience init(bytes: Data)throws  {
    let pointer =
        try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_secp256r1privatekey_new(
        FfiConverterData.lower(bytes),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_secp256r1privatekey(pointer, $0) }
    }

    
    /**
     * Decode a private key from `flag || privkey` in Bech32 starting with
     * "iotaprivkey".
     */
public static func fromBech32(value: String)throws  -> Secp256r1PrivateKey  {
    return try  FfiConverterTypeSecp256r1PrivateKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_secp256r1privatekey_from_bech32(
        FfiConverterString.lower(value),$0
    )
})
}
    
    /**
     * Deserialize PKCS#8 private key from ASN.1 DER-encoded data (binary
     * format).
     */
public static func fromDer(bytes: Data)throws  -> Secp256r1PrivateKey  {
    return try  FfiConverterTypeSecp256r1PrivateKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_secp256r1privatekey_from_der(
        FfiConverterData.lower(bytes),$0
    )
})
}
    
    /**
     * Construct the private key from a mnemonic phrase
     */
public static func fromMnemonic(phrase: String, accountIndex: UInt64 = UInt64(0), password: String = "")throws  -> Secp256r1PrivateKey  {
    return try  FfiConverterTypeSecp256r1PrivateKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_secp256r1privatekey_from_mnemonic(
        FfiConverterString.lower(phrase),
        FfiConverterUInt64.lower(accountIndex),
        FfiConverterString.lower(password),$0
    )
})
}
    
    /**
     * Create an instance from a mnemonic phrase and a derivation path like
     * `"m/74'/4218'/0'/0/0"`
     */
public static func fromMnemonicWithPath(phrase: String, path: String, password: String = "")throws  -> Secp256r1PrivateKey  {
    return try  FfiConverterTypeSecp256r1PrivateKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_secp256r1privatekey_from_mnemonic_with_path(
        FfiConverterString.lower(phrase),
        FfiConverterString.lower(path),
        FfiConverterString.lower(password),$0
    )
})
}
    
    /**
     * Deserialize PKCS#8-encoded private key from PEM.
     */
public static func fromPem(s: String)throws  -> Secp256r1PrivateKey  {
    return try  FfiConverterTypeSecp256r1PrivateKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_secp256r1privatekey_from_pem(
        FfiConverterString.lower(s),$0
    )
})
}
    
    /**
     * Generate a new random Secp256r1PrivateKey
     */
public static func generate() -> Secp256r1PrivateKey  {
    return try!  FfiConverterTypeSecp256r1PrivateKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_secp256r1privatekey_generate($0
    )
})
}
    

    
    /**
     * Get the public key corresponding to this private key.
     */
open func publicKey() -> Secp256r1PublicKey  {
    return try!  FfiConverterTypeSecp256r1PublicKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256r1privatekey_public_key(self.uniffiClonePointer(),$0
    )
})
}
    
open func scheme() -> SignatureScheme  {
    return try!  FfiConverterTypeSignatureScheme_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256r1privatekey_scheme(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Sign a personal message and return a UserSignature.
     */
open func signPersonalMessage(message: PersonalMessage)throws  -> UserSignature  {
    return try  FfiConverterTypeUserSignature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_secp256r1privatekey_sign_personal_message(self.uniffiClonePointer(),
        FfiConverterTypePersonalMessage_lower(message),$0
    )
})
}
    
    /**
     * Sign a transaction and return a UserSignature.
     */
open func signTransaction(transaction: Transaction)throws  -> UserSignature  {
    return try  FfiConverterTypeUserSignature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_secp256r1privatekey_sign_transaction(self.uniffiClonePointer(),
        FfiConverterTypeTransaction_lower(transaction),$0
    )
})
}
    
    /**
     * Encode this private key as `flag || privkey` in Bech32 starting with
     * "iotaprivkey" to a string.
     */
open func toBech32()throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_secp256r1privatekey_to_bech32(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Serialize this private key to bytes.
     */
open func toBytes() -> Data  {
    return try!  FfiConverterData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256r1privatekey_to_bytes(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Serialize this private key as DER-encoded PKCS#8
     */
open func toDer()throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_secp256r1privatekey_to_der(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Serialize this private key as PEM-encoded PKCS#8
     */
open func toPem()throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_secp256r1privatekey_to_pem(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Sign a message and return a Secp256r1Signature.
     */
open func trySign(message: Data)throws  -> Secp256r1Signature  {
    return try  FfiConverterTypeSecp256r1Signature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_secp256r1privatekey_try_sign(self.uniffiClonePointer(),
        FfiConverterData.lower(message),$0
    )
})
}
    
    /**
     * Sign a message and return a SimpleSignature.
     */
open func trySignSimple(message: Data)throws  -> SimpleSignature  {
    return try  FfiConverterTypeSimpleSignature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_secp256r1privatekey_try_sign_simple(self.uniffiClonePointer(),
        FfiConverterData.lower(message),$0
    )
})
}
    
    /**
     * Sign a message and return a UserSignature.
     */
open func trySignUser(message: Data)throws  -> UserSignature  {
    return try  FfiConverterTypeUserSignature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_secp256r1privatekey_try_sign_user(self.uniffiClonePointer(),
        FfiConverterData.lower(message),$0
    )
})
}
    
open func verifyingKey() -> Secp256r1VerifyingKey  {
    return try!  FfiConverterTypeSecp256r1VerifyingKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256r1privatekey_verifying_key(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256r1privatekey_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: Secp256r1PrivateKey, other: Secp256r1PrivateKey) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256r1privatekey_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeSecp256r1PrivateKey_lower(other),$0
    )
}
        )
    }

}
extension Secp256r1PrivateKey: CustomDebugStringConvertible {}
extension Secp256r1PrivateKey: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSecp256r1PrivateKey: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = Secp256r1PrivateKey

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> Secp256r1PrivateKey {
        return Secp256r1PrivateKey(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: Secp256r1PrivateKey) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Secp256r1PrivateKey {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: Secp256r1PrivateKey, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSecp256r1PrivateKey_lift(_ pointer: UnsafeMutableRawPointer) throws -> Secp256r1PrivateKey {
    return try FfiConverterTypeSecp256r1PrivateKey.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSecp256r1PrivateKey_lower(_ value: Secp256r1PrivateKey) -> UnsafeMutableRawPointer {
    return FfiConverterTypeSecp256r1PrivateKey.lower(value)
}






/**
 * A secp256r1 signature.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * secp256r1-signature = 64OCTET
 * ```
 */
public protocol Secp256r1PublicKeyProtocol: AnyObject, Sendable {
    
    /**
     * Derive an `Address` from this Public Key
     *
     * An `Address` can be derived from a `Secp256r1PublicKey` by hashing the
     * bytes of the public key prefixed with the Secp256r1
     * `SignatureScheme` flag (`0x02`).
     *
     * `hash( 0x02 || 33-byte secp256r1 public key)`
     */
    func deriveAddress()  -> Address
    
    /**
     * Returns the signature scheme for this public key.
     */
    func scheme()  -> SignatureScheme
    
    func toBytes()  -> Data
    
    /**
     * Returns the bytes with signature scheme flag prepended
     */
    func toFlaggedBytes()  -> Data
    
}
/**
 * A secp256r1 signature.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * secp256r1-signature = 64OCTET
 * ```
 */
open class Secp256r1PublicKey: Secp256r1PublicKeyProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_secp256r1publickey(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_secp256r1publickey(pointer, $0) }
    }

    
public static func fromBytes(bytes: Data)throws  -> Secp256r1PublicKey  {
    return try  FfiConverterTypeSecp256r1PublicKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_secp256r1publickey_from_bytes(
        FfiConverterData.lower(bytes),$0
    )
})
}
    
public static func fromStr(s: String)throws  -> Secp256r1PublicKey  {
    return try  FfiConverterTypeSecp256r1PublicKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_secp256r1publickey_from_str(
        FfiConverterString.lower(s),$0
    )
})
}
    
public static func generate() -> Secp256r1PublicKey  {
    return try!  FfiConverterTypeSecp256r1PublicKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_secp256r1publickey_generate($0
    )
})
}
    

    
    /**
     * Derive an `Address` from this Public Key
     *
     * An `Address` can be derived from a `Secp256r1PublicKey` by hashing the
     * bytes of the public key prefixed with the Secp256r1
     * `SignatureScheme` flag (`0x02`).
     *
     * `hash( 0x02 || 33-byte secp256r1 public key)`
     */
open func deriveAddress() -> Address  {
    return try!  FfiConverterTypeAddress_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256r1publickey_derive_address(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Returns the signature scheme for this public key.
     */
open func scheme() -> SignatureScheme  {
    return try!  FfiConverterTypeSignatureScheme_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256r1publickey_scheme(self.uniffiClonePointer(),$0
    )
})
}
    
open func toBytes() -> Data  {
    return try!  FfiConverterData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256r1publickey_to_bytes(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Returns the bytes with signature scheme flag prepended
     */
open func toFlaggedBytes() -> Data  {
    return try!  FfiConverterData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256r1publickey_to_flagged_bytes(self.uniffiClonePointer(),$0
    )
})
}
    
    public static func == (self: Secp256r1PublicKey, other: Secp256r1PublicKey) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256r1publickey_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeSecp256r1PublicKey_lower(other),$0
    )
}
        )
    }
    open func hash(into hasher: inout Hasher) {
        let val = try!  FfiConverterUInt64.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256r1publickey_uniffi_trait_hash(self.uniffiClonePointer(),$0
    )
}
        )
        hasher.combine(val)
    }

}
extension Secp256r1PublicKey: Equatable {}
extension Secp256r1PublicKey: Hashable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSecp256r1PublicKey: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = Secp256r1PublicKey

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> Secp256r1PublicKey {
        return Secp256r1PublicKey(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: Secp256r1PublicKey) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Secp256r1PublicKey {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: Secp256r1PublicKey, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSecp256r1PublicKey_lift(_ pointer: UnsafeMutableRawPointer) throws -> Secp256r1PublicKey {
    return try FfiConverterTypeSecp256r1PublicKey.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSecp256r1PublicKey_lower(_ value: Secp256r1PublicKey) -> UnsafeMutableRawPointer {
    return FfiConverterTypeSecp256r1PublicKey.lower(value)
}






/**
 * A secp256r1 public key.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * secp256r1-public-key = 33OCTET
 * ```
 */
public protocol Secp256r1SignatureProtocol: AnyObject, Sendable {
    
    func toBytes()  -> Data
    
}
/**
 * A secp256r1 public key.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * secp256r1-public-key = 33OCTET
 * ```
 */
open class Secp256r1Signature: Secp256r1SignatureProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_secp256r1signature(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_secp256r1signature(pointer, $0) }
    }

    
public static func fromBytes(bytes: Data)throws  -> Secp256r1Signature  {
    return try  FfiConverterTypeSecp256r1Signature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_secp256r1signature_from_bytes(
        FfiConverterData.lower(bytes),$0
    )
})
}
    
public static func fromStr(s: String)throws  -> Secp256r1Signature  {
    return try  FfiConverterTypeSecp256r1Signature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_secp256r1signature_from_str(
        FfiConverterString.lower(s),$0
    )
})
}
    
public static func generate() -> Secp256r1Signature  {
    return try!  FfiConverterTypeSecp256r1Signature_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_secp256r1signature_generate($0
    )
})
}
    

    
open func toBytes() -> Data  {
    return try!  FfiConverterData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256r1signature_to_bytes(self.uniffiClonePointer(),$0
    )
})
}
    
    public static func == (self: Secp256r1Signature, other: Secp256r1Signature) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256r1signature_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeSecp256r1Signature_lower(other),$0
    )
}
        )
    }
    open func hash(into hasher: inout Hasher) {
        let val = try!  FfiConverterUInt64.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256r1signature_uniffi_trait_hash(self.uniffiClonePointer(),$0
    )
}
        )
        hasher.combine(val)
    }

}
extension Secp256r1Signature: Equatable {}
extension Secp256r1Signature: Hashable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSecp256r1Signature: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = Secp256r1Signature

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> Secp256r1Signature {
        return Secp256r1Signature(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: Secp256r1Signature) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Secp256r1Signature {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: Secp256r1Signature, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSecp256r1Signature_lift(_ pointer: UnsafeMutableRawPointer) throws -> Secp256r1Signature {
    return try FfiConverterTypeSecp256r1Signature.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSecp256r1Signature_lower(_ value: Secp256r1Signature) -> UnsafeMutableRawPointer {
    return FfiConverterTypeSecp256r1Signature.lower(value)
}






public protocol Secp256r1VerifierProtocol: AnyObject, Sendable {
    
    func verifySimple(message: Data, signature: SimpleSignature) throws 
    
    func verifyUser(message: Data, signature: UserSignature) throws 
    
}
open class Secp256r1Verifier: Secp256r1VerifierProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_secp256r1verifier(self.pointer, $0) }
    }
public convenience init() {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_secp256r1verifier_new($0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_secp256r1verifier(pointer, $0) }
    }

    

    
open func verifySimple(message: Data, signature: SimpleSignature)throws   {try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_secp256r1verifier_verify_simple(self.uniffiClonePointer(),
        FfiConverterData.lower(message),
        FfiConverterTypeSimpleSignature_lower(signature),$0
    )
}
}
    
open func verifyUser(message: Data, signature: UserSignature)throws   {try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_secp256r1verifier_verify_user(self.uniffiClonePointer(),
        FfiConverterData.lower(message),
        FfiConverterTypeUserSignature_lower(signature),$0
    )
}
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256r1verifier_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }

}
extension Secp256r1Verifier: CustomDebugStringConvertible {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSecp256r1Verifier: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = Secp256r1Verifier

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> Secp256r1Verifier {
        return Secp256r1Verifier(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: Secp256r1Verifier) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Secp256r1Verifier {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: Secp256r1Verifier, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSecp256r1Verifier_lift(_ pointer: UnsafeMutableRawPointer) throws -> Secp256r1Verifier {
    return try FfiConverterTypeSecp256r1Verifier.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSecp256r1Verifier_lower(_ value: Secp256r1Verifier) -> UnsafeMutableRawPointer {
    return FfiConverterTypeSecp256r1Verifier.lower(value)
}






public protocol Secp256r1VerifyingKeyProtocol: AnyObject, Sendable {
    
    func publicKey()  -> Secp256r1PublicKey
    
    /**
     * Serialize this public key as DER-encoded data.
     */
    func toDer() throws  -> Data
    
    /**
     * Serialize this public key into PEM.
     */
    func toPem() throws  -> String
    
    func verify(message: Data, signature: Secp256r1Signature) throws 
    
    func verifySimple(message: Data, signature: SimpleSignature) throws 
    
    func verifyUser(message: Data, signature: UserSignature) throws 
    
}
open class Secp256r1VerifyingKey: Secp256r1VerifyingKeyProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_secp256r1verifyingkey(self.pointer, $0) }
    }
public convenience init(publicKey: Secp256r1PublicKey)throws  {
    let pointer =
        try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_secp256r1verifyingkey_new(
        FfiConverterTypeSecp256r1PublicKey_lower(publicKey),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_secp256r1verifyingkey(pointer, $0) }
    }

    
    /**
     * Deserialize public key from ASN.1 DER-encoded data (binary format).
     */
public static func fromDer(bytes: Data)throws  -> Secp256r1VerifyingKey  {
    return try  FfiConverterTypeSecp256r1VerifyingKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_secp256r1verifyingkey_from_der(
        FfiConverterData.lower(bytes),$0
    )
})
}
    
    /**
     * Deserialize public key from PEM.
     */
public static func fromPem(s: String)throws  -> Secp256r1VerifyingKey  {
    return try  FfiConverterTypeSecp256r1VerifyingKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_secp256r1verifyingkey_from_pem(
        FfiConverterString.lower(s),$0
    )
})
}
    

    
open func publicKey() -> Secp256r1PublicKey  {
    return try!  FfiConverterTypeSecp256r1PublicKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256r1verifyingkey_public_key(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Serialize this public key as DER-encoded data.
     */
open func toDer()throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_secp256r1verifyingkey_to_der(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Serialize this public key into PEM.
     */
open func toPem()throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_secp256r1verifyingkey_to_pem(self.uniffiClonePointer(),$0
    )
})
}
    
open func verify(message: Data, signature: Secp256r1Signature)throws   {try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_secp256r1verifyingkey_verify(self.uniffiClonePointer(),
        FfiConverterData.lower(message),
        FfiConverterTypeSecp256r1Signature_lower(signature),$0
    )
}
}
    
open func verifySimple(message: Data, signature: SimpleSignature)throws   {try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_secp256r1verifyingkey_verify_simple(self.uniffiClonePointer(),
        FfiConverterData.lower(message),
        FfiConverterTypeSimpleSignature_lower(signature),$0
    )
}
}
    
open func verifyUser(message: Data, signature: UserSignature)throws   {try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_secp256r1verifyingkey_verify_user(self.uniffiClonePointer(),
        FfiConverterData.lower(message),
        FfiConverterTypeUserSignature_lower(signature),$0
    )
}
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256r1verifyingkey_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: Secp256r1VerifyingKey, other: Secp256r1VerifyingKey) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_secp256r1verifyingkey_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeSecp256r1VerifyingKey_lower(other),$0
    )
}
        )
    }

}
extension Secp256r1VerifyingKey: CustomDebugStringConvertible {}
extension Secp256r1VerifyingKey: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSecp256r1VerifyingKey: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = Secp256r1VerifyingKey

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> Secp256r1VerifyingKey {
        return Secp256r1VerifyingKey(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: Secp256r1VerifyingKey) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Secp256r1VerifyingKey {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: Secp256r1VerifyingKey, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSecp256r1VerifyingKey_lift(_ pointer: UnsafeMutableRawPointer) throws -> Secp256r1VerifyingKey {
    return try FfiConverterTypeSecp256r1VerifyingKey.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSecp256r1VerifyingKey_lower(_ value: Secp256r1VerifyingKey) -> UnsafeMutableRawPointer {
    return FfiConverterTypeSecp256r1VerifyingKey.lower(value)
}






public protocol SimpleKeypairProtocol: AnyObject, Sendable {
    
    func publicKey()  -> MultisigMemberPublicKey
    
    func scheme()  -> SignatureScheme
    
    /**
     * Sign a personal message and return a UserSignature.
     */
    func signPersonalMessage(message: PersonalMessage) throws  -> UserSignature
    
    /**
     * Sign a transaction and return a UserSignature.
     */
    func signTransaction(transaction: Transaction) throws  -> UserSignature
    
    /**
     * Encode a SimpleKeypair as `flag || privkey` in Bech32 starting with
     * "iotaprivkey" to a string. Note that the pubkey is not encoded.
     */
    func toBech32() throws  -> String
    
    /**
     * Encode a SimpleKeypair as `flag || privkey` in bytes
     */
    func toBytes()  -> Data
    
    /**
     * Serialize this private key as DER-encoded PKCS#8
     */
    func toDer() throws  -> Data
    
    /**
     * Serialize this private key as DER-encoded PKCS#8
     */
    func toPem() throws  -> String
    
    func trySign(message: Data) throws  -> SimpleSignature
    
    func trySignUser(message: Data) throws  -> UserSignature
    
    func verifyingKey()  -> SimpleVerifyingKey
    
}
open class SimpleKeypair: SimpleKeypairProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_simplekeypair(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_simplekeypair(pointer, $0) }
    }

    
    /**
     * Decode a SimpleKeypair from `flag || privkey` in Bech32 starting with
     * "iotaprivkey" to SimpleKeypair. The public key is computed directly from
     * the private key bytes.
     */
public static func fromBech32(value: String)throws  -> SimpleKeypair  {
    return try  FfiConverterTypeSimpleKeypair_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_simplekeypair_from_bech32(
        FfiConverterString.lower(value),$0
    )
})
}
    
    /**
     * Decode a SimpleKeypair from `flag || privkey` bytes
     */
public static func fromBytes(bytes: Data)throws  -> SimpleKeypair  {
    return try  FfiConverterTypeSimpleKeypair_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_simplekeypair_from_bytes(
        FfiConverterData.lower(bytes),$0
    )
})
}
    
    /**
     * Deserialize PKCS#8 private key from ASN.1 DER-encoded data (binary
     * format).
     */
public static func fromDer(bytes: Data)throws  -> SimpleKeypair  {
    return try  FfiConverterTypeSimpleKeypair_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_simplekeypair_from_der(
        FfiConverterData.lower(bytes),$0
    )
})
}
    
public static func fromEd25519(keypair: Ed25519PrivateKey) -> SimpleKeypair  {
    return try!  FfiConverterTypeSimpleKeypair_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_simplekeypair_from_ed25519(
        FfiConverterTypeEd25519PrivateKey_lower(keypair),$0
    )
})
}
    
    /**
     * Deserialize PKCS#8-encoded private key from PEM.
     */
public static func fromPem(s: String)throws  -> SimpleKeypair  {
    return try  FfiConverterTypeSimpleKeypair_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_simplekeypair_from_pem(
        FfiConverterString.lower(s),$0
    )
})
}
    
public static func fromSecp256k1(keypair: Secp256k1PrivateKey) -> SimpleKeypair  {
    return try!  FfiConverterTypeSimpleKeypair_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_simplekeypair_from_secp256k1(
        FfiConverterTypeSecp256k1PrivateKey_lower(keypair),$0
    )
})
}
    
public static func fromSecp256r1(keypair: Secp256r1PrivateKey) -> SimpleKeypair  {
    return try!  FfiConverterTypeSimpleKeypair_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_simplekeypair_from_secp256r1(
        FfiConverterTypeSecp256r1PrivateKey_lower(keypair),$0
    )
})
}
    

    
open func publicKey() -> MultisigMemberPublicKey  {
    return try!  FfiConverterTypeMultisigMemberPublicKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_simplekeypair_public_key(self.uniffiClonePointer(),$0
    )
})
}
    
open func scheme() -> SignatureScheme  {
    return try!  FfiConverterTypeSignatureScheme_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_simplekeypair_scheme(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Sign a personal message and return a UserSignature.
     */
open func signPersonalMessage(message: PersonalMessage)throws  -> UserSignature  {
    return try  FfiConverterTypeUserSignature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_simplekeypair_sign_personal_message(self.uniffiClonePointer(),
        FfiConverterTypePersonalMessage_lower(message),$0
    )
})
}
    
    /**
     * Sign a transaction and return a UserSignature.
     */
open func signTransaction(transaction: Transaction)throws  -> UserSignature  {
    return try  FfiConverterTypeUserSignature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_simplekeypair_sign_transaction(self.uniffiClonePointer(),
        FfiConverterTypeTransaction_lower(transaction),$0
    )
})
}
    
    /**
     * Encode a SimpleKeypair as `flag || privkey` in Bech32 starting with
     * "iotaprivkey" to a string. Note that the pubkey is not encoded.
     */
open func toBech32()throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_simplekeypair_to_bech32(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Encode a SimpleKeypair as `flag || privkey` in bytes
     */
open func toBytes() -> Data  {
    return try!  FfiConverterData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_simplekeypair_to_bytes(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Serialize this private key as DER-encoded PKCS#8
     */
open func toDer()throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_simplekeypair_to_der(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Serialize this private key as DER-encoded PKCS#8
     */
open func toPem()throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_simplekeypair_to_pem(self.uniffiClonePointer(),$0
    )
})
}
    
open func trySign(message: Data)throws  -> SimpleSignature  {
    return try  FfiConverterTypeSimpleSignature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_simplekeypair_try_sign(self.uniffiClonePointer(),
        FfiConverterData.lower(message),$0
    )
})
}
    
open func trySignUser(message: Data)throws  -> UserSignature  {
    return try  FfiConverterTypeUserSignature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_simplekeypair_try_sign_user(self.uniffiClonePointer(),
        FfiConverterData.lower(message),$0
    )
})
}
    
open func verifyingKey() -> SimpleVerifyingKey  {
    return try!  FfiConverterTypeSimpleVerifyingKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_simplekeypair_verifying_key(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_simplekeypair_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }

}
extension SimpleKeypair: CustomDebugStringConvertible {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSimpleKeypair: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = SimpleKeypair

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> SimpleKeypair {
        return SimpleKeypair(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: SimpleKeypair) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SimpleKeypair {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: SimpleKeypair, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSimpleKeypair_lift(_ pointer: UnsafeMutableRawPointer) throws -> SimpleKeypair {
    return try FfiConverterTypeSimpleKeypair.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSimpleKeypair_lower(_ value: SimpleKeypair) -> UnsafeMutableRawPointer {
    return FfiConverterTypeSimpleKeypair.lower(value)
}






/**
 * A basic signature
 *
 * This enumeration defines the set of simple or basic signature schemes
 * supported by IOTA. Most signature schemes supported by IOTA end up
 * comprising of a at least one simple signature scheme.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * simple-signature-bcs = bytes ; where the contents of the bytes are defined by <simple-signature>
 * simple-signature = (ed25519-flag ed25519-signature ed25519-public-key) /
 * (secp256k1-flag secp256k1-signature secp256k1-public-key) /
 * (secp256r1-flag secp256r1-signature secp256r1-public-key)
 * ```
 *
 * Note: Due to historical reasons, signatures are serialized slightly
 * different from the majority of the types in IOTA. In particular if a
 * signature is ever embedded in another structure it generally is serialized
 * as `bytes` meaning it has a length prefix that defines the length of
 * the completely serialized signature.
 */
public protocol SimpleSignatureProtocol: AnyObject, Sendable {
    
    func ed25519PubKey()  -> Ed25519PublicKey
    
    func ed25519PubKeyOpt()  -> Ed25519PublicKey?
    
    func ed25519Sig()  -> Ed25519Signature
    
    func ed25519SigOpt()  -> Ed25519Signature?
    
    func isEd25519()  -> Bool
    
    func isSecp256k1()  -> Bool
    
    func isSecp256r1()  -> Bool
    
    func scheme()  -> SignatureScheme
    
    func secp256k1PubKey()  -> Secp256k1PublicKey
    
    func secp256k1PubKeyOpt()  -> Secp256k1PublicKey?
    
    func secp256k1Sig()  -> Secp256k1Signature
    
    func secp256k1SigOpt()  -> Secp256k1Signature?
    
    func secp256r1PubKey()  -> Secp256r1PublicKey
    
    func secp256r1PubKeyOpt()  -> Secp256r1PublicKey?
    
    func secp256r1Sig()  -> Secp256r1Signature
    
    func secp256r1SigOpt()  -> Secp256r1Signature?
    
    func toBytes()  -> Data
    
}
/**
 * A basic signature
 *
 * This enumeration defines the set of simple or basic signature schemes
 * supported by IOTA. Most signature schemes supported by IOTA end up
 * comprising of a at least one simple signature scheme.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * simple-signature-bcs = bytes ; where the contents of the bytes are defined by <simple-signature>
 * simple-signature = (ed25519-flag ed25519-signature ed25519-public-key) /
 * (secp256k1-flag secp256k1-signature secp256k1-public-key) /
 * (secp256r1-flag secp256r1-signature secp256r1-public-key)
 * ```
 *
 * Note: Due to historical reasons, signatures are serialized slightly
 * different from the majority of the types in IOTA. In particular if a
 * signature is ever embedded in another structure it generally is serialized
 * as `bytes` meaning it has a length prefix that defines the length of
 * the completely serialized signature.
 */
open class SimpleSignature: SimpleSignatureProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_simplesignature(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_simplesignature(pointer, $0) }
    }

    
public static func newEd25519(signature: Ed25519Signature, publicKey: Ed25519PublicKey) -> SimpleSignature  {
    return try!  FfiConverterTypeSimpleSignature_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_simplesignature_new_ed25519(
        FfiConverterTypeEd25519Signature_lower(signature),
        FfiConverterTypeEd25519PublicKey_lower(publicKey),$0
    )
})
}
    
public static func newSecp256k1(signature: Secp256k1Signature, publicKey: Secp256k1PublicKey) -> SimpleSignature  {
    return try!  FfiConverterTypeSimpleSignature_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_simplesignature_new_secp256k1(
        FfiConverterTypeSecp256k1Signature_lower(signature),
        FfiConverterTypeSecp256k1PublicKey_lower(publicKey),$0
    )
})
}
    
public static func newSecp256r1(signature: Secp256r1Signature, publicKey: Secp256r1PublicKey) -> SimpleSignature  {
    return try!  FfiConverterTypeSimpleSignature_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_simplesignature_new_secp256r1(
        FfiConverterTypeSecp256r1Signature_lower(signature),
        FfiConverterTypeSecp256r1PublicKey_lower(publicKey),$0
    )
})
}
    

    
open func ed25519PubKey() -> Ed25519PublicKey  {
    return try!  FfiConverterTypeEd25519PublicKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_simplesignature_ed25519_pub_key(self.uniffiClonePointer(),$0
    )
})
}
    
open func ed25519PubKeyOpt() -> Ed25519PublicKey?  {
    return try!  FfiConverterOptionTypeEd25519PublicKey.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_simplesignature_ed25519_pub_key_opt(self.uniffiClonePointer(),$0
    )
})
}
    
open func ed25519Sig() -> Ed25519Signature  {
    return try!  FfiConverterTypeEd25519Signature_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_simplesignature_ed25519_sig(self.uniffiClonePointer(),$0
    )
})
}
    
open func ed25519SigOpt() -> Ed25519Signature?  {
    return try!  FfiConverterOptionTypeEd25519Signature.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_simplesignature_ed25519_sig_opt(self.uniffiClonePointer(),$0
    )
})
}
    
open func isEd25519() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_simplesignature_is_ed25519(self.uniffiClonePointer(),$0
    )
})
}
    
open func isSecp256k1() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_simplesignature_is_secp256k1(self.uniffiClonePointer(),$0
    )
})
}
    
open func isSecp256r1() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_simplesignature_is_secp256r1(self.uniffiClonePointer(),$0
    )
})
}
    
open func scheme() -> SignatureScheme  {
    return try!  FfiConverterTypeSignatureScheme_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_simplesignature_scheme(self.uniffiClonePointer(),$0
    )
})
}
    
open func secp256k1PubKey() -> Secp256k1PublicKey  {
    return try!  FfiConverterTypeSecp256k1PublicKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_simplesignature_secp256k1_pub_key(self.uniffiClonePointer(),$0
    )
})
}
    
open func secp256k1PubKeyOpt() -> Secp256k1PublicKey?  {
    return try!  FfiConverterOptionTypeSecp256k1PublicKey.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_simplesignature_secp256k1_pub_key_opt(self.uniffiClonePointer(),$0
    )
})
}
    
open func secp256k1Sig() -> Secp256k1Signature  {
    return try!  FfiConverterTypeSecp256k1Signature_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_simplesignature_secp256k1_sig(self.uniffiClonePointer(),$0
    )
})
}
    
open func secp256k1SigOpt() -> Secp256k1Signature?  {
    return try!  FfiConverterOptionTypeSecp256k1Signature.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_simplesignature_secp256k1_sig_opt(self.uniffiClonePointer(),$0
    )
})
}
    
open func secp256r1PubKey() -> Secp256r1PublicKey  {
    return try!  FfiConverterTypeSecp256r1PublicKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_simplesignature_secp256r1_pub_key(self.uniffiClonePointer(),$0
    )
})
}
    
open func secp256r1PubKeyOpt() -> Secp256r1PublicKey?  {
    return try!  FfiConverterOptionTypeSecp256r1PublicKey.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_simplesignature_secp256r1_pub_key_opt(self.uniffiClonePointer(),$0
    )
})
}
    
open func secp256r1Sig() -> Secp256r1Signature  {
    return try!  FfiConverterTypeSecp256r1Signature_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_simplesignature_secp256r1_sig(self.uniffiClonePointer(),$0
    )
})
}
    
open func secp256r1SigOpt() -> Secp256r1Signature?  {
    return try!  FfiConverterOptionTypeSecp256r1Signature.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_simplesignature_secp256r1_sig_opt(self.uniffiClonePointer(),$0
    )
})
}
    
open func toBytes() -> Data  {
    return try!  FfiConverterData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_simplesignature_to_bytes(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_simplesignature_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: SimpleSignature, other: SimpleSignature) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_simplesignature_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeSimpleSignature_lower(other),$0
    )
}
        )
    }
    open func hash(into hasher: inout Hasher) {
        let val = try!  FfiConverterUInt64.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_simplesignature_uniffi_trait_hash(self.uniffiClonePointer(),$0
    )
}
        )
        hasher.combine(val)
    }

}
extension SimpleSignature: CustomDebugStringConvertible {}
extension SimpleSignature: Equatable {}
extension SimpleSignature: Hashable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSimpleSignature: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = SimpleSignature

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> SimpleSignature {
        return SimpleSignature(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: SimpleSignature) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SimpleSignature {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: SimpleSignature, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSimpleSignature_lift(_ pointer: UnsafeMutableRawPointer) throws -> SimpleSignature {
    return try FfiConverterTypeSimpleSignature.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSimpleSignature_lower(_ value: SimpleSignature) -> UnsafeMutableRawPointer {
    return FfiConverterTypeSimpleSignature.lower(value)
}






public protocol SimpleVerifierProtocol: AnyObject, Sendable {
    
    func verify(message: Data, signature: SimpleSignature) throws 
    
}
open class SimpleVerifier: SimpleVerifierProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_simpleverifier(self.pointer, $0) }
    }
public convenience init() {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_simpleverifier_new($0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_simpleverifier(pointer, $0) }
    }

    

    
open func verify(message: Data, signature: SimpleSignature)throws   {try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_simpleverifier_verify(self.uniffiClonePointer(),
        FfiConverterData.lower(message),
        FfiConverterTypeSimpleSignature_lower(signature),$0
    )
}
}
    

}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSimpleVerifier: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = SimpleVerifier

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> SimpleVerifier {
        return SimpleVerifier(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: SimpleVerifier) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SimpleVerifier {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: SimpleVerifier, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSimpleVerifier_lift(_ pointer: UnsafeMutableRawPointer) throws -> SimpleVerifier {
    return try FfiConverterTypeSimpleVerifier.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSimpleVerifier_lower(_ value: SimpleVerifier) -> UnsafeMutableRawPointer {
    return FfiConverterTypeSimpleVerifier.lower(value)
}






public protocol SimpleVerifyingKeyProtocol: AnyObject, Sendable {
    
    func publicKey()  -> MultisigMemberPublicKey
    
    func scheme()  -> SignatureScheme
    
    /**
     * Serialize this private key as DER-encoded PKCS#8
     */
    func toDer() throws  -> Data
    
    /**
     * Serialize this private key as DER-encoded PKCS#8
     */
    func toPem() throws  -> String
    
    func verify(message: Data, signature: SimpleSignature) throws 
    
}
open class SimpleVerifyingKey: SimpleVerifyingKeyProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_simpleverifyingkey(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_simpleverifyingkey(pointer, $0) }
    }

    
    /**
     * Deserialize PKCS#8 private key from ASN.1 DER-encoded data (binary
     * format).
     */
public static func fromDer(bytes: Data)throws  -> SimpleVerifyingKey  {
    return try  FfiConverterTypeSimpleVerifyingKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_simpleverifyingkey_from_der(
        FfiConverterData.lower(bytes),$0
    )
})
}
    
    /**
     * Deserialize PKCS#8-encoded private key from PEM.
     */
public static func fromPem(s: String)throws  -> SimpleVerifyingKey  {
    return try  FfiConverterTypeSimpleVerifyingKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_simpleverifyingkey_from_pem(
        FfiConverterString.lower(s),$0
    )
})
}
    

    
open func publicKey() -> MultisigMemberPublicKey  {
    return try!  FfiConverterTypeMultisigMemberPublicKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_simpleverifyingkey_public_key(self.uniffiClonePointer(),$0
    )
})
}
    
open func scheme() -> SignatureScheme  {
    return try!  FfiConverterTypeSignatureScheme_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_simpleverifyingkey_scheme(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Serialize this private key as DER-encoded PKCS#8
     */
open func toDer()throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_simpleverifyingkey_to_der(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Serialize this private key as DER-encoded PKCS#8
     */
open func toPem()throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_simpleverifyingkey_to_pem(self.uniffiClonePointer(),$0
    )
})
}
    
open func verify(message: Data, signature: SimpleSignature)throws   {try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_simpleverifyingkey_verify(self.uniffiClonePointer(),
        FfiConverterData.lower(message),
        FfiConverterTypeSimpleSignature_lower(signature),$0
    )
}
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_simpleverifyingkey_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: SimpleVerifyingKey, other: SimpleVerifyingKey) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_simpleverifyingkey_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeSimpleVerifyingKey_lower(other),$0
    )
}
        )
    }

}
extension SimpleVerifyingKey: CustomDebugStringConvertible {}
extension SimpleVerifyingKey: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSimpleVerifyingKey: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = SimpleVerifyingKey

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> SimpleVerifyingKey {
        return SimpleVerifyingKey(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: SimpleVerifyingKey) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SimpleVerifyingKey {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: SimpleVerifyingKey, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSimpleVerifyingKey_lift(_ pointer: UnsafeMutableRawPointer) throws -> SimpleVerifyingKey {
    return try FfiConverterTypeSimpleVerifyingKey.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSimpleVerifyingKey_lower(_ value: SimpleVerifyingKey) -> UnsafeMutableRawPointer {
    return FfiConverterTypeSimpleVerifyingKey.lower(value)
}






/**
 * Command to split a single coin object into multiple coins
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * split-coins = argument (vector argument)
 * ```
 */
public protocol SplitCoinsProtocol: AnyObject, Sendable {
    
    /**
     * The amounts to split off
     */
    func amounts()  -> [Argument]
    
    /**
     * The coin to split
     */
    func coin()  -> Argument
    
}
/**
 * Command to split a single coin object into multiple coins
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * split-coins = argument (vector argument)
 * ```
 */
open class SplitCoins: SplitCoinsProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_splitcoins(self.pointer, $0) }
    }
public convenience init(coin: Argument, amounts: [Argument]) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_splitcoins_new(
        FfiConverterTypeArgument_lower(coin),
        FfiConverterSequenceTypeArgument.lower(amounts),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_splitcoins(pointer, $0) }
    }

    

    
    /**
     * The amounts to split off
     */
open func amounts() -> [Argument]  {
    return try!  FfiConverterSequenceTypeArgument.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_splitcoins_amounts(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The coin to split
     */
open func coin() -> Argument  {
    return try!  FfiConverterTypeArgument_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_splitcoins_coin(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_splitcoins_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: SplitCoins, other: SplitCoins) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_splitcoins_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeSplitCoins_lower(other),$0
    )
}
        )
    }

}
extension SplitCoins: CustomDebugStringConvertible {}
extension SplitCoins: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSplitCoins: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = SplitCoins

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> SplitCoins {
        return SplitCoins(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: SplitCoins) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SplitCoins {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: SplitCoins, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSplitCoins_lift(_ pointer: UnsafeMutableRawPointer) throws -> SplitCoins {
    return try FfiConverterTypeSplitCoins.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSplitCoins_lower(_ value: SplitCoins) -> UnsafeMutableRawPointer {
    return FfiConverterTypeSplitCoins.lower(value)
}






/**
 * Type information for a move struct
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * struct-tag = address            ; address of the package
 * identifier         ; name of the module
 * identifier         ; name of the type
 * (vector type-tag)  ; type parameters
 * ```
 */
public protocol StructTagProtocol: AnyObject, Sendable {
    
    /**
     * Returns the address part of a `StructTag`
     */
    func address()  -> Address
    
    /**
     * Checks if this is a Coin type
     */
    func coinType()  -> TypeTag
    
    /**
     * Checks if this is a Coin type
     */
    func coinTypeOpt()  -> TypeTag?
    
    /**
     * Returns the module part of a `StructTag`
     */
    func module()  -> Identifier
    
    /**
     * Returns the name part of a `StructTag`
     */
    func name()  -> Identifier
    
    /**
     * Returns the string representation of this struct tag using the
     * canonical display, with or without a `0x` prefix.
     */
    func toCanonicalString(withPrefix: Bool)  -> String
    
    /**
     * Returns the type params part of a `StructTag`
     */
    func typeArgs()  -> [TypeTag]
    
}
/**
 * Type information for a move struct
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * struct-tag = address            ; address of the package
 * identifier         ; name of the module
 * identifier         ; name of the type
 * (vector type-tag)  ; type parameters
 * ```
 */
open class StructTag: StructTagProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_structtag(self.pointer, $0) }
    }
public convenience init(address: Address, module: Identifier, name: Identifier, typeParams: [TypeTag] = []) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_structtag_new(
        FfiConverterTypeAddress_lower(address),
        FfiConverterTypeIdentifier_lower(module),
        FfiConverterTypeIdentifier_lower(name),
        FfiConverterSequenceTypeTypeTag.lower(typeParams),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_structtag(pointer, $0) }
    }

    
public static func newAsciiString() -> StructTag  {
    return try!  FfiConverterTypeStructTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_structtag_new_ascii_string($0
    )
})
}
    
public static func newBalance(typeTag: TypeTag) -> StructTag  {
    return try!  FfiConverterTypeStructTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_structtag_new_balance(
        FfiConverterTypeTypeTag_lower(typeTag),$0
    )
})
}
    
public static func newClock() -> StructTag  {
    return try!  FfiConverterTypeStructTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_structtag_new_clock($0
    )
})
}
    
public static func newCoin(typeTag: TypeTag) -> StructTag  {
    return try!  FfiConverterTypeStructTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_structtag_new_coin(
        FfiConverterTypeTypeTag_lower(typeTag),$0
    )
})
}
    
public static func newCoinManager(structTag: StructTag) -> StructTag  {
    return try!  FfiConverterTypeStructTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_structtag_new_coin_manager(
        FfiConverterTypeStructTag_lower(structTag),$0
    )
})
}
    
public static func newCoinMetadata(structTag: StructTag) -> StructTag  {
    return try!  FfiConverterTypeStructTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_structtag_new_coin_metadata(
        FfiConverterTypeStructTag_lower(structTag),$0
    )
})
}
    
public static func newConfig() -> StructTag  {
    return try!  FfiConverterTypeStructTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_structtag_new_config($0
    )
})
}
    
public static func newConfigSetting(typeTag: TypeTag) -> StructTag  {
    return try!  FfiConverterTypeStructTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_structtag_new_config_setting(
        FfiConverterTypeTypeTag_lower(typeTag),$0
    )
})
}
    
public static func newDenyListAddressKey() -> StructTag  {
    return try!  FfiConverterTypeStructTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_structtag_new_deny_list_address_key($0
    )
})
}
    
public static func newDenyListConfigKey() -> StructTag  {
    return try!  FfiConverterTypeStructTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_structtag_new_deny_list_config_key($0
    )
})
}
    
public static func newDenyListGlobalPauseKey() -> StructTag  {
    return try!  FfiConverterTypeStructTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_structtag_new_deny_list_global_pause_key($0
    )
})
}
    
public static func newDisplayCreated(structTag: StructTag) -> StructTag  {
    return try!  FfiConverterTypeStructTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_structtag_new_display_created(
        FfiConverterTypeStructTag_lower(structTag),$0
    )
})
}
    
public static func newDynamicObjectFieldWrapper(typeTag: TypeTag) -> StructTag  {
    return try!  FfiConverterTypeStructTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_structtag_new_dynamic_object_field_wrapper(
        FfiConverterTypeTypeTag_lower(typeTag),$0
    )
})
}
    
public static func newField(key: TypeTag, value: TypeTag) -> StructTag  {
    return try!  FfiConverterTypeStructTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_structtag_new_field(
        FfiConverterTypeTypeTag_lower(key),
        FfiConverterTypeTypeTag_lower(value),$0
    )
})
}
    
public static func newGasCoin() -> StructTag  {
    return try!  FfiConverterTypeStructTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_structtag_new_gas_coin($0
    )
})
}
    
public static func newId() -> StructTag  {
    return try!  FfiConverterTypeStructTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_structtag_new_id($0
    )
})
}
    
public static func newIotaCoinType() -> StructTag  {
    return try!  FfiConverterTypeStructTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_structtag_new_iota_coin_type($0
    )
})
}
    
public static func newIotaSystemAdminCap() -> StructTag  {
    return try!  FfiConverterTypeStructTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_structtag_new_iota_system_admin_cap($0
    )
})
}
    
public static func newIotaSystemState() -> StructTag  {
    return try!  FfiConverterTypeStructTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_structtag_new_iota_system_state($0
    )
})
}
    
public static func newIotaTreasuryCap() -> StructTag  {
    return try!  FfiConverterTypeStructTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_structtag_new_iota_treasury_cap($0
    )
})
}
    
public static func newName(address: Address) -> StructTag  {
    return try!  FfiConverterTypeStructTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_structtag_new_name(
        FfiConverterTypeAddress_lower(address),$0
    )
})
}
    
public static func newStakedIota() -> StructTag  {
    return try!  FfiConverterTypeStructTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_structtag_new_staked_iota($0
    )
})
}
    
public static func newString() -> StructTag  {
    return try!  FfiConverterTypeStructTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_structtag_new_string($0
    )
})
}
    
public static func newSystemEpochInfoEvent() -> StructTag  {
    return try!  FfiConverterTypeStructTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_structtag_new_system_epoch_info_event($0
    )
})
}
    
public static func newTimeLock(typeTag: TypeTag) -> StructTag  {
    return try!  FfiConverterTypeStructTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_structtag_new_time_lock(
        FfiConverterTypeTypeTag_lower(typeTag),$0
    )
})
}
    
public static func newTimelockedStakedIota() -> StructTag  {
    return try!  FfiConverterTypeStructTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_structtag_new_timelocked_staked_iota($0
    )
})
}
    
public static func newTransferReceiving() -> StructTag  {
    return try!  FfiConverterTypeStructTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_structtag_new_transfer_receiving($0
    )
})
}
    
public static func newTreasuryCap(structTag: StructTag) -> StructTag  {
    return try!  FfiConverterTypeStructTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_structtag_new_treasury_cap(
        FfiConverterTypeStructTag_lower(structTag),$0
    )
})
}
    
public static func newUid() -> StructTag  {
    return try!  FfiConverterTypeStructTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_structtag_new_uid($0
    )
})
}
    
public static func newUpgradeCap() -> StructTag  {
    return try!  FfiConverterTypeStructTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_structtag_new_upgrade_cap($0
    )
})
}
    
public static func newUpgradeReceipt() -> StructTag  {
    return try!  FfiConverterTypeStructTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_structtag_new_upgrade_receipt($0
    )
})
}
    
public static func newUpgradeTicket() -> StructTag  {
    return try!  FfiConverterTypeStructTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_structtag_new_upgrade_ticket($0
    )
})
}
    
public static func newVersionUpdated(structTag: StructTag) -> StructTag  {
    return try!  FfiConverterTypeStructTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_structtag_new_version_updated(
        FfiConverterTypeStructTag_lower(structTag),$0
    )
})
}
    

    
    /**
     * Returns the address part of a `StructTag`
     */
open func address() -> Address  {
    return try!  FfiConverterTypeAddress_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_structtag_address(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Checks if this is a Coin type
     */
open func coinType() -> TypeTag  {
    return try!  FfiConverterTypeTypeTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_structtag_coin_type(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Checks if this is a Coin type
     */
open func coinTypeOpt() -> TypeTag?  {
    return try!  FfiConverterOptionTypeTypeTag.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_structtag_coin_type_opt(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Returns the module part of a `StructTag`
     */
open func module() -> Identifier  {
    return try!  FfiConverterTypeIdentifier_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_structtag_module(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Returns the name part of a `StructTag`
     */
open func name() -> Identifier  {
    return try!  FfiConverterTypeIdentifier_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_structtag_name(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Returns the string representation of this struct tag using the
     * canonical display, with or without a `0x` prefix.
     */
open func toCanonicalString(withPrefix: Bool) -> String  {
    return try!  FfiConverterString.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_structtag_to_canonical_string(self.uniffiClonePointer(),
        FfiConverterBool.lower(withPrefix),$0
    )
})
}
    
    /**
     * Returns the type params part of a `StructTag`
     */
open func typeArgs() -> [TypeTag]  {
    return try!  FfiConverterSequenceTypeTypeTag.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_structtag_type_args(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_structtag_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    open var description: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_structtag_uniffi_trait_display(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: StructTag, other: StructTag) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_structtag_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeStructTag_lower(other),$0
    )
}
        )
    }
    open func hash(into hasher: inout Hasher) {
        let val = try!  FfiConverterUInt64.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_structtag_uniffi_trait_hash(self.uniffiClonePointer(),$0
    )
}
        )
        hasher.combine(val)
    }

}
extension StructTag: CustomDebugStringConvertible {}
extension StructTag: CustomStringConvertible {}
extension StructTag: Equatable {}
extension StructTag: Hashable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeStructTag: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = StructTag

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> StructTag {
        return StructTag(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: StructTag) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> StructTag {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: StructTag, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeStructTag_lift(_ pointer: UnsafeMutableRawPointer) throws -> StructTag {
    return try FfiConverterTypeStructTag.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeStructTag_lower(_ value: StructTag) -> UnsafeMutableRawPointer {
    return FfiConverterTypeStructTag.lower(value)
}






/**
 * System package
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * system-package = u64                ; version
 * (vector bytes)     ; modules
 * (vector object-id) ; dependencies
 * ```
 */
public protocol SystemPackageProtocol: AnyObject, Sendable {
    
    func dependencies()  -> [ObjectId]
    
    func modules()  -> [Data]
    
    func version()  -> UInt64
    
}
/**
 * System package
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * system-package = u64                ; version
 * (vector bytes)     ; modules
 * (vector object-id) ; dependencies
 * ```
 */
open class SystemPackage: SystemPackageProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_systempackage(self.pointer, $0) }
    }
public convenience init(version: UInt64, modules: [Data], dependencies: [ObjectId]) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_systempackage_new(
        FfiConverterUInt64.lower(version),
        FfiConverterSequenceData.lower(modules),
        FfiConverterSequenceTypeObjectId.lower(dependencies),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_systempackage(pointer, $0) }
    }

    

    
open func dependencies() -> [ObjectId]  {
    return try!  FfiConverterSequenceTypeObjectId.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_systempackage_dependencies(self.uniffiClonePointer(),$0
    )
})
}
    
open func modules() -> [Data]  {
    return try!  FfiConverterSequenceData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_systempackage_modules(self.uniffiClonePointer(),$0
    )
})
}
    
open func version() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_systempackage_version(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_systempackage_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: SystemPackage, other: SystemPackage) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_systempackage_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeSystemPackage_lower(other),$0
    )
}
        )
    }

}
extension SystemPackage: CustomDebugStringConvertible {}
extension SystemPackage: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSystemPackage: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = SystemPackage

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> SystemPackage {
        return SystemPackage(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: SystemPackage) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SystemPackage {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: SystemPackage, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSystemPackage_lift(_ pointer: UnsafeMutableRawPointer) throws -> SystemPackage {
    return try FfiConverterTypeSystemPackage.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSystemPackage_lower(_ value: SystemPackage) -> UnsafeMutableRawPointer {
    return FfiConverterTypeSystemPackage.lower(value)
}






/**
 * Transaction
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * transaction = %x00 transaction-v1
 *
 * transaction-v1 = transaction-kind address gas-payment transaction-expiration
 * ```
 */
public protocol TransactionProtocol: AnyObject, Sendable {
    
    func asV1()  -> TransactionV1
    
    func digest()  -> Digest
    
    func expiration()  -> TransactionExpiration
    
    func gasPayment()  -> GasPayment
    
    func kind()  -> TransactionKind
    
    func sender()  -> Address
    
    /**
     * Get the signing digest.
     */
    func signingDigest()  -> Data
    
    /**
     * Get the signing digest as a hex string.
     */
    func signingDigestHex()  -> String
    
    /**
     * Serialize the transaction as a base64-encoded string.
     */
    func toBase64()  -> String
    
}
/**
 * Transaction
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * transaction = %x00 transaction-v1
 *
 * transaction-v1 = transaction-kind address gas-payment transaction-expiration
 * ```
 */
open class Transaction: TransactionProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_transaction(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_transaction(pointer, $0) }
    }

    
    /**
     * Deserialize a transaction from a base64-encoded string.
     */
public static func fromBase64(base64: String)throws  -> Transaction  {
    return try  FfiConverterTypeTransaction_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_transaction_from_base64(
        FfiConverterString.lower(base64),$0
    )
})
}
    
public static func newV1(transactionV1: TransactionV1) -> Transaction  {
    return try!  FfiConverterTypeTransaction_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_transaction_new_v1(
        FfiConverterTypeTransactionV1_lower(transactionV1),$0
    )
})
}
    

    
open func asV1() -> TransactionV1  {
    return try!  FfiConverterTypeTransactionV1_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transaction_as_v1(self.uniffiClonePointer(),$0
    )
})
}
    
open func digest() -> Digest  {
    return try!  FfiConverterTypeDigest_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transaction_digest(self.uniffiClonePointer(),$0
    )
})
}
    
open func expiration() -> TransactionExpiration  {
    return try!  FfiConverterTypeTransactionExpiration_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transaction_expiration(self.uniffiClonePointer(),$0
    )
})
}
    
open func gasPayment() -> GasPayment  {
    return try!  FfiConverterTypeGasPayment_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transaction_gas_payment(self.uniffiClonePointer(),$0
    )
})
}
    
open func kind() -> TransactionKind  {
    return try!  FfiConverterTypeTransactionKind_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transaction_kind(self.uniffiClonePointer(),$0
    )
})
}
    
open func sender() -> Address  {
    return try!  FfiConverterTypeAddress_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transaction_sender(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Get the signing digest.
     */
open func signingDigest() -> Data  {
    return try!  FfiConverterData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transaction_signing_digest(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Get the signing digest as a hex string.
     */
open func signingDigestHex() -> String  {
    return try!  FfiConverterString.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transaction_signing_digest_hex(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Serialize the transaction as a base64-encoded string.
     */
open func toBase64() -> String  {
    return try!  FfiConverterString.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transaction_to_base64(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transaction_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: Transaction, other: Transaction) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transaction_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeTransaction_lower(other),$0
    )
}
        )
    }

}
extension Transaction: CustomDebugStringConvertible {}
extension Transaction: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeTransaction: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = Transaction

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> Transaction {
        return Transaction(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: Transaction) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Transaction {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: Transaction, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransaction_lift(_ pointer: UnsafeMutableRawPointer) throws -> Transaction {
    return try FfiConverterTypeTransaction.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransaction_lower(_ value: Transaction) -> UnsafeMutableRawPointer {
    return FfiConverterTypeTransaction.lower(value)
}






/**
 * A builder for creating transactions. Use `finish` to finalize the
 * transaction data.
 */
public protocol TransactionBuilderProtocol: AnyObject, Sendable {
    
    /**
     * Execute the transaction using the gas station and return the JSON
     * transaction effects. This will fail unless data is set with the
     * `gas_station_sponsor` function.
     *
     * NOTE: These effects are not necessarily compatible with
     * `TransactionEffects`
     */
    func executeWithGasStation(signer: TransactionSigner) async throws  -> Value
    
    /**
     * Set the expiration of the transaction to be a specific epoch.
     */
    func expiration(epoch: UInt64)  -> TransactionBuilder
    
    /**
     * Convert this builder into a transaction.
     */
    func finish() throws  -> Transaction
    
    /**
     * Add gas coins that will be consumed. Optional.
     */
    func gas(objectRefs: [ObjectReference])  -> TransactionBuilder
    
    /**
     * Set the gas budget for the transaction.
     */
    func gasBudget(budget: UInt64)  -> TransactionBuilder
    
    /**
     * Set the gas price for the transaction.
     */
    func gasPrice(price: UInt64)  -> TransactionBuilder
    
    /**
     * Set the gas station sponsor.
     */
    func gasStationSponsor(url: String, duration: TimeInterval?, headers: [String: [String]]?)  -> TransactionBuilder
    
    /**
     * Make a move vector from a list of elements. The elements must all be of
     * the type indicated by `type_tag`.
     */
    func makeMoveVec(elements: [MoveArg], typeTag: TypeTag, name: String)  -> TransactionBuilder
    
    /**
     * Merge multiple coins into one.
     *
     * This method combines the balances of multiple coins of the same coin
     * type into a single coin. The `primary_coin` will receive the balances
     * from all `consumed_coins`. After merging, the `consumed_coins` will
     * be consumed and no longer exist.
     */
    func mergeCoins(primaryCoin: PtbArgument, consumedCoins: [PtbArgument])  -> TransactionBuilder
    
    /**
     * Call a Move function with the given arguments.
     */
    func moveCall(package: Address, module: Identifier, function: Identifier, arguments: [PtbArgument], typeArgs: [TypeTag], names: [String])  -> TransactionBuilder
    
    /**
     * Publish a list of modules with the given dependencies. The result
     * assigned to `upgrade_cap_name` is the `0x2::package::UpgradeCap`
     * Move type. Note that the upgrade capability needs to be handled
     * after this call:
     * - transfer it to the transaction sender or another address
     * - burn it
     * - wrap it for access control
     * - discard the it to make a package immutable
     *
     * The arguments required for this command are:
     * - `modules`: is the modules' bytecode to be published
     * - `dependencies`: is the list of IDs of the transitive dependencies of
     * the package
     */
    func publish(packageData: MovePackageData, upgradeCapName: String)  -> TransactionBuilder
    
    /**
     * Transfer some coins to a recipient address. If multiple coins are
     * provided then they will be merged.
     *
     * The `amount` parameter specifies the quantity in NANOS, where 1 IOTA
     * equals 1_000_000_000 NANOS.
     * If `amount` is provided, that amount is split from the provided coins
     * and sent.
     * If `amount` is `None`, the entire coins are transferred.
     *
     * All provided coins must have the same coin type. Mixing coins of
     * different types will result in an error.
     *
     * If you intend to transfer all provided coins to another address in a
     * single transaction, consider using
     * `TransactionBuilder::transfer_objects()` instead.
     */
    func sendCoins(coins: [PtbArgument], recipient: Address, amount: PtbArgument?)  -> TransactionBuilder
    
    /**
     * Send IOTA to a recipient address.
     *
     * The `amount` parameter specifies the quantity in NANOS, where 1 IOTA
     * equals 1_000_000_000 NANOS. That amount is split from the gas coin and
     * sent.
     */
    func sendIota(recipient: Address, amount: PtbArgument)  -> TransactionBuilder
    
    /**
     * Set the sender address.
     */
    func setSender(sender: Address) 
    
    /**
     * Split a coin by the provided amounts.
     */
    func splitCoins(coin: PtbArgument, amounts: [PtbArgument], names: [String])  -> TransactionBuilder
    
    /**
     * Set the sponsor of the transaction.
     */
    func sponsor(sponsor: Address)  -> TransactionBuilder
    
    /**
     * Add stake to a validator's staking pool.
     *
     * This is a high-level function which will split the provided stake amount
     * from the gas coin and then stake using the resulting coin.
     */
    func stake(stake: PtbArgument, validatorAddress: Address)  -> TransactionBuilder
    
    /**
     * Transfer a list of objects to the given address, without producing any
     * result.
     */
    func transferObjects(recipient: Address, objects: [PtbArgument])  -> TransactionBuilder
    
    /**
     * Withdraw stake from a validator's staking pool.
     */
    func unstake(stakedIota: PtbArgument)  -> TransactionBuilder
    
    /**
     * Upgrade a Move package.
     *
     * - `modules`: is the modules' bytecode for the modules to be published
     * - `dependencies`: is the list of IDs of the transitive dependencies of
     * the package to be upgraded
     * - `package`: is the ID of the current package being upgraded
     * - `ticket`: is the upgrade ticket
     *
     * To get the ticket, you have to call the
     * `0x2::package::authorize_upgrade` function, and pass the package
     * ID, the upgrade policy, and package digest.
     */
    func upgrade(packageId: ObjectId, packageData: MovePackageData, upgradeTicket: PtbArgument, name: String?)  -> TransactionBuilder
    
    func withClient(client: GraphQlClient)  -> ClientTransactionBuilder
    
}
/**
 * A builder for creating transactions. Use `finish` to finalize the
 * transaction data.
 */
open class TransactionBuilder: TransactionBuilderProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_transactionbuilder(self.pointer, $0) }
    }
    /**
     * Create a new transaction builder and initialize its elements to default.
     */
public convenience init(sender: Address) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_transactionbuilder_new(
        FfiConverterTypeAddress_lower(sender),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_transactionbuilder(pointer, $0) }
    }

    

    
    /**
     * Execute the transaction using the gas station and return the JSON
     * transaction effects. This will fail unless data is set with the
     * `gas_station_sponsor` function.
     *
     * NOTE: These effects are not necessarily compatible with
     * `TransactionEffects`
     */
open func executeWithGasStation(signer: TransactionSigner)async throws  -> Value  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_transactionbuilder_execute_with_gas_station(
                    self.uniffiClonePointer(),
                    FfiConverterTypeTransactionSigner_lower(signer)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterTypeValue_lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    
    /**
     * Set the expiration of the transaction to be a specific epoch.
     */
open func expiration(epoch: UInt64) -> TransactionBuilder  {
    return try!  FfiConverterTypeTransactionBuilder_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactionbuilder_expiration(self.uniffiClonePointer(),
        FfiConverterUInt64.lower(epoch),$0
    )
})
}
    
    /**
     * Convert this builder into a transaction.
     */
open func finish()throws  -> Transaction  {
    return try  FfiConverterTypeTransaction_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_transactionbuilder_finish(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Add gas coins that will be consumed. Optional.
     */
open func gas(objectRefs: [ObjectReference]) -> TransactionBuilder  {
    return try!  FfiConverterTypeTransactionBuilder_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactionbuilder_gas(self.uniffiClonePointer(),
        FfiConverterSequenceTypeObjectReference.lower(objectRefs),$0
    )
})
}
    
    /**
     * Set the gas budget for the transaction.
     */
open func gasBudget(budget: UInt64) -> TransactionBuilder  {
    return try!  FfiConverterTypeTransactionBuilder_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactionbuilder_gas_budget(self.uniffiClonePointer(),
        FfiConverterUInt64.lower(budget),$0
    )
})
}
    
    /**
     * Set the gas price for the transaction.
     */
open func gasPrice(price: UInt64) -> TransactionBuilder  {
    return try!  FfiConverterTypeTransactionBuilder_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactionbuilder_gas_price(self.uniffiClonePointer(),
        FfiConverterUInt64.lower(price),$0
    )
})
}
    
    /**
     * Set the gas station sponsor.
     */
open func gasStationSponsor(url: String, duration: TimeInterval? = nil, headers: [String: [String]]? = nil) -> TransactionBuilder  {
    return try!  FfiConverterTypeTransactionBuilder_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactionbuilder_gas_station_sponsor(self.uniffiClonePointer(),
        FfiConverterString.lower(url),
        FfiConverterOptionDuration.lower(duration),
        FfiConverterOptionDictionaryStringSequenceString.lower(headers),$0
    )
})
}
    
    /**
     * Make a move vector from a list of elements. The elements must all be of
     * the type indicated by `type_tag`.
     */
open func makeMoveVec(elements: [MoveArg], typeTag: TypeTag, name: String) -> TransactionBuilder  {
    return try!  FfiConverterTypeTransactionBuilder_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactionbuilder_make_move_vec(self.uniffiClonePointer(),
        FfiConverterSequenceTypeMoveArg.lower(elements),
        FfiConverterTypeTypeTag_lower(typeTag),
        FfiConverterString.lower(name),$0
    )
})
}
    
    /**
     * Merge multiple coins into one.
     *
     * This method combines the balances of multiple coins of the same coin
     * type into a single coin. The `primary_coin` will receive the balances
     * from all `consumed_coins`. After merging, the `consumed_coins` will
     * be consumed and no longer exist.
     */
open func mergeCoins(primaryCoin: PtbArgument, consumedCoins: [PtbArgument]) -> TransactionBuilder  {
    return try!  FfiConverterTypeTransactionBuilder_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactionbuilder_merge_coins(self.uniffiClonePointer(),
        FfiConverterTypePTBArgument_lower(primaryCoin),
        FfiConverterSequenceTypePTBArgument.lower(consumedCoins),$0
    )
})
}
    
    /**
     * Call a Move function with the given arguments.
     */
open func moveCall(package: Address, module: Identifier, function: Identifier, arguments: [PtbArgument] = [], typeArgs: [TypeTag] = [], names: [String] = []) -> TransactionBuilder  {
    return try!  FfiConverterTypeTransactionBuilder_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactionbuilder_move_call(self.uniffiClonePointer(),
        FfiConverterTypeAddress_lower(package),
        FfiConverterTypeIdentifier_lower(module),
        FfiConverterTypeIdentifier_lower(function),
        FfiConverterSequenceTypePTBArgument.lower(arguments),
        FfiConverterSequenceTypeTypeTag.lower(typeArgs),
        FfiConverterSequenceString.lower(names),$0
    )
})
}
    
    /**
     * Publish a list of modules with the given dependencies. The result
     * assigned to `upgrade_cap_name` is the `0x2::package::UpgradeCap`
     * Move type. Note that the upgrade capability needs to be handled
     * after this call:
     * - transfer it to the transaction sender or another address
     * - burn it
     * - wrap it for access control
     * - discard the it to make a package immutable
     *
     * The arguments required for this command are:
     * - `modules`: is the modules' bytecode to be published
     * - `dependencies`: is the list of IDs of the transitive dependencies of
     * the package
     */
open func publish(packageData: MovePackageData, upgradeCapName: String) -> TransactionBuilder  {
    return try!  FfiConverterTypeTransactionBuilder_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactionbuilder_publish(self.uniffiClonePointer(),
        FfiConverterTypeMovePackageData_lower(packageData),
        FfiConverterString.lower(upgradeCapName),$0
    )
})
}
    
    /**
     * Transfer some coins to a recipient address. If multiple coins are
     * provided then they will be merged.
     *
     * The `amount` parameter specifies the quantity in NANOS, where 1 IOTA
     * equals 1_000_000_000 NANOS.
     * If `amount` is provided, that amount is split from the provided coins
     * and sent.
     * If `amount` is `None`, the entire coins are transferred.
     *
     * All provided coins must have the same coin type. Mixing coins of
     * different types will result in an error.
     *
     * If you intend to transfer all provided coins to another address in a
     * single transaction, consider using
     * `TransactionBuilder::transfer_objects()` instead.
     */
open func sendCoins(coins: [PtbArgument], recipient: Address, amount: PtbArgument? = nil) -> TransactionBuilder  {
    return try!  FfiConverterTypeTransactionBuilder_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactionbuilder_send_coins(self.uniffiClonePointer(),
        FfiConverterSequenceTypePTBArgument.lower(coins),
        FfiConverterTypeAddress_lower(recipient),
        FfiConverterOptionTypePTBArgument.lower(amount),$0
    )
})
}
    
    /**
     * Send IOTA to a recipient address.
     *
     * The `amount` parameter specifies the quantity in NANOS, where 1 IOTA
     * equals 1_000_000_000 NANOS. That amount is split from the gas coin and
     * sent.
     */
open func sendIota(recipient: Address, amount: PtbArgument) -> TransactionBuilder  {
    return try!  FfiConverterTypeTransactionBuilder_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactionbuilder_send_iota(self.uniffiClonePointer(),
        FfiConverterTypeAddress_lower(recipient),
        FfiConverterTypePTBArgument_lower(amount),$0
    )
})
}
    
    /**
     * Set the sender address.
     */
open func setSender(sender: Address)  {try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactionbuilder_set_sender(self.uniffiClonePointer(),
        FfiConverterTypeAddress_lower(sender),$0
    )
}
}
    
    /**
     * Split a coin by the provided amounts.
     */
open func splitCoins(coin: PtbArgument, amounts: [PtbArgument], names: [String] = []) -> TransactionBuilder  {
    return try!  FfiConverterTypeTransactionBuilder_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactionbuilder_split_coins(self.uniffiClonePointer(),
        FfiConverterTypePTBArgument_lower(coin),
        FfiConverterSequenceTypePTBArgument.lower(amounts),
        FfiConverterSequenceString.lower(names),$0
    )
})
}
    
    /**
     * Set the sponsor of the transaction.
     */
open func sponsor(sponsor: Address) -> TransactionBuilder  {
    return try!  FfiConverterTypeTransactionBuilder_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactionbuilder_sponsor(self.uniffiClonePointer(),
        FfiConverterTypeAddress_lower(sponsor),$0
    )
})
}
    
    /**
     * Add stake to a validator's staking pool.
     *
     * This is a high-level function which will split the provided stake amount
     * from the gas coin and then stake using the resulting coin.
     */
open func stake(stake: PtbArgument, validatorAddress: Address) -> TransactionBuilder  {
    return try!  FfiConverterTypeTransactionBuilder_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactionbuilder_stake(self.uniffiClonePointer(),
        FfiConverterTypePTBArgument_lower(stake),
        FfiConverterTypeAddress_lower(validatorAddress),$0
    )
})
}
    
    /**
     * Transfer a list of objects to the given address, without producing any
     * result.
     */
open func transferObjects(recipient: Address, objects: [PtbArgument]) -> TransactionBuilder  {
    return try!  FfiConverterTypeTransactionBuilder_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactionbuilder_transfer_objects(self.uniffiClonePointer(),
        FfiConverterTypeAddress_lower(recipient),
        FfiConverterSequenceTypePTBArgument.lower(objects),$0
    )
})
}
    
    /**
     * Withdraw stake from a validator's staking pool.
     */
open func unstake(stakedIota: PtbArgument) -> TransactionBuilder  {
    return try!  FfiConverterTypeTransactionBuilder_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactionbuilder_unstake(self.uniffiClonePointer(),
        FfiConverterTypePTBArgument_lower(stakedIota),$0
    )
})
}
    
    /**
     * Upgrade a Move package.
     *
     * - `modules`: is the modules' bytecode for the modules to be published
     * - `dependencies`: is the list of IDs of the transitive dependencies of
     * the package to be upgraded
     * - `package`: is the ID of the current package being upgraded
     * - `ticket`: is the upgrade ticket
     *
     * To get the ticket, you have to call the
     * `0x2::package::authorize_upgrade` function, and pass the package
     * ID, the upgrade policy, and package digest.
     */
open func upgrade(packageId: ObjectId, packageData: MovePackageData, upgradeTicket: PtbArgument, name: String? = nil) -> TransactionBuilder  {
    return try!  FfiConverterTypeTransactionBuilder_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactionbuilder_upgrade(self.uniffiClonePointer(),
        FfiConverterTypeObjectId_lower(packageId),
        FfiConverterTypeMovePackageData_lower(packageData),
        FfiConverterTypePTBArgument_lower(upgradeTicket),
        FfiConverterOptionString.lower(name),$0
    )
})
}
    
open func withClient(client: GraphQlClient) -> ClientTransactionBuilder  {
    return try!  FfiConverterTypeClientTransactionBuilder_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactionbuilder_with_client(self.uniffiClonePointer(),
        FfiConverterTypeGraphQLClient_lower(client),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactionbuilder_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }

}
extension TransactionBuilder: CustomDebugStringConvertible {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeTransactionBuilder: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = TransactionBuilder

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> TransactionBuilder {
        return TransactionBuilder(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: TransactionBuilder) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> TransactionBuilder {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: TransactionBuilder, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionBuilder_lift(_ pointer: UnsafeMutableRawPointer) throws -> TransactionBuilder {
    return try FfiConverterTypeTransactionBuilder.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionBuilder_lower(_ value: TransactionBuilder) -> UnsafeMutableRawPointer {
    return FfiConverterTypeTransactionBuilder.lower(value)
}






/**
 * The output or effects of executing a transaction
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * transaction-effects =  %x00 effects-v1
 * =/ %x01 effects-v2
 * ```
 */
public protocol TransactionEffectsProtocol: AnyObject, Sendable {
    
    func asV1()  -> TransactionEffectsV1
    
    func digest()  -> Digest
    
    func isV1()  -> Bool
    
}
/**
 * The output or effects of executing a transaction
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * transaction-effects =  %x00 effects-v1
 * =/ %x01 effects-v2
 * ```
 */
open class TransactionEffects: TransactionEffectsProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_transactioneffects(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_transactioneffects(pointer, $0) }
    }

    
public static func newV1(effects: TransactionEffectsV1) -> TransactionEffects  {
    return try!  FfiConverterTypeTransactionEffects_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_transactioneffects_new_v1(
        FfiConverterTypeTransactionEffectsV1_lower(effects),$0
    )
})
}
    

    
open func asV1() -> TransactionEffectsV1  {
    return try!  FfiConverterTypeTransactionEffectsV1_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactioneffects_as_v1(self.uniffiClonePointer(),$0
    )
})
}
    
open func digest() -> Digest  {
    return try!  FfiConverterTypeDigest_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactioneffects_digest(self.uniffiClonePointer(),$0
    )
})
}
    
open func isV1() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactioneffects_is_v1(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactioneffects_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: TransactionEffects, other: TransactionEffects) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactioneffects_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeTransactionEffects_lower(other),$0
    )
}
        )
    }

}
extension TransactionEffects: CustomDebugStringConvertible {}
extension TransactionEffects: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeTransactionEffects: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = TransactionEffects

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> TransactionEffects {
        return TransactionEffects(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: TransactionEffects) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> TransactionEffects {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: TransactionEffects, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionEffects_lift(_ pointer: UnsafeMutableRawPointer) throws -> TransactionEffects {
    return try FfiConverterTypeTransactionEffects.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionEffects_lower(_ value: TransactionEffects) -> UnsafeMutableRawPointer {
    return FfiConverterTypeTransactionEffects.lower(value)
}






/**
 * Events emitted during the successful execution of a transaction
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * transaction-events = vector event
 * ```
 */
public protocol TransactionEventsProtocol: AnyObject, Sendable {
    
    func digest()  -> Digest
    
    func events()  -> [Event]
    
}
/**
 * Events emitted during the successful execution of a transaction
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * transaction-events = vector event
 * ```
 */
open class TransactionEvents: TransactionEventsProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_transactionevents(self.pointer, $0) }
    }
public convenience init(events: [Event]) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_transactionevents_new(
        FfiConverterSequenceTypeEvent.lower(events),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_transactionevents(pointer, $0) }
    }

    

    
open func digest() -> Digest  {
    return try!  FfiConverterTypeDigest_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactionevents_digest(self.uniffiClonePointer(),$0
    )
})
}
    
open func events() -> [Event]  {
    return try!  FfiConverterSequenceTypeEvent.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactionevents_events(self.uniffiClonePointer(),$0
    )
})
}
    

}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeTransactionEvents: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = TransactionEvents

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> TransactionEvents {
        return TransactionEvents(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: TransactionEvents) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> TransactionEvents {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: TransactionEvents, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionEvents_lift(_ pointer: UnsafeMutableRawPointer) throws -> TransactionEvents {
    return try FfiConverterTypeTransactionEvents.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionEvents_lower(_ value: TransactionEvents) -> UnsafeMutableRawPointer {
    return FfiConverterTypeTransactionEvents.lower(value)
}






/**
 * Transaction type
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * transaction-kind    =  %x00 ptb
 * =/ %x01 change-epoch
 * =/ %x02 genesis-transaction
 * =/ %x03 consensus-commit-prologue
 * =/ %x04 authenticator-state-update
 * =/ %x05 (vector end-of-epoch-transaction-kind)
 * =/ %x06 randomness-state-update
 * =/ %x07 consensus-commit-prologue-v2
 * =/ %x08 consensus-commit-prologue-v3
 * ```
 */
public protocol TransactionKindProtocol: AnyObject, Sendable {
    
}
/**
 * Transaction type
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * transaction-kind    =  %x00 ptb
 * =/ %x01 change-epoch
 * =/ %x02 genesis-transaction
 * =/ %x03 consensus-commit-prologue
 * =/ %x04 authenticator-state-update
 * =/ %x05 (vector end-of-epoch-transaction-kind)
 * =/ %x06 randomness-state-update
 * =/ %x07 consensus-commit-prologue-v2
 * =/ %x08 consensus-commit-prologue-v3
 * ```
 */
open class TransactionKind: TransactionKindProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_transactionkind(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_transactionkind(pointer, $0) }
    }

    
public static func newAuthenticatorStateUpdateV1(tx: AuthenticatorStateUpdateV1) -> TransactionKind  {
    return try!  FfiConverterTypeTransactionKind_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_transactionkind_new_authenticator_state_update_v1(
        FfiConverterTypeAuthenticatorStateUpdateV1_lower(tx),$0
    )
})
}
    
public static func newConsensusCommitPrologueV1(tx: ConsensusCommitPrologueV1) -> TransactionKind  {
    return try!  FfiConverterTypeTransactionKind_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_transactionkind_new_consensus_commit_prologue_v1(
        FfiConverterTypeConsensusCommitPrologueV1_lower(tx),$0
    )
})
}
    
public static func newEndOfEpoch(tx: [EndOfEpochTransactionKind]) -> TransactionKind  {
    return try!  FfiConverterTypeTransactionKind_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_transactionkind_new_end_of_epoch(
        FfiConverterSequenceTypeEndOfEpochTransactionKind.lower(tx),$0
    )
})
}
    
public static func newGenesis(tx: GenesisTransaction) -> TransactionKind  {
    return try!  FfiConverterTypeTransactionKind_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_transactionkind_new_genesis(
        FfiConverterTypeGenesisTransaction_lower(tx),$0
    )
})
}
    
public static func newProgrammableTransaction(tx: ProgrammableTransaction) -> TransactionKind  {
    return try!  FfiConverterTypeTransactionKind_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_transactionkind_new_programmable_transaction(
        FfiConverterTypeProgrammableTransaction_lower(tx),$0
    )
})
}
    
public static func newRandomnessStateUpdate(tx: RandomnessStateUpdate) -> TransactionKind  {
    return try!  FfiConverterTypeTransactionKind_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_transactionkind_new_randomness_state_update(
        FfiConverterTypeRandomnessStateUpdate_lower(tx),$0
    )
})
}
    

    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactionkind_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: TransactionKind, other: TransactionKind) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactionkind_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeTransactionKind_lower(other),$0
    )
}
        )
    }

}
extension TransactionKind: CustomDebugStringConvertible {}
extension TransactionKind: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeTransactionKind: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = TransactionKind

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> TransactionKind {
        return TransactionKind(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: TransactionKind) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> TransactionKind {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: TransactionKind, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionKind_lift(_ pointer: UnsafeMutableRawPointer) throws -> TransactionKind {
    return try FfiConverterTypeTransactionKind.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionKind_lower(_ value: TransactionKind) -> UnsafeMutableRawPointer {
    return FfiConverterTypeTransactionKind.lower(value)
}






/**
 * An async signer implementation which wraps a `TransactionSignerFn`
 * definition, which can be used to sign a transaction with a callback.
 */
public protocol TransactionSignerProtocol: AnyObject, Sendable {
    
    func sign(txn: Transaction) async throws  -> UserSignature
    
}
/**
 * An async signer implementation which wraps a `TransactionSignerFn`
 * definition, which can be used to sign a transaction with a callback.
 */
open class TransactionSigner: TransactionSignerProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_transactionsigner(self.pointer, $0) }
    }
public convenience init(signerFn: TransactionSignerFn) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_transactionsigner_new(
        FfiConverterTypeTransactionSignerFn_lower(signerFn),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_transactionsigner(pointer, $0) }
    }

    
public static func fromEd25519(key: Ed25519PrivateKey) -> TransactionSigner  {
    return try!  FfiConverterTypeTransactionSigner_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_transactionsigner_from_ed25519(
        FfiConverterTypeEd25519PrivateKey_lower(key),$0
    )
})
}
    
public static func fromKeypair(key: SimpleKeypair) -> TransactionSigner  {
    return try!  FfiConverterTypeTransactionSigner_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_transactionsigner_from_keypair(
        FfiConverterTypeSimpleKeypair_lower(key),$0
    )
})
}
    
public static func fromMoveAuthenticator(auth: MoveAuthenticator) -> TransactionSigner  {
    return try!  FfiConverterTypeTransactionSigner_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_transactionsigner_from_move_authenticator(
        FfiConverterTypeMoveAuthenticator_lower(auth),$0
    )
})
}
    
public static func fromSecp256k1(key: Secp256k1PrivateKey) -> TransactionSigner  {
    return try!  FfiConverterTypeTransactionSigner_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_transactionsigner_from_secp256k1(
        FfiConverterTypeSecp256k1PrivateKey_lower(key),$0
    )
})
}
    
public static func fromSecp256r1(key: Secp256r1PrivateKey) -> TransactionSigner  {
    return try!  FfiConverterTypeTransactionSigner_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_transactionsigner_from_secp256r1(
        FfiConverterTypeSecp256r1PrivateKey_lower(key),$0
    )
})
}
    

    
open func sign(txn: Transaction)async throws  -> UserSignature  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_transactionsigner_sign(
                    self.uniffiClonePointer(),
                    FfiConverterTypeTransaction_lower(txn)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_pointer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_pointer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_pointer,
            liftFunc: FfiConverterTypeUserSignature_lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    

}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeTransactionSigner: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = TransactionSigner

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> TransactionSigner {
        return TransactionSigner(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: TransactionSigner) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> TransactionSigner {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: TransactionSigner, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionSigner_lift(_ pointer: UnsafeMutableRawPointer) throws -> TransactionSigner {
    return try FfiConverterTypeTransactionSigner.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionSigner_lower(_ value: TransactionSigner) -> UnsafeMutableRawPointer {
    return FfiConverterTypeTransactionSigner.lower(value)
}






/**
 * Defines a type which can sign a transaction asynchronously.
 *
 * This trait can be implemented downstream to enable signing when using the
 * `TransactionBuilder::execute` function.
 */
public protocol TransactionSignerFn: AnyObject, Sendable {
    
    /**
     * Sign a transaction and return a `UserSignature`.
     */
    func sign(transaction: Transaction) async throws  -> TransactionSignerFnOutput
    
}
/**
 * Defines a type which can sign a transaction asynchronously.
 *
 * This trait can be implemented downstream to enable signing when using the
 * `TransactionBuilder::execute` function.
 */
open class TransactionSignerFnImpl: TransactionSignerFn, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_transactionsignerfn(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_transactionsignerfn(pointer, $0) }
    }

    

    
    /**
     * Sign a transaction and return a `UserSignature`.
     */
open func sign(transaction: Transaction)async throws  -> TransactionSignerFnOutput  {
    return
        try  await uniffiRustCallAsync(
            rustFutureFunc: {
                uniffi_iota_sdk_ffi_fn_method_transactionsignerfn_sign(
                    self.uniffiClonePointer(),
                    FfiConverterTypeTransaction_lower(transaction)
                )
            },
            pollFunc: ffi_iota_sdk_ffi_rust_future_poll_rust_buffer,
            completeFunc: ffi_iota_sdk_ffi_rust_future_complete_rust_buffer,
            freeFunc: ffi_iota_sdk_ffi_rust_future_free_rust_buffer,
            liftFunc: FfiConverterTypeTransactionSignerFnOutput_lift,
            errorHandler: FfiConverterTypeSdkFfiError_lift
        )
}
    

}


// Put the implementation in a struct so we don't pollute the top-level namespace
fileprivate struct UniffiCallbackInterfaceTransactionSignerFn {

    // Create the VTable using a series of closures.
    // Swift automatically converts these into C callback functions.
    //
    // This creates 1-element array, since this seems to be the only way to construct a const
    // pointer that we can pass to the Rust code.
    static let vtable: [UniffiVTableCallbackInterfaceTransactionSignerFn] = [UniffiVTableCallbackInterfaceTransactionSignerFn(
        sign: { (
            uniffiHandle: UInt64,
            transaction: UnsafeMutableRawPointer,
            uniffiFutureCallback: @escaping UniffiForeignFutureCompleteRustBuffer,
            uniffiCallbackData: UInt64,
            uniffiOutReturn: UnsafeMutablePointer<UniffiForeignFuture>
        ) in
            let makeCall = {
                () async throws -> TransactionSignerFnOutput in
                guard let uniffiObj = try? FfiConverterTypeTransactionSignerFn.handleMap.get(handle: uniffiHandle) else {
                    throw UniffiInternalError.unexpectedStaleHandle
                }
                return try await uniffiObj.sign(
                     transaction: try FfiConverterTypeTransaction_lift(transaction)
                )
            }

            let uniffiHandleSuccess = { (returnValue: TransactionSignerFnOutput) in
                uniffiFutureCallback(
                    uniffiCallbackData,
                    UniffiForeignFutureStructRustBuffer(
                        returnValue: FfiConverterTypeTransactionSignerFnOutput_lower(returnValue),
                        callStatus: RustCallStatus()
                    )
                )
            }
            let uniffiHandleError = { (statusCode, errorBuf) in
                uniffiFutureCallback(
                    uniffiCallbackData,
                    UniffiForeignFutureStructRustBuffer(
                        returnValue: RustBuffer.empty(),
                        callStatus: RustCallStatus(code: statusCode, errorBuf: errorBuf)
                    )
                )
            }
            let uniffiForeignFuture = uniffiTraitInterfaceCallAsyncWithError(
                makeCall: makeCall,
                handleSuccess: uniffiHandleSuccess,
                handleError: uniffiHandleError,
                lowerError: FfiConverterTypeSdkFfiError_lower
            )
            uniffiOutReturn.pointee = uniffiForeignFuture
        },
        uniffiFree: { (uniffiHandle: UInt64) -> () in
            let result = try? FfiConverterTypeTransactionSignerFn.handleMap.remove(handle: uniffiHandle)
            if result == nil {
                print("Uniffi callback interface TransactionSignerFn: handle missing in uniffiFree")
            }
        }
    )]
}

private func uniffiCallbackInitTransactionSignerFn() {
    uniffi_iota_sdk_ffi_fn_init_callback_vtable_transactionsignerfn(UniffiCallbackInterfaceTransactionSignerFn.vtable)
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeTransactionSignerFn: FfiConverter {
    fileprivate static let handleMap = UniffiHandleMap<TransactionSignerFn>()

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = TransactionSignerFn

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> TransactionSignerFn {
        return TransactionSignerFnImpl(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: TransactionSignerFn) -> UnsafeMutableRawPointer {
        guard let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: handleMap.insert(obj: value))) else {
            fatalError("Cast to UnsafeMutableRawPointer failed")
        }
        return ptr
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> TransactionSignerFn {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: TransactionSignerFn, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionSignerFn_lift(_ pointer: UnsafeMutableRawPointer) throws -> TransactionSignerFn {
    return try FfiConverterTypeTransactionSignerFn.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionSignerFn_lower(_ value: TransactionSignerFn) -> UnsafeMutableRawPointer {
    return FfiConverterTypeTransactionSignerFn.lower(value)
}






/**
 * A transaction
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * transaction = %x00 transaction-v1
 *
 * transaction-v1 = transaction-kind address gas-payment transaction-expiration
 * ```
 */
public protocol TransactionV1Protocol: AnyObject, Sendable {
    
    func digest()  -> Digest
    
    func expiration()  -> TransactionExpiration
    
    func gasPayment()  -> GasPayment
    
    func kind()  -> TransactionKind
    
    func sender()  -> Address
    
    /**
     * Get the signing digest.
     */
    func signingDigest()  -> Data
    
    /**
     * Get the signing digest as a hex string.
     */
    func signingDigestHex()  -> String
    
    /**
     * Serialize the transaction as a base64-encoded string.
     */
    func toBase64()  -> String
    
}
/**
 * A transaction
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * transaction = %x00 transaction-v1
 *
 * transaction-v1 = transaction-kind address gas-payment transaction-expiration
 * ```
 */
open class TransactionV1: TransactionV1Protocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_transactionv1(self.pointer, $0) }
    }
public convenience init(kind: TransactionKind, sender: Address, gasPayment: GasPayment, expiration: TransactionExpiration) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_transactionv1_new(
        FfiConverterTypeTransactionKind_lower(kind),
        FfiConverterTypeAddress_lower(sender),
        FfiConverterTypeGasPayment_lower(gasPayment),
        FfiConverterTypeTransactionExpiration_lower(expiration),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_transactionv1(pointer, $0) }
    }

    
    /**
     * Deserialize a transaction from a base64-encoded string.
     */
public static func fromBase64(bytes: String)throws  -> TransactionV1  {
    return try  FfiConverterTypeTransactionV1_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_transactionv1_from_base64(
        FfiConverterString.lower(bytes),$0
    )
})
}
    

    
open func digest() -> Digest  {
    return try!  FfiConverterTypeDigest_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactionv1_digest(self.uniffiClonePointer(),$0
    )
})
}
    
open func expiration() -> TransactionExpiration  {
    return try!  FfiConverterTypeTransactionExpiration_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactionv1_expiration(self.uniffiClonePointer(),$0
    )
})
}
    
open func gasPayment() -> GasPayment  {
    return try!  FfiConverterTypeGasPayment_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactionv1_gas_payment(self.uniffiClonePointer(),$0
    )
})
}
    
open func kind() -> TransactionKind  {
    return try!  FfiConverterTypeTransactionKind_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactionv1_kind(self.uniffiClonePointer(),$0
    )
})
}
    
open func sender() -> Address  {
    return try!  FfiConverterTypeAddress_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactionv1_sender(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Get the signing digest.
     */
open func signingDigest() -> Data  {
    return try!  FfiConverterData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactionv1_signing_digest(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Get the signing digest as a hex string.
     */
open func signingDigestHex() -> String  {
    return try!  FfiConverterString.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactionv1_signing_digest_hex(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Serialize the transaction as a base64-encoded string.
     */
open func toBase64() -> String  {
    return try!  FfiConverterString.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactionv1_to_base64(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactionv1_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: TransactionV1, other: TransactionV1) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transactionv1_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeTransactionV1_lower(other),$0
    )
}
        )
    }

}
extension TransactionV1: CustomDebugStringConvertible {}
extension TransactionV1: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeTransactionV1: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = TransactionV1

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> TransactionV1 {
        return TransactionV1(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: TransactionV1) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> TransactionV1 {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: TransactionV1, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionV1_lift(_ pointer: UnsafeMutableRawPointer) throws -> TransactionV1 {
    return try FfiConverterTypeTransactionV1.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionV1_lower(_ value: TransactionV1) -> UnsafeMutableRawPointer {
    return FfiConverterTypeTransactionV1.lower(value)
}






/**
 * Command to transfer ownership of a set of objects to an address
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * transfer-objects = (vector argument) argument
 * ```
 */
public protocol TransferObjectsProtocol: AnyObject, Sendable {
    
    /**
     * The address to transfer ownership to
     */
    func address()  -> Argument
    
    /**
     * Set of objects to transfer
     */
    func objects()  -> [Argument]
    
}
/**
 * Command to transfer ownership of a set of objects to an address
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * transfer-objects = (vector argument) argument
 * ```
 */
open class TransferObjects: TransferObjectsProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_transferobjects(self.pointer, $0) }
    }
public convenience init(objects: [Argument], address: Argument) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_transferobjects_new(
        FfiConverterSequenceTypeArgument.lower(objects),
        FfiConverterTypeArgument_lower(address),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_transferobjects(pointer, $0) }
    }

    

    
    /**
     * The address to transfer ownership to
     */
open func address() -> Argument  {
    return try!  FfiConverterTypeArgument_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transferobjects_address(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Set of objects to transfer
     */
open func objects() -> [Argument]  {
    return try!  FfiConverterSequenceTypeArgument.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transferobjects_objects(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transferobjects_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: TransferObjects, other: TransferObjects) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_transferobjects_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeTransferObjects_lower(other),$0
    )
}
        )
    }

}
extension TransferObjects: CustomDebugStringConvertible {}
extension TransferObjects: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeTransferObjects: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = TransferObjects

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> TransferObjects {
        return TransferObjects(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: TransferObjects) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> TransferObjects {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: TransferObjects, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransferObjects_lift(_ pointer: UnsafeMutableRawPointer) throws -> TransferObjects {
    return try FfiConverterTypeTransferObjects.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransferObjects_lower(_ value: TransferObjects) -> UnsafeMutableRawPointer {
    return FfiConverterTypeTransferObjects.lower(value)
}






/**
 * Type of a move value
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * type-tag = type-tag-u8 \
 * type-tag-u16 \
 * type-tag-u32 \
 * type-tag-u64 \
 * type-tag-u128 \
 * type-tag-u256 \
 * type-tag-bool \
 * type-tag-address \
 * type-tag-signer \
 * type-tag-vector \
 * type-tag-struct
 *
 * type-tag-u8 = %x01
 * type-tag-u16 = %x08
 * type-tag-u32 = %x09
 * type-tag-u64 = %x02
 * type-tag-u128 = %x03
 * type-tag-u256 = %x0a
 * type-tag-bool = %x00
 * type-tag-address = %x04
 * type-tag-signer = %x05
 * type-tag-vector = %x06 type-tag
 * type-tag-struct = %x07 struct-tag
 * ```
 */
public protocol TypeTagProtocol: AnyObject, Sendable {
    
    func asStructTag()  -> StructTag
    
    func asStructTagOpt()  -> StructTag?
    
    func asVectorTypeTag()  -> TypeTag
    
    func asVectorTypeTagOpt()  -> TypeTag?
    
    func isAddress()  -> Bool
    
    func isBool()  -> Bool
    
    func isSigner()  -> Bool
    
    func isStruct()  -> Bool
    
    func isU128()  -> Bool
    
    func isU16()  -> Bool
    
    func isU256()  -> Bool
    
    func isU32()  -> Bool
    
    func isU64()  -> Bool
    
    func isU8()  -> Bool
    
    func isVector()  -> Bool
    
    /**
     * Returns the string representation of this type tag using the
     * canonical display, with or without a `0x` prefix.
     */
    func toCanonicalString(withPrefix: Bool)  -> String
    
}
/**
 * Type of a move value
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * type-tag = type-tag-u8 \
 * type-tag-u16 \
 * type-tag-u32 \
 * type-tag-u64 \
 * type-tag-u128 \
 * type-tag-u256 \
 * type-tag-bool \
 * type-tag-address \
 * type-tag-signer \
 * type-tag-vector \
 * type-tag-struct
 *
 * type-tag-u8 = %x01
 * type-tag-u16 = %x08
 * type-tag-u32 = %x09
 * type-tag-u64 = %x02
 * type-tag-u128 = %x03
 * type-tag-u256 = %x0a
 * type-tag-bool = %x00
 * type-tag-address = %x04
 * type-tag-signer = %x05
 * type-tag-vector = %x06 type-tag
 * type-tag-struct = %x07 struct-tag
 * ```
 */
open class TypeTag: TypeTagProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_typetag(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_typetag(pointer, $0) }
    }

    
public static func newAddress() -> TypeTag  {
    return try!  FfiConverterTypeTypeTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_typetag_new_address($0
    )
})
}
    
public static func newBool() -> TypeTag  {
    return try!  FfiConverterTypeTypeTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_typetag_new_bool($0
    )
})
}
    
public static func newSigner() -> TypeTag  {
    return try!  FfiConverterTypeTypeTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_typetag_new_signer($0
    )
})
}
    
public static func newStruct(structTag: StructTag) -> TypeTag  {
    return try!  FfiConverterTypeTypeTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_typetag_new_struct(
        FfiConverterTypeStructTag_lower(structTag),$0
    )
})
}
    
public static func newU128() -> TypeTag  {
    return try!  FfiConverterTypeTypeTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_typetag_new_u128($0
    )
})
}
    
public static func newU16() -> TypeTag  {
    return try!  FfiConverterTypeTypeTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_typetag_new_u16($0
    )
})
}
    
public static func newU256() -> TypeTag  {
    return try!  FfiConverterTypeTypeTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_typetag_new_u256($0
    )
})
}
    
public static func newU32() -> TypeTag  {
    return try!  FfiConverterTypeTypeTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_typetag_new_u32($0
    )
})
}
    
public static func newU64() -> TypeTag  {
    return try!  FfiConverterTypeTypeTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_typetag_new_u64($0
    )
})
}
    
public static func newU8() -> TypeTag  {
    return try!  FfiConverterTypeTypeTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_typetag_new_u8($0
    )
})
}
    
public static func newVector(typeTag: TypeTag) -> TypeTag  {
    return try!  FfiConverterTypeTypeTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_typetag_new_vector(
        FfiConverterTypeTypeTag_lower(typeTag),$0
    )
})
}
    

    
open func asStructTag() -> StructTag  {
    return try!  FfiConverterTypeStructTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_typetag_as_struct_tag(self.uniffiClonePointer(),$0
    )
})
}
    
open func asStructTagOpt() -> StructTag?  {
    return try!  FfiConverterOptionTypeStructTag.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_typetag_as_struct_tag_opt(self.uniffiClonePointer(),$0
    )
})
}
    
open func asVectorTypeTag() -> TypeTag  {
    return try!  FfiConverterTypeTypeTag_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_typetag_as_vector_type_tag(self.uniffiClonePointer(),$0
    )
})
}
    
open func asVectorTypeTagOpt() -> TypeTag?  {
    return try!  FfiConverterOptionTypeTypeTag.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_typetag_as_vector_type_tag_opt(self.uniffiClonePointer(),$0
    )
})
}
    
open func isAddress() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_typetag_is_address(self.uniffiClonePointer(),$0
    )
})
}
    
open func isBool() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_typetag_is_bool(self.uniffiClonePointer(),$0
    )
})
}
    
open func isSigner() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_typetag_is_signer(self.uniffiClonePointer(),$0
    )
})
}
    
open func isStruct() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_typetag_is_struct(self.uniffiClonePointer(),$0
    )
})
}
    
open func isU128() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_typetag_is_u128(self.uniffiClonePointer(),$0
    )
})
}
    
open func isU16() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_typetag_is_u16(self.uniffiClonePointer(),$0
    )
})
}
    
open func isU256() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_typetag_is_u256(self.uniffiClonePointer(),$0
    )
})
}
    
open func isU32() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_typetag_is_u32(self.uniffiClonePointer(),$0
    )
})
}
    
open func isU64() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_typetag_is_u64(self.uniffiClonePointer(),$0
    )
})
}
    
open func isU8() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_typetag_is_u8(self.uniffiClonePointer(),$0
    )
})
}
    
open func isVector() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_typetag_is_vector(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Returns the string representation of this type tag using the
     * canonical display, with or without a `0x` prefix.
     */
open func toCanonicalString(withPrefix: Bool) -> String  {
    return try!  FfiConverterString.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_typetag_to_canonical_string(self.uniffiClonePointer(),
        FfiConverterBool.lower(withPrefix),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_typetag_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    open var description: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_typetag_uniffi_trait_display(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: TypeTag, other: TypeTag) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_typetag_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeTypeTag_lower(other),$0
    )
}
        )
    }
    open func hash(into hasher: inout Hasher) {
        let val = try!  FfiConverterUInt64.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_typetag_uniffi_trait_hash(self.uniffiClonePointer(),$0
    )
}
        )
        hasher.combine(val)
    }

}
extension TypeTag: CustomDebugStringConvertible {}
extension TypeTag: CustomStringConvertible {}
extension TypeTag: Equatable {}
extension TypeTag: Hashable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeTypeTag: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = TypeTag

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> TypeTag {
        return TypeTag(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: TypeTag) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> TypeTag {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: TypeTag, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTypeTag_lift(_ pointer: UnsafeMutableRawPointer) throws -> TypeTag {
    return try FfiConverterTypeTypeTag.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTypeTag_lower(_ value: TypeTag) -> UnsafeMutableRawPointer {
    return FfiConverterTypeTypeTag.lower(value)
}






/**
 * Command to upgrade an already published package
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * upgrade = (vector bytes)        ; move modules
 * (vector object-id)    ; dependencies
 * object-id             ; package-id of the package
 * argument              ; upgrade ticket
 * ```
 */
public protocol UpgradeProtocol: AnyObject, Sendable {
    
    /**
     * Set of packages that the to-be published package depends on
     */
    func dependencies()  -> [ObjectId]
    
    /**
     * The serialized move modules
     */
    func modules()  -> [Data]
    
    /**
     * Package id of the package to upgrade
     */
    func package()  -> ObjectId
    
    /**
     * Ticket authorizing the upgrade
     */
    func ticket()  -> Argument
    
}
/**
 * Command to upgrade an already published package
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * upgrade = (vector bytes)        ; move modules
 * (vector object-id)    ; dependencies
 * object-id             ; package-id of the package
 * argument              ; upgrade ticket
 * ```
 */
open class Upgrade: UpgradeProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_upgrade(self.pointer, $0) }
    }
public convenience init(modules: [Data], dependencies: [ObjectId], package: ObjectId, ticket: Argument) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_upgrade_new(
        FfiConverterSequenceData.lower(modules),
        FfiConverterSequenceTypeObjectId.lower(dependencies),
        FfiConverterTypeObjectId_lower(package),
        FfiConverterTypeArgument_lower(ticket),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_upgrade(pointer, $0) }
    }

    

    
    /**
     * Set of packages that the to-be published package depends on
     */
open func dependencies() -> [ObjectId]  {
    return try!  FfiConverterSequenceTypeObjectId.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_upgrade_dependencies(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * The serialized move modules
     */
open func modules() -> [Data]  {
    return try!  FfiConverterSequenceData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_upgrade_modules(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Package id of the package to upgrade
     */
open func package() -> ObjectId  {
    return try!  FfiConverterTypeObjectId_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_upgrade_package(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Ticket authorizing the upgrade
     */
open func ticket() -> Argument  {
    return try!  FfiConverterTypeArgument_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_upgrade_ticket(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_upgrade_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: Upgrade, other: Upgrade) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_upgrade_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeUpgrade_lower(other),$0
    )
}
        )
    }

}
extension Upgrade: CustomDebugStringConvertible {}
extension Upgrade: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeUpgrade: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = Upgrade

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> Upgrade {
        return Upgrade(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: Upgrade) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Upgrade {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: Upgrade, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeUpgrade_lift(_ pointer: UnsafeMutableRawPointer) throws -> Upgrade {
    return try FfiConverterTypeUpgrade.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeUpgrade_lower(_ value: Upgrade) -> UnsafeMutableRawPointer {
    return FfiConverterTypeUpgrade.lower(value)
}






/**
 * Representation of upgrade policy constants in `iota::package`.
 */
public protocol UpgradePolicyProtocol: AnyObject, Sendable {
    
    /**
     * Returns the internal representation.
     */
    func asU8()  -> UInt8
    
}
/**
 * Representation of upgrade policy constants in `iota::package`.
 */
open class UpgradePolicy: UpgradePolicyProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_upgradepolicy(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_upgradepolicy(pointer, $0) }
    }

    
    /**
     * Allows adding new functionalities (e.g., new public functions or
     * structs) but restricts changes to existing functionalities.
     */
public static func additive() -> UpgradePolicy  {
    return try!  FfiConverterTypeUpgradePolicy_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_upgradepolicy_additive($0
    )
})
}
    
    /**
     * The least restrictive policy. Permits changes to all function
     * implementations, the removal of ability constraints on generic type
     * parameters in function signatures, and modifications to private,
     * public(friend), and entry function signatures. However, public function
     * signatures and existing types cannot be changed.
     */
public static func compatible() -> UpgradePolicy  {
    return try!  FfiConverterTypeUpgradePolicy_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_upgradepolicy_compatible($0
    )
})
}
    
    /**
     * Limits modifications to the package’s dependencies only.
     */
public static func depOnly() -> UpgradePolicy  {
    return try!  FfiConverterTypeUpgradePolicy_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_upgradepolicy_dep_only($0
    )
})
}
    

    
    /**
     * Returns the internal representation.
     */
open func asU8() -> UInt8  {
    return try!  FfiConverterUInt8.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_upgradepolicy_as_u8(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_upgradepolicy_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    open var description: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_upgradepolicy_uniffi_trait_display(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: UpgradePolicy, other: UpgradePolicy) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_upgradepolicy_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeUpgradePolicy_lower(other),$0
    )
}
        )
    }

}
extension UpgradePolicy: CustomDebugStringConvertible {}
extension UpgradePolicy: CustomStringConvertible {}
extension UpgradePolicy: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeUpgradePolicy: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = UpgradePolicy

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> UpgradePolicy {
        return UpgradePolicy(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: UpgradePolicy) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> UpgradePolicy {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: UpgradePolicy, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeUpgradePolicy_lift(_ pointer: UnsafeMutableRawPointer) throws -> UpgradePolicy {
    return try FfiConverterTypeUpgradePolicy.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeUpgradePolicy_lower(_ value: UpgradePolicy) -> UnsafeMutableRawPointer {
    return FfiConverterTypeUpgradePolicy.lower(value)
}






/**
 * A signature from a user
 *
 * A `UserSignature` is most commonly used to authorize the execution and
 * inclusion of a transaction to the blockchain.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * user-signature-bcs = bytes ; where the contents of the bytes are defined by <user-signature>
 * user-signature = simple-signature / multisig / multisig-legacy / zklogin / passkey / move-authenticator
 * ```
 *
 * Note: Due to historical reasons, signatures are serialized slightly
 * different from the majority of the types in IOTA. In particular if a
 * signature is ever embedded in another structure it generally is serialized
 * as `bytes` meaning it has a length prefix that defines the length of
 * the completely serialized signature.
 */
public protocol UserSignatureProtocol: AnyObject, Sendable {
    
    func asMoveAuthenticator()  -> MoveAuthenticator
    
    func asMoveAuthenticatorOpt()  -> MoveAuthenticator?
    
    func asMultisig()  -> MultisigAggregatedSignature
    
    func asMultisigOpt()  -> MultisigAggregatedSignature?
    
    func asPasskeyAuthenticator()  -> PasskeyAuthenticator
    
    func asPasskeyAuthenticatorOpt()  -> PasskeyAuthenticator?
    
    func asSimple()  -> SimpleSignature
    
    func asSimpleOpt()  -> SimpleSignature?
    
    func asZkloginAuthenticator()  -> ZkLoginAuthenticator
    
    func asZkloginAuthenticatorOpt()  -> ZkLoginAuthenticator?
    
    func isMoveAuthenticator()  -> Bool
    
    func isMultisig()  -> Bool
    
    func isPasskeyAuthenticator()  -> Bool
    
    func isSimple()  -> Bool
    
    func isZkloginAuthenticator()  -> Bool
    
    /**
     * Return the flag for this signature scheme
     */
    func scheme()  -> SignatureScheme
    
    func toBase64()  -> String
    
    func toBytes()  -> Data
    
}
/**
 * A signature from a user
 *
 * A `UserSignature` is most commonly used to authorize the execution and
 * inclusion of a transaction to the blockchain.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * user-signature-bcs = bytes ; where the contents of the bytes are defined by <user-signature>
 * user-signature = simple-signature / multisig / multisig-legacy / zklogin / passkey / move-authenticator
 * ```
 *
 * Note: Due to historical reasons, signatures are serialized slightly
 * different from the majority of the types in IOTA. In particular if a
 * signature is ever embedded in another structure it generally is serialized
 * as `bytes` meaning it has a length prefix that defines the length of
 * the completely serialized signature.
 */
open class UserSignature: UserSignatureProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_usersignature(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_usersignature(pointer, $0) }
    }

    
public static func fromBase64(base64: String)throws  -> UserSignature  {
    return try  FfiConverterTypeUserSignature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_usersignature_from_base64(
        FfiConverterString.lower(base64),$0
    )
})
}
    
public static func fromBytes(bytes: Data)throws  -> UserSignature  {
    return try  FfiConverterTypeUserSignature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_usersignature_from_bytes(
        FfiConverterData.lower(bytes),$0
    )
})
}
    
public static func newMoveAuthenticator(authenticator: MoveAuthenticator) -> UserSignature  {
    return try!  FfiConverterTypeUserSignature_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_usersignature_new_move_authenticator(
        FfiConverterTypeMoveAuthenticator_lower(authenticator),$0
    )
})
}
    
public static func newMultisig(signature: MultisigAggregatedSignature) -> UserSignature  {
    return try!  FfiConverterTypeUserSignature_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_usersignature_new_multisig(
        FfiConverterTypeMultisigAggregatedSignature_lower(signature),$0
    )
})
}
    
public static func newPasskeyAuthenticator(authenticator: PasskeyAuthenticator) -> UserSignature  {
    return try!  FfiConverterTypeUserSignature_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_usersignature_new_passkey_authenticator(
        FfiConverterTypePasskeyAuthenticator_lower(authenticator),$0
    )
})
}
    
public static func newSimple(signature: SimpleSignature) -> UserSignature  {
    return try!  FfiConverterTypeUserSignature_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_usersignature_new_simple(
        FfiConverterTypeSimpleSignature_lower(signature),$0
    )
})
}
    
public static func newZkloginAuthenticator(authenticator: ZkLoginAuthenticator) -> UserSignature  {
    return try!  FfiConverterTypeUserSignature_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_usersignature_new_zklogin_authenticator(
        FfiConverterTypeZkLoginAuthenticator_lower(authenticator),$0
    )
})
}
    

    
open func asMoveAuthenticator() -> MoveAuthenticator  {
    return try!  FfiConverterTypeMoveAuthenticator_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_usersignature_as_move_authenticator(self.uniffiClonePointer(),$0
    )
})
}
    
open func asMoveAuthenticatorOpt() -> MoveAuthenticator?  {
    return try!  FfiConverterOptionTypeMoveAuthenticator.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_usersignature_as_move_authenticator_opt(self.uniffiClonePointer(),$0
    )
})
}
    
open func asMultisig() -> MultisigAggregatedSignature  {
    return try!  FfiConverterTypeMultisigAggregatedSignature_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_usersignature_as_multisig(self.uniffiClonePointer(),$0
    )
})
}
    
open func asMultisigOpt() -> MultisigAggregatedSignature?  {
    return try!  FfiConverterOptionTypeMultisigAggregatedSignature.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_usersignature_as_multisig_opt(self.uniffiClonePointer(),$0
    )
})
}
    
open func asPasskeyAuthenticator() -> PasskeyAuthenticator  {
    return try!  FfiConverterTypePasskeyAuthenticator_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_usersignature_as_passkey_authenticator(self.uniffiClonePointer(),$0
    )
})
}
    
open func asPasskeyAuthenticatorOpt() -> PasskeyAuthenticator?  {
    return try!  FfiConverterOptionTypePasskeyAuthenticator.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_usersignature_as_passkey_authenticator_opt(self.uniffiClonePointer(),$0
    )
})
}
    
open func asSimple() -> SimpleSignature  {
    return try!  FfiConverterTypeSimpleSignature_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_usersignature_as_simple(self.uniffiClonePointer(),$0
    )
})
}
    
open func asSimpleOpt() -> SimpleSignature?  {
    return try!  FfiConverterOptionTypeSimpleSignature.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_usersignature_as_simple_opt(self.uniffiClonePointer(),$0
    )
})
}
    
open func asZkloginAuthenticator() -> ZkLoginAuthenticator  {
    return try!  FfiConverterTypeZkLoginAuthenticator_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_usersignature_as_zklogin_authenticator(self.uniffiClonePointer(),$0
    )
})
}
    
open func asZkloginAuthenticatorOpt() -> ZkLoginAuthenticator?  {
    return try!  FfiConverterOptionTypeZkLoginAuthenticator.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_usersignature_as_zklogin_authenticator_opt(self.uniffiClonePointer(),$0
    )
})
}
    
open func isMoveAuthenticator() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_usersignature_is_move_authenticator(self.uniffiClonePointer(),$0
    )
})
}
    
open func isMultisig() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_usersignature_is_multisig(self.uniffiClonePointer(),$0
    )
})
}
    
open func isPasskeyAuthenticator() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_usersignature_is_passkey_authenticator(self.uniffiClonePointer(),$0
    )
})
}
    
open func isSimple() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_usersignature_is_simple(self.uniffiClonePointer(),$0
    )
})
}
    
open func isZkloginAuthenticator() -> Bool  {
    return try!  FfiConverterBool.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_usersignature_is_zklogin_authenticator(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Return the flag for this signature scheme
     */
open func scheme() -> SignatureScheme  {
    return try!  FfiConverterTypeSignatureScheme_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_usersignature_scheme(self.uniffiClonePointer(),$0
    )
})
}
    
open func toBase64() -> String  {
    return try!  FfiConverterString.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_usersignature_to_base64(self.uniffiClonePointer(),$0
    )
})
}
    
open func toBytes() -> Data  {
    return try!  FfiConverterData.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_usersignature_to_bytes(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_usersignature_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: UserSignature, other: UserSignature) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_usersignature_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeUserSignature_lower(other),$0
    )
}
        )
    }

}
extension UserSignature: CustomDebugStringConvertible {}
extension UserSignature: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeUserSignature: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = UserSignature

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> UserSignature {
        return UserSignature(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: UserSignature) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> UserSignature {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: UserSignature, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeUserSignature_lift(_ pointer: UnsafeMutableRawPointer) throws -> UserSignature {
    return try FfiConverterTypeUserSignature.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeUserSignature_lower(_ value: UserSignature) -> UnsafeMutableRawPointer {
    return FfiConverterTypeUserSignature.lower(value)
}






/**
 * Verifier that will verify all UserSignature variants
 */
public protocol UserSignatureVerifierProtocol: AnyObject, Sendable {
    
    func verify(message: Data, signature: UserSignature) throws 
    
    func withZkloginVerifier(zkloginVerifier: ZkloginVerifier)  -> UserSignatureVerifier
    
    func zkloginVerifier()  -> ZkloginVerifier?
    
}
/**
 * Verifier that will verify all UserSignature variants
 */
open class UserSignatureVerifier: UserSignatureVerifierProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_usersignatureverifier(self.pointer, $0) }
    }
public convenience init() {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_usersignatureverifier_new($0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_usersignatureverifier(pointer, $0) }
    }

    

    
open func verify(message: Data, signature: UserSignature)throws   {try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_usersignatureverifier_verify(self.uniffiClonePointer(),
        FfiConverterData.lower(message),
        FfiConverterTypeUserSignature_lower(signature),$0
    )
}
}
    
open func withZkloginVerifier(zkloginVerifier: ZkloginVerifier) -> UserSignatureVerifier  {
    return try!  FfiConverterTypeUserSignatureVerifier_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_usersignatureverifier_with_zklogin_verifier(self.uniffiClonePointer(),
        FfiConverterTypeZkloginVerifier_lower(zkloginVerifier),$0
    )
})
}
    
open func zkloginVerifier() -> ZkloginVerifier?  {
    return try!  FfiConverterOptionTypeZkloginVerifier.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_usersignatureverifier_zklogin_verifier(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_usersignatureverifier_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }

}
extension UserSignatureVerifier: CustomDebugStringConvertible {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeUserSignatureVerifier: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = UserSignatureVerifier

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> UserSignatureVerifier {
        return UserSignatureVerifier(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: UserSignatureVerifier) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> UserSignatureVerifier {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: UserSignatureVerifier, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeUserSignatureVerifier_lift(_ pointer: UnsafeMutableRawPointer) throws -> UserSignatureVerifier {
    return try FfiConverterTypeUserSignatureVerifier.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeUserSignatureVerifier_lower(_ value: UserSignatureVerifier) -> UnsafeMutableRawPointer {
    return FfiConverterTypeUserSignatureVerifier.lower(value)
}






/**
 * An aggregated signature from multiple Validators.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * validator-aggregated-signature = u64               ; epoch
 * bls-signature
 * roaring-bitmap
 * roaring-bitmap = bytes  ; where the contents of the bytes are valid
 * ; according to the serialized spec for
 * ; roaring bitmaps
 * ```
 *
 * See <https://github.com/RoaringBitmap/RoaringFormatSpec> for the specification for the
 * serialized format of RoaringBitmaps.
 */
public protocol ValidatorAggregatedSignatureProtocol: AnyObject, Sendable {
    
    func bitmapBytes() throws  -> Data
    
    func epoch()  -> UInt64
    
    func signature()  -> Bls12381Signature
    
}
/**
 * An aggregated signature from multiple Validators.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * validator-aggregated-signature = u64               ; epoch
 * bls-signature
 * roaring-bitmap
 * roaring-bitmap = bytes  ; where the contents of the bytes are valid
 * ; according to the serialized spec for
 * ; roaring bitmaps
 * ```
 *
 * See <https://github.com/RoaringBitmap/RoaringFormatSpec> for the specification for the
 * serialized format of RoaringBitmaps.
 */
open class ValidatorAggregatedSignature: ValidatorAggregatedSignatureProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_validatoraggregatedsignature(self.pointer, $0) }
    }
public convenience init(epoch: UInt64, signature: Bls12381Signature, bitmapBytes: Data)throws  {
    let pointer =
        try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_validatoraggregatedsignature_new(
        FfiConverterUInt64.lower(epoch),
        FfiConverterTypeBls12381Signature_lower(signature),
        FfiConverterData.lower(bitmapBytes),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_validatoraggregatedsignature(pointer, $0) }
    }

    

    
open func bitmapBytes()throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_validatoraggregatedsignature_bitmap_bytes(self.uniffiClonePointer(),$0
    )
})
}
    
open func epoch() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_validatoraggregatedsignature_epoch(self.uniffiClonePointer(),$0
    )
})
}
    
open func signature() -> Bls12381Signature  {
    return try!  FfiConverterTypeBls12381Signature_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_validatoraggregatedsignature_signature(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_validatoraggregatedsignature_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }

}
extension ValidatorAggregatedSignature: CustomDebugStringConvertible {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeValidatorAggregatedSignature: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = ValidatorAggregatedSignature

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> ValidatorAggregatedSignature {
        return ValidatorAggregatedSignature(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: ValidatorAggregatedSignature) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ValidatorAggregatedSignature {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: ValidatorAggregatedSignature, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeValidatorAggregatedSignature_lift(_ pointer: UnsafeMutableRawPointer) throws -> ValidatorAggregatedSignature {
    return try FfiConverterTypeValidatorAggregatedSignature.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeValidatorAggregatedSignature_lower(_ value: ValidatorAggregatedSignature) -> UnsafeMutableRawPointer {
    return FfiConverterTypeValidatorAggregatedSignature.lower(value)
}






public protocol ValidatorCommitteeSignatureAggregatorProtocol: AnyObject, Sendable {
    
    func addSignature(signature: ValidatorSignature) throws 
    
    func committee()  -> ValidatorCommittee
    
    func finish() throws  -> ValidatorAggregatedSignature
    
}
open class ValidatorCommitteeSignatureAggregator: ValidatorCommitteeSignatureAggregatorProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_validatorcommitteesignatureaggregator(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_validatorcommitteesignatureaggregator(pointer, $0) }
    }

    
public static func newCheckpointSummary(committee: ValidatorCommittee, summary: CheckpointSummary)throws  -> ValidatorCommitteeSignatureAggregator  {
    return try  FfiConverterTypeValidatorCommitteeSignatureAggregator_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_validatorcommitteesignatureaggregator_new_checkpoint_summary(
        FfiConverterTypeValidatorCommittee_lower(committee),
        FfiConverterTypeCheckpointSummary_lower(summary),$0
    )
})
}
    

    
open func addSignature(signature: ValidatorSignature)throws   {try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_validatorcommitteesignatureaggregator_add_signature(self.uniffiClonePointer(),
        FfiConverterTypeValidatorSignature_lower(signature),$0
    )
}
}
    
open func committee() -> ValidatorCommittee  {
    return try!  FfiConverterTypeValidatorCommittee_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_validatorcommitteesignatureaggregator_committee(self.uniffiClonePointer(),$0
    )
})
}
    
open func finish()throws  -> ValidatorAggregatedSignature  {
    return try  FfiConverterTypeValidatorAggregatedSignature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_validatorcommitteesignatureaggregator_finish(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_validatorcommitteesignatureaggregator_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }

}
extension ValidatorCommitteeSignatureAggregator: CustomDebugStringConvertible {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeValidatorCommitteeSignatureAggregator: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = ValidatorCommitteeSignatureAggregator

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> ValidatorCommitteeSignatureAggregator {
        return ValidatorCommitteeSignatureAggregator(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: ValidatorCommitteeSignatureAggregator) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ValidatorCommitteeSignatureAggregator {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: ValidatorCommitteeSignatureAggregator, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeValidatorCommitteeSignatureAggregator_lift(_ pointer: UnsafeMutableRawPointer) throws -> ValidatorCommitteeSignatureAggregator {
    return try FfiConverterTypeValidatorCommitteeSignatureAggregator.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeValidatorCommitteeSignatureAggregator_lower(_ value: ValidatorCommitteeSignatureAggregator) -> UnsafeMutableRawPointer {
    return FfiConverterTypeValidatorCommitteeSignatureAggregator.lower(value)
}






public protocol ValidatorCommitteeSignatureVerifierProtocol: AnyObject, Sendable {
    
    func committee()  -> ValidatorCommittee
    
    func verify(message: Data, signature: ValidatorSignature) throws 
    
    func verifyAggregated(message: Data, signature: ValidatorAggregatedSignature) throws 
    
    func verifyCheckpointSummary(summary: CheckpointSummary, signature: ValidatorAggregatedSignature) throws 
    
}
open class ValidatorCommitteeSignatureVerifier: ValidatorCommitteeSignatureVerifierProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_validatorcommitteesignatureverifier(self.pointer, $0) }
    }
public convenience init(committee: ValidatorCommittee)throws  {
    let pointer =
        try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_validatorcommitteesignatureverifier_new(
        FfiConverterTypeValidatorCommittee_lower(committee),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_validatorcommitteesignatureverifier(pointer, $0) }
    }

    

    
open func committee() -> ValidatorCommittee  {
    return try!  FfiConverterTypeValidatorCommittee_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_validatorcommitteesignatureverifier_committee(self.uniffiClonePointer(),$0
    )
})
}
    
open func verify(message: Data, signature: ValidatorSignature)throws   {try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_validatorcommitteesignatureverifier_verify(self.uniffiClonePointer(),
        FfiConverterData.lower(message),
        FfiConverterTypeValidatorSignature_lower(signature),$0
    )
}
}
    
open func verifyAggregated(message: Data, signature: ValidatorAggregatedSignature)throws   {try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_validatorcommitteesignatureverifier_verify_aggregated(self.uniffiClonePointer(),
        FfiConverterData.lower(message),
        FfiConverterTypeValidatorAggregatedSignature_lower(signature),$0
    )
}
}
    
open func verifyCheckpointSummary(summary: CheckpointSummary, signature: ValidatorAggregatedSignature)throws   {try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_validatorcommitteesignatureverifier_verify_checkpoint_summary(self.uniffiClonePointer(),
        FfiConverterTypeCheckpointSummary_lower(summary),
        FfiConverterTypeValidatorAggregatedSignature_lower(signature),$0
    )
}
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_validatorcommitteesignatureverifier_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }

}
extension ValidatorCommitteeSignatureVerifier: CustomDebugStringConvertible {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeValidatorCommitteeSignatureVerifier: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = ValidatorCommitteeSignatureVerifier

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> ValidatorCommitteeSignatureVerifier {
        return ValidatorCommitteeSignatureVerifier(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: ValidatorCommitteeSignatureVerifier) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ValidatorCommitteeSignatureVerifier {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: ValidatorCommitteeSignatureVerifier, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeValidatorCommitteeSignatureVerifier_lift(_ pointer: UnsafeMutableRawPointer) throws -> ValidatorCommitteeSignatureVerifier {
    return try FfiConverterTypeValidatorCommitteeSignatureVerifier.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeValidatorCommitteeSignatureVerifier_lower(_ value: ValidatorCommitteeSignatureVerifier) -> UnsafeMutableRawPointer {
    return FfiConverterTypeValidatorCommitteeSignatureVerifier.lower(value)
}






/**
 * An execution time observation from a particular validator
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * execution-time-observation = bls-public-key duration
 * duration =  u64 ; seconds
 * u32 ; subsecond nanoseconds
 * ```
 */
public protocol ValidatorExecutionTimeObservationProtocol: AnyObject, Sendable {
    
    func duration()  -> TimeInterval
    
    func validator()  -> Bls12381PublicKey
    
}
/**
 * An execution time observation from a particular validator
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * execution-time-observation = bls-public-key duration
 * duration =  u64 ; seconds
 * u32 ; subsecond nanoseconds
 * ```
 */
open class ValidatorExecutionTimeObservation: ValidatorExecutionTimeObservationProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_validatorexecutiontimeobservation(self.pointer, $0) }
    }
public convenience init(validator: Bls12381PublicKey, duration: TimeInterval) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_validatorexecutiontimeobservation_new(
        FfiConverterTypeBls12381PublicKey_lower(validator),
        FfiConverterDuration.lower(duration),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_validatorexecutiontimeobservation(pointer, $0) }
    }

    

    
open func duration() -> TimeInterval  {
    return try!  FfiConverterDuration.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_validatorexecutiontimeobservation_duration(self.uniffiClonePointer(),$0
    )
})
}
    
open func validator() -> Bls12381PublicKey  {
    return try!  FfiConverterTypeBls12381PublicKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_validatorexecutiontimeobservation_validator(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_validatorexecutiontimeobservation_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: ValidatorExecutionTimeObservation, other: ValidatorExecutionTimeObservation) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_validatorexecutiontimeobservation_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeValidatorExecutionTimeObservation_lower(other),$0
    )
}
        )
    }
    open func hash(into hasher: inout Hasher) {
        let val = try!  FfiConverterUInt64.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_validatorexecutiontimeobservation_uniffi_trait_hash(self.uniffiClonePointer(),$0
    )
}
        )
        hasher.combine(val)
    }

}
extension ValidatorExecutionTimeObservation: CustomDebugStringConvertible {}
extension ValidatorExecutionTimeObservation: Equatable {}
extension ValidatorExecutionTimeObservation: Hashable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeValidatorExecutionTimeObservation: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = ValidatorExecutionTimeObservation

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> ValidatorExecutionTimeObservation {
        return ValidatorExecutionTimeObservation(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: ValidatorExecutionTimeObservation) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ValidatorExecutionTimeObservation {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: ValidatorExecutionTimeObservation, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeValidatorExecutionTimeObservation_lift(_ pointer: UnsafeMutableRawPointer) throws -> ValidatorExecutionTimeObservation {
    return try FfiConverterTypeValidatorExecutionTimeObservation.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeValidatorExecutionTimeObservation_lower(_ value: ValidatorExecutionTimeObservation) -> UnsafeMutableRawPointer {
    return FfiConverterTypeValidatorExecutionTimeObservation.lower(value)
}






/**
 * A signature from a Validator
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * validator-signature = u64               ; epoch
 * bls-public-key
 * bls-signature
 * ```
 */
public protocol ValidatorSignatureProtocol: AnyObject, Sendable {
    
    func epoch()  -> UInt64
    
    func publicKey()  -> Bls12381PublicKey
    
    func signature()  -> Bls12381Signature
    
}
/**
 * A signature from a Validator
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * validator-signature = u64               ; epoch
 * bls-public-key
 * bls-signature
 * ```
 */
open class ValidatorSignature: ValidatorSignatureProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_validatorsignature(self.pointer, $0) }
    }
public convenience init(epoch: UInt64, publicKey: Bls12381PublicKey, signature: Bls12381Signature) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_validatorsignature_new(
        FfiConverterUInt64.lower(epoch),
        FfiConverterTypeBls12381PublicKey_lower(publicKey),
        FfiConverterTypeBls12381Signature_lower(signature),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_validatorsignature(pointer, $0) }
    }

    

    
open func epoch() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_validatorsignature_epoch(self.uniffiClonePointer(),$0
    )
})
}
    
open func publicKey() -> Bls12381PublicKey  {
    return try!  FfiConverterTypeBls12381PublicKey_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_validatorsignature_public_key(self.uniffiClonePointer(),$0
    )
})
}
    
open func signature() -> Bls12381Signature  {
    return try!  FfiConverterTypeBls12381Signature_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_validatorsignature_signature(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_validatorsignature_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: ValidatorSignature, other: ValidatorSignature) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_validatorsignature_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeValidatorSignature_lower(other),$0
    )
}
        )
    }

}
extension ValidatorSignature: CustomDebugStringConvertible {}
extension ValidatorSignature: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeValidatorSignature: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = ValidatorSignature

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> ValidatorSignature {
        return ValidatorSignature(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: ValidatorSignature) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ValidatorSignature {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: ValidatorSignature, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeValidatorSignature_lift(_ pointer: UnsafeMutableRawPointer) throws -> ValidatorSignature {
    return try FfiConverterTypeValidatorSignature.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeValidatorSignature_lower(_ value: ValidatorSignature) -> UnsafeMutableRawPointer {
    return FfiConverterTypeValidatorSignature.lower(value)
}






/**
 * Object version assignment from consensus
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * version-assignment = object-id u64
 * ```
 */
public protocol VersionAssignmentProtocol: AnyObject, Sendable {
    
    func objectId()  -> ObjectId
    
    func version()  -> UInt64
    
}
/**
 * Object version assignment from consensus
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * version-assignment = object-id u64
 * ```
 */
open class VersionAssignment: VersionAssignmentProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_versionassignment(self.pointer, $0) }
    }
public convenience init(objectId: ObjectId, version: UInt64) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_versionassignment_new(
        FfiConverterTypeObjectId_lower(objectId),
        FfiConverterUInt64.lower(version),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_versionassignment(pointer, $0) }
    }

    

    
open func objectId() -> ObjectId  {
    return try!  FfiConverterTypeObjectId_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_versionassignment_object_id(self.uniffiClonePointer(),$0
    )
})
}
    
open func version() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_versionassignment_version(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_versionassignment_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: VersionAssignment, other: VersionAssignment) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_versionassignment_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeVersionAssignment_lower(other),$0
    )
}
        )
    }

}
extension VersionAssignment: CustomDebugStringConvertible {}
extension VersionAssignment: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeVersionAssignment: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = VersionAssignment

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> VersionAssignment {
        return VersionAssignment(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: VersionAssignment) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> VersionAssignment {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: VersionAssignment, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeVersionAssignment_lift(_ pointer: UnsafeMutableRawPointer) throws -> VersionAssignment {
    return try FfiConverterTypeVersionAssignment.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeVersionAssignment_lower(_ value: VersionAssignment) -> UnsafeMutableRawPointer {
    return FfiConverterTypeVersionAssignment.lower(value)
}






/**
 * A zklogin authenticator
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * zklogin-bcs = bytes             ; contents are defined by <zklogin-authenticator>
 * zklogin     = zklogin-flag
 * zklogin-inputs
 * u64               ; max epoch
 * simple-signature
 * ```
 *
 * Note: Due to historical reasons, signatures are serialized slightly
 * different from the majority of the types in IOTA. In particular if a
 * signature is ever embedded in another structure it generally is serialized
 * as `bytes` meaning it has a length prefix that defines the length of
 * the completely serialized signature.
 */
public protocol ZkLoginAuthenticatorProtocol: AnyObject, Sendable {
    
    func inputs()  -> ZkLoginInputs
    
    func maxEpoch()  -> UInt64
    
    func signature()  -> SimpleSignature
    
}
/**
 * A zklogin authenticator
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * zklogin-bcs = bytes             ; contents are defined by <zklogin-authenticator>
 * zklogin     = zklogin-flag
 * zklogin-inputs
 * u64               ; max epoch
 * simple-signature
 * ```
 *
 * Note: Due to historical reasons, signatures are serialized slightly
 * different from the majority of the types in IOTA. In particular if a
 * signature is ever embedded in another structure it generally is serialized
 * as `bytes` meaning it has a length prefix that defines the length of
 * the completely serialized signature.
 */
open class ZkLoginAuthenticator: ZkLoginAuthenticatorProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_zkloginauthenticator(self.pointer, $0) }
    }
public convenience init(inputs: ZkLoginInputs, maxEpoch: UInt64, signature: SimpleSignature) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_zkloginauthenticator_new(
        FfiConverterTypeZkLoginInputs_lower(inputs),
        FfiConverterUInt64.lower(maxEpoch),
        FfiConverterTypeSimpleSignature_lower(signature),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_zkloginauthenticator(pointer, $0) }
    }

    

    
open func inputs() -> ZkLoginInputs  {
    return try!  FfiConverterTypeZkLoginInputs_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_zkloginauthenticator_inputs(self.uniffiClonePointer(),$0
    )
})
}
    
open func maxEpoch() -> UInt64  {
    return try!  FfiConverterUInt64.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_zkloginauthenticator_max_epoch(self.uniffiClonePointer(),$0
    )
})
}
    
open func signature() -> SimpleSignature  {
    return try!  FfiConverterTypeSimpleSignature_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_zkloginauthenticator_signature(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_zkloginauthenticator_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: ZkLoginAuthenticator, other: ZkLoginAuthenticator) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_zkloginauthenticator_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeZkLoginAuthenticator_lower(other),$0
    )
}
        )
    }

}
extension ZkLoginAuthenticator: CustomDebugStringConvertible {}
extension ZkLoginAuthenticator: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeZkLoginAuthenticator: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = ZkLoginAuthenticator

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> ZkLoginAuthenticator {
        return ZkLoginAuthenticator(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: ZkLoginAuthenticator) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ZkLoginAuthenticator {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: ZkLoginAuthenticator, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeZkLoginAuthenticator_lift(_ pointer: UnsafeMutableRawPointer) throws -> ZkLoginAuthenticator {
    return try FfiConverterTypeZkLoginAuthenticator.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeZkLoginAuthenticator_lower(_ value: ZkLoginAuthenticator) -> UnsafeMutableRawPointer {
    return FfiConverterTypeZkLoginAuthenticator.lower(value)
}






/**
 * A zklogin groth16 proof and the required inputs to perform proof
 * verification.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * zklogin-inputs = zklogin-proof
 * zklogin-claim
 * string              ; base64url-unpadded encoded JwtHeader
 * bn254-field-element ; address_seed
 * ```
 */
public protocol ZkLoginInputsProtocol: AnyObject, Sendable {
    
    func addressSeed()  -> Bn254FieldElement
    
    func headerBase64()  -> String
    
    func iss()  -> String
    
    func issBase64Details()  -> ZkLoginClaim
    
    func jwkId()  -> JwkId
    
    func proofPoints()  -> ZkLoginProof
    
    func publicIdentifier()  -> ZkLoginPublicIdentifier
    
}
/**
 * A zklogin groth16 proof and the required inputs to perform proof
 * verification.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * zklogin-inputs = zklogin-proof
 * zklogin-claim
 * string              ; base64url-unpadded encoded JwtHeader
 * bn254-field-element ; address_seed
 * ```
 */
open class ZkLoginInputs: ZkLoginInputsProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_zklogininputs(self.pointer, $0) }
    }
public convenience init(proofPoints: ZkLoginProof, issBase64Details: ZkLoginClaim, headerBase64: String, addressSeed: Bn254FieldElement)throws  {
    let pointer =
        try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_zklogininputs_new(
        FfiConverterTypeZkLoginProof_lower(proofPoints),
        FfiConverterTypeZkLoginClaim_lower(issBase64Details),
        FfiConverterString.lower(headerBase64),
        FfiConverterTypeBn254FieldElement_lower(addressSeed),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_zklogininputs(pointer, $0) }
    }

    

    
open func addressSeed() -> Bn254FieldElement  {
    return try!  FfiConverterTypeBn254FieldElement_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_zklogininputs_address_seed(self.uniffiClonePointer(),$0
    )
})
}
    
open func headerBase64() -> String  {
    return try!  FfiConverterString.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_zklogininputs_header_base64(self.uniffiClonePointer(),$0
    )
})
}
    
open func iss() -> String  {
    return try!  FfiConverterString.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_zklogininputs_iss(self.uniffiClonePointer(),$0
    )
})
}
    
open func issBase64Details() -> ZkLoginClaim  {
    return try!  FfiConverterTypeZkLoginClaim_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_zklogininputs_iss_base64_details(self.uniffiClonePointer(),$0
    )
})
}
    
open func jwkId() -> JwkId  {
    return try!  FfiConverterTypeJwkId_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_zklogininputs_jwk_id(self.uniffiClonePointer(),$0
    )
})
}
    
open func proofPoints() -> ZkLoginProof  {
    return try!  FfiConverterTypeZkLoginProof_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_zklogininputs_proof_points(self.uniffiClonePointer(),$0
    )
})
}
    
open func publicIdentifier() -> ZkLoginPublicIdentifier  {
    return try!  FfiConverterTypeZkLoginPublicIdentifier_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_zklogininputs_public_identifier(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_zklogininputs_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: ZkLoginInputs, other: ZkLoginInputs) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_zklogininputs_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeZkLoginInputs_lower(other),$0
    )
}
        )
    }

}
extension ZkLoginInputs: CustomDebugStringConvertible {}
extension ZkLoginInputs: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeZkLoginInputs: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = ZkLoginInputs

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> ZkLoginInputs {
        return ZkLoginInputs(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: ZkLoginInputs) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ZkLoginInputs {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: ZkLoginInputs, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeZkLoginInputs_lift(_ pointer: UnsafeMutableRawPointer) throws -> ZkLoginInputs {
    return try FfiConverterTypeZkLoginInputs.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeZkLoginInputs_lower(_ value: ZkLoginInputs) -> UnsafeMutableRawPointer {
    return FfiConverterTypeZkLoginInputs.lower(value)
}






/**
 * A zklogin groth16 proof
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * zklogin-proof = circom-g1 circom-g2 circom-g1
 * ```
 */
public protocol ZkLoginProofProtocol: AnyObject, Sendable {
    
    func a()  -> CircomG1
    
    func b()  -> CircomG2
    
    func c()  -> CircomG1
    
}
/**
 * A zklogin groth16 proof
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * zklogin-proof = circom-g1 circom-g2 circom-g1
 * ```
 */
open class ZkLoginProof: ZkLoginProofProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_zkloginproof(self.pointer, $0) }
    }
public convenience init(a: CircomG1, b: CircomG2, c: CircomG1) {
    let pointer =
        try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_zkloginproof_new(
        FfiConverterTypeCircomG1_lower(a),
        FfiConverterTypeCircomG2_lower(b),
        FfiConverterTypeCircomG1_lower(c),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_zkloginproof(pointer, $0) }
    }

    

    
open func a() -> CircomG1  {
    return try!  FfiConverterTypeCircomG1_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_zkloginproof_a(self.uniffiClonePointer(),$0
    )
})
}
    
open func b() -> CircomG2  {
    return try!  FfiConverterTypeCircomG2_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_zkloginproof_b(self.uniffiClonePointer(),$0
    )
})
}
    
open func c() -> CircomG1  {
    return try!  FfiConverterTypeCircomG1_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_zkloginproof_c(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_zkloginproof_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: ZkLoginProof, other: ZkLoginProof) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_zkloginproof_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeZkLoginProof_lower(other),$0
    )
}
        )
    }

}
extension ZkLoginProof: CustomDebugStringConvertible {}
extension ZkLoginProof: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeZkLoginProof: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = ZkLoginProof

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> ZkLoginProof {
        return ZkLoginProof(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: ZkLoginProof) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ZkLoginProof {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: ZkLoginProof, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeZkLoginProof_lift(_ pointer: UnsafeMutableRawPointer) throws -> ZkLoginProof {
    return try FfiConverterTypeZkLoginProof.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeZkLoginProof_lower(_ value: ZkLoginProof) -> UnsafeMutableRawPointer {
    return FfiConverterTypeZkLoginProof.lower(value)
}






/**
 * Public Key equivalent for Zklogin authenticators
 *
 * A `ZkLoginPublicIdentifier` is the equivalent of a public key for other
 * account authenticators, and contains the information required to derive the
 * onchain account `Address` for a Zklogin authenticator.
 *
 * ## Note
 *
 * Due to a historical bug that was introduced in the IOTA Typescript SDK when
 * the zklogin authenticator was first introduced, there are now possibly two
 * "valid" addresses for each zklogin authenticator depending on the
 * bit-pattern of the `address_seed` value.
 *
 * The original bug incorrectly derived a zklogin's address by stripping any
 * leading zero-bytes that could have been present in the 32-byte length
 * `address_seed` value prior to hashing, leading to a different derived
 * address. This incorrectly derived address was presented to users of various
 * wallets, leading them to sending funds to these addresses that they couldn't
 * access. Instead of letting these users lose any assets that were sent to
 * these addresses, the IOTA network decided to change the protocol to allow
 * for a zklogin authenticator who's `address_seed` value had leading
 * zero-bytes be authorized to sign for both the addresses derived from both
 * the unpadded and padded `address_seed` value.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * zklogin-public-identifier-bcs = bytes ; where the contents are defined by
 * ; <zklogin-public-identifier>
 *
 * zklogin-public-identifier = zklogin-public-identifier-iss
 * address-seed
 *
 * zklogin-public-identifier-unpadded = zklogin-public-identifier-iss
 * address-seed-unpadded
 *
 * ; The iss, or issuer, is a utf8 string that is less than 255 bytes long
 * ; and is serialized with the iss's length in bytes as a u8 followed by
 * ; the bytes of the iss
 * zklogin-public-identifier-iss = u8 *255(OCTET)
 *
 * ; A Bn254FieldElement serialized as a 32-byte big-endian value
 * address-seed = 32(OCTET)
 *
 * ; A Bn254FieldElement serialized as a 32-byte big-endian value
 * ; with any leading zero bytes stripped
 * address-seed-unpadded = %x00 / %x01-ff *31(OCTET)
 * ```
 */
public protocol ZkLoginPublicIdentifierProtocol: AnyObject, Sendable {
    
    func addressSeed()  -> Bn254FieldElement
    
    /**
     * Provides an iterator over the addresses that correspond to this zklogin
     * authenticator.
     *
     * In the majority of instances this will only yield a single address,
     * except for the instances where the `address_seed` value has a
     * leading zero-byte, in such cases the returned iterator will yield
     * two addresses.
     */
    func deriveAddress()  -> [Address]
    
    /**
     * Derive an `Address` from this `ZkLoginPublicIdentifier` by hashing the
     * byte length of the `iss` followed by the `iss` bytes themselves and
     * the full 32 byte `address_seed` value, all prefixed with the zklogin
     * `SignatureScheme` flag (`0x05`).
     *
     * `hash( 0x05 || iss_bytes_len || iss_bytes || 32_byte_address_seed )`
     */
    func deriveAddressPadded()  -> Address
    
    /**
     * Derive an `Address` from this `ZkLoginPublicIdentifier` by hashing the
     * byte length of the `iss` followed by the `iss` bytes themselves and
     * the `address_seed` bytes with any leading zero-bytes stripped, all
     * prefixed with the zklogin `SignatureScheme` flag (`0x05`).
     *
     * `hash( 0x05 || iss_bytes_len || iss_bytes ||
     * unpadded_32_byte_address_seed )`
     */
    func deriveAddressUnpadded()  -> Address
    
    func iss()  -> String
    
}
/**
 * Public Key equivalent for Zklogin authenticators
 *
 * A `ZkLoginPublicIdentifier` is the equivalent of a public key for other
 * account authenticators, and contains the information required to derive the
 * onchain account `Address` for a Zklogin authenticator.
 *
 * ## Note
 *
 * Due to a historical bug that was introduced in the IOTA Typescript SDK when
 * the zklogin authenticator was first introduced, there are now possibly two
 * "valid" addresses for each zklogin authenticator depending on the
 * bit-pattern of the `address_seed` value.
 *
 * The original bug incorrectly derived a zklogin's address by stripping any
 * leading zero-bytes that could have been present in the 32-byte length
 * `address_seed` value prior to hashing, leading to a different derived
 * address. This incorrectly derived address was presented to users of various
 * wallets, leading them to sending funds to these addresses that they couldn't
 * access. Instead of letting these users lose any assets that were sent to
 * these addresses, the IOTA network decided to change the protocol to allow
 * for a zklogin authenticator who's `address_seed` value had leading
 * zero-bytes be authorized to sign for both the addresses derived from both
 * the unpadded and padded `address_seed` value.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * zklogin-public-identifier-bcs = bytes ; where the contents are defined by
 * ; <zklogin-public-identifier>
 *
 * zklogin-public-identifier = zklogin-public-identifier-iss
 * address-seed
 *
 * zklogin-public-identifier-unpadded = zklogin-public-identifier-iss
 * address-seed-unpadded
 *
 * ; The iss, or issuer, is a utf8 string that is less than 255 bytes long
 * ; and is serialized with the iss's length in bytes as a u8 followed by
 * ; the bytes of the iss
 * zklogin-public-identifier-iss = u8 *255(OCTET)
 *
 * ; A Bn254FieldElement serialized as a 32-byte big-endian value
 * address-seed = 32(OCTET)
 *
 * ; A Bn254FieldElement serialized as a 32-byte big-endian value
 * ; with any leading zero bytes stripped
 * address-seed-unpadded = %x00 / %x01-ff *31(OCTET)
 * ```
 */
open class ZkLoginPublicIdentifier: ZkLoginPublicIdentifierProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_zkloginpublicidentifier(self.pointer, $0) }
    }
public convenience init(iss: String, addressSeed: Bn254FieldElement)throws  {
    let pointer =
        try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_constructor_zkloginpublicidentifier_new(
        FfiConverterString.lower(iss),
        FfiConverterTypeBn254FieldElement_lower(addressSeed),$0
    )
}
    self.init(unsafeFromRawPointer: pointer)
}

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_zkloginpublicidentifier(pointer, $0) }
    }

    

    
open func addressSeed() -> Bn254FieldElement  {
    return try!  FfiConverterTypeBn254FieldElement_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_zkloginpublicidentifier_address_seed(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Provides an iterator over the addresses that correspond to this zklogin
     * authenticator.
     *
     * In the majority of instances this will only yield a single address,
     * except for the instances where the `address_seed` value has a
     * leading zero-byte, in such cases the returned iterator will yield
     * two addresses.
     */
open func deriveAddress() -> [Address]  {
    return try!  FfiConverterSequenceTypeAddress.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_zkloginpublicidentifier_derive_address(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Derive an `Address` from this `ZkLoginPublicIdentifier` by hashing the
     * byte length of the `iss` followed by the `iss` bytes themselves and
     * the full 32 byte `address_seed` value, all prefixed with the zklogin
     * `SignatureScheme` flag (`0x05`).
     *
     * `hash( 0x05 || iss_bytes_len || iss_bytes || 32_byte_address_seed )`
     */
open func deriveAddressPadded() -> Address  {
    return try!  FfiConverterTypeAddress_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_zkloginpublicidentifier_derive_address_padded(self.uniffiClonePointer(),$0
    )
})
}
    
    /**
     * Derive an `Address` from this `ZkLoginPublicIdentifier` by hashing the
     * byte length of the `iss` followed by the `iss` bytes themselves and
     * the `address_seed` bytes with any leading zero-bytes stripped, all
     * prefixed with the zklogin `SignatureScheme` flag (`0x05`).
     *
     * `hash( 0x05 || iss_bytes_len || iss_bytes ||
     * unpadded_32_byte_address_seed )`
     */
open func deriveAddressUnpadded() -> Address  {
    return try!  FfiConverterTypeAddress_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_zkloginpublicidentifier_derive_address_unpadded(self.uniffiClonePointer(),$0
    )
})
}
    
open func iss() -> String  {
    return try!  FfiConverterString.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_zkloginpublicidentifier_iss(self.uniffiClonePointer(),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_zkloginpublicidentifier_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }
    public static func == (self: ZkLoginPublicIdentifier, other: ZkLoginPublicIdentifier) -> Bool {
        return try!  FfiConverterBool.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_zkloginpublicidentifier_uniffi_trait_eq_eq(self.uniffiClonePointer(),
        FfiConverterTypeZkLoginPublicIdentifier_lower(other),$0
    )
}
        )
    }

}
extension ZkLoginPublicIdentifier: CustomDebugStringConvertible {}
extension ZkLoginPublicIdentifier: Equatable {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeZkLoginPublicIdentifier: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = ZkLoginPublicIdentifier

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> ZkLoginPublicIdentifier {
        return ZkLoginPublicIdentifier(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: ZkLoginPublicIdentifier) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ZkLoginPublicIdentifier {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: ZkLoginPublicIdentifier, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeZkLoginPublicIdentifier_lift(_ pointer: UnsafeMutableRawPointer) throws -> ZkLoginPublicIdentifier {
    return try FfiConverterTypeZkLoginPublicIdentifier.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeZkLoginPublicIdentifier_lower(_ value: ZkLoginPublicIdentifier) -> UnsafeMutableRawPointer {
    return FfiConverterTypeZkLoginPublicIdentifier.lower(value)
}






public protocol ZkloginVerifierProtocol: AnyObject, Sendable {
    
    func jwks()  -> [JwkId: Jwk]
    
    func verify(message: Data, authenticator: ZkLoginAuthenticator) throws 
    
    func withJwks(jwks: [JwkId: Jwk])  -> ZkloginVerifier
    
}
open class ZkloginVerifier: ZkloginVerifierProtocol, @unchecked Sendable {
    fileprivate let pointer: UnsafeMutableRawPointer!

    /// Used to instantiate a [FFIObject] without an actual pointer, for fakes in tests, mostly.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public struct NoPointer {
        public init() {}
    }

    // TODO: We'd like this to be `private` but for Swifty reasons,
    // we can't implement `FfiConverter` without making this `required` and we can't
    // make it `required` without making it `public`.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    required public init(unsafeFromRawPointer pointer: UnsafeMutableRawPointer) {
        self.pointer = pointer
    }

    // This constructor can be used to instantiate a fake object.
    // - Parameter noPointer: Placeholder value so we can have a constructor separate from the default empty one that may be implemented for classes extending [FFIObject].
    //
    // - Warning:
    //     Any object instantiated with this constructor cannot be passed to an actual Rust-backed object. Since there isn't a backing [Pointer] the FFI lower functions will crash.
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public init(noPointer: NoPointer) {
        self.pointer = nil
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public func uniffiClonePointer() -> UnsafeMutableRawPointer {
        return try! rustCall { uniffi_iota_sdk_ffi_fn_clone_zkloginverifier(self.pointer, $0) }
    }
    // No primary constructor declared for this class.

    deinit {
        guard let pointer = pointer else {
            return
        }

        try! rustCall { uniffi_iota_sdk_ffi_fn_free_zkloginverifier(pointer, $0) }
    }

    
    /**
     * Load a fixed verifying key from zkLogin.vkey output. This is based on a
     * local setup and should not be used in production.
     */
public static func newDev() -> ZkloginVerifier  {
    return try!  FfiConverterTypeZkloginVerifier_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_zkloginverifier_new_dev($0
    )
})
}
    
public static func newMainnet() -> ZkloginVerifier  {
    return try!  FfiConverterTypeZkloginVerifier_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_constructor_zkloginverifier_new_mainnet($0
    )
})
}
    

    
open func jwks() -> [JwkId: Jwk]  {
    return try!  FfiConverterDictionaryTypeJwkIdTypeJwk.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_zkloginverifier_jwks(self.uniffiClonePointer(),$0
    )
})
}
    
open func verify(message: Data, authenticator: ZkLoginAuthenticator)throws   {try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_method_zkloginverifier_verify(self.uniffiClonePointer(),
        FfiConverterData.lower(message),
        FfiConverterTypeZkLoginAuthenticator_lower(authenticator),$0
    )
}
}
    
open func withJwks(jwks: [JwkId: Jwk]) -> ZkloginVerifier  {
    return try!  FfiConverterTypeZkloginVerifier_lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_zkloginverifier_with_jwks(self.uniffiClonePointer(),
        FfiConverterDictionaryTypeJwkIdTypeJwk.lower(jwks),$0
    )
})
}
    
    open var debugDescription: String {
        return try!  FfiConverterString.lift(
            try! rustCall() {
    uniffi_iota_sdk_ffi_fn_method_zkloginverifier_uniffi_trait_debug(self.uniffiClonePointer(),$0
    )
}
        )
    }

}
extension ZkloginVerifier: CustomDebugStringConvertible {}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeZkloginVerifier: FfiConverter {

    typealias FfiType = UnsafeMutableRawPointer
    typealias SwiftType = ZkloginVerifier

    public static func lift(_ pointer: UnsafeMutableRawPointer) throws -> ZkloginVerifier {
        return ZkloginVerifier(unsafeFromRawPointer: pointer)
    }

    public static func lower(_ value: ZkloginVerifier) -> UnsafeMutableRawPointer {
        return value.uniffiClonePointer()
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ZkloginVerifier {
        let v: UInt64 = try readInt(&buf)
        // The Rust code won't compile if a pointer won't fit in a UInt64.
        // We have to go via `UInt` because that's the thing that's the size of a pointer.
        let ptr = UnsafeMutableRawPointer(bitPattern: UInt(truncatingIfNeeded: v))
        if (ptr == nil) {
            throw UniffiInternalError.unexpectedNullPointer
        }
        return try lift(ptr!)
    }

    public static func write(_ value: ZkloginVerifier, into buf: inout [UInt8]) {
        // This fiddling is because `Int` is the thing that's the same size as a pointer.
        // The Rust code won't compile if a pointer won't fit in a `UInt64`.
        writeInt(&buf, UInt64(bitPattern: Int64(Int(bitPattern: lower(value)))))
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeZkloginVerifier_lift(_ pointer: UnsafeMutableRawPointer) throws -> ZkloginVerifier {
    return try FfiConverterTypeZkloginVerifier.lift(pointer)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeZkloginVerifier_lower(_ value: ZkloginVerifier) -> UnsafeMutableRawPointer {
    return FfiConverterTypeZkloginVerifier.lower(value)
}




/**
 * A new Jwk
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * active-jwk = jwk-id jwk u64
 * ```
 */
public struct ActiveJwk {
    /**
     * Identifier used to uniquely identify a Jwk
     */
    public var jwkId: JwkId
    /**
     * The Jwk
     */
    public var jwk: Jwk
    /**
     * Most recent epoch in which the jwk was validated
     */
    public var epoch: UInt64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * Identifier used to uniquely identify a Jwk
         */jwkId: JwkId, 
        /**
         * The Jwk
         */jwk: Jwk, 
        /**
         * Most recent epoch in which the jwk was validated
         */epoch: UInt64) {
        self.jwkId = jwkId
        self.jwk = jwk
        self.epoch = epoch
    }
}

#if compiler(>=6)
extension ActiveJwk: Sendable {}
#endif


extension ActiveJwk: Equatable, Hashable {
    public static func ==(lhs: ActiveJwk, rhs: ActiveJwk) -> Bool {
        if lhs.jwkId != rhs.jwkId {
            return false
        }
        if lhs.jwk != rhs.jwk {
            return false
        }
        if lhs.epoch != rhs.epoch {
            return false
        }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(jwkId)
        hasher.combine(jwk)
        hasher.combine(epoch)
    }
}



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeActiveJwk: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ActiveJwk {
        return
            try ActiveJwk(
                jwkId: FfiConverterTypeJwkId.read(from: &buf), 
                jwk: FfiConverterTypeJwk.read(from: &buf), 
                epoch: FfiConverterUInt64.read(from: &buf)
        )
    }

    public static func write(_ value: ActiveJwk, into buf: inout [UInt8]) {
        FfiConverterTypeJwkId.write(value.jwkId, into: &buf)
        FfiConverterTypeJwk.write(value.jwk, into: &buf)
        FfiConverterUInt64.write(value.epoch, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeActiveJwk_lift(_ buf: RustBuffer) throws -> ActiveJwk {
    return try FfiConverterTypeActiveJwk.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeActiveJwk_lower(_ value: ActiveJwk) -> RustBuffer {
    return FfiConverterTypeActiveJwk.lower(value)
}


/**
 * Expire old JWKs
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * authenticator-state-expire = u64 u64
 * ```
 */
public struct AuthenticatorStateExpire {
    /**
     * Expire JWKs that have a lower epoch than this
     */
    public var minEpoch: UInt64
    /**
     * The initial version of the authenticator object that it was shared at.
     */
    public var authenticatorObjInitialSharedVersion: UInt64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * Expire JWKs that have a lower epoch than this
         */minEpoch: UInt64, 
        /**
         * The initial version of the authenticator object that it was shared at.
         */authenticatorObjInitialSharedVersion: UInt64) {
        self.minEpoch = minEpoch
        self.authenticatorObjInitialSharedVersion = authenticatorObjInitialSharedVersion
    }
}

#if compiler(>=6)
extension AuthenticatorStateExpire: Sendable {}
#endif


extension AuthenticatorStateExpire: Equatable, Hashable {
    public static func ==(lhs: AuthenticatorStateExpire, rhs: AuthenticatorStateExpire) -> Bool {
        if lhs.minEpoch != rhs.minEpoch {
            return false
        }
        if lhs.authenticatorObjInitialSharedVersion != rhs.authenticatorObjInitialSharedVersion {
            return false
        }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(minEpoch)
        hasher.combine(authenticatorObjInitialSharedVersion)
    }
}



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeAuthenticatorStateExpire: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> AuthenticatorStateExpire {
        return
            try AuthenticatorStateExpire(
                minEpoch: FfiConverterUInt64.read(from: &buf), 
                authenticatorObjInitialSharedVersion: FfiConverterUInt64.read(from: &buf)
        )
    }

    public static func write(_ value: AuthenticatorStateExpire, into buf: inout [UInt8]) {
        FfiConverterUInt64.write(value.minEpoch, into: &buf)
        FfiConverterUInt64.write(value.authenticatorObjInitialSharedVersion, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeAuthenticatorStateExpire_lift(_ buf: RustBuffer) throws -> AuthenticatorStateExpire {
    return try FfiConverterTypeAuthenticatorStateExpire.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeAuthenticatorStateExpire_lower(_ value: AuthenticatorStateExpire) -> RustBuffer {
    return FfiConverterTypeAuthenticatorStateExpire.lower(value)
}


/**
 * Update the set of valid JWKs
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * authenticator-state-update = u64 ; epoch
 * u64 ; round
 * (vector active-jwk)
 * u64 ; initial version of the authenticator object
 * ```
 */
public struct AuthenticatorStateUpdateV1 {
    /**
     * Epoch of the authenticator state update transaction
     */
    public var epoch: UInt64
    /**
     * Consensus round of the authenticator state update
     */
    public var round: UInt64
    /**
     * newly active jwks
     */
    public var newActiveJwks: [ActiveJwk]
    public var authenticatorObjInitialSharedVersion: UInt64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * Epoch of the authenticator state update transaction
         */epoch: UInt64, 
        /**
         * Consensus round of the authenticator state update
         */round: UInt64, 
        /**
         * newly active jwks
         */newActiveJwks: [ActiveJwk], authenticatorObjInitialSharedVersion: UInt64) {
        self.epoch = epoch
        self.round = round
        self.newActiveJwks = newActiveJwks
        self.authenticatorObjInitialSharedVersion = authenticatorObjInitialSharedVersion
    }
}

#if compiler(>=6)
extension AuthenticatorStateUpdateV1: Sendable {}
#endif


extension AuthenticatorStateUpdateV1: Equatable, Hashable {
    public static func ==(lhs: AuthenticatorStateUpdateV1, rhs: AuthenticatorStateUpdateV1) -> Bool {
        if lhs.epoch != rhs.epoch {
            return false
        }
        if lhs.round != rhs.round {
            return false
        }
        if lhs.newActiveJwks != rhs.newActiveJwks {
            return false
        }
        if lhs.authenticatorObjInitialSharedVersion != rhs.authenticatorObjInitialSharedVersion {
            return false
        }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(epoch)
        hasher.combine(round)
        hasher.combine(newActiveJwks)
        hasher.combine(authenticatorObjInitialSharedVersion)
    }
}



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeAuthenticatorStateUpdateV1: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> AuthenticatorStateUpdateV1 {
        return
            try AuthenticatorStateUpdateV1(
                epoch: FfiConverterUInt64.read(from: &buf), 
                round: FfiConverterUInt64.read(from: &buf), 
                newActiveJwks: FfiConverterSequenceTypeActiveJwk.read(from: &buf), 
                authenticatorObjInitialSharedVersion: FfiConverterUInt64.read(from: &buf)
        )
    }

    public static func write(_ value: AuthenticatorStateUpdateV1, into buf: inout [UInt8]) {
        FfiConverterUInt64.write(value.epoch, into: &buf)
        FfiConverterUInt64.write(value.round, into: &buf)
        FfiConverterSequenceTypeActiveJwk.write(value.newActiveJwks, into: &buf)
        FfiConverterUInt64.write(value.authenticatorObjInitialSharedVersion, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeAuthenticatorStateUpdateV1_lift(_ buf: RustBuffer) throws -> AuthenticatorStateUpdateV1 {
    return try FfiConverterTypeAuthenticatorStateUpdateV1.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeAuthenticatorStateUpdateV1_lower(_ value: AuthenticatorStateUpdateV1) -> RustBuffer {
    return FfiConverterTypeAuthenticatorStateUpdateV1.lower(value)
}


public struct BatchSendStatus {
    public var status: BatchSendStatusType
    public var transferredGasObjects: FaucetReceipt?

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(status: BatchSendStatusType, transferredGasObjects: FaucetReceipt?) {
        self.status = status
        self.transferredGasObjects = transferredGasObjects
    }
}

#if compiler(>=6)
extension BatchSendStatus: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeBatchSendStatus: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> BatchSendStatus {
        return
            try BatchSendStatus(
                status: FfiConverterTypeBatchSendStatusType.read(from: &buf), 
                transferredGasObjects: FfiConverterOptionTypeFaucetReceipt.read(from: &buf)
        )
    }

    public static func write(_ value: BatchSendStatus, into buf: inout [UInt8]) {
        FfiConverterTypeBatchSendStatusType.write(value.status, into: &buf)
        FfiConverterOptionTypeFaucetReceipt.write(value.transferredGasObjects, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeBatchSendStatus_lift(_ buf: RustBuffer) throws -> BatchSendStatus {
    return try FfiConverterTypeBatchSendStatus.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeBatchSendStatus_lower(_ value: BatchSendStatus) -> RustBuffer {
    return FfiConverterTypeBatchSendStatus.lower(value)
}


/**
 * Input/output state of an object that was changed during execution
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * changed-object = object-id object-in object-out id-operation
 * ```
 */
public struct ChangedObject {
    /**
     * Id of the object
     */
    public var objectId: ObjectId
    /**
     * State of the object in the store prior to this transaction.
     */
    public var inputState: ObjectIn
    /**
     * State of the object in the store after this transaction.
     */
    public var outputState: ObjectOut
    /**
     * Whether this object ID is created or deleted in this transaction.
     * This information isn't required by the protocol but is useful for
     * providing more detailed semantics on object changes.
     */
    public var idOperation: IdOperation

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * Id of the object
         */objectId: ObjectId, 
        /**
         * State of the object in the store prior to this transaction.
         */inputState: ObjectIn, 
        /**
         * State of the object in the store after this transaction.
         */outputState: ObjectOut, 
        /**
         * Whether this object ID is created or deleted in this transaction.
         * This information isn't required by the protocol but is useful for
         * providing more detailed semantics on object changes.
         */idOperation: IdOperation) {
        self.objectId = objectId
        self.inputState = inputState
        self.outputState = outputState
        self.idOperation = idOperation
    }
}

#if compiler(>=6)
extension ChangedObject: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeChangedObject: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ChangedObject {
        return
            try ChangedObject(
                objectId: FfiConverterTypeObjectId.read(from: &buf), 
                inputState: FfiConverterTypeObjectIn.read(from: &buf), 
                outputState: FfiConverterTypeObjectOut.read(from: &buf), 
                idOperation: FfiConverterTypeIdOperation.read(from: &buf)
        )
    }

    public static func write(_ value: ChangedObject, into buf: inout [UInt8]) {
        FfiConverterTypeObjectId.write(value.objectId, into: &buf)
        FfiConverterTypeObjectIn.write(value.inputState, into: &buf)
        FfiConverterTypeObjectOut.write(value.outputState, into: &buf)
        FfiConverterTypeIdOperation.write(value.idOperation, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeChangedObject_lift(_ buf: RustBuffer) throws -> ChangedObject {
    return try FfiConverterTypeChangedObject.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeChangedObject_lower(_ value: ChangedObject) -> RustBuffer {
    return FfiConverterTypeChangedObject.lower(value)
}


/**
 * A page of items returned by the GraphQL server.
 */
public struct CheckpointSummaryPage {
    /**
     * Information about the page, such as the cursor and whether there are
     * more pages.
     */
    public var pageInfo: PageInfo
    /**
     * The data returned by the server.
     */
    public var data: [CheckpointSummary]

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * Information about the page, such as the cursor and whether there are
         * more pages.
         */pageInfo: PageInfo, 
        /**
         * The data returned by the server.
         */data: [CheckpointSummary]) {
        self.pageInfo = pageInfo
        self.data = data
    }
}

#if compiler(>=6)
extension CheckpointSummaryPage: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeCheckpointSummaryPage: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> CheckpointSummaryPage {
        return
            try CheckpointSummaryPage(
                pageInfo: FfiConverterTypePageInfo.read(from: &buf), 
                data: FfiConverterSequenceTypeCheckpointSummary.read(from: &buf)
        )
    }

    public static func write(_ value: CheckpointSummaryPage, into buf: inout [UInt8]) {
        FfiConverterTypePageInfo.write(value.pageInfo, into: &buf)
        FfiConverterSequenceTypeCheckpointSummary.write(value.data, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeCheckpointSummaryPage_lift(_ buf: RustBuffer) throws -> CheckpointSummaryPage {
    return try FfiConverterTypeCheckpointSummaryPage.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeCheckpointSummaryPage_lower(_ value: CheckpointSummaryPage) -> RustBuffer {
    return FfiConverterTypeCheckpointSummaryPage.lower(value)
}


public struct CoinInfo {
    public var amount: UInt64
    public var id: ObjectId
    public var transferTxDigest: Digest

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(amount: UInt64, id: ObjectId, transferTxDigest: Digest) {
        self.amount = amount
        self.id = id
        self.transferTxDigest = transferTxDigest
    }
}

#if compiler(>=6)
extension CoinInfo: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeCoinInfo: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> CoinInfo {
        return
            try CoinInfo(
                amount: FfiConverterUInt64.read(from: &buf), 
                id: FfiConverterTypeObjectId.read(from: &buf), 
                transferTxDigest: FfiConverterTypeDigest.read(from: &buf)
        )
    }

    public static func write(_ value: CoinInfo, into buf: inout [UInt8]) {
        FfiConverterUInt64.write(value.amount, into: &buf)
        FfiConverterTypeObjectId.write(value.id, into: &buf)
        FfiConverterTypeDigest.write(value.transferTxDigest, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeCoinInfo_lift(_ buf: RustBuffer) throws -> CoinInfo {
    return try FfiConverterTypeCoinInfo.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeCoinInfo_lower(_ value: CoinInfo) -> RustBuffer {
    return FfiConverterTypeCoinInfo.lower(value)
}


/**
 * The coin metadata associated with the given coin type.
 */
public struct CoinMetadata {
    /**
     * The CoinMetadata object ID.
     */
    public var address: ObjectId
    /**
     * The number of decimal places used to represent the token.
     */
    public var decimals: Int32?
    /**
     * Optional description of the token, provided by the creator of the token.
     */
    public var description: String?
    /**
     * Icon URL of the coin.
     */
    public var iconUrl: String?
    /**
     * Full, official name of the token.
     */
    public var name: String?
    /**
     * The token's identifying abbreviation.
     */
    public var symbol: String?
    /**
     * The overall quantity of tokens that will be issued.
     */
    public var supply: BigInt?
    /**
     * Version of the token.
     */
    public var version: UInt64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * The CoinMetadata object ID.
         */address: ObjectId, 
        /**
         * The number of decimal places used to represent the token.
         */decimals: Int32? = nil, 
        /**
         * Optional description of the token, provided by the creator of the token.
         */description: String? = nil, 
        /**
         * Icon URL of the coin.
         */iconUrl: String? = nil, 
        /**
         * Full, official name of the token.
         */name: String? = nil, 
        /**
         * The token's identifying abbreviation.
         */symbol: String? = nil, 
        /**
         * The overall quantity of tokens that will be issued.
         */supply: BigInt? = nil, 
        /**
         * Version of the token.
         */version: UInt64) {
        self.address = address
        self.decimals = decimals
        self.description = description
        self.iconUrl = iconUrl
        self.name = name
        self.symbol = symbol
        self.supply = supply
        self.version = version
    }
}

#if compiler(>=6)
extension CoinMetadata: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeCoinMetadata: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> CoinMetadata {
        return
            try CoinMetadata(
                address: FfiConverterTypeObjectId.read(from: &buf), 
                decimals: FfiConverterOptionInt32.read(from: &buf), 
                description: FfiConverterOptionString.read(from: &buf), 
                iconUrl: FfiConverterOptionString.read(from: &buf), 
                name: FfiConverterOptionString.read(from: &buf), 
                symbol: FfiConverterOptionString.read(from: &buf), 
                supply: FfiConverterOptionTypeBigInt.read(from: &buf), 
                version: FfiConverterUInt64.read(from: &buf)
        )
    }

    public static func write(_ value: CoinMetadata, into buf: inout [UInt8]) {
        FfiConverterTypeObjectId.write(value.address, into: &buf)
        FfiConverterOptionInt32.write(value.decimals, into: &buf)
        FfiConverterOptionString.write(value.description, into: &buf)
        FfiConverterOptionString.write(value.iconUrl, into: &buf)
        FfiConverterOptionString.write(value.name, into: &buf)
        FfiConverterOptionString.write(value.symbol, into: &buf)
        FfiConverterOptionTypeBigInt.write(value.supply, into: &buf)
        FfiConverterUInt64.write(value.version, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeCoinMetadata_lift(_ buf: RustBuffer) throws -> CoinMetadata {
    return try FfiConverterTypeCoinMetadata.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeCoinMetadata_lower(_ value: CoinMetadata) -> RustBuffer {
    return FfiConverterTypeCoinMetadata.lower(value)
}


/**
 * A page of items returned by the GraphQL server.
 */
public struct CoinPage {
    /**
     * Information about the page, such as the cursor and whether there are
     * more pages.
     */
    public var pageInfo: PageInfo
    /**
     * The data returned by the server.
     */
    public var data: [Coin]

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * Information about the page, such as the cursor and whether there are
         * more pages.
         */pageInfo: PageInfo, 
        /**
         * The data returned by the server.
         */data: [Coin]) {
        self.pageInfo = pageInfo
        self.data = data
    }
}

#if compiler(>=6)
extension CoinPage: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeCoinPage: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> CoinPage {
        return
            try CoinPage(
                pageInfo: FfiConverterTypePageInfo.read(from: &buf), 
                data: FfiConverterSequenceTypeCoin.read(from: &buf)
        )
    }

    public static func write(_ value: CoinPage, into buf: inout [UInt8]) {
        FfiConverterTypePageInfo.write(value.pageInfo, into: &buf)
        FfiConverterSequenceTypeCoin.write(value.data, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeCoinPage_lift(_ buf: RustBuffer) throws -> CoinPage {
    return try FfiConverterTypeCoinPage.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeCoinPage_lower(_ value: CoinPage) -> RustBuffer {
    return FfiConverterTypeCoinPage.lower(value)
}


/**
 * Effects of a single command in the dry run, including mutated references
 * and return values.
 */
public struct DryRunEffect {
    /**
     * Changes made to arguments that were mutably borrowed by this command.
     */
    public var mutatedReferences: [DryRunMutation]
    /**
     * Return results of this command.
     */
    public var returnValues: [DryRunReturn]

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * Changes made to arguments that were mutably borrowed by this command.
         */mutatedReferences: [DryRunMutation], 
        /**
         * Return results of this command.
         */returnValues: [DryRunReturn]) {
        self.mutatedReferences = mutatedReferences
        self.returnValues = returnValues
    }
}

#if compiler(>=6)
extension DryRunEffect: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeDryRunEffect: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> DryRunEffect {
        return
            try DryRunEffect(
                mutatedReferences: FfiConverterSequenceTypeDryRunMutation.read(from: &buf), 
                returnValues: FfiConverterSequenceTypeDryRunReturn.read(from: &buf)
        )
    }

    public static func write(_ value: DryRunEffect, into buf: inout [UInt8]) {
        FfiConverterSequenceTypeDryRunMutation.write(value.mutatedReferences, into: &buf)
        FfiConverterSequenceTypeDryRunReturn.write(value.returnValues, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeDryRunEffect_lift(_ buf: RustBuffer) throws -> DryRunEffect {
    return try FfiConverterTypeDryRunEffect.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeDryRunEffect_lower(_ value: DryRunEffect) -> RustBuffer {
    return FfiConverterTypeDryRunEffect.lower(value)
}


/**
 * A mutation to an argument that was mutably borrowed by a command.
 */
public struct DryRunMutation {
    /**
     * The transaction argument that was mutated.
     */
    public var input: TransactionArgument
    /**
     * The Move type of the mutated value.
     */
    public var typeTag: TypeTag
    /**
     * The BCS representation of the mutated value.
     */
    public var bcs: Data

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * The transaction argument that was mutated.
         */input: TransactionArgument, 
        /**
         * The Move type of the mutated value.
         */typeTag: TypeTag, 
        /**
         * The BCS representation of the mutated value.
         */bcs: Data) {
        self.input = input
        self.typeTag = typeTag
        self.bcs = bcs
    }
}

#if compiler(>=6)
extension DryRunMutation: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeDryRunMutation: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> DryRunMutation {
        return
            try DryRunMutation(
                input: FfiConverterTypeTransactionArgument.read(from: &buf), 
                typeTag: FfiConverterTypeTypeTag.read(from: &buf), 
                bcs: FfiConverterData.read(from: &buf)
        )
    }

    public static func write(_ value: DryRunMutation, into buf: inout [UInt8]) {
        FfiConverterTypeTransactionArgument.write(value.input, into: &buf)
        FfiConverterTypeTypeTag.write(value.typeTag, into: &buf)
        FfiConverterData.write(value.bcs, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeDryRunMutation_lift(_ buf: RustBuffer) throws -> DryRunMutation {
    return try FfiConverterTypeDryRunMutation.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeDryRunMutation_lower(_ value: DryRunMutation) -> RustBuffer {
    return FfiConverterTypeDryRunMutation.lower(value)
}


/**
 * The result of a simulation (dry run), which includes the effects of the
 * transaction, any errors that may have occurred, and intermediate results for
 * each command.
 */
public struct DryRunResult {
    /**
     * The error that occurred during dry run execution, if any.
     */
    public var error: String?
    /**
     * The intermediate results for each command of the dry run execution,
     * including contents of mutated references and return values.
     */
    public var results: [DryRunEffect]
    /**
     * The transaction block representing the dry run execution.
     */
    public var transaction: SignedTransaction?
    /**
     * The effects of the transaction execution.
     */
    public var effects: TransactionEffects?

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * The error that occurred during dry run execution, if any.
         */error: String?, 
        /**
         * The intermediate results for each command of the dry run execution,
         * including contents of mutated references and return values.
         */results: [DryRunEffect], 
        /**
         * The transaction block representing the dry run execution.
         */transaction: SignedTransaction?, 
        /**
         * The effects of the transaction execution.
         */effects: TransactionEffects?) {
        self.error = error
        self.results = results
        self.transaction = transaction
        self.effects = effects
    }
}

#if compiler(>=6)
extension DryRunResult: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeDryRunResult: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> DryRunResult {
        return
            try DryRunResult(
                error: FfiConverterOptionString.read(from: &buf), 
                results: FfiConverterSequenceTypeDryRunEffect.read(from: &buf), 
                transaction: FfiConverterOptionTypeSignedTransaction.read(from: &buf), 
                effects: FfiConverterOptionTypeTransactionEffects.read(from: &buf)
        )
    }

    public static func write(_ value: DryRunResult, into buf: inout [UInt8]) {
        FfiConverterOptionString.write(value.error, into: &buf)
        FfiConverterSequenceTypeDryRunEffect.write(value.results, into: &buf)
        FfiConverterOptionTypeSignedTransaction.write(value.transaction, into: &buf)
        FfiConverterOptionTypeTransactionEffects.write(value.effects, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeDryRunResult_lift(_ buf: RustBuffer) throws -> DryRunResult {
    return try FfiConverterTypeDryRunResult.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeDryRunResult_lower(_ value: DryRunResult) -> RustBuffer {
    return FfiConverterTypeDryRunResult.lower(value)
}


/**
 * A return value from a command in the dry run.
 */
public struct DryRunReturn {
    /**
     * The Move type of the return value.
     */
    public var typeTag: TypeTag
    /**
     * The BCS representation of the return value.
     */
    public var bcs: Data

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * The Move type of the return value.
         */typeTag: TypeTag, 
        /**
         * The BCS representation of the return value.
         */bcs: Data) {
        self.typeTag = typeTag
        self.bcs = bcs
    }
}

#if compiler(>=6)
extension DryRunReturn: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeDryRunReturn: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> DryRunReturn {
        return
            try DryRunReturn(
                typeTag: FfiConverterTypeTypeTag.read(from: &buf), 
                bcs: FfiConverterData.read(from: &buf)
        )
    }

    public static func write(_ value: DryRunReturn, into buf: inout [UInt8]) {
        FfiConverterTypeTypeTag.write(value.typeTag, into: &buf)
        FfiConverterData.write(value.bcs, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeDryRunReturn_lift(_ buf: RustBuffer) throws -> DryRunReturn {
    return try FfiConverterTypeDryRunReturn.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeDryRunReturn_lower(_ value: DryRunReturn) -> RustBuffer {
    return FfiConverterTypeDryRunReturn.lower(value)
}


/**
 * The name part of a dynamic field, including its type, bcs, and json
 * representation.
 */
public struct DynamicFieldName {
    /**
     * The type name of this dynamic field name
     */
    public var typeTag: TypeTag
    /**
     * The bcs bytes of this dynamic field name
     */
    public var bcs: Data
    /**
     * The json representation of the dynamic field name
     */
    public var json: Value?

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * The type name of this dynamic field name
         */typeTag: TypeTag, 
        /**
         * The bcs bytes of this dynamic field name
         */bcs: Data, 
        /**
         * The json representation of the dynamic field name
         */json: Value? = nil) {
        self.typeTag = typeTag
        self.bcs = bcs
        self.json = json
    }
}

#if compiler(>=6)
extension DynamicFieldName: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeDynamicFieldName: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> DynamicFieldName {
        return
            try DynamicFieldName(
                typeTag: FfiConverterTypeTypeTag.read(from: &buf), 
                bcs: FfiConverterData.read(from: &buf), 
                json: FfiConverterOptionTypeValue.read(from: &buf)
        )
    }

    public static func write(_ value: DynamicFieldName, into buf: inout [UInt8]) {
        FfiConverterTypeTypeTag.write(value.typeTag, into: &buf)
        FfiConverterData.write(value.bcs, into: &buf)
        FfiConverterOptionTypeValue.write(value.json, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeDynamicFieldName_lift(_ buf: RustBuffer) throws -> DynamicFieldName {
    return try FfiConverterTypeDynamicFieldName.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeDynamicFieldName_lower(_ value: DynamicFieldName) -> RustBuffer {
    return FfiConverterTypeDynamicFieldName.lower(value)
}


/**
 * The output of a dynamic field query, that includes the name, value, and
 * value's json representation.
 */
public struct DynamicFieldOutput {
    /**
     * The name of the dynamic field
     */
    public var name: DynamicFieldName
    /**
     * The dynamic field value typename and bcs
     */
    public var value: DynamicFieldValue?
    /**
     * The json representation of the dynamic field value object
     */
    public var valueAsJson: Value?

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * The name of the dynamic field
         */name: DynamicFieldName, 
        /**
         * The dynamic field value typename and bcs
         */value: DynamicFieldValue? = nil, 
        /**
         * The json representation of the dynamic field value object
         */valueAsJson: Value? = nil) {
        self.name = name
        self.value = value
        self.valueAsJson = valueAsJson
    }
}

#if compiler(>=6)
extension DynamicFieldOutput: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeDynamicFieldOutput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> DynamicFieldOutput {
        return
            try DynamicFieldOutput(
                name: FfiConverterTypeDynamicFieldName.read(from: &buf), 
                value: FfiConverterOptionTypeDynamicFieldValue.read(from: &buf), 
                valueAsJson: FfiConverterOptionTypeValue.read(from: &buf)
        )
    }

    public static func write(_ value: DynamicFieldOutput, into buf: inout [UInt8]) {
        FfiConverterTypeDynamicFieldName.write(value.name, into: &buf)
        FfiConverterOptionTypeDynamicFieldValue.write(value.value, into: &buf)
        FfiConverterOptionTypeValue.write(value.valueAsJson, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeDynamicFieldOutput_lift(_ buf: RustBuffer) throws -> DynamicFieldOutput {
    return try FfiConverterTypeDynamicFieldOutput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeDynamicFieldOutput_lower(_ value: DynamicFieldOutput) -> RustBuffer {
    return FfiConverterTypeDynamicFieldOutput.lower(value)
}


/**
 * A page of items returned by the GraphQL server.
 */
public struct DynamicFieldOutputPage {
    /**
     * Information about the page, such as the cursor and whether there are
     * more pages.
     */
    public var pageInfo: PageInfo
    /**
     * The data returned by the server.
     */
    public var data: [DynamicFieldOutput]

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * Information about the page, such as the cursor and whether there are
         * more pages.
         */pageInfo: PageInfo, 
        /**
         * The data returned by the server.
         */data: [DynamicFieldOutput]) {
        self.pageInfo = pageInfo
        self.data = data
    }
}

#if compiler(>=6)
extension DynamicFieldOutputPage: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeDynamicFieldOutputPage: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> DynamicFieldOutputPage {
        return
            try DynamicFieldOutputPage(
                pageInfo: FfiConverterTypePageInfo.read(from: &buf), 
                data: FfiConverterSequenceTypeDynamicFieldOutput.read(from: &buf)
        )
    }

    public static func write(_ value: DynamicFieldOutputPage, into buf: inout [UInt8]) {
        FfiConverterTypePageInfo.write(value.pageInfo, into: &buf)
        FfiConverterSequenceTypeDynamicFieldOutput.write(value.data, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeDynamicFieldOutputPage_lift(_ buf: RustBuffer) throws -> DynamicFieldOutputPage {
    return try FfiConverterTypeDynamicFieldOutputPage.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeDynamicFieldOutputPage_lower(_ value: DynamicFieldOutputPage) -> RustBuffer {
    return FfiConverterTypeDynamicFieldOutputPage.lower(value)
}


/**
 * The value part of a dynamic field.
 */
public struct DynamicFieldValue {
    public var typeTag: TypeTag
    public var bcs: Data

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(typeTag: TypeTag, bcs: Data) {
        self.typeTag = typeTag
        self.bcs = bcs
    }
}

#if compiler(>=6)
extension DynamicFieldValue: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeDynamicFieldValue: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> DynamicFieldValue {
        return
            try DynamicFieldValue(
                typeTag: FfiConverterTypeTypeTag.read(from: &buf), 
                bcs: FfiConverterData.read(from: &buf)
        )
    }

    public static func write(_ value: DynamicFieldValue, into buf: inout [UInt8]) {
        FfiConverterTypeTypeTag.write(value.typeTag, into: &buf)
        FfiConverterData.write(value.bcs, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeDynamicFieldValue_lift(_ buf: RustBuffer) throws -> DynamicFieldValue {
    return try FfiConverterTypeDynamicFieldValue.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeDynamicFieldValue_lower(_ value: DynamicFieldValue) -> RustBuffer {
    return FfiConverterTypeDynamicFieldValue.lower(value)
}


/**
 * Data which, when included in a [`CheckpointSummary`], signals the end of an
 * `Epoch`.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * end-of-epoch-data = (vector validator-committee-member) ; next_epoch_committee
 * u64                                 ; next_epoch_protocol_version
 * (vector checkpoint-commitment)      ; epoch_commitments
 * ```
 */
public struct EndOfEpochData {
    public var nextEpochCommittee: [ValidatorCommitteeMember]
    public var nextEpochProtocolVersion: UInt64
    public var epochCommitments: [CheckpointCommitment]
    public var epochSupplyChange: Int64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(nextEpochCommittee: [ValidatorCommitteeMember], nextEpochProtocolVersion: UInt64, epochCommitments: [CheckpointCommitment], epochSupplyChange: Int64) {
        self.nextEpochCommittee = nextEpochCommittee
        self.nextEpochProtocolVersion = nextEpochProtocolVersion
        self.epochCommitments = epochCommitments
        self.epochSupplyChange = epochSupplyChange
    }
}

#if compiler(>=6)
extension EndOfEpochData: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeEndOfEpochData: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> EndOfEpochData {
        return
            try EndOfEpochData(
                nextEpochCommittee: FfiConverterSequenceTypeValidatorCommitteeMember.read(from: &buf), 
                nextEpochProtocolVersion: FfiConverterUInt64.read(from: &buf), 
                epochCommitments: FfiConverterSequenceTypeCheckpointCommitment.read(from: &buf), 
                epochSupplyChange: FfiConverterInt64.read(from: &buf)
        )
    }

    public static func write(_ value: EndOfEpochData, into buf: inout [UInt8]) {
        FfiConverterSequenceTypeValidatorCommitteeMember.write(value.nextEpochCommittee, into: &buf)
        FfiConverterUInt64.write(value.nextEpochProtocolVersion, into: &buf)
        FfiConverterSequenceTypeCheckpointCommitment.write(value.epochCommitments, into: &buf)
        FfiConverterInt64.write(value.epochSupplyChange, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeEndOfEpochData_lift(_ buf: RustBuffer) throws -> EndOfEpochData {
    return try FfiConverterTypeEndOfEpochData.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeEndOfEpochData_lower(_ value: EndOfEpochData) -> RustBuffer {
    return FfiConverterTypeEndOfEpochData.lower(value)
}


public struct Epoch {
    /**
     * The epoch's id as a sequence number that starts at 0 and is incremented
     * by one at every epoch change.
     */
    public var epochId: UInt64
    /**
     * The storage fees paid for transactions executed during the epoch.
     */
    public var fundInflow: String?
    /**
     * The storage fee rebates paid to users who deleted the data associated
     * with past transactions.
     */
    public var fundOutflow: String?
    /**
     * The storage fund available in this epoch.
     * This fund is used to redistribute storage fees from past transactions
     * to future validators.
     */
    public var fundSize: String?
    /**
     * A commitment by the committee at the end of epoch on the contents of the
     * live object set at that time. This can be used to verify state
     * snapshots.
     */
    public var liveObjectSetDigest: String?
    /**
     * The difference between the fund inflow and outflow, representing
     * the net amount of storage fees accumulated in this epoch.
     */
    public var netInflow: String?
    /**
     * The epoch's corresponding protocol configuration, including the feature
     * flags and the configuration options.
     */
    public var protocolConfigs: ProtocolConfigs?
    /**
     * The minimum gas price that a quorum of validators are guaranteed to sign
     * a transaction for.
     */
    public var referenceGasPrice: String?
    /**
     * The epoch's starting timestamp.
     */
    public var startTimestamp: UInt64
    /**
     * The epoch's ending timestamp. Note that this is available only on epochs
     * that have ended.
     */
    public var endTimestamp: UInt64?
    /**
     * The value of the `version` field of `0x5`, the
     * `0x3::iota::IotaSystemState` object.  This version changes whenever
     * the fields contained in the system state object (held in a dynamic
     * field attached to `0x5`) change.
     */
    public var systemStateVersion: UInt64?
    /**
     * The total number of checkpoints in this epoch.
     */
    public var totalCheckpoints: UInt64?
    /**
     * The total amount of gas fees (in IOTA) that were paid in this epoch.
     */
    public var totalGasFees: String?
    /**
     * The total IOTA rewarded as stake.
     */
    public var totalStakeRewards: String?
    /**
     * The total number of transaction in this epoch.
     */
    public var totalTransactions: UInt64?
    /**
     * Validator related properties. For active validators, see
     * `active_validators` API.
     * For epochs other than the current the data provided refer to the start
     * of the epoch.
     */
    public var validatorSet: ValidatorSet?

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * The epoch's id as a sequence number that starts at 0 and is incremented
         * by one at every epoch change.
         */epochId: UInt64, 
        /**
         * The storage fees paid for transactions executed during the epoch.
         */fundInflow: String? = nil, 
        /**
         * The storage fee rebates paid to users who deleted the data associated
         * with past transactions.
         */fundOutflow: String? = nil, 
        /**
         * The storage fund available in this epoch.
         * This fund is used to redistribute storage fees from past transactions
         * to future validators.
         */fundSize: String? = nil, 
        /**
         * A commitment by the committee at the end of epoch on the contents of the
         * live object set at that time. This can be used to verify state
         * snapshots.
         */liveObjectSetDigest: String? = nil, 
        /**
         * The difference between the fund inflow and outflow, representing
         * the net amount of storage fees accumulated in this epoch.
         */netInflow: String? = nil, 
        /**
         * The epoch's corresponding protocol configuration, including the feature
         * flags and the configuration options.
         */protocolConfigs: ProtocolConfigs? = nil, 
        /**
         * The minimum gas price that a quorum of validators are guaranteed to sign
         * a transaction for.
         */referenceGasPrice: String? = nil, 
        /**
         * The epoch's starting timestamp.
         */startTimestamp: UInt64, 
        /**
         * The epoch's ending timestamp. Note that this is available only on epochs
         * that have ended.
         */endTimestamp: UInt64? = nil, 
        /**
         * The value of the `version` field of `0x5`, the
         * `0x3::iota::IotaSystemState` object.  This version changes whenever
         * the fields contained in the system state object (held in a dynamic
         * field attached to `0x5`) change.
         */systemStateVersion: UInt64? = nil, 
        /**
         * The total number of checkpoints in this epoch.
         */totalCheckpoints: UInt64? = nil, 
        /**
         * The total amount of gas fees (in IOTA) that were paid in this epoch.
         */totalGasFees: String? = nil, 
        /**
         * The total IOTA rewarded as stake.
         */totalStakeRewards: String? = nil, 
        /**
         * The total number of transaction in this epoch.
         */totalTransactions: UInt64? = nil, 
        /**
         * Validator related properties. For active validators, see
         * `active_validators` API.
         * For epochs other than the current the data provided refer to the start
         * of the epoch.
         */validatorSet: ValidatorSet? = nil) {
        self.epochId = epochId
        self.fundInflow = fundInflow
        self.fundOutflow = fundOutflow
        self.fundSize = fundSize
        self.liveObjectSetDigest = liveObjectSetDigest
        self.netInflow = netInflow
        self.protocolConfigs = protocolConfigs
        self.referenceGasPrice = referenceGasPrice
        self.startTimestamp = startTimestamp
        self.endTimestamp = endTimestamp
        self.systemStateVersion = systemStateVersion
        self.totalCheckpoints = totalCheckpoints
        self.totalGasFees = totalGasFees
        self.totalStakeRewards = totalStakeRewards
        self.totalTransactions = totalTransactions
        self.validatorSet = validatorSet
    }
}

#if compiler(>=6)
extension Epoch: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeEpoch: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Epoch {
        return
            try Epoch(
                epochId: FfiConverterUInt64.read(from: &buf), 
                fundInflow: FfiConverterOptionString.read(from: &buf), 
                fundOutflow: FfiConverterOptionString.read(from: &buf), 
                fundSize: FfiConverterOptionString.read(from: &buf), 
                liveObjectSetDigest: FfiConverterOptionString.read(from: &buf), 
                netInflow: FfiConverterOptionString.read(from: &buf), 
                protocolConfigs: FfiConverterOptionTypeProtocolConfigs.read(from: &buf), 
                referenceGasPrice: FfiConverterOptionString.read(from: &buf), 
                startTimestamp: FfiConverterUInt64.read(from: &buf), 
                endTimestamp: FfiConverterOptionUInt64.read(from: &buf), 
                systemStateVersion: FfiConverterOptionUInt64.read(from: &buf), 
                totalCheckpoints: FfiConverterOptionUInt64.read(from: &buf), 
                totalGasFees: FfiConverterOptionString.read(from: &buf), 
                totalStakeRewards: FfiConverterOptionString.read(from: &buf), 
                totalTransactions: FfiConverterOptionUInt64.read(from: &buf), 
                validatorSet: FfiConverterOptionTypeValidatorSet.read(from: &buf)
        )
    }

    public static func write(_ value: Epoch, into buf: inout [UInt8]) {
        FfiConverterUInt64.write(value.epochId, into: &buf)
        FfiConverterOptionString.write(value.fundInflow, into: &buf)
        FfiConverterOptionString.write(value.fundOutflow, into: &buf)
        FfiConverterOptionString.write(value.fundSize, into: &buf)
        FfiConverterOptionString.write(value.liveObjectSetDigest, into: &buf)
        FfiConverterOptionString.write(value.netInflow, into: &buf)
        FfiConverterOptionTypeProtocolConfigs.write(value.protocolConfigs, into: &buf)
        FfiConverterOptionString.write(value.referenceGasPrice, into: &buf)
        FfiConverterUInt64.write(value.startTimestamp, into: &buf)
        FfiConverterOptionUInt64.write(value.endTimestamp, into: &buf)
        FfiConverterOptionUInt64.write(value.systemStateVersion, into: &buf)
        FfiConverterOptionUInt64.write(value.totalCheckpoints, into: &buf)
        FfiConverterOptionString.write(value.totalGasFees, into: &buf)
        FfiConverterOptionString.write(value.totalStakeRewards, into: &buf)
        FfiConverterOptionUInt64.write(value.totalTransactions, into: &buf)
        FfiConverterOptionTypeValidatorSet.write(value.validatorSet, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeEpoch_lift(_ buf: RustBuffer) throws -> Epoch {
    return try FfiConverterTypeEpoch.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeEpoch_lower(_ value: Epoch) -> RustBuffer {
    return FfiConverterTypeEpoch.lower(value)
}


/**
 * A page of items returned by the GraphQL server.
 */
public struct EpochPage {
    /**
     * Information about the page, such as the cursor and whether there are
     * more pages.
     */
    public var pageInfo: PageInfo
    /**
     * The data returned by the server.
     */
    public var data: [Epoch]

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * Information about the page, such as the cursor and whether there are
         * more pages.
         */pageInfo: PageInfo, 
        /**
         * The data returned by the server.
         */data: [Epoch]) {
        self.pageInfo = pageInfo
        self.data = data
    }
}

#if compiler(>=6)
extension EpochPage: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeEpochPage: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> EpochPage {
        return
            try EpochPage(
                pageInfo: FfiConverterTypePageInfo.read(from: &buf), 
                data: FfiConverterSequenceTypeEpoch.read(from: &buf)
        )
    }

    public static func write(_ value: EpochPage, into buf: inout [UInt8]) {
        FfiConverterTypePageInfo.write(value.pageInfo, into: &buf)
        FfiConverterSequenceTypeEpoch.write(value.data, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeEpochPage_lift(_ buf: RustBuffer) throws -> EpochPage {
    return try FfiConverterTypeEpochPage.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeEpochPage_lower(_ value: EpochPage) -> RustBuffer {
    return FfiConverterTypeEpochPage.lower(value)
}


/**
 * An event
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * event = object-id identifier address struct-tag bytes
 * ```
 */
public struct Event {
    /**
     * Package id of the top-level function invoked by a MoveCall command which
     * triggered this event to be emitted.
     */
    public var packageId: ObjectId
    /**
     * Module name of the top-level function invoked by a MoveCall command
     * which triggered this event to be emitted.
     */
    public var module: String
    /**
     * Address of the account that sent the transaction where this event was
     * emitted.
     */
    public var sender: Address
    /**
     * The type of the event emitted
     */
    public var type: String
    /**
     * BCS serialized bytes of the event
     */
    public var contents: Data
    /**
     * UTC timestamp in milliseconds since epoch (1/1/1970)
     */
    public var timestamp: String
    /**
     * Structured contents of a Move value
     */
    public var data: String
    /**
     * Representation of a Move value in JSON
     */
    public var json: String

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * Package id of the top-level function invoked by a MoveCall command which
         * triggered this event to be emitted.
         */packageId: ObjectId, 
        /**
         * Module name of the top-level function invoked by a MoveCall command
         * which triggered this event to be emitted.
         */module: String, 
        /**
         * Address of the account that sent the transaction where this event was
         * emitted.
         */sender: Address, 
        /**
         * The type of the event emitted
         */type: String, 
        /**
         * BCS serialized bytes of the event
         */contents: Data, 
        /**
         * UTC timestamp in milliseconds since epoch (1/1/1970)
         */timestamp: String, 
        /**
         * Structured contents of a Move value
         */data: String, 
        /**
         * Representation of a Move value in JSON
         */json: String) {
        self.packageId = packageId
        self.module = module
        self.sender = sender
        self.type = type
        self.contents = contents
        self.timestamp = timestamp
        self.data = data
        self.json = json
    }
}

#if compiler(>=6)
extension Event: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeEvent: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Event {
        return
            try Event(
                packageId: FfiConverterTypeObjectId.read(from: &buf), 
                module: FfiConverterString.read(from: &buf), 
                sender: FfiConverterTypeAddress.read(from: &buf), 
                type: FfiConverterString.read(from: &buf), 
                contents: FfiConverterData.read(from: &buf), 
                timestamp: FfiConverterString.read(from: &buf), 
                data: FfiConverterString.read(from: &buf), 
                json: FfiConverterString.read(from: &buf)
        )
    }

    public static func write(_ value: Event, into buf: inout [UInt8]) {
        FfiConverterTypeObjectId.write(value.packageId, into: &buf)
        FfiConverterString.write(value.module, into: &buf)
        FfiConverterTypeAddress.write(value.sender, into: &buf)
        FfiConverterString.write(value.type, into: &buf)
        FfiConverterData.write(value.contents, into: &buf)
        FfiConverterString.write(value.timestamp, into: &buf)
        FfiConverterString.write(value.data, into: &buf)
        FfiConverterString.write(value.json, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeEvent_lift(_ buf: RustBuffer) throws -> Event {
    return try FfiConverterTypeEvent.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeEvent_lower(_ value: Event) -> RustBuffer {
    return FfiConverterTypeEvent.lower(value)
}


public struct EventFilter {
    public var emittingModule: String?
    public var eventType: String?
    public var sender: Address?
    public var transactionDigest: String?

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(emittingModule: String? = nil, eventType: String? = nil, sender: Address? = nil, transactionDigest: String? = nil) {
        self.emittingModule = emittingModule
        self.eventType = eventType
        self.sender = sender
        self.transactionDigest = transactionDigest
    }
}

#if compiler(>=6)
extension EventFilter: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeEventFilter: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> EventFilter {
        return
            try EventFilter(
                emittingModule: FfiConverterOptionString.read(from: &buf), 
                eventType: FfiConverterOptionString.read(from: &buf), 
                sender: FfiConverterOptionTypeAddress.read(from: &buf), 
                transactionDigest: FfiConverterOptionString.read(from: &buf)
        )
    }

    public static func write(_ value: EventFilter, into buf: inout [UInt8]) {
        FfiConverterOptionString.write(value.emittingModule, into: &buf)
        FfiConverterOptionString.write(value.eventType, into: &buf)
        FfiConverterOptionTypeAddress.write(value.sender, into: &buf)
        FfiConverterOptionString.write(value.transactionDigest, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeEventFilter_lift(_ buf: RustBuffer) throws -> EventFilter {
    return try FfiConverterTypeEventFilter.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeEventFilter_lower(_ value: EventFilter) -> RustBuffer {
    return FfiConverterTypeEventFilter.lower(value)
}


/**
 * A page of items returned by the GraphQL server.
 */
public struct EventPage {
    /**
     * Information about the page, such as the cursor and whether there are
     * more pages.
     */
    public var pageInfo: PageInfo
    /**
     * The data returned by the server.
     */
    public var data: [Event]

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * Information about the page, such as the cursor and whether there are
         * more pages.
         */pageInfo: PageInfo, 
        /**
         * The data returned by the server.
         */data: [Event]) {
        self.pageInfo = pageInfo
        self.data = data
    }
}

#if compiler(>=6)
extension EventPage: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeEventPage: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> EventPage {
        return
            try EventPage(
                pageInfo: FfiConverterTypePageInfo.read(from: &buf), 
                data: FfiConverterSequenceTypeEvent.read(from: &buf)
        )
    }

    public static func write(_ value: EventPage, into buf: inout [UInt8]) {
        FfiConverterTypePageInfo.write(value.pageInfo, into: &buf)
        FfiConverterSequenceTypeEvent.write(value.data, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeEventPage_lift(_ buf: RustBuffer) throws -> EventPage {
    return try FfiConverterTypeEventPage.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeEventPage_lower(_ value: EventPage) -> RustBuffer {
    return FfiConverterTypeEventPage.lower(value)
}


public struct FaucetReceipt {
    public var sent: [CoinInfo]

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(sent: [CoinInfo]) {
        self.sent = sent
    }
}

#if compiler(>=6)
extension FaucetReceipt: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeFaucetReceipt: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> FaucetReceipt {
        return
            try FaucetReceipt(
                sent: FfiConverterSequenceTypeCoinInfo.read(from: &buf)
        )
    }

    public static func write(_ value: FaucetReceipt, into buf: inout [UInt8]) {
        FfiConverterSequenceTypeCoinInfo.write(value.sent, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeFaucetReceipt_lift(_ buf: RustBuffer) throws -> FaucetReceipt {
    return try FfiConverterTypeFaucetReceipt.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeFaucetReceipt_lower(_ value: FaucetReceipt) -> RustBuffer {
    return FfiConverterTypeFaucetReceipt.lower(value)
}


public struct GqlAddress {
    public var address: Address

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(address: Address) {
        self.address = address
    }
}

#if compiler(>=6)
extension GqlAddress: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeGQLAddress: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> GqlAddress {
        return
            try GqlAddress(
                address: FfiConverterTypeAddress.read(from: &buf)
        )
    }

    public static func write(_ value: GqlAddress, into buf: inout [UInt8]) {
        FfiConverterTypeAddress.write(value.address, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeGQLAddress_lift(_ buf: RustBuffer) throws -> GqlAddress {
    return try FfiConverterTypeGQLAddress.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeGQLAddress_lower(_ value: GqlAddress) -> RustBuffer {
    return FfiConverterTypeGQLAddress.lower(value)
}


/**
 * Summary of gas charges.
 *
 * Storage is charged independently of computation.
 * There are 3 parts to the storage charges:
 * `storage_cost`: it is the charge of storage at the time the transaction is
 * executed.                 The cost of storage is the number of bytes of the
 * objects being mutated                 multiplied by a variable storage cost
 * per byte `storage_rebate`: this is the amount a user gets back when
 * manipulating an object.                   The `storage_rebate` is the
 * `storage_cost` for an object minus fees. `non_refundable_storage_fee`: not
 * all the value of the object storage cost is
 * given back to user and there is a small fraction that
 * is kept by the system. This value tracks that charge.
 *
 * When looking at a gas cost summary the amount charged to the user is
 * `computation_cost + storage_cost - storage_rebate`
 * and that is the amount that is deducted from the gas coins.
 * `non_refundable_storage_fee` is collected from the objects being
 * mutated/deleted and it is tracked by the system in storage funds.
 *
 * Objects deleted, including the older versions of objects mutated, have the
 * storage field on the objects added up to a pool of "potential rebate". This
 * rebate then is reduced by the "nonrefundable rate" such that:
 * `potential_rebate(storage cost of deleted/mutated objects) =
 * storage_rebate + non_refundable_storage_fee`
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * gas-cost-summary = u64 ; computation-cost
 * u64 ; storage-cost
 * u64 ; storage-rebate
 * u64 ; non-refundable-storage-fee
 * ```
 */
public struct GasCostSummary {
    /**
     * Cost of computation/execution
     */
    public var computationCost: UInt64
    /**
     * The burned component of the computation/execution costs
     */
    public var computationCostBurned: UInt64
    /**
     * Storage cost, it's the sum of all storage cost for all objects created
     * or mutated.
     */
    public var storageCost: UInt64
    /**
     * The amount of storage cost refunded to the user for all objects deleted
     * or mutated in the transaction.
     */
    public var storageRebate: UInt64
    /**
     * The fee for the rebate. The portion of the storage rebate kept by the
     * system.
     */
    public var nonRefundableStorageFee: UInt64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * Cost of computation/execution
         */computationCost: UInt64, 
        /**
         * The burned component of the computation/execution costs
         */computationCostBurned: UInt64, 
        /**
         * Storage cost, it's the sum of all storage cost for all objects created
         * or mutated.
         */storageCost: UInt64, 
        /**
         * The amount of storage cost refunded to the user for all objects deleted
         * or mutated in the transaction.
         */storageRebate: UInt64, 
        /**
         * The fee for the rebate. The portion of the storage rebate kept by the
         * system.
         */nonRefundableStorageFee: UInt64) {
        self.computationCost = computationCost
        self.computationCostBurned = computationCostBurned
        self.storageCost = storageCost
        self.storageRebate = storageRebate
        self.nonRefundableStorageFee = nonRefundableStorageFee
    }
}

#if compiler(>=6)
extension GasCostSummary: Sendable {}
#endif


extension GasCostSummary: Equatable, Hashable {
    public static func ==(lhs: GasCostSummary, rhs: GasCostSummary) -> Bool {
        if lhs.computationCost != rhs.computationCost {
            return false
        }
        if lhs.computationCostBurned != rhs.computationCostBurned {
            return false
        }
        if lhs.storageCost != rhs.storageCost {
            return false
        }
        if lhs.storageRebate != rhs.storageRebate {
            return false
        }
        if lhs.nonRefundableStorageFee != rhs.nonRefundableStorageFee {
            return false
        }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(computationCost)
        hasher.combine(computationCostBurned)
        hasher.combine(storageCost)
        hasher.combine(storageRebate)
        hasher.combine(nonRefundableStorageFee)
    }
}



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeGasCostSummary: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> GasCostSummary {
        return
            try GasCostSummary(
                computationCost: FfiConverterUInt64.read(from: &buf), 
                computationCostBurned: FfiConverterUInt64.read(from: &buf), 
                storageCost: FfiConverterUInt64.read(from: &buf), 
                storageRebate: FfiConverterUInt64.read(from: &buf), 
                nonRefundableStorageFee: FfiConverterUInt64.read(from: &buf)
        )
    }

    public static func write(_ value: GasCostSummary, into buf: inout [UInt8]) {
        FfiConverterUInt64.write(value.computationCost, into: &buf)
        FfiConverterUInt64.write(value.computationCostBurned, into: &buf)
        FfiConverterUInt64.write(value.storageCost, into: &buf)
        FfiConverterUInt64.write(value.storageRebate, into: &buf)
        FfiConverterUInt64.write(value.nonRefundableStorageFee, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeGasCostSummary_lift(_ buf: RustBuffer) throws -> GasCostSummary {
    return try FfiConverterTypeGasCostSummary.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeGasCostSummary_lower(_ value: GasCostSummary) -> RustBuffer {
    return FfiConverterTypeGasCostSummary.lower(value)
}


/**
 * Payment information for executing a transaction
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * gas-payment = (vector object-ref) ; gas coin objects
 * address             ; owner
 * u64                 ; price
 * u64                 ; budget
 * ```
 */
public struct GasPayment {
    public var objects: [ObjectReference]
    /**
     * Owner of the gas objects, either the transaction sender or a sponsor
     */
    public var owner: Address
    /**
     * Gas unit price to use when charging for computation
     *
     * Must be greater-than-or-equal-to the network's current RGP (reference
     * gas price)
     */
    public var price: UInt64
    /**
     * Total budget willing to spend for the execution of a transaction
     */
    public var budget: UInt64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(objects: [ObjectReference], 
        /**
         * Owner of the gas objects, either the transaction sender or a sponsor
         */owner: Address, 
        /**
         * Gas unit price to use when charging for computation
         *
         * Must be greater-than-or-equal-to the network's current RGP (reference
         * gas price)
         */price: UInt64, 
        /**
         * Total budget willing to spend for the execution of a transaction
         */budget: UInt64) {
        self.objects = objects
        self.owner = owner
        self.price = price
        self.budget = budget
    }
}

#if compiler(>=6)
extension GasPayment: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeGasPayment: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> GasPayment {
        return
            try GasPayment(
                objects: FfiConverterSequenceTypeObjectReference.read(from: &buf), 
                owner: FfiConverterTypeAddress.read(from: &buf), 
                price: FfiConverterUInt64.read(from: &buf), 
                budget: FfiConverterUInt64.read(from: &buf)
        )
    }

    public static func write(_ value: GasPayment, into buf: inout [UInt8]) {
        FfiConverterSequenceTypeObjectReference.write(value.objects, into: &buf)
        FfiConverterTypeAddress.write(value.owner, into: &buf)
        FfiConverterUInt64.write(value.price, into: &buf)
        FfiConverterUInt64.write(value.budget, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeGasPayment_lift(_ buf: RustBuffer) throws -> GasPayment {
    return try FfiConverterTypeGasPayment.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeGasPayment_lower(_ value: GasPayment) -> RustBuffer {
    return FfiConverterTypeGasPayment.lower(value)
}


/**
 * A JSON Web Key
 *
 * Struct that contains info for a JWK. A list of them for different kids can
 * be retrieved from the JWK endpoint (e.g. <https://www.googleapis.com/oauth2/v3/certs>).
 * The JWK is used to verify the JWT token.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * jwk = string string string string
 * ```
 */
public struct Jwk {
    /**
     * Key type parameter, <https://datatracker.ietf.org/doc/html/rfc7517#section-4.1>
     */
    public var kty: String
    /**
     * RSA public exponent, <https://datatracker.ietf.org/doc/html/rfc7517#section-9.3>
     */
    public var e: String
    /**
     * RSA modulus, <https://datatracker.ietf.org/doc/html/rfc7517#section-9.3>
     */
    public var n: String
    /**
     * Algorithm parameter, <https://datatracker.ietf.org/doc/html/rfc7517#section-4.4>
     */
    public var alg: String

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * Key type parameter, <https://datatracker.ietf.org/doc/html/rfc7517#section-4.1>
         */kty: String, 
        /**
         * RSA public exponent, <https://datatracker.ietf.org/doc/html/rfc7517#section-9.3>
         */e: String, 
        /**
         * RSA modulus, <https://datatracker.ietf.org/doc/html/rfc7517#section-9.3>
         */n: String, 
        /**
         * Algorithm parameter, <https://datatracker.ietf.org/doc/html/rfc7517#section-4.4>
         */alg: String) {
        self.kty = kty
        self.e = e
        self.n = n
        self.alg = alg
    }
}

#if compiler(>=6)
extension Jwk: Sendable {}
#endif


extension Jwk: Equatable, Hashable {
    public static func ==(lhs: Jwk, rhs: Jwk) -> Bool {
        if lhs.kty != rhs.kty {
            return false
        }
        if lhs.e != rhs.e {
            return false
        }
        if lhs.n != rhs.n {
            return false
        }
        if lhs.alg != rhs.alg {
            return false
        }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(kty)
        hasher.combine(e)
        hasher.combine(n)
        hasher.combine(alg)
    }
}



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeJwk: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Jwk {
        return
            try Jwk(
                kty: FfiConverterString.read(from: &buf), 
                e: FfiConverterString.read(from: &buf), 
                n: FfiConverterString.read(from: &buf), 
                alg: FfiConverterString.read(from: &buf)
        )
    }

    public static func write(_ value: Jwk, into buf: inout [UInt8]) {
        FfiConverterString.write(value.kty, into: &buf)
        FfiConverterString.write(value.e, into: &buf)
        FfiConverterString.write(value.n, into: &buf)
        FfiConverterString.write(value.alg, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeJwk_lift(_ buf: RustBuffer) throws -> Jwk {
    return try FfiConverterTypeJwk.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeJwk_lower(_ value: Jwk) -> RustBuffer {
    return FfiConverterTypeJwk.lower(value)
}


/**
 * Key to uniquely identify a JWK
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * jwk-id = string string
 * ```
 */
public struct JwkId {
    /**
     * The issuer or identity of the OIDC provider.
     */
    public var iss: String
    /**
     * A key id use to uniquely identify a key from an OIDC provider.
     */
    public var kid: String

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * The issuer or identity of the OIDC provider.
         */iss: String, 
        /**
         * A key id use to uniquely identify a key from an OIDC provider.
         */kid: String) {
        self.iss = iss
        self.kid = kid
    }
}

#if compiler(>=6)
extension JwkId: Sendable {}
#endif


extension JwkId: Equatable, Hashable {
    public static func ==(lhs: JwkId, rhs: JwkId) -> Bool {
        if lhs.iss != rhs.iss {
            return false
        }
        if lhs.kid != rhs.kid {
            return false
        }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(iss)
        hasher.combine(kid)
    }
}



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeJwkId: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> JwkId {
        return
            try JwkId(
                iss: FfiConverterString.read(from: &buf), 
                kid: FfiConverterString.read(from: &buf)
        )
    }

    public static func write(_ value: JwkId, into buf: inout [UInt8]) {
        FfiConverterString.write(value.iss, into: &buf)
        FfiConverterString.write(value.kid, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeJwkId_lift(_ buf: RustBuffer) throws -> JwkId {
    return try FfiConverterTypeJwkId.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeJwkId_lower(_ value: JwkId) -> RustBuffer {
    return FfiConverterTypeJwkId.lower(value)
}


public struct MoveEnum {
    public var abilities: [MoveAbility]?
    public var name: String
    public var typeParameters: [MoveStructTypeParameter]?
    public var variants: [MoveEnumVariant]?

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(abilities: [MoveAbility]? = nil, name: String, typeParameters: [MoveStructTypeParameter]? = nil, variants: [MoveEnumVariant]? = nil) {
        self.abilities = abilities
        self.name = name
        self.typeParameters = typeParameters
        self.variants = variants
    }
}

#if compiler(>=6)
extension MoveEnum: Sendable {}
#endif


extension MoveEnum: Equatable, Hashable {
    public static func ==(lhs: MoveEnum, rhs: MoveEnum) -> Bool {
        if lhs.abilities != rhs.abilities {
            return false
        }
        if lhs.name != rhs.name {
            return false
        }
        if lhs.typeParameters != rhs.typeParameters {
            return false
        }
        if lhs.variants != rhs.variants {
            return false
        }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(abilities)
        hasher.combine(name)
        hasher.combine(typeParameters)
        hasher.combine(variants)
    }
}



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMoveEnum: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MoveEnum {
        return
            try MoveEnum(
                abilities: FfiConverterOptionSequenceTypeMoveAbility.read(from: &buf), 
                name: FfiConverterString.read(from: &buf), 
                typeParameters: FfiConverterOptionSequenceTypeMoveStructTypeParameter.read(from: &buf), 
                variants: FfiConverterOptionSequenceTypeMoveEnumVariant.read(from: &buf)
        )
    }

    public static func write(_ value: MoveEnum, into buf: inout [UInt8]) {
        FfiConverterOptionSequenceTypeMoveAbility.write(value.abilities, into: &buf)
        FfiConverterString.write(value.name, into: &buf)
        FfiConverterOptionSequenceTypeMoveStructTypeParameter.write(value.typeParameters, into: &buf)
        FfiConverterOptionSequenceTypeMoveEnumVariant.write(value.variants, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveEnum_lift(_ buf: RustBuffer) throws -> MoveEnum {
    return try FfiConverterTypeMoveEnum.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveEnum_lower(_ value: MoveEnum) -> RustBuffer {
    return FfiConverterTypeMoveEnum.lower(value)
}


public struct MoveEnumConnection {
    public var nodes: [MoveEnum]
    public var pageInfo: PageInfo

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(nodes: [MoveEnum], pageInfo: PageInfo) {
        self.nodes = nodes
        self.pageInfo = pageInfo
    }
}

#if compiler(>=6)
extension MoveEnumConnection: Sendable {}
#endif


extension MoveEnumConnection: Equatable, Hashable {
    public static func ==(lhs: MoveEnumConnection, rhs: MoveEnumConnection) -> Bool {
        if lhs.nodes != rhs.nodes {
            return false
        }
        if lhs.pageInfo != rhs.pageInfo {
            return false
        }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(nodes)
        hasher.combine(pageInfo)
    }
}



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMoveEnumConnection: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MoveEnumConnection {
        return
            try MoveEnumConnection(
                nodes: FfiConverterSequenceTypeMoveEnum.read(from: &buf), 
                pageInfo: FfiConverterTypePageInfo.read(from: &buf)
        )
    }

    public static func write(_ value: MoveEnumConnection, into buf: inout [UInt8]) {
        FfiConverterSequenceTypeMoveEnum.write(value.nodes, into: &buf)
        FfiConverterTypePageInfo.write(value.pageInfo, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveEnumConnection_lift(_ buf: RustBuffer) throws -> MoveEnumConnection {
    return try FfiConverterTypeMoveEnumConnection.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveEnumConnection_lower(_ value: MoveEnumConnection) -> RustBuffer {
    return FfiConverterTypeMoveEnumConnection.lower(value)
}


public struct MoveEnumVariant {
    public var fields: [MoveField]?
    public var name: String

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(fields: [MoveField]? = nil, name: String) {
        self.fields = fields
        self.name = name
    }
}

#if compiler(>=6)
extension MoveEnumVariant: Sendable {}
#endif


extension MoveEnumVariant: Equatable, Hashable {
    public static func ==(lhs: MoveEnumVariant, rhs: MoveEnumVariant) -> Bool {
        if lhs.fields != rhs.fields {
            return false
        }
        if lhs.name != rhs.name {
            return false
        }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(fields)
        hasher.combine(name)
    }
}



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMoveEnumVariant: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MoveEnumVariant {
        return
            try MoveEnumVariant(
                fields: FfiConverterOptionSequenceTypeMoveField.read(from: &buf), 
                name: FfiConverterString.read(from: &buf)
        )
    }

    public static func write(_ value: MoveEnumVariant, into buf: inout [UInt8]) {
        FfiConverterOptionSequenceTypeMoveField.write(value.fields, into: &buf)
        FfiConverterString.write(value.name, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveEnumVariant_lift(_ buf: RustBuffer) throws -> MoveEnumVariant {
    return try FfiConverterTypeMoveEnumVariant.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveEnumVariant_lower(_ value: MoveEnumVariant) -> RustBuffer {
    return FfiConverterTypeMoveEnumVariant.lower(value)
}


public struct MoveField {
    public var name: String
    public var type: OpenMoveType?

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(name: String, type: OpenMoveType? = nil) {
        self.name = name
        self.type = type
    }
}

#if compiler(>=6)
extension MoveField: Sendable {}
#endif


extension MoveField: Equatable, Hashable {
    public static func ==(lhs: MoveField, rhs: MoveField) -> Bool {
        if lhs.name != rhs.name {
            return false
        }
        if lhs.type != rhs.type {
            return false
        }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(name)
        hasher.combine(type)
    }
}



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMoveField: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MoveField {
        return
            try MoveField(
                name: FfiConverterString.read(from: &buf), 
                type: FfiConverterOptionTypeOpenMoveType.read(from: &buf)
        )
    }

    public static func write(_ value: MoveField, into buf: inout [UInt8]) {
        FfiConverterString.write(value.name, into: &buf)
        FfiConverterOptionTypeOpenMoveType.write(value.type, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveField_lift(_ buf: RustBuffer) throws -> MoveField {
    return try FfiConverterTypeMoveField.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveField_lower(_ value: MoveField) -> RustBuffer {
    return FfiConverterTypeMoveField.lower(value)
}


public struct MoveFunctionConnection {
    public var nodes: [MoveFunction]
    public var pageInfo: PageInfo

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(nodes: [MoveFunction], pageInfo: PageInfo) {
        self.nodes = nodes
        self.pageInfo = pageInfo
    }
}

#if compiler(>=6)
extension MoveFunctionConnection: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMoveFunctionConnection: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MoveFunctionConnection {
        return
            try MoveFunctionConnection(
                nodes: FfiConverterSequenceTypeMoveFunction.read(from: &buf), 
                pageInfo: FfiConverterTypePageInfo.read(from: &buf)
        )
    }

    public static func write(_ value: MoveFunctionConnection, into buf: inout [UInt8]) {
        FfiConverterSequenceTypeMoveFunction.write(value.nodes, into: &buf)
        FfiConverterTypePageInfo.write(value.pageInfo, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveFunctionConnection_lift(_ buf: RustBuffer) throws -> MoveFunctionConnection {
    return try FfiConverterTypeMoveFunctionConnection.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveFunctionConnection_lower(_ value: MoveFunctionConnection) -> RustBuffer {
    return FfiConverterTypeMoveFunctionConnection.lower(value)
}


public struct MoveFunctionTypeParameter {
    public var constraints: [MoveAbility]

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(constraints: [MoveAbility]) {
        self.constraints = constraints
    }
}

#if compiler(>=6)
extension MoveFunctionTypeParameter: Sendable {}
#endif


extension MoveFunctionTypeParameter: Equatable, Hashable {
    public static func ==(lhs: MoveFunctionTypeParameter, rhs: MoveFunctionTypeParameter) -> Bool {
        if lhs.constraints != rhs.constraints {
            return false
        }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(constraints)
    }
}



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMoveFunctionTypeParameter: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MoveFunctionTypeParameter {
        return
            try MoveFunctionTypeParameter(
                constraints: FfiConverterSequenceTypeMoveAbility.read(from: &buf)
        )
    }

    public static func write(_ value: MoveFunctionTypeParameter, into buf: inout [UInt8]) {
        FfiConverterSequenceTypeMoveAbility.write(value.constraints, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveFunctionTypeParameter_lift(_ buf: RustBuffer) throws -> MoveFunctionTypeParameter {
    return try FfiConverterTypeMoveFunctionTypeParameter.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveFunctionTypeParameter_lower(_ value: MoveFunctionTypeParameter) -> RustBuffer {
    return FfiConverterTypeMoveFunctionTypeParameter.lower(value)
}


/**
 * Location in move bytecode where an error occurred
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * move-location = object-id identifier u16 u16 (option identifier)
 * ```
 */
public struct MoveLocation {
    /**
     * The package id
     */
    public var package: ObjectId
    /**
     * The module name
     */
    public var module: String
    /**
     * The function index
     */
    public var function: UInt16
    /**
     * Index into the code stream for a jump. The offset is relative to the
     * beginning of the instruction stream.
     */
    public var instruction: UInt16
    /**
     * The name of the function if available
     */
    public var functionName: String?

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * The package id
         */package: ObjectId, 
        /**
         * The module name
         */module: String, 
        /**
         * The function index
         */function: UInt16, 
        /**
         * Index into the code stream for a jump. The offset is relative to the
         * beginning of the instruction stream.
         */instruction: UInt16, 
        /**
         * The name of the function if available
         */functionName: String? = nil) {
        self.package = package
        self.module = module
        self.function = function
        self.instruction = instruction
        self.functionName = functionName
    }
}

#if compiler(>=6)
extension MoveLocation: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMoveLocation: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MoveLocation {
        return
            try MoveLocation(
                package: FfiConverterTypeObjectId.read(from: &buf), 
                module: FfiConverterString.read(from: &buf), 
                function: FfiConverterUInt16.read(from: &buf), 
                instruction: FfiConverterUInt16.read(from: &buf), 
                functionName: FfiConverterOptionString.read(from: &buf)
        )
    }

    public static func write(_ value: MoveLocation, into buf: inout [UInt8]) {
        FfiConverterTypeObjectId.write(value.package, into: &buf)
        FfiConverterString.write(value.module, into: &buf)
        FfiConverterUInt16.write(value.function, into: &buf)
        FfiConverterUInt16.write(value.instruction, into: &buf)
        FfiConverterOptionString.write(value.functionName, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveLocation_lift(_ buf: RustBuffer) throws -> MoveLocation {
    return try FfiConverterTypeMoveLocation.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveLocation_lower(_ value: MoveLocation) -> RustBuffer {
    return FfiConverterTypeMoveLocation.lower(value)
}


public struct MoveModule {
    public var fileFormatVersion: Int32
    public var enums: MoveEnumConnection?
    public var friends: MoveModuleConnection
    public var functions: MoveFunctionConnection?
    public var structs: MoveStructConnection?

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(fileFormatVersion: Int32, enums: MoveEnumConnection? = nil, friends: MoveModuleConnection, functions: MoveFunctionConnection? = nil, structs: MoveStructConnection? = nil) {
        self.fileFormatVersion = fileFormatVersion
        self.enums = enums
        self.friends = friends
        self.functions = functions
        self.structs = structs
    }
}

#if compiler(>=6)
extension MoveModule: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMoveModule: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MoveModule {
        return
            try MoveModule(
                fileFormatVersion: FfiConverterInt32.read(from: &buf), 
                enums: FfiConverterOptionTypeMoveEnumConnection.read(from: &buf), 
                friends: FfiConverterTypeMoveModuleConnection.read(from: &buf), 
                functions: FfiConverterOptionTypeMoveFunctionConnection.read(from: &buf), 
                structs: FfiConverterOptionTypeMoveStructConnection.read(from: &buf)
        )
    }

    public static func write(_ value: MoveModule, into buf: inout [UInt8]) {
        FfiConverterInt32.write(value.fileFormatVersion, into: &buf)
        FfiConverterOptionTypeMoveEnumConnection.write(value.enums, into: &buf)
        FfiConverterTypeMoveModuleConnection.write(value.friends, into: &buf)
        FfiConverterOptionTypeMoveFunctionConnection.write(value.functions, into: &buf)
        FfiConverterOptionTypeMoveStructConnection.write(value.structs, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveModule_lift(_ buf: RustBuffer) throws -> MoveModule {
    return try FfiConverterTypeMoveModule.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveModule_lower(_ value: MoveModule) -> RustBuffer {
    return FfiConverterTypeMoveModule.lower(value)
}


public struct MoveModuleConnection {
    public var nodes: [MoveModuleQuery]
    public var pageInfo: PageInfo

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(nodes: [MoveModuleQuery], pageInfo: PageInfo) {
        self.nodes = nodes
        self.pageInfo = pageInfo
    }
}

#if compiler(>=6)
extension MoveModuleConnection: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMoveModuleConnection: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MoveModuleConnection {
        return
            try MoveModuleConnection(
                nodes: FfiConverterSequenceTypeMoveModuleQuery.read(from: &buf), 
                pageInfo: FfiConverterTypePageInfo.read(from: &buf)
        )
    }

    public static func write(_ value: MoveModuleConnection, into buf: inout [UInt8]) {
        FfiConverterSequenceTypeMoveModuleQuery.write(value.nodes, into: &buf)
        FfiConverterTypePageInfo.write(value.pageInfo, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveModuleConnection_lift(_ buf: RustBuffer) throws -> MoveModuleConnection {
    return try FfiConverterTypeMoveModuleConnection.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveModuleConnection_lower(_ value: MoveModuleConnection) -> RustBuffer {
    return FfiConverterTypeMoveModuleConnection.lower(value)
}


public struct MoveModuleQuery {
    public var package: MovePackageQuery
    public var name: String

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(package: MovePackageQuery, name: String) {
        self.package = package
        self.name = name
    }
}

#if compiler(>=6)
extension MoveModuleQuery: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMoveModuleQuery: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MoveModuleQuery {
        return
            try MoveModuleQuery(
                package: FfiConverterTypeMovePackageQuery.read(from: &buf), 
                name: FfiConverterString.read(from: &buf)
        )
    }

    public static func write(_ value: MoveModuleQuery, into buf: inout [UInt8]) {
        FfiConverterTypeMovePackageQuery.write(value.package, into: &buf)
        FfiConverterString.write(value.name, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveModuleQuery_lift(_ buf: RustBuffer) throws -> MoveModuleQuery {
    return try FfiConverterTypeMoveModuleQuery.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveModuleQuery_lower(_ value: MoveModuleQuery) -> RustBuffer {
    return FfiConverterTypeMoveModuleQuery.lower(value)
}


public struct MoveObject {
    public var bcs: Base64?

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(bcs: Base64? = nil) {
        self.bcs = bcs
    }
}

#if compiler(>=6)
extension MoveObject: Sendable {}
#endif


extension MoveObject: Equatable, Hashable {
    public static func ==(lhs: MoveObject, rhs: MoveObject) -> Bool {
        if lhs.bcs != rhs.bcs {
            return false
        }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(bcs)
    }
}



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMoveObject: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MoveObject {
        return
            try MoveObject(
                bcs: FfiConverterOptionTypeBase64.read(from: &buf)
        )
    }

    public static func write(_ value: MoveObject, into buf: inout [UInt8]) {
        FfiConverterOptionTypeBase64.write(value.bcs, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveObject_lift(_ buf: RustBuffer) throws -> MoveObject {
    return try FfiConverterTypeMoveObject.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveObject_lower(_ value: MoveObject) -> RustBuffer {
    return FfiConverterTypeMoveObject.lower(value)
}


/**
 * A page of items returned by the GraphQL server.
 */
public struct MovePackagePage {
    /**
     * Information about the page, such as the cursor and whether there are
     * more pages.
     */
    public var pageInfo: PageInfo
    /**
     * The data returned by the server.
     */
    public var data: [MovePackage]

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * Information about the page, such as the cursor and whether there are
         * more pages.
         */pageInfo: PageInfo, 
        /**
         * The data returned by the server.
         */data: [MovePackage]) {
        self.pageInfo = pageInfo
        self.data = data
    }
}

#if compiler(>=6)
extension MovePackagePage: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMovePackagePage: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MovePackagePage {
        return
            try MovePackagePage(
                pageInfo: FfiConverterTypePageInfo.read(from: &buf), 
                data: FfiConverterSequenceTypeMovePackage.read(from: &buf)
        )
    }

    public static func write(_ value: MovePackagePage, into buf: inout [UInt8]) {
        FfiConverterTypePageInfo.write(value.pageInfo, into: &buf)
        FfiConverterSequenceTypeMovePackage.write(value.data, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMovePackagePage_lift(_ buf: RustBuffer) throws -> MovePackagePage {
    return try FfiConverterTypeMovePackagePage.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMovePackagePage_lower(_ value: MovePackagePage) -> RustBuffer {
    return FfiConverterTypeMovePackagePage.lower(value)
}


public struct MovePackageQuery {
    public var address: Address
    public var bcs: Base64?

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(address: Address, bcs: Base64? = nil) {
        self.address = address
        self.bcs = bcs
    }
}

#if compiler(>=6)
extension MovePackageQuery: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMovePackageQuery: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MovePackageQuery {
        return
            try MovePackageQuery(
                address: FfiConverterTypeAddress.read(from: &buf), 
                bcs: FfiConverterOptionTypeBase64.read(from: &buf)
        )
    }

    public static func write(_ value: MovePackageQuery, into buf: inout [UInt8]) {
        FfiConverterTypeAddress.write(value.address, into: &buf)
        FfiConverterOptionTypeBase64.write(value.bcs, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMovePackageQuery_lift(_ buf: RustBuffer) throws -> MovePackageQuery {
    return try FfiConverterTypeMovePackageQuery.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMovePackageQuery_lower(_ value: MovePackageQuery) -> RustBuffer {
    return FfiConverterTypeMovePackageQuery.lower(value)
}


/**
 * A move struct
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * object-move-struct = compressed-struct-tag bool u64 object-contents
 *
 * compressed-struct-tag = other-struct-type / gas-coin-type / staked-iota-type / coin-type
 * other-struct-type     = %x00 struct-tag
 * gas-coin-type         = %x01
 * staked-iota-type      = %x02
 * coin-type             = %x03 type-tag
 *
 * ; first 32 bytes of the contents are the object's object-id
 * object-contents = uleb128 (object-id *OCTET) ; length followed by contents
 * ```
 */
public struct MoveStruct {
    /**
     * The type of this object
     */
    public var structType: StructTag
    /**
     * Number that increases each time a tx takes this object as a mutable
     * input This is a lamport timestamp, not a sequentially increasing
     * version
     */
    public var version: UInt64
    /**
     * BCS bytes of a Move struct value
     */
    public var contents: Data

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * The type of this object
         */structType: StructTag, 
        /**
         * Number that increases each time a tx takes this object as a mutable
         * input This is a lamport timestamp, not a sequentially increasing
         * version
         */version: UInt64, 
        /**
         * BCS bytes of a Move struct value
         */contents: Data) {
        self.structType = structType
        self.version = version
        self.contents = contents
    }
}

#if compiler(>=6)
extension MoveStruct: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMoveStruct: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MoveStruct {
        return
            try MoveStruct(
                structType: FfiConverterTypeStructTag.read(from: &buf), 
                version: FfiConverterUInt64.read(from: &buf), 
                contents: FfiConverterData.read(from: &buf)
        )
    }

    public static func write(_ value: MoveStruct, into buf: inout [UInt8]) {
        FfiConverterTypeStructTag.write(value.structType, into: &buf)
        FfiConverterUInt64.write(value.version, into: &buf)
        FfiConverterData.write(value.contents, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveStruct_lift(_ buf: RustBuffer) throws -> MoveStruct {
    return try FfiConverterTypeMoveStruct.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveStruct_lower(_ value: MoveStruct) -> RustBuffer {
    return FfiConverterTypeMoveStruct.lower(value)
}


public struct MoveStructConnection {
    public var pageInfo: PageInfo
    public var nodes: [MoveStructQuery]

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(pageInfo: PageInfo, nodes: [MoveStructQuery]) {
        self.pageInfo = pageInfo
        self.nodes = nodes
    }
}

#if compiler(>=6)
extension MoveStructConnection: Sendable {}
#endif


extension MoveStructConnection: Equatable, Hashable {
    public static func ==(lhs: MoveStructConnection, rhs: MoveStructConnection) -> Bool {
        if lhs.pageInfo != rhs.pageInfo {
            return false
        }
        if lhs.nodes != rhs.nodes {
            return false
        }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(pageInfo)
        hasher.combine(nodes)
    }
}



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMoveStructConnection: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MoveStructConnection {
        return
            try MoveStructConnection(
                pageInfo: FfiConverterTypePageInfo.read(from: &buf), 
                nodes: FfiConverterSequenceTypeMoveStructQuery.read(from: &buf)
        )
    }

    public static func write(_ value: MoveStructConnection, into buf: inout [UInt8]) {
        FfiConverterTypePageInfo.write(value.pageInfo, into: &buf)
        FfiConverterSequenceTypeMoveStructQuery.write(value.nodes, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveStructConnection_lift(_ buf: RustBuffer) throws -> MoveStructConnection {
    return try FfiConverterTypeMoveStructConnection.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveStructConnection_lower(_ value: MoveStructConnection) -> RustBuffer {
    return FfiConverterTypeMoveStructConnection.lower(value)
}


public struct MoveStructQuery {
    public var abilities: [MoveAbility]?
    public var name: String
    public var fields: [MoveField]?
    public var typeParameters: [MoveStructTypeParameter]?

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(abilities: [MoveAbility]? = nil, name: String, fields: [MoveField]? = nil, typeParameters: [MoveStructTypeParameter]? = nil) {
        self.abilities = abilities
        self.name = name
        self.fields = fields
        self.typeParameters = typeParameters
    }
}

#if compiler(>=6)
extension MoveStructQuery: Sendable {}
#endif


extension MoveStructQuery: Equatable, Hashable {
    public static func ==(lhs: MoveStructQuery, rhs: MoveStructQuery) -> Bool {
        if lhs.abilities != rhs.abilities {
            return false
        }
        if lhs.name != rhs.name {
            return false
        }
        if lhs.fields != rhs.fields {
            return false
        }
        if lhs.typeParameters != rhs.typeParameters {
            return false
        }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(abilities)
        hasher.combine(name)
        hasher.combine(fields)
        hasher.combine(typeParameters)
    }
}



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMoveStructQuery: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MoveStructQuery {
        return
            try MoveStructQuery(
                abilities: FfiConverterOptionSequenceTypeMoveAbility.read(from: &buf), 
                name: FfiConverterString.read(from: &buf), 
                fields: FfiConverterOptionSequenceTypeMoveField.read(from: &buf), 
                typeParameters: FfiConverterOptionSequenceTypeMoveStructTypeParameter.read(from: &buf)
        )
    }

    public static func write(_ value: MoveStructQuery, into buf: inout [UInt8]) {
        FfiConverterOptionSequenceTypeMoveAbility.write(value.abilities, into: &buf)
        FfiConverterString.write(value.name, into: &buf)
        FfiConverterOptionSequenceTypeMoveField.write(value.fields, into: &buf)
        FfiConverterOptionSequenceTypeMoveStructTypeParameter.write(value.typeParameters, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveStructQuery_lift(_ buf: RustBuffer) throws -> MoveStructQuery {
    return try FfiConverterTypeMoveStructQuery.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveStructQuery_lower(_ value: MoveStructQuery) -> RustBuffer {
    return FfiConverterTypeMoveStructQuery.lower(value)
}


public struct MoveStructTypeParameter {
    public var constraints: [MoveAbility]
    public var isPhantom: Bool

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(constraints: [MoveAbility], isPhantom: Bool) {
        self.constraints = constraints
        self.isPhantom = isPhantom
    }
}

#if compiler(>=6)
extension MoveStructTypeParameter: Sendable {}
#endif


extension MoveStructTypeParameter: Equatable, Hashable {
    public static func ==(lhs: MoveStructTypeParameter, rhs: MoveStructTypeParameter) -> Bool {
        if lhs.constraints != rhs.constraints {
            return false
        }
        if lhs.isPhantom != rhs.isPhantom {
            return false
        }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(constraints)
        hasher.combine(isPhantom)
    }
}



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMoveStructTypeParameter: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MoveStructTypeParameter {
        return
            try MoveStructTypeParameter(
                constraints: FfiConverterSequenceTypeMoveAbility.read(from: &buf), 
                isPhantom: FfiConverterBool.read(from: &buf)
        )
    }

    public static func write(_ value: MoveStructTypeParameter, into buf: inout [UInt8]) {
        FfiConverterSequenceTypeMoveAbility.write(value.constraints, into: &buf)
        FfiConverterBool.write(value.isPhantom, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveStructTypeParameter_lift(_ buf: RustBuffer) throws -> MoveStructTypeParameter {
    return try FfiConverterTypeMoveStructTypeParameter.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveStructTypeParameter_lower(_ value: MoveStructTypeParameter) -> RustBuffer {
    return FfiConverterTypeMoveStructTypeParameter.lower(value)
}


/**
 * The result of executing a Move View Function.
 *
 * Execution errors are captured in the `error` field, in which case the
 * `results` field will be `None`. On success, the `results` field will contain
 * the return values of the Move view function, and the `error` field will be
 * `None`.
 */
public struct MoveViewResult {
    /**
     * Execution error from executing the Move view function.
     */
    public var error: String?
    /**
     * The return values of the Move view function, resolved and formatted as
     * JSON.
     */
    public var results: [String]?

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * Execution error from executing the Move view function.
         */error: String? = nil, 
        /**
         * The return values of the Move view function, resolved and formatted as
         * JSON.
         */results: [String]? = nil) {
        self.error = error
        self.results = results
    }
}

#if compiler(>=6)
extension MoveViewResult: Sendable {}
#endif


extension MoveViewResult: Equatable, Hashable {
    public static func ==(lhs: MoveViewResult, rhs: MoveViewResult) -> Bool {
        if lhs.error != rhs.error {
            return false
        }
        if lhs.results != rhs.results {
            return false
        }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(error)
        hasher.combine(results)
    }
}



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMoveViewResult: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MoveViewResult {
        return
            try MoveViewResult(
                error: FfiConverterOptionString.read(from: &buf), 
                results: FfiConverterOptionSequenceString.read(from: &buf)
        )
    }

    public static func write(_ value: MoveViewResult, into buf: inout [UInt8]) {
        FfiConverterOptionString.write(value.error, into: &buf)
        FfiConverterOptionSequenceString.write(value.results, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveViewResult_lift(_ buf: RustBuffer) throws -> MoveViewResult {
    return try FfiConverterTypeMoveViewResult.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveViewResult_lower(_ value: MoveViewResult) -> RustBuffer {
    return FfiConverterTypeMoveViewResult.lower(value)
}


/**
 * A page of items returned by the GraphQL server.
 */
public struct NameRegistrationPage {
    /**
     * Information about the page, such as the cursor and whether there are
     * more pages.
     */
    public var pageInfo: PageInfo
    /**
     * The data returned by the server.
     */
    public var data: [NameRegistration]

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * Information about the page, such as the cursor and whether there are
         * more pages.
         */pageInfo: PageInfo, 
        /**
         * The data returned by the server.
         */data: [NameRegistration]) {
        self.pageInfo = pageInfo
        self.data = data
    }
}

#if compiler(>=6)
extension NameRegistrationPage: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeNameRegistrationPage: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> NameRegistrationPage {
        return
            try NameRegistrationPage(
                pageInfo: FfiConverterTypePageInfo.read(from: &buf), 
                data: FfiConverterSequenceTypeNameRegistration.read(from: &buf)
        )
    }

    public static func write(_ value: NameRegistrationPage, into buf: inout [UInt8]) {
        FfiConverterTypePageInfo.write(value.pageInfo, into: &buf)
        FfiConverterSequenceTypeNameRegistration.write(value.data, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeNameRegistrationPage_lift(_ buf: RustBuffer) throws -> NameRegistrationPage {
    return try FfiConverterTypeNameRegistrationPage.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeNameRegistrationPage_lower(_ value: NameRegistrationPage) -> RustBuffer {
    return FfiConverterTypeNameRegistrationPage.lower(value)
}


public struct ObjectFilter {
    public var typeTag: String?
    public var owner: Address?
    public var objectIds: [ObjectId]?

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(typeTag: String? = nil, owner: Address? = nil, objectIds: [ObjectId]? = nil) {
        self.typeTag = typeTag
        self.owner = owner
        self.objectIds = objectIds
    }
}

#if compiler(>=6)
extension ObjectFilter: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeObjectFilter: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ObjectFilter {
        return
            try ObjectFilter(
                typeTag: FfiConverterOptionString.read(from: &buf), 
                owner: FfiConverterOptionTypeAddress.read(from: &buf), 
                objectIds: FfiConverterOptionSequenceTypeObjectId.read(from: &buf)
        )
    }

    public static func write(_ value: ObjectFilter, into buf: inout [UInt8]) {
        FfiConverterOptionString.write(value.typeTag, into: &buf)
        FfiConverterOptionTypeAddress.write(value.owner, into: &buf)
        FfiConverterOptionSequenceTypeObjectId.write(value.objectIds, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeObjectFilter_lift(_ buf: RustBuffer) throws -> ObjectFilter {
    return try FfiConverterTypeObjectFilter.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeObjectFilter_lower(_ value: ObjectFilter) -> RustBuffer {
    return FfiConverterTypeObjectFilter.lower(value)
}


/**
 * A page of items returned by the GraphQL server.
 */
public struct ObjectPage {
    /**
     * Information about the page, such as the cursor and whether there are
     * more pages.
     */
    public var pageInfo: PageInfo
    /**
     * The data returned by the server.
     */
    public var data: [Object]

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * Information about the page, such as the cursor and whether there are
         * more pages.
         */pageInfo: PageInfo, 
        /**
         * The data returned by the server.
         */data: [Object]) {
        self.pageInfo = pageInfo
        self.data = data
    }
}

#if compiler(>=6)
extension ObjectPage: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeObjectPage: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ObjectPage {
        return
            try ObjectPage(
                pageInfo: FfiConverterTypePageInfo.read(from: &buf), 
                data: FfiConverterSequenceTypeObject.read(from: &buf)
        )
    }

    public static func write(_ value: ObjectPage, into buf: inout [UInt8]) {
        FfiConverterTypePageInfo.write(value.pageInfo, into: &buf)
        FfiConverterSequenceTypeObject.write(value.data, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeObjectPage_lift(_ buf: RustBuffer) throws -> ObjectPage {
    return try FfiConverterTypeObjectPage.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeObjectPage_lower(_ value: ObjectPage) -> RustBuffer {
    return FfiConverterTypeObjectPage.lower(value)
}


public struct ObjectRef {
    public var address: ObjectId
    public var digest: String
    public var version: UInt64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(address: ObjectId, digest: String, version: UInt64) {
        self.address = address
        self.digest = digest
        self.version = version
    }
}

#if compiler(>=6)
extension ObjectRef: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeObjectRef: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ObjectRef {
        return
            try ObjectRef(
                address: FfiConverterTypeObjectId.read(from: &buf), 
                digest: FfiConverterString.read(from: &buf), 
                version: FfiConverterUInt64.read(from: &buf)
        )
    }

    public static func write(_ value: ObjectRef, into buf: inout [UInt8]) {
        FfiConverterTypeObjectId.write(value.address, into: &buf)
        FfiConverterString.write(value.digest, into: &buf)
        FfiConverterUInt64.write(value.version, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeObjectRef_lift(_ buf: RustBuffer) throws -> ObjectRef {
    return try FfiConverterTypeObjectRef.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeObjectRef_lower(_ value: ObjectRef) -> RustBuffer {
    return FfiConverterTypeObjectRef.lower(value)
}


/**
 * Reference to an object
 *
 * Contains sufficient information to uniquely identify a specific object.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * object-ref = object-id u64 digest
 * ```
 */
public struct ObjectReference {
    public var objectId: ObjectId
    public var version: UInt64
    public var digest: Digest

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(objectId: ObjectId, version: UInt64, digest: Digest) {
        self.objectId = objectId
        self.version = version
        self.digest = digest
    }
}

#if compiler(>=6)
extension ObjectReference: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeObjectReference: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ObjectReference {
        return
            try ObjectReference(
                objectId: FfiConverterTypeObjectId.read(from: &buf), 
                version: FfiConverterUInt64.read(from: &buf), 
                digest: FfiConverterTypeDigest.read(from: &buf)
        )
    }

    public static func write(_ value: ObjectReference, into buf: inout [UInt8]) {
        FfiConverterTypeObjectId.write(value.objectId, into: &buf)
        FfiConverterUInt64.write(value.version, into: &buf)
        FfiConverterTypeDigest.write(value.digest, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeObjectReference_lift(_ buf: RustBuffer) throws -> ObjectReference {
    return try FfiConverterTypeObjectReference.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeObjectReference_lower(_ value: ObjectReference) -> RustBuffer {
    return FfiConverterTypeObjectReference.lower(value)
}


public struct OpenMoveType {
    public var repr: String

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(repr: String) {
        self.repr = repr
    }
}

#if compiler(>=6)
extension OpenMoveType: Sendable {}
#endif


extension OpenMoveType: Equatable, Hashable {
    public static func ==(lhs: OpenMoveType, rhs: OpenMoveType) -> Bool {
        if lhs.repr != rhs.repr {
            return false
        }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(repr)
    }
}



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeOpenMoveType: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> OpenMoveType {
        return
            try OpenMoveType(
                repr: FfiConverterString.read(from: &buf)
        )
    }

    public static func write(_ value: OpenMoveType, into buf: inout [UInt8]) {
        FfiConverterString.write(value.repr, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeOpenMoveType_lift(_ buf: RustBuffer) throws -> OpenMoveType {
    return try FfiConverterTypeOpenMoveType.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeOpenMoveType_lower(_ value: OpenMoveType) -> RustBuffer {
    return FfiConverterTypeOpenMoveType.lower(value)
}


/**
 * Information about pagination in a connection.
 */
public struct PageInfo {
    /**
     * When paginating backwards, are there more items?
     */
    public var hasPreviousPage: Bool
    /**
     * Are there more items when paginating forwards?
     */
    public var hasNextPage: Bool
    /**
     * When paginating backwards, the cursor to continue.
     */
    public var startCursor: String?
    /**
     * When paginating forwards, the cursor to continue.
     */
    public var endCursor: String?

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * When paginating backwards, are there more items?
         */hasPreviousPage: Bool, 
        /**
         * Are there more items when paginating forwards?
         */hasNextPage: Bool, 
        /**
         * When paginating backwards, the cursor to continue.
         */startCursor: String? = nil, 
        /**
         * When paginating forwards, the cursor to continue.
         */endCursor: String? = nil) {
        self.hasPreviousPage = hasPreviousPage
        self.hasNextPage = hasNextPage
        self.startCursor = startCursor
        self.endCursor = endCursor
    }
}

#if compiler(>=6)
extension PageInfo: Sendable {}
#endif


extension PageInfo: Equatable, Hashable {
    public static func ==(lhs: PageInfo, rhs: PageInfo) -> Bool {
        if lhs.hasPreviousPage != rhs.hasPreviousPage {
            return false
        }
        if lhs.hasNextPage != rhs.hasNextPage {
            return false
        }
        if lhs.startCursor != rhs.startCursor {
            return false
        }
        if lhs.endCursor != rhs.endCursor {
            return false
        }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(hasPreviousPage)
        hasher.combine(hasNextPage)
        hasher.combine(startCursor)
        hasher.combine(endCursor)
    }
}



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypePageInfo: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> PageInfo {
        return
            try PageInfo(
                hasPreviousPage: FfiConverterBool.read(from: &buf), 
                hasNextPage: FfiConverterBool.read(from: &buf), 
                startCursor: FfiConverterOptionString.read(from: &buf), 
                endCursor: FfiConverterOptionString.read(from: &buf)
        )
    }

    public static func write(_ value: PageInfo, into buf: inout [UInt8]) {
        FfiConverterBool.write(value.hasPreviousPage, into: &buf)
        FfiConverterBool.write(value.hasNextPage, into: &buf)
        FfiConverterOptionString.write(value.startCursor, into: &buf)
        FfiConverterOptionString.write(value.endCursor, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePageInfo_lift(_ buf: RustBuffer) throws -> PageInfo {
    return try FfiConverterTypePageInfo.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePageInfo_lower(_ value: PageInfo) -> RustBuffer {
    return FfiConverterTypePageInfo.lower(value)
}


/**
 * Pagination options for querying the GraphQL server. It defaults to forward
 * pagination with the GraphQL server's max page size.
 */
public struct PaginationFilter {
    /**
     * The direction of pagination.
     */
    public var direction: Direction
    /**
     * An opaque cursor used for pagination.
     */
    public var cursor: String?
    /**
     * The maximum number of items to return. If this is omitted, it will
     * lazily query the service configuration for the max page size.
     */
    public var limit: Int32?

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * The direction of pagination.
         */direction: Direction, 
        /**
         * An opaque cursor used for pagination.
         */cursor: String? = nil, 
        /**
         * The maximum number of items to return. If this is omitted, it will
         * lazily query the service configuration for the max page size.
         */limit: Int32? = nil) {
        self.direction = direction
        self.cursor = cursor
        self.limit = limit
    }
}

#if compiler(>=6)
extension PaginationFilter: Sendable {}
#endif


extension PaginationFilter: Equatable, Hashable {
    public static func ==(lhs: PaginationFilter, rhs: PaginationFilter) -> Bool {
        if lhs.direction != rhs.direction {
            return false
        }
        if lhs.cursor != rhs.cursor {
            return false
        }
        if lhs.limit != rhs.limit {
            return false
        }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(direction)
        hasher.combine(cursor)
        hasher.combine(limit)
    }
}



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypePaginationFilter: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> PaginationFilter {
        return
            try PaginationFilter(
                direction: FfiConverterTypeDirection.read(from: &buf), 
                cursor: FfiConverterOptionString.read(from: &buf), 
                limit: FfiConverterOptionInt32.read(from: &buf)
        )
    }

    public static func write(_ value: PaginationFilter, into buf: inout [UInt8]) {
        FfiConverterTypeDirection.write(value.direction, into: &buf)
        FfiConverterOptionString.write(value.cursor, into: &buf)
        FfiConverterOptionInt32.write(value.limit, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePaginationFilter_lift(_ buf: RustBuffer) throws -> PaginationFilter {
    return try FfiConverterTypePaginationFilter.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePaginationFilter_lower(_ value: PaginationFilter) -> RustBuffer {
    return FfiConverterTypePaginationFilter.lower(value)
}


/**
 * A key-value protocol configuration attribute.
 */
public struct ProtocolConfigAttr {
    public var key: String
    public var value: String?

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(key: String, value: String?) {
        self.key = key
        self.value = value
    }
}

#if compiler(>=6)
extension ProtocolConfigAttr: Sendable {}
#endif


extension ProtocolConfigAttr: Equatable, Hashable {
    public static func ==(lhs: ProtocolConfigAttr, rhs: ProtocolConfigAttr) -> Bool {
        if lhs.key != rhs.key {
            return false
        }
        if lhs.value != rhs.value {
            return false
        }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(key)
        hasher.combine(value)
    }
}



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeProtocolConfigAttr: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ProtocolConfigAttr {
        return
            try ProtocolConfigAttr(
                key: FfiConverterString.read(from: &buf), 
                value: FfiConverterOptionString.read(from: &buf)
        )
    }

    public static func write(_ value: ProtocolConfigAttr, into buf: inout [UInt8]) {
        FfiConverterString.write(value.key, into: &buf)
        FfiConverterOptionString.write(value.value, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeProtocolConfigAttr_lift(_ buf: RustBuffer) throws -> ProtocolConfigAttr {
    return try FfiConverterTypeProtocolConfigAttr.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeProtocolConfigAttr_lower(_ value: ProtocolConfigAttr) -> RustBuffer {
    return FfiConverterTypeProtocolConfigAttr.lower(value)
}


/**
 * Feature flags are a form of boolean configuration that are usually used to
 * gate features while they are in development. Once a lag has been enabled, it
 * is rare for it to be disabled.
 */
public struct ProtocolConfigFeatureFlag {
    public var key: String
    public var value: Bool

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(key: String, value: Bool) {
        self.key = key
        self.value = value
    }
}

#if compiler(>=6)
extension ProtocolConfigFeatureFlag: Sendable {}
#endif


extension ProtocolConfigFeatureFlag: Equatable, Hashable {
    public static func ==(lhs: ProtocolConfigFeatureFlag, rhs: ProtocolConfigFeatureFlag) -> Bool {
        if lhs.key != rhs.key {
            return false
        }
        if lhs.value != rhs.value {
            return false
        }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(key)
        hasher.combine(value)
    }
}



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeProtocolConfigFeatureFlag: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ProtocolConfigFeatureFlag {
        return
            try ProtocolConfigFeatureFlag(
                key: FfiConverterString.read(from: &buf), 
                value: FfiConverterBool.read(from: &buf)
        )
    }

    public static func write(_ value: ProtocolConfigFeatureFlag, into buf: inout [UInt8]) {
        FfiConverterString.write(value.key, into: &buf)
        FfiConverterBool.write(value.value, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeProtocolConfigFeatureFlag_lift(_ buf: RustBuffer) throws -> ProtocolConfigFeatureFlag {
    return try FfiConverterTypeProtocolConfigFeatureFlag.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeProtocolConfigFeatureFlag_lower(_ value: ProtocolConfigFeatureFlag) -> RustBuffer {
    return FfiConverterTypeProtocolConfigFeatureFlag.lower(value)
}


/**
 * Information about the configuration of the protocol.
 * Constants that control how the chain operates.
 * These can only change during protocol upgrades which happen on epoch
 * boundaries.
 */
public struct ProtocolConfigs {
    /**
     * The protocol is not required to change on every epoch boundary, so the
     * protocol version tracks which change to the protocol these configs
     * are from.
     */
    public var protocolVersion: UInt64
    /**
     * List all available feature flags and their values. Feature flags are a
     * form of boolean configuration that are usually used to gate features
     * while they are in development. Once a flag has been enabled, it is
     * rare for it to be disabled.
     */
    public var featureFlags: [ProtocolConfigFeatureFlag]
    /**
     * List all available configurations and their values. These configurations
     * can take any value (but they will all be represented in string
     * form), and do not include feature flags.
     */
    public var configs: [ProtocolConfigAttr]

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * The protocol is not required to change on every epoch boundary, so the
         * protocol version tracks which change to the protocol these configs
         * are from.
         */protocolVersion: UInt64, 
        /**
         * List all available feature flags and their values. Feature flags are a
         * form of boolean configuration that are usually used to gate features
         * while they are in development. Once a flag has been enabled, it is
         * rare for it to be disabled.
         */featureFlags: [ProtocolConfigFeatureFlag], 
        /**
         * List all available configurations and their values. These configurations
         * can take any value (but they will all be represented in string
         * form), and do not include feature flags.
         */configs: [ProtocolConfigAttr]) {
        self.protocolVersion = protocolVersion
        self.featureFlags = featureFlags
        self.configs = configs
    }
}

#if compiler(>=6)
extension ProtocolConfigs: Sendable {}
#endif


extension ProtocolConfigs: Equatable, Hashable {
    public static func ==(lhs: ProtocolConfigs, rhs: ProtocolConfigs) -> Bool {
        if lhs.protocolVersion != rhs.protocolVersion {
            return false
        }
        if lhs.featureFlags != rhs.featureFlags {
            return false
        }
        if lhs.configs != rhs.configs {
            return false
        }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(protocolVersion)
        hasher.combine(featureFlags)
        hasher.combine(configs)
    }
}



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeProtocolConfigs: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ProtocolConfigs {
        return
            try ProtocolConfigs(
                protocolVersion: FfiConverterUInt64.read(from: &buf), 
                featureFlags: FfiConverterSequenceTypeProtocolConfigFeatureFlag.read(from: &buf), 
                configs: FfiConverterSequenceTypeProtocolConfigAttr.read(from: &buf)
        )
    }

    public static func write(_ value: ProtocolConfigs, into buf: inout [UInt8]) {
        FfiConverterUInt64.write(value.protocolVersion, into: &buf)
        FfiConverterSequenceTypeProtocolConfigFeatureFlag.write(value.featureFlags, into: &buf)
        FfiConverterSequenceTypeProtocolConfigAttr.write(value.configs, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeProtocolConfigs_lift(_ buf: RustBuffer) throws -> ProtocolConfigs {
    return try FfiConverterTypeProtocolConfigs.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeProtocolConfigs_lower(_ value: ProtocolConfigs) -> RustBuffer {
    return FfiConverterTypeProtocolConfigs.lower(value)
}


public struct Query {
    public var query: String
    public var variables: Value?

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(query: String, variables: Value? = nil) {
        self.query = query
        self.variables = variables
    }
}

#if compiler(>=6)
extension Query: Sendable {}
#endif


extension Query: Equatable, Hashable {
    public static func ==(lhs: Query, rhs: Query) -> Bool {
        if lhs.query != rhs.query {
            return false
        }
        if lhs.variables != rhs.variables {
            return false
        }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(query)
        hasher.combine(variables)
    }
}



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeQuery: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Query {
        return
            try Query(
                query: FfiConverterString.read(from: &buf), 
                variables: FfiConverterOptionTypeValue.read(from: &buf)
        )
    }

    public static func write(_ value: Query, into buf: inout [UInt8]) {
        FfiConverterString.write(value.query, into: &buf)
        FfiConverterOptionTypeValue.write(value.variables, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeQuery_lift(_ buf: RustBuffer) throws -> Query {
    return try FfiConverterTypeQuery.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeQuery_lower(_ value: Query) -> RustBuffer {
    return FfiConverterTypeQuery.lower(value)
}


/**
 * Randomness update
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * randomness-state-update = u64 u64 bytes u64
 * ```
 */
public struct RandomnessStateUpdate {
    /**
     * Epoch of the randomness state update transaction
     */
    public var epoch: UInt64
    /**
     * Randomness round of the update
     */
    public var randomnessRound: UInt64
    /**
     * Updated random bytes
     */
    public var randomBytes: Data
    /**
     * The initial version of the randomness object that it was shared at
     */
    public var randomnessObjInitialSharedVersion: UInt64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * Epoch of the randomness state update transaction
         */epoch: UInt64, 
        /**
         * Randomness round of the update
         */randomnessRound: UInt64, 
        /**
         * Updated random bytes
         */randomBytes: Data, 
        /**
         * The initial version of the randomness object that it was shared at
         */randomnessObjInitialSharedVersion: UInt64) {
        self.epoch = epoch
        self.randomnessRound = randomnessRound
        self.randomBytes = randomBytes
        self.randomnessObjInitialSharedVersion = randomnessObjInitialSharedVersion
    }
}

#if compiler(>=6)
extension RandomnessStateUpdate: Sendable {}
#endif


extension RandomnessStateUpdate: Equatable, Hashable {
    public static func ==(lhs: RandomnessStateUpdate, rhs: RandomnessStateUpdate) -> Bool {
        if lhs.epoch != rhs.epoch {
            return false
        }
        if lhs.randomnessRound != rhs.randomnessRound {
            return false
        }
        if lhs.randomBytes != rhs.randomBytes {
            return false
        }
        if lhs.randomnessObjInitialSharedVersion != rhs.randomnessObjInitialSharedVersion {
            return false
        }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(epoch)
        hasher.combine(randomnessRound)
        hasher.combine(randomBytes)
        hasher.combine(randomnessObjInitialSharedVersion)
    }
}



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeRandomnessStateUpdate: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> RandomnessStateUpdate {
        return
            try RandomnessStateUpdate(
                epoch: FfiConverterUInt64.read(from: &buf), 
                randomnessRound: FfiConverterUInt64.read(from: &buf), 
                randomBytes: FfiConverterData.read(from: &buf), 
                randomnessObjInitialSharedVersion: FfiConverterUInt64.read(from: &buf)
        )
    }

    public static func write(_ value: RandomnessStateUpdate, into buf: inout [UInt8]) {
        FfiConverterUInt64.write(value.epoch, into: &buf)
        FfiConverterUInt64.write(value.randomnessRound, into: &buf)
        FfiConverterData.write(value.randomBytes, into: &buf)
        FfiConverterUInt64.write(value.randomnessObjInitialSharedVersion, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeRandomnessStateUpdate_lift(_ buf: RustBuffer) throws -> RandomnessStateUpdate {
    return try FfiConverterTypeRandomnessStateUpdate.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeRandomnessStateUpdate_lower(_ value: RandomnessStateUpdate) -> RustBuffer {
    return FfiConverterTypeRandomnessStateUpdate.lower(value)
}


public struct ServiceConfig {
    /**
     * Default number of elements allowed on a single page of a connection.
     */
    public var defaultPageSize: Int32
    /**
     * List of all features that are enabled on this RPC service.
     */
    public var enabledFeatures: [Feature]
    /**
     * Maximum estimated cost of a database query used to serve a GraphQL
     * request.  This is measured in the same units that the database uses
     * in EXPLAIN queries.
     * Maximum nesting allowed in struct fields when calculating the layout of
     * a single Move Type.
     */
    public var maxMoveValueDepth: Int32
    /**
     * The maximum number of output nodes in a GraphQL response.
     * Non-connection nodes have a count of 1, while connection nodes are
     * counted as the specified 'first' or 'last' number of items, or the
     * default_page_size as set by the server if those arguments are not
     * set. Counts accumulate multiplicatively down the query tree. For
     * example, if a query starts with a connection of first: 10 and has a
     * field to a connection with last: 20, the count at the second level
     * would be 200 nodes. This is then summed to the count of 10 nodes
     * at the first level, for a total of 210 nodes.
     */
    public var maxOutputNodes: Int32
    /**
     * Maximum number of elements allowed on a single page of a connection.
     */
    public var maxPageSize: Int32
    /**
     * The maximum depth a GraphQL query can be to be accepted by this service.
     */
    public var maxQueryDepth: Int32
    /**
     * The maximum number of nodes (field names) the service will accept in a
     * single query.
     */
    public var maxQueryNodes: Int32
    /**
     * Maximum length of a query payload string.
     */
    public var maxQueryPayloadSize: Int32
    /**
     * Maximum nesting allowed in type arguments in Move Types resolved by this
     * service.
     */
    public var maxTypeArgumentDepth: Int32
    /**
     * Maximum number of type arguments passed into a generic instantiation of
     * a Move Type resolved by this service.
     */
    public var maxTypeArgumentWidth: Int32
    /**
     * Maximum number of structs that need to be processed when calculating the
     * layout of a single Move Type.
     */
    public var maxTypeNodes: Int32
    /**
     * Maximum time in milliseconds spent waiting for a response from fullnode
     * after issuing a transaction to execute. Note that the transaction
     * may still succeed even in the case of a timeout. Transactions are
     * idempotent, so a transaction that times out should be resubmitted
     * until the network returns a definite response (success or failure, not
     * timeout).
     */
    public var mutationTimeoutMs: Int32
    /**
     * Maximum time in milliseconds that will be spent to serve one query
     * request.
     */
    public var requestTimeoutMs: Int32

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * Default number of elements allowed on a single page of a connection.
         */defaultPageSize: Int32, 
        /**
         * List of all features that are enabled on this RPC service.
         */enabledFeatures: [Feature], 
        /**
         * Maximum estimated cost of a database query used to serve a GraphQL
         * request.  This is measured in the same units that the database uses
         * in EXPLAIN queries.
         * Maximum nesting allowed in struct fields when calculating the layout of
         * a single Move Type.
         */maxMoveValueDepth: Int32, 
        /**
         * The maximum number of output nodes in a GraphQL response.
         * Non-connection nodes have a count of 1, while connection nodes are
         * counted as the specified 'first' or 'last' number of items, or the
         * default_page_size as set by the server if those arguments are not
         * set. Counts accumulate multiplicatively down the query tree. For
         * example, if a query starts with a connection of first: 10 and has a
         * field to a connection with last: 20, the count at the second level
         * would be 200 nodes. This is then summed to the count of 10 nodes
         * at the first level, for a total of 210 nodes.
         */maxOutputNodes: Int32, 
        /**
         * Maximum number of elements allowed on a single page of a connection.
         */maxPageSize: Int32, 
        /**
         * The maximum depth a GraphQL query can be to be accepted by this service.
         */maxQueryDepth: Int32, 
        /**
         * The maximum number of nodes (field names) the service will accept in a
         * single query.
         */maxQueryNodes: Int32, 
        /**
         * Maximum length of a query payload string.
         */maxQueryPayloadSize: Int32, 
        /**
         * Maximum nesting allowed in type arguments in Move Types resolved by this
         * service.
         */maxTypeArgumentDepth: Int32, 
        /**
         * Maximum number of type arguments passed into a generic instantiation of
         * a Move Type resolved by this service.
         */maxTypeArgumentWidth: Int32, 
        /**
         * Maximum number of structs that need to be processed when calculating the
         * layout of a single Move Type.
         */maxTypeNodes: Int32, 
        /**
         * Maximum time in milliseconds spent waiting for a response from fullnode
         * after issuing a transaction to execute. Note that the transaction
         * may still succeed even in the case of a timeout. Transactions are
         * idempotent, so a transaction that times out should be resubmitted
         * until the network returns a definite response (success or failure, not
         * timeout).
         */mutationTimeoutMs: Int32, 
        /**
         * Maximum time in milliseconds that will be spent to serve one query
         * request.
         */requestTimeoutMs: Int32) {
        self.defaultPageSize = defaultPageSize
        self.enabledFeatures = enabledFeatures
        self.maxMoveValueDepth = maxMoveValueDepth
        self.maxOutputNodes = maxOutputNodes
        self.maxPageSize = maxPageSize
        self.maxQueryDepth = maxQueryDepth
        self.maxQueryNodes = maxQueryNodes
        self.maxQueryPayloadSize = maxQueryPayloadSize
        self.maxTypeArgumentDepth = maxTypeArgumentDepth
        self.maxTypeArgumentWidth = maxTypeArgumentWidth
        self.maxTypeNodes = maxTypeNodes
        self.mutationTimeoutMs = mutationTimeoutMs
        self.requestTimeoutMs = requestTimeoutMs
    }
}

#if compiler(>=6)
extension ServiceConfig: Sendable {}
#endif


extension ServiceConfig: Equatable, Hashable {
    public static func ==(lhs: ServiceConfig, rhs: ServiceConfig) -> Bool {
        if lhs.defaultPageSize != rhs.defaultPageSize {
            return false
        }
        if lhs.enabledFeatures != rhs.enabledFeatures {
            return false
        }
        if lhs.maxMoveValueDepth != rhs.maxMoveValueDepth {
            return false
        }
        if lhs.maxOutputNodes != rhs.maxOutputNodes {
            return false
        }
        if lhs.maxPageSize != rhs.maxPageSize {
            return false
        }
        if lhs.maxQueryDepth != rhs.maxQueryDepth {
            return false
        }
        if lhs.maxQueryNodes != rhs.maxQueryNodes {
            return false
        }
        if lhs.maxQueryPayloadSize != rhs.maxQueryPayloadSize {
            return false
        }
        if lhs.maxTypeArgumentDepth != rhs.maxTypeArgumentDepth {
            return false
        }
        if lhs.maxTypeArgumentWidth != rhs.maxTypeArgumentWidth {
            return false
        }
        if lhs.maxTypeNodes != rhs.maxTypeNodes {
            return false
        }
        if lhs.mutationTimeoutMs != rhs.mutationTimeoutMs {
            return false
        }
        if lhs.requestTimeoutMs != rhs.requestTimeoutMs {
            return false
        }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(defaultPageSize)
        hasher.combine(enabledFeatures)
        hasher.combine(maxMoveValueDepth)
        hasher.combine(maxOutputNodes)
        hasher.combine(maxPageSize)
        hasher.combine(maxQueryDepth)
        hasher.combine(maxQueryNodes)
        hasher.combine(maxQueryPayloadSize)
        hasher.combine(maxTypeArgumentDepth)
        hasher.combine(maxTypeArgumentWidth)
        hasher.combine(maxTypeNodes)
        hasher.combine(mutationTimeoutMs)
        hasher.combine(requestTimeoutMs)
    }
}



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeServiceConfig: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ServiceConfig {
        return
            try ServiceConfig(
                defaultPageSize: FfiConverterInt32.read(from: &buf), 
                enabledFeatures: FfiConverterSequenceTypeFeature.read(from: &buf), 
                maxMoveValueDepth: FfiConverterInt32.read(from: &buf), 
                maxOutputNodes: FfiConverterInt32.read(from: &buf), 
                maxPageSize: FfiConverterInt32.read(from: &buf), 
                maxQueryDepth: FfiConverterInt32.read(from: &buf), 
                maxQueryNodes: FfiConverterInt32.read(from: &buf), 
                maxQueryPayloadSize: FfiConverterInt32.read(from: &buf), 
                maxTypeArgumentDepth: FfiConverterInt32.read(from: &buf), 
                maxTypeArgumentWidth: FfiConverterInt32.read(from: &buf), 
                maxTypeNodes: FfiConverterInt32.read(from: &buf), 
                mutationTimeoutMs: FfiConverterInt32.read(from: &buf), 
                requestTimeoutMs: FfiConverterInt32.read(from: &buf)
        )
    }

    public static func write(_ value: ServiceConfig, into buf: inout [UInt8]) {
        FfiConverterInt32.write(value.defaultPageSize, into: &buf)
        FfiConverterSequenceTypeFeature.write(value.enabledFeatures, into: &buf)
        FfiConverterInt32.write(value.maxMoveValueDepth, into: &buf)
        FfiConverterInt32.write(value.maxOutputNodes, into: &buf)
        FfiConverterInt32.write(value.maxPageSize, into: &buf)
        FfiConverterInt32.write(value.maxQueryDepth, into: &buf)
        FfiConverterInt32.write(value.maxQueryNodes, into: &buf)
        FfiConverterInt32.write(value.maxQueryPayloadSize, into: &buf)
        FfiConverterInt32.write(value.maxTypeArgumentDepth, into: &buf)
        FfiConverterInt32.write(value.maxTypeArgumentWidth, into: &buf)
        FfiConverterInt32.write(value.maxTypeNodes, into: &buf)
        FfiConverterInt32.write(value.mutationTimeoutMs, into: &buf)
        FfiConverterInt32.write(value.requestTimeoutMs, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeServiceConfig_lift(_ buf: RustBuffer) throws -> ServiceConfig {
    return try FfiConverterTypeServiceConfig.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeServiceConfig_lower(_ value: ServiceConfig) -> RustBuffer {
    return FfiConverterTypeServiceConfig.lower(value)
}


public struct SignedTransaction {
    public var transaction: Transaction
    public var signatures: [UserSignature]

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(transaction: Transaction, signatures: [UserSignature]) {
        self.transaction = transaction
        self.signatures = signatures
    }
}

#if compiler(>=6)
extension SignedTransaction: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSignedTransaction: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SignedTransaction {
        return
            try SignedTransaction(
                transaction: FfiConverterTypeTransaction.read(from: &buf), 
                signatures: FfiConverterSequenceTypeUserSignature.read(from: &buf)
        )
    }

    public static func write(_ value: SignedTransaction, into buf: inout [UInt8]) {
        FfiConverterTypeTransaction.write(value.transaction, into: &buf)
        FfiConverterSequenceTypeUserSignature.write(value.signatures, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSignedTransaction_lift(_ buf: RustBuffer) throws -> SignedTransaction {
    return try FfiConverterTypeSignedTransaction.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSignedTransaction_lower(_ value: SignedTransaction) -> RustBuffer {
    return FfiConverterTypeSignedTransaction.lower(value)
}


/**
 * A page of items returned by the GraphQL server.
 */
public struct SignedTransactionPage {
    /**
     * Information about the page, such as the cursor and whether there are
     * more pages.
     */
    public var pageInfo: PageInfo
    /**
     * The data returned by the server.
     */
    public var data: [SignedTransaction]

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * Information about the page, such as the cursor and whether there are
         * more pages.
         */pageInfo: PageInfo, 
        /**
         * The data returned by the server.
         */data: [SignedTransaction]) {
        self.pageInfo = pageInfo
        self.data = data
    }
}

#if compiler(>=6)
extension SignedTransactionPage: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSignedTransactionPage: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SignedTransactionPage {
        return
            try SignedTransactionPage(
                pageInfo: FfiConverterTypePageInfo.read(from: &buf), 
                data: FfiConverterSequenceTypeSignedTransaction.read(from: &buf)
        )
    }

    public static func write(_ value: SignedTransactionPage, into buf: inout [UInt8]) {
        FfiConverterTypePageInfo.write(value.pageInfo, into: &buf)
        FfiConverterSequenceTypeSignedTransaction.write(value.data, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSignedTransactionPage_lift(_ buf: RustBuffer) throws -> SignedTransactionPage {
    return try FfiConverterTypeSignedTransactionPage.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSignedTransactionPage_lower(_ value: SignedTransactionPage) -> RustBuffer {
    return FfiConverterTypeSignedTransactionPage.lower(value)
}


public struct TransactionDataEffects {
    public var tx: SignedTransaction
    public var effects: TransactionEffects

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(tx: SignedTransaction, effects: TransactionEffects) {
        self.tx = tx
        self.effects = effects
    }
}

#if compiler(>=6)
extension TransactionDataEffects: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeTransactionDataEffects: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> TransactionDataEffects {
        return
            try TransactionDataEffects(
                tx: FfiConverterTypeSignedTransaction.read(from: &buf), 
                effects: FfiConverterTypeTransactionEffects.read(from: &buf)
        )
    }

    public static func write(_ value: TransactionDataEffects, into buf: inout [UInt8]) {
        FfiConverterTypeSignedTransaction.write(value.tx, into: &buf)
        FfiConverterTypeTransactionEffects.write(value.effects, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionDataEffects_lift(_ buf: RustBuffer) throws -> TransactionDataEffects {
    return try FfiConverterTypeTransactionDataEffects.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionDataEffects_lower(_ value: TransactionDataEffects) -> RustBuffer {
    return FfiConverterTypeTransactionDataEffects.lower(value)
}


/**
 * A page of items returned by the GraphQL server.
 */
public struct TransactionDataEffectsPage {
    /**
     * Information about the page, such as the cursor and whether there are
     * more pages.
     */
    public var pageInfo: PageInfo
    /**
     * The data returned by the server.
     */
    public var data: [TransactionDataEffects]

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * Information about the page, such as the cursor and whether there are
         * more pages.
         */pageInfo: PageInfo, 
        /**
         * The data returned by the server.
         */data: [TransactionDataEffects]) {
        self.pageInfo = pageInfo
        self.data = data
    }
}

#if compiler(>=6)
extension TransactionDataEffectsPage: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeTransactionDataEffectsPage: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> TransactionDataEffectsPage {
        return
            try TransactionDataEffectsPage(
                pageInfo: FfiConverterTypePageInfo.read(from: &buf), 
                data: FfiConverterSequenceTypeTransactionDataEffects.read(from: &buf)
        )
    }

    public static func write(_ value: TransactionDataEffectsPage, into buf: inout [UInt8]) {
        FfiConverterTypePageInfo.write(value.pageInfo, into: &buf)
        FfiConverterSequenceTypeTransactionDataEffects.write(value.data, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionDataEffectsPage_lift(_ buf: RustBuffer) throws -> TransactionDataEffectsPage {
    return try FfiConverterTypeTransactionDataEffectsPage.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionDataEffectsPage_lower(_ value: TransactionDataEffectsPage) -> RustBuffer {
    return FfiConverterTypeTransactionDataEffectsPage.lower(value)
}


/**
 * A page of items returned by the GraphQL server.
 */
public struct TransactionEffectsPage {
    /**
     * Information about the page, such as the cursor and whether there are
     * more pages.
     */
    public var pageInfo: PageInfo
    /**
     * The data returned by the server.
     */
    public var data: [TransactionEffects]

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * Information about the page, such as the cursor and whether there are
         * more pages.
         */pageInfo: PageInfo, 
        /**
         * The data returned by the server.
         */data: [TransactionEffects]) {
        self.pageInfo = pageInfo
        self.data = data
    }
}

#if compiler(>=6)
extension TransactionEffectsPage: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeTransactionEffectsPage: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> TransactionEffectsPage {
        return
            try TransactionEffectsPage(
                pageInfo: FfiConverterTypePageInfo.read(from: &buf), 
                data: FfiConverterSequenceTypeTransactionEffects.read(from: &buf)
        )
    }

    public static func write(_ value: TransactionEffectsPage, into buf: inout [UInt8]) {
        FfiConverterTypePageInfo.write(value.pageInfo, into: &buf)
        FfiConverterSequenceTypeTransactionEffects.write(value.data, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionEffectsPage_lift(_ buf: RustBuffer) throws -> TransactionEffectsPage {
    return try FfiConverterTypeTransactionEffectsPage.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionEffectsPage_lower(_ value: TransactionEffectsPage) -> RustBuffer {
    return FfiConverterTypeTransactionEffectsPage.lower(value)
}


/**
 * Version 1 of TransactionEffects
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * effects-v1 = execution-status
 * u64                                ; epoch
 * gas-cost-summary
 * digest                             ; transaction digest
 * (option u32)                       ; gas object index
 * (option digest)                    ; events digest
 * (vector digest)                    ; list of transaction dependencies
 * u64                                ; lamport version
 * (vector changed-object)
 * (vector unchanged-shared-object)
 * (option digest)                    ; auxiliary data digest
 * ```
 */
public struct TransactionEffectsV1 {
    /**
     * The status of the execution
     */
    public var status: ExecutionStatus
    /**
     * The epoch when this transaction was executed.
     */
    public var epoch: UInt64
    /**
     * The gas used by this transaction
     */
    public var gasUsed: GasCostSummary
    /**
     * The transaction digest
     */
    public var transactionDigest: Digest
    /**
     * The updated gas object reference, as an index into the `changed_objects`
     * vector. Having a dedicated field for convenient access.
     * System transaction that don't require gas will leave this as None.
     */
    public var gasObjectIndex: UInt32?
    /**
     * The digest of the events emitted during execution,
     * can be None if the transaction does not emit any event.
     */
    public var eventsDigest: Digest?
    /**
     * The set of transaction digests this transaction depends on.
     */
    public var dependencies: [Digest]
    /**
     * The version number of all the written Move objects by this transaction.
     */
    public var lamportVersion: UInt64
    /**
     * Objects whose state are changed in the object store.
     */
    public var changedObjects: [ChangedObject]
    /**
     * Shared objects that are not mutated in this transaction. Unlike owned
     * objects, read-only shared objects' version are not committed in the
     * transaction, and in order for a node to catch up and execute it
     * without consensus sequencing, the version needs to be committed in
     * the effects.
     */
    public var unchangedSharedObjects: [UnchangedSharedObject]
    /**
     * Auxiliary data that are not protocol-critical, generated as part of the
     * effects but are stored separately. Storing it separately allows us
     * to avoid bloating the effects with data that are not critical.
     * It also provides more flexibility on the format and type of the data.
     */
    public var auxiliaryDataDigest: Digest?

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * The status of the execution
         */status: ExecutionStatus, 
        /**
         * The epoch when this transaction was executed.
         */epoch: UInt64, 
        /**
         * The gas used by this transaction
         */gasUsed: GasCostSummary, 
        /**
         * The transaction digest
         */transactionDigest: Digest, 
        /**
         * The updated gas object reference, as an index into the `changed_objects`
         * vector. Having a dedicated field for convenient access.
         * System transaction that don't require gas will leave this as None.
         */gasObjectIndex: UInt32? = nil, 
        /**
         * The digest of the events emitted during execution,
         * can be None if the transaction does not emit any event.
         */eventsDigest: Digest? = nil, 
        /**
         * The set of transaction digests this transaction depends on.
         */dependencies: [Digest], 
        /**
         * The version number of all the written Move objects by this transaction.
         */lamportVersion: UInt64, 
        /**
         * Objects whose state are changed in the object store.
         */changedObjects: [ChangedObject], 
        /**
         * Shared objects that are not mutated in this transaction. Unlike owned
         * objects, read-only shared objects' version are not committed in the
         * transaction, and in order for a node to catch up and execute it
         * without consensus sequencing, the version needs to be committed in
         * the effects.
         */unchangedSharedObjects: [UnchangedSharedObject], 
        /**
         * Auxiliary data that are not protocol-critical, generated as part of the
         * effects but are stored separately. Storing it separately allows us
         * to avoid bloating the effects with data that are not critical.
         * It also provides more flexibility on the format and type of the data.
         */auxiliaryDataDigest: Digest? = nil) {
        self.status = status
        self.epoch = epoch
        self.gasUsed = gasUsed
        self.transactionDigest = transactionDigest
        self.gasObjectIndex = gasObjectIndex
        self.eventsDigest = eventsDigest
        self.dependencies = dependencies
        self.lamportVersion = lamportVersion
        self.changedObjects = changedObjects
        self.unchangedSharedObjects = unchangedSharedObjects
        self.auxiliaryDataDigest = auxiliaryDataDigest
    }
}

#if compiler(>=6)
extension TransactionEffectsV1: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeTransactionEffectsV1: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> TransactionEffectsV1 {
        return
            try TransactionEffectsV1(
                status: FfiConverterTypeExecutionStatus.read(from: &buf), 
                epoch: FfiConverterUInt64.read(from: &buf), 
                gasUsed: FfiConverterTypeGasCostSummary.read(from: &buf), 
                transactionDigest: FfiConverterTypeDigest.read(from: &buf), 
                gasObjectIndex: FfiConverterOptionUInt32.read(from: &buf), 
                eventsDigest: FfiConverterOptionTypeDigest.read(from: &buf), 
                dependencies: FfiConverterSequenceTypeDigest.read(from: &buf), 
                lamportVersion: FfiConverterUInt64.read(from: &buf), 
                changedObjects: FfiConverterSequenceTypeChangedObject.read(from: &buf), 
                unchangedSharedObjects: FfiConverterSequenceTypeUnchangedSharedObject.read(from: &buf), 
                auxiliaryDataDigest: FfiConverterOptionTypeDigest.read(from: &buf)
        )
    }

    public static func write(_ value: TransactionEffectsV1, into buf: inout [UInt8]) {
        FfiConverterTypeExecutionStatus.write(value.status, into: &buf)
        FfiConverterUInt64.write(value.epoch, into: &buf)
        FfiConverterTypeGasCostSummary.write(value.gasUsed, into: &buf)
        FfiConverterTypeDigest.write(value.transactionDigest, into: &buf)
        FfiConverterOptionUInt32.write(value.gasObjectIndex, into: &buf)
        FfiConverterOptionTypeDigest.write(value.eventsDigest, into: &buf)
        FfiConverterSequenceTypeDigest.write(value.dependencies, into: &buf)
        FfiConverterUInt64.write(value.lamportVersion, into: &buf)
        FfiConverterSequenceTypeChangedObject.write(value.changedObjects, into: &buf)
        FfiConverterSequenceTypeUnchangedSharedObject.write(value.unchangedSharedObjects, into: &buf)
        FfiConverterOptionTypeDigest.write(value.auxiliaryDataDigest, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionEffectsV1_lift(_ buf: RustBuffer) throws -> TransactionEffectsV1 {
    return try FfiConverterTypeTransactionEffectsV1.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionEffectsV1_lower(_ value: TransactionEffectsV1) -> RustBuffer {
    return FfiConverterTypeTransactionEffectsV1.lower(value)
}


public struct TransactionMetadata {
    public var gasBudget: UInt64?
    public var gasObjects: [ObjectRef]?
    public var gasPrice: UInt64?
    public var gasSponsor: Address?
    public var sender: Address?

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(gasBudget: UInt64? = nil, gasObjects: [ObjectRef]? = nil, gasPrice: UInt64? = nil, gasSponsor: Address? = nil, sender: Address? = nil) {
        self.gasBudget = gasBudget
        self.gasObjects = gasObjects
        self.gasPrice = gasPrice
        self.gasSponsor = gasSponsor
        self.sender = sender
    }
}

#if compiler(>=6)
extension TransactionMetadata: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeTransactionMetadata: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> TransactionMetadata {
        return
            try TransactionMetadata(
                gasBudget: FfiConverterOptionUInt64.read(from: &buf), 
                gasObjects: FfiConverterOptionSequenceTypeObjectRef.read(from: &buf), 
                gasPrice: FfiConverterOptionUInt64.read(from: &buf), 
                gasSponsor: FfiConverterOptionTypeAddress.read(from: &buf), 
                sender: FfiConverterOptionTypeAddress.read(from: &buf)
        )
    }

    public static func write(_ value: TransactionMetadata, into buf: inout [UInt8]) {
        FfiConverterOptionUInt64.write(value.gasBudget, into: &buf)
        FfiConverterOptionSequenceTypeObjectRef.write(value.gasObjects, into: &buf)
        FfiConverterOptionUInt64.write(value.gasPrice, into: &buf)
        FfiConverterOptionTypeAddress.write(value.gasSponsor, into: &buf)
        FfiConverterOptionTypeAddress.write(value.sender, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionMetadata_lift(_ buf: RustBuffer) throws -> TransactionMetadata {
    return try FfiConverterTypeTransactionMetadata.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionMetadata_lower(_ value: TransactionMetadata) -> RustBuffer {
    return FfiConverterTypeTransactionMetadata.lower(value)
}


/**
 * The result of an async sign call containing the `UserSignature`.
 */
public struct TransactionSignerFnOutput {
    public var signature: UserSignature

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(signature: UserSignature) {
        self.signature = signature
    }
}

#if compiler(>=6)
extension TransactionSignerFnOutput: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeTransactionSignerFnOutput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> TransactionSignerFnOutput {
        return
            try TransactionSignerFnOutput(
                signature: FfiConverterTypeUserSignature.read(from: &buf)
        )
    }

    public static func write(_ value: TransactionSignerFnOutput, into buf: inout [UInt8]) {
        FfiConverterTypeUserSignature.write(value.signature, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionSignerFnOutput_lift(_ buf: RustBuffer) throws -> TransactionSignerFnOutput {
    return try FfiConverterTypeTransactionSignerFnOutput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionSignerFnOutput_lower(_ value: TransactionSignerFnOutput) -> RustBuffer {
    return FfiConverterTypeTransactionSignerFnOutput.lower(value)
}


public struct TransactionsFilter {
    public var function: String?
    public var kind: TransactionBlockKindInput?
    public var afterCheckpoint: UInt64?
    public var atCheckpoint: UInt64?
    public var beforeCheckpoint: UInt64?
    public var signAddress: Address?
    public var recvAddress: Address?
    public var inputObject: ObjectId?
    public var changedObject: ObjectId?
    public var transactionIds: [String]?
    public var wrappedOrDeletedObject: ObjectId?

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(function: String? = nil, kind: TransactionBlockKindInput? = nil, afterCheckpoint: UInt64? = nil, atCheckpoint: UInt64? = nil, beforeCheckpoint: UInt64? = nil, signAddress: Address? = nil, recvAddress: Address? = nil, inputObject: ObjectId? = nil, changedObject: ObjectId? = nil, transactionIds: [String]? = nil, wrappedOrDeletedObject: ObjectId? = nil) {
        self.function = function
        self.kind = kind
        self.afterCheckpoint = afterCheckpoint
        self.atCheckpoint = atCheckpoint
        self.beforeCheckpoint = beforeCheckpoint
        self.signAddress = signAddress
        self.recvAddress = recvAddress
        self.inputObject = inputObject
        self.changedObject = changedObject
        self.transactionIds = transactionIds
        self.wrappedOrDeletedObject = wrappedOrDeletedObject
    }
}

#if compiler(>=6)
extension TransactionsFilter: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeTransactionsFilter: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> TransactionsFilter {
        return
            try TransactionsFilter(
                function: FfiConverterOptionString.read(from: &buf), 
                kind: FfiConverterOptionTypeTransactionBlockKindInput.read(from: &buf), 
                afterCheckpoint: FfiConverterOptionUInt64.read(from: &buf), 
                atCheckpoint: FfiConverterOptionUInt64.read(from: &buf), 
                beforeCheckpoint: FfiConverterOptionUInt64.read(from: &buf), 
                signAddress: FfiConverterOptionTypeAddress.read(from: &buf), 
                recvAddress: FfiConverterOptionTypeAddress.read(from: &buf), 
                inputObject: FfiConverterOptionTypeObjectId.read(from: &buf), 
                changedObject: FfiConverterOptionTypeObjectId.read(from: &buf), 
                transactionIds: FfiConverterOptionSequenceString.read(from: &buf), 
                wrappedOrDeletedObject: FfiConverterOptionTypeObjectId.read(from: &buf)
        )
    }

    public static func write(_ value: TransactionsFilter, into buf: inout [UInt8]) {
        FfiConverterOptionString.write(value.function, into: &buf)
        FfiConverterOptionTypeTransactionBlockKindInput.write(value.kind, into: &buf)
        FfiConverterOptionUInt64.write(value.afterCheckpoint, into: &buf)
        FfiConverterOptionUInt64.write(value.atCheckpoint, into: &buf)
        FfiConverterOptionUInt64.write(value.beforeCheckpoint, into: &buf)
        FfiConverterOptionTypeAddress.write(value.signAddress, into: &buf)
        FfiConverterOptionTypeAddress.write(value.recvAddress, into: &buf)
        FfiConverterOptionTypeObjectId.write(value.inputObject, into: &buf)
        FfiConverterOptionTypeObjectId.write(value.changedObject, into: &buf)
        FfiConverterOptionSequenceString.write(value.transactionIds, into: &buf)
        FfiConverterOptionTypeObjectId.write(value.wrappedOrDeletedObject, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionsFilter_lift(_ buf: RustBuffer) throws -> TransactionsFilter {
    return try FfiConverterTypeTransactionsFilter.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionsFilter_lower(_ value: TransactionsFilter) -> RustBuffer {
    return FfiConverterTypeTransactionsFilter.lower(value)
}


/**
 * Identifies a struct and the module it was defined in
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * type-origin = identifier identifier object-id
 * ```
 */
public struct TypeOrigin {
    public var moduleName: Identifier
    public var structName: Identifier
    public var package: ObjectId

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(moduleName: Identifier, structName: Identifier, package: ObjectId) {
        self.moduleName = moduleName
        self.structName = structName
        self.package = package
    }
}

#if compiler(>=6)
extension TypeOrigin: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeTypeOrigin: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> TypeOrigin {
        return
            try TypeOrigin(
                moduleName: FfiConverterTypeIdentifier.read(from: &buf), 
                structName: FfiConverterTypeIdentifier.read(from: &buf), 
                package: FfiConverterTypeObjectId.read(from: &buf)
        )
    }

    public static func write(_ value: TypeOrigin, into buf: inout [UInt8]) {
        FfiConverterTypeIdentifier.write(value.moduleName, into: &buf)
        FfiConverterTypeIdentifier.write(value.structName, into: &buf)
        FfiConverterTypeObjectId.write(value.package, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTypeOrigin_lift(_ buf: RustBuffer) throws -> TypeOrigin {
    return try FfiConverterTypeTypeOrigin.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTypeOrigin_lower(_ value: TypeOrigin) -> RustBuffer {
    return FfiConverterTypeTypeOrigin.lower(value)
}


/**
 * A shared object that wasn't changed during execution
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * unchanged-shared-object = object-id unchanged-shared-object-kind
 * ```
 */
public struct UnchangedSharedObject {
    public var objectId: ObjectId
    public var kind: UnchangedSharedKind

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(objectId: ObjectId, kind: UnchangedSharedKind) {
        self.objectId = objectId
        self.kind = kind
    }
}

#if compiler(>=6)
extension UnchangedSharedObject: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeUnchangedSharedObject: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> UnchangedSharedObject {
        return
            try UnchangedSharedObject(
                objectId: FfiConverterTypeObjectId.read(from: &buf), 
                kind: FfiConverterTypeUnchangedSharedKind.read(from: &buf)
        )
    }

    public static func write(_ value: UnchangedSharedObject, into buf: inout [UInt8]) {
        FfiConverterTypeObjectId.write(value.objectId, into: &buf)
        FfiConverterTypeUnchangedSharedKind.write(value.kind, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeUnchangedSharedObject_lift(_ buf: RustBuffer) throws -> UnchangedSharedObject {
    return try FfiConverterTypeUnchangedSharedObject.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeUnchangedSharedObject_lower(_ value: UnchangedSharedObject) -> RustBuffer {
    return FfiConverterTypeUnchangedSharedObject.lower(value)
}


/**
 * Upgraded package info for the linkage table
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * upgrade-info = object-id u64
 * ```
 */
public struct UpgradeInfo {
    /**
     * Id of the upgraded packages
     */
    public var upgradedId: ObjectId
    /**
     * Version of the upgraded package
     */
    public var upgradedVersion: UInt64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * Id of the upgraded packages
         */upgradedId: ObjectId, 
        /**
         * Version of the upgraded package
         */upgradedVersion: UInt64) {
        self.upgradedId = upgradedId
        self.upgradedVersion = upgradedVersion
    }
}

#if compiler(>=6)
extension UpgradeInfo: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeUpgradeInfo: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> UpgradeInfo {
        return
            try UpgradeInfo(
                upgradedId: FfiConverterTypeObjectId.read(from: &buf), 
                upgradedVersion: FfiConverterUInt64.read(from: &buf)
        )
    }

    public static func write(_ value: UpgradeInfo, into buf: inout [UInt8]) {
        FfiConverterTypeObjectId.write(value.upgradedId, into: &buf)
        FfiConverterUInt64.write(value.upgradedVersion, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeUpgradeInfo_lift(_ buf: RustBuffer) throws -> UpgradeInfo {
    return try FfiConverterTypeUpgradeInfo.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeUpgradeInfo_lower(_ value: UpgradeInfo) -> RustBuffer {
    return FfiConverterTypeUpgradeInfo.lower(value)
}


/**
 * Represents a validator in the system.
 */
public struct Validator {
    /**
     * The APY of this validator in basis points.
     * To get the APY in percentage, divide by 100.
     */
    public var apy: Int32?
    /**
     * The validator's address.
     */
    public var address: Address
    /**
     * The fee charged by the validator for staking services.
     */
    public var commissionRate: Int32?
    /**
     * Validator's credentials.
     */
    public var credentials: ValidatorCredentials?
    /**
     * Validator's description.
     */
    public var description: String?
    /**
     * Number of exchange rates in the table.
     */
    public var exchangeRatesSize: UInt64?
    /**
     * The reference gas price for this epoch.
     */
    public var gasPrice: UInt64?
    /**
     * Validator's name.
     */
    public var name: String?
    /**
     * Validator's url containing their custom image.
     */
    public var imageUrl: String?
    /**
     * The proposed next epoch fee for the validator's staking services.
     */
    public var nextEpochCommissionRate: Int32?
    /**
     * Validator's credentials for the next epoch.
     */
    public var nextEpochCredentials: ValidatorCredentials?
    /**
     * The validator's gas price quote for the next epoch.
     */
    public var nextEpochGasPrice: UInt64?
    /**
     * The total number of IOTA tokens in this pool plus
     * the pending stake amount for this epoch.
     */
    public var nextEpochStake: UInt64?
    /**
     * The validator's current valid `Cap` object. Validators can delegate
     * the operation ability to another address. The address holding this `Cap`
     * object can then update the reference gas price and tallying rule on
     * behalf of the validator.
     */
    public var operationCap: Data?
    /**
     * Pending pool token withdrawn during the current epoch, emptied at epoch
     * boundaries. Zero for past epochs.
     */
    public var pendingPoolTokenWithdraw: UInt64?
    /**
     * Pending stake amount for the current epoch, emptied at epoch boundaries.
     * Zero for past epochs.
     */
    public var pendingStake: UInt64?
    /**
     * Pending stake withdrawn during the current epoch, emptied at epoch
     * boundaries. Zero for past epochs.
     */
    public var pendingTotalIotaWithdraw: UInt64?
    /**
     * Total number of pool tokens issued by the pool.
     */
    public var poolTokenBalance: UInt64?
    /**
     * Validator's homepage URL.
     */
    public var projectUrl: String?
    /**
     * The epoch stake rewards will be added here at the end of each epoch.
     */
    public var rewardsPool: UInt64?
    /**
     * The epoch at which this pool became active.
     */
    public var stakingPoolActivationEpoch: UInt64?
    /**
     * The ID of this validator's `0x3::staking_pool::StakingPool`.
     */
    public var stakingPoolId: ObjectId
    /**
     * The total number of IOTA tokens in this pool.
     */
    public var stakingPoolIotaBalance: UInt64?
    /**
     * The voting power of this validator in basis points (e.g., 100 = 1%
     * voting power).
     */
    public var votingPower: Int32?

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * The APY of this validator in basis points.
         * To get the APY in percentage, divide by 100.
         */apy: Int32? = nil, 
        /**
         * The validator's address.
         */address: Address, 
        /**
         * The fee charged by the validator for staking services.
         */commissionRate: Int32? = nil, 
        /**
         * Validator's credentials.
         */credentials: ValidatorCredentials? = nil, 
        /**
         * Validator's description.
         */description: String? = nil, 
        /**
         * Number of exchange rates in the table.
         */exchangeRatesSize: UInt64? = nil, 
        /**
         * The reference gas price for this epoch.
         */gasPrice: UInt64? = nil, 
        /**
         * Validator's name.
         */name: String? = nil, 
        /**
         * Validator's url containing their custom image.
         */imageUrl: String? = nil, 
        /**
         * The proposed next epoch fee for the validator's staking services.
         */nextEpochCommissionRate: Int32? = nil, 
        /**
         * Validator's credentials for the next epoch.
         */nextEpochCredentials: ValidatorCredentials? = nil, 
        /**
         * The validator's gas price quote for the next epoch.
         */nextEpochGasPrice: UInt64? = nil, 
        /**
         * The total number of IOTA tokens in this pool plus
         * the pending stake amount for this epoch.
         */nextEpochStake: UInt64? = nil, 
        /**
         * The validator's current valid `Cap` object. Validators can delegate
         * the operation ability to another address. The address holding this `Cap`
         * object can then update the reference gas price and tallying rule on
         * behalf of the validator.
         */operationCap: Data? = nil, 
        /**
         * Pending pool token withdrawn during the current epoch, emptied at epoch
         * boundaries. Zero for past epochs.
         */pendingPoolTokenWithdraw: UInt64? = nil, 
        /**
         * Pending stake amount for the current epoch, emptied at epoch boundaries.
         * Zero for past epochs.
         */pendingStake: UInt64? = nil, 
        /**
         * Pending stake withdrawn during the current epoch, emptied at epoch
         * boundaries. Zero for past epochs.
         */pendingTotalIotaWithdraw: UInt64? = nil, 
        /**
         * Total number of pool tokens issued by the pool.
         */poolTokenBalance: UInt64? = nil, 
        /**
         * Validator's homepage URL.
         */projectUrl: String? = nil, 
        /**
         * The epoch stake rewards will be added here at the end of each epoch.
         */rewardsPool: UInt64? = nil, 
        /**
         * The epoch at which this pool became active.
         */stakingPoolActivationEpoch: UInt64? = nil, 
        /**
         * The ID of this validator's `0x3::staking_pool::StakingPool`.
         */stakingPoolId: ObjectId, 
        /**
         * The total number of IOTA tokens in this pool.
         */stakingPoolIotaBalance: UInt64? = nil, 
        /**
         * The voting power of this validator in basis points (e.g., 100 = 1%
         * voting power).
         */votingPower: Int32? = nil) {
        self.apy = apy
        self.address = address
        self.commissionRate = commissionRate
        self.credentials = credentials
        self.description = description
        self.exchangeRatesSize = exchangeRatesSize
        self.gasPrice = gasPrice
        self.name = name
        self.imageUrl = imageUrl
        self.nextEpochCommissionRate = nextEpochCommissionRate
        self.nextEpochCredentials = nextEpochCredentials
        self.nextEpochGasPrice = nextEpochGasPrice
        self.nextEpochStake = nextEpochStake
        self.operationCap = operationCap
        self.pendingPoolTokenWithdraw = pendingPoolTokenWithdraw
        self.pendingStake = pendingStake
        self.pendingTotalIotaWithdraw = pendingTotalIotaWithdraw
        self.poolTokenBalance = poolTokenBalance
        self.projectUrl = projectUrl
        self.rewardsPool = rewardsPool
        self.stakingPoolActivationEpoch = stakingPoolActivationEpoch
        self.stakingPoolId = stakingPoolId
        self.stakingPoolIotaBalance = stakingPoolIotaBalance
        self.votingPower = votingPower
    }
}

#if compiler(>=6)
extension Validator: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeValidator: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Validator {
        return
            try Validator(
                apy: FfiConverterOptionInt32.read(from: &buf), 
                address: FfiConverterTypeAddress.read(from: &buf), 
                commissionRate: FfiConverterOptionInt32.read(from: &buf), 
                credentials: FfiConverterOptionTypeValidatorCredentials.read(from: &buf), 
                description: FfiConverterOptionString.read(from: &buf), 
                exchangeRatesSize: FfiConverterOptionUInt64.read(from: &buf), 
                gasPrice: FfiConverterOptionUInt64.read(from: &buf), 
                name: FfiConverterOptionString.read(from: &buf), 
                imageUrl: FfiConverterOptionString.read(from: &buf), 
                nextEpochCommissionRate: FfiConverterOptionInt32.read(from: &buf), 
                nextEpochCredentials: FfiConverterOptionTypeValidatorCredentials.read(from: &buf), 
                nextEpochGasPrice: FfiConverterOptionUInt64.read(from: &buf), 
                nextEpochStake: FfiConverterOptionUInt64.read(from: &buf), 
                operationCap: FfiConverterOptionData.read(from: &buf), 
                pendingPoolTokenWithdraw: FfiConverterOptionUInt64.read(from: &buf), 
                pendingStake: FfiConverterOptionUInt64.read(from: &buf), 
                pendingTotalIotaWithdraw: FfiConverterOptionUInt64.read(from: &buf), 
                poolTokenBalance: FfiConverterOptionUInt64.read(from: &buf), 
                projectUrl: FfiConverterOptionString.read(from: &buf), 
                rewardsPool: FfiConverterOptionUInt64.read(from: &buf), 
                stakingPoolActivationEpoch: FfiConverterOptionUInt64.read(from: &buf), 
                stakingPoolId: FfiConverterTypeObjectId.read(from: &buf), 
                stakingPoolIotaBalance: FfiConverterOptionUInt64.read(from: &buf), 
                votingPower: FfiConverterOptionInt32.read(from: &buf)
        )
    }

    public static func write(_ value: Validator, into buf: inout [UInt8]) {
        FfiConverterOptionInt32.write(value.apy, into: &buf)
        FfiConverterTypeAddress.write(value.address, into: &buf)
        FfiConverterOptionInt32.write(value.commissionRate, into: &buf)
        FfiConverterOptionTypeValidatorCredentials.write(value.credentials, into: &buf)
        FfiConverterOptionString.write(value.description, into: &buf)
        FfiConverterOptionUInt64.write(value.exchangeRatesSize, into: &buf)
        FfiConverterOptionUInt64.write(value.gasPrice, into: &buf)
        FfiConverterOptionString.write(value.name, into: &buf)
        FfiConverterOptionString.write(value.imageUrl, into: &buf)
        FfiConverterOptionInt32.write(value.nextEpochCommissionRate, into: &buf)
        FfiConverterOptionTypeValidatorCredentials.write(value.nextEpochCredentials, into: &buf)
        FfiConverterOptionUInt64.write(value.nextEpochGasPrice, into: &buf)
        FfiConverterOptionUInt64.write(value.nextEpochStake, into: &buf)
        FfiConverterOptionData.write(value.operationCap, into: &buf)
        FfiConverterOptionUInt64.write(value.pendingPoolTokenWithdraw, into: &buf)
        FfiConverterOptionUInt64.write(value.pendingStake, into: &buf)
        FfiConverterOptionUInt64.write(value.pendingTotalIotaWithdraw, into: &buf)
        FfiConverterOptionUInt64.write(value.poolTokenBalance, into: &buf)
        FfiConverterOptionString.write(value.projectUrl, into: &buf)
        FfiConverterOptionUInt64.write(value.rewardsPool, into: &buf)
        FfiConverterOptionUInt64.write(value.stakingPoolActivationEpoch, into: &buf)
        FfiConverterTypeObjectId.write(value.stakingPoolId, into: &buf)
        FfiConverterOptionUInt64.write(value.stakingPoolIotaBalance, into: &buf)
        FfiConverterOptionInt32.write(value.votingPower, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeValidator_lift(_ buf: RustBuffer) throws -> Validator {
    return try FfiConverterTypeValidator.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeValidator_lower(_ value: Validator) -> RustBuffer {
    return FfiConverterTypeValidator.lower(value)
}


/**
 * The Validator Set for a particular epoch.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * validator-committee = u64 ; epoch
 * (vector validator-committee-member)
 * ```
 */
public struct ValidatorCommittee {
    public var epoch: UInt64
    public var members: [ValidatorCommitteeMember]

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(epoch: UInt64, members: [ValidatorCommitteeMember]) {
        self.epoch = epoch
        self.members = members
    }
}

#if compiler(>=6)
extension ValidatorCommittee: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeValidatorCommittee: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ValidatorCommittee {
        return
            try ValidatorCommittee(
                epoch: FfiConverterUInt64.read(from: &buf), 
                members: FfiConverterSequenceTypeValidatorCommitteeMember.read(from: &buf)
        )
    }

    public static func write(_ value: ValidatorCommittee, into buf: inout [UInt8]) {
        FfiConverterUInt64.write(value.epoch, into: &buf)
        FfiConverterSequenceTypeValidatorCommitteeMember.write(value.members, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeValidatorCommittee_lift(_ buf: RustBuffer) throws -> ValidatorCommittee {
    return try FfiConverterTypeValidatorCommittee.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeValidatorCommittee_lower(_ value: ValidatorCommittee) -> RustBuffer {
    return FfiConverterTypeValidatorCommittee.lower(value)
}


/**
 * A member of a Validator Committee
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * validator-committee-member = bls-public-key
 * u64 ; stake
 * ```
 */
public struct ValidatorCommitteeMember {
    public var publicKey: Bls12381PublicKey
    public var stake: UInt64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(publicKey: Bls12381PublicKey, stake: UInt64) {
        self.publicKey = publicKey
        self.stake = stake
    }
}

#if compiler(>=6)
extension ValidatorCommitteeMember: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeValidatorCommitteeMember: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ValidatorCommitteeMember {
        return
            try ValidatorCommitteeMember(
                publicKey: FfiConverterTypeBls12381PublicKey.read(from: &buf), 
                stake: FfiConverterUInt64.read(from: &buf)
        )
    }

    public static func write(_ value: ValidatorCommitteeMember, into buf: inout [UInt8]) {
        FfiConverterTypeBls12381PublicKey.write(value.publicKey, into: &buf)
        FfiConverterUInt64.write(value.stake, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeValidatorCommitteeMember_lift(_ buf: RustBuffer) throws -> ValidatorCommitteeMember {
    return try FfiConverterTypeValidatorCommitteeMember.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeValidatorCommitteeMember_lower(_ value: ValidatorCommitteeMember) -> RustBuffer {
    return FfiConverterTypeValidatorCommitteeMember.lower(value)
}


public struct ValidatorConnection {
    public var pageInfo: PageInfo
    public var nodes: [Validator]

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(pageInfo: PageInfo, nodes: [Validator]) {
        self.pageInfo = pageInfo
        self.nodes = nodes
    }
}

#if compiler(>=6)
extension ValidatorConnection: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeValidatorConnection: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ValidatorConnection {
        return
            try ValidatorConnection(
                pageInfo: FfiConverterTypePageInfo.read(from: &buf), 
                nodes: FfiConverterSequenceTypeValidator.read(from: &buf)
        )
    }

    public static func write(_ value: ValidatorConnection, into buf: inout [UInt8]) {
        FfiConverterTypePageInfo.write(value.pageInfo, into: &buf)
        FfiConverterSequenceTypeValidator.write(value.nodes, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeValidatorConnection_lift(_ buf: RustBuffer) throws -> ValidatorConnection {
    return try FfiConverterTypeValidatorConnection.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeValidatorConnection_lower(_ value: ValidatorConnection) -> RustBuffer {
    return FfiConverterTypeValidatorConnection.lower(value)
}


/**
 * The credentials related fields associated with a validator.
 */
public struct ValidatorCredentials {
    public var authorityPubKey: Base64?
    public var networkPubKey: Base64?
    public var protocolPubKey: Base64?
    public var proofOfPossession: Base64?
    public var netAddress: String?
    public var p2pAddress: String?
    public var primaryAddress: String?

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(authorityPubKey: Base64? = nil, networkPubKey: Base64? = nil, protocolPubKey: Base64? = nil, proofOfPossession: Base64? = nil, netAddress: String? = nil, p2pAddress: String? = nil, primaryAddress: String? = nil) {
        self.authorityPubKey = authorityPubKey
        self.networkPubKey = networkPubKey
        self.protocolPubKey = protocolPubKey
        self.proofOfPossession = proofOfPossession
        self.netAddress = netAddress
        self.p2pAddress = p2pAddress
        self.primaryAddress = primaryAddress
    }
}

#if compiler(>=6)
extension ValidatorCredentials: Sendable {}
#endif


extension ValidatorCredentials: Equatable, Hashable {
    public static func ==(lhs: ValidatorCredentials, rhs: ValidatorCredentials) -> Bool {
        if lhs.authorityPubKey != rhs.authorityPubKey {
            return false
        }
        if lhs.networkPubKey != rhs.networkPubKey {
            return false
        }
        if lhs.protocolPubKey != rhs.protocolPubKey {
            return false
        }
        if lhs.proofOfPossession != rhs.proofOfPossession {
            return false
        }
        if lhs.netAddress != rhs.netAddress {
            return false
        }
        if lhs.p2pAddress != rhs.p2pAddress {
            return false
        }
        if lhs.primaryAddress != rhs.primaryAddress {
            return false
        }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(authorityPubKey)
        hasher.combine(networkPubKey)
        hasher.combine(protocolPubKey)
        hasher.combine(proofOfPossession)
        hasher.combine(netAddress)
        hasher.combine(p2pAddress)
        hasher.combine(primaryAddress)
    }
}



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeValidatorCredentials: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ValidatorCredentials {
        return
            try ValidatorCredentials(
                authorityPubKey: FfiConverterOptionTypeBase64.read(from: &buf), 
                networkPubKey: FfiConverterOptionTypeBase64.read(from: &buf), 
                protocolPubKey: FfiConverterOptionTypeBase64.read(from: &buf), 
                proofOfPossession: FfiConverterOptionTypeBase64.read(from: &buf), 
                netAddress: FfiConverterOptionString.read(from: &buf), 
                p2pAddress: FfiConverterOptionString.read(from: &buf), 
                primaryAddress: FfiConverterOptionString.read(from: &buf)
        )
    }

    public static func write(_ value: ValidatorCredentials, into buf: inout [UInt8]) {
        FfiConverterOptionTypeBase64.write(value.authorityPubKey, into: &buf)
        FfiConverterOptionTypeBase64.write(value.networkPubKey, into: &buf)
        FfiConverterOptionTypeBase64.write(value.protocolPubKey, into: &buf)
        FfiConverterOptionTypeBase64.write(value.proofOfPossession, into: &buf)
        FfiConverterOptionString.write(value.netAddress, into: &buf)
        FfiConverterOptionString.write(value.p2pAddress, into: &buf)
        FfiConverterOptionString.write(value.primaryAddress, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeValidatorCredentials_lift(_ buf: RustBuffer) throws -> ValidatorCredentials {
    return try FfiConverterTypeValidatorCredentials.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeValidatorCredentials_lower(_ value: ValidatorCredentials) -> RustBuffer {
    return FfiConverterTypeValidatorCredentials.lower(value)
}


/**
 * A page of items returned by the GraphQL server.
 */
public struct ValidatorPage {
    /**
     * Information about the page, such as the cursor and whether there are
     * more pages.
     */
    public var pageInfo: PageInfo
    /**
     * The data returned by the server.
     */
    public var data: [Validator]

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * Information about the page, such as the cursor and whether there are
         * more pages.
         */pageInfo: PageInfo, 
        /**
         * The data returned by the server.
         */data: [Validator]) {
        self.pageInfo = pageInfo
        self.data = data
    }
}

#if compiler(>=6)
extension ValidatorPage: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeValidatorPage: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ValidatorPage {
        return
            try ValidatorPage(
                pageInfo: FfiConverterTypePageInfo.read(from: &buf), 
                data: FfiConverterSequenceTypeValidator.read(from: &buf)
        )
    }

    public static func write(_ value: ValidatorPage, into buf: inout [UInt8]) {
        FfiConverterTypePageInfo.write(value.pageInfo, into: &buf)
        FfiConverterSequenceTypeValidator.write(value.data, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeValidatorPage_lift(_ buf: RustBuffer) throws -> ValidatorPage {
    return try FfiConverterTypeValidatorPage.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeValidatorPage_lower(_ value: ValidatorPage) -> RustBuffer {
    return FfiConverterTypeValidatorPage.lower(value)
}


public struct ValidatorSet {
    /**
     * Object ID of the `Table` storing the inactive staking pools.
     */
    public var inactivePoolsId: ObjectId?
    /**
     * Size of the inactive pools `Table`.
     */
    public var inactivePoolsSize: Int32?
    /**
     * Object ID of the wrapped object `TableVec` storing the pending active
     * validators.
     */
    public var pendingActiveValidatorsId: ObjectId?
    /**
     * Size of the pending active validators table.
     */
    public var pendingActiveValidatorsSize: Int32?
    /**
     * Validators that are pending removal from the active validator set,
     * expressed as indices in to `activeValidators`.
     */
    public var pendingRemovals: [Int32]?
    /**
     * Object ID of the `Table` storing the mapping from staking pool ids to
     * the addresses of the corresponding validators. This is needed
     * because a validator's address can potentially change but the object
     * ID of its pool will not.
     */
    public var stakingPoolMappingsId: ObjectId?
    /**
     * Size of the stake pool mappings `Table`.
     */
    public var stakingPoolMappingsSize: Int32?
    /**
     * Total amount of stake for all active validators at the beginning of the
     * epoch.
     */
    public var totalStake: String?
    /**
     * Size of the validator candidates `Table`.
     */
    public var validatorCandidatesSize: Int32?
    /**
     * Object ID of the `Table` storing the validator candidates.
     */
    public var validatorCandidatesId: ObjectId?

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(
        /**
         * Object ID of the `Table` storing the inactive staking pools.
         */inactivePoolsId: ObjectId? = nil, 
        /**
         * Size of the inactive pools `Table`.
         */inactivePoolsSize: Int32? = nil, 
        /**
         * Object ID of the wrapped object `TableVec` storing the pending active
         * validators.
         */pendingActiveValidatorsId: ObjectId? = nil, 
        /**
         * Size of the pending active validators table.
         */pendingActiveValidatorsSize: Int32? = nil, 
        /**
         * Validators that are pending removal from the active validator set,
         * expressed as indices in to `activeValidators`.
         */pendingRemovals: [Int32]? = nil, 
        /**
         * Object ID of the `Table` storing the mapping from staking pool ids to
         * the addresses of the corresponding validators. This is needed
         * because a validator's address can potentially change but the object
         * ID of its pool will not.
         */stakingPoolMappingsId: ObjectId? = nil, 
        /**
         * Size of the stake pool mappings `Table`.
         */stakingPoolMappingsSize: Int32? = nil, 
        /**
         * Total amount of stake for all active validators at the beginning of the
         * epoch.
         */totalStake: String? = nil, 
        /**
         * Size of the validator candidates `Table`.
         */validatorCandidatesSize: Int32? = nil, 
        /**
         * Object ID of the `Table` storing the validator candidates.
         */validatorCandidatesId: ObjectId? = nil) {
        self.inactivePoolsId = inactivePoolsId
        self.inactivePoolsSize = inactivePoolsSize
        self.pendingActiveValidatorsId = pendingActiveValidatorsId
        self.pendingActiveValidatorsSize = pendingActiveValidatorsSize
        self.pendingRemovals = pendingRemovals
        self.stakingPoolMappingsId = stakingPoolMappingsId
        self.stakingPoolMappingsSize = stakingPoolMappingsSize
        self.totalStake = totalStake
        self.validatorCandidatesSize = validatorCandidatesSize
        self.validatorCandidatesId = validatorCandidatesId
    }
}

#if compiler(>=6)
extension ValidatorSet: Sendable {}
#endif



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeValidatorSet: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ValidatorSet {
        return
            try ValidatorSet(
                inactivePoolsId: FfiConverterOptionTypeObjectId.read(from: &buf), 
                inactivePoolsSize: FfiConverterOptionInt32.read(from: &buf), 
                pendingActiveValidatorsId: FfiConverterOptionTypeObjectId.read(from: &buf), 
                pendingActiveValidatorsSize: FfiConverterOptionInt32.read(from: &buf), 
                pendingRemovals: FfiConverterOptionSequenceInt32.read(from: &buf), 
                stakingPoolMappingsId: FfiConverterOptionTypeObjectId.read(from: &buf), 
                stakingPoolMappingsSize: FfiConverterOptionInt32.read(from: &buf), 
                totalStake: FfiConverterOptionString.read(from: &buf), 
                validatorCandidatesSize: FfiConverterOptionInt32.read(from: &buf), 
                validatorCandidatesId: FfiConverterOptionTypeObjectId.read(from: &buf)
        )
    }

    public static func write(_ value: ValidatorSet, into buf: inout [UInt8]) {
        FfiConverterOptionTypeObjectId.write(value.inactivePoolsId, into: &buf)
        FfiConverterOptionInt32.write(value.inactivePoolsSize, into: &buf)
        FfiConverterOptionTypeObjectId.write(value.pendingActiveValidatorsId, into: &buf)
        FfiConverterOptionInt32.write(value.pendingActiveValidatorsSize, into: &buf)
        FfiConverterOptionSequenceInt32.write(value.pendingRemovals, into: &buf)
        FfiConverterOptionTypeObjectId.write(value.stakingPoolMappingsId, into: &buf)
        FfiConverterOptionInt32.write(value.stakingPoolMappingsSize, into: &buf)
        FfiConverterOptionString.write(value.totalStake, into: &buf)
        FfiConverterOptionInt32.write(value.validatorCandidatesSize, into: &buf)
        FfiConverterOptionTypeObjectId.write(value.validatorCandidatesId, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeValidatorSet_lift(_ buf: RustBuffer) throws -> ValidatorSet {
    return try FfiConverterTypeValidatorSet.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeValidatorSet_lower(_ value: ValidatorSet) -> RustBuffer {
    return FfiConverterTypeValidatorSet.lower(value)
}


/**
 * A claim of the iss in a zklogin proof
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * zklogin-claim = string u8
 * ```
 */
public struct ZkLoginClaim {
    public var value: String
    public var indexMod4: UInt8

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(value: String, indexMod4: UInt8) {
        self.value = value
        self.indexMod4 = indexMod4
    }
}

#if compiler(>=6)
extension ZkLoginClaim: Sendable {}
#endif


extension ZkLoginClaim: Equatable, Hashable {
    public static func ==(lhs: ZkLoginClaim, rhs: ZkLoginClaim) -> Bool {
        if lhs.value != rhs.value {
            return false
        }
        if lhs.indexMod4 != rhs.indexMod4 {
            return false
        }
        return true
    }

    public func hash(into hasher: inout Hasher) {
        hasher.combine(value)
        hasher.combine(indexMod4)
    }
}



#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeZkLoginClaim: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ZkLoginClaim {
        return
            try ZkLoginClaim(
                value: FfiConverterString.read(from: &buf), 
                indexMod4: FfiConverterUInt8.read(from: &buf)
        )
    }

    public static func write(_ value: ZkLoginClaim, into buf: inout [UInt8]) {
        FfiConverterString.write(value.value, into: &buf)
        FfiConverterUInt8.write(value.indexMod4, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeZkLoginClaim_lift(_ buf: RustBuffer) throws -> ZkLoginClaim {
    return try FfiConverterTypeZkLoginClaim.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeZkLoginClaim_lower(_ value: ZkLoginClaim) -> RustBuffer {
    return FfiConverterTypeZkLoginClaim.lower(value)
}

// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.

public enum BatchSendStatusType {
    
    case inProgress
    case succeeded
    case discarded
}


#if compiler(>=6)
extension BatchSendStatusType: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeBatchSendStatusType: FfiConverterRustBuffer {
    typealias SwiftType = BatchSendStatusType

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> BatchSendStatusType {
        let variant: Int32 = try readInt(&buf)
        switch variant {
        
        case 1: return .inProgress
        
        case 2: return .succeeded
        
        case 3: return .discarded
        
        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: BatchSendStatusType, into buf: inout [UInt8]) {
        switch value {
        
        
        case .inProgress:
            writeInt(&buf, Int32(1))
        
        
        case .succeeded:
            writeInt(&buf, Int32(2))
        
        
        case .discarded:
            writeInt(&buf, Int32(3))
        
        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeBatchSendStatusType_lift(_ buf: RustBuffer) throws -> BatchSendStatusType {
    return try FfiConverterTypeBatchSendStatusType.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeBatchSendStatusType_lower(_ value: BatchSendStatusType) -> RustBuffer {
    return FfiConverterTypeBatchSendStatusType.lower(value)
}


extension BatchSendStatusType: Equatable, Hashable {}






// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.
/**
 * An error with an argument to a command
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * command-argument-error =  type-mismatch
 * =/ invalid-bcs-bytes
 * =/ invalid-usage-of-pure-argument
 * =/ invalid-argument-to-private-entry-function
 * =/ index-out-of-bounds
 * =/ secondary-index-out-of-bound
 * =/ invalid-result-arity
 * =/ invalid-gas-coin-usage
 * =/ invalid-value-usage
 * =/ invalid-object-by-value
 * =/ invalid-object-by-mut-ref
 * =/ shared-object-operation-not-allowed
 *
 * type-mismatch                               = %x00
 * invalid-bcs-bytes                           = %x01
 * invalid-usage-of-pure-argument              = %x02
 * invalid-argument-to-private-entry-function  = %x03
 * index-out-of-bounds                         = %x04 u16
 * secondary-index-out-of-bound                = %x05 u16 u16
 * invalid-result-arity                        = %x06 u16
 * invalid-gas-coin-usage                      = %x07
 * invalid-value-usage                         = %x08
 * invalid-object-by-value                     = %x09
 * invalid-object-by-mut-ref                   = %x0a
 * shared-object-operation-not-allowed         = %x0b
 * ```
 */

public enum CommandArgumentError {
    
    /**
     * The type of the value does not match the expected type
     */
    case typeMismatch
    /**
     * The argument cannot be deserialized into a value of the specified type
     */
    case invalidBcsBytes
    /**
     * The argument cannot be instantiated from raw bytes
     */
    case invalidUsageOfPureArgument
    /**
     * Invalid argument to private entry function.
     * Private entry functions cannot take arguments from other Move functions.
     */
    case invalidArgumentToPrivateEntryFunction
    /**
     * Out of bounds access to input or results
     */
    case indexOutOfBounds(index: UInt16
    )
    /**
     * Out of bounds access to subresult
     */
    case secondaryIndexOutOfBounds(result: UInt16, subresult: UInt16
    )
    /**
     * Invalid usage of result.
     * Expected a single result but found either no return value or multiple.
     */
    case invalidResultArity(result: UInt16
    )
    /**
     * Invalid usage of Gas coin.
     * The Gas coin can only be used by-value with a TransferObjects command.
     */
    case invalidGasCoinUsage
    /**
     * Invalid usage of move value.
     */
    case invalidValueUsage
    /**
     * Immutable objects cannot be passed by-value.
     */
    case invalidObjectByValue
    /**
     * Immutable objects cannot be passed by mutable reference, &mut.
     */
    case invalidObjectByMutRef
    /**
     * Shared object operations such a wrapping, freezing, or converting to
     * owned are not allowed.
     */
    case sharedObjectOperationNotAllowed
    /**
     * Invalid argument arity. Expected a single argument but found a result
     * that expanded to multiple arguments.
     */
    case invalidArgumentArity
}


#if compiler(>=6)
extension CommandArgumentError: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeCommandArgumentError: FfiConverterRustBuffer {
    typealias SwiftType = CommandArgumentError

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> CommandArgumentError {
        let variant: Int32 = try readInt(&buf)
        switch variant {
        
        case 1: return .typeMismatch
        
        case 2: return .invalidBcsBytes
        
        case 3: return .invalidUsageOfPureArgument
        
        case 4: return .invalidArgumentToPrivateEntryFunction
        
        case 5: return .indexOutOfBounds(index: try FfiConverterUInt16.read(from: &buf)
        )
        
        case 6: return .secondaryIndexOutOfBounds(result: try FfiConverterUInt16.read(from: &buf), subresult: try FfiConverterUInt16.read(from: &buf)
        )
        
        case 7: return .invalidResultArity(result: try FfiConverterUInt16.read(from: &buf)
        )
        
        case 8: return .invalidGasCoinUsage
        
        case 9: return .invalidValueUsage
        
        case 10: return .invalidObjectByValue
        
        case 11: return .invalidObjectByMutRef
        
        case 12: return .sharedObjectOperationNotAllowed
        
        case 13: return .invalidArgumentArity
        
        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: CommandArgumentError, into buf: inout [UInt8]) {
        switch value {
        
        
        case .typeMismatch:
            writeInt(&buf, Int32(1))
        
        
        case .invalidBcsBytes:
            writeInt(&buf, Int32(2))
        
        
        case .invalidUsageOfPureArgument:
            writeInt(&buf, Int32(3))
        
        
        case .invalidArgumentToPrivateEntryFunction:
            writeInt(&buf, Int32(4))
        
        
        case let .indexOutOfBounds(index):
            writeInt(&buf, Int32(5))
            FfiConverterUInt16.write(index, into: &buf)
            
        
        case let .secondaryIndexOutOfBounds(result,subresult):
            writeInt(&buf, Int32(6))
            FfiConverterUInt16.write(result, into: &buf)
            FfiConverterUInt16.write(subresult, into: &buf)
            
        
        case let .invalidResultArity(result):
            writeInt(&buf, Int32(7))
            FfiConverterUInt16.write(result, into: &buf)
            
        
        case .invalidGasCoinUsage:
            writeInt(&buf, Int32(8))
        
        
        case .invalidValueUsage:
            writeInt(&buf, Int32(9))
        
        
        case .invalidObjectByValue:
            writeInt(&buf, Int32(10))
        
        
        case .invalidObjectByMutRef:
            writeInt(&buf, Int32(11))
        
        
        case .sharedObjectOperationNotAllowed:
            writeInt(&buf, Int32(12))
        
        
        case .invalidArgumentArity:
            writeInt(&buf, Int32(13))
        
        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeCommandArgumentError_lift(_ buf: RustBuffer) throws -> CommandArgumentError {
    return try FfiConverterTypeCommandArgumentError.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeCommandArgumentError_lower(_ value: CommandArgumentError) -> RustBuffer {
    return FfiConverterTypeCommandArgumentError.lower(value)
}


extension CommandArgumentError: Equatable, Hashable {}






// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.
/**
 * Pagination direction.
 */

public enum Direction {
    
    case forward
    case backward
}


#if compiler(>=6)
extension Direction: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeDirection: FfiConverterRustBuffer {
    typealias SwiftType = Direction

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Direction {
        let variant: Int32 = try readInt(&buf)
        switch variant {
        
        case 1: return .forward
        
        case 2: return .backward
        
        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: Direction, into buf: inout [UInt8]) {
        switch value {
        
        
        case .forward:
            writeInt(&buf, Int32(1))
        
        
        case .backward:
            writeInt(&buf, Int32(2))
        
        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeDirection_lift(_ buf: RustBuffer) throws -> Direction {
    return try FfiConverterTypeDirection.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeDirection_lower(_ value: Direction) -> RustBuffer {
    return FfiConverterTypeDirection.lower(value)
}


extension Direction: Equatable, Hashable {}






// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.
/**
 * An error that can occur during the execution of a transaction
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 *
 * execution-error =  insufficient-gas
 * =/ invalid-gas-object
 * =/ invariant-violation
 * =/ feature-not-yet-supported
 * =/ object-too-big
 * =/ package-too-big
 * =/ circular-object-ownership
 * =/ insufficient-coin-balance
 * =/ coin-balance-overflow
 * =/ publish-error-non-zero-address
 * =/ iota-move-verification-error
 * =/ move-primitive-runtime-error
 * =/ move-abort
 * =/ vm-verification-or-deserialization-error
 * =/ vm-invariant-violation
 * =/ function-not-found
 * =/ arity-mismatch
 * =/ type-arity-mismatch
 * =/ non-entry-function-invoked
 * =/ command-argument-error
 * =/ type-argument-error
 * =/ unused-value-without-drop
 * =/ invalid-public-function-return-type
 * =/ invalid-transfer-object
 * =/ effects-too-large
 * =/ publish-upgrade-missing-dependency
 * =/ publish-upgrade-dependency-downgrade
 * =/ package-upgrade-error
 * =/ written-objects-too-large
 * =/ certificate-denied
 * =/ iota-move-verification-timeout
 * =/ shared-object-operation-not-allowed
 * =/ input-object-deleted
 * =/ execution-cancelled-due-to-shared-object-congestion
 * =/ address-denied-for-coin
 * =/ coin-type-global-pause
 * =/ execution-cancelled-due-to-randomness-unavailable
 *
 * insufficient-gas                                    = %x00
 * invalid-gas-object                                  = %x01
 * invariant-violation                                 = %x02
 * feature-not-yet-supported                           = %x03
 * object-too-big                                      = %x04 u64 u64
 * package-too-big                                     = %x05 u64 u64
 * circular-object-ownership                           = %x06 object-id
 * insufficient-coin-balance                           = %x07
 * coin-balance-overflow                               = %x08
 * publish-error-non-zero-address                      = %x09
 * iota-move-verification-error                        = %x0a
 * move-primitive-runtime-error                        = %x0b (option move-location)
 * move-abort                                          = %x0c move-location u64
 * vm-verification-or-deserialization-error            = %x0d
 * vm-invariant-violation                              = %x0e
 * function-not-found                                  = %x0f
 * arity-mismatch                                      = %x10
 * type-arity-mismatch                                 = %x11
 * non-entry-function-invoked                          = %x12
 * command-argument-error                              = %x13 u16 command-argument-error
 * type-argument-error                                 = %x14 u16 type-argument-error
 * unused-value-without-drop                           = %x15 u16 u16
 * invalid-public-function-return-type                 = %x16 u16
 * invalid-transfer-object                             = %x17
 * effects-too-large                                   = %x18 u64 u64
 * publish-upgrade-missing-dependency                  = %x19
 * publish-upgrade-dependency-downgrade                = %x1a
 * package-upgrade-error                               = %x1b package-upgrade-error
 * written-objects-too-large                           = %x1c u64 u64
 * certificate-denied                                  = %x1d
 * iota-move-verification-timeout                      = %x1e
 * shared-object-operation-not-allowed                 = %x1f
 * input-object-deleted                                = %x20
 * execution-cancelled-due-to-shared-object-congestion = %x21 (vector object-id)
 * address-denied-for-coin                             = %x22 address string
 * coin-type-global-pause                              = %x23 string
 * execution-cancelled-due-to-randomness-unavailable   = %x24
 * ```
 */

public enum ExecutionError {
    
    /**
     * Insufficient Gas
     */
    case insufficientGas
    /**
     * Invalid Gas Object.
     */
    case invalidGasObject
    /**
     * Invariant Violation
     */
    case invariantViolation
    /**
     * Attempted to used feature that is not supported yet
     */
    case featureNotYetSupported
    /**
     * Move object is larger than the maximum allowed size
     */
    case objectTooBig(objectSize: UInt64, maxObjectSize: UInt64
    )
    /**
     * Package is larger than the maximum allowed size
     */
    case packageTooBig(objectSize: UInt64, maxObjectSize: UInt64
    )
    /**
     * Circular Object Ownership
     */
    case circularObjectOwnership(object: ObjectId
    )
    /**
     * Insufficient coin balance for requested operation
     */
    case insufficientCoinBalance
    /**
     * Coin balance overflowed an u64
     */
    case coinBalanceOverflow
    /**
     * Publish Error, Non-zero Address.
     * The modules in the package must have their self-addresses set to zero.
     */
    case publishErrorNonZeroAddress
    /**
     * IOTA Move Bytecode Verification Error.
     */
    case iotaMoveVerification
    /**
     * Error from a non-abort instruction.
     * Possible causes:
     * Arithmetic error, stack overflow, max value depth, etc."
     */
    case movePrimitiveRuntime(location: MoveLocation?
    )
    /**
     * Move runtime abort
     */
    case moveAbort(location: MoveLocation, code: UInt64
    )
    /**
     * Bytecode verification error.
     */
    case vmVerificationOrDeserialization
    /**
     * MoveVm invariant violation
     */
    case vmInvariantViolation
    /**
     * Function not found
     */
    case functionNotFound
    /**
     * Arity mismatch for Move function.
     * The number of arguments does not match the number of parameters
     */
    case arityMismatch
    /**
     * Type arity mismatch for Move function.
     * Mismatch between the number of actual versus expected type arguments.
     */
    case typeArityMismatch
    /**
     * Non Entry Function Invoked. Move Call must start with an entry function.
     */
    case nonEntryFunctionInvoked
    /**
     * Invalid command argument
     */
    case commandArgument(argument: UInt16, kind: CommandArgumentError
    )
    /**
     * Type argument error
     */
    case typeArgument(
        /**
         * Index of the problematic type argument
         */typeArgument: UInt16, kind: TypeArgumentError
    )
    /**
     * Unused result without the drop ability.
     */
    case unusedValueWithoutDrop(result: UInt16, subresult: UInt16
    )
    /**
     * Invalid public Move function signature.
     * Unsupported return type for return value
     */
    case invalidPublicFunctionReturnType(index: UInt16
    )
    /**
     * Invalid Transfer Object, object does not have public transfer.
     */
    case invalidTransferObject
    /**
     * Effects from the transaction are too large
     */
    case effectsTooLarge(currentSize: UInt64, maxSize: UInt64
    )
    /**
     * Publish or Upgrade is missing dependency
     */
    case publishUpgradeMissingDependency
    /**
     * Publish or Upgrade dependency downgrade.
     *
     * Indirect (transitive) dependency of published or upgraded package has
     * been assigned an on-chain version that is less than the version
     * required by one of the package's transitive dependencies.
     */
    case publishUpgradeDependencyDowngrade
    /**
     * Invalid package upgrade
     */
    case packageUpgrade(kind: PackageUpgradeError
    )
    /**
     * Indicates the transaction tried to write objects too large to storage
     */
    case writtenObjectsTooLarge(objectSize: UInt64, maxObjectSize: UInt64
    )
    /**
     * Certificate is on the deny list
     */
    case certificateDenied
    /**
     * IOTA Move Bytecode verification timed out.
     */
    case iotaMoveVerificationTimeout
    /**
     * The requested shared object operation is not allowed
     */
    case sharedObjectOperationNotAllowed
    /**
     * Requested shared object has been deleted
     */
    case inputObjectDeleted
    /**
     * Certificate is cancelled due to congestion on shared objects
     */
    case executionCancelledDueToSharedObjectCongestion(congestedObjects: [ObjectId]
    )
    /**
     * Certificate is cancelled due to congestion on shared objects;
     * suggested gas price can be used to give this certificate more priority.
     */
    case executionCancelledDueToSharedObjectCongestionV2(congestedObjects: [ObjectId], suggestedGasPrice: UInt64
    )
    /**
     * Address is denied for this coin type
     */
    case addressDeniedForCoin(address: Address, coinType: String
    )
    /**
     * Coin type is globally paused for use
     */
    case coinTypeGlobalPause(coinType: String
    )
    /**
     * Certificate is cancelled because randomness could not be generated this
     * epoch
     */
    case executionCancelledDueToRandomnessUnavailable
    /**
     * A valid linkage was unable to be determined for the transaction or one
     * of its commands.
     */
    case invalidLinkage
}


#if compiler(>=6)
extension ExecutionError: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeExecutionError: FfiConverterRustBuffer {
    typealias SwiftType = ExecutionError

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ExecutionError {
        let variant: Int32 = try readInt(&buf)
        switch variant {
        
        case 1: return .insufficientGas
        
        case 2: return .invalidGasObject
        
        case 3: return .invariantViolation
        
        case 4: return .featureNotYetSupported
        
        case 5: return .objectTooBig(objectSize: try FfiConverterUInt64.read(from: &buf), maxObjectSize: try FfiConverterUInt64.read(from: &buf)
        )
        
        case 6: return .packageTooBig(objectSize: try FfiConverterUInt64.read(from: &buf), maxObjectSize: try FfiConverterUInt64.read(from: &buf)
        )
        
        case 7: return .circularObjectOwnership(object: try FfiConverterTypeObjectId.read(from: &buf)
        )
        
        case 8: return .insufficientCoinBalance
        
        case 9: return .coinBalanceOverflow
        
        case 10: return .publishErrorNonZeroAddress
        
        case 11: return .iotaMoveVerification
        
        case 12: return .movePrimitiveRuntime(location: try FfiConverterOptionTypeMoveLocation.read(from: &buf)
        )
        
        case 13: return .moveAbort(location: try FfiConverterTypeMoveLocation.read(from: &buf), code: try FfiConverterUInt64.read(from: &buf)
        )
        
        case 14: return .vmVerificationOrDeserialization
        
        case 15: return .vmInvariantViolation
        
        case 16: return .functionNotFound
        
        case 17: return .arityMismatch
        
        case 18: return .typeArityMismatch
        
        case 19: return .nonEntryFunctionInvoked
        
        case 20: return .commandArgument(argument: try FfiConverterUInt16.read(from: &buf), kind: try FfiConverterTypeCommandArgumentError.read(from: &buf)
        )
        
        case 21: return .typeArgument(typeArgument: try FfiConverterUInt16.read(from: &buf), kind: try FfiConverterTypeTypeArgumentError.read(from: &buf)
        )
        
        case 22: return .unusedValueWithoutDrop(result: try FfiConverterUInt16.read(from: &buf), subresult: try FfiConverterUInt16.read(from: &buf)
        )
        
        case 23: return .invalidPublicFunctionReturnType(index: try FfiConverterUInt16.read(from: &buf)
        )
        
        case 24: return .invalidTransferObject
        
        case 25: return .effectsTooLarge(currentSize: try FfiConverterUInt64.read(from: &buf), maxSize: try FfiConverterUInt64.read(from: &buf)
        )
        
        case 26: return .publishUpgradeMissingDependency
        
        case 27: return .publishUpgradeDependencyDowngrade
        
        case 28: return .packageUpgrade(kind: try FfiConverterTypePackageUpgradeError.read(from: &buf)
        )
        
        case 29: return .writtenObjectsTooLarge(objectSize: try FfiConverterUInt64.read(from: &buf), maxObjectSize: try FfiConverterUInt64.read(from: &buf)
        )
        
        case 30: return .certificateDenied
        
        case 31: return .iotaMoveVerificationTimeout
        
        case 32: return .sharedObjectOperationNotAllowed
        
        case 33: return .inputObjectDeleted
        
        case 34: return .executionCancelledDueToSharedObjectCongestion(congestedObjects: try FfiConverterSequenceTypeObjectId.read(from: &buf)
        )
        
        case 35: return .executionCancelledDueToSharedObjectCongestionV2(congestedObjects: try FfiConverterSequenceTypeObjectId.read(from: &buf), suggestedGasPrice: try FfiConverterUInt64.read(from: &buf)
        )
        
        case 36: return .addressDeniedForCoin(address: try FfiConverterTypeAddress.read(from: &buf), coinType: try FfiConverterString.read(from: &buf)
        )
        
        case 37: return .coinTypeGlobalPause(coinType: try FfiConverterString.read(from: &buf)
        )
        
        case 38: return .executionCancelledDueToRandomnessUnavailable
        
        case 39: return .invalidLinkage
        
        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: ExecutionError, into buf: inout [UInt8]) {
        switch value {
        
        
        case .insufficientGas:
            writeInt(&buf, Int32(1))
        
        
        case .invalidGasObject:
            writeInt(&buf, Int32(2))
        
        
        case .invariantViolation:
            writeInt(&buf, Int32(3))
        
        
        case .featureNotYetSupported:
            writeInt(&buf, Int32(4))
        
        
        case let .objectTooBig(objectSize,maxObjectSize):
            writeInt(&buf, Int32(5))
            FfiConverterUInt64.write(objectSize, into: &buf)
            FfiConverterUInt64.write(maxObjectSize, into: &buf)
            
        
        case let .packageTooBig(objectSize,maxObjectSize):
            writeInt(&buf, Int32(6))
            FfiConverterUInt64.write(objectSize, into: &buf)
            FfiConverterUInt64.write(maxObjectSize, into: &buf)
            
        
        case let .circularObjectOwnership(object):
            writeInt(&buf, Int32(7))
            FfiConverterTypeObjectId.write(object, into: &buf)
            
        
        case .insufficientCoinBalance:
            writeInt(&buf, Int32(8))
        
        
        case .coinBalanceOverflow:
            writeInt(&buf, Int32(9))
        
        
        case .publishErrorNonZeroAddress:
            writeInt(&buf, Int32(10))
        
        
        case .iotaMoveVerification:
            writeInt(&buf, Int32(11))
        
        
        case let .movePrimitiveRuntime(location):
            writeInt(&buf, Int32(12))
            FfiConverterOptionTypeMoveLocation.write(location, into: &buf)
            
        
        case let .moveAbort(location,code):
            writeInt(&buf, Int32(13))
            FfiConverterTypeMoveLocation.write(location, into: &buf)
            FfiConverterUInt64.write(code, into: &buf)
            
        
        case .vmVerificationOrDeserialization:
            writeInt(&buf, Int32(14))
        
        
        case .vmInvariantViolation:
            writeInt(&buf, Int32(15))
        
        
        case .functionNotFound:
            writeInt(&buf, Int32(16))
        
        
        case .arityMismatch:
            writeInt(&buf, Int32(17))
        
        
        case .typeArityMismatch:
            writeInt(&buf, Int32(18))
        
        
        case .nonEntryFunctionInvoked:
            writeInt(&buf, Int32(19))
        
        
        case let .commandArgument(argument,kind):
            writeInt(&buf, Int32(20))
            FfiConverterUInt16.write(argument, into: &buf)
            FfiConverterTypeCommandArgumentError.write(kind, into: &buf)
            
        
        case let .typeArgument(typeArgument,kind):
            writeInt(&buf, Int32(21))
            FfiConverterUInt16.write(typeArgument, into: &buf)
            FfiConverterTypeTypeArgumentError.write(kind, into: &buf)
            
        
        case let .unusedValueWithoutDrop(result,subresult):
            writeInt(&buf, Int32(22))
            FfiConverterUInt16.write(result, into: &buf)
            FfiConverterUInt16.write(subresult, into: &buf)
            
        
        case let .invalidPublicFunctionReturnType(index):
            writeInt(&buf, Int32(23))
            FfiConverterUInt16.write(index, into: &buf)
            
        
        case .invalidTransferObject:
            writeInt(&buf, Int32(24))
        
        
        case let .effectsTooLarge(currentSize,maxSize):
            writeInt(&buf, Int32(25))
            FfiConverterUInt64.write(currentSize, into: &buf)
            FfiConverterUInt64.write(maxSize, into: &buf)
            
        
        case .publishUpgradeMissingDependency:
            writeInt(&buf, Int32(26))
        
        
        case .publishUpgradeDependencyDowngrade:
            writeInt(&buf, Int32(27))
        
        
        case let .packageUpgrade(kind):
            writeInt(&buf, Int32(28))
            FfiConverterTypePackageUpgradeError.write(kind, into: &buf)
            
        
        case let .writtenObjectsTooLarge(objectSize,maxObjectSize):
            writeInt(&buf, Int32(29))
            FfiConverterUInt64.write(objectSize, into: &buf)
            FfiConverterUInt64.write(maxObjectSize, into: &buf)
            
        
        case .certificateDenied:
            writeInt(&buf, Int32(30))
        
        
        case .iotaMoveVerificationTimeout:
            writeInt(&buf, Int32(31))
        
        
        case .sharedObjectOperationNotAllowed:
            writeInt(&buf, Int32(32))
        
        
        case .inputObjectDeleted:
            writeInt(&buf, Int32(33))
        
        
        case let .executionCancelledDueToSharedObjectCongestion(congestedObjects):
            writeInt(&buf, Int32(34))
            FfiConverterSequenceTypeObjectId.write(congestedObjects, into: &buf)
            
        
        case let .executionCancelledDueToSharedObjectCongestionV2(congestedObjects,suggestedGasPrice):
            writeInt(&buf, Int32(35))
            FfiConverterSequenceTypeObjectId.write(congestedObjects, into: &buf)
            FfiConverterUInt64.write(suggestedGasPrice, into: &buf)
            
        
        case let .addressDeniedForCoin(address,coinType):
            writeInt(&buf, Int32(36))
            FfiConverterTypeAddress.write(address, into: &buf)
            FfiConverterString.write(coinType, into: &buf)
            
        
        case let .coinTypeGlobalPause(coinType):
            writeInt(&buf, Int32(37))
            FfiConverterString.write(coinType, into: &buf)
            
        
        case .executionCancelledDueToRandomnessUnavailable:
            writeInt(&buf, Int32(38))
        
        
        case .invalidLinkage:
            writeInt(&buf, Int32(39))
        
        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeExecutionError_lift(_ buf: RustBuffer) throws -> ExecutionError {
    return try FfiConverterTypeExecutionError.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeExecutionError_lower(_ value: ExecutionError) -> RustBuffer {
    return FfiConverterTypeExecutionError.lower(value)
}






// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.
/**
 * The status of an executed Transaction
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * execution-status = success / failure
 * success = %x00
 * failure = %x01 execution-error (option u64)
 * ```
 */

public enum ExecutionStatus {
    
    /**
     * The Transaction successfully executed.
     */
    case success
    /**
     * The Transaction didn't execute successfully.
     *
     * Failed transactions are still committed to the blockchain but any
     * intended effects are rolled back to prior to this transaction
     * executing with the caveat that gas objects are still smashed and gas
     * usage is still charged.
     */
    case failure(
        /**
         * The error encountered during execution.
         */error: ExecutionError, 
        /**
         * The command, if any, during which the error occurred.
         */command: UInt64?
    )
}


#if compiler(>=6)
extension ExecutionStatus: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeExecutionStatus: FfiConverterRustBuffer {
    typealias SwiftType = ExecutionStatus

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ExecutionStatus {
        let variant: Int32 = try readInt(&buf)
        switch variant {
        
        case 1: return .success
        
        case 2: return .failure(error: try FfiConverterTypeExecutionError.read(from: &buf), command: try FfiConverterOptionUInt64.read(from: &buf)
        )
        
        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: ExecutionStatus, into buf: inout [UInt8]) {
        switch value {
        
        
        case .success:
            writeInt(&buf, Int32(1))
        
        
        case let .failure(error,command):
            writeInt(&buf, Int32(2))
            FfiConverterTypeExecutionError.write(error, into: &buf)
            FfiConverterOptionUInt64.write(command, into: &buf)
            
        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeExecutionStatus_lift(_ buf: RustBuffer) throws -> ExecutionStatus {
    return try FfiConverterTypeExecutionStatus.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeExecutionStatus_lower(_ value: ExecutionStatus) -> RustBuffer {
    return FfiConverterTypeExecutionStatus.lower(value)
}






// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.

public enum Feature {
    
    case analytics
    case coins
    case dynamicFields
    case subscriptions
    case systemState
}


#if compiler(>=6)
extension Feature: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeFeature: FfiConverterRustBuffer {
    typealias SwiftType = Feature

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Feature {
        let variant: Int32 = try readInt(&buf)
        switch variant {
        
        case 1: return .analytics
        
        case 2: return .coins
        
        case 3: return .dynamicFields
        
        case 4: return .subscriptions
        
        case 5: return .systemState
        
        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: Feature, into buf: inout [UInt8]) {
        switch value {
        
        
        case .analytics:
            writeInt(&buf, Int32(1))
        
        
        case .coins:
            writeInt(&buf, Int32(2))
        
        
        case .dynamicFields:
            writeInt(&buf, Int32(3))
        
        
        case .subscriptions:
            writeInt(&buf, Int32(4))
        
        
        case .systemState:
            writeInt(&buf, Int32(5))
        
        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeFeature_lift(_ buf: RustBuffer) throws -> Feature {
    return try FfiConverterTypeFeature.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeFeature_lower(_ value: Feature) -> RustBuffer {
    return FfiConverterTypeFeature.lower(value)
}


extension Feature: Equatable, Hashable {}






// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.
/**
 * A 1-byte domain separator for hashing Object ID in IOTA. It starts from
 * 0xf0 to ensure no hashing collision for any ObjectID vs IotaAddress which is
 * derived as the hash of `flag || pubkey`.
 */

public enum HashingIntentScope {
    
    case childObjectId
    case regularObjectId
}


#if compiler(>=6)
extension HashingIntentScope: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeHashingIntentScope: FfiConverterRustBuffer {
    typealias SwiftType = HashingIntentScope

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> HashingIntentScope {
        let variant: Int32 = try readInt(&buf)
        switch variant {
        
        case 1: return .childObjectId
        
        case 2: return .regularObjectId
        
        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: HashingIntentScope, into buf: inout [UInt8]) {
        switch value {
        
        
        case .childObjectId:
            writeInt(&buf, Int32(1))
        
        
        case .regularObjectId:
            writeInt(&buf, Int32(2))
        
        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeHashingIntentScope_lift(_ buf: RustBuffer) throws -> HashingIntentScope {
    return try FfiConverterTypeHashingIntentScope.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeHashingIntentScope_lower(_ value: HashingIntentScope) -> RustBuffer {
    return FfiConverterTypeHashingIntentScope.lower(value)
}


extension HashingIntentScope: Equatable, Hashable {}






// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.
/**
 * Defines what happened to an ObjectId during execution
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * id-operation =  id-operation-none
 * =/ id-operation-created
 * =/ id-operation-deleted
 *
 * id-operation-none       = %x00
 * id-operation-created    = %x01
 * id-operation-deleted    = %x02
 * ```
 */

public enum IdOperation : UInt8 {
    
    case none = 0
    case created = 1
    case deleted = 2
}


#if compiler(>=6)
extension IdOperation: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeIdOperation: FfiConverterRustBuffer {
    typealias SwiftType = IdOperation

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> IdOperation {
        let variant: Int32 = try readInt(&buf)
        switch variant {
        
        case 1: return .none
        
        case 2: return .created
        
        case 3: return .deleted
        
        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: IdOperation, into buf: inout [UInt8]) {
        switch value {
        
        
        case .none:
            writeInt(&buf, Int32(1))
        
        
        case .created:
            writeInt(&buf, Int32(2))
        
        
        case .deleted:
            writeInt(&buf, Int32(3))
        
        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeIdOperation_lift(_ buf: RustBuffer) throws -> IdOperation {
    return try FfiConverterTypeIdOperation.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeIdOperation_lower(_ value: IdOperation) -> RustBuffer {
    return FfiConverterTypeIdOperation.lower(value)
}


extension IdOperation: Equatable, Hashable {}






// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.
/**
 * Byte signifying the application id of an Intent
 *
 * This enum specifies the application ID. Two intents in two different
 * applications (i.e., IOTA, Ethereum etc) should never collide, so
 * that even when a signing key is reused, nobody can take a signature
 * designated for app_1 and present it as a valid signature for an (any) intent
 * in app_2.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * intent-app-id = u8
 * ```
 */

public enum IntentAppId {
    
    case iota
    case consensus
}


#if compiler(>=6)
extension IntentAppId: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeIntentAppId: FfiConverterRustBuffer {
    typealias SwiftType = IntentAppId

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> IntentAppId {
        let variant: Int32 = try readInt(&buf)
        switch variant {
        
        case 1: return .iota
        
        case 2: return .consensus
        
        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: IntentAppId, into buf: inout [UInt8]) {
        switch value {
        
        
        case .iota:
            writeInt(&buf, Int32(1))
        
        
        case .consensus:
            writeInt(&buf, Int32(2))
        
        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeIntentAppId_lift(_ buf: RustBuffer) throws -> IntentAppId {
    return try FfiConverterTypeIntentAppId.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeIntentAppId_lower(_ value: IntentAppId) -> RustBuffer {
    return FfiConverterTypeIntentAppId.lower(value)
}


extension IntentAppId: Equatable, Hashable {}







/**
 * Intent errors.
 */
public enum IntentError: Swift.Error {

    
    
    /**
     * Invalid bytes for Intent
     */
    case Bytes(message: String)
    
    /**
     * Invalid hex string for Intent
     */
    case Hex(message: String)
    
    /**
     * Invalid Scope for Intent
     */
    case Scope(message: String)
    
    /**
     * Invalid Version for Intent
     */
    case Version(message: String)
    
    /**
     * Invalid AppId for Intent
     */
    case AppId(message: String)
    
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeIntentError: FfiConverterRustBuffer {
    typealias SwiftType = IntentError

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> IntentError {
        let variant: Int32 = try readInt(&buf)
        switch variant {

        

        
        case 1: return .Bytes(
            message: try FfiConverterString.read(from: &buf)
        )
        
        case 2: return .Hex(
            message: try FfiConverterString.read(from: &buf)
        )
        
        case 3: return .Scope(
            message: try FfiConverterString.read(from: &buf)
        )
        
        case 4: return .Version(
            message: try FfiConverterString.read(from: &buf)
        )
        
        case 5: return .AppId(
            message: try FfiConverterString.read(from: &buf)
        )
        

        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: IntentError, into buf: inout [UInt8]) {
        switch value {

        

        
        case .Bytes(_ /* message is ignored*/):
            writeInt(&buf, Int32(1))
        case .Hex(_ /* message is ignored*/):
            writeInt(&buf, Int32(2))
        case .Scope(_ /* message is ignored*/):
            writeInt(&buf, Int32(3))
        case .Version(_ /* message is ignored*/):
            writeInt(&buf, Int32(4))
        case .AppId(_ /* message is ignored*/):
            writeInt(&buf, Int32(5))

        
        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeIntentError_lift(_ buf: RustBuffer) throws -> IntentError {
    return try FfiConverterTypeIntentError.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeIntentError_lower(_ value: IntentError) -> RustBuffer {
    return FfiConverterTypeIntentError.lower(value)
}


extension IntentError: Equatable, Hashable {}




extension IntentError: Foundation.LocalizedError {
    public var errorDescription: String? {
        String(reflecting: self)
    }
}




// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.
/**
 * Byte signifying the scope of an Intent
 *
 * This enum specifies the intent scope. Two intents for different scopes
 * should never collide, so no signature provided for one intent scope can be
 * used for another, even when the serialized data itself may be the same.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * intent-scope = u8
 * ```
 */

public enum IntentScope {
    
    /**
     * Used for a user signature on a transaction data.
     */
    case transactionData
    /**
     * Used for an authority signature on transaction effects.
     */
    case transactionEffects
    /**
     * Used for an authority signature on a checkpoint summary.
     */
    case checkpointSummary
    /**
     * Used for a user signature on a personal message.
     */
    case personalMessage
    /**
     * Used for an authority signature on a user signed transaction.
     */
    case senderSignedTransaction
    /**
     * Used as a signature representing an authority's proof of possession of
     * its authority key.
     */
    case proofOfPossession
    /**
     * Deprecated. Should not be reused. Introduced for bridge purposes but was
     * never included in messages.
     */
    case bridgeEventDeprecated
    /**
     * Used for consensus authority signature on block's digest.
     */
    case consensusBlock
    /**
     * Used for reporting peer addresses in discovery
     */
    case discoveryPeers
    /**
     * Used for authority capabilities from non-committee authorities.
     */
    case authorityCapabilities
}


#if compiler(>=6)
extension IntentScope: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeIntentScope: FfiConverterRustBuffer {
    typealias SwiftType = IntentScope

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> IntentScope {
        let variant: Int32 = try readInt(&buf)
        switch variant {
        
        case 1: return .transactionData
        
        case 2: return .transactionEffects
        
        case 3: return .checkpointSummary
        
        case 4: return .personalMessage
        
        case 5: return .senderSignedTransaction
        
        case 6: return .proofOfPossession
        
        case 7: return .bridgeEventDeprecated
        
        case 8: return .consensusBlock
        
        case 9: return .discoveryPeers
        
        case 10: return .authorityCapabilities
        
        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: IntentScope, into buf: inout [UInt8]) {
        switch value {
        
        
        case .transactionData:
            writeInt(&buf, Int32(1))
        
        
        case .transactionEffects:
            writeInt(&buf, Int32(2))
        
        
        case .checkpointSummary:
            writeInt(&buf, Int32(3))
        
        
        case .personalMessage:
            writeInt(&buf, Int32(4))
        
        
        case .senderSignedTransaction:
            writeInt(&buf, Int32(5))
        
        
        case .proofOfPossession:
            writeInt(&buf, Int32(6))
        
        
        case .bridgeEventDeprecated:
            writeInt(&buf, Int32(7))
        
        
        case .consensusBlock:
            writeInt(&buf, Int32(8))
        
        
        case .discoveryPeers:
            writeInt(&buf, Int32(9))
        
        
        case .authorityCapabilities:
            writeInt(&buf, Int32(10))
        
        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeIntentScope_lift(_ buf: RustBuffer) throws -> IntentScope {
    return try FfiConverterTypeIntentScope.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeIntentScope_lower(_ value: IntentScope) -> RustBuffer {
    return FfiConverterTypeIntentScope.lower(value)
}


extension IntentScope: Equatable, Hashable {}






// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.
/**
 * Byte signifying the version of an Intent
 *
 * The version here is to distinguish between signing different versions of the
 * struct or enum. Serialized output between two different versions of the same
 * struct/enum might accidentally (or maliciously on purpose) match.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * intent-version = u8
 * ```
 */

public enum IntentVersion {
    
    case v0
}


#if compiler(>=6)
extension IntentVersion: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeIntentVersion: FfiConverterRustBuffer {
    typealias SwiftType = IntentVersion

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> IntentVersion {
        let variant: Int32 = try readInt(&buf)
        switch variant {
        
        case 1: return .v0
        
        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: IntentVersion, into buf: inout [UInt8]) {
        switch value {
        
        
        case .v0:
            writeInt(&buf, Int32(1))
        
        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeIntentVersion_lift(_ buf: RustBuffer) throws -> IntentVersion {
    return try FfiConverterTypeIntentVersion.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeIntentVersion_lower(_ value: IntentVersion) -> RustBuffer {
    return FfiConverterTypeIntentVersion.lower(value)
}


extension IntentVersion: Equatable, Hashable {}






// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.

public enum MnemonicLength {
    
    case words12
    case words24
}


#if compiler(>=6)
extension MnemonicLength: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMnemonicLength: FfiConverterRustBuffer {
    typealias SwiftType = MnemonicLength

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MnemonicLength {
        let variant: Int32 = try readInt(&buf)
        switch variant {
        
        case 1: return .words12
        
        case 2: return .words24
        
        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: MnemonicLength, into buf: inout [UInt8]) {
        switch value {
        
        
        case .words12:
            writeInt(&buf, Int32(1))
        
        
        case .words24:
            writeInt(&buf, Int32(2))
        
        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMnemonicLength_lift(_ buf: RustBuffer) throws -> MnemonicLength {
    return try FfiConverterTypeMnemonicLength.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMnemonicLength_lower(_ value: MnemonicLength) -> RustBuffer {
    return FfiConverterTypeMnemonicLength.lower(value)
}


extension MnemonicLength: Equatable, Hashable {}






// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.

public enum MoveAbility {
    
    case copy
    case drop
    case key
    case store
}


#if compiler(>=6)
extension MoveAbility: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMoveAbility: FfiConverterRustBuffer {
    typealias SwiftType = MoveAbility

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MoveAbility {
        let variant: Int32 = try readInt(&buf)
        switch variant {
        
        case 1: return .copy
        
        case 2: return .drop
        
        case 3: return .key
        
        case 4: return .store
        
        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: MoveAbility, into buf: inout [UInt8]) {
        switch value {
        
        
        case .copy:
            writeInt(&buf, Int32(1))
        
        
        case .drop:
            writeInt(&buf, Int32(2))
        
        
        case .key:
            writeInt(&buf, Int32(3))
        
        
        case .store:
            writeInt(&buf, Int32(4))
        
        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveAbility_lift(_ buf: RustBuffer) throws -> MoveAbility {
    return try FfiConverterTypeMoveAbility.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveAbility_lower(_ value: MoveAbility) -> RustBuffer {
    return FfiConverterTypeMoveAbility.lower(value)
}


extension MoveAbility: Equatable, Hashable {}






// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.

public enum MoveVisibility {
    
    case `public`
    case `private`
    case friend
}


#if compiler(>=6)
extension MoveVisibility: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMoveVisibility: FfiConverterRustBuffer {
    typealias SwiftType = MoveVisibility

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MoveVisibility {
        let variant: Int32 = try readInt(&buf)
        switch variant {
        
        case 1: return .`public`
        
        case 2: return .`private`
        
        case 3: return .friend
        
        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: MoveVisibility, into buf: inout [UInt8]) {
        switch value {
        
        
        case .`public`:
            writeInt(&buf, Int32(1))
        
        
        case .`private`:
            writeInt(&buf, Int32(2))
        
        
        case .friend:
            writeInt(&buf, Int32(3))
        
        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveVisibility_lift(_ buf: RustBuffer) throws -> MoveVisibility {
    return try FfiConverterTypeMoveVisibility.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMoveVisibility_lower(_ value: MoveVisibility) -> RustBuffer {
    return FfiConverterTypeMoveVisibility.lower(value)
}


extension MoveVisibility: Equatable, Hashable {}






// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.
/**
 * Two different view options for a name.
 * `At` -> `test@example` | `Dot` -> `test.example.iota`
 */

public enum NameFormat {
    
    case at
    case dot
}


#if compiler(>=6)
extension NameFormat: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeNameFormat: FfiConverterRustBuffer {
    typealias SwiftType = NameFormat

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> NameFormat {
        let variant: Int32 = try readInt(&buf)
        switch variant {
        
        case 1: return .at
        
        case 2: return .dot
        
        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: NameFormat, into buf: inout [UInt8]) {
        switch value {
        
        
        case .at:
            writeInt(&buf, Int32(1))
        
        
        case .dot:
            writeInt(&buf, Int32(2))
        
        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeNameFormat_lift(_ buf: RustBuffer) throws -> NameFormat {
    return try FfiConverterTypeNameFormat.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeNameFormat_lower(_ value: NameFormat) -> RustBuffer {
    return FfiConverterTypeNameFormat.lower(value)
}


extension NameFormat: Equatable, Hashable {}






// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.
/**
 * State of an object prior to execution
 *
 * If an object exists (at root-level) in the store prior to this transaction,
 * it should be Data, otherwise it's Missing, e.g. wrapped objects should be
 * Missing.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * object-in = object-in-missing / object-in-data
 *
 * object-in-missing = %x00
 * object-in-data    = %x01 u64 digest owner
 * ```
 */

public enum ObjectIn {
    
    case missing
    /**
     * The old version, digest and owner.
     */
    case data(version: UInt64, digest: Digest, owner: Owner
    )
}


#if compiler(>=6)
extension ObjectIn: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeObjectIn: FfiConverterRustBuffer {
    typealias SwiftType = ObjectIn

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ObjectIn {
        let variant: Int32 = try readInt(&buf)
        switch variant {
        
        case 1: return .missing
        
        case 2: return .data(version: try FfiConverterUInt64.read(from: &buf), digest: try FfiConverterTypeDigest.read(from: &buf), owner: try FfiConverterTypeOwner.read(from: &buf)
        )
        
        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: ObjectIn, into buf: inout [UInt8]) {
        switch value {
        
        
        case .missing:
            writeInt(&buf, Int32(1))
        
        
        case let .data(version,digest,owner):
            writeInt(&buf, Int32(2))
            FfiConverterUInt64.write(version, into: &buf)
            FfiConverterTypeDigest.write(digest, into: &buf)
            FfiConverterTypeOwner.write(owner, into: &buf)
            
        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeObjectIn_lift(_ buf: RustBuffer) throws -> ObjectIn {
    return try FfiConverterTypeObjectIn.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeObjectIn_lower(_ value: ObjectIn) -> RustBuffer {
    return FfiConverterTypeObjectIn.lower(value)
}






// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.
/**
 * State of an object after execution
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * object-out  =  object-out-missing
 * =/ object-out-object-write
 * =/ object-out-package-write
 *
 *
 * object-out-missing        = %x00
 * object-out-object-write   = %x01 digest owner
 * object-out-package-write  = %x02 version digest
 * ```
 */

public enum ObjectOut {
    
    /**
     * Same definition as in ObjectIn.
     */
    case missing
    /**
     * Any written object, including all of mutated, created, unwrapped today.
     */
    case objectWrite(digest: Digest, owner: Owner
    )
    /**
     * Packages writes need to be tracked separately with version because
     * we don't use lamport version for package publish and upgrades.
     */
    case packageWrite(version: UInt64, digest: Digest
    )
}


#if compiler(>=6)
extension ObjectOut: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeObjectOut: FfiConverterRustBuffer {
    typealias SwiftType = ObjectOut

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ObjectOut {
        let variant: Int32 = try readInt(&buf)
        switch variant {
        
        case 1: return .missing
        
        case 2: return .objectWrite(digest: try FfiConverterTypeDigest.read(from: &buf), owner: try FfiConverterTypeOwner.read(from: &buf)
        )
        
        case 3: return .packageWrite(version: try FfiConverterUInt64.read(from: &buf), digest: try FfiConverterTypeDigest.read(from: &buf)
        )
        
        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: ObjectOut, into buf: inout [UInt8]) {
        switch value {
        
        
        case .missing:
            writeInt(&buf, Int32(1))
        
        
        case let .objectWrite(digest,owner):
            writeInt(&buf, Int32(2))
            FfiConverterTypeDigest.write(digest, into: &buf)
            FfiConverterTypeOwner.write(owner, into: &buf)
            
        
        case let .packageWrite(version,digest):
            writeInt(&buf, Int32(3))
            FfiConverterUInt64.write(version, into: &buf)
            FfiConverterTypeDigest.write(digest, into: &buf)
            
        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeObjectOut_lift(_ buf: RustBuffer) throws -> ObjectOut {
    return try FfiConverterTypeObjectOut.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeObjectOut_lower(_ value: ObjectOut) -> RustBuffer {
    return FfiConverterTypeObjectOut.lower(value)
}






// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.
/**
 * An error with a upgrading a package
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * package-upgrade-error = unable-to-fetch-package /
 * not-a-package           /
 * incompatible-upgrade    /
 * digest-does-not-match   /
 * unknown-upgrade-policy  /
 * package-id-does-not-match
 *
 * unable-to-fetch-package     = %x00 object-id
 * not-a-package               = %x01 object-id
 * incompatible-upgrade        = %x02
 * digest-does-not-match       = %x03 digest
 * unknown-upgrade-policy      = %x04 u8
 * package-id-does-not-match   = %x05 object-id object-id
 * ```
 */

public enum PackageUpgradeError {
    
    /**
     * Unable to fetch package
     */
    case unableToFetchPackage(packageId: ObjectId
    )
    /**
     * Object is not a package
     */
    case notAPackage(objectId: ObjectId
    )
    /**
     * Package upgrade is incompatible with previous version
     */
    case incompatibleUpgrade
    /**
     * Digest in upgrade ticket and computed digest differ
     */
    case digestDoesNotMatch(digest: Digest
    )
    /**
     * Upgrade policy is not valid
     */
    case unknownUpgradePolicy(policy: UInt8
    )
    /**
     * PackageId does not matach PackageId in upgrade ticket
     */
    case packageIdDoesNotMatch(packageId: ObjectId, ticketId: ObjectId
    )
}


#if compiler(>=6)
extension PackageUpgradeError: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypePackageUpgradeError: FfiConverterRustBuffer {
    typealias SwiftType = PackageUpgradeError

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> PackageUpgradeError {
        let variant: Int32 = try readInt(&buf)
        switch variant {
        
        case 1: return .unableToFetchPackage(packageId: try FfiConverterTypeObjectId.read(from: &buf)
        )
        
        case 2: return .notAPackage(objectId: try FfiConverterTypeObjectId.read(from: &buf)
        )
        
        case 3: return .incompatibleUpgrade
        
        case 4: return .digestDoesNotMatch(digest: try FfiConverterTypeDigest.read(from: &buf)
        )
        
        case 5: return .unknownUpgradePolicy(policy: try FfiConverterUInt8.read(from: &buf)
        )
        
        case 6: return .packageIdDoesNotMatch(packageId: try FfiConverterTypeObjectId.read(from: &buf), ticketId: try FfiConverterTypeObjectId.read(from: &buf)
        )
        
        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: PackageUpgradeError, into buf: inout [UInt8]) {
        switch value {
        
        
        case let .unableToFetchPackage(packageId):
            writeInt(&buf, Int32(1))
            FfiConverterTypeObjectId.write(packageId, into: &buf)
            
        
        case let .notAPackage(objectId):
            writeInt(&buf, Int32(2))
            FfiConverterTypeObjectId.write(objectId, into: &buf)
            
        
        case .incompatibleUpgrade:
            writeInt(&buf, Int32(3))
        
        
        case let .digestDoesNotMatch(digest):
            writeInt(&buf, Int32(4))
            FfiConverterTypeDigest.write(digest, into: &buf)
            
        
        case let .unknownUpgradePolicy(policy):
            writeInt(&buf, Int32(5))
            FfiConverterUInt8.write(policy, into: &buf)
            
        
        case let .packageIdDoesNotMatch(packageId,ticketId):
            writeInt(&buf, Int32(6))
            FfiConverterTypeObjectId.write(packageId, into: &buf)
            FfiConverterTypeObjectId.write(ticketId, into: &buf)
            
        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePackageUpgradeError_lift(_ buf: RustBuffer) throws -> PackageUpgradeError {
    return try FfiConverterTypePackageUpgradeError.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePackageUpgradeError_lower(_ value: PackageUpgradeError) -> RustBuffer {
    return FfiConverterTypePackageUpgradeError.lower(value)
}







public enum SdkFfiError: Swift.Error {

    
    
    case Generic(message: String)
    
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSdkFfiError: FfiConverterRustBuffer {
    typealias SwiftType = SdkFfiError

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SdkFfiError {
        let variant: Int32 = try readInt(&buf)
        switch variant {

        

        
        case 1: return .Generic(
            message: try FfiConverterString.read(from: &buf)
        )
        

        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: SdkFfiError, into buf: inout [UInt8]) {
        switch value {

        

        
        case .Generic(_ /* message is ignored*/):
            writeInt(&buf, Int32(1))

        
        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSdkFfiError_lift(_ buf: RustBuffer) throws -> SdkFfiError {
    return try FfiConverterTypeSdkFfiError.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSdkFfiError_lower(_ value: SdkFfiError) -> RustBuffer {
    return FfiConverterTypeSdkFfiError.lower(value)
}


extension SdkFfiError: Equatable, Hashable {}




extension SdkFfiError: Foundation.LocalizedError {
    public var errorDescription: String? {
        String(reflecting: self)
    }
}




// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.
/**
 * Flag use to disambiguate the signature schemes supported by IOTA.
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * signature-scheme = ed25519-flag / secp256k1-flag / secp256r1-flag /
 * multisig-flag / bls-flag / zklogin-auth-flag / passkey-auth-flag /
 * move-auth-flag
 * ed25519-flag        = %x00
 * secp256k1-flag      = %x01
 * secp256r1-flag      = %x02
 * multisig-flag       = %x03
 * bls-flag            = %x04
 * zklogin-auth-flag   = %x05
 * passkey-auth-flag   = %x06
 * move-auth-flag      = %x07
 * ```
 */

public enum SignatureScheme : UInt8 {
    
    case ed25519 = 0
    case secp256k1 = 1
    case secp256r1 = 2
    case multisig = 3
    case bls12381 = 4
    case zkLoginAuthenticator = 5
    case passkeyAuthenticator = 6
    case moveAuthenticator = 7
}


#if compiler(>=6)
extension SignatureScheme: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSignatureScheme: FfiConverterRustBuffer {
    typealias SwiftType = SignatureScheme

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SignatureScheme {
        let variant: Int32 = try readInt(&buf)
        switch variant {
        
        case 1: return .ed25519
        
        case 2: return .secp256k1
        
        case 3: return .secp256r1
        
        case 4: return .multisig
        
        case 5: return .bls12381
        
        case 6: return .zkLoginAuthenticator
        
        case 7: return .passkeyAuthenticator
        
        case 8: return .moveAuthenticator
        
        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: SignatureScheme, into buf: inout [UInt8]) {
        switch value {
        
        
        case .ed25519:
            writeInt(&buf, Int32(1))
        
        
        case .secp256k1:
            writeInt(&buf, Int32(2))
        
        
        case .secp256r1:
            writeInt(&buf, Int32(3))
        
        
        case .multisig:
            writeInt(&buf, Int32(4))
        
        
        case .bls12381:
            writeInt(&buf, Int32(5))
        
        
        case .zkLoginAuthenticator:
            writeInt(&buf, Int32(6))
        
        
        case .passkeyAuthenticator:
            writeInt(&buf, Int32(7))
        
        
        case .moveAuthenticator:
            writeInt(&buf, Int32(8))
        
        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSignatureScheme_lift(_ buf: RustBuffer) throws -> SignatureScheme {
    return try FfiConverterTypeSignatureScheme.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSignatureScheme_lower(_ value: SignatureScheme) -> RustBuffer {
    return FfiConverterTypeSignatureScheme.lower(value)
}


extension SignatureScheme: Equatable, Hashable {}






// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.
/**
 * A transaction argument used in programmable transactions.
 */

public enum TransactionArgument {
    
    /**
     * Reference to the gas coin.
     */
    case gasCoin
    /**
     * An input to the programmable transaction block.
     */
    case input(
        /**
         * Index of the programmable transaction block input (0-indexed).
         */index: UInt32
    )
    /**
     * The result of another transaction command.
     */
    case result(
        /**
         * The index of the previous command (0-indexed) that returned this
         * result.
         */cmd: UInt32, 
        /**
         * If the previous command returns multiple values, this is the index
         * of the individual result among the multiple results from
         * that command (also 0-indexed).
         */index: UInt32?
    )
}


#if compiler(>=6)
extension TransactionArgument: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeTransactionArgument: FfiConverterRustBuffer {
    typealias SwiftType = TransactionArgument

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> TransactionArgument {
        let variant: Int32 = try readInt(&buf)
        switch variant {
        
        case 1: return .gasCoin
        
        case 2: return .input(index: try FfiConverterUInt32.read(from: &buf)
        )
        
        case 3: return .result(cmd: try FfiConverterUInt32.read(from: &buf), index: try FfiConverterOptionUInt32.read(from: &buf)
        )
        
        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: TransactionArgument, into buf: inout [UInt8]) {
        switch value {
        
        
        case .gasCoin:
            writeInt(&buf, Int32(1))
        
        
        case let .input(index):
            writeInt(&buf, Int32(2))
            FfiConverterUInt32.write(index, into: &buf)
            
        
        case let .result(cmd,index):
            writeInt(&buf, Int32(3))
            FfiConverterUInt32.write(cmd, into: &buf)
            FfiConverterOptionUInt32.write(index, into: &buf)
            
        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionArgument_lift(_ buf: RustBuffer) throws -> TransactionArgument {
    return try FfiConverterTypeTransactionArgument.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionArgument_lower(_ value: TransactionArgument) -> RustBuffer {
    return FfiConverterTypeTransactionArgument.lower(value)
}


extension TransactionArgument: Equatable, Hashable {}






// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.

public enum TransactionBlockKindInput {
    
    case systemTx
    case programmableTx
    case genesis
    case consensusCommitPrologueV1
    case authenticatorStateUpdateV1
    case randomnessStateUpdate
    case endOfEpochTx
}


#if compiler(>=6)
extension TransactionBlockKindInput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeTransactionBlockKindInput: FfiConverterRustBuffer {
    typealias SwiftType = TransactionBlockKindInput

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> TransactionBlockKindInput {
        let variant: Int32 = try readInt(&buf)
        switch variant {
        
        case 1: return .systemTx
        
        case 2: return .programmableTx
        
        case 3: return .genesis
        
        case 4: return .consensusCommitPrologueV1
        
        case 5: return .authenticatorStateUpdateV1
        
        case 6: return .randomnessStateUpdate
        
        case 7: return .endOfEpochTx
        
        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: TransactionBlockKindInput, into buf: inout [UInt8]) {
        switch value {
        
        
        case .systemTx:
            writeInt(&buf, Int32(1))
        
        
        case .programmableTx:
            writeInt(&buf, Int32(2))
        
        
        case .genesis:
            writeInt(&buf, Int32(3))
        
        
        case .consensusCommitPrologueV1:
            writeInt(&buf, Int32(4))
        
        
        case .authenticatorStateUpdateV1:
            writeInt(&buf, Int32(5))
        
        
        case .randomnessStateUpdate:
            writeInt(&buf, Int32(6))
        
        
        case .endOfEpochTx:
            writeInt(&buf, Int32(7))
        
        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionBlockKindInput_lift(_ buf: RustBuffer) throws -> TransactionBlockKindInput {
    return try FfiConverterTypeTransactionBlockKindInput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionBlockKindInput_lower(_ value: TransactionBlockKindInput) -> RustBuffer {
    return FfiConverterTypeTransactionBlockKindInput.lower(value)
}


extension TransactionBlockKindInput: Equatable, Hashable {}






// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.
/**
 * A TTL for a transaction
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * transaction-expiration =  %x00      ; none
 * =/ %x01 u64  ; epoch
 * ```
 */

public enum TransactionExpiration {
    
    /**
     * The transaction has no expiration
     */
    case none
    /**
     * Validators won't sign a transaction unless the expiration Epoch
     * is greater than or equal to the current epoch
     */
    case epoch(UInt64
    )
}


#if compiler(>=6)
extension TransactionExpiration: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeTransactionExpiration: FfiConverterRustBuffer {
    typealias SwiftType = TransactionExpiration

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> TransactionExpiration {
        let variant: Int32 = try readInt(&buf)
        switch variant {
        
        case 1: return .none
        
        case 2: return .epoch(try FfiConverterUInt64.read(from: &buf)
        )
        
        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: TransactionExpiration, into buf: inout [UInt8]) {
        switch value {
        
        
        case .none:
            writeInt(&buf, Int32(1))
        
        
        case let .epoch(v1):
            writeInt(&buf, Int32(2))
            FfiConverterUInt64.write(v1, into: &buf)
            
        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionExpiration_lift(_ buf: RustBuffer) throws -> TransactionExpiration {
    return try FfiConverterTypeTransactionExpiration.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTransactionExpiration_lower(_ value: TransactionExpiration) -> RustBuffer {
    return FfiConverterTypeTransactionExpiration.lower(value)
}


extension TransactionExpiration: Equatable, Hashable {}






// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.
/**
 * An error with a type argument
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * type-argument-error = type-not-found / constraint-not-satisfied
 * type-not-found = %x00
 * constraint-not-satisfied = %x01
 * ```
 */

public enum TypeArgumentError : UInt8 {
    
    /**
     * A type was not found in the module specified
     */
    case typeNotFound = 0
    /**
     * A type provided did not match the specified constraint
     */
    case constraintNotSatisfied = 1
}


#if compiler(>=6)
extension TypeArgumentError: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeTypeArgumentError: FfiConverterRustBuffer {
    typealias SwiftType = TypeArgumentError

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> TypeArgumentError {
        let variant: Int32 = try readInt(&buf)
        switch variant {
        
        case 1: return .typeNotFound
        
        case 2: return .constraintNotSatisfied
        
        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: TypeArgumentError, into buf: inout [UInt8]) {
        switch value {
        
        
        case .typeNotFound:
            writeInt(&buf, Int32(1))
        
        
        case .constraintNotSatisfied:
            writeInt(&buf, Int32(2))
        
        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTypeArgumentError_lift(_ buf: RustBuffer) throws -> TypeArgumentError {
    return try FfiConverterTypeTypeArgumentError.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTypeArgumentError_lower(_ value: TypeArgumentError) -> RustBuffer {
    return FfiConverterTypeTypeArgumentError.lower(value)
}


extension TypeArgumentError: Equatable, Hashable {}






// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.
/**
 * Type of unchanged shared object
 *
 * # BCS
 *
 * The BCS serialized form for this type is defined by the following ABNF:
 *
 * ```text
 * unchanged-shared-object-kind =  read-only-root
 * =/ mutate-deleted
 * =/ read-deleted
 * =/ cancelled
 * =/ per-epoch-config
 *
 * read-only-root      = %x00 u64 digest
 * mutate-deleted      = %x01 u64
 * read-deleted        = %x02 u64
 * cancelled           = %x03 u64
 * per-epoch-config    = %x04
 * ```
 */

public enum UnchangedSharedKind {
    
    /**
     * Read-only shared objects from the input. We don't really need
     * ObjectDigest for protocol correctness, but it will make it easier to
     * verify untrusted read.
     */
    case readOnlyRoot(version: UInt64, digest: Digest
    )
    /**
     * Deleted shared objects that appear mutably/owned in the input.
     */
    case mutateDeleted(version: UInt64
    )
    /**
     * Deleted shared objects that appear as read-only in the input.
     */
    case readDeleted(version: UInt64
    )
    /**
     * Shared objects in cancelled transaction. The sequence number embed
     * cancellation reason.
     */
    case cancelled(version: UInt64
    )
    /**
     * Read of a per-epoch config object that should remain the same during an
     * epoch.
     */
    case perEpochConfig
}


#if compiler(>=6)
extension UnchangedSharedKind: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeUnchangedSharedKind: FfiConverterRustBuffer {
    typealias SwiftType = UnchangedSharedKind

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> UnchangedSharedKind {
        let variant: Int32 = try readInt(&buf)
        switch variant {
        
        case 1: return .readOnlyRoot(version: try FfiConverterUInt64.read(from: &buf), digest: try FfiConverterTypeDigest.read(from: &buf)
        )
        
        case 2: return .mutateDeleted(version: try FfiConverterUInt64.read(from: &buf)
        )
        
        case 3: return .readDeleted(version: try FfiConverterUInt64.read(from: &buf)
        )
        
        case 4: return .cancelled(version: try FfiConverterUInt64.read(from: &buf)
        )
        
        case 5: return .perEpochConfig
        
        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: UnchangedSharedKind, into buf: inout [UInt8]) {
        switch value {
        
        
        case let .readOnlyRoot(version,digest):
            writeInt(&buf, Int32(1))
            FfiConverterUInt64.write(version, into: &buf)
            FfiConverterTypeDigest.write(digest, into: &buf)
            
        
        case let .mutateDeleted(version):
            writeInt(&buf, Int32(2))
            FfiConverterUInt64.write(version, into: &buf)
            
        
        case let .readDeleted(version):
            writeInt(&buf, Int32(3))
            FfiConverterUInt64.write(version, into: &buf)
            
        
        case let .cancelled(version):
            writeInt(&buf, Int32(4))
            FfiConverterUInt64.write(version, into: &buf)
            
        
        case .perEpochConfig:
            writeInt(&buf, Int32(5))
        
        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeUnchangedSharedKind_lift(_ buf: RustBuffer) throws -> UnchangedSharedKind {
    return try FfiConverterTypeUnchangedSharedKind.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeUnchangedSharedKind_lower(_ value: UnchangedSharedKind) -> RustBuffer {
    return FfiConverterTypeUnchangedSharedKind.lower(value)
}






// Note that we don't yet support `indirect` for enums.
// See https://github.com/mozilla/uniffi-rs/issues/396 for further discussion.
/**
 * Determines what to wait for after executing a transaction.
 *
 * Users should almost always use WaitForTx::Finalized (the default).
 * The GraphQL client interacts with the indexer, not the fullnode directly.
 * Using WaitForTx::IndexedOnNode only guarantees the transaction is
 * indexed on the fullnode (meaning you can submit transactions that reference
 * objects created by this transaction), but subsequent queries using the
 * transaction ID can still fail until the transaction is indexed on the
 * indexer.
 */

public enum WaitForTx {
    
    /**
     * Indicates that the transaction effects will be usable in subsequent
     * transactions (you can reference objects created by this transaction),
     * and that the transaction itself is indexed on the fullnode.
     *
     * **Warning:** This does not guarantee the transaction is indexed on the
     * indexer. Since the GraphQL client queries the indexer, subsequent
     * queries with this transaction ID may still fail. Prefer
     * WaitForTx::Finalized unless you have a specific reason to use this.
     */
    case indexedOnNode
    /**
     * Indicates that the transaction has been included in a checkpoint, and
     * all queries may include it.
     */
    case finalized
}


#if compiler(>=6)
extension WaitForTx: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeWaitForTx: FfiConverterRustBuffer {
    typealias SwiftType = WaitForTx

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> WaitForTx {
        let variant: Int32 = try readInt(&buf)
        switch variant {
        
        case 1: return .indexedOnNode
        
        case 2: return .finalized
        
        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: WaitForTx, into buf: inout [UInt8]) {
        switch value {
        
        
        case .indexedOnNode:
            writeInt(&buf, Int32(1))
        
        
        case .finalized:
            writeInt(&buf, Int32(2))
        
        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeWaitForTx_lift(_ buf: RustBuffer) throws -> WaitForTx {
    return try FfiConverterTypeWaitForTx.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeWaitForTx_lower(_ value: WaitForTx) -> RustBuffer {
    return FfiConverterTypeWaitForTx.lower(value)
}


extension WaitForTx: Equatable, Hashable {}






#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionUInt32: FfiConverterRustBuffer {
    typealias SwiftType = UInt32?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterUInt32.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterUInt32.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionInt32: FfiConverterRustBuffer {
    typealias SwiftType = Int32?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterInt32.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterInt32.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionUInt64: FfiConverterRustBuffer {
    typealias SwiftType = UInt64?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterUInt64.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterUInt64.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionString: FfiConverterRustBuffer {
    typealias SwiftType = String?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterString.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterString.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionData: FfiConverterRustBuffer {
    typealias SwiftType = Data?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterData.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterData.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionDuration: FfiConverterRustBuffer {
    typealias SwiftType = TimeInterval?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterDuration.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterDuration.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeAddress: FfiConverterRustBuffer {
    typealias SwiftType = Address?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeAddress.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeAddress.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeArgument: FfiConverterRustBuffer {
    typealias SwiftType = Argument?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeArgument.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeArgument.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeCheckpointSummary: FfiConverterRustBuffer {
    typealias SwiftType = CheckpointSummary?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeCheckpointSummary.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeCheckpointSummary.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeDigest: FfiConverterRustBuffer {
    typealias SwiftType = Digest?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeDigest.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeDigest.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeEd25519PublicKey: FfiConverterRustBuffer {
    typealias SwiftType = Ed25519PublicKey?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeEd25519PublicKey.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeEd25519PublicKey.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeEd25519Signature: FfiConverterRustBuffer {
    typealias SwiftType = Ed25519Signature?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeEd25519Signature.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeEd25519Signature.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeMoveArg: FfiConverterRustBuffer {
    typealias SwiftType = MoveArg?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeMoveArg.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeMoveArg.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeMoveAuthenticator: FfiConverterRustBuffer {
    typealias SwiftType = MoveAuthenticator?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeMoveAuthenticator.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeMoveAuthenticator.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeMoveFunction: FfiConverterRustBuffer {
    typealias SwiftType = MoveFunction?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeMoveFunction.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeMoveFunction.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeMovePackage: FfiConverterRustBuffer {
    typealias SwiftType = MovePackage?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeMovePackage.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeMovePackage.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeMoveViewArg: FfiConverterRustBuffer {
    typealias SwiftType = MoveViewArg?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeMoveViewArg.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeMoveViewArg.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeMultisigAggregatedSignature: FfiConverterRustBuffer {
    typealias SwiftType = MultisigAggregatedSignature?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeMultisigAggregatedSignature.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeMultisigAggregatedSignature.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeName: FfiConverterRustBuffer {
    typealias SwiftType = Name?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeName.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeName.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeObject: FfiConverterRustBuffer {
    typealias SwiftType = Object?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeObject.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeObject.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeObjectId: FfiConverterRustBuffer {
    typealias SwiftType = ObjectId?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeObjectId.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeObjectId.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypePTBArgument: FfiConverterRustBuffer {
    typealias SwiftType = PtbArgument?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypePTBArgument.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypePTBArgument.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypePasskeyAuthenticator: FfiConverterRustBuffer {
    typealias SwiftType = PasskeyAuthenticator?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypePasskeyAuthenticator.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypePasskeyAuthenticator.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeSecp256k1PublicKey: FfiConverterRustBuffer {
    typealias SwiftType = Secp256k1PublicKey?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeSecp256k1PublicKey.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeSecp256k1PublicKey.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeSecp256k1Signature: FfiConverterRustBuffer {
    typealias SwiftType = Secp256k1Signature?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeSecp256k1Signature.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeSecp256k1Signature.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeSecp256r1PublicKey: FfiConverterRustBuffer {
    typealias SwiftType = Secp256r1PublicKey?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeSecp256r1PublicKey.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeSecp256r1PublicKey.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeSecp256r1Signature: FfiConverterRustBuffer {
    typealias SwiftType = Secp256r1Signature?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeSecp256r1Signature.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeSecp256r1Signature.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeSimpleSignature: FfiConverterRustBuffer {
    typealias SwiftType = SimpleSignature?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeSimpleSignature.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeSimpleSignature.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeStructTag: FfiConverterRustBuffer {
    typealias SwiftType = StructTag?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeStructTag.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeStructTag.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeTransactionEffects: FfiConverterRustBuffer {
    typealias SwiftType = TransactionEffects?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeTransactionEffects.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeTransactionEffects.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeTypeTag: FfiConverterRustBuffer {
    typealias SwiftType = TypeTag?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeTypeTag.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeTypeTag.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeZkLoginAuthenticator: FfiConverterRustBuffer {
    typealias SwiftType = ZkLoginAuthenticator?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeZkLoginAuthenticator.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeZkLoginAuthenticator.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeZkLoginPublicIdentifier: FfiConverterRustBuffer {
    typealias SwiftType = ZkLoginPublicIdentifier?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeZkLoginPublicIdentifier.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeZkLoginPublicIdentifier.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeZkloginVerifier: FfiConverterRustBuffer {
    typealias SwiftType = ZkloginVerifier?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeZkloginVerifier.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeZkloginVerifier.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeBatchSendStatus: FfiConverterRustBuffer {
    typealias SwiftType = BatchSendStatus?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeBatchSendStatus.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeBatchSendStatus.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeCoinMetadata: FfiConverterRustBuffer {
    typealias SwiftType = CoinMetadata?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeCoinMetadata.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeCoinMetadata.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeDynamicFieldOutput: FfiConverterRustBuffer {
    typealias SwiftType = DynamicFieldOutput?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeDynamicFieldOutput.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeDynamicFieldOutput.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeDynamicFieldValue: FfiConverterRustBuffer {
    typealias SwiftType = DynamicFieldValue?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeDynamicFieldValue.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeDynamicFieldValue.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeEndOfEpochData: FfiConverterRustBuffer {
    typealias SwiftType = EndOfEpochData?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeEndOfEpochData.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeEndOfEpochData.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeEpoch: FfiConverterRustBuffer {
    typealias SwiftType = Epoch?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeEpoch.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeEpoch.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeEventFilter: FfiConverterRustBuffer {
    typealias SwiftType = EventFilter?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeEventFilter.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeEventFilter.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeFaucetReceipt: FfiConverterRustBuffer {
    typealias SwiftType = FaucetReceipt?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeFaucetReceipt.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeFaucetReceipt.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeMoveEnumConnection: FfiConverterRustBuffer {
    typealias SwiftType = MoveEnumConnection?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeMoveEnumConnection.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeMoveEnumConnection.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeMoveFunctionConnection: FfiConverterRustBuffer {
    typealias SwiftType = MoveFunctionConnection?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeMoveFunctionConnection.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeMoveFunctionConnection.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeMoveLocation: FfiConverterRustBuffer {
    typealias SwiftType = MoveLocation?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeMoveLocation.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeMoveLocation.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeMoveModule: FfiConverterRustBuffer {
    typealias SwiftType = MoveModule?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeMoveModule.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeMoveModule.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeMoveStruct: FfiConverterRustBuffer {
    typealias SwiftType = MoveStruct?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeMoveStruct.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeMoveStruct.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeMoveStructConnection: FfiConverterRustBuffer {
    typealias SwiftType = MoveStructConnection?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeMoveStructConnection.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeMoveStructConnection.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeObjectFilter: FfiConverterRustBuffer {
    typealias SwiftType = ObjectFilter?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeObjectFilter.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeObjectFilter.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeOpenMoveType: FfiConverterRustBuffer {
    typealias SwiftType = OpenMoveType?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeOpenMoveType.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeOpenMoveType.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypePaginationFilter: FfiConverterRustBuffer {
    typealias SwiftType = PaginationFilter?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypePaginationFilter.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypePaginationFilter.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeProtocolConfigs: FfiConverterRustBuffer {
    typealias SwiftType = ProtocolConfigs?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeProtocolConfigs.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeProtocolConfigs.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeSignedTransaction: FfiConverterRustBuffer {
    typealias SwiftType = SignedTransaction?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeSignedTransaction.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeSignedTransaction.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeTransactionDataEffects: FfiConverterRustBuffer {
    typealias SwiftType = TransactionDataEffects?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeTransactionDataEffects.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeTransactionDataEffects.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeTransactionsFilter: FfiConverterRustBuffer {
    typealias SwiftType = TransactionsFilter?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeTransactionsFilter.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeTransactionsFilter.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeValidatorCredentials: FfiConverterRustBuffer {
    typealias SwiftType = ValidatorCredentials?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeValidatorCredentials.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeValidatorCredentials.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeValidatorSet: FfiConverterRustBuffer {
    typealias SwiftType = ValidatorSet?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeValidatorSet.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeValidatorSet.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeMnemonicLength: FfiConverterRustBuffer {
    typealias SwiftType = MnemonicLength?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeMnemonicLength.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeMnemonicLength.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeMoveVisibility: FfiConverterRustBuffer {
    typealias SwiftType = MoveVisibility?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeMoveVisibility.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeMoveVisibility.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeNameFormat: FfiConverterRustBuffer {
    typealias SwiftType = NameFormat?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeNameFormat.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeNameFormat.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeTransactionBlockKindInput: FfiConverterRustBuffer {
    typealias SwiftType = TransactionBlockKindInput?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeTransactionBlockKindInput.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeTransactionBlockKindInput.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeWaitForTx: FfiConverterRustBuffer {
    typealias SwiftType = WaitForTx?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeWaitForTx.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeWaitForTx.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionSequenceInt32: FfiConverterRustBuffer {
    typealias SwiftType = [Int32]?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterSequenceInt32.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterSequenceInt32.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionSequenceString: FfiConverterRustBuffer {
    typealias SwiftType = [String]?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterSequenceString.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterSequenceString.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionSequenceTypeMoveViewArg: FfiConverterRustBuffer {
    typealias SwiftType = [MoveViewArg]?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterSequenceTypeMoveViewArg.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterSequenceTypeMoveViewArg.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionSequenceTypeObjectId: FfiConverterRustBuffer {
    typealias SwiftType = [ObjectId]?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterSequenceTypeObjectId.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterSequenceTypeObjectId.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionSequenceTypeTypeTag: FfiConverterRustBuffer {
    typealias SwiftType = [TypeTag]?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterSequenceTypeTypeTag.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterSequenceTypeTypeTag.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionSequenceTypeMoveEnumVariant: FfiConverterRustBuffer {
    typealias SwiftType = [MoveEnumVariant]?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterSequenceTypeMoveEnumVariant.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterSequenceTypeMoveEnumVariant.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionSequenceTypeMoveField: FfiConverterRustBuffer {
    typealias SwiftType = [MoveField]?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterSequenceTypeMoveField.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterSequenceTypeMoveField.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionSequenceTypeMoveFunctionTypeParameter: FfiConverterRustBuffer {
    typealias SwiftType = [MoveFunctionTypeParameter]?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterSequenceTypeMoveFunctionTypeParameter.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterSequenceTypeMoveFunctionTypeParameter.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionSequenceTypeMoveStructTypeParameter: FfiConverterRustBuffer {
    typealias SwiftType = [MoveStructTypeParameter]?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterSequenceTypeMoveStructTypeParameter.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterSequenceTypeMoveStructTypeParameter.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionSequenceTypeObjectRef: FfiConverterRustBuffer {
    typealias SwiftType = [ObjectRef]?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterSequenceTypeObjectRef.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterSequenceTypeObjectRef.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionSequenceTypeOpenMoveType: FfiConverterRustBuffer {
    typealias SwiftType = [OpenMoveType]?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterSequenceTypeOpenMoveType.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterSequenceTypeOpenMoveType.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionSequenceTypeMoveAbility: FfiConverterRustBuffer {
    typealias SwiftType = [MoveAbility]?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterSequenceTypeMoveAbility.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterSequenceTypeMoveAbility.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionSequenceTypeValue: FfiConverterRustBuffer {
    typealias SwiftType = [Value]?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterSequenceTypeValue.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterSequenceTypeValue.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionDictionaryStringSequenceString: FfiConverterRustBuffer {
    typealias SwiftType = [String: [String]]?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterDictionaryStringSequenceString.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterDictionaryStringSequenceString.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeBase64: FfiConverterRustBuffer {
    typealias SwiftType = Base64?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeBase64.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeBase64.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeBigInt: FfiConverterRustBuffer {
    typealias SwiftType = BigInt?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeBigInt.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeBigInt.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionTypeValue: FfiConverterRustBuffer {
    typealias SwiftType = Value?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterTypeValue.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterTypeValue.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceUInt16: FfiConverterRustBuffer {
    typealias SwiftType = [UInt16]

    public static func write(_ value: [UInt16], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterUInt16.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [UInt16] {
        let len: Int32 = try readInt(&buf)
        var seq = [UInt16]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterUInt16.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceUInt32: FfiConverterRustBuffer {
    typealias SwiftType = [UInt32]

    public static func write(_ value: [UInt32], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterUInt32.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [UInt32] {
        let len: Int32 = try readInt(&buf)
        var seq = [UInt32]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterUInt32.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceInt32: FfiConverterRustBuffer {
    typealias SwiftType = [Int32]

    public static func write(_ value: [Int32], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterInt32.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [Int32] {
        let len: Int32 = try readInt(&buf)
        var seq = [Int32]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterInt32.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceUInt64: FfiConverterRustBuffer {
    typealias SwiftType = [UInt64]

    public static func write(_ value: [UInt64], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterUInt64.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [UInt64] {
        let len: Int32 = try readInt(&buf)
        var seq = [UInt64]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterUInt64.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceBool: FfiConverterRustBuffer {
    typealias SwiftType = [Bool]

    public static func write(_ value: [Bool], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterBool.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [Bool] {
        let len: Int32 = try readInt(&buf)
        var seq = [Bool]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterBool.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceString: FfiConverterRustBuffer {
    typealias SwiftType = [String]

    public static func write(_ value: [String], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterString.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [String] {
        let len: Int32 = try readInt(&buf)
        var seq = [String]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterString.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceData: FfiConverterRustBuffer {
    typealias SwiftType = [Data]

    public static func write(_ value: [Data], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterData.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [Data] {
        let len: Int32 = try readInt(&buf)
        var seq = [Data]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterData.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeAddress: FfiConverterRustBuffer {
    typealias SwiftType = [Address]

    public static func write(_ value: [Address], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeAddress.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [Address] {
        let len: Int32 = try readInt(&buf)
        var seq = [Address]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeAddress.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeArgument: FfiConverterRustBuffer {
    typealias SwiftType = [Argument]

    public static func write(_ value: [Argument], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeArgument.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [Argument] {
        let len: Int32 = try readInt(&buf)
        var seq = [Argument]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeArgument.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeCancelledTransaction: FfiConverterRustBuffer {
    typealias SwiftType = [CancelledTransaction]

    public static func write(_ value: [CancelledTransaction], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeCancelledTransaction.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [CancelledTransaction] {
        let len: Int32 = try readInt(&buf)
        var seq = [CancelledTransaction]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeCancelledTransaction.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeCheckpointCommitment: FfiConverterRustBuffer {
    typealias SwiftType = [CheckpointCommitment]

    public static func write(_ value: [CheckpointCommitment], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeCheckpointCommitment.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [CheckpointCommitment] {
        let len: Int32 = try readInt(&buf)
        var seq = [CheckpointCommitment]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeCheckpointCommitment.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeCheckpointSummary: FfiConverterRustBuffer {
    typealias SwiftType = [CheckpointSummary]

    public static func write(_ value: [CheckpointSummary], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeCheckpointSummary.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [CheckpointSummary] {
        let len: Int32 = try readInt(&buf)
        var seq = [CheckpointSummary]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeCheckpointSummary.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeCheckpointTransactionInfo: FfiConverterRustBuffer {
    typealias SwiftType = [CheckpointTransactionInfo]

    public static func write(_ value: [CheckpointTransactionInfo], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeCheckpointTransactionInfo.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [CheckpointTransactionInfo] {
        let len: Int32 = try readInt(&buf)
        var seq = [CheckpointTransactionInfo]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeCheckpointTransactionInfo.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeCoin: FfiConverterRustBuffer {
    typealias SwiftType = [Coin]

    public static func write(_ value: [Coin], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeCoin.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [Coin] {
        let len: Int32 = try readInt(&buf)
        var seq = [Coin]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeCoin.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeCommand: FfiConverterRustBuffer {
    typealias SwiftType = [Command]

    public static func write(_ value: [Command], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeCommand.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [Command] {
        let len: Int32 = try readInt(&buf)
        var seq = [Command]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeCommand.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeDigest: FfiConverterRustBuffer {
    typealias SwiftType = [Digest]

    public static func write(_ value: [Digest], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeDigest.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [Digest] {
        let len: Int32 = try readInt(&buf)
        var seq = [Digest]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeDigest.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeEndOfEpochTransactionKind: FfiConverterRustBuffer {
    typealias SwiftType = [EndOfEpochTransactionKind]

    public static func write(_ value: [EndOfEpochTransactionKind], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeEndOfEpochTransactionKind.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [EndOfEpochTransactionKind] {
        let len: Int32 = try readInt(&buf)
        var seq = [EndOfEpochTransactionKind]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeEndOfEpochTransactionKind.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeExecutionTimeObservation: FfiConverterRustBuffer {
    typealias SwiftType = [ExecutionTimeObservation]

    public static func write(_ value: [ExecutionTimeObservation], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeExecutionTimeObservation.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [ExecutionTimeObservation] {
        let len: Int32 = try readInt(&buf)
        var seq = [ExecutionTimeObservation]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeExecutionTimeObservation.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeGenesisObject: FfiConverterRustBuffer {
    typealias SwiftType = [GenesisObject]

    public static func write(_ value: [GenesisObject], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeGenesisObject.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [GenesisObject] {
        let len: Int32 = try readInt(&buf)
        var seq = [GenesisObject]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeGenesisObject.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeInput: FfiConverterRustBuffer {
    typealias SwiftType = [Input]

    public static func write(_ value: [Input], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeInput.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [Input] {
        let len: Int32 = try readInt(&buf)
        var seq = [Input]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeInput.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeMoveArg: FfiConverterRustBuffer {
    typealias SwiftType = [MoveArg]

    public static func write(_ value: [MoveArg], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeMoveArg.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [MoveArg] {
        let len: Int32 = try readInt(&buf)
        var seq = [MoveArg]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeMoveArg.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeMoveFunction: FfiConverterRustBuffer {
    typealias SwiftType = [MoveFunction]

    public static func write(_ value: [MoveFunction], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeMoveFunction.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [MoveFunction] {
        let len: Int32 = try readInt(&buf)
        var seq = [MoveFunction]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeMoveFunction.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeMovePackage: FfiConverterRustBuffer {
    typealias SwiftType = [MovePackage]

    public static func write(_ value: [MovePackage], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeMovePackage.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [MovePackage] {
        let len: Int32 = try readInt(&buf)
        var seq = [MovePackage]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeMovePackage.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeMoveViewArg: FfiConverterRustBuffer {
    typealias SwiftType = [MoveViewArg]

    public static func write(_ value: [MoveViewArg], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeMoveViewArg.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [MoveViewArg] {
        let len: Int32 = try readInt(&buf)
        var seq = [MoveViewArg]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeMoveViewArg.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeMultisigMember: FfiConverterRustBuffer {
    typealias SwiftType = [MultisigMember]

    public static func write(_ value: [MultisigMember], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeMultisigMember.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [MultisigMember] {
        let len: Int32 = try readInt(&buf)
        var seq = [MultisigMember]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeMultisigMember.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeMultisigMemberSignature: FfiConverterRustBuffer {
    typealias SwiftType = [MultisigMemberSignature]

    public static func write(_ value: [MultisigMemberSignature], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeMultisigMemberSignature.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [MultisigMemberSignature] {
        let len: Int32 = try readInt(&buf)
        var seq = [MultisigMemberSignature]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeMultisigMemberSignature.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeNameRegistration: FfiConverterRustBuffer {
    typealias SwiftType = [NameRegistration]

    public static func write(_ value: [NameRegistration], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeNameRegistration.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [NameRegistration] {
        let len: Int32 = try readInt(&buf)
        var seq = [NameRegistration]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeNameRegistration.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeObject: FfiConverterRustBuffer {
    typealias SwiftType = [Object]

    public static func write(_ value: [Object], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeObject.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [Object] {
        let len: Int32 = try readInt(&buf)
        var seq = [Object]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeObject.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeObjectId: FfiConverterRustBuffer {
    typealias SwiftType = [ObjectId]

    public static func write(_ value: [ObjectId], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeObjectId.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [ObjectId] {
        let len: Int32 = try readInt(&buf)
        var seq = [ObjectId]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeObjectId.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypePTBArgument: FfiConverterRustBuffer {
    typealias SwiftType = [PtbArgument]

    public static func write(_ value: [PtbArgument], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypePTBArgument.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [PtbArgument] {
        let len: Int32 = try readInt(&buf)
        var seq = [PtbArgument]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypePTBArgument.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeSystemPackage: FfiConverterRustBuffer {
    typealias SwiftType = [SystemPackage]

    public static func write(_ value: [SystemPackage], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeSystemPackage.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [SystemPackage] {
        let len: Int32 = try readInt(&buf)
        var seq = [SystemPackage]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeSystemPackage.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeTransactionEffects: FfiConverterRustBuffer {
    typealias SwiftType = [TransactionEffects]

    public static func write(_ value: [TransactionEffects], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeTransactionEffects.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [TransactionEffects] {
        let len: Int32 = try readInt(&buf)
        var seq = [TransactionEffects]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeTransactionEffects.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeTypeTag: FfiConverterRustBuffer {
    typealias SwiftType = [TypeTag]

    public static func write(_ value: [TypeTag], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeTypeTag.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [TypeTag] {
        let len: Int32 = try readInt(&buf)
        var seq = [TypeTag]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeTypeTag.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeUserSignature: FfiConverterRustBuffer {
    typealias SwiftType = [UserSignature]

    public static func write(_ value: [UserSignature], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeUserSignature.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [UserSignature] {
        let len: Int32 = try readInt(&buf)
        var seq = [UserSignature]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeUserSignature.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeValidatorExecutionTimeObservation: FfiConverterRustBuffer {
    typealias SwiftType = [ValidatorExecutionTimeObservation]

    public static func write(_ value: [ValidatorExecutionTimeObservation], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeValidatorExecutionTimeObservation.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [ValidatorExecutionTimeObservation] {
        let len: Int32 = try readInt(&buf)
        var seq = [ValidatorExecutionTimeObservation]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeValidatorExecutionTimeObservation.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeVersionAssignment: FfiConverterRustBuffer {
    typealias SwiftType = [VersionAssignment]

    public static func write(_ value: [VersionAssignment], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeVersionAssignment.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [VersionAssignment] {
        let len: Int32 = try readInt(&buf)
        var seq = [VersionAssignment]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeVersionAssignment.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeActiveJwk: FfiConverterRustBuffer {
    typealias SwiftType = [ActiveJwk]

    public static func write(_ value: [ActiveJwk], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeActiveJwk.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [ActiveJwk] {
        let len: Int32 = try readInt(&buf)
        var seq = [ActiveJwk]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeActiveJwk.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeChangedObject: FfiConverterRustBuffer {
    typealias SwiftType = [ChangedObject]

    public static func write(_ value: [ChangedObject], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeChangedObject.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [ChangedObject] {
        let len: Int32 = try readInt(&buf)
        var seq = [ChangedObject]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeChangedObject.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeCoinInfo: FfiConverterRustBuffer {
    typealias SwiftType = [CoinInfo]

    public static func write(_ value: [CoinInfo], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeCoinInfo.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [CoinInfo] {
        let len: Int32 = try readInt(&buf)
        var seq = [CoinInfo]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeCoinInfo.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeDryRunEffect: FfiConverterRustBuffer {
    typealias SwiftType = [DryRunEffect]

    public static func write(_ value: [DryRunEffect], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeDryRunEffect.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [DryRunEffect] {
        let len: Int32 = try readInt(&buf)
        var seq = [DryRunEffect]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeDryRunEffect.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeDryRunMutation: FfiConverterRustBuffer {
    typealias SwiftType = [DryRunMutation]

    public static func write(_ value: [DryRunMutation], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeDryRunMutation.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [DryRunMutation] {
        let len: Int32 = try readInt(&buf)
        var seq = [DryRunMutation]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeDryRunMutation.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeDryRunReturn: FfiConverterRustBuffer {
    typealias SwiftType = [DryRunReturn]

    public static func write(_ value: [DryRunReturn], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeDryRunReturn.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [DryRunReturn] {
        let len: Int32 = try readInt(&buf)
        var seq = [DryRunReturn]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeDryRunReturn.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeDynamicFieldOutput: FfiConverterRustBuffer {
    typealias SwiftType = [DynamicFieldOutput]

    public static func write(_ value: [DynamicFieldOutput], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeDynamicFieldOutput.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [DynamicFieldOutput] {
        let len: Int32 = try readInt(&buf)
        var seq = [DynamicFieldOutput]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeDynamicFieldOutput.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeEpoch: FfiConverterRustBuffer {
    typealias SwiftType = [Epoch]

    public static func write(_ value: [Epoch], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeEpoch.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [Epoch] {
        let len: Int32 = try readInt(&buf)
        var seq = [Epoch]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeEpoch.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeEvent: FfiConverterRustBuffer {
    typealias SwiftType = [Event]

    public static func write(_ value: [Event], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeEvent.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [Event] {
        let len: Int32 = try readInt(&buf)
        var seq = [Event]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeEvent.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeMoveEnum: FfiConverterRustBuffer {
    typealias SwiftType = [MoveEnum]

    public static func write(_ value: [MoveEnum], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeMoveEnum.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [MoveEnum] {
        let len: Int32 = try readInt(&buf)
        var seq = [MoveEnum]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeMoveEnum.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeMoveEnumVariant: FfiConverterRustBuffer {
    typealias SwiftType = [MoveEnumVariant]

    public static func write(_ value: [MoveEnumVariant], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeMoveEnumVariant.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [MoveEnumVariant] {
        let len: Int32 = try readInt(&buf)
        var seq = [MoveEnumVariant]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeMoveEnumVariant.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeMoveField: FfiConverterRustBuffer {
    typealias SwiftType = [MoveField]

    public static func write(_ value: [MoveField], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeMoveField.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [MoveField] {
        let len: Int32 = try readInt(&buf)
        var seq = [MoveField]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeMoveField.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeMoveFunctionTypeParameter: FfiConverterRustBuffer {
    typealias SwiftType = [MoveFunctionTypeParameter]

    public static func write(_ value: [MoveFunctionTypeParameter], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeMoveFunctionTypeParameter.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [MoveFunctionTypeParameter] {
        let len: Int32 = try readInt(&buf)
        var seq = [MoveFunctionTypeParameter]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeMoveFunctionTypeParameter.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeMoveModuleQuery: FfiConverterRustBuffer {
    typealias SwiftType = [MoveModuleQuery]

    public static func write(_ value: [MoveModuleQuery], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeMoveModuleQuery.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [MoveModuleQuery] {
        let len: Int32 = try readInt(&buf)
        var seq = [MoveModuleQuery]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeMoveModuleQuery.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeMoveStructQuery: FfiConverterRustBuffer {
    typealias SwiftType = [MoveStructQuery]

    public static func write(_ value: [MoveStructQuery], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeMoveStructQuery.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [MoveStructQuery] {
        let len: Int32 = try readInt(&buf)
        var seq = [MoveStructQuery]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeMoveStructQuery.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeMoveStructTypeParameter: FfiConverterRustBuffer {
    typealias SwiftType = [MoveStructTypeParameter]

    public static func write(_ value: [MoveStructTypeParameter], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeMoveStructTypeParameter.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [MoveStructTypeParameter] {
        let len: Int32 = try readInt(&buf)
        var seq = [MoveStructTypeParameter]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeMoveStructTypeParameter.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeObjectRef: FfiConverterRustBuffer {
    typealias SwiftType = [ObjectRef]

    public static func write(_ value: [ObjectRef], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeObjectRef.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [ObjectRef] {
        let len: Int32 = try readInt(&buf)
        var seq = [ObjectRef]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeObjectRef.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeObjectReference: FfiConverterRustBuffer {
    typealias SwiftType = [ObjectReference]

    public static func write(_ value: [ObjectReference], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeObjectReference.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [ObjectReference] {
        let len: Int32 = try readInt(&buf)
        var seq = [ObjectReference]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeObjectReference.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeOpenMoveType: FfiConverterRustBuffer {
    typealias SwiftType = [OpenMoveType]

    public static func write(_ value: [OpenMoveType], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeOpenMoveType.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [OpenMoveType] {
        let len: Int32 = try readInt(&buf)
        var seq = [OpenMoveType]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeOpenMoveType.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeProtocolConfigAttr: FfiConverterRustBuffer {
    typealias SwiftType = [ProtocolConfigAttr]

    public static func write(_ value: [ProtocolConfigAttr], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeProtocolConfigAttr.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [ProtocolConfigAttr] {
        let len: Int32 = try readInt(&buf)
        var seq = [ProtocolConfigAttr]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeProtocolConfigAttr.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeProtocolConfigFeatureFlag: FfiConverterRustBuffer {
    typealias SwiftType = [ProtocolConfigFeatureFlag]

    public static func write(_ value: [ProtocolConfigFeatureFlag], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeProtocolConfigFeatureFlag.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [ProtocolConfigFeatureFlag] {
        let len: Int32 = try readInt(&buf)
        var seq = [ProtocolConfigFeatureFlag]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeProtocolConfigFeatureFlag.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeSignedTransaction: FfiConverterRustBuffer {
    typealias SwiftType = [SignedTransaction]

    public static func write(_ value: [SignedTransaction], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeSignedTransaction.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [SignedTransaction] {
        let len: Int32 = try readInt(&buf)
        var seq = [SignedTransaction]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeSignedTransaction.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeTransactionDataEffects: FfiConverterRustBuffer {
    typealias SwiftType = [TransactionDataEffects]

    public static func write(_ value: [TransactionDataEffects], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeTransactionDataEffects.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [TransactionDataEffects] {
        let len: Int32 = try readInt(&buf)
        var seq = [TransactionDataEffects]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeTransactionDataEffects.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeTypeOrigin: FfiConverterRustBuffer {
    typealias SwiftType = [TypeOrigin]

    public static func write(_ value: [TypeOrigin], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeTypeOrigin.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [TypeOrigin] {
        let len: Int32 = try readInt(&buf)
        var seq = [TypeOrigin]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeTypeOrigin.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeUnchangedSharedObject: FfiConverterRustBuffer {
    typealias SwiftType = [UnchangedSharedObject]

    public static func write(_ value: [UnchangedSharedObject], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeUnchangedSharedObject.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [UnchangedSharedObject] {
        let len: Int32 = try readInt(&buf)
        var seq = [UnchangedSharedObject]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeUnchangedSharedObject.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeValidator: FfiConverterRustBuffer {
    typealias SwiftType = [Validator]

    public static func write(_ value: [Validator], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeValidator.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [Validator] {
        let len: Int32 = try readInt(&buf)
        var seq = [Validator]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeValidator.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeValidatorCommitteeMember: FfiConverterRustBuffer {
    typealias SwiftType = [ValidatorCommitteeMember]

    public static func write(_ value: [ValidatorCommitteeMember], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeValidatorCommitteeMember.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [ValidatorCommitteeMember] {
        let len: Int32 = try readInt(&buf)
        var seq = [ValidatorCommitteeMember]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeValidatorCommitteeMember.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeFeature: FfiConverterRustBuffer {
    typealias SwiftType = [Feature]

    public static func write(_ value: [Feature], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeFeature.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [Feature] {
        let len: Int32 = try readInt(&buf)
        var seq = [Feature]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeFeature.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeMoveAbility: FfiConverterRustBuffer {
    typealias SwiftType = [MoveAbility]

    public static func write(_ value: [MoveAbility], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeMoveAbility.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [MoveAbility] {
        let len: Int32 = try readInt(&buf)
        var seq = [MoveAbility]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeMoveAbility.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceTypeValue: FfiConverterRustBuffer {
    typealias SwiftType = [Value]

    public static func write(_ value: [Value], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterTypeValue.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [Value] {
        let len: Int32 = try readInt(&buf)
        var seq = [Value]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterTypeValue.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterDictionaryStringSequenceString: FfiConverterRustBuffer {
    public static func write(_ value: [String: [String]], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for (key, value) in value {
            FfiConverterString.write(key, into: &buf)
            FfiConverterSequenceString.write(value, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [String: [String]] {
        let len: Int32 = try readInt(&buf)
        var dict = [String: [String]]()
        dict.reserveCapacity(Int(len))
        for _ in 0..<len {
            let key = try FfiConverterString.read(from: &buf)
            let value = try FfiConverterSequenceString.read(from: &buf)
            dict[key] = value
        }
        return dict
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterDictionaryTypeIdentifierData: FfiConverterRustBuffer {
    public static func write(_ value: [Identifier: Data], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for (key, value) in value {
            FfiConverterTypeIdentifier.write(key, into: &buf)
            FfiConverterData.write(value, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [Identifier: Data] {
        let len: Int32 = try readInt(&buf)
        var dict = [Identifier: Data]()
        dict.reserveCapacity(Int(len))
        for _ in 0..<len {
            let key = try FfiConverterTypeIdentifier.read(from: &buf)
            let value = try FfiConverterData.read(from: &buf)
            dict[key] = value
        }
        return dict
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterDictionaryTypeObjectIdTypeUpgradeInfo: FfiConverterRustBuffer {
    public static func write(_ value: [ObjectId: UpgradeInfo], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for (key, value) in value {
            FfiConverterTypeObjectId.write(key, into: &buf)
            FfiConverterTypeUpgradeInfo.write(value, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [ObjectId: UpgradeInfo] {
        let len: Int32 = try readInt(&buf)
        var dict = [ObjectId: UpgradeInfo]()
        dict.reserveCapacity(Int(len))
        for _ in 0..<len {
            let key = try FfiConverterTypeObjectId.read(from: &buf)
            let value = try FfiConverterTypeUpgradeInfo.read(from: &buf)
            dict[key] = value
        }
        return dict
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterDictionaryTypeJwkIdTypeJwk: FfiConverterRustBuffer {
    public static func write(_ value: [JwkId: Jwk], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for (key, value) in value {
            FfiConverterTypeJwkId.write(key, into: &buf)
            FfiConverterTypeJwk.write(value, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [JwkId: Jwk] {
        let len: Int32 = try readInt(&buf)
        var dict = [JwkId: Jwk]()
        dict.reserveCapacity(Int(len))
        for _ in 0..<len {
            let key = try FfiConverterTypeJwkId.read(from: &buf)
            let value = try FfiConverterTypeJwk.read(from: &buf)
            dict[key] = value
        }
        return dict
    }
}


/**
 * Typealias from the type name used in the UDL file to the builtin type.  This
 * is needed because the UDL type name is used in function/method signatures.
 */
public typealias Base64 = String

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeBase64: FfiConverter {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Base64 {
        return try FfiConverterString.read(from: &buf)
    }

    public static func write(_ value: Base64, into buf: inout [UInt8]) {
        return FfiConverterString.write(value, into: &buf)
    }

    public static func lift(_ value: RustBuffer) throws -> Base64 {
        return try FfiConverterString.lift(value)
    }

    public static func lower(_ value: Base64) -> RustBuffer {
        return FfiConverterString.lower(value)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeBase64_lift(_ value: RustBuffer) throws -> Base64 {
    return try FfiConverterTypeBase64.lift(value)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeBase64_lower(_ value: Base64) -> RustBuffer {
    return FfiConverterTypeBase64.lower(value)
}



/**
 * Typealias from the type name used in the UDL file to the builtin type.  This
 * is needed because the UDL type name is used in function/method signatures.
 */
public typealias BigInt = String

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeBigInt: FfiConverter {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> BigInt {
        return try FfiConverterString.read(from: &buf)
    }

    public static func write(_ value: BigInt, into buf: inout [UInt8]) {
        return FfiConverterString.write(value, into: &buf)
    }

    public static func lift(_ value: RustBuffer) throws -> BigInt {
        return try FfiConverterString.lift(value)
    }

    public static func lower(_ value: BigInt) -> RustBuffer {
        return FfiConverterString.lower(value)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeBigInt_lift(_ value: RustBuffer) throws -> BigInt {
    return try FfiConverterTypeBigInt.lift(value)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeBigInt_lower(_ value: BigInt) -> RustBuffer {
    return FfiConverterTypeBigInt.lower(value)
}



/**
 * Typealias from the type name used in the UDL file to the builtin type.  This
 * is needed because the UDL type name is used in function/method signatures.
 */
public typealias Value = String

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeValue: FfiConverter {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Value {
        return try FfiConverterString.read(from: &buf)
    }

    public static func write(_ value: Value, into buf: inout [UInt8]) {
        return FfiConverterString.write(value, into: &buf)
    }

    public static func lift(_ value: RustBuffer) throws -> Value {
        return try FfiConverterString.lift(value)
    }

    public static func lower(_ value: Value) -> RustBuffer {
        return FfiConverterString.lower(value)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeValue_lift(_ value: RustBuffer) throws -> Value {
    return try FfiConverterTypeValue.lift(value)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeValue_lower(_ value: Value) -> RustBuffer {
    return FfiConverterTypeValue.lower(value)
}

private let UNIFFI_RUST_FUTURE_POLL_READY: Int8 = 0
private let UNIFFI_RUST_FUTURE_POLL_MAYBE_READY: Int8 = 1

fileprivate let uniffiContinuationHandleMap = UniffiHandleMap<UnsafeContinuation<Int8, Never>>()

fileprivate func uniffiRustCallAsync<F, T>(
    rustFutureFunc: () -> UInt64,
    pollFunc: (UInt64, @escaping UniffiRustFutureContinuationCallback, UInt64) -> (),
    completeFunc: (UInt64, UnsafeMutablePointer<RustCallStatus>) -> F,
    freeFunc: (UInt64) -> (),
    liftFunc: (F) throws -> T,
    errorHandler: ((RustBuffer) throws -> Swift.Error)?
) async throws -> T {
    // Make sure to call the ensure init function since future creation doesn't have a
    // RustCallStatus param, so doesn't use makeRustCall()
    uniffiEnsureIotaSdkFfiInitialized()
    let rustFuture = rustFutureFunc()
    defer {
        freeFunc(rustFuture)
    }
    var pollResult: Int8;
    repeat {
        pollResult = await withUnsafeContinuation {
            pollFunc(
                rustFuture,
                uniffiFutureContinuationCallback,
                uniffiContinuationHandleMap.insert(obj: $0)
            )
        }
    } while pollResult != UNIFFI_RUST_FUTURE_POLL_READY

    return try liftFunc(makeRustCall(
        { completeFunc(rustFuture, $0) },
        errorHandler: errorHandler
    ))
}

// Callback handlers for an async calls.  These are invoked by Rust when the future is ready.  They
// lift the return value or error and resume the suspended function.
fileprivate func uniffiFutureContinuationCallback(handle: UInt64, pollResult: Int8) {
    if let continuation = try? uniffiContinuationHandleMap.remove(handle: handle) {
        continuation.resume(returning: pollResult)
    } else {
        print("uniffiFutureContinuationCallback invalid handle")
    }
}
private func uniffiTraitInterfaceCallAsync<T>(
    makeCall: @escaping () async throws -> T,
    handleSuccess: @escaping (T) -> (),
    handleError: @escaping (Int8, RustBuffer) -> ()
) -> UniffiForeignFuture {
    let task = Task {
        // Note: it's important we call either `handleSuccess` or `handleError` exactly once.  Each
        // call consumes an Arc reference, which means there should be no possibility of a double
        // call.  The following code is structured so that will will never call both `handleSuccess`
        // and `handleError`, even in the face of weird errors.
        //
        // On platforms that need extra machinery to make C-ABI calls, like JNA or ctypes, it's
        // possible that we fail to make either call.  However, it doesn't seem like this is
        // possible on Swift since swift can just make the C call directly.
        var callResult: T
        do {
            callResult = try await makeCall()
        } catch {
            handleError(CALL_UNEXPECTED_ERROR, FfiConverterString.lower(String(describing: error)))
            return
        }
        handleSuccess(callResult)
    }
    let handle = UNIFFI_FOREIGN_FUTURE_HANDLE_MAP.insert(obj: task)
    return UniffiForeignFuture(handle: handle, free: uniffiForeignFutureFree)

}

private func uniffiTraitInterfaceCallAsyncWithError<T, E>(
    makeCall: @escaping () async throws -> T,
    handleSuccess: @escaping (T) -> (),
    handleError: @escaping (Int8, RustBuffer) -> (),
    lowerError: @escaping (E) -> RustBuffer
) -> UniffiForeignFuture {
    let task = Task {
        // See the note in uniffiTraitInterfaceCallAsync for details on `handleSuccess` and
        // `handleError`.
        var callResult: T
        do {
            callResult = try await makeCall()
        } catch let error as E {
            handleError(CALL_ERROR, lowerError(error))
            return
        } catch {
            handleError(CALL_UNEXPECTED_ERROR, FfiConverterString.lower(String(describing: error)))
            return
        }
        handleSuccess(callResult)
    }
    let handle = UNIFFI_FOREIGN_FUTURE_HANDLE_MAP.insert(obj: task)
    return UniffiForeignFuture(handle: handle, free: uniffiForeignFutureFree)
}

// Borrow the callback handle map implementation to store foreign future handles
// TODO: consolidate the handle-map code (https://github.com/mozilla/uniffi-rs/pull/1823)
fileprivate let UNIFFI_FOREIGN_FUTURE_HANDLE_MAP = UniffiHandleMap<UniffiForeignFutureTask>()

// Protocol for tasks that handle foreign futures.
//
// Defining a protocol allows all tasks to be stored in the same handle map.  This can't be done
// with the task object itself, since has generic parameters.
fileprivate protocol UniffiForeignFutureTask {
    func cancel()
}

extension Task: UniffiForeignFutureTask {}

private func uniffiForeignFutureFree(handle: UInt64) {
    do {
        let task = try UNIFFI_FOREIGN_FUTURE_HANDLE_MAP.remove(handle: handle)
        // Set the cancellation flag on the task.  If it's still running, the code can check the
        // cancellation flag or call `Task.checkCancellation()`.  If the task has completed, this is
        // a no-op.
        task.cancel()
    } catch {
        print("uniffiForeignFutureFree: handle missing from handlemap")
    }
}

// For testing
public func uniffiForeignFutureHandleCountIotaSdkFfi() -> Int {
    UNIFFI_FOREIGN_FUTURE_HANDLE_MAP.count
}
/**
 * Create this type from BCS encoded bytes.
 */
public func activeJwkFromBcs(bcs: Data)throws  -> ActiveJwk  {
    return try  FfiConverterTypeActiveJwk_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_active_jwk_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func activeJwkFromJson(json: String)throws  -> ActiveJwk  {
    return try  FfiConverterTypeActiveJwk_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_active_jwk_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func activeJwkToBcs(data: ActiveJwk)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_active_jwk_to_bcs(
        FfiConverterTypeActiveJwk_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func activeJwkToJson(data: ActiveJwk)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_active_jwk_to_json(
        FfiConverterTypeActiveJwk_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func addressFromBcs(bcs: Data)throws  -> Address  {
    return try  FfiConverterTypeAddress_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_address_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func addressFromJson(json: String)throws  -> Address  {
    return try  FfiConverterTypeAddress_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_address_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func addressToBcs(data: Address)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_address_to_bcs(
        FfiConverterTypeAddress_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func addressToJson(data: Address)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_address_to_json(
        FfiConverterTypeAddress_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func argumentFromBcs(bcs: Data)throws  -> Argument  {
    return try  FfiConverterTypeArgument_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_argument_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func argumentFromJson(json: String)throws  -> Argument  {
    return try  FfiConverterTypeArgument_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_argument_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func argumentToBcs(data: Argument)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_argument_to_bcs(
        FfiConverterTypeArgument_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func argumentToJson(data: Argument)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_argument_to_json(
        FfiConverterTypeArgument_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func authenticatorStateExpireFromBcs(bcs: Data)throws  -> AuthenticatorStateExpire  {
    return try  FfiConverterTypeAuthenticatorStateExpire_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_authenticator_state_expire_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func authenticatorStateExpireFromJson(json: String)throws  -> AuthenticatorStateExpire  {
    return try  FfiConverterTypeAuthenticatorStateExpire_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_authenticator_state_expire_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func authenticatorStateExpireToBcs(data: AuthenticatorStateExpire)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_authenticator_state_expire_to_bcs(
        FfiConverterTypeAuthenticatorStateExpire_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func authenticatorStateExpireToJson(data: AuthenticatorStateExpire)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_authenticator_state_expire_to_json(
        FfiConverterTypeAuthenticatorStateExpire_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func authenticatorStateUpdateV1FromBcs(bcs: Data)throws  -> AuthenticatorStateUpdateV1  {
    return try  FfiConverterTypeAuthenticatorStateUpdateV1_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_authenticator_state_update_v1_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func authenticatorStateUpdateV1FromJson(json: String)throws  -> AuthenticatorStateUpdateV1  {
    return try  FfiConverterTypeAuthenticatorStateUpdateV1_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_authenticator_state_update_v1_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func authenticatorStateUpdateV1ToBcs(data: AuthenticatorStateUpdateV1)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_authenticator_state_update_v1_to_bcs(
        FfiConverterTypeAuthenticatorStateUpdateV1_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func authenticatorStateUpdateV1ToJson(data: AuthenticatorStateUpdateV1)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_authenticator_state_update_v1_to_json(
        FfiConverterTypeAuthenticatorStateUpdateV1_lower(data),$0
    )
})
}
public func base64Decode(input: String)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_base64_decode(
        FfiConverterString.lower(input),$0
    )
})
}
public func base64Encode(input: Data) -> String  {
    return try!  FfiConverterString.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_func_base64_encode(
        FfiConverterData.lower(input),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func bls12381PublicKeyFromBcs(bcs: Data)throws  -> Bls12381PublicKey  {
    return try  FfiConverterTypeBls12381PublicKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_bls12381_public_key_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func bls12381PublicKeyFromJson(json: String)throws  -> Bls12381PublicKey  {
    return try  FfiConverterTypeBls12381PublicKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_bls12381_public_key_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func bls12381PublicKeyToBcs(data: Bls12381PublicKey)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_bls12381_public_key_to_bcs(
        FfiConverterTypeBls12381PublicKey_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func bls12381PublicKeyToJson(data: Bls12381PublicKey)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_bls12381_public_key_to_json(
        FfiConverterTypeBls12381PublicKey_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func bls12381SignatureFromBcs(bcs: Data)throws  -> Bls12381Signature  {
    return try  FfiConverterTypeBls12381Signature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_bls12381_signature_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func bls12381SignatureFromJson(json: String)throws  -> Bls12381Signature  {
    return try  FfiConverterTypeBls12381Signature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_bls12381_signature_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func bls12381SignatureToBcs(data: Bls12381Signature)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_bls12381_signature_to_bcs(
        FfiConverterTypeBls12381Signature_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func bls12381SignatureToJson(data: Bls12381Signature)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_bls12381_signature_to_json(
        FfiConverterTypeBls12381Signature_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func bn254FieldElementFromBcs(bcs: Data)throws  -> Bn254FieldElement  {
    return try  FfiConverterTypeBn254FieldElement_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_bn254_field_element_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func bn254FieldElementFromJson(json: String)throws  -> Bn254FieldElement  {
    return try  FfiConverterTypeBn254FieldElement_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_bn254_field_element_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func bn254FieldElementToBcs(data: Bn254FieldElement)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_bn254_field_element_to_bcs(
        FfiConverterTypeBn254FieldElement_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func bn254FieldElementToJson(data: Bn254FieldElement)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_bn254_field_element_to_json(
        FfiConverterTypeBn254FieldElement_lower(data),$0
    )
})
}
/**
 * Create a bool from BCS encoded bytes.
 */
public func boolFromBcs(input: Data)throws  -> Bool  {
    return try  FfiConverterBool.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_bool_from_bcs(
        FfiConverterData.lower(input),$0
    )
})
}
/**
 * Create a bool from JSON encoded string.
 */
public func boolFromJson(input: String)throws  -> Bool  {
    return try  FfiConverterBool.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_bool_from_json(
        FfiConverterString.lower(input),$0
    )
})
}
/**
 * Convert this bool to BCS encoded bytes.
 */
public func boolToBcs(input: Bool)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_bool_to_bcs(
        FfiConverterBool.lower(input),$0
    )
})
}
/**
 * Convert this bool to JSON encoded string.
 */
public func boolToJson(input: Bool)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_bool_to_json(
        FfiConverterBool.lower(input),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func cancelledTransactionFromBcs(bcs: Data)throws  -> CancelledTransaction  {
    return try  FfiConverterTypeCancelledTransaction_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_cancelled_transaction_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func cancelledTransactionFromJson(json: String)throws  -> CancelledTransaction  {
    return try  FfiConverterTypeCancelledTransaction_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_cancelled_transaction_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func cancelledTransactionToBcs(data: CancelledTransaction)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_cancelled_transaction_to_bcs(
        FfiConverterTypeCancelledTransaction_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func cancelledTransactionToJson(data: CancelledTransaction)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_cancelled_transaction_to_json(
        FfiConverterTypeCancelledTransaction_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func changeEpochFromBcs(bcs: Data)throws  -> ChangeEpoch  {
    return try  FfiConverterTypeChangeEpoch_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_change_epoch_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func changeEpochFromJson(json: String)throws  -> ChangeEpoch  {
    return try  FfiConverterTypeChangeEpoch_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_change_epoch_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func changeEpochToBcs(data: ChangeEpoch)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_change_epoch_to_bcs(
        FfiConverterTypeChangeEpoch_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func changeEpochToJson(data: ChangeEpoch)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_change_epoch_to_json(
        FfiConverterTypeChangeEpoch_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func changeEpochV2FromBcs(bcs: Data)throws  -> ChangeEpochV2  {
    return try  FfiConverterTypeChangeEpochV2_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_change_epoch_v2_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func changeEpochV2FromJson(json: String)throws  -> ChangeEpochV2  {
    return try  FfiConverterTypeChangeEpochV2_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_change_epoch_v2_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func changeEpochV2ToBcs(data: ChangeEpochV2)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_change_epoch_v2_to_bcs(
        FfiConverterTypeChangeEpochV2_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func changeEpochV2ToJson(data: ChangeEpochV2)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_change_epoch_v2_to_json(
        FfiConverterTypeChangeEpochV2_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func changedObjectFromBcs(bcs: Data)throws  -> ChangedObject  {
    return try  FfiConverterTypeChangedObject_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_changed_object_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func changedObjectFromJson(json: String)throws  -> ChangedObject  {
    return try  FfiConverterTypeChangedObject_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_changed_object_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func changedObjectToBcs(data: ChangedObject)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_changed_object_to_bcs(
        FfiConverterTypeChangedObject_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func changedObjectToJson(data: ChangedObject)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_changed_object_to_json(
        FfiConverterTypeChangedObject_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func checkpointCommitmentFromBcs(bcs: Data)throws  -> CheckpointCommitment  {
    return try  FfiConverterTypeCheckpointCommitment_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_checkpoint_commitment_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func checkpointCommitmentFromJson(json: String)throws  -> CheckpointCommitment  {
    return try  FfiConverterTypeCheckpointCommitment_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_checkpoint_commitment_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func checkpointCommitmentToBcs(data: CheckpointCommitment)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_checkpoint_commitment_to_bcs(
        FfiConverterTypeCheckpointCommitment_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func checkpointCommitmentToJson(data: CheckpointCommitment)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_checkpoint_commitment_to_json(
        FfiConverterTypeCheckpointCommitment_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func checkpointContentsFromBcs(bcs: Data)throws  -> CheckpointContents  {
    return try  FfiConverterTypeCheckpointContents_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_checkpoint_contents_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func checkpointContentsFromJson(json: String)throws  -> CheckpointContents  {
    return try  FfiConverterTypeCheckpointContents_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_checkpoint_contents_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func checkpointContentsToBcs(data: CheckpointContents)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_checkpoint_contents_to_bcs(
        FfiConverterTypeCheckpointContents_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func checkpointContentsToJson(data: CheckpointContents)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_checkpoint_contents_to_json(
        FfiConverterTypeCheckpointContents_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func checkpointSummaryFromBcs(bcs: Data)throws  -> CheckpointSummary  {
    return try  FfiConverterTypeCheckpointSummary_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_checkpoint_summary_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func checkpointSummaryFromJson(json: String)throws  -> CheckpointSummary  {
    return try  FfiConverterTypeCheckpointSummary_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_checkpoint_summary_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func checkpointSummaryToBcs(data: CheckpointSummary)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_checkpoint_summary_to_bcs(
        FfiConverterTypeCheckpointSummary_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func checkpointSummaryToJson(data: CheckpointSummary)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_checkpoint_summary_to_json(
        FfiConverterTypeCheckpointSummary_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func checkpointTransactionInfoFromBcs(bcs: Data)throws  -> CheckpointTransactionInfo  {
    return try  FfiConverterTypeCheckpointTransactionInfo_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_checkpoint_transaction_info_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func checkpointTransactionInfoFromJson(json: String)throws  -> CheckpointTransactionInfo  {
    return try  FfiConverterTypeCheckpointTransactionInfo_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_checkpoint_transaction_info_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func checkpointTransactionInfoToBcs(data: CheckpointTransactionInfo)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_checkpoint_transaction_info_to_bcs(
        FfiConverterTypeCheckpointTransactionInfo_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func checkpointTransactionInfoToJson(data: CheckpointTransactionInfo)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_checkpoint_transaction_info_to_json(
        FfiConverterTypeCheckpointTransactionInfo_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func circomG1FromBcs(bcs: Data)throws  -> CircomG1  {
    return try  FfiConverterTypeCircomG1_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_circom_g1_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func circomG1FromJson(json: String)throws  -> CircomG1  {
    return try  FfiConverterTypeCircomG1_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_circom_g1_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func circomG1ToBcs(data: CircomG1)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_circom_g1_to_bcs(
        FfiConverterTypeCircomG1_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func circomG1ToJson(data: CircomG1)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_circom_g1_to_json(
        FfiConverterTypeCircomG1_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func circomG2FromBcs(bcs: Data)throws  -> CircomG2  {
    return try  FfiConverterTypeCircomG2_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_circom_g2_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func circomG2FromJson(json: String)throws  -> CircomG2  {
    return try  FfiConverterTypeCircomG2_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_circom_g2_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func circomG2ToBcs(data: CircomG2)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_circom_g2_to_bcs(
        FfiConverterTypeCircomG2_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func circomG2ToJson(data: CircomG2)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_circom_g2_to_json(
        FfiConverterTypeCircomG2_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func commandArgumentErrorFromBcs(bcs: Data)throws  -> CommandArgumentError  {
    return try  FfiConverterTypeCommandArgumentError_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_command_argument_error_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func commandArgumentErrorFromJson(json: String)throws  -> CommandArgumentError  {
    return try  FfiConverterTypeCommandArgumentError_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_command_argument_error_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func commandArgumentErrorToBcs(data: CommandArgumentError)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_command_argument_error_to_bcs(
        FfiConverterTypeCommandArgumentError_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func commandArgumentErrorToJson(data: CommandArgumentError)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_command_argument_error_to_json(
        FfiConverterTypeCommandArgumentError_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func commandFromBcs(bcs: Data)throws  -> Command  {
    return try  FfiConverterTypeCommand_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_command_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func commandFromJson(json: String)throws  -> Command  {
    return try  FfiConverterTypeCommand_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_command_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func commandToBcs(data: Command)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_command_to_bcs(
        FfiConverterTypeCommand_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func commandToJson(data: Command)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_command_to_json(
        FfiConverterTypeCommand_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func consensusCommitPrologueV1FromBcs(bcs: Data)throws  -> ConsensusCommitPrologueV1  {
    return try  FfiConverterTypeConsensusCommitPrologueV1_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_consensus_commit_prologue_v1_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func consensusCommitPrologueV1FromJson(json: String)throws  -> ConsensusCommitPrologueV1  {
    return try  FfiConverterTypeConsensusCommitPrologueV1_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_consensus_commit_prologue_v1_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func consensusCommitPrologueV1ToBcs(data: ConsensusCommitPrologueV1)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_consensus_commit_prologue_v1_to_bcs(
        FfiConverterTypeConsensusCommitPrologueV1_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func consensusCommitPrologueV1ToJson(data: ConsensusCommitPrologueV1)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_consensus_commit_prologue_v1_to_json(
        FfiConverterTypeConsensusCommitPrologueV1_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func consensusDeterminedVersionAssignmentsFromBcs(bcs: Data)throws  -> ConsensusDeterminedVersionAssignments  {
    return try  FfiConverterTypeConsensusDeterminedVersionAssignments_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_consensus_determined_version_assignments_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func consensusDeterminedVersionAssignmentsFromJson(json: String)throws  -> ConsensusDeterminedVersionAssignments  {
    return try  FfiConverterTypeConsensusDeterminedVersionAssignments_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_consensus_determined_version_assignments_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func consensusDeterminedVersionAssignmentsToBcs(data: ConsensusDeterminedVersionAssignments)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_consensus_determined_version_assignments_to_bcs(
        FfiConverterTypeConsensusDeterminedVersionAssignments_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func consensusDeterminedVersionAssignmentsToJson(data: ConsensusDeterminedVersionAssignments)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_consensus_determined_version_assignments_to_json(
        FfiConverterTypeConsensusDeterminedVersionAssignments_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func digestFromBcs(bcs: Data)throws  -> Digest  {
    return try  FfiConverterTypeDigest_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_digest_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func digestFromJson(json: String)throws  -> Digest  {
    return try  FfiConverterTypeDigest_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_digest_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func digestToBcs(data: Digest)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_digest_to_bcs(
        FfiConverterTypeDigest_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func digestToJson(data: Digest)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_digest_to_json(
        FfiConverterTypeDigest_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func ed25519PublicKeyFromBcs(bcs: Data)throws  -> Ed25519PublicKey  {
    return try  FfiConverterTypeEd25519PublicKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_ed25519_public_key_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func ed25519PublicKeyFromJson(json: String)throws  -> Ed25519PublicKey  {
    return try  FfiConverterTypeEd25519PublicKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_ed25519_public_key_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func ed25519PublicKeyToBcs(data: Ed25519PublicKey)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_ed25519_public_key_to_bcs(
        FfiConverterTypeEd25519PublicKey_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func ed25519PublicKeyToJson(data: Ed25519PublicKey)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_ed25519_public_key_to_json(
        FfiConverterTypeEd25519PublicKey_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func ed25519SignatureFromBcs(bcs: Data)throws  -> Ed25519Signature  {
    return try  FfiConverterTypeEd25519Signature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_ed25519_signature_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func ed25519SignatureFromJson(json: String)throws  -> Ed25519Signature  {
    return try  FfiConverterTypeEd25519Signature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_ed25519_signature_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func ed25519SignatureToBcs(data: Ed25519Signature)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_ed25519_signature_to_bcs(
        FfiConverterTypeEd25519Signature_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func ed25519SignatureToJson(data: Ed25519Signature)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_ed25519_signature_to_json(
        FfiConverterTypeEd25519Signature_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func endOfEpochDataFromBcs(bcs: Data)throws  -> EndOfEpochData  {
    return try  FfiConverterTypeEndOfEpochData_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_end_of_epoch_data_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func endOfEpochDataFromJson(json: String)throws  -> EndOfEpochData  {
    return try  FfiConverterTypeEndOfEpochData_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_end_of_epoch_data_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func endOfEpochDataToBcs(data: EndOfEpochData)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_end_of_epoch_data_to_bcs(
        FfiConverterTypeEndOfEpochData_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func endOfEpochDataToJson(data: EndOfEpochData)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_end_of_epoch_data_to_json(
        FfiConverterTypeEndOfEpochData_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func eventFromBcs(bcs: Data)throws  -> Event  {
    return try  FfiConverterTypeEvent_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_event_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func eventFromJson(json: String)throws  -> Event  {
    return try  FfiConverterTypeEvent_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_event_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func eventToBcs(data: Event)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_event_to_bcs(
        FfiConverterTypeEvent_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func eventToJson(data: Event)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_event_to_json(
        FfiConverterTypeEvent_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func executionErrorFromBcs(bcs: Data)throws  -> ExecutionError  {
    return try  FfiConverterTypeExecutionError_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_execution_error_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func executionErrorFromJson(json: String)throws  -> ExecutionError  {
    return try  FfiConverterTypeExecutionError_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_execution_error_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func executionErrorToBcs(data: ExecutionError)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_execution_error_to_bcs(
        FfiConverterTypeExecutionError_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func executionErrorToJson(data: ExecutionError)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_execution_error_to_json(
        FfiConverterTypeExecutionError_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func executionStatusFromBcs(bcs: Data)throws  -> ExecutionStatus  {
    return try  FfiConverterTypeExecutionStatus_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_execution_status_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func executionStatusFromJson(json: String)throws  -> ExecutionStatus  {
    return try  FfiConverterTypeExecutionStatus_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_execution_status_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func executionStatusToBcs(data: ExecutionStatus)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_execution_status_to_bcs(
        FfiConverterTypeExecutionStatus_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func executionStatusToJson(data: ExecutionStatus)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_execution_status_to_json(
        FfiConverterTypeExecutionStatus_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func executionTimeObservationFromBcs(bcs: Data)throws  -> ExecutionTimeObservation  {
    return try  FfiConverterTypeExecutionTimeObservation_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_execution_time_observation_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func executionTimeObservationFromJson(json: String)throws  -> ExecutionTimeObservation  {
    return try  FfiConverterTypeExecutionTimeObservation_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_execution_time_observation_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func executionTimeObservationKeyFromBcs(bcs: Data)throws  -> ExecutionTimeObservationKey  {
    return try  FfiConverterTypeExecutionTimeObservationKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_execution_time_observation_key_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func executionTimeObservationKeyFromJson(json: String)throws  -> ExecutionTimeObservationKey  {
    return try  FfiConverterTypeExecutionTimeObservationKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_execution_time_observation_key_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func executionTimeObservationKeyToBcs(data: ExecutionTimeObservationKey)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_execution_time_observation_key_to_bcs(
        FfiConverterTypeExecutionTimeObservationKey_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func executionTimeObservationKeyToJson(data: ExecutionTimeObservationKey)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_execution_time_observation_key_to_json(
        FfiConverterTypeExecutionTimeObservationKey_lower(data),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func executionTimeObservationToBcs(data: ExecutionTimeObservation)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_execution_time_observation_to_bcs(
        FfiConverterTypeExecutionTimeObservation_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func executionTimeObservationToJson(data: ExecutionTimeObservation)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_execution_time_observation_to_json(
        FfiConverterTypeExecutionTimeObservation_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func executionTimeObservationsFromBcs(bcs: Data)throws  -> ExecutionTimeObservations  {
    return try  FfiConverterTypeExecutionTimeObservations_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_execution_time_observations_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func executionTimeObservationsFromJson(json: String)throws  -> ExecutionTimeObservations  {
    return try  FfiConverterTypeExecutionTimeObservations_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_execution_time_observations_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func executionTimeObservationsToBcs(data: ExecutionTimeObservations)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_execution_time_observations_to_bcs(
        FfiConverterTypeExecutionTimeObservations_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func executionTimeObservationsToJson(data: ExecutionTimeObservations)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_execution_time_observations_to_json(
        FfiConverterTypeExecutionTimeObservations_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func gasCostSummaryFromBcs(bcs: Data)throws  -> GasCostSummary  {
    return try  FfiConverterTypeGasCostSummary_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_gas_cost_summary_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func gasCostSummaryFromJson(json: String)throws  -> GasCostSummary  {
    return try  FfiConverterTypeGasCostSummary_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_gas_cost_summary_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func gasCostSummaryToBcs(data: GasCostSummary)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_gas_cost_summary_to_bcs(
        FfiConverterTypeGasCostSummary_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func gasCostSummaryToJson(data: GasCostSummary)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_gas_cost_summary_to_json(
        FfiConverterTypeGasCostSummary_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func gasPaymentFromBcs(bcs: Data)throws  -> GasPayment  {
    return try  FfiConverterTypeGasPayment_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_gas_payment_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func gasPaymentFromJson(json: String)throws  -> GasPayment  {
    return try  FfiConverterTypeGasPayment_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_gas_payment_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func gasPaymentToBcs(data: GasPayment)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_gas_payment_to_bcs(
        FfiConverterTypeGasPayment_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func gasPaymentToJson(data: GasPayment)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_gas_payment_to_json(
        FfiConverterTypeGasPayment_lower(data),$0
    )
})
}
/**
 * Generate a new BIP-39 mnemonic in English.
 * Supported word counts are 12 and 24 (default).
 */
public func generateMnemonic(wordCount: MnemonicLength?) -> String  {
    return try!  FfiConverterString.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_func_generate_mnemonic(
        FfiConverterOptionTypeMnemonicLength.lower(wordCount),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func genesisObjectFromBcs(bcs: Data)throws  -> GenesisObject  {
    return try  FfiConverterTypeGenesisObject_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_genesis_object_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func genesisObjectFromJson(json: String)throws  -> GenesisObject  {
    return try  FfiConverterTypeGenesisObject_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_genesis_object_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func genesisObjectToBcs(data: GenesisObject)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_genesis_object_to_bcs(
        FfiConverterTypeGenesisObject_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func genesisObjectToJson(data: GenesisObject)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_genesis_object_to_json(
        FfiConverterTypeGenesisObject_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func genesisTransactionFromBcs(bcs: Data)throws  -> GenesisTransaction  {
    return try  FfiConverterTypeGenesisTransaction_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_genesis_transaction_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func genesisTransactionFromJson(json: String)throws  -> GenesisTransaction  {
    return try  FfiConverterTypeGenesisTransaction_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_genesis_transaction_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func genesisTransactionToBcs(data: GenesisTransaction)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_genesis_transaction_to_bcs(
        FfiConverterTypeGenesisTransaction_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func genesisTransactionToJson(data: GenesisTransaction)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_genesis_transaction_to_json(
        FfiConverterTypeGenesisTransaction_lower(data),$0
    )
})
}
public func hexDecode(input: String)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_hex_decode(
        FfiConverterString.lower(input),$0
    )
})
}
public func hexEncode(input: Data) -> String  {
    return try!  FfiConverterString.lift(try! rustCall() {
    uniffi_iota_sdk_ffi_fn_func_hex_encode(
        FfiConverterData.lower(input),$0
    )
})
}
/**
 * Create a i16 from BCS encoded bytes.
 */
public func i16FromBcs(input: Data)throws  -> Int16  {
    return try  FfiConverterInt16.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_i16_from_bcs(
        FfiConverterData.lower(input),$0
    )
})
}
/**
 * Create a i16 from JSON encoded string.
 */
public func i16FromJson(input: String)throws  -> Int16  {
    return try  FfiConverterInt16.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_i16_from_json(
        FfiConverterString.lower(input),$0
    )
})
}
/**
 * Convert this i16 to BCS encoded bytes.
 */
public func i16ToBcs(input: Int16)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_i16_to_bcs(
        FfiConverterInt16.lower(input),$0
    )
})
}
/**
 * Convert this i16 to JSON encoded string.
 */
public func i16ToJson(input: Int16)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_i16_to_json(
        FfiConverterInt16.lower(input),$0
    )
})
}
/**
 * Create a i32 from BCS encoded bytes.
 */
public func i32FromBcs(input: Data)throws  -> Int32  {
    return try  FfiConverterInt32.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_i32_from_bcs(
        FfiConverterData.lower(input),$0
    )
})
}
/**
 * Create a i32 from JSON encoded string.
 */
public func i32FromJson(input: String)throws  -> Int32  {
    return try  FfiConverterInt32.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_i32_from_json(
        FfiConverterString.lower(input),$0
    )
})
}
/**
 * Convert this i32 to BCS encoded bytes.
 */
public func i32ToBcs(input: Int32)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_i32_to_bcs(
        FfiConverterInt32.lower(input),$0
    )
})
}
/**
 * Convert this i32 to JSON encoded string.
 */
public func i32ToJson(input: Int32)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_i32_to_json(
        FfiConverterInt32.lower(input),$0
    )
})
}
/**
 * Create a i64 from BCS encoded bytes.
 */
public func i64FromBcs(input: Data)throws  -> Int64  {
    return try  FfiConverterInt64.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_i64_from_bcs(
        FfiConverterData.lower(input),$0
    )
})
}
/**
 * Create a i64 from JSON encoded string.
 */
public func i64FromJson(input: String)throws  -> Int64  {
    return try  FfiConverterInt64.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_i64_from_json(
        FfiConverterString.lower(input),$0
    )
})
}
/**
 * Convert this i64 to BCS encoded bytes.
 */
public func i64ToBcs(input: Int64)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_i64_to_bcs(
        FfiConverterInt64.lower(input),$0
    )
})
}
/**
 * Convert this i64 to JSON encoded string.
 */
public func i64ToJson(input: Int64)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_i64_to_json(
        FfiConverterInt64.lower(input),$0
    )
})
}
/**
 * Create a i8 from BCS encoded bytes.
 */
public func i8FromBcs(input: Data)throws  -> Int8  {
    return try  FfiConverterInt8.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_i8_from_bcs(
        FfiConverterData.lower(input),$0
    )
})
}
/**
 * Create a i8 from JSON encoded string.
 */
public func i8FromJson(input: String)throws  -> Int8  {
    return try  FfiConverterInt8.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_i8_from_json(
        FfiConverterString.lower(input),$0
    )
})
}
/**
 * Convert this i8 to BCS encoded bytes.
 */
public func i8ToBcs(input: Int8)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_i8_to_bcs(
        FfiConverterInt8.lower(input),$0
    )
})
}
/**
 * Convert this i8 to JSON encoded string.
 */
public func i8ToJson(input: Int8)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_i8_to_json(
        FfiConverterInt8.lower(input),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func idOperationFromBcs(bcs: Data)throws  -> IdOperation  {
    return try  FfiConverterTypeIdOperation_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_id_operation_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func idOperationFromJson(json: String)throws  -> IdOperation  {
    return try  FfiConverterTypeIdOperation_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_id_operation_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func idOperationToBcs(data: IdOperation)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_id_operation_to_bcs(
        FfiConverterTypeIdOperation_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func idOperationToJson(data: IdOperation)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_id_operation_to_json(
        FfiConverterTypeIdOperation_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func identifierFromBcs(bcs: Data)throws  -> Identifier  {
    return try  FfiConverterTypeIdentifier_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_identifier_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func identifierFromJson(json: String)throws  -> Identifier  {
    return try  FfiConverterTypeIdentifier_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_identifier_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func identifierToBcs(data: Identifier)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_identifier_to_bcs(
        FfiConverterTypeIdentifier_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func identifierToJson(data: Identifier)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_identifier_to_json(
        FfiConverterTypeIdentifier_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func inputFromBcs(bcs: Data)throws  -> Input  {
    return try  FfiConverterTypeInput_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_input_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func inputFromJson(json: String)throws  -> Input  {
    return try  FfiConverterTypeInput_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_input_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func inputToBcs(data: Input)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_input_to_bcs(
        FfiConverterTypeInput_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func inputToJson(data: Input)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_input_to_json(
        FfiConverterTypeInput_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func jwkFromBcs(bcs: Data)throws  -> Jwk  {
    return try  FfiConverterTypeJwk_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_jwk_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func jwkFromJson(json: String)throws  -> Jwk  {
    return try  FfiConverterTypeJwk_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_jwk_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func jwkIdFromBcs(bcs: Data)throws  -> JwkId  {
    return try  FfiConverterTypeJwkId_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_jwk_id_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func jwkIdFromJson(json: String)throws  -> JwkId  {
    return try  FfiConverterTypeJwkId_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_jwk_id_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func jwkIdToBcs(data: JwkId)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_jwk_id_to_bcs(
        FfiConverterTypeJwkId_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func jwkIdToJson(data: JwkId)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_jwk_id_to_json(
        FfiConverterTypeJwkId_lower(data),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func jwkToBcs(data: Jwk)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_jwk_to_bcs(
        FfiConverterTypeJwk_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func jwkToJson(data: Jwk)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_jwk_to_json(
        FfiConverterTypeJwk_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func makeMoveVectorFromBcs(bcs: Data)throws  -> MakeMoveVector  {
    return try  FfiConverterTypeMakeMoveVector_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_make_move_vector_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func makeMoveVectorFromJson(json: String)throws  -> MakeMoveVector  {
    return try  FfiConverterTypeMakeMoveVector_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_make_move_vector_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func makeMoveVectorToBcs(data: MakeMoveVector)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_make_move_vector_to_bcs(
        FfiConverterTypeMakeMoveVector_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func makeMoveVectorToJson(data: MakeMoveVector)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_make_move_vector_to_json(
        FfiConverterTypeMakeMoveVector_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func mergeCoinsFromBcs(bcs: Data)throws  -> MergeCoins  {
    return try  FfiConverterTypeMergeCoins_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_merge_coins_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func mergeCoinsFromJson(json: String)throws  -> MergeCoins  {
    return try  FfiConverterTypeMergeCoins_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_merge_coins_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func mergeCoinsToBcs(data: MergeCoins)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_merge_coins_to_bcs(
        FfiConverterTypeMergeCoins_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func mergeCoinsToJson(data: MergeCoins)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_merge_coins_to_json(
        FfiConverterTypeMergeCoins_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func moveCallFromBcs(bcs: Data)throws  -> MoveCall  {
    return try  FfiConverterTypeMoveCall_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_move_call_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func moveCallFromJson(json: String)throws  -> MoveCall  {
    return try  FfiConverterTypeMoveCall_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_move_call_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func moveCallToBcs(data: MoveCall)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_move_call_to_bcs(
        FfiConverterTypeMoveCall_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func moveCallToJson(data: MoveCall)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_move_call_to_json(
        FfiConverterTypeMoveCall_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func moveLocationFromBcs(bcs: Data)throws  -> MoveLocation  {
    return try  FfiConverterTypeMoveLocation_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_move_location_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func moveLocationFromJson(json: String)throws  -> MoveLocation  {
    return try  FfiConverterTypeMoveLocation_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_move_location_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func moveLocationToBcs(data: MoveLocation)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_move_location_to_bcs(
        FfiConverterTypeMoveLocation_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func moveLocationToJson(data: MoveLocation)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_move_location_to_json(
        FfiConverterTypeMoveLocation_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func movePackageFromBcs(bcs: Data)throws  -> MovePackage  {
    return try  FfiConverterTypeMovePackage_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_move_package_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func movePackageFromJson(json: String)throws  -> MovePackage  {
    return try  FfiConverterTypeMovePackage_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_move_package_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func movePackageToBcs(data: MovePackage)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_move_package_to_bcs(
        FfiConverterTypeMovePackage_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func movePackageToJson(data: MovePackage)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_move_package_to_json(
        FfiConverterTypeMovePackage_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func moveStructFromBcs(bcs: Data)throws  -> MoveStruct  {
    return try  FfiConverterTypeMoveStruct_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_move_struct_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func moveStructFromJson(json: String)throws  -> MoveStruct  {
    return try  FfiConverterTypeMoveStruct_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_move_struct_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func moveStructToBcs(data: MoveStruct)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_move_struct_to_bcs(
        FfiConverterTypeMoveStruct_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func moveStructToJson(data: MoveStruct)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_move_struct_to_json(
        FfiConverterTypeMoveStruct_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func multisigAggregatedSignatureFromBcs(bcs: Data)throws  -> MultisigAggregatedSignature  {
    return try  FfiConverterTypeMultisigAggregatedSignature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_multisig_aggregated_signature_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func multisigAggregatedSignatureFromJson(json: String)throws  -> MultisigAggregatedSignature  {
    return try  FfiConverterTypeMultisigAggregatedSignature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_multisig_aggregated_signature_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func multisigAggregatedSignatureToBcs(data: MultisigAggregatedSignature)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_multisig_aggregated_signature_to_bcs(
        FfiConverterTypeMultisigAggregatedSignature_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func multisigAggregatedSignatureToJson(data: MultisigAggregatedSignature)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_multisig_aggregated_signature_to_json(
        FfiConverterTypeMultisigAggregatedSignature_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func multisigCommitteeFromBcs(bcs: Data)throws  -> MultisigCommittee  {
    return try  FfiConverterTypeMultisigCommittee_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_multisig_committee_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func multisigCommitteeFromJson(json: String)throws  -> MultisigCommittee  {
    return try  FfiConverterTypeMultisigCommittee_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_multisig_committee_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func multisigCommitteeToBcs(data: MultisigCommittee)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_multisig_committee_to_bcs(
        FfiConverterTypeMultisigCommittee_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func multisigCommitteeToJson(data: MultisigCommittee)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_multisig_committee_to_json(
        FfiConverterTypeMultisigCommittee_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func multisigMemberFromBcs(bcs: Data)throws  -> MultisigMember  {
    return try  FfiConverterTypeMultisigMember_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_multisig_member_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func multisigMemberFromJson(json: String)throws  -> MultisigMember  {
    return try  FfiConverterTypeMultisigMember_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_multisig_member_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func multisigMemberPublicKeyFromBcs(bcs: Data)throws  -> MultisigMemberPublicKey  {
    return try  FfiConverterTypeMultisigMemberPublicKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_multisig_member_public_key_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func multisigMemberPublicKeyFromJson(json: String)throws  -> MultisigMemberPublicKey  {
    return try  FfiConverterTypeMultisigMemberPublicKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_multisig_member_public_key_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func multisigMemberPublicKeyToBcs(data: MultisigMemberPublicKey)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_multisig_member_public_key_to_bcs(
        FfiConverterTypeMultisigMemberPublicKey_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func multisigMemberPublicKeyToJson(data: MultisigMemberPublicKey)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_multisig_member_public_key_to_json(
        FfiConverterTypeMultisigMemberPublicKey_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func multisigMemberSignatureFromBcs(bcs: Data)throws  -> MultisigMemberSignature  {
    return try  FfiConverterTypeMultisigMemberSignature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_multisig_member_signature_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func multisigMemberSignatureFromJson(json: String)throws  -> MultisigMemberSignature  {
    return try  FfiConverterTypeMultisigMemberSignature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_multisig_member_signature_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func multisigMemberSignatureToBcs(data: MultisigMemberSignature)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_multisig_member_signature_to_bcs(
        FfiConverterTypeMultisigMemberSignature_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func multisigMemberSignatureToJson(data: MultisigMemberSignature)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_multisig_member_signature_to_json(
        FfiConverterTypeMultisigMemberSignature_lower(data),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func multisigMemberToBcs(data: MultisigMember)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_multisig_member_to_bcs(
        FfiConverterTypeMultisigMember_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func multisigMemberToJson(data: MultisigMember)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_multisig_member_to_json(
        FfiConverterTypeMultisigMember_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func objectDataFromBcs(bcs: Data)throws  -> ObjectData  {
    return try  FfiConverterTypeObjectData_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_object_data_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func objectDataFromJson(json: String)throws  -> ObjectData  {
    return try  FfiConverterTypeObjectData_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_object_data_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func objectDataToBcs(data: ObjectData)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_object_data_to_bcs(
        FfiConverterTypeObjectData_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func objectDataToJson(data: ObjectData)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_object_data_to_json(
        FfiConverterTypeObjectData_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func objectFromBcs(bcs: Data)throws  -> Object  {
    return try  FfiConverterTypeObject_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_object_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func objectFromJson(json: String)throws  -> Object  {
    return try  FfiConverterTypeObject_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_object_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func objectIdFromBcs(bcs: Data)throws  -> ObjectId  {
    return try  FfiConverterTypeObjectId_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_object_id_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func objectIdFromJson(json: String)throws  -> ObjectId  {
    return try  FfiConverterTypeObjectId_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_object_id_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func objectIdToBcs(data: ObjectId)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_object_id_to_bcs(
        FfiConverterTypeObjectId_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func objectIdToJson(data: ObjectId)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_object_id_to_json(
        FfiConverterTypeObjectId_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func objectInFromBcs(bcs: Data)throws  -> ObjectIn  {
    return try  FfiConverterTypeObjectIn_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_object_in_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func objectInFromJson(json: String)throws  -> ObjectIn  {
    return try  FfiConverterTypeObjectIn_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_object_in_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func objectInToBcs(data: ObjectIn)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_object_in_to_bcs(
        FfiConverterTypeObjectIn_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func objectInToJson(data: ObjectIn)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_object_in_to_json(
        FfiConverterTypeObjectIn_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func objectOutFromBcs(bcs: Data)throws  -> ObjectOut  {
    return try  FfiConverterTypeObjectOut_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_object_out_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func objectOutFromJson(json: String)throws  -> ObjectOut  {
    return try  FfiConverterTypeObjectOut_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_object_out_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func objectOutToBcs(data: ObjectOut)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_object_out_to_bcs(
        FfiConverterTypeObjectOut_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func objectOutToJson(data: ObjectOut)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_object_out_to_json(
        FfiConverterTypeObjectOut_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func objectReferenceFromBcs(bcs: Data)throws  -> ObjectReference  {
    return try  FfiConverterTypeObjectReference_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_object_reference_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func objectReferenceFromJson(json: String)throws  -> ObjectReference  {
    return try  FfiConverterTypeObjectReference_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_object_reference_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func objectReferenceToBcs(data: ObjectReference)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_object_reference_to_bcs(
        FfiConverterTypeObjectReference_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func objectReferenceToJson(data: ObjectReference)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_object_reference_to_json(
        FfiConverterTypeObjectReference_lower(data),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func objectToBcs(data: Object)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_object_to_bcs(
        FfiConverterTypeObject_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func objectToJson(data: Object)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_object_to_json(
        FfiConverterTypeObject_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func ownerFromBcs(bcs: Data)throws  -> Owner  {
    return try  FfiConverterTypeOwner_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_owner_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func ownerFromJson(json: String)throws  -> Owner  {
    return try  FfiConverterTypeOwner_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_owner_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func ownerToBcs(data: Owner)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_owner_to_bcs(
        FfiConverterTypeOwner_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func ownerToJson(data: Owner)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_owner_to_json(
        FfiConverterTypeOwner_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func packageUpgradeErrorFromBcs(bcs: Data)throws  -> PackageUpgradeError  {
    return try  FfiConverterTypePackageUpgradeError_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_package_upgrade_error_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func packageUpgradeErrorFromJson(json: String)throws  -> PackageUpgradeError  {
    return try  FfiConverterTypePackageUpgradeError_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_package_upgrade_error_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func packageUpgradeErrorToBcs(data: PackageUpgradeError)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_package_upgrade_error_to_bcs(
        FfiConverterTypePackageUpgradeError_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func packageUpgradeErrorToJson(data: PackageUpgradeError)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_package_upgrade_error_to_json(
        FfiConverterTypePackageUpgradeError_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func passkeyAuthenticatorFromBcs(bcs: Data)throws  -> PasskeyAuthenticator  {
    return try  FfiConverterTypePasskeyAuthenticator_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_passkey_authenticator_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func passkeyAuthenticatorFromJson(json: String)throws  -> PasskeyAuthenticator  {
    return try  FfiConverterTypePasskeyAuthenticator_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_passkey_authenticator_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func passkeyAuthenticatorToBcs(data: PasskeyAuthenticator)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_passkey_authenticator_to_bcs(
        FfiConverterTypePasskeyAuthenticator_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func passkeyAuthenticatorToJson(data: PasskeyAuthenticator)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_passkey_authenticator_to_json(
        FfiConverterTypePasskeyAuthenticator_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func programmableTransactionFromBcs(bcs: Data)throws  -> ProgrammableTransaction  {
    return try  FfiConverterTypeProgrammableTransaction_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_programmable_transaction_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func programmableTransactionFromJson(json: String)throws  -> ProgrammableTransaction  {
    return try  FfiConverterTypeProgrammableTransaction_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_programmable_transaction_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func programmableTransactionToBcs(data: ProgrammableTransaction)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_programmable_transaction_to_bcs(
        FfiConverterTypeProgrammableTransaction_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func programmableTransactionToJson(data: ProgrammableTransaction)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_programmable_transaction_to_json(
        FfiConverterTypeProgrammableTransaction_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func publishFromBcs(bcs: Data)throws  -> Publish  {
    return try  FfiConverterTypePublish_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_publish_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func publishFromJson(json: String)throws  -> Publish  {
    return try  FfiConverterTypePublish_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_publish_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func publishToBcs(data: Publish)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_publish_to_bcs(
        FfiConverterTypePublish_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func publishToJson(data: Publish)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_publish_to_json(
        FfiConverterTypePublish_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func randomnessStateUpdateFromBcs(bcs: Data)throws  -> RandomnessStateUpdate  {
    return try  FfiConverterTypeRandomnessStateUpdate_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_randomness_state_update_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func randomnessStateUpdateFromJson(json: String)throws  -> RandomnessStateUpdate  {
    return try  FfiConverterTypeRandomnessStateUpdate_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_randomness_state_update_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func randomnessStateUpdateToBcs(data: RandomnessStateUpdate)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_randomness_state_update_to_bcs(
        FfiConverterTypeRandomnessStateUpdate_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func randomnessStateUpdateToJson(data: RandomnessStateUpdate)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_randomness_state_update_to_json(
        FfiConverterTypeRandomnessStateUpdate_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func secp256k1PublicKeyFromBcs(bcs: Data)throws  -> Secp256k1PublicKey  {
    return try  FfiConverterTypeSecp256k1PublicKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_secp256k1_public_key_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func secp256k1PublicKeyFromJson(json: String)throws  -> Secp256k1PublicKey  {
    return try  FfiConverterTypeSecp256k1PublicKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_secp256k1_public_key_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func secp256k1PublicKeyToBcs(data: Secp256k1PublicKey)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_secp256k1_public_key_to_bcs(
        FfiConverterTypeSecp256k1PublicKey_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func secp256k1PublicKeyToJson(data: Secp256k1PublicKey)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_secp256k1_public_key_to_json(
        FfiConverterTypeSecp256k1PublicKey_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func secp256k1SignatureFromBcs(bcs: Data)throws  -> Secp256k1Signature  {
    return try  FfiConverterTypeSecp256k1Signature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_secp256k1_signature_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func secp256k1SignatureFromJson(json: String)throws  -> Secp256k1Signature  {
    return try  FfiConverterTypeSecp256k1Signature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_secp256k1_signature_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func secp256k1SignatureToBcs(data: Secp256k1Signature)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_secp256k1_signature_to_bcs(
        FfiConverterTypeSecp256k1Signature_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func secp256k1SignatureToJson(data: Secp256k1Signature)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_secp256k1_signature_to_json(
        FfiConverterTypeSecp256k1Signature_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func secp256r1PublicKeyFromBcs(bcs: Data)throws  -> Secp256r1PublicKey  {
    return try  FfiConverterTypeSecp256r1PublicKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_secp256r1_public_key_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func secp256r1PublicKeyFromJson(json: String)throws  -> Secp256r1PublicKey  {
    return try  FfiConverterTypeSecp256r1PublicKey_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_secp256r1_public_key_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func secp256r1PublicKeyToBcs(data: Secp256r1PublicKey)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_secp256r1_public_key_to_bcs(
        FfiConverterTypeSecp256r1PublicKey_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func secp256r1PublicKeyToJson(data: Secp256r1PublicKey)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_secp256r1_public_key_to_json(
        FfiConverterTypeSecp256r1PublicKey_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func secp256r1SignatureFromBcs(bcs: Data)throws  -> Secp256r1Signature  {
    return try  FfiConverterTypeSecp256r1Signature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_secp256r1_signature_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func secp256r1SignatureFromJson(json: String)throws  -> Secp256r1Signature  {
    return try  FfiConverterTypeSecp256r1Signature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_secp256r1_signature_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func secp256r1SignatureToBcs(data: Secp256r1Signature)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_secp256r1_signature_to_bcs(
        FfiConverterTypeSecp256r1Signature_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func secp256r1SignatureToJson(data: Secp256r1Signature)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_secp256r1_signature_to_json(
        FfiConverterTypeSecp256r1Signature_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func signedTransactionFromBcs(bcs: Data)throws  -> SignedTransaction  {
    return try  FfiConverterTypeSignedTransaction_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_signed_transaction_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func signedTransactionFromJson(json: String)throws  -> SignedTransaction  {
    return try  FfiConverterTypeSignedTransaction_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_signed_transaction_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func signedTransactionToBcs(data: SignedTransaction)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_signed_transaction_to_bcs(
        FfiConverterTypeSignedTransaction_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func signedTransactionToJson(data: SignedTransaction)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_signed_transaction_to_json(
        FfiConverterTypeSignedTransaction_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func simpleSignatureFromBcs(bcs: Data)throws  -> SimpleSignature  {
    return try  FfiConverterTypeSimpleSignature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_simple_signature_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func simpleSignatureFromJson(json: String)throws  -> SimpleSignature  {
    return try  FfiConverterTypeSimpleSignature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_simple_signature_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func simpleSignatureToBcs(data: SimpleSignature)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_simple_signature_to_bcs(
        FfiConverterTypeSimpleSignature_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func simpleSignatureToJson(data: SimpleSignature)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_simple_signature_to_json(
        FfiConverterTypeSimpleSignature_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func splitCoinsFromBcs(bcs: Data)throws  -> SplitCoins  {
    return try  FfiConverterTypeSplitCoins_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_split_coins_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func splitCoinsFromJson(json: String)throws  -> SplitCoins  {
    return try  FfiConverterTypeSplitCoins_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_split_coins_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func splitCoinsToBcs(data: SplitCoins)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_split_coins_to_bcs(
        FfiConverterTypeSplitCoins_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func splitCoinsToJson(data: SplitCoins)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_split_coins_to_json(
        FfiConverterTypeSplitCoins_lower(data),$0
    )
})
}
/**
 * Create a String from BCS encoded bytes.
 */
public func stringFromBcs(input: Data)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_string_from_bcs(
        FfiConverterData.lower(input),$0
    )
})
}
/**
 * Create a String from JSON encoded string.
 */
public func stringFromJson(input: String)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_string_from_json(
        FfiConverterString.lower(input),$0
    )
})
}
/**
 * Convert this String to BCS encoded bytes.
 */
public func stringToBcs(input: String)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_string_to_bcs(
        FfiConverterString.lower(input),$0
    )
})
}
/**
 * Convert this String to JSON encoded string.
 */
public func stringToJson(input: String)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_string_to_json(
        FfiConverterString.lower(input),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func structTagFromBcs(bcs: Data)throws  -> StructTag  {
    return try  FfiConverterTypeStructTag_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_struct_tag_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func structTagFromJson(json: String)throws  -> StructTag  {
    return try  FfiConverterTypeStructTag_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_struct_tag_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func structTagToBcs(data: StructTag)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_struct_tag_to_bcs(
        FfiConverterTypeStructTag_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func structTagToJson(data: StructTag)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_struct_tag_to_json(
        FfiConverterTypeStructTag_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func systemPackageFromBcs(bcs: Data)throws  -> SystemPackage  {
    return try  FfiConverterTypeSystemPackage_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_system_package_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func systemPackageFromJson(json: String)throws  -> SystemPackage  {
    return try  FfiConverterTypeSystemPackage_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_system_package_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func systemPackageToBcs(data: SystemPackage)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_system_package_to_bcs(
        FfiConverterTypeSystemPackage_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func systemPackageToJson(data: SystemPackage)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_system_package_to_json(
        FfiConverterTypeSystemPackage_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func transactionEffectsFromBcs(bcs: Data)throws  -> TransactionEffects  {
    return try  FfiConverterTypeTransactionEffects_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_transaction_effects_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func transactionEffectsFromJson(json: String)throws  -> TransactionEffects  {
    return try  FfiConverterTypeTransactionEffects_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_transaction_effects_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func transactionEffectsToBcs(data: TransactionEffects)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_transaction_effects_to_bcs(
        FfiConverterTypeTransactionEffects_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func transactionEffectsToJson(data: TransactionEffects)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_transaction_effects_to_json(
        FfiConverterTypeTransactionEffects_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func transactionEffectsV1FromBcs(bcs: Data)throws  -> TransactionEffectsV1  {
    return try  FfiConverterTypeTransactionEffectsV1_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_transaction_effects_v1_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func transactionEffectsV1FromJson(json: String)throws  -> TransactionEffectsV1  {
    return try  FfiConverterTypeTransactionEffectsV1_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_transaction_effects_v1_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func transactionEffectsV1ToBcs(data: TransactionEffectsV1)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_transaction_effects_v1_to_bcs(
        FfiConverterTypeTransactionEffectsV1_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func transactionEffectsV1ToJson(data: TransactionEffectsV1)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_transaction_effects_v1_to_json(
        FfiConverterTypeTransactionEffectsV1_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func transactionEventsFromBcs(bcs: Data)throws  -> TransactionEvents  {
    return try  FfiConverterTypeTransactionEvents_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_transaction_events_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func transactionEventsFromJson(json: String)throws  -> TransactionEvents  {
    return try  FfiConverterTypeTransactionEvents_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_transaction_events_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func transactionEventsToBcs(data: TransactionEvents)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_transaction_events_to_bcs(
        FfiConverterTypeTransactionEvents_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func transactionEventsToJson(data: TransactionEvents)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_transaction_events_to_json(
        FfiConverterTypeTransactionEvents_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func transactionExpirationFromBcs(bcs: Data)throws  -> TransactionExpiration  {
    return try  FfiConverterTypeTransactionExpiration_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_transaction_expiration_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func transactionExpirationFromJson(json: String)throws  -> TransactionExpiration  {
    return try  FfiConverterTypeTransactionExpiration_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_transaction_expiration_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func transactionExpirationToBcs(data: TransactionExpiration)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_transaction_expiration_to_bcs(
        FfiConverterTypeTransactionExpiration_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func transactionExpirationToJson(data: TransactionExpiration)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_transaction_expiration_to_json(
        FfiConverterTypeTransactionExpiration_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func transactionFromBcs(bcs: Data)throws  -> Transaction  {
    return try  FfiConverterTypeTransaction_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_transaction_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func transactionFromJson(json: String)throws  -> Transaction  {
    return try  FfiConverterTypeTransaction_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_transaction_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func transactionKindFromBcs(bcs: Data)throws  -> TransactionKind  {
    return try  FfiConverterTypeTransactionKind_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_transaction_kind_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func transactionKindFromJson(json: String)throws  -> TransactionKind  {
    return try  FfiConverterTypeTransactionKind_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_transaction_kind_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func transactionKindToBcs(data: TransactionKind)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_transaction_kind_to_bcs(
        FfiConverterTypeTransactionKind_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func transactionKindToJson(data: TransactionKind)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_transaction_kind_to_json(
        FfiConverterTypeTransactionKind_lower(data),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func transactionToBcs(data: Transaction)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_transaction_to_bcs(
        FfiConverterTypeTransaction_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func transactionToJson(data: Transaction)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_transaction_to_json(
        FfiConverterTypeTransaction_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func transactionV1FromBcs(bcs: Data)throws  -> TransactionV1  {
    return try  FfiConverterTypeTransactionV1_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_transaction_v1_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func transactionV1FromJson(json: String)throws  -> TransactionV1  {
    return try  FfiConverterTypeTransactionV1_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_transaction_v1_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func transactionV1ToBcs(data: TransactionV1)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_transaction_v1_to_bcs(
        FfiConverterTypeTransactionV1_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func transactionV1ToJson(data: TransactionV1)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_transaction_v1_to_json(
        FfiConverterTypeTransactionV1_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func transferObjectsFromBcs(bcs: Data)throws  -> TransferObjects  {
    return try  FfiConverterTypeTransferObjects_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_transfer_objects_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func transferObjectsFromJson(json: String)throws  -> TransferObjects  {
    return try  FfiConverterTypeTransferObjects_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_transfer_objects_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func transferObjectsToBcs(data: TransferObjects)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_transfer_objects_to_bcs(
        FfiConverterTypeTransferObjects_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func transferObjectsToJson(data: TransferObjects)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_transfer_objects_to_json(
        FfiConverterTypeTransferObjects_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func typeArgumentErrorFromBcs(bcs: Data)throws  -> TypeArgumentError  {
    return try  FfiConverterTypeTypeArgumentError_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_type_argument_error_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func typeArgumentErrorFromJson(json: String)throws  -> TypeArgumentError  {
    return try  FfiConverterTypeTypeArgumentError_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_type_argument_error_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func typeArgumentErrorToBcs(data: TypeArgumentError)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_type_argument_error_to_bcs(
        FfiConverterTypeTypeArgumentError_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func typeArgumentErrorToJson(data: TypeArgumentError)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_type_argument_error_to_json(
        FfiConverterTypeTypeArgumentError_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func typeOriginFromBcs(bcs: Data)throws  -> TypeOrigin  {
    return try  FfiConverterTypeTypeOrigin_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_type_origin_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func typeOriginFromJson(json: String)throws  -> TypeOrigin  {
    return try  FfiConverterTypeTypeOrigin_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_type_origin_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func typeOriginToBcs(data: TypeOrigin)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_type_origin_to_bcs(
        FfiConverterTypeTypeOrigin_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func typeOriginToJson(data: TypeOrigin)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_type_origin_to_json(
        FfiConverterTypeTypeOrigin_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func typeTagFromBcs(bcs: Data)throws  -> TypeTag  {
    return try  FfiConverterTypeTypeTag_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_type_tag_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func typeTagFromJson(json: String)throws  -> TypeTag  {
    return try  FfiConverterTypeTypeTag_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_type_tag_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func typeTagToBcs(data: TypeTag)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_type_tag_to_bcs(
        FfiConverterTypeTypeTag_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func typeTagToJson(data: TypeTag)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_type_tag_to_json(
        FfiConverterTypeTypeTag_lower(data),$0
    )
})
}
/**
 * Create a u16 from BCS encoded bytes.
 */
public func u16FromBcs(input: Data)throws  -> UInt16  {
    return try  FfiConverterUInt16.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_u16_from_bcs(
        FfiConverterData.lower(input),$0
    )
})
}
/**
 * Create a u16 from JSON encoded string.
 */
public func u16FromJson(input: String)throws  -> UInt16  {
    return try  FfiConverterUInt16.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_u16_from_json(
        FfiConverterString.lower(input),$0
    )
})
}
/**
 * Convert this u16 to BCS encoded bytes.
 */
public func u16ToBcs(input: UInt16)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_u16_to_bcs(
        FfiConverterUInt16.lower(input),$0
    )
})
}
/**
 * Convert this u16 to JSON encoded string.
 */
public func u16ToJson(input: UInt16)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_u16_to_json(
        FfiConverterUInt16.lower(input),$0
    )
})
}
/**
 * Create a u32 from BCS encoded bytes.
 */
public func u32FromBcs(input: Data)throws  -> UInt32  {
    return try  FfiConverterUInt32.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_u32_from_bcs(
        FfiConverterData.lower(input),$0
    )
})
}
/**
 * Create a u32 from JSON encoded string.
 */
public func u32FromJson(input: String)throws  -> UInt32  {
    return try  FfiConverterUInt32.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_u32_from_json(
        FfiConverterString.lower(input),$0
    )
})
}
/**
 * Convert this u32 to BCS encoded bytes.
 */
public func u32ToBcs(input: UInt32)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_u32_to_bcs(
        FfiConverterUInt32.lower(input),$0
    )
})
}
/**
 * Convert this u32 to JSON encoded string.
 */
public func u32ToJson(input: UInt32)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_u32_to_json(
        FfiConverterUInt32.lower(input),$0
    )
})
}
/**
 * Create a u64 from BCS encoded bytes.
 */
public func u64FromBcs(input: Data)throws  -> UInt64  {
    return try  FfiConverterUInt64.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_u64_from_bcs(
        FfiConverterData.lower(input),$0
    )
})
}
/**
 * Create a u64 from JSON encoded string.
 */
public func u64FromJson(input: String)throws  -> UInt64  {
    return try  FfiConverterUInt64.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_u64_from_json(
        FfiConverterString.lower(input),$0
    )
})
}
/**
 * Convert this u64 to BCS encoded bytes.
 */
public func u64ToBcs(input: UInt64)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_u64_to_bcs(
        FfiConverterUInt64.lower(input),$0
    )
})
}
/**
 * Convert this u64 to JSON encoded string.
 */
public func u64ToJson(input: UInt64)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_u64_to_json(
        FfiConverterUInt64.lower(input),$0
    )
})
}
/**
 * Create a u8 from BCS encoded bytes.
 */
public func u8FromBcs(input: Data)throws  -> UInt8  {
    return try  FfiConverterUInt8.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_u8_from_bcs(
        FfiConverterData.lower(input),$0
    )
})
}
/**
 * Create a u8 from JSON encoded string.
 */
public func u8FromJson(input: String)throws  -> UInt8  {
    return try  FfiConverterUInt8.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_u8_from_json(
        FfiConverterString.lower(input),$0
    )
})
}
/**
 * Convert this u8 to BCS encoded bytes.
 */
public func u8ToBcs(input: UInt8)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_u8_to_bcs(
        FfiConverterUInt8.lower(input),$0
    )
})
}
/**
 * Convert this u8 to JSON encoded string.
 */
public func u8ToJson(input: UInt8)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_u8_to_json(
        FfiConverterUInt8.lower(input),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func unchangedSharedKindFromBcs(bcs: Data)throws  -> UnchangedSharedKind  {
    return try  FfiConverterTypeUnchangedSharedKind_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_unchanged_shared_kind_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func unchangedSharedKindFromJson(json: String)throws  -> UnchangedSharedKind  {
    return try  FfiConverterTypeUnchangedSharedKind_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_unchanged_shared_kind_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func unchangedSharedKindToBcs(data: UnchangedSharedKind)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_unchanged_shared_kind_to_bcs(
        FfiConverterTypeUnchangedSharedKind_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func unchangedSharedKindToJson(data: UnchangedSharedKind)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_unchanged_shared_kind_to_json(
        FfiConverterTypeUnchangedSharedKind_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func unchangedSharedObjectFromBcs(bcs: Data)throws  -> UnchangedSharedObject  {
    return try  FfiConverterTypeUnchangedSharedObject_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_unchanged_shared_object_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func unchangedSharedObjectFromJson(json: String)throws  -> UnchangedSharedObject  {
    return try  FfiConverterTypeUnchangedSharedObject_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_unchanged_shared_object_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func unchangedSharedObjectToBcs(data: UnchangedSharedObject)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_unchanged_shared_object_to_bcs(
        FfiConverterTypeUnchangedSharedObject_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func unchangedSharedObjectToJson(data: UnchangedSharedObject)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_unchanged_shared_object_to_json(
        FfiConverterTypeUnchangedSharedObject_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func upgradeFromBcs(bcs: Data)throws  -> Upgrade  {
    return try  FfiConverterTypeUpgrade_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_upgrade_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func upgradeFromJson(json: String)throws  -> Upgrade  {
    return try  FfiConverterTypeUpgrade_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_upgrade_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func upgradeInfoFromBcs(bcs: Data)throws  -> UpgradeInfo  {
    return try  FfiConverterTypeUpgradeInfo_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_upgrade_info_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func upgradeInfoFromJson(json: String)throws  -> UpgradeInfo  {
    return try  FfiConverterTypeUpgradeInfo_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_upgrade_info_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func upgradeInfoToBcs(data: UpgradeInfo)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_upgrade_info_to_bcs(
        FfiConverterTypeUpgradeInfo_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func upgradeInfoToJson(data: UpgradeInfo)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_upgrade_info_to_json(
        FfiConverterTypeUpgradeInfo_lower(data),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func upgradeToBcs(data: Upgrade)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_upgrade_to_bcs(
        FfiConverterTypeUpgrade_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func upgradeToJson(data: Upgrade)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_upgrade_to_json(
        FfiConverterTypeUpgrade_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func userSignatureFromBcs(bcs: Data)throws  -> UserSignature  {
    return try  FfiConverterTypeUserSignature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_user_signature_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func userSignatureFromJson(json: String)throws  -> UserSignature  {
    return try  FfiConverterTypeUserSignature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_user_signature_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func userSignatureToBcs(data: UserSignature)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_user_signature_to_bcs(
        FfiConverterTypeUserSignature_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func userSignatureToJson(data: UserSignature)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_user_signature_to_json(
        FfiConverterTypeUserSignature_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func validatorAggregatedSignatureFromBcs(bcs: Data)throws  -> ValidatorAggregatedSignature  {
    return try  FfiConverterTypeValidatorAggregatedSignature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_validator_aggregated_signature_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func validatorAggregatedSignatureFromJson(json: String)throws  -> ValidatorAggregatedSignature  {
    return try  FfiConverterTypeValidatorAggregatedSignature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_validator_aggregated_signature_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func validatorAggregatedSignatureToBcs(data: ValidatorAggregatedSignature)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_validator_aggregated_signature_to_bcs(
        FfiConverterTypeValidatorAggregatedSignature_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func validatorAggregatedSignatureToJson(data: ValidatorAggregatedSignature)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_validator_aggregated_signature_to_json(
        FfiConverterTypeValidatorAggregatedSignature_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func validatorCommitteeFromBcs(bcs: Data)throws  -> ValidatorCommittee  {
    return try  FfiConverterTypeValidatorCommittee_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_validator_committee_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func validatorCommitteeFromJson(json: String)throws  -> ValidatorCommittee  {
    return try  FfiConverterTypeValidatorCommittee_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_validator_committee_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func validatorCommitteeMemberFromBcs(bcs: Data)throws  -> ValidatorCommitteeMember  {
    return try  FfiConverterTypeValidatorCommitteeMember_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_validator_committee_member_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func validatorCommitteeMemberFromJson(json: String)throws  -> ValidatorCommitteeMember  {
    return try  FfiConverterTypeValidatorCommitteeMember_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_validator_committee_member_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func validatorCommitteeMemberToBcs(data: ValidatorCommitteeMember)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_validator_committee_member_to_bcs(
        FfiConverterTypeValidatorCommitteeMember_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func validatorCommitteeMemberToJson(data: ValidatorCommitteeMember)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_validator_committee_member_to_json(
        FfiConverterTypeValidatorCommitteeMember_lower(data),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func validatorCommitteeToBcs(data: ValidatorCommittee)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_validator_committee_to_bcs(
        FfiConverterTypeValidatorCommittee_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func validatorCommitteeToJson(data: ValidatorCommittee)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_validator_committee_to_json(
        FfiConverterTypeValidatorCommittee_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func validatorExecutionTimeObservationFromBcs(bcs: Data)throws  -> ValidatorExecutionTimeObservation  {
    return try  FfiConverterTypeValidatorExecutionTimeObservation_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_validator_execution_time_observation_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func validatorExecutionTimeObservationFromJson(json: String)throws  -> ValidatorExecutionTimeObservation  {
    return try  FfiConverterTypeValidatorExecutionTimeObservation_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_validator_execution_time_observation_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func validatorExecutionTimeObservationToBcs(data: ValidatorExecutionTimeObservation)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_validator_execution_time_observation_to_bcs(
        FfiConverterTypeValidatorExecutionTimeObservation_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func validatorExecutionTimeObservationToJson(data: ValidatorExecutionTimeObservation)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_validator_execution_time_observation_to_json(
        FfiConverterTypeValidatorExecutionTimeObservation_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func validatorSignatureFromBcs(bcs: Data)throws  -> ValidatorSignature  {
    return try  FfiConverterTypeValidatorSignature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_validator_signature_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func validatorSignatureFromJson(json: String)throws  -> ValidatorSignature  {
    return try  FfiConverterTypeValidatorSignature_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_validator_signature_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func validatorSignatureToBcs(data: ValidatorSignature)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_validator_signature_to_bcs(
        FfiConverterTypeValidatorSignature_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func validatorSignatureToJson(data: ValidatorSignature)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_validator_signature_to_json(
        FfiConverterTypeValidatorSignature_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func versionAssignmentFromBcs(bcs: Data)throws  -> VersionAssignment  {
    return try  FfiConverterTypeVersionAssignment_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_version_assignment_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func versionAssignmentFromJson(json: String)throws  -> VersionAssignment  {
    return try  FfiConverterTypeVersionAssignment_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_version_assignment_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func versionAssignmentToBcs(data: VersionAssignment)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_version_assignment_to_bcs(
        FfiConverterTypeVersionAssignment_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func versionAssignmentToJson(data: VersionAssignment)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_version_assignment_to_json(
        FfiConverterTypeVersionAssignment_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func zkLoginAuthenticatorFromBcs(bcs: Data)throws  -> ZkLoginAuthenticator  {
    return try  FfiConverterTypeZkLoginAuthenticator_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_zk_login_authenticator_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func zkLoginAuthenticatorFromJson(json: String)throws  -> ZkLoginAuthenticator  {
    return try  FfiConverterTypeZkLoginAuthenticator_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_zk_login_authenticator_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func zkLoginAuthenticatorToBcs(data: ZkLoginAuthenticator)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_zk_login_authenticator_to_bcs(
        FfiConverterTypeZkLoginAuthenticator_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func zkLoginAuthenticatorToJson(data: ZkLoginAuthenticator)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_zk_login_authenticator_to_json(
        FfiConverterTypeZkLoginAuthenticator_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func zkLoginClaimFromBcs(bcs: Data)throws  -> ZkLoginClaim  {
    return try  FfiConverterTypeZkLoginClaim_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_zk_login_claim_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func zkLoginClaimFromJson(json: String)throws  -> ZkLoginClaim  {
    return try  FfiConverterTypeZkLoginClaim_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_zk_login_claim_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func zkLoginClaimToBcs(data: ZkLoginClaim)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_zk_login_claim_to_bcs(
        FfiConverterTypeZkLoginClaim_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func zkLoginClaimToJson(data: ZkLoginClaim)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_zk_login_claim_to_json(
        FfiConverterTypeZkLoginClaim_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func zkLoginProofFromBcs(bcs: Data)throws  -> ZkLoginProof  {
    return try  FfiConverterTypeZkLoginProof_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_zk_login_proof_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func zkLoginProofFromJson(json: String)throws  -> ZkLoginProof  {
    return try  FfiConverterTypeZkLoginProof_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_zk_login_proof_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func zkLoginProofToBcs(data: ZkLoginProof)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_zk_login_proof_to_bcs(
        FfiConverterTypeZkLoginProof_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func zkLoginProofToJson(data: ZkLoginProof)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_zk_login_proof_to_json(
        FfiConverterTypeZkLoginProof_lower(data),$0
    )
})
}
/**
 * Create this type from BCS encoded bytes.
 */
public func zkLoginPublicIdentifierFromBcs(bcs: Data)throws  -> ZkLoginPublicIdentifier  {
    return try  FfiConverterTypeZkLoginPublicIdentifier_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_zk_login_public_identifier_from_bcs(
        FfiConverterData.lower(bcs),$0
    )
})
}
/**
 * Create this type from JSON encoded string.
 */
public func zkLoginPublicIdentifierFromJson(json: String)throws  -> ZkLoginPublicIdentifier  {
    return try  FfiConverterTypeZkLoginPublicIdentifier_lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_zk_login_public_identifier_from_json(
        FfiConverterString.lower(json),$0
    )
})
}
/**
 * Convert this type to BCS encoded bytes.
 */
public func zkLoginPublicIdentifierToBcs(data: ZkLoginPublicIdentifier)throws  -> Data  {
    return try  FfiConverterData.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_zk_login_public_identifier_to_bcs(
        FfiConverterTypeZkLoginPublicIdentifier_lower(data),$0
    )
})
}
/**
 * Convert this type to JSON encoded string.
 */
public func zkLoginPublicIdentifierToJson(data: ZkLoginPublicIdentifier)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeSdkFfiError_lift) {
    uniffi_iota_sdk_ffi_fn_func_zk_login_public_identifier_to_json(
        FfiConverterTypeZkLoginPublicIdentifier_lower(data),$0
    )
})
}

private enum InitializationResult {
    case ok
    case contractVersionMismatch
    case apiChecksumMismatch
}
// Use a global variable to perform the versioning checks. Swift ensures that
// the code inside is only computed once.
private let initializationResult: InitializationResult = {
    // Get the bindings contract version from our ComponentInterface
    let bindings_contract_version = 29
    // Get the scaffolding contract version by calling the into the dylib
    let scaffolding_contract_version = ffi_iota_sdk_ffi_uniffi_contract_version()
    if bindings_contract_version != scaffolding_contract_version {
        return InitializationResult.contractVersionMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_active_jwk_from_bcs() != 65415) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_active_jwk_from_json() != 5693) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_active_jwk_to_bcs() != 37978) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_active_jwk_to_json() != 56327) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_address_from_bcs() != 8229) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_address_from_json() != 41660) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_address_to_bcs() != 61852) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_address_to_json() != 25843) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_argument_from_bcs() != 3719) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_argument_from_json() != 63647) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_argument_to_bcs() != 11813) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_argument_to_json() != 34680) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_authenticator_state_expire_from_bcs() != 23378) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_authenticator_state_expire_from_json() != 19654) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_authenticator_state_expire_to_bcs() != 26245) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_authenticator_state_expire_to_json() != 46170) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_authenticator_state_update_v1_from_bcs() != 39650) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_authenticator_state_update_v1_from_json() != 30949) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_authenticator_state_update_v1_to_bcs() != 31251) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_authenticator_state_update_v1_to_json() != 36459) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_base64_decode() != 57367) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_base64_encode() != 54791) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_bls12381_public_key_from_bcs() != 27639) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_bls12381_public_key_from_json() != 30085) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_bls12381_public_key_to_bcs() != 45714) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_bls12381_public_key_to_json() != 23914) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_bls12381_signature_from_bcs() != 28208) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_bls12381_signature_from_json() != 64948) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_bls12381_signature_to_bcs() != 29002) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_bls12381_signature_to_json() != 30321) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_bn254_field_element_from_bcs() != 13721) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_bn254_field_element_from_json() != 23224) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_bn254_field_element_to_bcs() != 3863) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_bn254_field_element_to_json() != 7045) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_bool_from_bcs() != 591) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_bool_from_json() != 50565) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_bool_to_bcs() != 11338) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_bool_to_json() != 60181) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_cancelled_transaction_from_bcs() != 3386) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_cancelled_transaction_from_json() != 50792) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_cancelled_transaction_to_bcs() != 32242) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_cancelled_transaction_to_json() != 64090) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_change_epoch_from_bcs() != 49588) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_change_epoch_from_json() != 460) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_change_epoch_to_bcs() != 4565) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_change_epoch_to_json() != 61948) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_change_epoch_v2_from_bcs() != 16404) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_change_epoch_v2_from_json() != 8135) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_change_epoch_v2_to_bcs() != 12967) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_change_epoch_v2_to_json() != 24993) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_changed_object_from_bcs() != 41461) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_changed_object_from_json() != 63813) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_changed_object_to_bcs() != 58498) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_changed_object_to_json() != 4423) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_checkpoint_commitment_from_bcs() != 62657) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_checkpoint_commitment_from_json() != 39325) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_checkpoint_commitment_to_bcs() != 35587) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_checkpoint_commitment_to_json() != 50864) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_checkpoint_contents_from_bcs() != 1002) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_checkpoint_contents_from_json() != 34494) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_checkpoint_contents_to_bcs() != 52481) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_checkpoint_contents_to_json() != 25866) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_checkpoint_summary_from_bcs() != 23217) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_checkpoint_summary_from_json() != 35311) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_checkpoint_summary_to_bcs() != 48157) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_checkpoint_summary_to_json() != 7348) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_checkpoint_transaction_info_from_bcs() != 40094) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_checkpoint_transaction_info_from_json() != 29420) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_checkpoint_transaction_info_to_bcs() != 62034) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_checkpoint_transaction_info_to_json() != 12780) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_circom_g1_from_bcs() != 60233) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_circom_g1_from_json() != 3835) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_circom_g1_to_bcs() != 19531) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_circom_g1_to_json() != 15996) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_circom_g2_from_bcs() != 1042) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_circom_g2_from_json() != 48517) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_circom_g2_to_bcs() != 8225) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_circom_g2_to_json() != 30332) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_command_argument_error_from_bcs() != 5998) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_command_argument_error_from_json() != 63658) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_command_argument_error_to_bcs() != 30279) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_command_argument_error_to_json() != 21781) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_command_from_bcs() != 25814) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_command_from_json() != 21028) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_command_to_bcs() != 38816) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_command_to_json() != 13937) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_consensus_commit_prologue_v1_from_bcs() != 22391) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_consensus_commit_prologue_v1_from_json() != 19933) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_consensus_commit_prologue_v1_to_bcs() != 13457) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_consensus_commit_prologue_v1_to_json() != 11681) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_consensus_determined_version_assignments_from_bcs() != 30528) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_consensus_determined_version_assignments_from_json() != 33488) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_consensus_determined_version_assignments_to_bcs() != 35973) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_consensus_determined_version_assignments_to_json() != 29958) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_digest_from_bcs() != 56231) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_digest_from_json() != 51793) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_digest_to_bcs() != 10551) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_digest_to_json() != 54129) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_ed25519_public_key_from_bcs() != 6221) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_ed25519_public_key_from_json() != 33237) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_ed25519_public_key_to_bcs() != 30464) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_ed25519_public_key_to_json() != 63411) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_ed25519_signature_from_bcs() != 39614) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_ed25519_signature_from_json() != 19797) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_ed25519_signature_to_bcs() != 17200) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_ed25519_signature_to_json() != 12851) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_end_of_epoch_data_from_bcs() != 36284) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_end_of_epoch_data_from_json() != 49680) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_end_of_epoch_data_to_bcs() != 6697) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_end_of_epoch_data_to_json() != 24758) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_event_from_bcs() != 11285) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_event_from_json() != 44239) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_event_to_bcs() != 35514) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_event_to_json() != 20073) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_execution_error_from_bcs() != 54349) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_execution_error_from_json() != 22231) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_execution_error_to_bcs() != 1185) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_execution_error_to_json() != 32172) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_execution_status_from_bcs() != 65175) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_execution_status_from_json() != 47218) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_execution_status_to_bcs() != 42821) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_execution_status_to_json() != 27344) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_execution_time_observation_from_bcs() != 19124) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_execution_time_observation_from_json() != 46087) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_execution_time_observation_key_from_bcs() != 3465) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_execution_time_observation_key_from_json() != 23737) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_execution_time_observation_key_to_bcs() != 166) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_execution_time_observation_key_to_json() != 63452) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_execution_time_observation_to_bcs() != 35779) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_execution_time_observation_to_json() != 26451) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_execution_time_observations_from_bcs() != 954) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_execution_time_observations_from_json() != 31779) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_execution_time_observations_to_bcs() != 38212) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_execution_time_observations_to_json() != 36369) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_gas_cost_summary_from_bcs() != 305) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_gas_cost_summary_from_json() != 41962) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_gas_cost_summary_to_bcs() != 39838) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_gas_cost_summary_to_json() != 48602) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_gas_payment_from_bcs() != 20862) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_gas_payment_from_json() != 50456) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_gas_payment_to_bcs() != 2681) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_gas_payment_to_json() != 5922) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_generate_mnemonic() != 15726) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_genesis_object_from_bcs() != 15482) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_genesis_object_from_json() != 30203) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_genesis_object_to_bcs() != 63349) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_genesis_object_to_json() != 27957) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_genesis_transaction_from_bcs() != 21010) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_genesis_transaction_from_json() != 40483) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_genesis_transaction_to_bcs() != 17884) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_genesis_transaction_to_json() != 26531) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_hex_decode() != 35424) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_hex_encode() != 34343) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_i16_from_bcs() != 10273) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_i16_from_json() != 14587) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_i16_to_bcs() != 28969) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_i16_to_json() != 40601) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_i32_from_bcs() != 7849) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_i32_from_json() != 59121) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_i32_to_bcs() != 52773) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_i32_to_json() != 47860) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_i64_from_bcs() != 17714) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_i64_from_json() != 14092) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_i64_to_bcs() != 38028) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_i64_to_json() != 34550) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_i8_from_bcs() != 21810) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_i8_from_json() != 60479) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_i8_to_bcs() != 57592) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_i8_to_json() != 33302) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_id_operation_from_bcs() != 10384) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_id_operation_from_json() != 34478) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_id_operation_to_bcs() != 62670) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_id_operation_to_json() != 37062) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_identifier_from_bcs() != 3668) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_identifier_from_json() != 21050) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_identifier_to_bcs() != 46605) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_identifier_to_json() != 589) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_input_from_bcs() != 13339) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_input_from_json() != 1202) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_input_to_bcs() != 51152) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_input_to_json() != 58430) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_jwk_from_bcs() != 14752) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_jwk_from_json() != 33813) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_jwk_id_from_bcs() != 22836) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_jwk_id_from_json() != 1916) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_jwk_id_to_bcs() != 2155) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_jwk_id_to_json() != 11288) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_jwk_to_bcs() != 29567) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_jwk_to_json() != 53403) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_make_move_vector_from_bcs() != 17479) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_make_move_vector_from_json() != 38727) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_make_move_vector_to_bcs() != 17288) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_make_move_vector_to_json() != 56268) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_merge_coins_from_bcs() != 33652) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_merge_coins_from_json() != 51938) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_merge_coins_to_bcs() != 40802) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_merge_coins_to_json() != 48742) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_move_call_from_bcs() != 3402) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_move_call_from_json() != 51308) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_move_call_to_bcs() != 40106) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_move_call_to_json() != 42556) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_move_location_from_bcs() != 62161) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_move_location_from_json() != 18887) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_move_location_to_bcs() != 25104) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_move_location_to_json() != 62545) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_move_package_from_bcs() != 922) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_move_package_from_json() != 11792) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_move_package_to_bcs() != 53617) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_move_package_to_json() != 32768) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_move_struct_from_bcs() != 25715) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_move_struct_from_json() != 38200) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_move_struct_to_bcs() != 9518) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_move_struct_to_json() != 34559) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_multisig_aggregated_signature_from_bcs() != 56951) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_multisig_aggregated_signature_from_json() != 98) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_multisig_aggregated_signature_to_bcs() != 57074) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_multisig_aggregated_signature_to_json() != 4796) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_multisig_committee_from_bcs() != 49092) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_multisig_committee_from_json() != 46010) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_multisig_committee_to_bcs() != 11846) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_multisig_committee_to_json() != 32969) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_multisig_member_from_bcs() != 4473) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_multisig_member_from_json() != 8986) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_multisig_member_public_key_from_bcs() != 55389) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_multisig_member_public_key_from_json() != 11231) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_multisig_member_public_key_to_bcs() != 8657) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_multisig_member_public_key_to_json() != 44891) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_multisig_member_signature_from_bcs() != 26016) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_multisig_member_signature_from_json() != 53075) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_multisig_member_signature_to_bcs() != 49432) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_multisig_member_signature_to_json() != 39965) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_multisig_member_to_bcs() != 50267) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_multisig_member_to_json() != 14243) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_object_data_from_bcs() != 29702) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_object_data_from_json() != 54757) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_object_data_to_bcs() != 13288) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_object_data_to_json() != 49367) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_object_from_bcs() != 58875) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_object_from_json() != 36259) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_object_id_from_bcs() != 39824) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_object_id_from_json() != 64886) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_object_id_to_bcs() != 48535) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_object_id_to_json() != 46143) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_object_in_from_bcs() != 36098) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_object_in_from_json() != 34053) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_object_in_to_bcs() != 37270) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_object_in_to_json() != 27756) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_object_out_from_bcs() != 55820) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_object_out_from_json() != 33803) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_object_out_to_bcs() != 57777) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_object_out_to_json() != 28836) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_object_reference_from_bcs() != 2613) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_object_reference_from_json() != 5254) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_object_reference_to_bcs() != 50964) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_object_reference_to_json() != 2887) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_object_to_bcs() != 59689) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_object_to_json() != 59309) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_owner_from_bcs() != 60479) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_owner_from_json() != 23721) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_owner_to_bcs() != 40596) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_owner_to_json() != 31227) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_package_upgrade_error_from_bcs() != 19771) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_package_upgrade_error_from_json() != 25546) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_package_upgrade_error_to_bcs() != 36019) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_package_upgrade_error_to_json() != 50911) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_passkey_authenticator_from_bcs() != 5683) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_passkey_authenticator_from_json() != 60040) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_passkey_authenticator_to_bcs() != 26250) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_passkey_authenticator_to_json() != 2367) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_programmable_transaction_from_bcs() != 28762) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_programmable_transaction_from_json() != 2263) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_programmable_transaction_to_bcs() != 49182) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_programmable_transaction_to_json() != 18529) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_publish_from_bcs() != 59778) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_publish_from_json() != 6116) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_publish_to_bcs() != 44879) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_publish_to_json() != 26693) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_randomness_state_update_from_bcs() != 17429) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_randomness_state_update_from_json() != 53809) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_randomness_state_update_to_bcs() != 53446) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_randomness_state_update_to_json() != 8065) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_secp256k1_public_key_from_bcs() != 26826) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_secp256k1_public_key_from_json() != 3328) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_secp256k1_public_key_to_bcs() != 20301) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_secp256k1_public_key_to_json() != 15075) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_secp256k1_signature_from_bcs() != 19555) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_secp256k1_signature_from_json() != 18554) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_secp256k1_signature_to_bcs() != 65529) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_secp256k1_signature_to_json() != 23137) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_secp256r1_public_key_from_bcs() != 44046) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_secp256r1_public_key_from_json() != 50594) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_secp256r1_public_key_to_bcs() != 12590) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_secp256r1_public_key_to_json() != 48547) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_secp256r1_signature_from_bcs() != 17047) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_secp256r1_signature_from_json() != 14620) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_secp256r1_signature_to_bcs() != 2418) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_secp256r1_signature_to_json() != 32844) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_signed_transaction_from_bcs() != 42264) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_signed_transaction_from_json() != 26759) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_signed_transaction_to_bcs() != 44178) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_signed_transaction_to_json() != 24370) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_simple_signature_from_bcs() != 19746) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_simple_signature_from_json() != 15462) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_simple_signature_to_bcs() != 36033) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_simple_signature_to_json() != 21272) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_split_coins_from_bcs() != 55454) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_split_coins_from_json() != 40834) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_split_coins_to_bcs() != 30667) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_split_coins_to_json() != 35573) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_string_from_bcs() != 53853) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_string_from_json() != 26837) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_string_to_bcs() != 54804) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_string_to_json() != 9027) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_struct_tag_from_bcs() != 24910) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_struct_tag_from_json() != 17752) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_struct_tag_to_bcs() != 23529) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_struct_tag_to_json() != 24877) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_system_package_from_bcs() != 60428) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_system_package_from_json() != 21275) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_system_package_to_bcs() != 40961) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_system_package_to_json() != 63223) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_transaction_effects_from_bcs() != 36911) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_transaction_effects_from_json() != 55089) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_transaction_effects_to_bcs() != 43876) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_transaction_effects_to_json() != 61943) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_transaction_effects_v1_from_bcs() != 64140) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_transaction_effects_v1_from_json() != 44964) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_transaction_effects_v1_to_bcs() != 12420) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_transaction_effects_v1_to_json() != 4583) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_transaction_events_from_bcs() != 50477) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_transaction_events_from_json() != 43904) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_transaction_events_to_bcs() != 49708) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_transaction_events_to_json() != 24644) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_transaction_expiration_from_bcs() != 12318) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_transaction_expiration_from_json() != 54989) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_transaction_expiration_to_bcs() != 34807) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_transaction_expiration_to_json() != 21600) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_transaction_from_bcs() != 52964) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_transaction_from_json() != 14866) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_transaction_kind_from_bcs() != 19749) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_transaction_kind_from_json() != 26639) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_transaction_kind_to_bcs() != 38945) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_transaction_kind_to_json() != 728) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_transaction_to_bcs() != 38438) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_transaction_to_json() != 45859) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_transaction_v1_from_bcs() != 56096) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_transaction_v1_from_json() != 50289) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_transaction_v1_to_bcs() != 62454) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_transaction_v1_to_json() != 56289) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_transfer_objects_from_bcs() != 34010) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_transfer_objects_from_json() != 39182) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_transfer_objects_to_bcs() != 59364) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_transfer_objects_to_json() != 55404) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_type_argument_error_from_bcs() != 30578) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_type_argument_error_from_json() != 40427) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_type_argument_error_to_bcs() != 22531) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_type_argument_error_to_json() != 63397) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_type_origin_from_bcs() != 63210) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_type_origin_from_json() != 33178) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_type_origin_to_bcs() != 62869) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_type_origin_to_json() != 37099) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_type_tag_from_bcs() != 31167) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_type_tag_from_json() != 35777) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_type_tag_to_bcs() != 57365) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_type_tag_to_json() != 56224) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_u16_from_bcs() != 3653) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_u16_from_json() != 2492) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_u16_to_bcs() != 13254) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_u16_to_json() != 3460) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_u32_from_bcs() != 335) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_u32_from_json() != 43957) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_u32_to_bcs() != 36384) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_u32_to_json() != 37222) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_u64_from_bcs() != 63829) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_u64_from_json() != 47252) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_u64_to_bcs() != 2947) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_u64_to_json() != 47609) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_u8_from_bcs() != 34764) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_u8_from_json() != 40043) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_u8_to_bcs() != 59020) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_u8_to_json() != 32613) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_unchanged_shared_kind_from_bcs() != 6308) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_unchanged_shared_kind_from_json() != 31138) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_unchanged_shared_kind_to_bcs() != 46464) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_unchanged_shared_kind_to_json() != 53371) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_unchanged_shared_object_from_bcs() != 28910) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_unchanged_shared_object_from_json() != 44447) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_unchanged_shared_object_to_bcs() != 38998) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_unchanged_shared_object_to_json() != 17180) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_upgrade_from_bcs() != 19484) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_upgrade_from_json() != 44433) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_upgrade_info_from_bcs() != 58871) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_upgrade_info_from_json() != 60702) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_upgrade_info_to_bcs() != 8867) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_upgrade_info_to_json() != 13396) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_upgrade_to_bcs() != 32303) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_upgrade_to_json() != 54761) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_user_signature_from_bcs() != 8567) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_user_signature_from_json() != 19747) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_user_signature_to_bcs() != 47630) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_user_signature_to_json() != 29042) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_validator_aggregated_signature_from_bcs() != 21169) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_validator_aggregated_signature_from_json() != 30596) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_validator_aggregated_signature_to_bcs() != 44404) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_validator_aggregated_signature_to_json() != 27783) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_validator_committee_from_bcs() != 30352) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_validator_committee_from_json() != 56969) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_validator_committee_member_from_bcs() != 27463) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_validator_committee_member_from_json() != 32289) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_validator_committee_member_to_bcs() != 54694) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_validator_committee_member_to_json() != 42545) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_validator_committee_to_bcs() != 31448) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_validator_committee_to_json() != 40417) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_validator_execution_time_observation_from_bcs() != 28604) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_validator_execution_time_observation_from_json() != 59417) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_validator_execution_time_observation_to_bcs() != 35325) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_validator_execution_time_observation_to_json() != 11778) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_validator_signature_from_bcs() != 57394) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_validator_signature_from_json() != 19090) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_validator_signature_to_bcs() != 19559) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_validator_signature_to_json() != 55532) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_version_assignment_from_bcs() != 23398) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_version_assignment_from_json() != 921) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_version_assignment_to_bcs() != 44956) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_version_assignment_to_json() != 21440) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_zk_login_authenticator_from_bcs() != 48317) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_zk_login_authenticator_from_json() != 19619) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_zk_login_authenticator_to_bcs() != 50917) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_zk_login_authenticator_to_json() != 43592) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_zk_login_claim_from_bcs() != 47140) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_zk_login_claim_from_json() != 60759) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_zk_login_claim_to_bcs() != 58412) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_zk_login_claim_to_json() != 9777) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_zk_login_proof_from_bcs() != 13910) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_zk_login_proof_from_json() != 31645) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_zk_login_proof_to_bcs() != 22664) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_zk_login_proof_to_json() != 6200) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_zk_login_public_identifier_from_bcs() != 35391) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_zk_login_public_identifier_from_json() != 24841) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_zk_login_public_identifier_to_bcs() != 39685) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_func_zk_login_public_identifier_to_json() != 12096) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_address_to_bytes() != 57710) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_address_to_canonical_string() != 50168) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_address_to_hex() != 22032) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_address_to_short_string() != 56908) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_argument_get_nested_result() != 53358) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_bls12381privatekey_public_key() != 53765) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_bls12381privatekey_scheme() != 8293) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_bls12381privatekey_sign_checkpoint_summary() != 1487) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_bls12381privatekey_try_sign() != 59341) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_bls12381privatekey_verifying_key() != 36438) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_bls12381publickey_to_bytes() != 9890) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_bls12381signature_to_bytes() != 56969) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_bls12381verifyingkey_public_key() != 59353) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_bls12381verifyingkey_verify() != 54718) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_bn254fieldelement_padded() != 44301) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_bn254fieldelement_unpadded() != 33350) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_cancelledtransaction_digest() != 52811) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_cancelledtransaction_version_assignments() != 52539) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepoch_computation_charge() != 25355) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepoch_epoch() != 49990) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepoch_epoch_start_timestamp_ms() != 57669) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepoch_non_refundable_storage_fee() != 28070) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepoch_protocol_version() != 40406) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepoch_storage_charge() != 35870) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepoch_storage_rebate() != 21786) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepoch_system_packages() != 55002) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepochv2_computation_charge() != 4379) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepochv2_computation_charge_burned() != 17712) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepochv2_epoch() != 52992) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepochv2_epoch_start_timestamp_ms() != 35398) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepochv2_non_refundable_storage_fee() != 38234) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepochv2_protocol_version() != 16414) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepochv2_storage_charge() != 4751) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepochv2_storage_rebate() != 52102) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepochv2_system_packages() != 48705) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepochv3_computation_charge() != 29832) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepochv3_computation_charge_burned() != 40461) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepochv3_eligible_active_validators() != 6336) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepochv3_epoch() != 4081) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepochv3_epoch_start_timestamp_ms() != 22703) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepochv3_non_refundable_storage_fee() != 5224) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepochv3_protocol_version() != 48266) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepochv3_storage_charge() != 7690) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepochv3_storage_rebate() != 54084) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepochv3_system_packages() != 38309) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepochv4_adjust_rewards_by_score() != 4486) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepochv4_computation_charge() != 1611) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepochv4_computation_charge_burned() != 43788) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepochv4_eligible_active_validators() != 8119) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepochv4_epoch() != 51880) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepochv4_epoch_start_timestamp_ms() != 44908) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepochv4_non_refundable_storage_fee() != 53698) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepochv4_protocol_version() != 53730) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepochv4_scores() != 33143) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepochv4_storage_charge() != 40410) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepochv4_storage_rebate() != 15234) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_changeepochv4_system_packages() != 2075) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_checkpointcommitment_as_ecmh_live_object_set_digest() != 41616) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_checkpointcommitment_is_ecmh_live_object_set() != 22589) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_checkpointcontents_digest() != 22345) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_checkpointcontents_transaction_info() != 56465) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_checkpointsummary_checkpoint_commitments() != 61600) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_checkpointsummary_content_digest() != 31627) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_checkpointsummary_digest() != 14291) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_checkpointsummary_end_of_epoch_data() != 49930) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_checkpointsummary_epoch() != 35840) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_checkpointsummary_epoch_rolling_gas_cost_summary() != 10205) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_checkpointsummary_network_total_transactions() != 50558) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_checkpointsummary_previous_digest() != 933) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_checkpointsummary_sequence_number() != 33896) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_checkpointsummary_signing_message() != 59962) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_checkpointsummary_signing_message_hex() != 52548) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_checkpointsummary_timestamp_ms() != 62474) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_checkpointsummary_version_specific_data() != 43828) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_checkpointtransactioninfo_effects() != 54822) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_checkpointtransactioninfo_signatures() != 36925) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_checkpointtransactioninfo_transaction() != 58570) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_clienttransactionbuilder_dry_run() != 10983) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_clienttransactionbuilder_execute() != 58102) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_clienttransactionbuilder_execute_with_sponsor() != 8183) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_clienttransactionbuilder_expiration() != 32958) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_clienttransactionbuilder_finish() != 56677) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_clienttransactionbuilder_gas() != 522) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_clienttransactionbuilder_gas_budget() != 27427) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_clienttransactionbuilder_gas_price() != 56003) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_clienttransactionbuilder_gas_station_sponsor() != 25572) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_clienttransactionbuilder_make_move_vec() != 4858) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_clienttransactionbuilder_merge_coins() != 43478) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_clienttransactionbuilder_move_call() != 13617) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_clienttransactionbuilder_publish() != 25909) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_clienttransactionbuilder_send_coins() != 65187) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_clienttransactionbuilder_send_iota() != 65011) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_clienttransactionbuilder_set_sender() != 20194) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_clienttransactionbuilder_split_coins() != 2932) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_clienttransactionbuilder_sponsor() != 44990) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_clienttransactionbuilder_stake() != 9961) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_clienttransactionbuilder_transfer_objects() != 23790) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_clienttransactionbuilder_unstake() != 40181) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_clienttransactionbuilder_upgrade() != 59977) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_coin_balance() != 29928) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_coin_coin_type() != 18211) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_coin_id() != 40013) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_consensuscommitprologuev1_commit_timestamp_ms() != 14198) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_consensuscommitprologuev1_consensus_commit_digest() != 34585) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_consensuscommitprologuev1_consensus_determined_version_assignments() != 32713) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_consensuscommitprologuev1_epoch() != 1832) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_consensuscommitprologuev1_round() != 6355) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_consensuscommitprologuev1_sub_dag_index() != 56426) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_consensusdeterminedversionassignments_as_cancelled_transactions() != 59888) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_consensusdeterminedversionassignments_is_cancelled_transactions() != 10241) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_digest_next_lexicographical() != 53914) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_digest_to_base58() != 54638) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_digest_to_bytes() != 14244) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_ed25519privatekey_public_key() != 55389) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_ed25519privatekey_scheme() != 8128) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_ed25519privatekey_sign_personal_message() != 59851) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_ed25519privatekey_sign_transaction() != 4951) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_ed25519privatekey_to_bech32() != 64514) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_ed25519privatekey_to_bytes() != 26261) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_ed25519privatekey_to_der() != 61433) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_ed25519privatekey_to_pem() != 34166) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_ed25519privatekey_try_sign() != 39795) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_ed25519privatekey_try_sign_simple() != 56024) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_ed25519privatekey_try_sign_user() != 42020) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_ed25519privatekey_verifying_key() != 59162) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_ed25519publickey_derive_address() != 37757) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_ed25519publickey_scheme() != 3041) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_ed25519publickey_to_bytes() != 16656) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_ed25519publickey_to_flagged_bytes() != 62359) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_ed25519signature_to_bytes() != 31911) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_ed25519verifier_verify_simple() != 6579) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_ed25519verifier_verify_user() != 16895) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_ed25519verifyingkey_public_key() != 55026) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_ed25519verifyingkey_to_der() != 56779) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_ed25519verifyingkey_to_pem() != 56327) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_ed25519verifyingkey_verify() != 24673) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_ed25519verifyingkey_verify_simple() != 29563) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_ed25519verifyingkey_verify_user() != 43622) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_executiontimeobservation_key() != 10295) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_executiontimeobservation_observations() != 58594) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_faucetclient_request() != 13326) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_faucetclient_request_and_wait() != 25235) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_faucetclient_request_and_wait_for_finalized() != 39496) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_faucetclient_request_status() != 48258) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_genesisobject_data() != 26598) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_genesisobject_object_id() != 9601) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_genesisobject_object_type() != 32731) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_genesisobject_owner() != 50201) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_genesisobject_version() != 36305) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_genesistransaction_events() != 64664) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_genesistransaction_objects() != 14715) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_active_validators() != 29559) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_balance() != 9953) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_chain_id() != 45619) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_checkpoint() != 9094) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_checkpoints() != 36867) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_coin_metadata() != 10872) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_coins() != 47450) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_dry_run_tx() != 7552) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_dry_run_tx_kind() != 37663) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_dynamic_field() != 12346) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_dynamic_fields() != 14745) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_dynamic_object_field() != 28336) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_epoch() != 62805) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_epoch_total_checkpoints() != 29086) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_epoch_total_transaction_blocks() != 61978) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_events() != 20245) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_execute_tx() != 65277) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_gas_coins() != 24826) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_iota_names_default_name() != 53764) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_iota_names_lookup() != 20908) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_iota_names_registrations() != 44467) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_is_tx_finalized() != 8647) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_is_tx_indexed_on_node() != 14842) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_latest_checkpoint_sequence_number() != 40336) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_max_page_size() != 44733) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_move_object_contents() != 40412) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_move_object_contents_bcs() != 49694) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_move_view_call() != 52742) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_move_view_call_json() != 5635) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_normalized_move_function() != 16965) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_normalized_move_module() != 51355) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_object() != 27424) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_object_bcs() != 29653) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_objects() != 14040) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_package() != 7913) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_package_latest() != 55024) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_package_versions() != 34213) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_packages() != 45891) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_protocol_config() != 58559) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_reference_gas_price() != 39065) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_run_query() != 54586) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_service_config() != 11931) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_set_rpc_server() != 63707) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_total_supply() != 21504) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_total_transaction_blocks() != 9583) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_total_transaction_blocks_by_digest() != 24739) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_total_transaction_blocks_by_seq_num() != 18624) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_transaction() != 58857) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_transaction_data_effects() != 53397) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_transaction_effects() != 27010) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_transactions() != 20537) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_transactions_data_effects() != 46218) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_transactions_effects() != 25858) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_graphqlclient_wait_for_tx() != 10761) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_identifier_as_str() != 63815) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_intent_app_id() != 27733) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_intent_scope() != 19072) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_intent_to_bytes() != 31961) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_intent_version() != 64349) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_makemovevector_elements() != 20773) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_makemovevector_type_tag() != 31154) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_mergecoins_coin() != 38884) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_mergecoins_coins_to_merge() != 44350) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_moveauthenticator_address() != 24164) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_moveauthenticator_call_args() != 1111) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_moveauthenticator_object_to_authenticate() != 40516) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_moveauthenticator_type_args() != 63576) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_moveauthenticatorbuilder_finish() != 16948) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_movecall_arguments() != 17202) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_movecall_function() != 2751) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_movecall_module() != 35106) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_movecall_package() != 24481) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_movecall_type_arguments() != 46468) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_movefunction_is_entry() != 5688) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_movefunction_name() != 15389) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_movefunction_parameters() != 34373) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_movefunction_return_type() != 2574) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_movefunction_type_parameters() != 3798) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_movefunction_visibility() != 3892) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_movepackage_id() != 28435) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_movepackage_linkage_table() != 40601) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_movepackage_modules() != 49866) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_movepackage_type_origin_table() != 7308) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_movepackage_version() != 22970) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_movepackagedata_dependencies() != 61113) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_movepackagedata_digest() != 31652) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_movepackagedata_modules() != 63377) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_movepackagedata_to_base64() != 1835) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_movepackagedata_to_json() != 3153) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigaggregatedsignature_bitmap() != 41489) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigaggregatedsignature_committee() != 17432) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigaggregatedsignature_signatures() != 5488) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigaggregator_finish() != 31014) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigaggregator_verifier() != 36902) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigaggregator_with_signature() != 48209) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigaggregator_with_verifier() != 10820) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigcommittee_derive_address() != 26282) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigcommittee_is_valid() != 45468) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigcommittee_members() != 62870) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigcommittee_scheme() != 15458) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigcommittee_threshold() != 21653) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigmember_public_key() != 7804) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigmember_weight() != 57194) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigmemberpublickey_as_ed25519() != 8241) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigmemberpublickey_as_ed25519_opt() != 28021) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigmemberpublickey_as_secp256k1() != 52073) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigmemberpublickey_as_secp256k1_opt() != 40194) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigmemberpublickey_as_secp256r1() != 38170) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigmemberpublickey_as_secp256r1_opt() != 28963) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigmemberpublickey_as_zklogin() != 17714) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigmemberpublickey_as_zklogin_opt() != 23106) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigmemberpublickey_is_ed25519() != 1939) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigmemberpublickey_is_secp256k1() != 49521) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigmemberpublickey_is_secp256r1() != 16265) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigmemberpublickey_is_zklogin() != 37193) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigmemberpublickey_scheme() != 44341) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigmembersignature_as_ed25519() != 22855) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigmembersignature_as_ed25519_opt() != 56690) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigmembersignature_as_secp256k1() != 49085) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigmembersignature_as_secp256k1_opt() != 26984) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigmembersignature_as_secp256r1() != 57510) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigmembersignature_as_secp256r1_opt() != 12419) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigmembersignature_as_zklogin() != 39624) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigmembersignature_as_zklogin_opt() != 34526) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigmembersignature_is_ed25519() != 18913) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigmembersignature_is_secp256k1() != 16841) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigmembersignature_is_secp256r1() != 51171) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigmembersignature_is_zklogin() != 65193) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigverifier_verify() != 49901) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigverifier_with_zklogin_verifier() != 20062) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_multisigverifier_zklogin_verifier() != 5971) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_name_format() != 66) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_name_is_sln() != 9860) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_name_is_subname() != 22382) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_name_label() != 9695) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_name_labels() != 44675) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_name_num_labels() != 62037) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_name_parent() != 40819) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_nameregistration_expiration_timestamp_ms() != 13855) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_nameregistration_id() != 17049) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_nameregistration_name() != 16565) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_nameregistration_name_str() != 19903) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_object_as_package() != 21763) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_object_as_package_opt() != 61571) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_object_as_struct() != 5928) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_object_as_struct_opt() != 49657) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_object_data() != 4330) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_object_digest() != 48655) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_object_object_id() != 6575) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_object_object_ref() != 13587) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_object_object_type() != 1843) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_object_owner() != 3724) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_object_previous_transaction() != 4427) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_object_storage_rebate() != 24969) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_object_version() != 18433) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_objectdata_as_package_opt() != 50334) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_objectdata_as_struct_opt() != 8956) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_objectdata_is_package() != 11147) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_objectdata_is_struct() != 58579) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_objectid_derive_dynamic_child_id() != 47819) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_objectid_to_address() != 21880) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_objectid_to_bytes() != 38367) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_objectid_to_canonical_string() != 62489) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_objectid_to_hex() != 4418) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_objectid_to_short_string() != 63526) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_objecttype_as_struct() != 15094) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_objecttype_as_struct_opt() != 14701) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_objecttype_is_package() != 40585) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_objecttype_is_struct() != 33698) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_owner_as_address() != 19200) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_owner_as_address_opt() != 36265) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_owner_as_object() != 42917) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_owner_as_object_opt() != 17159) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_owner_as_shared() != 56096) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_owner_as_shared_opt() != 4209) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_owner_is_address() != 26982) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_owner_is_immutable() != 23542) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_owner_is_object() != 29892) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_owner_is_shared() != 6506) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_passkeyauthenticator_authenticator_data() != 36642) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_passkeyauthenticator_challenge() != 28147) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_passkeyauthenticator_client_data_json() != 26196) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_passkeyauthenticator_public_key() != 18555) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_passkeyauthenticator_signature() != 5489) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_passkeypublickey_derive_address() != 61803) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_passkeypublickey_inner() != 65008) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_passkeyverifier_verify() != 19101) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_personalmessage_message_bytes() != 17910) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_personalmessage_signing_digest() != 909) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_personalmessage_signing_digest_hex() != 27805) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_programmabletransaction_commands() != 49868) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_programmabletransaction_inputs() != 25458) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_publish_dependencies() != 57311) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_publish_modules() != 26011) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256k1privatekey_public_key() != 27155) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256k1privatekey_scheme() != 60810) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256k1privatekey_sign_personal_message() != 53577) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256k1privatekey_sign_transaction() != 34208) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256k1privatekey_to_bech32() != 60488) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256k1privatekey_to_bytes() != 18583) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256k1privatekey_to_der() != 65507) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256k1privatekey_to_pem() != 12369) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256k1privatekey_try_sign() != 5798) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256k1privatekey_try_sign_simple() != 11597) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256k1privatekey_try_sign_user() != 20597) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256k1privatekey_verifying_key() != 51137) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256k1publickey_derive_address() != 48490) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256k1publickey_scheme() != 60874) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256k1publickey_to_bytes() != 49170) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256k1publickey_to_flagged_bytes() != 46673) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256k1signature_to_bytes() != 49705) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256k1verifier_verify_simple() != 36777) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256k1verifier_verify_user() != 26362) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256k1verifyingkey_public_key() != 56083) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256k1verifyingkey_to_der() != 21325) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256k1verifyingkey_to_pem() != 29137) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256k1verifyingkey_verify() != 27904) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256k1verifyingkey_verify_simple() != 35045) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256k1verifyingkey_verify_user() != 41639) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256r1privatekey_public_key() != 58075) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256r1privatekey_scheme() != 20973) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256r1privatekey_sign_personal_message() != 113) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256r1privatekey_sign_transaction() != 1687) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256r1privatekey_to_bech32() != 4230) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256r1privatekey_to_bytes() != 8648) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256r1privatekey_to_der() != 48507) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256r1privatekey_to_pem() != 34634) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256r1privatekey_try_sign() != 39126) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256r1privatekey_try_sign_simple() != 57038) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256r1privatekey_try_sign_user() != 36924) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256r1privatekey_verifying_key() != 55895) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256r1publickey_derive_address() != 27344) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256r1publickey_scheme() != 48083) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256r1publickey_to_bytes() != 21066) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256r1publickey_to_flagged_bytes() != 14205) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256r1signature_to_bytes() != 64948) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256r1verifier_verify_simple() != 18491) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256r1verifier_verify_user() != 19940) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256r1verifyingkey_public_key() != 35474) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256r1verifyingkey_to_der() != 49763) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256r1verifyingkey_to_pem() != 51401) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256r1verifyingkey_verify() != 32594) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256r1verifyingkey_verify_simple() != 35191) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_secp256r1verifyingkey_verify_user() != 46052) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_simplekeypair_public_key() != 11009) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_simplekeypair_scheme() != 19826) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_simplekeypair_sign_personal_message() != 2940) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_simplekeypair_sign_transaction() != 35221) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_simplekeypair_to_bech32() != 4776) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_simplekeypair_to_bytes() != 1555) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_simplekeypair_to_der() != 22161) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_simplekeypair_to_pem() != 18854) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_simplekeypair_try_sign() != 52266) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_simplekeypair_try_sign_user() != 17485) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_simplekeypair_verifying_key() != 20797) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_simplesignature_ed25519_pub_key() != 36693) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_simplesignature_ed25519_pub_key_opt() != 11858) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_simplesignature_ed25519_sig() != 56126) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_simplesignature_ed25519_sig_opt() != 33862) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_simplesignature_is_ed25519() != 64494) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_simplesignature_is_secp256k1() != 39262) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_simplesignature_is_secp256r1() != 49536) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_simplesignature_scheme() != 30423) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_simplesignature_secp256k1_pub_key() != 51778) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_simplesignature_secp256k1_pub_key_opt() != 20475) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_simplesignature_secp256k1_sig() != 36141) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_simplesignature_secp256k1_sig_opt() != 16111) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_simplesignature_secp256r1_pub_key() != 25197) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_simplesignature_secp256r1_pub_key_opt() != 22487) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_simplesignature_secp256r1_sig() != 30390) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_simplesignature_secp256r1_sig_opt() != 51961) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_simplesignature_to_bytes() != 28081) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_simpleverifier_verify() != 8441) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_simpleverifyingkey_public_key() != 58667) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_simpleverifyingkey_scheme() != 7296) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_simpleverifyingkey_to_der() != 3936) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_simpleverifyingkey_to_pem() != 55066) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_simpleverifyingkey_verify() != 22348) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_splitcoins_amounts() != 10377) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_splitcoins_coin() != 17278) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_structtag_address() != 20393) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_structtag_coin_type() != 37745) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_structtag_coin_type_opt() != 65306) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_structtag_module() != 28022) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_structtag_name() != 62865) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_structtag_to_canonical_string() != 21212) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_structtag_type_args() != 48594) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_systempackage_dependencies() != 25411) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_systempackage_modules() != 23597) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_systempackage_version() != 39738) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transaction_as_v1() != 53004) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transaction_digest() != 52429) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transaction_expiration() != 47752) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transaction_gas_payment() != 5316) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transaction_kind() != 49492) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transaction_sender() != 38190) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transaction_signing_digest() != 21125) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transaction_signing_digest_hex() != 44484) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transaction_to_base64() != 51030) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transactionbuilder_execute_with_gas_station() != 6441) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transactionbuilder_expiration() != 5328) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transactionbuilder_finish() != 39582) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transactionbuilder_gas() != 23371) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transactionbuilder_gas_budget() != 48686) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transactionbuilder_gas_price() != 7437) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transactionbuilder_gas_station_sponsor() != 41106) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transactionbuilder_make_move_vec() != 64922) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transactionbuilder_merge_coins() != 136) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transactionbuilder_move_call() != 22281) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transactionbuilder_publish() != 22805) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transactionbuilder_send_coins() != 6220) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transactionbuilder_send_iota() != 2185) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transactionbuilder_set_sender() != 37952) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transactionbuilder_split_coins() != 17747) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transactionbuilder_sponsor() != 25655) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transactionbuilder_stake() != 41361) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transactionbuilder_transfer_objects() != 16313) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transactionbuilder_unstake() != 30530) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transactionbuilder_upgrade() != 14493) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transactionbuilder_with_client() != 26579) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transactioneffects_as_v1() != 48710) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transactioneffects_digest() != 46963) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transactioneffects_is_v1() != 39808) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transactionevents_digest() != 55750) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transactionevents_events() != 36651) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transactionsigner_sign() != 29986) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transactionsignerfn_sign() != 56946) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transactionv1_digest() != 52708) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transactionv1_expiration() != 9317) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transactionv1_gas_payment() != 61676) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transactionv1_kind() != 56302) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transactionv1_sender() != 8513) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transactionv1_signing_digest() != 51695) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transactionv1_signing_digest_hex() != 21009) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transactionv1_to_base64() != 54846) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transferobjects_address() != 37833) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_transferobjects_objects() != 24154) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_typetag_as_struct_tag() != 1715) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_typetag_as_struct_tag_opt() != 15734) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_typetag_as_vector_type_tag() != 20180) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_typetag_as_vector_type_tag_opt() != 55130) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_typetag_is_address() != 38219) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_typetag_is_bool() != 30264) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_typetag_is_signer() != 57678) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_typetag_is_struct() != 39029) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_typetag_is_u128() != 65460) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_typetag_is_u16() != 34540) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_typetag_is_u256() != 65130) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_typetag_is_u32() != 40795) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_typetag_is_u64() != 28705) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_typetag_is_u8() != 18761) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_typetag_is_vector() != 49992) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_typetag_to_canonical_string() != 24741) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_upgrade_dependencies() != 7113) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_upgrade_modules() != 62138) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_upgrade_package() != 35757) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_upgrade_ticket() != 11416) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_upgradepolicy_as_u8() != 10203) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_usersignature_as_move_authenticator() != 62044) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_usersignature_as_move_authenticator_opt() != 31940) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_usersignature_as_multisig() != 36332) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_usersignature_as_multisig_opt() != 21895) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_usersignature_as_passkey_authenticator() != 38663) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_usersignature_as_passkey_authenticator_opt() != 5213) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_usersignature_as_simple() != 57455) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_usersignature_as_simple_opt() != 47248) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_usersignature_as_zklogin_authenticator() != 64789) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_usersignature_as_zklogin_authenticator_opt() != 62080) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_usersignature_is_move_authenticator() != 59771) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_usersignature_is_multisig() != 61839) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_usersignature_is_passkey_authenticator() != 7851) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_usersignature_is_simple() != 58211) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_usersignature_is_zklogin_authenticator() != 35546) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_usersignature_scheme() != 25381) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_usersignature_to_base64() != 33757) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_usersignature_to_bytes() != 58893) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_usersignatureverifier_verify() != 47797) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_usersignatureverifier_with_zklogin_verifier() != 44658) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_usersignatureverifier_zklogin_verifier() != 9821) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_validatoraggregatedsignature_bitmap_bytes() != 59039) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_validatoraggregatedsignature_epoch() != 54283) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_validatoraggregatedsignature_signature() != 39125) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_validatorcommitteesignatureaggregator_add_signature() != 13923) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_validatorcommitteesignatureaggregator_committee() != 36159) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_validatorcommitteesignatureaggregator_finish() != 7324) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_validatorcommitteesignatureverifier_committee() != 5093) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_validatorcommitteesignatureverifier_verify() != 29238) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_validatorcommitteesignatureverifier_verify_aggregated() != 46271) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_validatorcommitteesignatureverifier_verify_checkpoint_summary() != 36331) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_validatorexecutiontimeobservation_duration() != 59803) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_validatorexecutiontimeobservation_validator() != 10003) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_validatorsignature_epoch() != 15301) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_validatorsignature_public_key() != 16384) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_validatorsignature_signature() != 58273) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_versionassignment_object_id() != 50440) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_versionassignment_version() != 51219) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_zkloginauthenticator_inputs() != 1512) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_zkloginauthenticator_max_epoch() != 9769) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_zkloginauthenticator_signature() != 18838) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_zklogininputs_address_seed() != 4892) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_zklogininputs_header_base64() != 32056) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_zklogininputs_iss() != 1099) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_zklogininputs_iss_base64_details() != 20914) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_zklogininputs_jwk_id() != 37580) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_zklogininputs_proof_points() != 28172) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_zklogininputs_public_identifier() != 48158) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_zkloginproof_a() != 6891) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_zkloginproof_b() != 36477) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_zkloginproof_c() != 10897) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_zkloginpublicidentifier_address_seed() != 3936) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_zkloginpublicidentifier_derive_address() != 14353) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_zkloginpublicidentifier_derive_address_padded() != 45141) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_zkloginpublicidentifier_derive_address_unpadded() != 51424) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_zkloginpublicidentifier_iss() != 58864) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_zkloginverifier_jwks() != 62366) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_zkloginverifier_verify() != 29967) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_method_zkloginverifier_with_jwks() != 49665) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_address_framework() != 52951) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_address_from_bytes() != 58901) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_address_from_hex() != 38044) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_address_generate() != 48865) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_address_std() != 28998) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_address_system() != 4297) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_address_zero() != 46553) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_argument_new_gas() != 14489) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_argument_new_input() != 33966) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_argument_new_nested_result() != 57666) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_argument_new_result() != 44025) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_bls12381privatekey_generate() != 14780) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_bls12381privatekey_new() != 52467) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_bls12381publickey_from_bytes() != 6069) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_bls12381publickey_from_str() != 26128) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_bls12381publickey_generate() != 30791) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_bls12381signature_from_bytes() != 42745) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_bls12381signature_from_str() != 5412) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_bls12381signature_generate() != 58435) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_bls12381verifyingkey_new() != 22402) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_bn254fieldelement_from_bytes() != 3672) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_bn254fieldelement_from_str() != 21214) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_bn254fieldelement_from_str_radix_10() != 17556) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_cancelledtransaction_new() != 59199) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_changeepoch_new() != 48694) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_changeepochv2_new() != 52433) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_changeepochv3_new() != 2388) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_changeepochv4_new() != 38653) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_checkpointcontents_new() != 27130) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_checkpointsummary_new() != 16062) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_checkpointtransactioninfo_new() != 65327) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_circomg1_new() != 39786) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_circomg2_new() != 50489) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_coin_try_from_object() != 35349) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_command_new_make_move_vector() != 54610) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_command_new_merge_coins() != 1888) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_command_new_move_call() != 23161) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_command_new_publish() != 7239) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_command_new_split_coins() != 59484) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_command_new_transfer_objects() != 54265) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_command_new_upgrade() != 48835) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_consensuscommitprologuev1_new() != 50376) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_consensusdeterminedversionassignments_new_cancelled_transactions() != 929) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_digest_from_base58() != 41234) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_digest_from_bytes() != 65530) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_digest_generate() != 8094) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ed25519privatekey_from_bech32() != 16842) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ed25519privatekey_from_der() != 42838) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ed25519privatekey_from_mnemonic() != 55789) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ed25519privatekey_from_mnemonic_with_path() != 15255) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ed25519privatekey_from_pem() != 53776) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ed25519privatekey_generate() != 53932) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ed25519privatekey_new() != 12862) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ed25519publickey_from_bytes() != 60403) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ed25519publickey_from_str() != 38751) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ed25519publickey_generate() != 46412) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ed25519signature_from_bytes() != 61841) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ed25519signature_from_str() != 39607) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ed25519signature_generate() != 41607) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ed25519verifier_new() != 6910) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ed25519verifyingkey_from_der() != 1677) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ed25519verifyingkey_from_pem() != 37214) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ed25519verifyingkey_new() != 23280) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_endofepochtransactionkind_new_authenticator_state_create() != 42248) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_endofepochtransactionkind_new_authenticator_state_expire() != 58811) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_endofepochtransactionkind_new_change_epoch() != 56235) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_endofepochtransactionkind_new_change_epoch_v2() != 13653) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_endofepochtransactionkind_new_change_epoch_v3() != 21107) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_endofepochtransactionkind_new_change_epoch_v4() != 45170) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_executiontimeobservation_new() != 22119) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_executiontimeobservationkey_new_make_move_vec() != 1498) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_executiontimeobservationkey_new_merge_coins() != 40848) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_executiontimeobservationkey_new_move_entry_point() != 6711) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_executiontimeobservationkey_new_publish() != 6398) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_executiontimeobservationkey_new_split_coins() != 28564) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_executiontimeobservationkey_new_transfer_objects() != 29560) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_executiontimeobservationkey_new_upgrade() != 26115) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_executiontimeobservations_new_v1() != 19098) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_faucetclient_new() != 15001) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_faucetclient_new_devnet() != 41429) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_faucetclient_new_localnet() != 53173) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_faucetclient_new_testnet() != 11124) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_genesisobject_new() != 35390) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_genesistransaction_new() != 47990) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_graphqlclient_new() != 32097) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_graphqlclient_new_devnet() != 6494) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_graphqlclient_new_localnet() != 2330) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_graphqlclient_new_mainnet() != 3613) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_graphqlclient_new_testnet() != 48529) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_identifier_new() != 9398) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_input_new_immutable_or_owned() != 33908) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_input_new_pure() != 53404) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_input_new_receiving() != 28060) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_input_new_shared() != 61970) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_intent_from_bytes() != 49795) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_intent_from_hex() != 14259) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_intent_new() != 25063) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_intent_new_consensus_app() != 33296) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_intent_new_iota_app() != 43238) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_intent_new_iota_transaction() != 43817) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_intent_new_personal_message() != 5450) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_makemovevector_new() != 20934) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_mergecoins_new() != 1506) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_movearg_address() != 46522) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_movearg_address_from_hex() != 44452) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_movearg_address_vec() != 6097) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_movearg_address_vec_from_hex() != 4963) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_movearg_bool() != 52909) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_movearg_bool_vec() != 25067) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_movearg_digest() != 60114) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_movearg_digest_from_base58() != 42108) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_movearg_digest_vec() != 42012) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_movearg_digest_vec_from_base58() != 36057) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_movearg_option() != 48206) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_movearg_string() != 18343) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_movearg_string_vec() != 22295) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_movearg_u128() != 8902) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_movearg_u128_vec() != 64018) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_movearg_u16() != 62318) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_movearg_u16_vec() != 29254) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_movearg_u256() != 32008) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_movearg_u256_vec() != 50179) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_movearg_u32() != 16255) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_movearg_u32_vec() != 41122) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_movearg_u64() != 7097) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_movearg_u64_vec() != 17684) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_movearg_u8() != 10135) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_movearg_u8_vec() != 4587) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_moveauthenticator_new_immutable() != 19047) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_moveauthenticator_new_shared() != 5389) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_moveauthenticatorbuilder_new() != 1961) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_movecall_new() != 30411) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_movepackage_new() != 17506) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_movepackagedata_from_base64() != 61420) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_movepackagedata_from_json() != 13174) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_movepackagedata_new() != 65225) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_moveviewarg_address() != 16142) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_moveviewarg_bool() != 22335) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_moveviewarg_json() != 28379) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_moveviewarg_null() != 64193) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_moveviewarg_object_id() != 5941) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_moveviewarg_option() != 51508) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_moveviewarg_string() != 21544) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_moveviewarg_string_vec() != 46543) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_moveviewarg_u128() != 54996) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_moveviewarg_u16() != 8159) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_moveviewarg_u32() != 57064) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_moveviewarg_u64() != 23977) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_moveviewarg_u8() != 23834) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_moveviewarg_u8_vec() != 19629) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_multisigaggregatedsignature_new() != 3396) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_multisigaggregator_new_with_message() != 41388) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_multisigaggregator_new_with_transaction() != 27644) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_multisigcommittee_new() != 40069) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_multisigmember_new() != 63622) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_multisigverifier_new() != 53197) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_name_from_str() != 30248) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_nameregistration_new() != 19327) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_object_new() != 41346) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_objectdata_new_move_package() != 5274) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_objectdata_new_move_struct() != 1861) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_objectid_clock() != 14732) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_objectid_derive_id() != 16970) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_objectid_from_bytes() != 41789) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_objectid_from_hex() != 30954) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_objectid_system() != 9600) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_objectid_zero() != 40526) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_objecttype_new_package() != 63533) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_objecttype_new_struct() != 65488) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_owner_new_address() != 6008) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_owner_new_immutable() != 51786) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_owner_new_object() != 381) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_owner_new_shared() != 36753) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ptbargument_address() != 14619) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ptbargument_address_from_hex() != 40759) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ptbargument_address_vec() != 326) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ptbargument_address_vec_from_hex() != 60030) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ptbargument_assigned() != 10192) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ptbargument_bool() != 51030) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ptbargument_bool_vec() != 65126) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ptbargument_digest() != 54344) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ptbargument_digest_from_base58() != 5017) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ptbargument_digest_vec() != 19113) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ptbargument_digest_vec_from_base58() != 59134) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ptbargument_gas() != 10767) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ptbargument_move_arg() != 26972) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ptbargument_object_id() != 41681) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ptbargument_object_id_from_hex() != 47640) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ptbargument_object_ref() != 24215) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ptbargument_option() != 37559) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ptbargument_receiving() != 50553) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ptbargument_receiving_from_hex() != 48453) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ptbargument_shared() != 59619) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ptbargument_shared_from_hex() != 60985) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ptbargument_shared_mut() != 43242) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ptbargument_shared_mut_from_hex() != 52415) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ptbargument_string() != 60971) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ptbargument_u128() != 47870) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ptbargument_u128_vec() != 37355) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ptbargument_u16() != 58656) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ptbargument_u16_vec() != 10787) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ptbargument_u256() != 19985) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ptbargument_u256_vec() != 57693) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ptbargument_u32() != 13754) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ptbargument_u32_vec() != 50917) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ptbargument_u64() != 6870) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ptbargument_u64_vec() != 27400) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ptbargument_u8() != 22414) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_ptbargument_u8_vec() != 51245) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_passkeypublickey_new() != 30856) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_passkeyverifier_new() != 23457) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_personalmessage_new() != 17579) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_programmabletransaction_new() != 38638) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_publish_new() != 4785) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_secp256k1privatekey_from_bech32() != 34529) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_secp256k1privatekey_from_der() != 45448) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_secp256k1privatekey_from_mnemonic() != 33082) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_secp256k1privatekey_from_mnemonic_with_path() != 7431) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_secp256k1privatekey_from_pem() != 20937) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_secp256k1privatekey_generate() != 49496) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_secp256k1privatekey_new() != 35513) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_secp256k1publickey_from_bytes() != 20339) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_secp256k1publickey_from_str() != 24158) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_secp256k1publickey_generate() != 36411) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_secp256k1signature_from_bytes() != 36237) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_secp256k1signature_from_str() != 16397) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_secp256k1signature_generate() != 63087) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_secp256k1verifier_new() != 59813) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_secp256k1verifyingkey_from_der() != 40127) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_secp256k1verifyingkey_from_pem() != 40573) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_secp256k1verifyingkey_new() != 16080) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_secp256r1privatekey_from_bech32() != 7016) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_secp256r1privatekey_from_der() != 63595) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_secp256r1privatekey_from_mnemonic() != 57849) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_secp256r1privatekey_from_mnemonic_with_path() != 7709) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_secp256r1privatekey_from_pem() != 28166) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_secp256r1privatekey_generate() != 47736) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_secp256r1privatekey_new() != 32825) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_secp256r1publickey_from_bytes() != 60002) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_secp256r1publickey_from_str() != 27991) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_secp256r1publickey_generate() != 49992) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_secp256r1signature_from_bytes() != 8469) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_secp256r1signature_from_str() != 15312) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_secp256r1signature_generate() != 40260) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_secp256r1verifier_new() != 59881) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_secp256r1verifyingkey_from_der() != 6292) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_secp256r1verifyingkey_from_pem() != 20421) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_secp256r1verifyingkey_new() != 57317) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_simplekeypair_from_bech32() != 51811) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_simplekeypair_from_bytes() != 9299) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_simplekeypair_from_der() != 24923) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_simplekeypair_from_ed25519() != 22142) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_simplekeypair_from_pem() != 2041) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_simplekeypair_from_secp256k1() != 46546) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_simplekeypair_from_secp256r1() != 13117) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_simplesignature_new_ed25519() != 65185) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_simplesignature_new_secp256k1() != 56524) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_simplesignature_new_secp256r1() != 19953) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_simpleverifier_new() != 34783) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_simpleverifyingkey_from_der() != 21482) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_simpleverifyingkey_from_pem() != 11192) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_splitcoins_new() != 50321) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_structtag_new() != 61625) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_structtag_new_ascii_string() != 60972) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_structtag_new_balance() != 10874) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_structtag_new_clock() != 18419) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_structtag_new_coin() != 38232) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_structtag_new_coin_manager() != 16973) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_structtag_new_coin_metadata() != 56669) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_structtag_new_config() != 33902) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_structtag_new_config_setting() != 18224) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_structtag_new_deny_list_address_key() != 6409) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_structtag_new_deny_list_config_key() != 3059) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_structtag_new_deny_list_global_pause_key() != 36813) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_structtag_new_display_created() != 24465) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_structtag_new_dynamic_object_field_wrapper() != 48905) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_structtag_new_field() != 4196) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_structtag_new_gas_coin() != 5754) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_structtag_new_id() != 62017) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_structtag_new_iota_coin_type() != 44499) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_structtag_new_iota_system_admin_cap() != 5595) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_structtag_new_iota_system_state() != 29781) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_structtag_new_iota_treasury_cap() != 59282) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_structtag_new_name() != 26361) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_structtag_new_staked_iota() != 60970) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_structtag_new_string() != 47333) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_structtag_new_system_epoch_info_event() != 36154) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_structtag_new_time_lock() != 38402) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_structtag_new_timelocked_staked_iota() != 46742) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_structtag_new_transfer_receiving() != 40850) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_structtag_new_treasury_cap() != 2523) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_structtag_new_uid() != 54533) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_structtag_new_upgrade_cap() != 6365) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_structtag_new_upgrade_receipt() != 44298) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_structtag_new_upgrade_ticket() != 43936) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_structtag_new_version_updated() != 40840) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_systempackage_new() != 25070) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_transaction_from_base64() != 30255) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_transaction_new_v1() != 58632) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_transactionbuilder_new() != 35216) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_transactioneffects_new_v1() != 63561) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_transactionevents_new() != 1310) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_transactionkind_new_authenticator_state_update_v1() != 29264) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_transactionkind_new_consensus_commit_prologue_v1() != 27756) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_transactionkind_new_end_of_epoch() != 44556) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_transactionkind_new_genesis() != 45541) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_transactionkind_new_programmable_transaction() != 9153) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_transactionkind_new_randomness_state_update() != 37051) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_transactionsigner_from_ed25519() != 15863) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_transactionsigner_from_keypair() != 13858) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_transactionsigner_from_move_authenticator() != 17379) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_transactionsigner_from_secp256k1() != 54758) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_transactionsigner_from_secp256r1() != 17667) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_transactionsigner_new() != 8178) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_transactionv1_from_base64() != 19681) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_transactionv1_new() != 17484) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_transferobjects_new() != 22470) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_typetag_new_address() != 65087) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_typetag_new_bool() != 404) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_typetag_new_signer() != 49791) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_typetag_new_struct() != 40686) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_typetag_new_u128() != 24239) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_typetag_new_u16() != 14922) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_typetag_new_u256() != 41658) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_typetag_new_u32() != 59185) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_typetag_new_u64() != 29045) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_typetag_new_u8() != 55184) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_typetag_new_vector() != 2453) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_upgrade_new() != 61663) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_upgradepolicy_additive() != 63459) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_upgradepolicy_compatible() != 63292) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_upgradepolicy_dep_only() != 44371) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_usersignature_from_base64() != 8029) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_usersignature_from_bytes() != 37499) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_usersignature_new_move_authenticator() != 7262) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_usersignature_new_multisig() != 39922) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_usersignature_new_passkey_authenticator() != 45324) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_usersignature_new_simple() != 31310) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_usersignature_new_zklogin_authenticator() != 30659) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_usersignatureverifier_new() != 32322) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_validatoraggregatedsignature_new() != 15846) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_validatorcommitteesignatureaggregator_new_checkpoint_summary() != 25823) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_validatorcommitteesignatureverifier_new() != 17424) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_validatorexecutiontimeobservation_new() != 47546) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_validatorsignature_new() != 2599) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_versionassignment_new() != 14186) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_zkloginauthenticator_new() != 32812) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_zklogininputs_new() != 48962) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_zkloginproof_new() != 19950) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_zkloginpublicidentifier_new() != 53294) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_zkloginverifier_new_dev() != 44446) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_iota_sdk_ffi_checksum_constructor_zkloginverifier_new_mainnet() != 12123) {
        return InitializationResult.apiChecksumMismatch
    }

    uniffiCallbackInitTransactionSignerFn()
    return InitializationResult.ok
}()

// Make the ensure init function public so that other modules which have external type references to
// our types can call it.
public func uniffiEnsureIotaSdkFfiInitialized() {
    switch initializationResult {
    case .ok:
        break
    case .contractVersionMismatch:
        fatalError("UniFFI contract version mismatch: try cleaning and rebuilding your project")
    case .apiChecksumMismatch:
        fatalError("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
}

// swiftlint:enable all